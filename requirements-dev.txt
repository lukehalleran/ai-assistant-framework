# requirements-dev.txt
# Development and testing dependencies for Daemon RAG Agent

# Include all production dependencies
-r requirements.txt

# Testing frameworks
pytest==8.4.2
pytest-asyncio==1.2.0
pytest-cov==7.0.0

# Code quality and linting
# Bug-focused rule floor configured in pyproject.toml [tool.ruff] — enforced in CI.
ruff>=0.14.0
# (Add these if/when needed)
# black>=23.0.0
# mypy>=1.0.0

# Development tools
# ipython>=8.0.0
# jupyter>=1.0.0
