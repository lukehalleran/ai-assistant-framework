# Class-guard completion — execution work log (2026-09-13)

Executes `PLAN_20260913_class_guard_completion.md` in
`/home/lukeh/daemon_exec/class_guards`. Evidence only; no commit, push,
remote-setting change, deploy, or provider call was made by this session.

## Fresh-session setup checks (all passed before any edit)

| Check | Observed |
|---|---|
| Base | HEAD `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` on `master`; `git status --short` empty |
| Planning docs in HEAD | both plans, the CI review, `PROBE_HARNESS_DESIGN.md`, and the five edited docs are all tracked at HEAD (the HEAD commit adds them) |
| Checkout | `origin` fetch `/home/lukeh/Daemon_v1`, push URL `DISABLED` |
| Same base elsewhere (read-only) | `/home/lukeh/daemon_exec/generalization` HEAD `328a8ec…`; `/home/lukeh/Daemon_v1` HEAD `328a8ec…` |
| Interpreter / tools | `python --version` 3.11.8 (pyenv shim); ruff 0.14.9; pytest 9.0.2; gh 2.79.0 authenticated |
| Private files | `.env` absent; `config/config.local.yaml` absent; `.git/hooks/pre-push` absent (git does not clone hooks) |
| `data/` | exists only for 8 tracked files (`git ls-files -- data`). Not traversed. Directory size 126 bytes on btrfs equals 2 × the 63 name bytes of its three tracked entries (`benchmark_per_case.csv`, `embedding_migration_manifest.json`, `pipeline`), so no ignored store is present |
| `.agent_snapshots/` | absent in this clone |
| Other pytest | none running (`pgrep` matched only this session's shell) |
| Plan 2 exchange | `/home/lukeh/daemon_exec/generalization/docs/execution/generalization/` does not exist yet: no responses, signals, or workflow proposals |

## Phase 0 — isolate and inventory

### Scanner registry (`scripts/bug_class_guards/scanners/__init__.py` at base)

| Scanner | Mode | Classes | Inputs at base |
|---|---|---|---|
| `dm01_raw_substring` | gate | BC-01, BC-02 | `common.SOURCE_ROOTS` walk: core, memory, knowledge, utils, gui, api, models, processing, config, scripts, main.py |
| `dm16_config_key_reachability` | report | BC-12, BC-10 | `config/config.yaml`, `config/app_config.py`, optional `config/schema.py`; consumer roots = SOURCE_ROOTS minus config |
| `dm17_apply_without_guard` | gate | BC-37 | `scripts/*.py` (non-recursive) and `tests/**/*.py`; `tests/conftest.py` exempt; one shared file count |
| `dm18_except_returns_empty` | gate | BC-20, BC-47 | memory, knowledge, core/prompt, api |
| `dm29_phrase_append_signature` | report | BC-76 | `CLAUDE_CHANGELOG.md` (gitignored, local-only) |
| `dm31_live_state_default` | gate | BC-78, BC-11, BC-12 | SOURCE_ROOTS walk |
| `catalog` | gate | BC-71 | `docs/BUG_CLASSES.md` |

Walk exclusions at any depth: venv, data, web, node_modules, `__pycache__`,
integration.bak, .git; symlinks skipped.

### Scan at base (`python scripts/check_bug_classes.py scan --root .`, exit 0, 3.4 s)

| Scanner | Mode | Files | Found | Baseline | New | Stale |
|---|---|---:|---:|---:|---:|---:|
| dm01_raw_substring | gate | 393 | 12 | 12 | 0 | 0 |
| dm16_config_key_reachability | report | 392 | 87 | – | – | – |
| dm17_apply_without_guard | gate | 593 | 39 | 39 | 0 | 0 |
| dm18_except_returns_empty | gate | 122 | 79 | 79 | 0 | 0 |
| dm29_phrase_append_signature | report | 0 | 0 | – | – | – |
| dm31_live_state_default | gate | 393 | 3 | 3 | 0 | 0 |
| catalog | gate | 1 | 0 | 0 | 0 | 0 |

In this clone DM-29 processed zero files, exactly as in CI. That zero means
the input was unavailable; it is not a clean result.

`inspect --catalog docs/BUG_CLASSES.md`: 78 entries, 31 detection methods,
14 closure methods, 16 singleton bullets, no diagnostics.

### Baseline bytes

- `config/bug_class_baseline.json`: 28,145 bytes, SHA-256
  `9bb56e6d3f072cce5becd97293aaac93f97f7a0f592b212f9579a5f6dc47a756`, schema 1.
- 133 occurrences, 129 unique four-field keys: DM-01 12, DM-17 39, DM-18 79,
  DM-31 3.
- The four duplicate keys, each with multiplicity 2 (all DM-18):
  `core/prompt/gatherer_knowledge.py` `KnowledgeRetrievalMixin.get_relevant_emails` `return []`;
  `knowledge/semantic_search.py` `SemanticSearchIndex.search` `return []`;
  `memory/llm_fact_extractor.py` `LLMFactExtractor.extract_triples` `return []`;
  `memory/memory_storage.py` `MemoryStorage.store_skill` `return None`.
- Re-rendering the loaded multiset with the base `baseline.render()`
  reproduces the file byte-for-byte, so the legacy occurrence set can be
  proven from records alone.

### Top-level Python inventory (names and direct `*.py` only)

Python-bearing top-level entries in this clone: agent_branch (16 direct),
api, config, `conftest.py`, core, `debug_graph_context.py`, eval (16),
gui, hooks (6 PyInstaller hook files), integrations (1), knowledge,
`main.py`, memory, models, processing, scripts, tests, utils. Of these,
agent_branch, eval, hooks, integrations, `conftest.py` and
`debug_graph_context.py` are in no scanner input. A read-only shallow listing of
`/home/lukeh/Daemon_v1` (names and direct `*.py` counts only; `data`,
`.agent_snapshots`, `.git` not listed) shows the same Python-bearing set.
Its extra local directories hold no direct `*.py`.

### Hook and workflow

- `hooks/pre-push` step 2a runs `python scripts/check_bug_classes.py scan --root .`
  then the isolated harness. Not installed in this clone; the review recorded
  the main checkout's `.git/hooks/pre-push` as a symlink to it.
- `.github/workflows/tests.yml`: events push `master`, `refactor/prompt-modular`;
  pull_request `master`; no paths filter. Jobs `test` (matrix 3.11, check name
  `test (3.11)`; scan step inside it after the full dependency install) and
  `frontend`. No aggregate job.

### GitHub (read-only)

- `branches/master`: `protected=false`, protection `enabled=false`, no required
  checks; `branches/master/protection` → 404 "Branch not protected";
  `rulesets` → `[]`. Repository owner type User, public, default branch master.
- Latest run [34728362771](https://github.com/lukehalleran/ai-assistant-framework/actions/runs/34728362771):
  push to master, head `85435ed3df165ee78943da44168b00816d12255d`, success.
  Check runs `test (3.11)` and `frontend`, both app `github-actions`
  (app id 15368), both success; the bug-class step concluded success.
- Local HEAD `328a8ec` is two commits ahead of that pushed SHA, so no CI
  evidence exists for this base.

### Pre-implementation measurements

- DM-01 module exemption: removing it on the base tree raises DM-01 from 12 to
  24 candidates (core/agentic/gate.py 8, utils/query_checker.py 1,
  utils/tone_detector.py 2, utils/web_search_trigger.py 1).
- Syntax preflight cost: `ast.parse` over the 874 files in SOURCE_ROOTS ∪
  tests takes 1.22 s.
- DM-17 base semantics count any `daemon_guard`/`daemon_running` text as a
  guard. 16 of the 20 `--apply` scripts carry such text; the four without it
  are the legacy baseline rows.

## Phase 1 — red contract tests (run against the base implementation)

The policy file was written first. The base CLI ignores it, so every control
below exercised the base scanner and CLI code. New or extended test files:
`test_policy.py`, `test_coverage.py`, `test_cli_contract.py`,
`test_receipts.py`, `test_enforcement.py`, and contract-v2 classes appended to
`test_scanners.py`. Shared pins and helpers were added to
`fixtures/__init__.py`.

Command (under `systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M`):
`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider --confcutdir=tests/bug_class_guards <the six files>`

Result: **195 failed, 23 errors, 49 passed** (exit 1, 2.9 s).

| File | Pass | Fail | Error | Dominant red reason on the base code |
|---|---:|---:|---:|---|
| test_policy | 6 | 31 | 0 | no `policy_invalid` / `registry_mismatch` code: registry deletion, rename, downgrade and extra scanner all ran on. The 6 passes read only the new policy file itself |
| test_coverage | 0 | 51 | 0 | missing roots and legs gave no leg failure; a syntax error surfaced only as a generic scanner error; no inventory |
| test_cli_contract | 1 | 67 | 0 | no baseline/dispositions validation codes; missing baseline is not an error; partial scope unlabelled; unknown scanner raised SystemExit |
| test_receipts | 0 | 0 | 23 | no `--receipt` / `verify-receipts` |
| test_enforcement | 0 | 27 | 0 | no validator module; the base hook and workflow lack the full-contract commands, receipts and final gate |
| test_scanners (new classes) | 42 old | 19 | 0 | DM-01 import exemption and old prefilter hid all four raw-test controls; DM-17 treated comment/string/import text as a guard (3) and found no late/unreachable/uncalled-guard candidates (3); no leg receipts or anchors |

All 42 base `test_scanners` tests still passed. The 7 new tests that passed
on the base code are preservation or pin checks, not red controls. Six are in
`test_policy.TestPinnedContract`: the policy declares the seven scanners, the
legs and roots, report-only status, the scoped claim and the five guard
paths, and the base registry already matched the pinned IDs, modes and
classes. The seventh is `test_cli_contract.TestReport.test_report_only_findings_never_reach_the_baseline_or_exit`,
whose behavior the base code already had and the plan requires preserved.

## Phase 2 — policy, scanner and coverage implementation

Owned files changed or added: `scripts/check_bug_classes.py`;
`scripts/bug_class_guards/{policy,baseline,dispositions,receipts,enforcement,migration}.py`;
every module under `scripts/bug_class_guards/scanners/`;
`config/bug_class_policy.json` (new); the harness files under
`tests/bug_class_guards/`; `hooks/pre-push`; `.github/workflows/tests.yml`.

- **Evaluation order:** policy → registry parity → shallow inventory (full
  scope only) → syntax preflight over every declared Python input → scanners
  with per-leg, per-root receipts → strict baseline (schema 2) → dispositions
  ledger (schema 1) → two-way ratchet → disposition reconciliation.
- **Exit codes:** 2 means the contract could not be evaluated (usage, policy,
  registry, syntax, read, scanner, baseline or ledger). 1 means it was
  evaluated and failed (NEW/STALE, missing/orphan/changed-source dispositions,
  legacy reconciliation, missing or empty required leg, unclassified Python
  root). 0 means every required gate contract passed.
- **DM-01:** module exemption and lexical prefilter removed; every file is parsed.
- **DM-17:** now requires executed guard evidence. A recognized guard `if`
  (a direct `daemon_running()` call or a module wrapper returning it; the
  body refuses) must structurally precede every apply-consuming statement.
  A try whose body holds only imports is transparent. A call handing the
  args object to a self-guarded module function is covered. Kinds:
  `apply_without_guard`, `apply_guard_unresolved`, and
  `test_data_path_literal` (per line). The data-literal prefilter was removed:
  implicit concatenation escaped it. Only `tests/conftest.py` itself is exempt.
- **DM-18:** candidate anchor is the whole handler; the `except` prefilter is gone.
- **DM-16:** YAML, app_config and every consumer root are required legs;
  schema is optional. Unsupported YAML shapes (anchors, aliases, tags,
  non-empty flow collections, unmatched lines, list-item mappings) are
  `config_yaml_unresolved` candidates.
- **DM-29:** its optional leg reports `unavailable`, never a clean zero.
- **Anchor v2:** `(scanner, path, qualname, kind, SHA-256 of a canonical AST
  rendering)`. The canonical rendering drops positions, contexts and empty
  fields. Each scanner keeps its legacy granularity (per line; per parameter
  for DM-31; per file for DM-17 scripts). Excerpt and span are for humans only.
- **Real-tree scan:** 7.8 s, up from 3.4 s (the syntax preflight and
  parse-everything detectors).

## Phase 3 — the 133 legacy occurrences

- **Migration proposal:** `migrate-baseline` against the legacy bytes taken
  read-only from `git show HEAD:config/bug_class_baseline.json`, in a dev
  shell, not a test. Written to scratch, never into `config/`.
- **Mapping:** one-to-one. All 133 legacy occurrences (129 keys + 4 duplicate
  extras) map, the legacy multiset is equal, nothing is pending, and 19 new
  DM-01 candidates are exposed by contract v2. The proposed baseline has 152
  occurrences over 148 anchors.
- **Review:** all 152 occurrences against source — 80 `product_risk` (10
  request packets), 63 `reviewed_benign`, 9 `uncertain`. Every record is
  `accepted_debt`. See `BASELINE_REVIEW_20260913.md`,
  `baseline_review_20260913.json` and `requests/CGR-20260913-001…010.md`.
- **Pre-write invariants in the build script, all held:** accepted records
  equal proposed baseline anchors as a per-anchor multiset; the ledger's legacy
  history re-renders to SHA-256 `9bb56e6d…` (133 occurrences, 129 keys); every
  bound source hash equals the current file.
- **Committed outputs:** `config/bug_class_baseline.json` SHA-256 `39d836b0…`,
  51,882 bytes, schema 2, 152 rows. `config/bug_class_dispositions.json`
  SHA-256 `8e4e5d3c…`, 204,357 bytes, 152 records, legacy snapshot 133 at
  `9bb56e6d…`.
- **Reviewer signoff (Phase 3 step 5):** the migration is multiplicity
  preserving and bound to source hashes. No historical fingerprint was
  silently rewritten: every legacy key and ordinal is retained in its record.
  Signed by Claude Opus 5 as the executing reviewer. Owner review of the
  uncommitted tree is still required before commit.
- **After the migration:** `scan --root .` exits 0 (DM-01 31/31, DM-17
  39/39, DM-18 79/79, DM-31 3/3, catalog 0; DM-16 report 91 with 4
  unresolved; DM-29 unavailable; coverage 11 of 78 classes, 67 uncovered).
  The harness passes 311/311. `verify-receipts` accepts the real scan and
  harness receipts for HEAD and rejects a different expected SHA.
- **DM-16 bounded triage:** 87 unreached-leaf candidates (unchanged) plus 4
  unresolved plain multi-line scalars in `prompts:`. The first ten samples are
  `daemon.*` and `memory.*` leaves such as `daemon.data_dir` and
  `memory.max_final_memories`. Their names read as legacy or relocated knobs,
  but whether each is dead is a human question. They remain report-only
  candidates, not defects, and are not baseline rows.

## Phase 4 — the five repo-wide guards

The four newly exposed candidates are pre-migration measurements. None of the
five guard changes required a Plan 2 source edit.

| Guard | Change | Live-tree effect | Red evidence vs base guard (controls run against `HEAD` copies in scratch) |
|---|---|---|---|
| `test_no_git_state_in_tests.py` | AST git-call detection (split calls, `cwd=` in any position, `-C`, module root aliases, `os.getcwd()`/`.`, no-cwd reads, shell strings); unparseable files fail; path/line/rule-only messages | prototype over all tests: 0 violations | base missed 7 of 8 new controls (it only caught `cwd=` before the argument list, on the same line) |
| `test_ordered_slice_guard.py` | AST slices (multi-line, expression bounds, reversal); the sort exemption must name the same variable, within 3 lines or among the 3 preceding statements; a same-line duplicate is a second occurrence | +3 slices the regex could not see: `CrossCollectionDeduplicator._load_all_documents` is exempt through the multi-line `items.sort` above it; two new reasoned allowlist entries (`AdaptiveExemplarStore.record` append-only head delete; `MemoryRetriever._gate_memories` priority-ordered fallback); 29/40 entries | base found 0 hits for the multi-line, expression-bound and unrelated-sort controls, and 1 false positive for slice text inside a string |
| `test_budget_meters_rendered_sections.py` | rendered keys from the formatter AST (any quotes, subscript, membership, literal loops, local helper indirection); reasoned exceptions; stale-exception check; TokenManager behavior tests kept | same 31 keys as the regex today (the `_count` helper is uncalled) | base regex extracted none of the 5 spellings |
| `test_tool_wiring_parity.py` | flag consumption by evaluating `DISPATCH_TABLE` predicates against a one-flag probe; no flag served by two rows; no row without a flag; handler hand-off bound against the real signature; both routers driven through a sentinel table (including controller→executor delegation); executors resolved by the real `ActionSpec.resolve_executor` | green | the base router check accepted any function whose source merely contains the string `DISPATCH_TABLE` |
| `test_model_capability_wiring.py` | registry helper (missing/orphan rows, missing/unknown keys, non-bool flags, caching and `forced_top_p` values) with controls; public `ModelManager` classifiers and `detect_protocol` checked for every alias; slug path kept | green | the base key and caching check accepted `{"tools": "yes"}` |

Result under the plan's bounded command: **225 passed** (the base guards had 85).

## Phase 5 — hook and workflow

- `hooks/pre-push`: runs the full scan with a receipt, the harness with JUnit,
  and `verify-receipts`. The five guards run with a JUnit receipt followed by
  guard receipt verification. A missing guard file blocks the push. No
  `|| true` remains: `grep`'s no-match exit is tolerated explicitly as exit 1
  only. `bash -n` is clean.
- `.github/workflows/tests.yml`:
  - new `bug-class-scan` job (stdlib plus `pytest==9.0.2`; scan receipt,
    harness JUnit, artifact upload);
  - `test` job: the old scan step is removed and an explicit five-guard step
    with a JUnit artifact is added;
  - `frontend` is unchanged;
  - new `bug-class-gate` job (`needs` all three, `if: ${{ always() }}`): it
    fails unless every `needs.<job>.result` is `success`, then re-runs
    `verify-receipts` with `--expect-sha "$GITHUB_SHA"`.

  No path filters and no `continue-on-error`; `permissions: contents: read`.
  The YAML parses.
- `test_enforcement.py` pins the literal commands. All 10 hook mutations and
  15 workflow mutations are rejected.
- Plan 2 workflow proposals merged: none existed.
- Branch-protection payload prepared at
  `docs/execution/class_guards/branch_protection_master.json`. It is **not
  applied**; the payload is not evidence of protection.
- GitHub (read-only, unchanged): `master` unprotected, no rulesets. **No CI run
  exists for this tree**; pushing was out of scope.

## Phase 6 — final evidence (tree state at handoff, base `328a8ec`)

| Check | Command | Result |
|---|---|---|
| Catalog | `python scripts/check_bug_classes.py inspect --catalog docs/BUG_CLASSES.md` | 78 / 31 / 14 / 16, no diagnostics |
| Full scan | `GITHUB_SHA=$(git rev-parse HEAD) python scripts/check_bug_classes.py scan --root . --receipt <r>/bug-class-scan.json` | exit 0; gate occurrences 152 matched, 0 new, 0 stale; coverage 11/78 |
| Harness | `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider --confcutdir=tests/bug_class_guards --junitxml=<r>/bug-class-harness.xml tests/bug_class_guards` (6G scope) | 311 passed |
| Five guards | plan's bounded `env … timeout 120s` command plus `--junitxml` (6G scope) | 225 passed |
| Receipts | `verify-receipts --scan … --harness-junit … --guards-junit … --expect-sha $(git rev-parse HEAD)` | OK (and rejects a different SHA) |
| Lint | plan's ruff command on owned paths; `ruff check .` | clean; clean |
| Ownership | `git status --porcelain --untracked-files=all` against the plan's owned list | 51 paths, 0 outside ownership |
| Plan 2 exchange | `…/generalization/docs/execution/generalization/` | only `batches/`; no responses, signals or workflow proposals |

Pinned files at handoff: policy SHA-256 `21e7c189…`, baseline `39d836b0…`,
dispositions `8e4e5d3c…`.

## Privacy pre-commit hook finding (2026-09-13, after the session resumed)

The session was killed at 16:47 when the owner's SSH connection dropped. The
finished tree was intact.

- **How it surfaced.** The owner commits with `~/daemon_checkpoints/plan1_commit.sh`.
  The runner stages exactly the 52 changed files into a scratch index and runs
  the main checkout's `hooks/pre-commit-privacy` (gitleaks plus the local terms
  list) on that tree. The clone has no hooks installed, so the commit line in
  the first completion report would have skipped this hook.
- **Finding.** gitleaks blocked on three `generic-api-key` false positives:
  - `tests/bug_class_guards/test_policy.py:83`
  - `tests/bug_class_guards/test_coverage.py:78`
  - `tests/bug_class_guards/test_coverage.py:149`

  Each line pairs the DM-16 scanner ID, which contains the word "key", with
  another quoted scanner or leg name. No secret is involved.
- **Reproduced here, before the fix.** `hooks/pre-commit-privacy` is
  byte-identical in the clone and the main checkout (SHA-256 `268760e2…`). A
  copy of `.git/index` got the runner's add list staged (52 files), then
  `gitleaks git --pre-commit --redact --staged --verbose` ran (gitleaks
  8.30.1): exit 1, exactly those three findings. The real index was unchanged.
  The terms-list half was not run here: the list is private and the runner
  runs it.
- **Fix.** `  # gitleaks:allow` appended to exactly those three lines, the fix
  the owner validated on a scratch copy. Moving the ID into a constant would
  not help, because the ID itself contains "key". Neither file has baseline or
  disposition rows, and comments do not change scanner anchors.
- **After the fix.** The same gitleaks run exits 0 with no leaks. `scan --root .`
  exits 0 (152 matched, 0 new, 0 stale). The harness passes 311, the five guards
  225. `verify-receipts` accepts the new scan, harness and guard receipts for
  HEAD. ruff is clean on owned paths and the whole tree. The catalog has no
  diagnostics.
- **Completion report.** Owner step 1 is now the runner: `--repin`, then
  `--check`, then a run with no option.
- **Runner timing.** The runner's busy check refuses while any non-shell
  process has its working directory inside the clone. This session's `claude`
  process does, so the runner has to be started after the session exits.
- **Tree state.** Still uncommitted, 52 paths (the 51 recorded above plus the
  completion report), all owned. Nothing was committed or pushed.
- **For later edits.** Any line under these paths that puts the DM-16 scanner
  ID beside another quoted name of ten or more characters can trigger the same
  gitleaks rule. Re-run the hook's gitleaks command before re-pinning.
