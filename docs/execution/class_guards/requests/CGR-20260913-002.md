# CGR-20260913-002 — Agentic gate keyword tests outside utils.trigger_match

Immutable class-guard request, written 2026-09-13 by the class-guard owner
under `PLAN_20260913_class_guard_completion.md` (see "Exclusive ownership
and execution boundary"). Respond with one immutable file in Plan 2's
checkout: `docs/execution/generalization/class_guard_responses/CGR-20260913-002.md`.
Follow-ups go in `-2`, `-3`, and so on. Do not edit this packet, the
baseline, the dispositions ledger, the catalog, or `.github/**`.

| Field | Value |
|---|---|
| Scanner | dm01_raw_substring (gate; BC-01, BC-02) |
| Base tree | `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` |
| Occurrences | 7 across 1 file(s) |
| Severity (review judgment) | high: gate routing decides multi-second agentic loops (BC-06) |
| Source paths (Plan 2 owned) | `core/agentic/gate.py` |

## Observed shape and semantic risk

`evaluate_agentic_gate` matches CONTINUATION_PHRASES (four sites), email-intent verbs, SEARCH_SIGNAL_WORDS and EXPLICIT_SEARCH_KEYWORDS with raw `in` against the lowered user text, so a phrase fires inside a longer unrelated word and negation is ignored — the mechanism of the 'how' ⊂ 'shower' and 'solve' ⊂ 'resolution' gate incidents. The module already imports the chokepoint elsewhere; contract v1 exempted the whole module on that import, which is why these sites were never reported before 2026-09-13.

This is a bounded structural review, not a reproduced runtime failure. If
the source owner shows a site is not an instance of the class, the response
should say so with deployed-function evidence. The class-guard owner then
decides between a detector change and continued accepted debt.

## Requested remedy (the source owner owns the design)

Compile each vocabulary through `utils.trigger_match` (word-boundary matching; `has_non_negated_hit` where a cue is a request) and use the compiled matcher at every listed site. Keep URL-scheme detection (`'http://' in _lower`) as is; it is recorded separately as reviewed benign.

## Expected deployed-function regression tests

Deployed `evaluate_agentic_gate` outcome fixtures, each in clean and wrapped/indented form: a continuation phrase contained inside a longer word does not count as an affirmation; 'sender'/'rewrite' do not satisfy the email-intent verbs; a search signal word inside an unrelated word does not defeat the casual-skip filter; negated explicit search keywords do not grant the veto exemption. Paired positive controls for each.

## Response contents

Request ID; owning batch ID; each changed file's SHA-256 before and after;
changed files; the focused test command with its exit code and counts; any
site left unfixed, with the reason; limitations.

## Class-guard acceptance

The request stays open and its occurrences stay `accepted_debt` until the
owner integrates the fix into a tree this checkout can scan. Then
`python scripts/check_bug_classes.py scan --root .` must stop reporting the
anchors below; they show up as STALE. The class-guard owner removes those
rows and marks their records `confirmed_fixed` with this request ID. A
partial fix closes only the anchors that disappear. An edited candidate that
still matches gets a new anchor and is re-reviewed. Any edit to a listed file
makes the scan fail with `disposition_source_changed` until the class-guard
owner re-reviews the remaining debt. If that happens before the fix is
complete, record a pending integration signal.

## Candidate anchors

| Review # | Path | Symbol | Kind | Digest | Ordinal | Line | Legacy key | Source SHA-256 | Note |
|---:|---|---|---|---|---:|---:|---|---|---|
| 3 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `c73ff08563691b1b21228cff68366b238f74ec19993cba6bf0f77e6725a68ee4` | 1 | 724 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | CONTINUATION_PHRASES affirmation after a tone-deferral. |
| 4 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `c73ff08563691b1b21228cff68366b238f74ec19993cba6bf0f77e6725a68ee4` | 2 | 770 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | CONTINUATION_PHRASES affirmation after an insight offer. |
| 5 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `bc595068cda174dc0167c0d796cacd53c59fbcaa4495b485f710ac9e825203b7` | 1 | 973 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | Email-intent verbs ('send' also matches 'sender'; 'write' also matches 'rewrite'). |
| 6 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `4a720fec20a104bc21b96aac57c663422efae743f623ecb2bbda291afb054298` | 1 | 1080 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | SEARCH_SIGNAL_WORDS in the casual-skip filter, the family of the 'how'-inside-'shower' incident. |
| 7 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `c73ff08563691b1b21228cff68366b238f74ec19993cba6bf0f77e6725a68ee4` | 3 | 1107 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | CONTINUATION_PHRASES continuation check. |
| 8 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `c73ff08563691b1b21228cff68366b238f74ec19993cba6bf0f77e6725a68ee4` | 4 | 1185 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | CONTINUATION_PHRASES affirmation check. |
| 9 | `core/agentic/gate.py` | `evaluate_agentic_gate` | raw_membership | `bd46f63251b2a19f90450974e5679d57c2d24982a6fa8b8626c9cdb038aaaa31` | 1 | 1514 | new in contract v2 | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` | EXPLICIT_SEARCH_KEYWORDS intent-veto exemption. |

## Reproduction

```bash
python scripts/check_bug_classes.py scan --root . --json > /tmp/scan.json
python - <<'EOF'
import json
report = json.load(open('/tmp/scan.json'))
paths = ['core/agentic/gate.py']
for scanner, data in report['scanners'].items():
    for f in data['findings']:
        if f['path'] in paths:
            print(scanner, f['path'], f['symbol'], f['kind'], f['digest'][:12], f['line'], f['excerpt'])
EOF
```

Full expressions and rationales: `docs/execution/class_guards/baseline_review_20260913.json` (review numbers above).
