# CGR-20260913-005 — Query-routing keyword lists outside utils.trigger_match

Immutable class-guard request, written 2026-09-13 by the class-guard owner
under `PLAN_20260913_class_guard_completion.md` (see "Exclusive ownership
and execution boundary"). Respond with one immutable file in Plan 2's
checkout: `docs/execution/generalization/class_guard_responses/CGR-20260913-005.md`.
Follow-ups go in `-2`, `-3`, and so on. Do not edit this packet, the
baseline, the dispositions ledger, the catalog, or `.github/**`.

| Field | Value |
|---|---|
| Scanner | dm01_raw_substring (gate; BC-01, BC-02) |
| Base tree | `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` |
| Occurrences | 4 across 4 file(s) |
| Severity (review judgment) | medium |
| Source paths (Plan 2 owned) | `memory/memory_retriever.py`, `memory/user_profile.py`, `utils/query_checker.py`, `utils/web_search_trigger.py` |

## Observed shape and semantic risk

Four routing predicates match keyword vocabularies by raw `in` against the lowered query: `query_checker.has_thread_break_marker` (THREAD_BREAK_MARKERS), `web_search_trigger.quick_prefilter_should_skip` (EXPLICIT_SEARCH_PHRASES), `MemoryRetriever._maybe_temporal_window_rerank` (_RETROSPECTIVE_MARKERS) and `UserProfile._is_temporal_query` (TEMPORAL_KEYWORDS). Containment in unrelated words and negation change thread continuity, the web-search prefilter and temporal retrieval.

This is a bounded structural review, not a reproduced runtime failure. If
the source owner shows a site is not an instance of the class, the response
should say so with deployed-function evidence. The class-guard owner then
decides between a detector change and continued accepted debt.

## Requested remedy (the source owner owns the design)

Route each vocabulary through `utils.trigger_match`. `memory/user_profile.py` is a shared seam in Plan 2's contract and needs its sequential owner.

## Expected deployed-function regression tests

Deployed-predicate fixtures for each function in clean and wrapped form with a containment counterexample and a negated form, plus positive controls preserving current true positives.

## Response contents

Request ID; owning batch ID; each changed file's SHA-256 before and after;
changed files; the focused test command with its exit code and counts; any
site left unfixed, with the reason; limitations.

## Class-guard acceptance

The request stays open and its occurrences stay `accepted_debt` until the
owner integrates the fix into a tree this checkout can scan. Then
`python scripts/check_bug_classes.py scan --root .` must stop reporting the
anchors below; they show up as STALE. The class-guard owner removes those
rows and marks their records `confirmed_fixed` with this request ID. A
partial fix closes only the anchors that disappear. An edited candidate that
still matches gets a new anchor and is re-reviewed. Any edit to a listed file
makes the scan fail with `disposition_source_changed` until the class-guard
owner re-reviews the remaining debt. If that happens before the fix is
complete, record a pending integration signal.

## Candidate anchors

| Review # | Path | Symbol | Kind | Digest | Ordinal | Line | Legacy key | Source SHA-256 | Note |
|---:|---|---|---|---|---:|---:|---|---|---|
| 21 | `memory/memory_retriever.py` | `MemoryRetriever._maybe_temporal_window_rerank` | raw_membership | `1ed687b5c34c26ee0c15f19b28e242a1eb1df13e4dc23acaed7d41f20e44845c` | 1 | 1693 | `if not any(m in query_lower for m in self._RETROSPECTIVE_MARKERS):` #1 | `d05ac8bc62e3db56af10e2cb237e90c632c0189e272112af1dd1e62860566905` | _RETROSPECTIVE_MARKERS on the query. |
| 23 | `memory/user_profile.py` | `UserProfile._is_temporal_query` | raw_membership | `031e1c0068a017f75df0c3c4de84c37c2f3b7fb27d4e12b891128d2a8a42a90e` | 1 | 686 | `return any(kw in query_lower for kw in self.TEMPORAL_KEYWORDS)` #1 | `2cff4062a8340c20a5d36ba26a21df6c3eca9b56d06f73c926536278f6847f08` | TEMPORAL_KEYWORDS on the query. |
| 28 | `utils/query_checker.py` | `has_thread_break_marker` | raw_membership | `6eec6b756d470457f347e268d18936f9c5d1096a8f8cbfc756c0abd409986393` | 1 | 1436 | new in contract v2 | `3d7c6f95be60bb51bb089977fb4d309e03b878de82795b41c4db9805c851ee50` | THREAD_BREAK_MARKERS on the query. |
| 31 | `utils/web_search_trigger.py` | `quick_prefilter_should_skip` | raw_membership | `f50b50809eecfa19791adb461723e9c0c80206bb82c02afb8e613ee6e0696795` | 1 | 1178 | new in contract v2 | `640006ec2dfe964886b54b5c90e90fec23fceecdae28c2c6c7e7590a9d7b4267` | EXPLICIT_SEARCH_PHRASES in the long-paste prefilter. |

## Reproduction

```bash
python scripts/check_bug_classes.py scan --root . --json > /tmp/scan.json
python - <<'EOF'
import json
report = json.load(open('/tmp/scan.json'))
paths = ['memory/memory_retriever.py', 'memory/user_profile.py', 'utils/query_checker.py', 'utils/web_search_trigger.py']
for scanner, data in report['scanners'].items():
    for f in data['findings']:
        if f['path'] in paths:
            print(scanner, f['path'], f['symbol'], f['kind'], f['digest'][:12], f['line'], f['excerpt'])
EOF
```

Full expressions and rationales: `docs/execution/class_guards/baseline_review_20260913.json` (review numbers above).
