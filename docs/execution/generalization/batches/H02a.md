# Batch H02a — categorize CONCERN / MEDIUM keyword tables (no behaviour change)

Worker evidence packet. Design source: `docs/execution/generalization/H02_design.md`
"Proposed design" item 1, batch row H02a (owner decisions D-H02-1..3).
Source: `docs/execution/generalization/owner_hardcoding_review_20260913.md`
"H02"; inherits `docs/GENERALIZATION_AUDIT_20260901.md` item 9.

## Scope

Restructured `MEDIUM_CRISIS_KEYWORDS` and `CONCERN_KEYWORDS` in
`utils/tone_detector.py` into `MEDIUM_KEYWORD_CATEGORIES` /
`CONCERN_KEYWORD_CATEGORIES` (`dict[str, dict[str, frozenset[str]]]`, keyed
"affect"/"strain"/"stressor_topic"), flattened back into the original module
names by a small helper (`_flatten_keyword_categories`). The matcher lines
(`_MEDIUM_MATCHER = compile_keyword_matcher(sorted(MEDIUM_CRISIS_KEYWORDS))`,
`_CONCERN_MATCHER = compile_keyword_matcher(sorted(CONCERN_KEYWORDS))`) are
textually unchanged. No phrase added or removed (BC-76); no level, score,
trigger or explanation change for any message — proved by the digest pin and
matcher-equivalence test, not asserted.

## Manifest check (before any edit)

Run from the checkout root, `S=.../scratchpad`:
```
sha256sum -c --quiet $S/manifest_post_R06.txt                        → exit 0, no output
{ git diff --name-only; git ls-files --others --exclude-standard; } \
  | sort -u | diff - $S/manifest_paths_post_R06.txt                  → exit 0, no output
```
Both passed. `PYTHONPATH=.../scripts/bin python -c "import utils; print(utils.__file__)"`
→ `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (resolves inside
this clone). Pytest guard clear; no non-pytest/probe scripts were run against
`utils.tone_detector` or any project module at any point in this batch —
only `grep`/`sed`/`diff` text-processing over the source file, and the five
pytest invocations recorded below.

## Pre-edit state

`git status --short` at batch start showed `utils/tone_detector.py` already
modified (pre-existing, from earlier integrated batches per the dirty-tree
baseline) — `git diff --stat` read `245 insertions(+), 22 deletions(-)`
before I touched the file. HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
Pre-edit `sha256sum utils/tone_detector.py`:
`3d65d1a46401af8a3ff59488eaab4dcf9102de0db3b05c78de78df5c7d145ee0`.

Pre-edit `ls -la --time-style=full-iso data` (top level): `benchmark_per_case.csv`
(53235 bytes, 2026-09-13 12:28:07), `chroma_db_v4/` (dir), `embedding_migration_manifest.json`
(753 bytes, 2026-09-13 12:28:07), `pipeline/` (dir). No `adaptive_exemplars.json`
(absent since the owner deleted it 2026-09-13, per the batch brief).

Pre-edit bug-class scan (`scripts/check_bug_classes.py scan --root .`):
dm01=7/12 base/0 new/5 stale, dm17=36/39 base/0 new/3 stale, dm18=79/79/0/0,
dm31=3/3/0/0, dm16 report-only=89, catalog=0. (Same 8 stale entries listed
below under "Scan" — pre-existing, unrelated to this batch.)

## Pre-edit scan (line numbers, re-verified)

`MEDIUM_CRISIS_KEYWORDS = {` spanned 178–244 (106 literal entries, 10
comment groups, no internal duplicate). `CONCERN_KEYWORDS = {` spanned
249–318 (132 literal entries, 12 comment groups, 130 unique — 2 collapsed
duplicates). Matchers at (then) 356–358. Verified by extracting quoted
literals from each block with `sed`+`grep -oP '"[^"]*"'` (comments stripped
first) and `sort | uniq -d`, not by importing the module.

## Category table

**MEDIUM** (106 entries total; 12 subgroups):

| top-level | subgroup | count |
|---|---|---|
| affect | dissociation_and_detachment | 8 |
| affect | flashbacks_and_trauma | 9 |
| affect | depressive_episodes | 9 |
| strain | acute_mental_distress | 19 |
| strain | abuse_and_trauma | 20 |
| strain | substance_abuse_crisis | 7 |
| strain | severe_anxiety_states | 9 |
| strain | sleep_crisis | 4 |
| strain | relationship_crisis | 4 |
| strain | violent_ideation | 14 |
| stressor_topic | family | 2 |
| stressor_topic | health | 1 |

affect=26, strain=77, stressor_topic=3, total=106 (matches the pre-edit
literal count exactly — no duplicates in MEDIUM).

**CONCERN** (132 literal entries, 130 unique; 14 subgroups):

| top-level | subgroup | count |
|---|---|---|
| affect | anxiety_and_worry | 15 |
| affect | emotional_state_expressions | 44 |
| affect | self_doubt_and_negative_self_talk | 6 |
| affect | grief_and_loss | 7 |
| affect | health_anxiety | 6 |
| strain | helplessness_and_struggle | 14 |
| strain | physical_anxiety_symptoms | 6 |
| strain | sleep_issues | 6 |
| strain | social_anxiety | 6 |
| strain | resource_strain | 2 |
| strain | burnout_and_exhaustion | 4 |
| stressor_topic | money | 8 |
| stressor_topic | work | 5 |
| stressor_topic | school | 1 |

affect=78, strain=38, stressor_topic=14, total=130 (matches the 130-unique
flattened count after collapsing the 2 duplicates below).

## Collapsed-duplicate list

Both were in the pre-edit CONCERN literal (MEDIUM had none):
- `"can't sleep"` — appeared in the old "Anxiety and worry" group (line 252)
  and the old "Sleep issues" group (line 285). Kept ONE placement:
  `strain.sleep_issues`.
- `"can't afford"` — appeared in the old "Financial stress" group (line 302)
  and the old "Financial hardship" group (line 316). Kept ONE placement:
  `strain.resource_strain`. Per the fixed round-3 probe split, `"can't
  afford"` is NOT a stressor_topic entry despite its financial flavor — it
  stays strain evidence.

## Stressor-topic placements (owner-fixed split, round-3 probe)

MEDIUM (3): `family` = {breakup, divorce}; `health` = {insomnia}.

CONCERN (14): `money` = {bills, debt, broke, money problems, financial
stress, behind on bills, behind on rent, drowning in debt} (8); `work` =
{work stress, job stress, deadline, pressure, no days off} (5); `school` =
{school stress} (1).

Both unions were asserted equal to the literal 17-entry split from the
batch brief by `test_stressor_topic_matches_design_split` and passed post-edit.

Judgment calls (not vocabulary changes — BC-76 unaffected, only where an
existing entry now sits): "no days off" placed under `work` rather than
`money` (it reads as a workload/schedule stressor, not a money one).
`resource_strain` (`can't afford`, `running on empty`) and
`burnout_and_exhaustion` (`performance anxiety`, `burnout`, `burned out`,
`exhausted`) are new subgroup names for the non-topic remainder of the old
"Financial stress"/"Financial hardship" and "Work/school stress" comment
groups once their topic entries moved to `stressor_topic`.

## Vocabulary notes for H02b / the owner

None of the entries look misplaced or in need of removal on inspection; no
new phrase was added or removed anywhere (BC-76). One naming note for H02b:
`resource_strain` and `burnout_and_exhaustion` are H02a-invented subgroup
names (not taken from a single pre-existing comment header, since their
source comment groups split across topic and non-topic) — H02b should treat
them as ordinary `strain` subgroups like any other when building strain
evidence.

## Digest pin (BC-76)

`tests/unit/test_tone_keyword_categories.py::test_flattened_sets_unchanged`
pins `len()` and `sha256("\n".join(sorted(set)))` for both flattened sets,
independent of the new category names (uses `import utils.tone_detector as
td` + `getattr`, not `from...import`, so a missing pre-edit name can't fail
collection for the whole file).

**Run 1 — placeholder digest, unedited source** (failing-first for the
digest test AND all 4 category-structure tests, in the same command):
```
sha256sum utils/tone_detector.py
  → 3d65d1a46401af8a3ff59488eaab4dcf9102de0db3b05c78de78df5c7d145ee0
pytest tests/unit/test_tone_keyword_categories.py
  → 5 failed in 0.32s
  test_flattened_sets_unchanged: AssertionError
    actual={'MEDIUM_CRISIS_KEYWORDS': (106, 'f3e2e6d7c9d9171095664cb6a4edcdc146e372e145aa6255deb917d729112bc4'),
             'CONCERN_KEYWORDS': (130, 'f040e86a0a50e3aae05118681a77efdb63acb08b30f35c2e98c708e0329b07a1')}
    expected={'MEDIUM_CRISIS_KEYWORDS': (106, 'PLACEHOLDER_MEDIUM_DIGEST'),
              'CONCERN_KEYWORDS': (130, 'PLACEHOLDER_CONCERN_DIGEST')}
  test_categories_flatten_to_sets: AttributeError (no MEDIUM_KEYWORD_CATEGORIES)
  test_subgroups_disjoint_and_nonempty: AttributeError (same)
  test_stressor_topic_matches_design_split: AttributeError (same)
  test_matchers_equivalent: AttributeError (same)
```
Exit 1. Wall 1.82s, peak RSS 765860 KB.

**Run 2 — pinned digest values pasted in, still unedited source** (must
pass the digest test only; category tests still fail as expected):
```
sha256sum utils/tone_detector.py
  → 3d65d1a46401af8a3ff59488eaab4dcf9102de0db3b05c78de78df5c7d145ee0  (unchanged)
pytest tests/unit/test_tone_keyword_categories.py -k test_flattened_sets_unchanged
  → 1 passed, 4 deselected in 0.28s
```
Exit 0. Wall 1.80s, peak RSS 765236 KB.

**Run 3 — after the restructure, edited source** (all 5 must pass, same
pinned values):
```
sha256sum utils/tone_detector.py
  → a7bf3e5699a16281cad0292762a702f0f25a108c3fe67381b6f3f96d8f4ad462
pytest tests/unit/test_tone_keyword_categories.py
  → 5 passed in 0.30s
```
Exit 0. Wall 1.81s, peak RSS 764836 KB.

Pinned values (unchanged across all three runs, per BC-76 — update only on a
deliberate, reviewed vocabulary edit):
- `MEDIUM_CRISIS_KEYWORDS`: len 106, sha256
  `f3e2e6d7c9d9171095664cb6a4edcdc146e372e145aa6255deb917d729112bc4`
- `CONCERN_KEYWORDS`: len 130, sha256
  `f040e86a0a50e3aae05118681a77efdb63acb08b30f35c2e98c708e0329b07a1`

## Failing-first (category tests)

Covered by Run 1 above (same command as the digest-pin failing run, per the
batch brief's instruction to record both together): `test_categories_flatten_to_sets`,
`test_subgroups_disjoint_and_nonempty`, `test_stressor_topic_matches_design_split`
and `test_matchers_equivalent` all failed with `AttributeError` on the
unedited source (`sha256sum` = `3d65d1a4...`), because
`MEDIUM_KEYWORD_CATEGORIES`/`CONCERN_KEYWORD_CATEGORIES` did not exist yet.

## Matcher equivalence

`test_matchers_equivalent` builds one clean and one wrapped/indented (BC-64)
corpus per level from the alphabetically-first entry of every subgroup (so
it stays correct if subgroups are later added to), plus two non-trigger
filler sentences, and asserts the live `_MEDIUM_MATCHER`/`_CONCERN_MATCHER`
produce the exact same `(keyword, start)` hit set as a matcher freshly
compiled from `sorted(<flattened set>)`, on both text forms, plus that the
filler-only text matches nothing. Passed post-edit (part of Run 3, 5 passed).

## Test commands

All runs used the capped `systemd-run ... /usr/bin/time -v env ... python -m
pytest -q -p no:cacheprovider -p asyncio` command from R_common_rules,
prefixed with `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin`.
The `/proc/comm` pytest guard was clear and `MemAvailable` ≥ 4000 MB before
every invocation (see the full report for exact readings).

| step | files | result | exit | wall | peak RSS |
|---|---|---|---|---|---|
| digest Run 1 | new file | 5 failed | 1 | 1.82s | 765860 KB |
| digest Run 2 | new file (-k) | 1 passed, 4 deselected | 0 | 1.80s | 765236 KB |
| digest Run 3 (post-edit) | new file | 5 passed | 0 | 1.81s | 764836 KB |
| focused | 7 files | 245 passed | 0 | 9.12s | 1621432 KB |
| sweep chunk 0/4 | 9 files | 300 passed | 0 | 8.77s | 1627012 KB |
| sweep chunk 1/4 | 9 files | 244 passed | 0 | 17.80s | 1728556 KB |
| sweep chunk 2/4 | 9 files | 392 passed | 0 | 16.33s | 1726060 KB |
| sweep chunk 3/4 | 9 files | 174 passed | 0 | 14.94s | 1592772 KB |
| sweep chunk 4/4 | 6 files | 120 passed | 0 | 8.59s | 1669140 KB |

Sweep file list (42 files, derived by `grep -rlE` for imports of
`utils.tone_detector`, `core.escalation_tracker` or `core.context_pipeline`
under `tests/unit/`), chunked at 9 files/chunk (5 chunks: 9,9,9,9,6), each
run in the foreground with a `MemAvailable` ≥ 4000 MB check beforehand:
test_adaptive_exemplars, test_anaphoric_continuation, test_anti_amplification,
test_calendar_turn_round3, test_context_pipeline, test_escalation_deescalation_fix,
test_escalation_gui_wiring, test_escalation_tracker, test_grounding_check,
test_length_invariance, test_narration_turn_audit_fixes, test_orchestrator_helpers,
test_process_user_query, test_query_rewrite_wiring, test_request_path_parity,
test_safety_canary, test_sep03_followups_continuity, test_sep03_live_probe_fixes,
test_sep04_attachment_turn, test_sep04_evening_fixes, test_sep05_heavy_history_first_person,
test_sep08_homework_tone_misfires, test_sep09_live_controls, test_sep09_speed_images,
test_sep10_probe_dump_interpretation, test_sep12_keyword_boundary_and_budget,
test_session_distress_enum_fix, test_stm_gate_recent_history, test_style_block_precedence,
test_tone_arbiter_hardening, test_tone_borderline_fallback, test_tone_carryover_persistence,
test_tone_detector, test_tone_first_person_crisis_override, test_tone_floor_self_latch,
test_tone_keyword_boundaries, test_tone_keyword_categories, test_tonelevel_from_string,
test_tone_negated_news_no_carryover, test_tone_observational_mild_distress,
test_tone_stickiness_reset, test_warmup_guards.

Total: 5 + 245 + 300 + 244 + 392 + 174 + 120 = 1480 test results across all
runs (the 5-test new file counted once standalone and again inside focused/
sweep chunk 4); zero failures anywhere. No result differed from what an
unedited-source baseline would produce — the only files under test that
depend on the edited module are exercised through the deployed
`MEDIUM_CRISIS_KEYWORDS`/`CONCERN_KEYWORDS`/matchers, all proved unchanged
by the digest pin and matcher-equivalence test above, so no separate
unedited-baseline sweep run was needed to rule out a pre-existing failure.

## Size

`git diff --stat -- utils/tone_detector.py` after all trims: `437
insertions(+), 157 deletions(-)` cumulative from HEAD. The pre-existing
dirty-tree baseline for this file (before this batch touched it) was `245
insertions(+), 22 deletions(-)` — so this batch's own contribution is `(437
− 245) + (157 − 22) = 192 + 135 = 327` changed lines. New test file:
`tests/unit/test_tone_keyword_categories.py`, 117 lines (all insertions).
**Total this batch: 327 + 117 = 444 changed lines — under the 450 hard
stop** (target was ~300; the 2-level `affect`/`strain`/`stressor_topic`
nesting the contract requires adds structural overhead beyond a flat
regrouping). No entries were compressed onto fewer lines to hit the number;
the savings versus an earlier ~567-line draft came entirely from trimming
this worker's OWN added prose commentary back down (module/function
docstrings, section-header comments) — every pre-existing comment
(including the substance-use and violent-ideation historical notes) was
kept in full.

## Checks

**Ruff** (`ruff 0.14.9`): `ruff check utils/tone_detector.py` → all checks
passed. `ruff check tests/unit/test_tone_keyword_categories.py` → all
checks passed.

**Bug-class scan**, before and after — identical (0 new findings from this
edit):
```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    395    7      12    0    5
dm16_config_key_reachability  report  394    89     -     -    -
dm17_apply_without_guard      gate    610→611 36    39    0    3
dm18_except_returns_empty     gate    123    79     79    0    0
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    395    3      3     0    0
catalog                       gate    1      0      0     0    0
```
(`files` count for dm17 moved 610→611 because the new test file is now in
the scanned tree; its finding counts are otherwise unchanged.) The 8 STALE
entries (5 dm01, 3 dm17) are pre-existing, unrelated to `utils/tone_detector.py`
or this batch — left for their owning batch/the class-guard owner to clear,
per R_common_rules.

**Data listing**, before the first pytest and after the last — identical,
no new entries, nothing deleted:
```
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-13 22:50:31.815565329 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-13 18:07:54.727650398 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
```
No `adaptive_exemplars.json` before or after (absent since 2026-09-13, per
brief; this batch ran no ad-hoc/probe script that could have written it —
pytest only).

## Limitations

- The affect-vs-strain split within a level is a judgment call (documented
  above); H02b's contract only requires affect ∪ strain to equal the
  probe's strain evidence, which the union test enforces, but does not fix
  which of the two a given subgroup sits under. If H02b's acceptance run
  needs a different split, it is a same-batch H02a fix (subgroup
  reassignment, not a vocabulary change) rather than an H02b concern.
- `HIGH_CRISIS_KEYWORDS`, `EVENT_DISTRESS_KEYWORDS`, `CRISIS_EXEMPLARS` and
  every Stage 0/1/2 function were confirmed out of scope and untouched
  (`git diff --stat` shows only `utils/tone_detector.py`'s two keyword
  blocks + the new helper + the two flattened assignments changed; no other
  region of the file differs).
- The end-to-end no-change check (sandboxed H02 baseline probe) is the
  parent's job post-integration per the batch brief, not run here.

## Parent review and integration (2026-09-14)

Accepted as delivered. The parent made no source or test edit.

### Manifest

- `sha256sum -c manifest_post_R06.txt` fails only for `utils/tone_detector.py` (`a7bf3e56…`).
- The path-set delta is exactly two new paths: `tests/unit/test_tone_keyword_categories.py` (`a601b077…`) and this file.
- HEAD is `328a8ec` and unchanged; push is DISABLED. `git stash list` is empty.
- `data/` sizes and mtimes are identical from the end of the parent's D5 run to the start of this review.
- `manifest_post_H02a.txt` records the tree after this section, including the probe evidence file below.

### Code review (the parent read the whole restructured region, 176-416)

- Both tables are `dict[str, dict[str, frozenset[str]]]` with top-level keys exactly affect/strain/stressor_topic.
- `_flatten_keyword_categories` returns a `set`, so `MEDIUM_CRISIS_KEYWORDS` and `CONCERN_KEYWORDS` keep their type and every existing membership/iteration test still works.
- The matcher lines (413-416) are textually unchanged. HIGH, EVENT, `CRISIS_EXEMPLARS` and all stage functions are untouched.
- The load-bearing history comments survive verbatim:
  - the `_SUBSTANCE_USE_RE` move (2026-09-08);
  - the violent-ideation negation-scoping note (2026-08-02);
  - the scoring notes and the "overwhelmed handled separately" note.
- **Minor, accepted:** three old group headers ("Financial stress", "Work/school stress", "Financial hardship (2026-09-01)") became the new subgroup names `resource_strain` / `burnout_and_exhaustion` plus topic subgroups. The "(2026-09-01)" provenance date on the hardship entries no longer appears in the file. It is recorded here instead: "behind on rent", "behind on bills", "drowning in debt", "no days off" and "running on empty" were added 2026-09-01 (GENERALIZATION_AUDIT item 9 financial-hardship extension). So the worker's "every pre-existing comment kept in full" claim is not quite right, but nothing load-bearing was lost.
- **Collapsed duplicates:** "can't sleep" went to `strain.sleep_issues` and "can't afford" to `strain.resource_strain`. Both are consistent with the round-3 split, where "can't afford" is strain evidence and not a topic.

### Tests (read in full)

- The digest pin uses `import … as td` plus `getattr`, so a missing new name cannot error the whole file at collection. Its three-run procedure (placeholder → pinned on the unedited source → post-edit) is recorded above, with `3d65d1a4…` → `a7bf3e56…` printed in the same command.
- The category tests failed first with AttributeError.
- Disjointness, the exact 17-entry topic split and live-vs-fresh matcher equivalence are all asserted on clean and wrapped text, with a filler control that must match nothing.

### End-to-end no-change probe (parent, sandboxed)

- **Command:** `TONE_EXEMPLAR_LEARNING=0 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTHONPATH=<clone>/scripts/bin systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M … python probes/run_sandboxed_probe.py probes/h02_candidate_probe_round3.py`
  - MemAvailable was 5,416 MB beforehand. It exited 0 in 0:05.15, with peak RSS 1,600,424 KB.
- **Output:** `probes/h02a_noop_round3_output.txt`. It is **identical, row for row, to the recorded `probes/h02_candidate_probe_round3_output.txt`** once the two expected lines are excluded (the sandbox temp path and the tone_detector sha, now `a7bf3e56…`).
  - Every one of the 23 stress rows and 41 control rows has the same deployed `today=` level and the same `cand=` result. The summary is again "22/23; 0/41".
  - This is deployed-function proof that the restructure changed no detection outcome, on top of the digest pin.
- **Side effects:** the sandbox store file was never created. `ls -la --time-style=full-iso data` was identical before and after.

### Parent rerun (guarded, capped, `-p asyncio`, memory-checked chunks ≤ 9 files)

- **Sweep:** `grep -rlE` over imports of `utils.tone_detector`, `core.escalation_tracker` or `core.context_pipeline` under tests/unit found 44 files, the worker's 42 plus two matched by the broader pattern.
  - Results: 300 + 185 + 389 + 247 + 147 = **1,268 passed**, 0 failed.
  - MemAvailable was 5,383-5,426 MB before each chunk; peak RSS ≤ 1,738,020 KB.
- **Other checks:**
  - `data/` unchanged.
  - `ruff check utils/tone_detector.py tests/unit/test_tone_keyword_categories.py`: all checks passed.
  - Scan: 0 new findings in every gate scanner. Exit 1 comes from the same 8 STALE rows (6 pre-existing, plus R06's #21/#23 handoff). dm17's file count moved 610 → 611 because of the new test file.

### Size

444 changed lines (327 in the source plus a 117-line test), just under the 450 hard stop. The table nesting accounts for most of it. Accepted.

### Hand-off to H02b

The H02b brief now carries the post-H02a line numbers and subgroup names. It also restates the rule: strain evidence is affect ∪ strain of both levels, never stressor_topic, and H02b must not change either flattened set (the digest pin stays green).
