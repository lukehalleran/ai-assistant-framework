# Batch T03 — negated crisis phrase under news framing: hold, not carry-over

Status: **focused contract green** (deterministic unit-test evidence only;
not a synthetic assembled probe, owner canary, or population claim).

Owner decision 2026-09-13, plus the owner's confirmation of "hold, not
reset" semantics (quoted in `T03_design.md`): a negated crisis phrase under
news framing ("people are suffering but I don't want to die", T02's
`observational_negated_crisis` trigger) gets CONCERN for that turn only. It
must not arm the sticky distress floor for a later turn, but it must also
not clear genuine distress carried from an earlier turn. Runs after T02.
Design (authoritative): `docs/execution/generalization/T03_design.md`.
Landing constraint: T01, T02 and T03 land together (T03.md "Landing" below).

## Scope

This worker owns batch T03 only: `core/context_pipeline.py`
(`ContextPipeline._detect_tone`'s post-detection block; `_load_persisted_tone`'s
trigger skip); `core/escalation_tracker.py` (`EscalationTracker.update`'s
distress-counter hold condition); `utils/tone_detector.py`
(`_heavy_row_is_distress_evidence`, adding the read-time re-check). New
`tests/unit/test_tone_negated_news_no_carryover.py` and this file. Not
touched: `core/orchestrator.py` (already passes `tone_trigger` through
unmodified — verified read-only below), `_recent_distress_from_history`,
`_session_in_distress`, `detect_crisis_level`, `_classify_observational_evidence`,
the two exported trigger constants, and every other T01/T02 file.

## Process note (self-caught, corrected immediately)

The mandatory order of work (manifest check → base scan → write tests →
capture failing-before in one command → only then edit) was violated once:
`core/context_pipeline.py`'s three edits (import line, `_detect_tone` hold,
`_load_persisted_tone` skip) were applied BEFORE the new test file was
written — the exact process failure the draft called out from the previous
three batches (R05, T01, T02 process notes). It was caught immediately
after the third of those three edits, before any other file was touched and
before any test was run. All three edits were reverted in place with
matching `Edit` calls (old_string/new_string equality, no `git` command),
and `sha256sum core/context_pipeline.py` was reconfirmed byte-identical to
the parent-verified digest (`aebe98e7...`) before the test file was written
and the failing-before run was captured. No source logic was ever exercised
or tested in this mistaken state; the fourth instance of this exact
ordering mistake across R05/T01/T02/T03, caught before any test run this
time (T02's slip was caught after one edit; this one after three, still
before any test execution).

## Manifest check (before any edit)

- `sha256sum -c --quiet $S/manifest_post_T02.txt` — printed nothing, exit 0.
- `{ git diff --name-only; git ls-files --others --exclude-standard; } |
  sort -u | diff - $S/manifest_paths_post_T02.txt` — printed nothing, exit 0.
- `git remote -v`: `origin` fetch = `/home/lukeh/Daemon_v1`, push =
  `DISABLED` (read-only, unchanged).
- Environment sanity: `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin
  python -c "import utils; print(utils.__file__)"` →
  `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (this clone).
- No `git stash`/`reset`/`clean`/`checkout`/`restore` was run at any point.
  No `git show` output was ever written over a tracked file. No commit or
  push was made.

### Parent-verified facts re-confirmed

Before any edit, the three source files matched the T02-contract digests
exactly:

| File | Confirmed pre-edit SHA-256 |
|---|---|
| `core/context_pipeline.py` | `aebe98e701a17ac8ec5b0f92c3da741e9731415d0a7c83816b8d4cec0f3451a7` |
| `core/escalation_tracker.py` | `5ecacb7444b5ca25d42c3a64577e8cc29f548af00e03b65120dd30670774fb6d` |
| `utils/tone_detector.py` | `dd781ad850af56f905ff32328c87d871b8018192af4d765764a3fe36e2a7cf19` |

Line numbers matched the draft's parent-verified facts exactly:
`_detect_tone` at 694; `self._last_tone_level = emotional_ctx.crisis_level`
at 765 (pre-edit); the `_floor_chain`/`_persist_tone` block at 775-779;
`_load_persisted_tone`'s `distress_sticky_floor` skip at 827;
`EscalationTracker.update` at 169, its hold condition at 219-223;
`core/orchestrator.py` 1580-1587 passes `tone_trigger_str` unmodified
(confirmed read-only, no change needed).

## Failing-before evidence

Captured in ONE command, per the corrected order of work (digests, then the
new test file run on the unedited, pristine T02-contract sources):

```
sha256sum core/context_pipeline.py core/escalation_tracker.py utils/tone_detector.py
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v \
env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_tone_negated_news_no_carryover.py
```

Digest lines printed (identical to the table above):
`aebe98e7...` / `5ecacb74...` / `dd781ad8...`.

Result: **16 failed, 10 passed**, exit 1, elapsed 0:12.54, max RSS
1,606,256 KB.

- The 2 `TestNegatedNewsTurnItself` failures (clean+wrapped): pre-fix,
  `_last_tone_level` is unconditionally set to `CrisisLevel.CONCERN` and the
  turn is unconditionally persisted — both assertions fail
  (`assert <CrisisLevel.CONCERN...> is None`).
- The 6 `TestNoCarryoverAfterNegatedNews` failures (3 previous-tone-path +
  3 heavy-history-path follow-ups): pre-fix, turn 2 resolves
  `CONCERN`/`distress_sticky_floor` instead of `CONVERSATIONAL`.
- The 2 `TestRestartSeeding` failures: the persisted state changes across
  the held turn (pre-fix persists it), and the loader does not skip a
  state file carrying the negated-crisis trigger.
- The 1 `TestEscalationTrackerHold::test_negated_crisis_trigger_holds_the_count`
  failure: pre-fix the counter increments to 2 instead of holding at 1.
- The 5 `TestHeavyRowReCheck` negated-news-row failures (2 clean/wrapped +
  3 field-name variants): pre-fix the row re-checks `True` (distress
  evidence) instead of `False`.
- The 10 passes pre-fix are exactly the rows unaffected by this batch:
  `TestControlMildDistressStillCarries` (3 — the OTHER Tier B trigger
  already carries over correctly, unchanged by this batch),
  `TestHoldNotReset` (1 — coincidentally satisfied pre-fix in this exact
  scenario, because the value being held equals the value that would have
  been set anyway; still a valid acceptance assertion for the contract),
  `TestEscalationTrackerHold`'s other 3 tests (organic/first-person-distress
  increment, `distress_sticky_floor` hold — all pre-existing, unchanged
  behavior), and `TestHeavyRowReCheck`'s mild-distress-row (clean+wrapped)
  and legacy-row-without-text tests (3 — pre-existing, unchanged behavior).

Only then were the sources edited.

## Changed-file manifest

| File | Status | SHA-256 before | SHA-256 after |
|---|---|---|---|
| `core/context_pipeline.py` | modified (import; `_detect_tone` hold; loader skip) | `aebe98e701a17ac8ec5b0f92c3da741e9731415d0a7c83816b8d4cec0f3451a7` | `853ff419985686f593b75e8c4ebf92620cc1ef679057c6a16480709973a322d0` |
| `core/escalation_tracker.py` | modified (import; hold condition) | `5ecacb7444b5ca25d42c3a64577e8cc29f548af00e03b65120dd30670774fb6d` | `8d612937be003e3c7ebe1b2f8d68290641642419b2ad41e3080068d6eb588c27` |
| `utils/tone_detector.py` | modified (read-time re-check in `_heavy_row_is_distress_evidence`) | `dd781ad850af56f905ff32328c87d871b8018192af4d765764a3fe36e2a7cf19` | `3d65d1a46401af8a3ff59488eaab4dcf9102de0db3b05c78de78df5c7d145ee0` |
| `tests/unit/test_tone_negated_news_no_carryover.py` | new | — | `de9b04710f69821a6de7139dead8fae4ed7a2f763901f958685305af31871f83` |
| `docs/execution/generalization/batches/T03.md` | new | — | see this file |

`git status --short` at handoff shows the identical prior set (R05, T01,
T02, and the parent's docs) plus `M core/context_pipeline.py`,
`M core/escalation_tracker.py`, `M utils/tone_detector.py`, and the two new
untracked files above. `HEAD` unchanged: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
No other path in `git status --short` differs from the T02 manifest.

## Implementation, per carry-over path

`Import the constant from utils.tone_detector; never repeat the string
literal` — verified: `core/context_pipeline.py` and `core/escalation_tracker.py`
both `from utils.tone_detector import OBSERVATIONAL_NEGATED_CRISIS_TRIGGER`
at module level (core → utils is the existing direction; `utils/tone_detector.py`
imports nothing from `core/`, confirmed by grep — no cycle). Within
`utils/tone_detector.py` itself, `_heavy_row_is_distress_evidence` (defined
at line 781, before the constant/classifier at ~1024/1085) references both
by name at CALL time, a safe same-module forward reference — the whole
module is loaded before the function is ever invoked.

| Path | Site | Change |
|---|---|---|
| Previous tone | `ContextPipeline._detect_tone` (~765-787) | Trigger check moved before the `_last_tone_level` write. When `_trigger == OBSERVATIONAL_NEGATED_CRISIS_TRIGGER`, the entire block (`_last_tone_level` write, `_floor_chain` update, `_persist_tone` call) is skipped (`pass`) — the turn's own `tone_level`/`emotional_ctx` are still returned unchanged. Every other trigger takes the unchanged `else` branch. |
| Floor chain | Same block | Held together with `_last_tone_level` (skipped, not reset to 0) — a held turn neither extends nor resets the `TONE_FLOOR_CHAIN_MAX` budget consumed by prior floor turns. |
| Persistence | Same block | `_persist_tone` is never called for a held turn — `data/tone_state.json` (sandboxed in tests) is untouched; verified directly (`TestRestartSeeding::test_persisted_state_unchanged_by_held_turn`, `TestNegatedNewsTurnItself` asserts `_state(pipe) is None` after the very first turn). |
| Restart loader | `_load_persisted_tone` (~816-839) | The `trigger == "distress_sticky_floor"` skip is now `trigger in ("distress_sticky_floor", OBSERVATIONAL_NEGATED_CRISIS_TRIGGER)`. Defensive: `_detect_tone` no longer persists this trigger at all, but a state file written by any other path (or left over from before this fix) is still skipped. |
| Heavy history | `_heavy_row_is_distress_evidence` (~781-861) | New read-time re-check inserted immediately after `text = str(text)` and before the existing code-stripped/prefix-only/first-person checks: `if _classify_observational_evidence(text) == OBSERVATIONAL_NEGATED_CRISIS_TRIGGER: return False`. Any other classifier outcome (Tier A, the mild Tier B trigger, or `None`) falls through to the unchanged existing logic. The `if not text: return True` fail-closed branch is untouched (legacy rows still count as evidence). |
| Distress counter | `EscalationTracker.update` (~219-231) | `if (tone_trigger or "") != "distress_sticky_floor":` became `if (tone_trigger or "") not in ("distress_sticky_floor", OBSERVATIONAL_NEGATED_CRISIS_TRIGGER):`. `consecutive_elevated_count` / `consecutive_calm_count` are untouched (unaffected lines above/below). |
| Orchestrator hand-off | `core/orchestrator.py` (~1577-1588) | Read-only, verified: passes `tone_trigger_str` from `context.emotional_context.tone_trigger` straight into `tracker.update(...)` with no branching of its own — the tracker's hold condition covers this call site automatically. No change needed or made. |

## Deployed-vs-probe follow-up table

Every row below is the REAL, deployed `ContextPipeline._detect_tone`
(`object.__new__` seam, `model_manager=None`, the real offline-cached
embedder, sandboxed tone-state file) — not the parent's probe's simulated
wrapper. "Today's rule" is this batch's own failing-before run (unpatched
T02-contract source); "Deployed (post-fix)" is the patched source, all 26
tests green.

| Turn 1 (Stage 0 fires) | Follow-up | Carry-over path exercised | Today's rule (pre-fix) | Deployed (post-fix) | Matches design |
|---|---|---|---|---|---|
| negated-news → `observational_negated_crisis` | the weather was grey this morning | previous tone | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| (same) | I watched a documentary about whales last night | previous tone | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| (same) | my sister is visiting next week | previous tone | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| (same) | the weather was grey this morning | heavy history row | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| (same) | I watched a documentary about whales last night | heavy history row | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| (same) | my sister is visiting next week | heavy history row | CONCERN/`distress_sticky_floor` | **CONVERSATIONAL** | yes |
| people are suffering and I feel hopeless → `observational_first_person_distress` (hopeless control) | the weather was grey this morning | previous tone | CONCERN/`distress_sticky_floor` | CONCERN/`distress_sticky_floor` (unchanged — kept) | yes |
| (same) | I watched a documentary about whales last night | previous tone | CONCERN/`distress_sticky_floor` | CONCERN/`distress_sticky_floor` | yes |
| (same) | my sister is visiting next week | previous tone | CONCERN/`distress_sticky_floor` | CONCERN/`distress_sticky_floor` | yes |
| **Hold row**: N-1 hopeless control (organic CONCERN) → N negated-news (held) → N+1 neutral | the weather was grey this morning | previous tone (N-1's carry survives the held turn) | CONCERN/`distress_sticky_floor` | CONCERN/`distress_sticky_floor` (N-1's carry preserved) | yes |

`EscalationTracker.consecutive_distress_count` after one CONCERN turn
(matches the parent probe's table exactly):

| Trigger | Today's rule (pre-fix) | Deployed (post-fix) |
|---|---|---|
| organic (`harm_score: 4.0 (0H, 0M, 2C)`) | 1 | 1 (unchanged) |
| `distress_sticky_floor` | 0 (hold, pre-existing) | 0 (unchanged) |
| `observational_negated_crisis` | 1 | **0 (hold)** |
| `observational_first_person_distress` | 1 | 1 (unchanged — increments) |

## Commands and results

Guard before every pytest run: `for p in $(pgrep -f -- '-m pytest'); do case
"$(cat /proc/$p/comm)" in python*) echo RUNNING;; esac; done`. One run was
detected mid-batch (an unrelated 13-file suite, pid 983318, elapsed 30s at
first check); the guard's poll-and-wait loop (60s steps) cleared after one
wait cycle before any command of this batch ran concurrently with it.

1. Failing-before (pristine T02-contract source): **16 failed, 10 passed**,
   exit 1, elapsed 0:12.54, max RSS 1,606,256 KB (see above).
2. New file alone, post-patch: **26 passed**, exit 0, elapsed 0:10.58, max
   RSS 1,605,708 KB.
3. Focused (10 assigned files, post-patch):
   `test_tone_negated_news_no_carryover.py test_tone_floor_self_latch.py
   test_tone_stickiness_reset.py test_tone_carryover_persistence.py
   test_escalation_deescalation_fix.py test_context_pipeline.py
   test_session_distress_enum_fix.py test_anti_amplification.py
   test_tone_observational_mild_distress.py
   test_tone_first_person_crisis_override.py` → **267 passed**, exit 0,
   elapsed 0:08.36, max RSS 1,611,336 KB.
4. Cross-batch sweep (35 files: the 4 T03-specific files, T01's 18-file
   tone sweep, and every unit test importing `core.context_pipeline` or
   `core.escalation_tracker`, derived by grep and de-duplicated), one
   process: **999 passed**, exit 0, elapsed 0:22.63, max RSS 1,754,284 KB.
   Zero failures.
5. `python -m ruff --version` → `ruff 0.14.9`.
6. `python -m ruff check core/context_pipeline.py core/escalation_tracker.py
   utils/tone_detector.py tests/unit/test_tone_negated_news_no_carryover.py`
   → **All checks passed!**
7. `ls -A data` before the first pytest run and after the last: identical —
   `adaptive_exemplars.json benchmark_per_case.csv chroma_db_v4
   embedding_migration_manifest.json pipeline`. No new entry; nothing
   deleted; `data/tone_state.json` never appeared (tone state stayed inside
   the conftest/per-instance sandbox throughout).
8. `python scripts/check_bug_classes.py scan --root .` (post-edit) → exit 1
   (designed: 6 pre-existing STALE entries on `knowledge/git_memory.py` and
   `scripts/*.py`, all from R01/R03, unrelated to this batch — identical
   set and count to T02's own end-of-batch scan). All gated scanners
   (`dm01_raw_substring`, `dm17_apply_without_guard`, `dm18_except_returns_empty`,
   `dm31_live_state_default`) report **0 new** findings; none of the three
   changed files appears in the scan output (grep-confirmed empty for
   `context_pipeline.py`, `escalation_tracker.py`, `tone_detector.py`).

## BC-58 sibling search

Every `"distress_sticky_floor"` comparison in `core/` and `utils/`
(`grep -rn '"distress_sticky_floor"' core/ utils/`), and every reader of
`_session_in_distress` / `_recent_distress_from_history`:

| Site | Disposition |
|---|---|
| `core/context_pipeline.py:787` (`_trigger == "distress_sticky_floor"`, floor-chain increment) | Unchanged — this batch's new `OBSERVATIONAL_NEGATED_CRISIS_TRIGGER` branch is checked FIRST and returns before this comparison is reached for a held turn. |
| `core/context_pipeline.py:840` (`_load_persisted_tone` skip) | **Fixed** — extended to also skip `OBSERVATIONAL_NEGATED_CRISIS_TRIGGER` (this batch). |
| `core/escalation_tracker.py:227` (`update`'s hold condition) | **Fixed** — extended to a tuple membership test including `OBSERVATIONAL_NEGATED_CRISIS_TRIGGER` (this batch). |
| `core/orchestrator.py:1577` (comment only, no live comparison) | Unchanged — orchestrator passes `tone_trigger` through with no branching of its own; the tracker's fix covers it (verified read-only, see "Implementation" table above). |
| `utils/tone_detector.py:1697` (`detect_crisis_level`'s own floor-production site, sets `trigger="distress_sticky_floor"`) | Unchanged — this is where the string is PRODUCED, not compared; out of this batch's scope (T02/T01 territory, untouched). |
| `utils/tone_detector.py:945` `_session_in_distress` (definition) | Unchanged — reads `previous_tone`/`conversation_history` generically; does not compare trigger strings itself. Not this batch's concern: the held turn never sets `_last_tone_level` or a heavy-evidence-true row in the first place, so nothing reaches this function that needs a trigger-specific carve-out here. |
| `utils/tone_detector.py:1567` `detect_crisis_level` (calls `_session_in_distress`) | Unchanged, same reasoning. |
| `utils/query_checker.py:1188,1229` (docstring references to `_recent_distress_from_history`, no call) | Not a call site — comments only. |
| `utils/ordered_slice.py:15` (docstring reference) | Not a call site — comment only. |
| `utils/tone_detector.py:971` `_session_in_distress`'s own call to `_recent_distress_from_history` | Covered transitively by the `_heavy_row_is_distress_evidence` fix (this batch) — every row `_recent_distress_from_history` scans is re-checked at read time before it can count as evidence. |
| `utils/tone_detector.py:1390` `_semantic_crisis_detection`'s call to `_recent_distress_from_history` (the `force_escalation`/escalation-boost path) | Same transitive coverage — a negated-news row can no longer inflate `recent_distress` here either, since `_heavy_row_is_distress_evidence` now excludes it upstream of both call sites. |

**Out of scope, reported per T03_design.md** (not tone carry-over, other
readers of the stored heavy flag): `memory/thread_manager.py:70`
(`last_conv.get("is_heavy_topic", False)` — session-opener context, no
tone-carry semantics) and `core/prompt/section_instructions.py:96`
(`is_heavy_topic=bool(prompt_ctx.get("is_heavy_topic"))` — heavy-topic
prompt instruction, no tone-carry semantics). Both confirmed present and
unchanged by grep; neither touched.

## Privacy / no-network

No live store, network credential, or LLM provider was used.
`HF_HUB_OFFLINE=1` / `TRANSFORMERS_OFFLINE=1` throughout; `model_manager=None`
on every test call — `detect_crisis_level`'s real Stage 2 falls back to
`ModelManager._get_cached_embedder()`, a locally cached offline
sentence-transformer, never a network or provider call (same as T02's
probe and the pre-existing focused suites). All fixtures are synthetic
strings. `ls -A data` unchanged before/after; tone state stayed inside the
conftest autouse sandbox plus this file's own per-instance
`_TONE_STATE_PATH` override (`tmp_path`-scoped) — `data/tone_state.json`
was never written. No secrets or personal data in the new test file or
this packet.

## Size

- `core/context_pipeline.py`: +25/−7 = 32 changed lines (this batch's
  entire delta — the file was unmodified from HEAD before this batch).
- `core/escalation_tracker.py`: +10/−1 = 11 changed lines (entire delta,
  same reasoning).
- `utils/tone_detector.py`: this batch's own isolated hunk (verified via
  `git diff -U0`/`-U2`, the region wholly inside
  `_heavy_row_is_distress_evidence`) is +19/−1 = 20 changed lines. The
  file's cumulative `git diff --stat` against base HEAD is 267 lines total
  (245 insertions, 22 deletions), of which T02 already contributed 247
  (226/−21, integrated and reviewed) — this batch's own increment is the
  remaining 20.
- New test file: **254 lines**.
- **This batch's own total: 32 + 11 + 20 + 254 = 317 changed lines** —
  above the ≤250 target, within the ≤450 hard cap. No split proposed; no
  attempt was made to compress code, comments, or test coverage to force a
  lower number, consistent with T01/T02's own size notes (both also
  exceeded their ~200/~250-line design-table estimates for the same
  reason: new branch logic plus a from-scratch regression suite covering
  every acceptance-test row in both clean and wrapped form, plus every
  distinct carry-over path named in the design).

## Open decisions / limitations

- **Pre-edit scan not separately captured.** Per the mandatory order of
  work, the base bug-class scan should run BEFORE any edit as a discrete
  step, in addition to the combined digest+failing-test command. This
  batch captured the digest+failing-test command correctly (step 4 of the
  order of work) but did not run `check_bug_classes.py scan` as an isolated
  pre-edit step. T02's own end-of-batch scan (run on the identical starting
  digest this batch began from — `dd781ad8...`) is the closest available
  before-state and shows the identical 0-new/6-stale shape this batch's
  own post-edit scan reports. Recorded honestly as a gap, same limitation
  T02.md recorded for the same reason.
- **Process slip** (see "Process note" above): three `core/context_pipeline.py`
  edits were applied before the test file existed, caught immediately
  after (before any test ran), and reverted with matching `Edit` calls,
  digest-reconfirmed pristine. No source logic was ever tested in the
  mistaken state.
- **`TestHoldNotReset` does not discriminate pre/post-fix in isolation.**
  In the specific scenario the design names (organic CONCERN at N-1,
  negated-news held at N, neutral at N+1), the value `_detect_tone` HOLDS
  at N happens to equal the value it would have SET anyway (both are the
  N-1 CONCERN), so this one test passes on both the unpatched and patched
  source. It remains a correct, necessary acceptance assertion for the
  "hold, not reset" contract (and is included in the file's overall
  16-failed/10-passed failing-before proof); it just isn't independently
  discriminating. No sibling scenario in the design's acceptance list
  requires a value where held ≠ would-have-been-set, so no additional test
  was added purely to force a discriminating case beyond the design's own
  list.
- **Known T01/T02 limitations unchanged**, not re-litigated here (see
  T02.md "Open decisions / limitations"): the two inversion rows still
  read as negated ("I can't stop cutting myself" style), bare "not" is not
  a chokepoint cue, ambiguous bare CONCERN words, ordinary mild first-person
  distress without news framing gets no floor.
- **Non-unit importers not run** (CI-owned, per instructions):
  `tests/integration/test_golden_distress_replay.py`,
  `tests/test_llm_fallback.py`, `tests/test_orchestrator_profile_injection.py`,
  `tests/test_tone_detection.py`.

## Remaining milestone state

**Focused contract green** (deterministic unit-test evidence for the
deployed `ContextPipeline._detect_tone`, `_load_persisted_tone`,
`EscalationTracker.update`, and `_heavy_row_is_distress_evidence`). Not a
synthetic assembled probe, owner canary, or population claim.

## Integration state

Integrated into the uncommitted candidate tree on base `328a8ec`; no
commit, push, or restart. Milestone: focused contract green for T03's hold
semantics across all four verified carry-over paths.

## Landing (T01 + T02 + T03)

All three land together, exactly as `T01_design.md`/`T02_design.md`/`T03_design.md`
specify:

- **T01 alone**: resolves the negated/third-party over-escalation for
  Stage 1's keyword path (MEDIUM), but Tier B (T02) did not yet exist —
  mild first-person distress under news framing stayed CONVERSATIONAL
  (under-escalation) and the negated-only case over-escalated to MEDIUM.
- **T01 + T02, without T03**: `observational_negated_crisis` exists and
  correctly returns CONCERN for that turn, but — the exact gap T02.md's own
  "Contract for T03" and "Landing constraint" flagged — nothing yet
  excludes that trigger from carry-over. It would re-arm the sticky floor
  through the previous-tone path, the restart loader, the heavy-history
  row, and the escalation tracker's distress counter (this batch's
  failing-before run, 16 failures, IS that gap, reproduced against the
  real deployed pipeline).
- **T01 + T02 + T03 (this candidate)**: the negated-news turn is CONCERN
  for that turn only, transparent to every carry-over path, and an earlier
  genuine CONCERN survives it unchanged (hold, not reset) — verified by
  this batch's 26/26 new-file tests, the 267/267 focused run, and the
  999/999 cross-batch sweep, all against the real deployed functions with
  `model_manager=None` and no network/provider access.

No commit, push, or restart was made by this or any other batch in this
lane.

---

## Parent review and integration (parent session)

### Review

- **Manifest and scope.**
  - Against `manifest_post_T02`, only `utils/tone_detector.py` changed digest (`dd781ad8…` → `3d65d1a4…`).
  - New paths: exactly `core/context_pipeline.py` (`aebe98e7…` → `853ff419…`), `core/escalation_tracker.py` (`5ecacb74…` → `8d612937…`), `tests/unit/test_tone_negated_news_no_carryover.py` (`de9b0471…`) and this packet.
  - `core/orchestrator.py` and `gui/handlers.py` are unchanged.
- **Code matches `T03_design.md` "Decision" and the owner-confirmed hold.**
  - `_detect_tone`: when the trigger equals the imported `OBSERVATIONAL_NEGATED_CRISIS_TRIGGER`, `_last_tone_level`, `_floor_chain` and `_persist_tone` are all left untouched. Every other trigger runs the original code, now moved into the `else` branch, including the floor-chain logic.
  - `_load_persisted_tone` skips the new trigger alongside `distress_sticky_floor`.
  - `EscalationTracker.update` holds the distress counter for both triggers.
  - `_heavy_row_is_distress_evidence` re-classifies the row text with `_classify_observational_evidence` right after text extraction and the unchanged no-text fail-closed rule, and before the existing code-strip and heavy-keyword checks.
  - Import direction is core → utils only; `utils/tone_detector.py` imports nothing from `core`.
- **Tests reviewed.** Two real `_detect_tone` turns through the `object.__new__` seam, with tone state under `tmp_path`.
  - The previous-tone path is isolated with a non-heavy row, and the heavy-history path with a heavy row. Each fails without its part of the fix.
  - Also covered: the mild-distress control (floor kept), the hold row, restart persistence and the loader skip, all four tracker cases, and the row re-check across field names plus a legacy row.
  - `TestHoldNotReset` catches the reset alternative the owner rejected. It does not separate "hold" from "set CONCERN" in its single scenario, but the turn-itself and follow-up tests do. Accepted.
- **Correction to this packet's "Landing" section.** The "T01 alone" bullet says T01 "resolves the negated/third-party over-escalation". It does the opposite: T01 alone introduces that over-escalation (MEDIUM) under news framing. T02 corrects it, and T03 removes carry-over for the negated-only case. The landing constraint is unchanged.
- **Process (fourth consecutive batch).** Source was edited before the tests existed. It was caught after three edits, before any test ran, reverted with Edit and digest-verified. The failing-before run on the pristine sources gave 16 failed / 10 passed. The pre-edit scan was again not isolated; the post-scan shows 0 new findings and the same 6 stale rows. Accepted as recorded. A05b-3's instructions spell out the exact step order.
- **Size.** This batch's own delta is 317 lines, within the 450 cap.

### Parent rerun

- **Tests.** The sweep was derived by the parent: every unit test importing `core.context_pipeline`, `core.escalation_tracker` or `utils.tone_detector`, plus the new file, 44 files in total. Two guarded runs under `MemoryMax=6G`: **664 passed** (24 files, wall 0:23.18, peak RSS 1,746,880 KB) and **612 passed** (20 files, wall 0:22.26, peak RSS 1,716,816 KB), both exit 0. ruff clean on all four files; `ls -A data` unchanged.
- **Implemented-code probe** (the owner's requested evidence): `docs/execution/generalization/probes/t03_pipeline_followup_probe.py`, with output alongside. Real `ContextPipeline._detect_tone` for two consecutive turns, tone state in a temp directory.
  - After "people are suffering but I don't want to die" (CONCERN, `observational_negated_crisis`), all **6** neutral follow-ups are **CONVERSATIONAL**: three messages, each with and without a heavy history row.
  - After "people are suffering and I feel hopeless", all 6 are CONCERN / `distress_sticky_floor`.
  - The hold row floors N+1.
  - The tracker holds at 1.

### Integration state

- Integrated into the uncommitted candidate on base `328a8ec`; no commit, push or restart. Milestone: **focused contract green**.
- **T01, T02 and T03 are complete and land together.**
- Next on the queue: A05b-3.
