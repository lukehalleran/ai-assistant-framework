# F10a — a failed turn write raises StoreWriteError instead of returning the same None as a deliberate skip

Batch ID: F10a. Request: CGR-20260913-010 (BASE response, anchors #127 and #140).
Base commit: 328a8ecea1dae91de16f5974ea71727c4e2e1aa0 (uncommitted tree, no commits made).
Worker started: 2026-09-14.

## 1. Manifest check (before any edit)

```
git status --short   # ~114 tracked M, ~30+ untracked paths incl. docs/execution/generalization/ (new tree)
git rev-parse HEAD    # 328a8ecea1dae91de16f5974ea71727c4e2e1aa0
git diff --stat       # 69 files changed, 3627 insertions(+), 688 deletions(-)
git remote -v         # origin (fetch) /home/lukeh/Daemon_v1 ; origin (push) DISABLED
```

Assigned-file digests at start (match F10a.md brief's PARENT-VERIFIED FACTS exactly):
```
16f7bcb93e44885f353e679067eb96a8407ba9e316f00503432c021ad2ea2c3f  utils/retrieval_outcome.py
66833584f2dfe725c27e3c4fb39af2e71e28b822d341e3da12667bca6e0992fe  memory/storage/multi_collection_chroma_store.py
efadf44fa77f97c1191f9c2733c1211277263ea75f3ccc6701696cedf56205c6  memory/memory_storage.py
dcac512839c67a68b08b99d7f3a9ff7f31aa4ea1ec8892f2583bedf81bda9006  memory/memory_coordinator.py
f29c967c16ca5d979c58e744f8821c17075ed5a41f38da81cc001f5d2a9e7518  memory/memory_interface.py
017d85383b0e073591c3e209fad654c683c0770333012c2c3a295ae28552869c  gui/handlers.py
```
(core/orchestrator.py is read-only this batch; not re-verified here.)

Manifest check against `$S/manifest_post_F9b.txt` and `$S/manifest_paths_post_F9b.txt` (220 paths):
- `sha256sum -c "$S/manifest_post_F9b.txt"` run from the repo root: **0 mismatches** (220/220 OK).
- Path listing: `awk '{print $2}' "$S/manifest_post_F9b.txt" | sort` vs `sort "$S/manifest_paths_post_F9b.txt"`: `diff` → 0 lines, 220 == 220. **Empty path diff.**

MemAvailable before first chunk: 6457 MB (≥4000, OK). `logs/` absent (confirmed, `ls -ld logs` → No such file or directory).

data/ listing (top level only) matches `$S/data_baseline_f7a_rerun.txt` exactly (7 entries: benchmark_per_case.csv, chroma_db_v4, chroma_multi, embedding_migration_manifest.json, pipeline, user_profile.json, web_search_credits.json). `diff` → 0 lines. No new/changed entry.

Manifest check: **PASS**. Proceeding.

## 2. Pre-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (gate, as expected; unrelated pre-existing gate findings elsewhere in the tree). stderr empty.

dm18 summary row (matches brief exactly): `dm18_except_returns_empty  gate  123  47  79  0  32`.

The #127 (`memory/memory_storage.py [MemoryStorage.store_interaction]`) and #140
(`memory/storage/multi_collection_chroma_store.py [MultiCollectionChromaStore.add_conversation_memory]`)
rows do not appear in the plain-text "found" detail list pre-edit (the scan's plain-text
output only enumerates a subset / STALE rows in detail); the dm18 summary row above
(47/79/0/32) is the baseline this batch diffs against post-edit, per the F9a/F9b precedent.

## 3. New test file and failing-first run

New file: `tests/unit/test_store_write_outcomes.py` (350 lines; fakes only — never a real
Chroma client, embedder or data path). Covers, per the brief's TESTS section: the
`StoreWriteError` leaf contract; `add_conversation_memory` raise/success (fake
'conversations' collection via `MultiCollectionChromaStore.__new__`, F9a pattern);
`store_interaction` — a `StoreWriteError` from the Chroma write propagating unchanged
(same object), a non-`StoreWriteError` body exception (corpus `add_entry`) wrapping as
`StoreWriteError(source="store_interaction", …)`, the success control; the four
deliberate skips (parametrized) returning `None` with no write (paired controls,
BC-64); `MemoryCoordinator.store_interaction` — sync-back on raise (via
`MemoryCoordinator.__new__` + a lightweight fake `_storage`) and the success control;
`gui.handlers._background_store_interaction` — logged-and-transcript-still-recorded on
raise (`db_id=None`) and the success control; a privacy marker check at each layer
(chroma reason/str, storage reason/str, handler caplog).

`StoreWriteError` does not exist pre-edit, so every test needing it imports it LOCALLY
inside the test body (not at module scope), so the file still collects and the
skip/success controls run and pass on the unedited tree.

FAILING-FIRST command (digest line + new tests, in one command, UNEDITED source):
```
sha256sum utils/retrieval_outcome.py memory/storage/multi_collection_chroma_store.py \
  memory/memory_storage.py memory/memory_coordinator.py memory/memory_interface.py gui/handlers.py
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
  python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_store_write_outcomes.py
```
Digests (unchanged from section 1, confirming the run was against unedited source):
```
16f7bcb93e44885f353e679067eb96a8407ba9e316f00503432c021ad2ea2c3f  utils/retrieval_outcome.py
66833584f2dfe725c27e3c4fb39af2e71e28b822d341e3da12667bca6e0992fe  memory/storage/multi_collection_chroma_store.py
efadf44fa77f97c1191f9c2733c1211277263ea75f3ccc6701696cedf56205c6  memory/memory_storage.py
dcac512839c67a68b08b99d7f3a9ff7f31aa4ea1ec8892f2583bedf81bda9006  memory/memory_coordinator.py
f29c967c16ca5d979c58e744f8821c17075ed5a41f38da81cc001f5d2a9e7518  memory/memory_interface.py
017d85383b0e073591c3e209fad654c683c0770333012c2c3a295ae28552869c  gui/handlers.py
```
Result: **6 failed, 8 passed** in 5.01s (elapsed 8.50s incl. systemd-run/time overhead;
Maximum resident set size 1,111,356 KB ≈ 1,085 MB). Exit 1.

Failures (exactly as predicted): all 6 are `ImportError: cannot import name 'StoreWriteError'
from 'utils.retrieval_outcome'`, raised by the local import inside the test body —
`TestStoreWriteErrorLeaf.test_str_and_attributes_and_subclassing`,
`TestAddConversationMemoryOutcomes.test_failed_add_raises_store_write_error`,
`TestStoreInteractionFailureModes.test_store_write_error_from_chroma_propagates_unchanged`,
`TestStoreInteractionFailureModes.test_corpus_add_entry_failure_wraps_as_store_write_error`,
`TestCoordinatorStoreInteraction.test_raise_still_syncs_context_back`,
`TestBackgroundStoreInteraction.test_store_write_error_logged_and_transcript_still_recorded`.

Passed (8, unedited-tree controls, as expected): `test_success_returns_doc_id` (chroma),
`test_success_control_returns_id` (storage), the 4 parametrized deliberate-skip tests,
`test_success_control_syncs_and_returns` (coordinator), `test_success_control_unchanged`
(handler).

data/ listing after this chunk: unchanged (`diff` against `$S/data_baseline_f7a_rerun.txt`
→ 0 lines). `logs/` still absent.

## 4. Source edits

Per OWNERSHIP/CONTRACT:
- `utils/retrieval_outcome.py`: one-line module-docstring addition; new
  `StoreWriteError(RuntimeError)` class (keyword-only `source`/`reason`, `str` =
  "source: reason") added after `RetrievalError`. No other change.
- `memory/storage/multi_collection_chroma_store.py`: extended the existing
  `from utils.retrieval_outcome import RetrievalError` line to add
  `StoreWriteError`; `add_conversation_memory`'s except now `raise
  StoreWriteError(source="chroma_conversations", reason=type(e).__name__) from e`
  (log line and traceback kept, dropped nothing).
- `memory/memory_storage.py`: added `from utils.retrieval_outcome import
  StoreWriteError`; the module-header docstring's `store_interaction` bullet and
  the method's own `Returns:` docstring updated; `store_interaction`'s except now
  `except StoreWriteError: raise` (unchanged propagation) before `except
  Exception as e: ... raise StoreWriteError(source="store_interaction",
  reason=type(e).__name__) from e` (log line and traceback kept).
- `memory/memory_coordinator.py`: `store_interaction`'s docstring updated; the
  storage call and the two sync-back lines are now `try: ... finally: ...`, so the
  sync-back runs even when storage raises; the exception then propagates
  unchanged (thread resolution below is unreached on a raise, by ordinary control
  flow — no additional guard needed).
- `memory/memory_interface.py`: `store_interaction`'s docstring `Returns:` line
  updated (protocol body is `...`, unchanged).
- `gui/handlers.py`: `_background_store_interaction`'s docstring gained one
  sentence; a local `from utils.retrieval_outcome import StoreWriteError` and
  `memory_id = None` were added; the original single try/except is now a nested
  try (inner `except StoreWriteError as e: logger.error(...)` — same log line as
  before — falls through to the transcript `log_interaction` call with
  `db_id=None`); the outer `except Exception as e: logger.error(...)` is
  unchanged and still catches everything else (nothing escapes the task). The
  success path (memory_id set, info log, transcript call) is byte-for-byte
  unchanged.

Digests after edit:
```
1abc1586534171b05e3a06fd976d08eba76cb6ecdfef565944f4dcdebf4c95eb  utils/retrieval_outcome.py
5af28e2bbf5f2d3de968a8398e095cae9a2e1265aca68a8fe93c57d2101c6327  memory/storage/multi_collection_chroma_store.py
e8f3b6e8844e601653005cc4b2b8ad4f8ecf89afd702e0cb06629fc00f20e018  memory/memory_storage.py
726b1d2a560048a998965983a466867a4df057e4bcb6c54ea2d57d88675d630f  memory/memory_coordinator.py
d17701d99af670cea2914f1210ef5929ebb24247a1118e44503628d23f7abb1c  memory/memory_interface.py
9cf718e47c2672b1e926da3e217d66048af5a59a13d2e2ab8d56fe51c5391e31  gui/handlers.py
```

### Size accounting (methodology note)

`gui/handlers.py`, `memory/storage/multi_collection_chroma_store.py`,
`memory/memory_storage.py`, `memory/memory_coordinator.py` and
`memory/memory_interface.py` were ALL already modified by earlier batches
before F10a started (confirmed by the initial `git status --short`), and
`utils/retrieval_outcome.py` is untracked (created by F1, never `git add`ed).
`git diff --stat` against HEAD (328a8ec) therefore reports each file's FULL
accumulated diff since the base commit, not this batch's own contribution
(e.g. `gui/handlers.py` alone shows ~500 lines touched by many unrelated
prior hunks). Per-file size below is computed either from `git diff
--numstat` for files where this batch's edit is the file's ONLY hunk since
HEAD (`memory_coordinator.py`, `memory_interface.py`; confirmed no other `@@`
hunks exist), or by isolating exactly this batch's own hunk(s) inside a
larger diff via the unique `@@` hunk header(s) for that edit
(`multi_collection_chroma_store.py`, `gui/handlers.py`, cross-checked against
a direct diff of each edit's own before/after text) — never the whole-file
`git diff --stat` number. `memory/memory_storage.py`'s 4 hunks were confirmed
to correspond 1:1 to this batch's 4 edits (matching line ranges), and its
`--numstat` total was independently reproduced by summing each edit's own
before/after diff, so it is trusted directly. `utils/retrieval_outcome.py`
(untracked) is sized from each edit's own before/after text diff, since
plain `git diff` shows nothing for an untracked file and the file's TOTAL
line count would wrongly include F1's unrelated prior content.

Per-file changed lines (insertions + deletions, this batch only):
```
utils/retrieval_outcome.py                        17  (2 doc + 15 class, both pure insertions)
memory/storage/multi_collection_chroma_store.py     4  (2 single-line replacements)
memory/memory_storage.py                           10  (8 ins + 2 del; numstat, file's only hunks)
memory/memory_coordinator.py                       26  (15 ins + 11 del; numstat, file's only hunk)
memory/memory_interface.py                          4  (3 ins + 1 del; numstat, file's only hunk)
gui/handlers.py                                    35  (23 ins + 12 del; isolated hunk)
                                                    ---
source subtotal                                    96
tests/unit/test_store_write_outcomes.py (new)      330  (wc -l)
                                                    ---
TOTAL                                              426
```
426 is over the 380 target but under the 450 hard cap — the same band as F9b's
427 (source 54 + test 373). The overage is almost entirely the new test file:
fakes for four independent components (chroma collection, storage fixture,
coordinator, handler) plus paired positive controls for every behavior
(BC-64/FIXTURE RULE). No split proposal is needed (only required when the
hard cap would be exceeded).

Note: after this section was written, the test file's module docstring (comments
only, no test logic/assertions changed) was trimmed for size; the recorded
failing-first result (6 failed, 8 passed) remains valid — a docstring change
does not affect collection or execution. Final test file: 330 lines (down
from 350), giving the totals in the "Size accounting" section above.

`python -m ruff check` on all 7 changed files (the 6 source files + the new test
file): **All checks passed!** (ruff 0.14.9).

## 5. Focused tests (guarded, capped, `-p asyncio`)

- **Chunk 1** (MemAvailable 6464 MB before): `test_store_write_outcomes.py`,
  `test_retrieval_outcome.py`, `test_api_error_storage_guard.py`,
  `test_thinking_leak_storage.py`, `test_memory_file_error_filter.py`,
  `test_provenance.py`, `test_entity_facts.py`, `test_calendar_turn_round3.py`,
  `test_handle_submit.py` → **238 passed, 0 failed** (60.70s, RSS 3,246,976 KB ≈
  3,171 MB). data/ unchanged after chunk (diff → 0 lines); `logs/` absent.
- **Chunk 2** (MemAvailable 6635 MB before): `test_process_user_query.py`,
  `test_request_path_parity.py`, `test_api_chat.py`,
  `test_action_guard_integration.py`, `test_orchestrator_helpers.py`,
  `test_extraction_replay_provenance.py`, `test_knowledge_graph.py`,
  `test_fact_verification.py`, `test_stance_write_path.py` → **276 passed, 0
  failed** (12.78s, RSS 1,735,612 KB ≈ 1,695 MB). data/ unchanged; `logs/`
  absent.
- **Chunk 3** (MemAvailable 6622 MB before): `test_store_get_by_id_outcomes.py`,
  `test_expander_outcomes.py`, `test_chroma_store_no_autodelete.py`,
  `test_recent_summaries_fix.py` → **39 passed, 0 failed** (4.00s, RSS
  1,106,156 KB ≈ 1,080 MB). data/ unchanged; `logs/` absent.

**Focused total: 553 passed, 0 failed.**

## 6. Sweep: candidate set and vetting

`grep -rlE "memory\.memory_storage|MemoryStorage|memory\.memory_coordinator|MemoryCoordinator|memory\.memory_interface|utils\.retrieval_outcome|multi_collection_chroma_store|store_interaction|_background_store_interaction" tests/unit/*.py`
found 55 files. Removing the 22 already run in the 3 focused chunks above and
excluding `tests/unit/test_graph_integration.py` (never run, per rules — the
`ContextGatherer`/`MagicMock(spec=[])`/real-`UserProfile()` hazard) leaves 32
sweep candidates.

Vetting (before running any of them): grepped all 32 for
`ContextGatherer\(|UserProfile\(\)|chromadb\.(Persistent)?Client|PersistentClient`,
`SentenceTransformer\(|sentence_transformers\.`, `MultiCollectionChromaStore\(`
(real init, not `__new__`), `["']data/` / default rate-limiter/cache
constructors, and `= MemoryStorage\(|= MemoryCoordinator\(` (real
construction). Only one file matched (`test_gatherer_outcomes_memory.py`,
matching inside its OWN docstring/comment text: "never constructs a real
UserProfile() or ContextGatherer()" — read in full; it builds gatherers via
`.__new__` + attribute injection only, confirmed safe, matches the F9a/F9b
pattern). `test_sep09_curation_integrity.py` and `test_sep09_storage_repairs.py`
construct a real `chromadb.EphemeralClient` (in-memory, no disk path) and
`UserProfile(str(tmp_path / "profile.json"))` (tmp_path-backed) — both safe,
no default/data path touched. Spot-checked `test_memory_interface.py`
(dataclass-only tests, no store construction) and `test_gate_min_results_cap.py`
(`inspect.getsource(MemoryCoordinator.get_memories)` — source introspection,
no instantiation). All 32 vetted safe to run.

Sweep candidates (32, 4 chunks of ≤9):
```
Chunk 1: test_backfill_stance.py, test_claim_tracker.py, test_gate_min_results_cap.py,
  test_gate_retrieval_space.py, test_gatherer_outcomes_background_knowledge.py,
  test_gatherer_outcomes_memory.py, test_gatherer_outcomes_notes_docs_uploads.py,
  test_gatherer_outcomes_remaining_knowledge.py, test_gatherer_outcomes_web.py
Chunk 2: test_graph_species_guard.py, test_hybrid_semantic_score.py,
  test_memory_coordinator_methods.py, test_memory_interface.py,
  test_obsidian_failure_outcomes.py, test_ordered_slice_guard.py,
  test_probe_insight_sweep_relevance.py, test_prompt_builder_self_report_trim.py,
  test_proposal_generator.py
Chunk 3: test_refdocs_failure_outcomes.py, test_reflection_retrieval.py,
  test_retrieval_pool_caps.py, test_section_outcomes.py,
  test_semantic_visual_failure_outcomes.py, test_sep03_cat_session_fixes.py,
  test_sep03_followups_continuity.py, test_sep09_curation_integrity.py,
  test_sep09_storage_repairs.py
Chunk 4: test_sep10_web_search_gap.py, test_source_excerpt_pipeline.py,
  test_syllabus_turn_misfires.py, test_tavily_failure_outcomes.py,
  test_upload_retrieval_pool.py
```
Excluded and recorded (never run): `tests/unit/test_graph_integration.py`
(ContextGatherer/UserProfile default-path hazard, per rules), plus the standing
exclusions `tests/test_web_search_manager.py`, `tests/test_prompt_internal_methods.py`
(neither matched the importer grep anyway) and all top-level `tests/` files.

## 7. Sweep run (guarded, capped, `-p asyncio`)

- **Chunk 1** (MemAvailable 6621 MB before): 9 files → **179 passed, 0 failed**
  (9.80s, RSS 1,306,188 KB ≈ 1,276 MB). data/ unchanged; `logs/` absent.
- **Chunk 2** (MemAvailable 6607 MB before): 9 files → **247 passed, 0 failed**
  (9.52s, RSS 1,244,500 KB ≈ 1,215 MB; one pre-existing unrelated
  `RuntimeWarning: coroutine ... was never awaited` from an `AsyncMockMixin` in
  `core/prompt/builder.py` — not touched by this batch). data/ unchanged;
  `logs/` absent.
- **Chunk 3** (MemAvailable 6589 MB before): 9 files (incl. the two real
  `chromadb.EphemeralClient` tests) → **235 passed, 0 failed** (9.91s, RSS
  1,333,868 KB ≈ 1,303 MB). data/ unchanged; `logs/` absent.
- **Chunk 4** (MemAvailable 6565 MB before): 5 files → **133 passed, 0 failed**
  (7.34s, RSS 1,139,412 KB ≈ 1,113 MB). data/ unchanged; `logs/` absent.

**Sweep total: 794 passed, 0 failed.**

**Grand total (focused + sweep): 1,347 passed, 0 failed, across 54 test files
(22 focused + 32 sweep).**

## 8. Post-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (gate; unrelated pre-existing findings). stderr empty.

Summary row comparison (pre-edit → post-edit):
```
dm01_raw_substring            7/12/0/5   → 7/12/0/5    (unchanged)
dm17_apply_without_guard      36/39/0/3  → 36/39/0/3   (unchanged; files 631→632, the new test file)
dm18_except_returns_empty     47/79/0/32 → 45/79/0/34  (found -2, stale +2, new 0)
dm31_live_state_default       3/3/0/0    → 3/3/0/0     (unchanged)
```
`dm18` found dropped by exactly 2 and stale rose by exactly 2, **new = 0** — the
predicted outcome. The STALE detail list (post-edit) now contains:
```
dm18_except_returns_empty: memory/memory_storage.py [MemoryStorage.store_interaction] 'return None'
dm18_except_returns_empty: memory/storage/multi_collection_chroma_store.py [MultiCollectionChromaStore.add_conversation_memory] 'return None'
```
(next to F9a's `get_by_id` and F9b's `_fetch_conversations_in_range`, already STALE) —
confirming #127 and #140 went STALE as expected, with zero new findings anywhere.

## 9. BC-58 sibling search

Production callers of the two edited write methods (grep for `.add_conversation_memory(`
and `.store_interaction(` outside `tests/`, `docs/`, `data/`, `.agent_snapshots/`):
- `add_conversation_memory`: only `memory/memory_storage.py:1003` (edited this batch)
  and `scripts/migrate_corpus_to_chromadb.py:136` (read-only; per the brief's
  PARENT-VERIFIED FACTS, its per-row `except Exception` at 142-145 already counts
  failures, so a raise is now counted correctly instead of miscounted as migrated).
- `store_interaction` (MemoryStorage / MemoryCoordinator): `memory/memory_coordinator.py:369`
  (edited), and the production callers already mapped in the brief —
  `core/orchestrator.py` `_handle_deictic`/`_maybe_document_generation`/
  `_maybe_agentic_search`/`_store_interaction` (read-only, F13/interim, see §10) and
  `gui/handlers.py` `_background_store_interaction` (edited) plus the other
  `except: pass` sites (`_run_doc_generation`, `_save_daemon_note`,
  `_run_action_retry` — read-only, F13).
- Siblings still swallowing on the SAME write pattern (BC-58 class), left
  unfixed and out of scope for F10a: `memory/proposal_store.py
  ProposalStore.store_proposal` (#131 → F11b-1), `memory/thread_store.py
  ThreadStore.store_thread` (#146 → F11a-1), `MemoryStorage.store_skill` (#128,
  #129 → F10b), and the store's own `search_all` / `query_multiple_collections`
  / `update_metadata` / `get_collection_stats` (no anchor; unedited, per
  OWNERSHIP). `corpus_manager.save_corpus`'s own swallow of its disk-failure
  (noted in the brief, sibling to the corpus `add_entry` call inside
  `store_interaction`) is unedited — it is now a body exception `store_interaction`
  itself will catch and wrap as `StoreWriteError(source="store_interaction", ...)`
  if `add_entry` propagates, but `save_corpus`'s OWN internal try/except is
  untouched and out of OWNERSHIP.

## 10. Privacy / no-network note

No real institution names or real IPs were introduced; the test file's only
free-text markers are synthetic (`SYNTH_MARKER_f10a_9d3e7b21`), asserted
ABSENT from every `StoreWriteError` reason/str and from the handler's caplog
text. `StoreWriteError` itself carries only a `source` label and an exception
class name (`type(e).__name__`), per CONTRACT #7 — never the exception's
message text. No test constructs a real network client, real Chroma
persistent client (only `EphemeralClient`, in-memory, in two pre-existing
sibling tests untouched by this batch), real embedder, or LLM/API call; all
env vars in the test command are the standing offline/CPU-only set
(`HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, `CHROMA_DEVICE=cpu`, etc.).
`data/` and `logs/` were checked before the first pytest and after every one
of the 7 chunks (3 focused + 4 sweep) plus the failing-first run — all 8
listings are identical to `$S/data_baseline_f7a_rerun.txt`; `logs/` stayed
absent throughout.

## 11. Milestone

Focused contract green (553 passed) plus the full sweep green (794 passed);
1,347 passed / 0 failed total; ruff clean; scan confirms exactly the intended
#127 and #140 anchors moved to STALE with 0 new findings anywhere. Response
file `docs/execution/generalization/class_guard_responses/CGR-20260913-010.md`
(BASE) written next.

## 12. Final state check (pre-handoff)

```
git rev-parse HEAD    # 328a8ecea1dae91de16f5974ea71727c4e2e1aa0 (unchanged; no commits made)
git status --short    # 118 lines: the pre-batch tracked-M set plus new M lines for the
                       # 6 owned source files, plus this batch's ONE new untracked file
                       # (tests/unit/test_store_write_outcomes.py) inside the already-
                       # untracked docs/execution/generalization/ tree's batch file and
                       # the (not-yet-written) response file
git diff --stat        # 72 files changed, 3677 insertions(+), 715 deletions(-)
```
The only NEW untracked file at the repository root or elsewhere outside
`docs/execution/generalization/` is `tests/unit/test_store_write_outcomes.py`;
every other untracked path (`scan_pre.stderr`, `utils/retrieval_outcome.py`,
and ~40 other test files) predates this batch (other workers' in-flight
files). No stray files anywhere. No `git add` in any form was used; no
commit, stash, reset, checkout or restore was run. `git remote -v` unchanged
(`origin` fetch = `/home/lukeh/Daemon_v1`, push = `DISABLED`).

Batch F10a: **COMPLETE**. Response file written next.

## Parent review and integration (2026-09-14)

Accepted as delivered. No COMPLIANCE breach and no FIXTURE RULE edits. The size is 426 changed lines (96 source plus 330 test), over the 380 target but under the 450 hard cap.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F9b.txt` shows five mismatches.
  - Three are this batch's edits to files already in the manifest:
    - `utils/retrieval_outcome.py`: `16f7bcb9…` → `1abc1586…`;
    - `memory/storage/multi_collection_chroma_store.py`: `66833584…` → `5af28e2b…`;
    - `gui/handlers.py`: `017d8538…` → `9cf718e4…`.
  - Two are parent edits made while F10a ran: `briefs/PARENT_STATE.md` and `briefs/R_common_rules.md`. The rules edit adds the `test_daily_notes_auto_update.py` exclusion from the F12 map.
- **New paths (six):**
  - `memory/memory_storage.py` (`efadf44f…` → `e8f3b6e8…`);
  - `memory/memory_coordinator.py` (`dcac5128…` → `726b1d2a…`);
  - `memory/memory_interface.py` (`f29c967c…` → `d17701d9…`);
  - `tests/unit/test_store_write_outcomes.py` (`98c9c180…`, 330 lines);
  - `class_guard_responses/CGR-20260913-010.md` (`2318acde…`, 295 lines);
  - this file.
- **Unchanged:** `core/orchestrator.py` is still `e336883d…`.
- **Git state:**
  - `git diff --cached --name-only` is empty;
  - HEAD `328a8ec` is unchanged;
  - the reflog shows only the clone entry, and the stash list is empty.
- **Stray files:** none new. The only untracked file at the repository root is `scan_pre.stderr`.
- **`data/`:** the 7-entry baseline is unchanged, and `logs/` is absent.
- **Parent edits while F10a ran or after it returned:**
  - `briefs/PARENT_STATE.md` gained the F11b and F12 map decisions and F10a's integration.
  - `briefs/R_common_rules.md` gained the `test_daily_notes_auto_update.py` exclusion.
  - `briefs/F10b.md` was written with post-F10a line numbers.
  - The F11a-1, F11a-2, F11b-1, F11b-2 and F12a–F12d drafts live in the parent scratchpad.
  - A read-only F13 map was started once this batch returned.
- **Recorded tree:** `manifest_post_F10a.txt` records the tree after this section.

### Code review (the parent read every diff)

- **Leaf:** `StoreWriteError(RuntimeError)` sits after `RetrievalError`, with keyword-only `source`/`reason` and `str` "source: reason". Its docstring says a deliberate skip stays None, and the module docstring gains one line. No existing name changed.
- **`add_conversation_memory`:** the except keeps its log line and `traceback.print_exc()`, then raises `StoreWriteError(source="chroma_conversations", reason=<class>) from e`. The import line was extended (`RetrievalError, StoreWriteError`).
- **`store_interaction`:**
  - `except StoreWriteError: raise` comes before `except Exception as e`, which keeps its log line and raises `StoreWriteError(source="store_interaction", reason=<class>) from e`.
  - The four deliberate skips and the inner best-effort excepts are unchanged.
  - The module header and method docstrings are updated.
- **Coordinator:**
  - The storage call sits inside `try`/`finally`, and the `finally` syncs `conversation_context` and `interactions_since_consolidation` back.
  - Quick thread resolution runs only after a non-raising call, because the raise leaves before it.
  - The docstring is updated.
- **Interface:** docstring only.
- **`_background_store_interaction`:**
  - `memory_id = None`, then an inner `try` around the store call with the "complete" log.
  - `except StoreWriteError as e` logs "Background storage failed: {e}" (labels only), and the transcript `log_interaction` then runs with `db_id=None` and the same sanitized response and metadata.
  - The outer `except Exception` is unchanged. `store_interaction` now wraps every body failure as `StoreWriteError`, so only non-storage failures reach it, as before.
- **Callers (parent grep):**
  - The orchestrator store sites are unchanged: 2018, 2181, 2313, 2490.
  - The handler sites moved down 11 lines with this edit: `_run_doc_generation` ≈1714, `_save_daemon_note` ≈2608, `_run_action_retry` ≈3509.
  - `scripts/migrate_corpus_to_chromadb.py:136` now counts a raised failure in its own per-row except.

### Tests (the parent read the whole new file)

- **Leaf:** `str`, attributes, `RuntimeError` subclass, not a `RetrievalError`.
- **Chroma:** a raising `.add` gives `StoreWriteError` with the labels and no marker; success gives a doc id.
- **Storage:**
  - a chroma `StoreWriteError` propagates as the same object;
  - a corpus `add_entry` failure is wrapped as `store_interaction`/`RuntimeError`, with no marker and chroma never reached;
  - success gives the id;
  - the four parametrized deliberate skips give None with no corpus and no chroma write.
- **Coordinator:**
  - a raise propagates the same object, context and counter are synced back, and `list_open_threads` is never called;
  - success syncs, returns and runs the quick resolution.
- **Handler:**
  - a `StoreWriteError` is logged with no marker in caplog, and `log_interaction` is called once with `db_id is None` and the sanitized response;
  - success gives `db_id == "mem-42"`.
- **Import placement:** `StoreWriteError` is imported inside each test, so the controls ran on the unedited leaf. Failing-first gave 6 failed (ImportError) and 8 passed.

### Parent rerun (guarded, capped, `-p asyncio`)

| Chunk | MemAvailable | Passed | Time | Peak RSS (KB) |
|---|---|---|---|---|
| Focused 1 (9 files) | 6,577 MB | **238** | 22.80 s | 3,247,076 |
| Focused 2 (9 files) | 6,599 MB | **276** | 16.40 s | 1,735,392 |
| Focused 3 (4 files) | 6,602 MB | **39** | 6.80 s | 1,107,148 |
| Sweep 1 (9 files) | 6,560 MB | **179** | 9.88 s | 1,308,404 |
| Sweep 2 (9 files) | 6,580 MB | **247** | 10.74 s | 1,244,920 |
| Sweep 3 (9 files) | 6,558 MB | **235** | 9.59 s | 1,335,108 |
| Sweep 4 (5 files) | 6,543 MB | **133** | 7.48 s | 1,139,592 |

- **Total:** **1,347 passed, 0 failed**, matching the worker.
- **Importer set:** the parent's independent grep found 55 files. That is 22 focused plus 32 sweep plus the excluded `test_graph_integration.py`, identical to §6.
- **`data/`:** unchanged after every chunk, and `logs/` is absent.
- **ruff 0.14.9:** clean on all seven files.
- **Scan:**
  - dm18 moved from found 47 / base 79 / new 0 / stale 32 to 45 / 79 / 0 / 34.
  - `MemoryStorage.store_interaction 'return None'` and `MultiCollectionChromaStore.add_conversation_memory 'return None'` are now in the STALE list, which has 42 rows in total.
  - dm17's file count went from 631 to 632. dm01 (7/12/0/5) and dm31 (3/3/0/0) are unchanged, and scan stderr is empty.

### Size

426 changed lines, within the 450 cap.

### Limitations carried forward

- **Until F13:** the five `except Exception: pass` sites (orchestrator doc-generation and agentic search; handlers doc-generation, daemon note and action retry) still silently ignore a raised `StoreWriteError`, exactly as they ignored None. There is no turn-record receipt yet.
- **Out of scope and unedited:**
  - `save_corpus`'s own disk-failure swallow;
  - the fallback coordinators that always return None;
  - the store siblings `search_all`, `query_multiple_collections`, `update_metadata` and `get_collection_stats`.

### Class-guard status

- **CGR-010:**
  - #127 and #140 are answered (base response).
  - #128 and #129 → F10b (`-2`).
  - #146 → F11a-1 (`-3`).
  - #131 → F11b-1 (`-4`).
  - Receipts and the five `pass` sites → F13.
- **CGR-009:**
  - #139 and #122 are answered.
  - #130 → F10b (`-3`).
