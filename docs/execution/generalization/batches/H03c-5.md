# H03c-5 evidence packet

Batch: H03c-5 — replace the real-looking example contact first name (EN) in the
contacts/email tests and the agentic tool-schema example with "Maren". Labels
only per brief; no real strings appear in this file.

## 1. Setup / manifest check

- 2026-09-15 12:38:03 CDT: `git status --short web` diffed clean against
  `$S/web_status_post_F13c-2a.txt` (WEB STATUS MATCHES BASELINE). MemAvailable
  = 6175 MB.
- 2026-09-15 12:38:03 CDT: HEAD = 328a8ecea1dae91de16f5974ea71727c4e2e1aa0
  (branch master). origin fetch = /home/lukeh/Daemon_v1, origin push =
  DISABLED (verified via `git remote -v` only). Pre-existing `git status
  --short` shows 262 modified/untracked paths (prior batches' work, not
  mine); `git diff --stat` tail: `200 files changed, 5489 insertions(+),
  1669 deletions(-)`.
- 2026-09-15 12:38:06 CDT: `sha256sum -c $S/manifest_pre_H03c-5.txt --quiet`
  → exit 0, 0 mismatch lines. All 431 manifest entries match the current
  tree pre-edit.

(This file created before any pre-edit scan or edit, per ORDER.)

## 2. Pre-edit scan (read-only)

- 2026-09-15 12:38:19 CDT: `python scripts/check_bug_classes.py scan --root .`
  → exit 1 (STALE entries present, expected doctrine; stderr empty).
  Summary table: dm01 396/7/12/0/5, dm16 395/89 (report-only), dm17
  648/36/39/0/3, dm18 123/23/79/0/56, dm29 0/0, dm31 396/3/3/0/0, catalog
  1/0/0/0/0. dm18 = found 23 / base 79 / new 0 / stale 56 and dm17 files =
  648 — both match the brief's expected values exactly.

## 3. Collision checks (launch)

- 2026-09-15 12:38:39 CDT: repo-wide case-insensitive grep for the mapped
  replacement "Maren" (brief's excludes applied): 11 files hit, all either
  docs/execution (briefs/batches, including this packet's own title line
  and `PARENT_STATE.md`) or files explicitly recorded as already handled by
  H03c-3a/H03c-3b (`core/agentic/gate.py`,
  `tests/unit/test_ingest_turn_misfires.py`,
  `tests/unit/test_sep10_probe_dump_actions.py`). Per-file occurrence
  counts: core/agentic/gate.py 1, docs/ARCHITECTURE_GUIDE.md 1,
  batches/H03c-3a.md 4, batches/H03c-3b.md 1, batches/H03c-5.md 1 (this
  packet's title line, the synthetic replacement value — safe to state per
  the established convention), briefs/H03c-3a.md 3, briefs/H03c-3b.md 2,
  briefs/H03c-5.md 3, briefs/PARENT_STATE.md 1,
  test_ingest_turn_misfires.py 1, test_sep10_probe_dump_actions.py 1.
- 2026-09-15 12:39:02 CDT: none of the 6 scope files (types.py,
  test_agentic_search.py, test_agentic_gate.py, test_gmail_send.py,
  test_google_contacts.py, test_sep08_ops_fixes.py) already contain "Maren"
  (0 hits each). The brief's collision-check line says "5 target files";
  SCOPE lists 6 files (types.py plus 5 test files) — noting this as an
  informational count mismatch in the brief's own wording, not a conflict:
  all 6 scope files were checked and all 6 are confirmed clean.
- 2026-09-15 12:39:04 CDT: the other first name queried at
  `test_google_contacts.py:429` is a different, unrelated first name
  ("John") — confirmed by reading lines 425-432. "Maren" differs from it in
  every case form. No collision.

## 4. Pre-copies and digest verification

- 2026-09-15 12:39:22 CDT: all 6 scope files' sha256 recomputed and
  compared against the brief's 6 digest prefixes — exact match for every
  file (types.py 98e1229c…, test_agentic_search.py 206c7b13…,
  test_agentic_gate.py 57d7c44e…, test_gmail_send.py 86e1f7bc…,
  test_google_contacts.py e770e185…, test_sep08_ops_fixes.py e7a4572a…).
- 2026-09-15 12:39:29 CDT: all 6 files copied to `$S/h03c5_pre/<relpath>`;
  `sha256sum` of each pre-copy matches its live counterpart exactly (6/6
  pairs identical).

## 5. Scope reconciliation (real-form greps; output not copied into this file)

Case-insensitive, non-word-bounded grep for the EN real form run against
each of the 6 scope files, line numbers only, reconciled against the
brief's line lists:
- `core/agentic/types.py`: found {1167}. Brief: {1167}. Exact match, 0
  extras/misses.
- `tests/test_agentic_search.py`: found {421, 426}. Brief: {421, 426}.
  Exact match.
- `tests/unit/test_agentic_gate.py`: found {84}. Brief: {84}. Exact match.
- `tests/unit/test_gmail_send.py`: found {310, 315, 332, 337(×3), 362,
  367} — 8 occurrences across 6 lines. Brief: 310, 332, 362 (recipient);
  315 (EN-lower email); 337 (error string, EN ×3); 367 — same 6 lines, 8
  occurrences. Exact match.
- `tests/unit/test_google_contacts.py`: found 42 distinct lines / 43
  occurrences (441 carries 2 occurrences on one line): {52, 53, 63, 64, 69,
  70, 80, 96, 103, 113, 122, 132, 142, 149, 164, 167, 168, 179, 182, 183,
  193, 217, 232, 248, 263, 264, 271, 272, 299, 303, 312, 313, 331, 335,
  359, 398, 399, 413, 415, 428, 431, 441}. Brief's 41 required lines (13
  fixtures + 21 queries + 7 exact assertions) are all present with 0
  misses; the one extra is line 431, which the brief marks optional
  ("swap it only if it is plain comment text"). Read at 12:39:04 CDT:
  line 431 is `# warmup(cached after first) + search_<EN-lower> +
  search_john = 3 calls` — plain comment text (an identifier-style
  substring reference), confirmed eligible; will be swapped.
- `tests/unit/test_sep08_ops_fixes.py`: found {126, 145, 166, 178, 185,
  206}. Brief: {126, 145, 166, 178, 185, 206}. Exact match.

Pairing checks (read full context, offsets 40-140, 140-280, 280-380,
415-445 of test_google_contacts.py, plus test_gmail_send.py 300-369 and
test_sep08_ops_fixes.py 115-214): 52/53 and 63/64 (SINGLE_CONTACT_RESPONSE
and the first MULTIPLE_CONTACTS_RESPONSE entry, both "<EN> Smith" /
"<EN-lower>@example.com") correspond to assertions 167/168 and 441 (same
values, `_parse_contacts_response` reuses SINGLE_CONTACT_RESPONSE); 70 is
the dotted-email second MULTIPLE_CONTACTS_RESPONSE entry
("<EN-lower>.jones@work.com"), pairing with assertion 183; 264
("<EN-lower>@saved.com") pairs with assertion 303; 313
("<EN-lower>@same.com") pairs with assertion 335. The saved/other emails
at 264/272 stay distinct after replacement (different domains,
`@saved.com` vs `@other.com`); 312 (bare "<EN>") and 313
("<EN-lower>@same.com") stay a consistent same-entity pair (both map to
the same replacement token family). Confirmed a uniform 1:1 token swap
(same replacement string per casing, applied via `replace_all` per file)
preserves every pairing and the `(endpoint, query.lower())` cache-key
relationships in `TestCache`, since each call site keeps its pre-existing
value equal/distinct relationship to the others (test_different_queries_
cached_separately's two calls, 428 "<EN>" vs 429 "John", stay distinct).

No misses across any of the 6 files. One explicitly-optional extra
(contacts.py:431) accepted per the brief's own instruction. No occurrence
found outside the 6 scope files' listed/optional lines.

## 6. SIZE estimate (before editing)

Distinct changed-line count: types.py 1 + test_agentic_search.py 2 +
test_agentic_gate.py 1 + test_gmail_send.py 6 + test_google_contacts.py 42
(41 required + 1 optional) + test_sep08_ops_fixes.py 6 = **58 changed
lines** (57 required + 1 optional). Matches the brief's "≈57" estimate
(57 without the optional line, 58 with it). Well under the ≤150 target and
far under the 450 hard stop. Proceeding with the full contract as scoped,
including the optional line.

## 7. Baseline test runs (BEFORE any Edit call on a scope file)

- 2026-09-15 12:41:49 CDT: guard clear (no RUNNING pytest), MemAvailable
  6170 MB.
- 2026-09-15 12:41:56 CDT: data/ listing (top-level) matches
  `$S/data_baseline_post_F12a.txt` exactly (benchmark_per_case.csv,
  chroma_db_v4/, embedding_migration_manifest.json, pipeline/); logs/ does
  not exist in this clone (expected).
- Importer choice for `core.agentic.types`: grepped
  `core\.agentic\.types|from core\.agentic import (.*[ ,])?types\b` over
  `tests/unit` (29 importers found); chose
  `tests/unit/test_tool_wiring_parity.py` (small, directly exercises tool
  schema wiring) as "at least one" importer per the brief's TESTS section.
- **Chunk 1** (5 files: the 4 brief-named test files plus the importer),
  digests recorded in the same command as the run, 2026-09-15 12:42:02 CDT
  start / 12:42:13 CDT end: `tests/unit/test_agentic_gate.py`,
  `tests/unit/test_gmail_send.py`, `tests/unit/test_google_contacts.py`,
  `tests/unit/test_sep08_ops_fixes.py`, `tests/unit/test_tool_wiring_parity.py`
  (digests for these 5 plus `core/agentic/types.py` and
  `tests/test_agentic_search.py` all recorded in the same command — all 7
  match the pre-copy/brief digests exactly). Command: systemd-run --user
  --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=.../scripts/bin python -m
  pytest -q -p no:cacheprovider -p asyncio <5 files>. Result: **107
  passed**, exit 0, 7.56s elapsed (pytest), 0:10.78 wall (time -v),
  Maximum resident set size 1714092 KB.
- Per-file (via a separate guarded `--collect-only` run, 12:42:25-12:42:27
  CDT and 12:42:38-12:42:40 CDT, matching 107 total): test_agentic_gate 48,
  test_gmail_send 16, test_google_contacts 27, test_sep08_ops_fixes 10,
  test_tool_wiring_parity 6.
- 2026-09-15 12:42:49 CDT: data/ listing after chunk 1: unchanged (matches
  `$S/data_baseline_post_F12a.txt`); logs/ absent; `git status --short web`
  still matches `$S/web_status_post_F13c-2a.txt`.

BASELINE TOTAL: **107 passed**, 0 failed, across all 5 baseline test
files, before any Edit call. `tests/test_agentic_search.py` is verified
read-only (not executed, per the brief and the top-level-tests
prohibition) — its digest was captured in the same command as chunk 1 for
later post-edit comparison. This satisfies the ORDER rule: the first Edit
call below comes strictly after this section.

## 8. All edits

First Edit call: 2026-09-15 12:44:38 CDT (`date` run immediately before it;
`core/agentic/types.py`). Strictly after every baseline chunk (last
baseline/data-check timestamp 12:42:49 CDT). All 6 scope files edited
2026-09-15 12:44:38-12:45:01 CDT via the Edit tool only (no scripts), each
as a whole-file `replace_all` pass per casing present (capitalized EN pass,
then a separate lowercase EN pass where that casing appeared): types.py
(single edit, unique occurrence), test_agentic_search.py (1 pass,
capitalized only, 2 occurrences), test_agentic_gate.py (single edit,
unique occurrence), test_gmail_send.py (2 passes: capitalized 7
occurrences across 5 lines incl. line 337's 3x, then lowercase 1
occurrence), test_google_contacts.py (2 passes: capitalized 30
occurrences, then lowercase 13 occurrences), test_sep08_ops_fixes.py
(1 pass, capitalized only, 6 occurrences). Every replacement used the
real EN/EN-lower form as `old_string` and the brief's mapped "Maren"/
"maren" as `new_string`.

Pre-edit per-file case-sensitive occurrence counts (verified via grep
before editing, matching the Section 5 reconciliation exactly): types.py
1 (cap), test_agentic_search.py 2 (cap), test_agentic_gate.py 1 (cap),
test_gmail_send.py 7 (cap, 5 lines incl. one x3) + 1 (lower) = 8,
test_google_contacts.py 30 (cap) + 13 (lower) = 43, test_sep08_ops_fixes.py
6 (cap). Sum = 61 occurrences (58 distinct changed lines, matching the
SIZE estimate).

## 9. Privacy self-check (performed after ALL edits)

- 2026-09-15 12:45:01 CDT: post-edit case-insensitive real-form grep
  (`[Mm][Ee][Aa][Gg][Aa][Nn]`) across all 6 edited files: 0 hits in
  every file (real form fully removed).
- 2026-09-15 12:45:01 CDT: post-edit "Maren"-family (case-insensitive)
  occurrence counts per file: types.py 1, test_agentic_search.py 2,
  test_agentic_gate.py 1, test_gmail_send.py 8, test_google_contacts.py
  43, test_sep08_ops_fixes.py 6 -- identical to the pre-edit real-form
  occurrence counts per file (61 total), confirming a clean 1:1 token
  swap with no extra or missing replacements.
- 2026-09-15 12:45:07 CDT: line-count parity confirmed for all 6 files --
  `wc -l` unchanged file-for-file (types.py 1551, test_agentic_search.py
  1717, test_agentic_gate.py 744, test_gmail_send.py 370,
  test_google_contacts.py 478, test_sep08_ops_fixes.py 213, before and
  after). `diff` against `$S/h03c5_pre/<path>` shows exactly 58 changed
  lines total (1+2+1+6+42+6), matching the SIZE estimate exactly.
- 2026-09-15 12:45:15 CDT: built the removed-line corpus
  (`diff $S/h03c5_pre/<path> <path> | grep '^<'`) across all 6 files -- 58
  removed lines total (matches the changed-line count exactly).
- 2026-09-15 12:45:22-12:45:40 CDT: extracted the four token families
  (long-capitalised `\b[A-Z][A-Za-z.-]{2,}\b`, long-lowercase
  `\b[a-z][a-z_.-]{3,}\b`, short/numeric `\b[0-9]{3,}\b`, short-alpha
  `\b[A-Za-z]{2,3}\b`) from BOTH the removed-line and added-line corpora,
  then computed removed-only sets (`comm -23`, removed minus added) per
  the parent's method. Removed-only results: long-capitalised = {the EN
  label's real form} (1 token); long-lowercase = {the EN-lower real form,
  its dotted variant, and the comment identifier substring built from it}
  (3 tokens); short/numeric = empty; short-alpha = empty. This confirms
  the edit touched nothing but the real-form tokens themselves -- no
  incidental word changed.
- 2026-09-15 12:45:51 CDT: grepped every removed-only long token
  case-insensitively (counts only, no content copied) against this packet
  and all 6 edited files' POST-EDIT content: 0 hits everywhere. (The
  short/numeric and short-alpha removed-only sets were empty, so no grep
  was needed for those families -- vacuously 0 hits.)

Result: privacy self-check clean for both token families, first pass,
no correction needed.

- 2026-09-15 12:46:52 CDT: re-ran the same removed-only-token grep against
  this packet after the Section 8/9 packet edit (per the "re-run after
  EVERY packet edit" rule): 0 hits.

## 10. Post-edit test runs

- 2026-09-15 12:46:56 CDT: guard clear, MemAvailable 6176 MB.
- 2026-09-15 12:47:02 CDT: data/ listing before post-edit chunk: matches
  `$S/data_baseline_post_F12a.txt` exactly; logs/ absent.
- **Post-edit chunk** (same 5 files as baseline chunk 1), digests recorded
  in the same command as the run, 2026-09-15 12:47:07-12:47:18 CDT:
  `tests/unit/test_agentic_gate.py`, `tests/unit/test_gmail_send.py`,
  `tests/unit/test_google_contacts.py`, `tests/unit/test_sep08_ops_fixes.py`,
  `tests/unit/test_tool_wiring_parity.py` (post-edit digests for these 5
  plus `core/agentic/types.py` and `tests/test_agentic_search.py`
  recorded in the same command). `tests/unit/test_tool_wiring_parity.py`'s
  digest is **byte-identical** to its pre-edit value (not in scope, as
  expected); the other 6 scope files' digests all changed. Result: **107
  passed**, exit 0, 7.80s elapsed (pytest), 0:11.01 wall (time -v), Maximum
  resident set size 1714276 KB. Identical to baseline (107 passed).
- Per-file: identical file set/order to the baseline chunk, so the same
  107 collected (no `--collect-only` re-run needed; total pass count
  matches exactly and no test was added, removed, or renamed by this
  batch's edits — confirmed by `wc -l` line-count parity in Section 9).
- 2026-09-15 12:47:25 CDT: data/ listing after post-edit chunk: unchanged;
  logs/ absent; `git status --short web` still matches
  `$S/web_status_post_F13c-2a.txt`.

POST-EDIT TOTAL: **107 passed**, 0 failed — identical to the baseline
total. No behavior change.

**tests/test_agentic_search.py read-verification** (top-level; never
executed, per R_common_rules and the brief): confirmed no
`tests/unit/test_agentic_search.py` counterpart exists (only the
top-level file). Read lines 417-427 post-edit: line 421's fixture
(`<parameter name="name">Maren</parameter>`) and line 426's assertion
(`decisions[0].lookup_contact_name == "Maren"`) both now read "Maren",
consistent with each other, matching the pre-edit real form's positions
exactly (421, 426). The digest recorded in the post-edit chunk command
(`73544d2a…`) differs from the pre-edit digest (`206c7b13…`), confirming
the file changed only as expected.

## 11. Importer check

Grepped `core\.agentic\.types|from core\.agentic import (.*[ ,])?types\b`
over `tests/unit`, 2026-09-15 12:41:39 CDT (before baseline): 29 importer
files found, covering `import pkg.mod`, `from pkg.mod import …`, and
`from pkg import mod` forms (no dotted `patch("core.agentic.types...")`
string forms were found in the 29-file list; the module is consumed via
plain imports, not monkeypatched by string path, in this tree). Chose
`tests/unit/test_tool_wiring_parity.py` as "at least one" importer per the
brief's TESTS section — run in both baseline and post-edit chunks above
(6 tests, unchanged pass count, unchanged digest, confirming the
`recipient` tool-schema description edit in `core/agentic/types.py:1167`
does not change any asserted behavior in this importer). The other 28
importers were not run (not required by the brief's TESTS section, which
names the 4 test files plus "at least one" importer); this is recorded as
a limitation, not a gap in the required contract.

## 12. ruff and scan

- 2026-09-15 12:47:56 CDT: `python -m ruff --version` → ruff 0.14.9.
  `python -m ruff check core/agentic/types.py tests/test_agentic_search.py
  tests/unit/test_agentic_gate.py tests/unit/test_gmail_send.py
  tests/unit/test_google_contacts.py tests/unit/test_sep08_ops_fixes.py`
  (all 6 edited files) → **All checks passed!**, exit 0.
- 2026-09-15 12:48:03 CDT: post-edit `python scripts/check_bug_classes.py
  scan --root .` → exit 1 (STALE entries present, same doctrine as
  pre-edit; stderr empty). `diff` of the pre-edit and post-edit scan
  output files: **byte-identical** (exit 0). Summary table unchanged:
  dm01 396/7/12/0/5, dm16 395/89, dm17 648/36/39/0/3, dm18 123/23/79/0/56,
  dm29 0/0, dm31 396/3/3/0/0, catalog 1/0/0/0/0 — matches the brief's
  expected "identical tables" exactly.

## 13. BC-58 sibling search

N/A (privacy batch) — the brief states BUG_CLASSES: none for this batch,
so no sibling-search evidence file is required.

## 14. Final data/, logs/ and web/ state

- 2026-09-15 12:48:24 CDT: data/ (checked before the first pytest chunk,
  after every chunk of both baseline and post-edit phases, and once more
  as a final check): unchanged throughout — same 4 top-level entries
  (`benchmark_per_case.csv`, `chroma_db_v4/`, `embedding_migration_manifest.json`,
  `pipeline/`) with identical mtimes from before this batch started.
  Matches `$S/data_baseline_post_F12a.txt` exactly at every checkpoint.
- logs/: does not exist in this clone at any checkpoint (expected).
- `git status --short web`: unchanged at every checkpoint, still matches
  `$S/web_status_post_F13c-2a.txt` exactly. No web/ file touched (this
  batch never ran any npm/vitest command — none was in scope).

## 15. Post-edit manifest check

- 2026-09-15 12:48:30 CDT: `sha256sum -c $S/manifest_pre_H03c-5.txt
  --quiet` → exit 1, **1 FAILED entry**: `tests/test_agentic_search.py`.
  The 431-entry manifest is a curated subset of the tree, not every
  tracked file; only 1 of this batch's 6 edited files happens to be a
  manifest entry (confirmed by checking each of the 6 scope-file paths
  individually against `$S/manifest_paths_pre_H03c-5.txt`: only
  `tests/test_agentic_search.py` is present; the other 5 —
  `core/agentic/types.py`, `tests/unit/test_agentic_gate.py`,
  `tests/unit/test_gmail_send.py`, `tests/unit/test_google_contacts.py`,
  `tests/unit/test_sep08_ops_fixes.py` — are not manifest entries, so
  `sha256sum -c` produces no line for them). Reconciliation: 1 mismatch,
  1 expected-in-scope, 0 unexpected/out-of-scope mismatches, 0 misses.

## 16. Final sha256 of changed files

- 2026-09-15 12:49:00 CDT:
  - `core/agentic/types.py`: e5982ff483d1a6a496b84da34270d42a1b9c034ddcfdde360816eab4bed974a1
  - `tests/test_agentic_search.py`: 73544d2a73dbd1352309a3dc95a2d511ce6cf259e0e00c0d5714c6848558df31
  - `tests/unit/test_agentic_gate.py`: 8e7d4b6b3086bdb7f652bc02f925a94873743ba94fdba604a070d4efcb9a1444
  - `tests/unit/test_gmail_send.py`: 2c65fedc52844b38e680a7dd2c2039d9254446584de5b86fd2e6778b593af3ac
  - `tests/unit/test_google_contacts.py`: 41d00969bffa979cc48f78d3d412fbba11793bf0aedff01891eac06de58c68c1
  - `tests/unit/test_sep08_ops_fixes.py`: 25cd558c38ecfb4d81bef4c62857b291ea64e288d78fca1d26c7006238a78810
- `git status --short` at this point shows exactly the 6 scope files
  modified (`M`) plus this packet untracked (`??`); nothing else changed.

## 17. Privacy / no-network note

No network call, LLM provider call, or paid API was invoked at any point
in this batch — every command was `git` (read-only forms), `sha256sum`,
`grep`/`diff`/`wc`/`comm`/`sort`/`uniq`/`ls`/`date`/`awk`/`sed` (read-only
uses), the Edit/Write/Read tools, one `python -m ruff --version` +
`python -m ruff check` pair, one `python scripts/check_bug_classes.py scan
--root .` pair (before and after), and the `systemd-run`-wrapped
`python -m pytest` chunks (offline, `HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1`; no LLM provider touched by any of the 5 executed
test files — they are unit tests against contact-lookup/email-action
mocks and gate-classifier logic, all deterministic and HTTP-mocked). No
real name was written into any file this batch created or edited, or into
this packet, except as the required Edit `old_string` values themselves
(never persisted in any file) and inside `grep`/`sed`/`Read` outputs
viewed in the terminal but not copied into any written file. The privacy
self-check (Section 9, re-run after every subsequent packet edit per
Sections 9/12/15's re-check notes) found 0 hits at every check.

## 18. Milestone

Focused contract green: all 6 scope-file edits applied (core/agentic/types.py
prompt-text description; tests/test_agentic_search.py fixture+assertion,
read-verified only; tests/unit/test_agentic_gate.py possessive-phrase
test; tests/unit/test_gmail_send.py recipient/email/error-string tests;
tests/unit/test_google_contacts.py fixtures+queries+assertions, including
the optional plain-comment line 431; tests/unit/test_sep08_ops_fixes.py
query-argument tests), identical baseline/post-edit test counts (107/107,
0 failed) across the 5 executed test files in 1 chunk, ruff clean on all 6
edited files, scan tables byte-identical pre/post, data/logs/web state
unchanged throughout, privacy self-check clean (0 hits at every
re-check), manifest reconciliation clean (1 expected mismatch, 0
unexpected), no breach or deviation from the ORDER or any rule. "Focused
contract green" reached, at most, per the lane's stop condition.

## Parent review and integration (2026-09-15)

Accepted. No behaviour change beyond the owner-approved prompt-text example. Nothing changed in data/ or web/. No privacy issue remains, and no breach occurred.

### Process findings (parent)

1. **ORDER verified.**
   - data/ listing at 12:41:56, then the baseline chunk at 12:42:02–12:42:13 and its data check at 12:42:49.
   - First Edit at 12:44:38; file mtimes 12:44:40–12:44:54, all after the baseline.
   - Privacy self-check 12:45:01–12:46:52, then the post-edit chunk 12:47:07–12:47:18.
2. **No class-guard file was read.** Repo-wide greps used the brief's exclude list.
3. **Optional extra taken:** `test_google_contacts.py:431`, a plain comment identifier, now reads `search_maren`.

### Parent verification

- **Word-diff pairs (old side masked):** every change is EN → "Maren" / "maren" / "Maren's". That covers contact queries (21 search_contacts, 3 search_other_contacts, 3 resolve_contact), fixture display names and emails (the existing surname and domains unchanged), the possessive at test_agentic_gate.py:84, the tool-call XML parameter in test_agentic_search.py, and the `recipient` example at types.py:1167.
- **Size and shape:** 58 changed lines; every line count is unchanged.
- **Manifest:** 1 mismatch (`tests/test_agentic_search.py`, in scope). 6 new paths: the other 5 scope files plus this packet. 0 gone paths.
- **Web and git state:** `git status --short web` equals `$S/web_status_post_F13c-2a.txt`. HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`. `logs/` is absent.
- **Privacy:** 4 removed-only tokens (all families).
  - Case-insensitive hits: edited files 0, this packet 0, lane docs 0.
  - EN has 0 hits repo-wide outside lane docs (class-guard paths excluded).
- **Parent edits after the worker returned:** this section; `briefs/PARENT_STATE.md`; `LANDING_NOTES.md` (final "Real-name scrub" section).

### Parent rerun (guarded, capped, `-p asyncio`; data/ listed before and after)

- **Chunk** (MemAvailable 6,148 MB): test_agentic_gate 48, test_gmail_send 16, test_google_contacts 27, test_sep08_ops_fixes 10, importer test_tool_wiring_parity 6 = **107 passed** (7.62 s, 1,713,744 KB), matching the worker. data/ was unchanged, and `logs/` is absent.
- **ruff:** clean on all 6 edited files.
- **Scan:** byte-identical to `$S/scan_parent_H03c-4.txt` (`$S/scan_parent_H03c-5.txt`). Scan stderr is empty.

### Size

58 changed lines (target ≤150).

### Final repo-wide sweep (parent, after this batch)

The results are recorded in `LANDING_NOTES.md` under "Real-name scrub".

Two leftovers were outside every owner decision: a real course code in two dev-script sample tuples, and a real portal name in one production comment. The owner answered "Swap both" (2026-09-15 ≈12:53), so they go to H03c-6.
