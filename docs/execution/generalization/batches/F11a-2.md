# F11a-2: staged replacement for `resolve_thread` / `_update_thread`

Worker evidence packet. Brief: `docs/execution/generalization/briefs/F11a-2.md`. No request packet, no
anchor, no response file (per brief and R_common_rules).

## Manifest check (before)

- `sha256sum -c $S/manifest_post_F11a-1.txt --quiet` over the 240-path manifest: exit 0, **0 mismatches**.
- Path listing: `git status --short` expanded (`M` lines as-is; `??` directories walked with `find -type f`,
  matching the F9a precedent), sorted, diffed against `$S/manifest_paths_post_F11a-1.txt` (240 lines):
  **empty diff**, 240 == 240.
- `git remote -v`: `origin  /home/lukeh/Daemon_v1 (fetch)`, `origin  DISABLED (push)`.
- HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged from the brief's base commit).
- Starting digests, verified against the brief:
  - `memory/thread_store.py` `efe7f863c61bfe402a4acbf76fa25b7a710088f11cceb442b6b51748f0845a37` (506 lines) — matches.
  - `tests/unit/test_thread_store.py` `39bab8f3a2cff1d51da950410a9df6dd81c7887f245efe6a5c27d2547f4cbedf` (829 lines, read-only) — matches.
  - `memory/storage/multi_collection_chroma_store.py` `5af28e2bbf5f2d3de968a8398e095cae9a2e1265aca68a8fe93c57d2101c6327` (978 lines, read-only) — matches.
- Sanity check: `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c "import utils; print(utils.__file__)"`
  → `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (inside this clone).

## chromadb `GetResult` finding (read-only, no Python)

Read `/home/lukeh/.pyenv/versions/3.11.8/lib/python3.11/site-packages/chromadb/api/types.py` (≈385):

```
class GetResult(TypedDict):
    ids: List[ID]
    embeddings: Optional[...]
    documents: Optional[List[Document]]
    uris: Optional[URIs]
    data: Optional[Loadable]
    metadatas: Optional[List[Metadata]]
    included: Include
```

`ids` is typed `List[ID]`, NOT `Optional[...]` like every other field in this TypedDict (`documents`,
`metadatas`, `embeddings`, `uris`, `data` are all `Optional`). In the installed chromadb, a `get()` result's
`ids` key is always present and non-optional — `list_all`'s comment ("Some builds still return ids even if
not requested; handle if present") is defensive against builds other than this installed one; on the
installed build, `item.get("id")` should not be None in practice. The contract (item without an id → False)
is still required defensively, since `list_all`'s `ids[i] if i < len(ids) else None` guard shows the author
already anticipated a shorter `ids` list than `docs`/`metas`.

## Pre-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .`
→ exit 1 (gate scanners with findings exit non-zero by design), stderr empty.

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 634 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **38** | **79** | **0** | **41** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

dm18 matches the brief's expected post-F11a-1 baseline exactly (38 / 79 / 0 / 41).

## New test file

`tests/unit/test_thread_staged_replacement.py` — final: 267 lines, sha256
`49238488f45a7b7e1961d821846628a433033333b82d893aae87a8a22c10011f` (the version used for failing-first was
256 lines, sha256 `712d8b49f9368ce4cdaeff8c041042e9a1dc32de483ca23abdc4ffe46b788850`, before the privacy-test
scoping correction below). A local `FakeChromaStore`/`FakeCollection`
(no real Chroma) that records `add`/`delete` calls and can be told to raise on the next `add_to_collection()`
call and/or on a `delete()` naming a specific document id.

## Failing-first (on the UNEDITED source; digest in the same command)

```
sha256sum memory/thread_store.py && systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M \
  /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_thread_staged_replacement.py
```

Guard: clear (no running pytest). MemAvailable: 6569 MB. data/: unchanged (7 entries, baseline timestamps).
logs/: absent.

`sha256sum memory/thread_store.py` → `efe7f863c61bfe402a4acbf76fa25b7a710088f11cceb442b6b51748f0845a37` (matches
the starting digest — proves this ran on the unedited source).

**Result: 5 failed, 6 passed**, exit 1, elapsed 0:01.84, peak RSS 763,488 KB.

Failures (all on the unedited delete-before-store code):
- `TestResolveThreadStagedReplacement::test_store_fails_keeps_old_document` — a raising re-store deletes the
  old document unconditionally first, so the record is gone (`0 == 1`).
- `TestResolveThreadStagedReplacement::test_item_without_id_returns_false_and_writes_nothing` — today's code
  stores a duplicate and returns True when the found item has no id (`True is False`).
- `TestUpdateThreadStagedReplacement::test_store_fails_keeps_old_document` — same record loss via `_update_thread`.
- `TestStagedReplacementPrivacy::test_store_failure_marker_not_in_logs` and `test_delete_failure_marker_not_in_logs`
  — the pre-existing outer `except Exception as e: logger.error(f"... failed: {e}")` interpolates the raw
  exception, so the injected marker leaks into the log text under the unedited code.

Passing controls (already true under the unedited code, not staged-replacement-specific): the "not found"
control, the happy-path success cases (store+delete both succeed — order doesn't matter when nothing fails),
and the "delete of the old document fails" cases for both `resolve_thread` and `_update_thread` — under the
unedited delete-BEFORE-store order, a raising delete is hit before `store_thread` is ever called, so the old
document is trivially preserved (no new document to roll back). `enforce_cap`'s "counts only successful
updates" case also already passes: `_update_thread`'s existing outer `except Exception` already returns False
for any exception raised inside the try (including a raised `StoreWriteError` from `store_thread`), so this
honesty was already present after F11a-1 — F11a-2 only fixes when the delete happens relative to the write.

## Source edit

`memory/thread_store.py`: sha256 before `efe7f863c61bfe402a4acbf76fa25b7a710088f11cceb442b6b51748f0845a37` (506
lines) → after `c102fa7686c2eb61082ec496f4e09408c27a79db3710c9c27e51c17d1ab038a9` (552 lines). Confirmed by
reconstructing the exact pre-F11a-2 file from the brief's map and diffing: the reconstruction's own sha256
matches the starting digest, and the diff against the current file is **51 added / 13 removed lines**, touching
only `resolve_thread`, `_update_thread`, and one new shared private helper (`_replace_stored_thread`), plus its
docstring and the two callers' docstrings — nothing else in the file changed by this batch (the rest of the
`git diff` against HEAD 328a8ec is F11a-1's prior, already-uncommitted edit to `store_thread`/`list_open_threads`/
`query_threads`, not touched here).

**`_replace_stored_thread(self, thread, old_item)`** (the one shared private helper named in OWNERSHIP): given
the found `old_item` and the already-mutated `thread`, it (1) refuses to write when the collection handle or the
old document's id is missing (warning, labels only, False — this closes the "no id" duplicate-storing bug); (2)
calls `self.store_thread(thread)`, catching the typed `StoreWriteError` (labels only) → False with the old
document untouched; a `None` return (deliberate skip) → False, old document untouched; (3) only after a
confirmed write, deletes the old document — a raising `coll.delete()` (untyped, broad catch, as the contract
requires for this raw-collection call) triggers a best-effort rollback delete of the new document, its own
failure also logged (labels only) and swallowed, and returns False either way, so at most the old document
survives (or, if the rollback delete also fails, both may remain — the residual case named in the contract).
`resolve_thread` and `_update_thread` now find the item exactly as before, mutate `thread`, then delegate to
`_replace_stored_thread`; their own outer `except Exception as e: logger.error(... {e}) → False` is unchanged,
per CONTRACT point 7 (it now only fires for a genuinely different failure, e.g. `list_all` raising, since the
new helper fully absorbs `StoreWriteError` and delete failures itself).

## Test-authoring correction (found by the first focused run)

The first focused run (below) failed one test after the source edit: `test_store_failure_marker_not_in_logs`.
Investigation showed this was a scoping bug in my own new test, not a source defect, and — re-reading the
failing-first output above — it means that specific test's failing-first "failure" was not solid evidence of
the CM-07 defect either; both runs failed it for the same, unrelated reason:

- `caplog.text` (the original assertion target) captures ALL records logged during the call, including
  `store_thread`'s own pre-existing, out-of-scope log line (`memory/thread_store.py:261`,
  `logger.error(f"[ThreadStore] Failed to store thread: {e}")`, unchanged by this batch, part of F11a-1, and
  read-only under OWNERSHIP). That line interpolates the raw exception and so always contains an injected
  marker whenever `add_to_collection` raises — regardless of whether `resolve_thread`/`_update_thread` use the
  old delete-before-store order or the new staged replacement. It is a genuine, pre-existing sibling
  limitation (recorded below), not a CM-07 regression, and not fixable within this batch's ownership.
- The delete-failure test did NOT have this problem: under the OLD code its marker leak came from
  `resolve_thread`'s own outer `except Exception as e: logger.error(f"... failed: {e}")`, which is in scope and
  is exactly what `_replace_stored_thread`'s new, labels-only delete/rollback log lines fix — that test's
  failing-first result is solid CM-07 evidence.

CONTRACT point 9 and the TESTS section both scope the privacy requirement to "the new log lines," so I
corrected `test_store_failure_marker_not_in_logs` (and, for consistency, its sibling) to check only
`caplog.records` whose message contains "Staged replacement" — the two new log lines inside
`_replace_stored_thread`. No assertion about the *presence* of a failure log was weakened; the tests still
require a staged-replacement log line to have fired and to be marker-free. New test file re-hashed after the
fix (see "New test file" above, updated to the final sha256/line count reported at the end of this packet).

## Focused chunk (re-run after the test correction)

Files (5, ≤9): `tests/unit/test_thread_staged_replacement.py`, `tests/unit/test_thread_store.py`,
`tests/unit/test_thread_outcomes.py`, `tests/unit/test_thread_context_and_resolution.py`,
`tests/unit/test_thread_extractor.py`. Vetted before running: none constructs `ContextGatherer` with a
memory coordinator lacking `user_profile`, none builds `MemoryConsolidator`/`DailyNotesGenerator`, and none
is outside `tests/unit` or in the excluded list; `test_thread_staged_replacement.py` and `test_thread_store.py`
use only in-memory fakes, `test_thread_outcomes.py`'s own docstring states no real ChromaDB/embedder.

Guard: clear. MemAvailable: 6576 MB. data/ before: unchanged (7 entries). logs/: absent.

First attempt (before the test correction above): **1 failed, 132 passed** —
`TestStagedReplacementPrivacy::test_store_failure_marker_not_in_logs`, root-caused and fixed as described above.

Re-run after the correction: **133 passed, 0 failed**, exit 0, elapsed 0:07.20, peak RSS 1,126,648 KB.

data/ after: unchanged (`diff` against baseline: empty). logs/: absent.

## Sweep

`grep -rl --include='*.py' -E "^\s*from memory\.thread_store|^\s*import memory\.thread_store" tests/unit` and a
broader case-insensitive `thread_store`/`ThreadStore` sweep both return exactly the 4 files already in the
focused chunk that import the module or the class: `test_thread_context_and_resolution.py`,
`test_thread_outcomes.py`, `test_thread_staged_replacement.py`, `test_thread_store.py`. One extra hit,
`tests/unit/test_store_write_outcomes.py`, does not import `memory.thread_store` or `ThreadStore` at all — it
only names a local `MagicMock()` fixture attribute `thread_store` on a fake coordinator (lines 227-265); a
false positive from the case-insensitive text match, not a real importer. `test_thread_extractor.py` (in the
focused set per the brief) imports only `memory.thread_extractor` and `memory.thread_models`, not
`memory.thread_store` — also not a real importer, consistent with the brief listing it as a related-suite
addition rather than a sweep hit.

**No additional files import the changed module.** The sweep set is empty; no additional chunks were run.

## BC-58 sibling search

`grep -rn --include='*.py' --exclude-dir=data --exclude-dir=.agent_snapshots -B3 "\.delete(ids=" .` (production
code, excluding tests): the only other delete-THEN-re-store (update) pattern is `ProposalStore.update_status`
(`memory/proposal_store.py:261-267`) — already named in failure_outcome_design.md's "[verified] Delete-then-
store loses the record" and assigned to F11b, out of this batch's ownership. Every other `.delete(ids=...)` site
found (`knowledge/git_memory_loader.py:187`, `knowledge/obsidian_manager.py:487`,
`knowledge/web_search_manager.py:1123`, `knowledge/reference_docs_manager.py:481`,
`memory/storage/multi_collection_chroma_store.py:717`, `memory/cross_deduplicator.py:631`, and the
`scripts/purge_*.py`/`scripts/dedup_reference_docs.py`/`scripts/generate_test_facts.py` maintenance scripts) is
a pure delete/purge/dedup with no accompanying re-store — not an instance of the CM-07 class, so left
untouched.

## ruff

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check memory/thread_store.py
tests/unit/test_thread_staged_replacement.py` — **all checks passed**, exit 0. `ruff 0.14.9`.

## Post-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .`
→ exit 1 (unchanged, gate scanners with findings exit non-zero by design), stderr empty.

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 635 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **38** | **79** | **0** | **41** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

dm18's row is byte-for-byte unchanged pre- to post-edit (38/79/0/41) — no anchor change, no new finding, as the
brief expected. dm17's `files` count moved 634→635 (the new test file added to the scanned tree); its
found/base/new/stale are unchanged. No scanner-shape escalation was needed.

## Final manifest re-check

- `git rev-parse HEAD` → `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged).
- `git diff --cached --name-only` → empty; `git stash list` → empty; reflog shows only the clone entry.
- `git remote -v` → `origin  /home/lukeh/Daemon_v1 (fetch)`, `origin  DISABLED (push)` (unchanged).
- Only two tracked files differ from the pre-batch state: `memory/thread_store.py` (this batch's edit) and
  `tests/unit/test_thread_staged_replacement.py` (new, this batch). `tests/unit/test_thread_store.py` still
  hashes to `39bab8f3…` — unchanged, confirming it stayed read-only despite showing " M" in `git status` (that
  modification is F11a-1's prior, already-uncommitted edit relative to HEAD, not this batch's).
- Stray files: none added. The untracked `scan_pre.stderr` at the repository root is the same pre-existing
  artifact recorded in `manifest_paths_post_F11a-1.txt` (line 159) from an earlier batch; this batch's own scan
  stderr went to `/tmp/scan_pre_f11a2.stderr` / `/tmp/scan_post_f11a2.stderr`, both empty, never the repo.
- `data/`: unchanged after every chunk (7-entry baseline, `diff` empty each time). `logs/`: absent throughout.

## Size

**64 changed lines in `memory/thread_store.py`** (51 added / 13 removed, verified by reconstructing the exact
pre-edit file from the brief's digest-matching map and diffing) **+ 267 lines in the new test file = 331
changed lines total.** Under the ≤450 hard cap (target was ≤300; this batch runs slightly over target because
the fake-store infrastructure and five distinct failure-mode tests per method could not be trimmed without
compressing assertions or docstrings, which R_common_rules forbids). No trimming was applied to fit any number.

## Limitations carried forward

- **Residual double-failure case (contract point 4):** if both the old-document delete AND the rollback delete
  of the new document fail, `_replace_stored_thread` returns False but BOTH documents remain in the store (one
  resolved/updated, one stale old copy) — logged (labels only) at each step, never silently dropped, but not
  reconciled. This is the contract-named residual case; no test exercises the double-failure path specifically
  (the five required scenarios per method — store fails, delete fails and rolls back, success, missing id,
  not found/control — are all covered; a sixth "rollback also fails" test was judged out of the required scope
  and was not added, to keep the file at 267 lines rather than growing it further).
- **`store_thread`'s own log line leaks exception text (pre-existing, out of scope):** `memory/thread_store.py:261`,
  `logger.error(f"[ThreadStore] Failed to store thread: {e}")`, unchanged by F11a-1 or F11a-2 and outside this
  batch's OWNERSHIP (only `resolve_thread`/`_update_thread`/one shared helper may change). Any caller of
  `store_thread` whose write raises will have the raw exception text logged there, including a marker/sensitive
  detail if the underlying exception carried one. Not a CM-07 regression — this line predates both F11a batches
  — but worth flagging as a privacy sibling for a future batch that owns `store_thread`.
- **`resolve_thread`/`_update_thread`'s outer except still uses raw `{e}` interpolation** (CONTRACT point 7:
  "the existing outer except and its log line stay"). Under the new staged-replacement code this outer except
  is reached only for a genuinely different failure (e.g. `list_all` raising), not for a write or delete
  failure inside the staged replacement (those are now fully absorbed by `_replace_stored_thread`'s own,
  labels-only log lines) — but if `list_all` (or `OpenThread.from_metadata`) ever raised with sensitive text in
  its message, that would still reach `{e}`. This is pre-existing behavior the brief explicitly required to
  leave unchanged, not a new gap introduced here.
- **`get_top_threads`'s `_update_thread` call ignores the return value** (line ≈335, read-only, unchanged) — a
  failed staleness-update is silently dropped from that caller's perspective (though the record itself is now
  never lost, only the "did the update succeed" signal). Pre-existing, out of this batch's ownership.

## Privacy / no-network note

No real institution names or IPs appear anywhere in this batch's new files; the only injected text is the
synthetic marker `SYNTHETIC_test_thread_staged_replacement_marker` (plus `_add`/`_delete` suffixes), used only
to prove markers don't leak into the new log lines. No network call, LLM/API call, or `pip install` was made;
all fixtures are local in-memory fakes (`FakeChromaStore`/`FakeCollection`), no real ChromaDB/embedder.

## Milestone

Focused contract green: 133/133 passed in the focused chunk (including the new file), 0 sweep files (none
exist beyond the focused set), ruff clean, dm18 scan unchanged (0 new findings), data/ and logs/ unchanged
throughout, manifest re-check clean. No deviation from ORDER; the one disclosed deviation is the test-authoring
correction to the new file's own privacy assertions (documented above), made between the first and second
focused runs, before any claim of completion.

## Parent review and integration (2026-09-14)

Accepted as delivered. No COMPLIANCE breach. The typed catch was used where the failure is typed. The draft never exceeded the cap, and nothing was trimmed. Each pytest invocation ran behind its own guard check, and every chunk's file list is recorded.

### Process findings (parent)

- **Test-authoring correction:** accepted. It was disclosed honestly, and the root cause is verified: `store_thread`'s pre-existing `{e}` log line, which F11a-1 kept by contract, carries the marker whenever the write raises. Scoping the two privacy tests to the new "Staged replacement" log lines matches CONTRACT 9. That `store_thread` line is recorded below as a privacy sibling.
- **Failing-first scope:** per the packet, 5 failed and 6 passed on the unedited source.
  - The failures prove the defect: record loss on a failed re-store (both methods) and a missing-id item that stored a duplicate. The two privacy tests also failed, but the store-failure one failed for the unrelated `store_thread` log line (see the correction above).
  - `test_delete_of_old_document_fails_rolls_back_new_document` (both classes) passes on the old code too: the old order deleted first, and that delete raised before any write. It guards the new write-then-delete order rather than proving the old defect. It is correct as a regression control.
- **Pre-edit reconstruction:** the size and diff check rebuilt the pre-edit file under /tmp. Its sha256 matched the starting digest `efe7f863…`, so the 51/13 count is trustworthy. The repo has no stray file.

### Manifest

- **Checksum mismatches against `manifest_post_F11a-1.txt` (three):**
  - `memory/thread_store.py`, this batch's edit (`efe7f863…` → `c102fa76…`, 552 lines);
  - `briefs/PARENT_STATE.md` and `LANDING_NOTES.md`, parent edits made while the batch ran (the owner's 2026-09-14 decisions).
- **New paths (three):**
  - `tests/unit/test_thread_staged_replacement.py` (`49238488…`, 267 lines);
  - this file;
  - `briefs/F10c.md` (a parent brief written while the batch ran).
- **Unchanged:** `tests/unit/test_thread_store.py` is still `39bab8f3…`.
- **Git state:** `git diff --cached --name-only` is empty, HEAD `328a8ec` is unchanged, the reflog shows only the clone entry, and the stash list is empty.
- **No new stray files:** the only untracked file at the repository root is `scan_pre.stderr`.
- **`data/`:** unchanged against `$S/data_baseline_f7a_rerun.txt` (7 entries), and `logs/` is absent. The owner has approved deleting the F2-era leftovers but had not run the cleanup at review time. This listing stays the data/ baseline for F10c until the owner confirms the cleanup.
- **Recorded tree:** `manifest_post_F11a-2.txt` records the tree after this section.

### Code review (the parent read the helper and both rewired methods in full)

- **`_replace_stored_thread(thread, old_item)`:**
  - A missing collection handle or old document id logs a labels-only warning and returns False without writing. This closes the duplicate-storing gap for items with no id.
  - `new_id = self.store_thread(thread)` catches `except StoreWriteError`, logs the class name and returns False with the old document untouched. A None `new_id` (the skip) returns False.
  - `coll.delete(ids=[old_doc_id])` sits under a broad except, which is expected for the raw collection. On failure it logs the class name and tries `coll.delete(ids=[new_id])` as a best-effort rollback, whose own failure is logged by class name, then returns False.
  - It returns True only after the write and the delete both succeed.
- **`resolve_thread`:** finds the item and calls `mark_resolved` as before. If the helper fails it returns False; otherwise it logs "Resolved thread" and returns True. Not-found and the outer except (still `{e}`, unchanged per CONTRACT 7) are unchanged.
- **`_update_thread`:** finds the item as before and `return self._replace_stored_thread(thread, item)`. Not-found and the outer except are unchanged.
- **Callers:**
  - `touch_thread`, and `enforce_cap` (which counts only True), now see honest results.
  - `get_top_threads` (≈335) ignores `_update_thread`'s return; it is read-only here.
  - Coordinator quick resolution and shutdown's `resolve_thread`/`touch_thread` calls ignore returns as before, but the record is no longer lost.

### Tests (the parent read the whole new file)

- **Fake store:** records deletes and can raise on the next add or on a delete of a named id.
- **`resolve_thread`:**
  - store fails → False, exactly the old open document, no delete attempted;
  - delete fails → False, exactly the old document with its old id and open status (the new copy was rolled back);
  - success → True, exactly one document with a fresh id, resolved status and `resolution_hint`;
  - item with no id → False, nothing written, nothing deleted;
  - not found → False.
- **`_update_thread` and `touch_thread`:** store fails → the old urgency is kept and no delete happens; delete fails → rollback to the old id and urgency; touch success → exactly one document with a fresh id.
- **`enforce_cap`:** `max_open=0` with one queued add failure → pruned == 1.
- **Privacy:** the new "Staged replacement" log lines fired, are marker-free, and name `StoreWriteError`.

### Parent rerun (guarded, capped, `-p asyncio`)

- **Focused chunk** (MemAvailable 6,558 MB): `test_thread_staged_replacement`, `test_thread_store`, `test_thread_outcomes`, `test_thread_context_and_resolution` and `test_thread_extractor` gave **133 passed** (7.12 s, 1,123,908 KB), matching the worker.
- **Sweep:** the parent's independent importer grep for `memory.thread_store` or `ThreadStore` found 4 files, all in the focused chunk, so the sweep set is empty.
- **`data/`:** unchanged after the chunk, and `logs/` is absent.
- **ruff 0.14.9:** clean on both files.
- **Scan:** the dm18 row is identical before and after (38/79/0/41). The only table change is dm17's file count (634 → 635). Scan stderr is empty.

### Size

331 changed lines (64 source + 267 test), under the cap.

### Limitations carried forward

- **Double-failure residue:** if both the old-document delete and the rollback delete fail, both documents remain. This is logged and untested, and is the contract-named residual case.
- **Privacy sibling:** `store_thread`'s kept log line `logger.error(f"[ThreadStore] Failed to store thread: {e}")` (≈261) and the outer `{e}` lines in `resolve_thread` and `_update_thread` still carry raw exception text. This is pre-existing and was kept by contract. A future batch that owns those lines should switch them to labels.
- **`get_top_threads`:** it ignores `_update_thread`'s result.
- **`ProposalStore.update_status`:** it is the remaining delete-then-store instance, owned by F11b-2.

### Class-guard status

This batch has no anchor, so no counts changed. The open items are unchanged from F11a-1's parent section.
