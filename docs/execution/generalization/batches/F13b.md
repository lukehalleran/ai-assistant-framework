# F13b: GUI inline `storage_failed` receipts + background store transcript receipt

Worker evidence packet. Written incrementally, in run order. Every timestamp
is `date` output from the same command as the step it records.

## Step 1 — manifest check

```
$ date
Mon Sep 14 09:54:03 PM CDT 2026
$ git status --short  (see full listing in final report; 139-entry pre-existing
  dirty tree from prior integrated-but-uncommitted batches, unchanged shape)
$ git rev-parse HEAD
328a8ecea1dae91de16f5974ea71727c4e2e1aa0
$ git remote -v
origin	/home/lukeh/Daemon_v1 (fetch)
origin	DISABLED (push)
$ git diff --stat | tail -1
82 files changed, 4124 insertions(+), 830 deletions(-)
```

HEAD matches the brief's post-F13a tree (328a8ec). origin push is DISABLED
(verified, not changed). No stash/reset/checkout/restore/clean used.

```
$ date
Mon Sep 14 09:54:14 PM CDT 2026
$ sha256sum -c $S/manifest_post_F13a.txt --quiet; echo "exit: $?"
exit: 0
```

285/285 files match, 0 mismatches.

```
$ date
Mon Sep 14 09:54:18 PM CDT 2026
$ awk '{print $2}' $S/manifest_post_F13a.txt | sort > $S/f13b_manifest_paths_sorted.txt
$ sort $S/manifest_paths_post_F13a.txt > $S/f13b_paths_ref_sorted.txt
$ diff $S/f13b_manifest_paths_sorted.txt $S/f13b_paths_ref_sorted.txt && echo "PATH SET MATCHES"
PATH SET MATCHES
```

Index is empty (git status --porcelain shows only unstaged `M`/untracked
`??` entries, no staged `A`/`M`/`D`/`R`/`C` in column 1).

Starting digests (confirmed against the brief's PARENT-VERIFIED FACTS):

```
$ date
Mon Sep 14 09:54:22 PM CDT 2026
$ sha256sum gui/handlers.py utils/conversation_logger.py
9cf718e47c2672b1e926da3e217d66048af5a59a13d2e2ab8d56fe51c5391e31  gui/handlers.py
52828cbfe27af5aaf3c6ba17e746a187654f36776fa4f97dec7b018b412dbdbe  utils/conversation_logger.py
```

Matches the brief's stated `9cf718e4…` / `52828cbf…` prefixes.

```
$ wc -l gui/handlers.py utils/conversation_logger.py
5861 gui/handlers.py
216 utils/conversation_logger.py
```

gui/handlers.py line count (5861) matches the brief exactly.

data/ baseline check:

```
$ ls -la --time-style=full-iso data
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-14 19:30:21 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-14 20:10:17 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07 pipeline
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
```

Matches `$S/data_baseline_post_F12a.txt` exactly (byte-identical listing).
logs/ absent, as expected.

## Step 2 — pre-edit scan (read-only)

```
$ date
Mon Sep 14 09:54:40 PM CDT 2026
$ python scripts/check_bug_classes.py scan --root . > $S/f13b_scan_pre.txt 2>&1; echo "exit: $?"
exit: 1
```

(PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin prefixed, per
R_common_rules ENVIRONMENT section, on all python invocations in this batch.)

Summary table:
```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    645    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Matches the brief's stated baseline exactly: dm18 found 23 / base 79 / new 0
/ stale 56; dm17 files 645.

## Step 3 — size estimate (before any source edit)

New test file `tests/unit/test_handler_storage_receipts.py` written first
(fakes only; LOCAL ctx/store shapes reusing the fixture patterns named in
the brief — test_doc_conversation_source.py, test_sep07_calendar_offer_continuation.py,
test_handle_submit.py, test_store_write_outcomes.py's `TestBackgroundStoreInteraction`).

```
$ date
Mon Sep 14 10:01:03 PM CDT 2026
$ wc -l tests/unit/test_handler_storage_receipts.py
386 tests/unit/test_handler_storage_receipts.py
```

386 lines, all new (no existing test file edited — FIXTURE RULE N/A, this
batch owns no existing test file). Estimated source-edit diff, drafted
before applying (three inline sites ~11 lines each incl. the `extra=`
addition to `_build_debug_record`, the background site ~5 lines, `_log_text`
~2 lines): **~40 lines**. Projected total **~426 changed lines** — over the
≤350 soft target (not itself a stop condition, per the F13a precedent) but
comfortably under the ≤450 hard stop. No trimming performed to hit any
number (SIZE clarification honored — this is the pre-edit estimate, not a
post-measurement squeeze). Proceeding to the failing-first run.

## Step 4 — failing-first (pre-edit; digests in the same command)

Guard + memory check immediately before:

```
$ date
Mon Sep 14 10:02:27 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output — clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6746
```

Digests + new-test run, unedited source, same command:

```
$ date
Mon Sep 14 10:02:32 PM CDT 2026
$ sha256sum gui/handlers.py utils/conversation_logger.py
9cf718e47c2672b1e926da3e217d66048af5a59a13d2e2ab8d56fe51c5391e31  gui/handlers.py
52828cbfe27af5aaf3c6ba17e746a187654f36776fa4f97dec7b018b412dbdbe  utils/conversation_logger.py
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
  python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_handler_storage_receipts.py
```

Result: **7 failed, 7 passed** in 2.67s. Maximum resident set size: 1076748 KB
(~1.05 GB). Exit status 1 (expected — this is the failing-first proof).

Failures (all `KeyError: 'storage_failed'` or the paired text-format
assertion — exactly the gap the unedited `except Exception: pass` /
`except StoreWriteError as e: logger.error(...)` sites leave open):
- `TestDocGenerationStorageReceipt::test_failure_sets_receipt_matches_success_content`
- `TestSelfNoteStorageReceipt::test_failure_sets_receipt_matches_success_content`
- `TestSelfNoteStorageReceipt::test_ctx_without_telemetry_attribute_is_handled_without_exception`
- `TestActionRetryStorageReceipt::test_failure_sets_receipt_matches_success_content`
- `TestActionRetryStorageReceipt::test_plain_runtime_error_gives_generic_label`
- `TestBackgroundStoreReceipt::test_failure_sets_metadata_receipt`
- `TestBackgroundStoreReceipt::test_text_format_shows_label_only_on_the_failed_entry`

The 7 passing tests are the controls that must already hold true on the
unedited source (success / `memory_system=None` / `store_interaction`
returns `None` — no `storage_failed` key anywhere) — a correct paired-control
shape (BC-64).

data/ and logs/ after this chunk:

```
$ date
Mon Sep 14 10:02:42 PM CDT 2026
$ ls -la --time-style=full-iso data
(unchanged from Step 1 — byte-identical)
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
```

No new or changed entry. Proceeding to the source edits.

## Step 5 — source edits

Six `Edit` tool calls on `gui/handlers.py` (the three inline sites' try/except
blocks + their `_build_debug_record(extra=...)` calls, plus
`_background_store_interaction`'s `except StoreWriteError` block and
`log_metadata`), one on `utils/conversation_logger.py` (`_log_text`'s
metadata block). Each site: local `from core.orchestrator import
_storage_failure_label` inside the except (reusing F13a's helper, never
re-implementing the label format), a `logger.warning`/kept `logger.error`
with the label only (never `{e}`), a getattr-defensive
`ctx.telemetry["storage_failed"] = label` write, and (inline sites only) an
`extra={"storage_failed": label} if label else None` argument to
`_build_debug_record`. No change to `_dispatch_storage`, its signature, any
caller, or any yielded chunk's content/keys on success or skip.

```
$ date
Mon Sep 14 10:03:28 PM CDT 2026
$ sha256sum gui/handlers.py utils/conversation_logger.py
c1f3f7e9e6162004b7fb66a06c3ec4ce24852b10b5d1a96a758b23d6d7b11b50  gui/handlers.py
786c8a2ee9565b059c1d3c12802ce85dd7240596b9b43f225793c85fa576278b  utils/conversation_logger.py
$ wc -l gui/handlers.py utils/conversation_logger.py
5887 gui/handlers.py   (was 5861, +26)
218 utils/conversation_logger.py   (was 216, +2)
```

### This batch's own changed-line count (isolated from other batches' uncommitted work)

Both files were already modified before this batch touched them (139
pre-existing dirty files from prior integrated-but-uncommitted batches — HEAD
stays 328a8ec throughout). To isolate this batch's OWN contribution: each of
the 7 Edit calls above was reversed (new_string → old_string) on a scratch
copy of the post-edit file, and each reconstruction was verified byte-for-byte
against the Step-1 digest before diffing:

```
$ date
Mon Sep 14 10:04:29 PM CDT 2026
$ sha256sum $S/f13b_scratch_handlers_postedit.py
9cf718e47c2672b1e926da3e217d66048af5a59a13d2e2ab8d56fe51c5391e31  (matches Step 1)
$ sha256sum $S/f13b_scratch_convlogger_postedit.py
52828cbfe27af5aaf3c6ba17e746a187654f36776fa4f97dec7b018b412dbdbe  (matches Step 1)
$ date
Mon Sep 14 10:04:31 PM CDT 2026
$ git diff --no-index -- $S/f13b_scratch_handlers_postedit.py gui/handlers.py | grep -cE '^[+-][^+-]'
38
$ git diff --no-index -- $S/f13b_scratch_convlogger_postedit.py utils/conversation_logger.py | grep -cE '^[+-][^+-]'
2
```

`git diff --no-index` was used read-only (no index write, not `git add`)
purely to compare two ordinary files.

**Source: 38 (gui/handlers.py) + 2 (utils/conversation_logger.py) = 40 changed
lines.** Combined with the new test file (386 lines, all-new): **426 changed
lines total** — under the ≤450 hard stop (over the ≤300-350 soft target,
which is not itself a stop condition, per the F13a precedent). No trimming
was done to hit this number (SIZE clarification honored); the pre-edit
estimate (Step 3, ~426) was confirmed almost exactly by this exact count.

## Step 6 — focused chunk 1 (5 files, ≤9 limit)

Guard + memory check:

```
$ date
Mon Sep 14 10:04:50 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output — clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6732
```

Command (files: test_handler_storage_receipts.py, test_store_write_outcomes.py,
test_doc_conversation_source.py, test_sep07_calendar_offer_continuation.py,
test_handle_submit.py):

```
$ date
Mon Sep 14 10:04:55 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_handler_storage_receipts.py tests/unit/test_store_write_outcomes.py \
  tests/unit/test_doc_conversation_source.py tests/unit/test_sep07_calendar_offer_continuation.py \
  tests/unit/test_handle_submit.py
```

Result: **208 passed, 0 failed** in 15.93s. Maximum resident set size:
1731272 KB (~1.69 GB). Exit status 0.

data/ and logs/ after this chunk:

```
$ date
Mon Sep 14 10:05:16 PM CDT 2026
(data/ unchanged, byte-identical; logs/ absent)
```

## Step 7 — focused chunk 2 (5 files, ≤9 limit)

Guard + memory check:

```
$ date
Mon Sep 14 10:05:19 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output — clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6728
```

Command (files: test_request_path_parity.py, test_action_guard_integration.py,
test_section_outcome_receipts.py, test_calendar_turn_round3.py,
test_orchestrator_storage_receipts.py):

```
$ date
Mon Sep 14 10:05:25 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_request_path_parity.py tests/unit/test_action_guard_integration.py \
  tests/unit/test_section_outcome_receipts.py tests/unit/test_calendar_turn_round3.py \
  tests/unit/test_orchestrator_storage_receipts.py
```

Result: **74 passed, 0 failed** in 9.28s. Maximum resident set size:
1718912 KB (~1.68 GB). Exit status 0.

data/ and logs/ after this chunk:

```
$ date
Mon Sep 14 10:05:39 PM CDT 2026
(data/ unchanged, byte-identical; logs/ absent)
```

## Step 8 — sweep (every remaining tests/unit importer of gui.handlers / utils.conversation_logger)

```
$ date
Mon Sep 14 10:05:57 PM CDT 2026
$ grep -rlE 'import gui\.handlers|from gui\.handlers|from gui import handlers|import utils\.conversation_logger|from utils\.conversation_logger|from utils import conversation_logger' tests/unit --include='*.py' --exclude-dir=data --exclude-dir=.agent_snapshots | sort
```

48 files matched. 9 already run in Steps 6-7 (test_action_guard_integration.py,
test_calendar_turn_round3.py, test_doc_conversation_source.py,
test_handler_storage_receipts.py, test_handle_submit.py,
test_request_path_parity.py, test_section_outcome_receipts.py,
test_sep07_calendar_offer_continuation.py, test_store_write_outcomes.py) —
39 remain for the sweep.

Vetting (before running): grepped all 39 for the known hazard patterns
(`ContextGatherer(`, `MemoryConsolidator(`, `DailyNotesGenerator(`,
`UserProfile()`, Gradio `.Blocks`/`.launch(`, real Chroma/SentenceTransformer
construction, `ConversationLogger()`/`conversation_logs` default dir,
`spec=[]`) — no hazard hits. One incidental match
(test_grounding_delivery_atomicity.py, "real...chroma") was a docstring
disclaiming real-Chroma construction ("Fakes only -- no LLM, no network, no
live store; `_dispatch_storage` is a MagicMock throughout"), not a hazard.
None of the 39 is test_graph_integration.py, a top-level tests/ file,
tests/test_web_search_manager.py, or tests/test_prompt_internal_methods.py.
Ran in 5 chunks of ≤9.

### Sweep chunk A (9 files)

```
$ date
Mon Sep 14 10:07:06 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output — clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6732
$ date
Mon Sep 14 10:07:11 PM CDT 2026
```
Files: test_adaptive_adopters.py, test_api_actions.py,
test_api_error_fail_fast.py, test_audit0831_fixes.py,
test_calendar_turn_round2.py, test_calendar_update_delete.py,
test_correction_gui_wiring.py, test_deferred_request_clarify.py,
test_escalation_gui_wiring.py.

Result: **168 passed, 1 skipped** (pre-existing skip, unrelated: "message
did not hit the keyword fast path") in 6.78s. Max RSS: 1682760 KB. Exit 0.
data/ and logs/ after (10:07:22 PM CDT): unchanged, absent.

### Sweep chunk B (9 files)

```
$ date
Mon Sep 14 10:07:25 PM CDT 2026
(guard clear; MemAvailable 6718)
$ date
Mon Sep 14 10:07:30 PM CDT 2026
```
Files: test_evidence_transport.py, test_fetch_url.py,
test_gate_debug_summary_robust.py, test_grounding_buffered_delivery.py,
test_grounding_cancel_storage_gate.py, test_grounding_delivery_atomicity.py,
test_grounding_log_only.py, test_grounding_wiring.py, test_ingress_guard.py.

Result: **178 passed** in 18.78s. Max RSS: 1189100 KB. Exit 0.
data/ and logs/ after (10:07:54 PM CDT): unchanged, absent.

### Sweep chunk C (9 files)

```
$ date
Mon Sep 14 10:07:57 PM CDT 2026
(guard clear; MemAvailable 6719)
$ date
Mon Sep 14 10:08:01 PM CDT 2026
```
Files: test_insight_completion_fixes.py, test_insight_evidence_layout.py,
test_insight_mode_handler.py, test_insight_pattern_facet.py,
test_insight_sweep_coverage.py, test_pacing_metrics_fix.py,
test_retrospective_small_guards.py, test_review_gate_strict_parse.py,
test_sep02_checkin_fixes.py.

Result: **243 passed** in 8.24s. Max RSS: 1723300 KB. Exit 0.
data/ and logs/ after (10:08:15 PM CDT): unchanged, absent.

### Sweep chunk D (9 files)

```
$ date
Mon Sep 14 10:08:18 PM CDT 2026
(guard clear; MemAvailable 6712)
$ date
Mon Sep 14 10:08:23 PM CDT 2026
```
Files: test_sep08_agentic_answer_integrity.py,
test_sep08_document_continuity.py, test_sep09_speed_batch.py,
test_sep10_probe_dump_actions.py, test_sep10_probe_dump_interpretation.py,
test_sep12_action_claim_eligibility.py,
test_sep12_followup_budget_outcomes.py,
test_sep12_followup_calendar_attribution.py,
test_sep12_web_evidence_budget.py.

Result: **586 passed** in 28.51s. Max RSS: 1748940 KB. Exit 0.
data/ and logs/ after (10:08:57 PM CDT): unchanged, absent.

### Sweep chunk E (3 files, final)

```
$ date
Mon Sep 14 10:09:00 PM CDT 2026
(guard clear; MemAvailable 6712)
$ date
Mon Sep 14 10:09:03 PM CDT 2026
```
Files: test_stream_artifacts.py, test_turn_progress.py,
test_wiki_citations.py.

Result: **69 passed** in 7.04s. Max RSS: 1707348 KB. Exit 0.
data/ and logs/ after (10:09:16 PM CDT): unchanged, absent.

**Sweep total: 1244 passed, 1 skipped (pre-existing, unrelated), 0 failed**
across 39 files in 5 chunks.

## Step 9 — ruff

```
$ date
Mon Sep 14 10:09:38 PM CDT 2026
$ python -m ruff check gui/handlers.py utils/conversation_logger.py tests/unit/test_handler_storage_receipts.py
All checks passed!
$ date
Mon Sep 14 10:09:41 PM CDT 2026
$ python -m ruff --version
ruff 0.14.9
```

## Step 10 — post-edit scan (read-only)

```
$ date
Mon Sep 14 10:09:45 PM CDT 2026
$ python scripts/check_bug_classes.py scan --root . ; echo "exit: $?"
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    646    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
exit: 1
```

Matches expectation exactly: dm18 unchanged (found 23 / base 79 / new 0 /
stale 56), dm17 files 645 → 646 (+1, the new test file), **new = 0 across
every scanner**. No new gate finding caused by this batch's edits (SCANNER
SHAPE: nothing to escalate).

## Step 11 — BC-58 sibling search

Before closing: searched for every caller-side `store_interaction(` call
site in the non-test codebase, to confirm no sibling silent-swallow site was
left unguarded by this batch + F13a together.

```
$ date
Mon Sep 14 10:10:08 PM CDT 2026
$ grep -n "except Exception:" -A2 gui/handlers.py | grep -B2 "pass" | grep "except Exception:"
782, 1865, 2851, 2862, 4340, 5013, 5122, 5131
```

Checked each: text-truncation fallback (782), optional MemoryExpander
construction (1865), pending-proposal/action-kind detection (2851, 2862), a
prompt-limit guardrail comment block (4340), debug-record provenance
extraction (5013), and action-intent/duplicate-reply detection (5122, 5131).
**None wraps a `store_interaction` call** — all eight are unrelated bare
`except: pass` sites outside this batch's ownership.

```
$ date
Mon Sep 14 10:10:16 PM CDT 2026
$ grep -rn "store_interaction(" --include="*.py" --exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=tests . | grep -v "def store_interaction"
```

Every caller-side `store_interaction(` call site in the repo (excluding the
method's own definitions and tests/) is one of: `core/orchestrator.py`
2033/2198/2335/2517 (F13a's four sites — `_handle_deictic`,
`_maybe_document_generation`, `_maybe_agentic_search`, `_store_interaction`);
`memory/memory_coordinator.py:372` (`MemoryCoordinator.store_interaction`'s
own delegation to `self._storage.store_interaction` — the deployed function
itself, not a caller swallowing a failure); or `gui/handlers.py`
173/1720/2621/3529 (this batch's four sites — background, doc-gen, self-note,
action-retry). **No sibling site is left unguarded.**

## Step 12 — final data/ and logs/ check

```
$ date
Mon Sep 14 10:09:56 PM CDT 2026
$ ls -la --time-style=full-iso data
(byte-identical to Step 1 baseline / $S/data_baseline_post_F12a.txt)
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
```

No new or changed data/ entry at any point in this batch; logs/ never
created. Every telemetry/log path exercised by the new tests used
`tmp_path` (the `TestBackgroundStoreReceipt.test_text_format_shows_label_only_on_the_failed_entry`
test's real `ConversationLogger`) or a `MagicMock`/`AsyncMock` — no real
store, model, or Gradio UI was constructed at any point. Privacy: every
assertion checks label/class-name text only (`"store_interaction: RuntimeError"`
etc.); the synthetic MARKER strings are asserted ABSENT from content, debug
records and logs, never asserted present outside the raising mock's own
internal exception chain.

## Milestone

Focused contract green (208 + 74 passed across the two brief-specified
chunks), sweep green (1244 passed, 1 pre-existing unrelated skip), ruff
clean, scan clean (new = 0 everywhere), data/ and logs/ untouched throughout.

## Deviations / limitations

1. No breach to disclose. `python -m ruff --version` was run as one of the
   four explicitly-permitted forms per this brief's COMPLIANCE section
   ("which now explicitly include `python -m ruff --version`").
2. **Limitation (by design, per brief item 6):** no status message exists
   yet on any path (that is F13c-2); the background (main chat) failure
   does not reach the turn record or the already-delivered debug record
   (that is F13c-1). This batch only adds the labelled receipt to
   `ctx.telemetry`, the inline sites' debug records, and the background
   transcript's log metadata / text-format printer.
3. **Limitation:** per owner decision 4, no chat text ever carries the
   failure — verified structurally (every yielded chunk's `content` is
   asserted byte-for-byte identical between a failure run and a success run
   for the same inputs, at all three inline sites).

## Parent review and integration (2026-09-14)

Accepted. There is no rule breach, and no correctness, data/ or privacy issue. The parent's rerun covered five more importer files than the worker's sweep (finding 2), and all pass.

### Process findings (parent)

1. **ORDER verified from mtimes.**
   - Test file last written 22:01:02.
   - Failing-first `date` 22:02:32, printing the unedited `9cf718e4…` / `52828cbf…` (7 failed, 7 passed).
   - `gui/handlers.py` edited 22:03:23 and `utils/conversation_logger.py` 22:03:26.
   - This packet last written 22:10:42.
   - Section headers still carry no `date` (the brief asked for one); the in-command `date` outputs are real.
2. **Importer-set gap (the two greps differed).** The worker's sweep grep matched only explicit import statements (`import gui.handlers`, `from gui.handlers`, `from gui import handlers`, and the same for conversation_logger), giving 39 sweep files.
   - The parent's grep (`gui\.handlers|utils\.conversation_logger`, which also matches `patch("gui.handlers…")` strings) found three more handler consumers the worker did not run: `test_api_chat.py`, `test_api_origin_security.py`, `test_sep08_ops_fixes.py`.
   - The parent's grep in turn missed `test_audit0831_fixes.py` and `test_pacing_metrics_fix.py` (`from gui import handlers`). The parent ran those two separately.
   - Every file in the union passed. R_common_rules now requires importer greps to match every import form AND dotted patch-target strings.
3. **Size.** 426 changed lines (38 handlers + 2 conversation_logger + 386 test), under the 450 hard stop but over the 350 target.
   - Measured on reconstructed pre-edit copies (`$S/f13b_scratch_handlers_postedit.py` = `9cf718e4…`, 5861 lines; `$S/f13b_scratch_convlogger_postedit.py` = `52828cbf…`, 216 lines; both named "postedit" but pre-edit). The parent diffed both against the live files.
4. **Local import inside the excepts.** Each site imports `core.orchestrator._storage_failure_label` inside its except block.
   - Should that import ever fail, the error would reach the enclosing except. That would mean the doc-generation fallback answer, or skipping the background transcript write.
   - The risk is negligible: the orchestrator module is already loaded in any process running these handlers. Recorded.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F13a.txt` shows exactly one mismatch, `gui/handlers.py` (`9cf718e4…` → `c1f3f7e9…`, 5861 → 5887 lines).
- **New paths (three):**
  - `utils/conversation_logger.py` (`52828cbf…` → `786c8a2e…`, 216 → 218 lines, its first modification);
  - `tests/unit/test_handler_storage_receipts.py` (`b9aedeb6…`, 386 lines);
  - this file.
- **Git state:** HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`, which stays the baseline. `logs/` is absent.
- **Parent edits after the worker returned:**
  - `briefs/PARENT_STATE.md`;
  - `briefs/R_common_rules.md` (importer-grep rule);
  - `briefs/F13c-1.md` (new).
- **Recorded tree:** `manifest_post_F13b.txt` records the tree after this section.

### Code review (the parent read the exact own diffs)

- **`_background_store_interaction`:**
  - `storage_failed_label = None`;
  - in `except StoreWriteError`, the error log is unchanged, then `storage_failed_label = _storage_failure_label(e)`;
  - `log_metadata['storage_failed']` is set only when the label is set;
  - still one `log_interaction` call.
- **Three inline sites (doc generation, self-note, action retry):**
  - `label = None`;
  - `except Exception as e:` → label → one `logger.warning` with the label only → `ctx.telemetry["storage_failed"]` when `ctx.telemetry` is a dict;
  - `_build_debug_record(..., extra={"storage_failed": label} if label else None)`;
  - the yielded chunks are unchanged.
- **`utils/conversation_logger.py` `_log_text`:** `Storage failed: <label>` after the Memory ID line, only when present.
- **Unchanged:** `_dispatch_storage`, the turn-record path, the `section_outcomes=` count (3) and the getattr count (2).

### Tests (the parent read the whole new file)

- **Inline sites:** for each, a failure run and a success run with the same inputs give byte-identical final `content`.
  - The failure gives exactly one debug-carrying chunk; `ctx.handled` True; the label in `ctx.telemetry`, in the debug record and in the WARNING log; no marker in content, debug or logs.
  - Controls: `memory_system=None` and a None-returning store give no key.
- **Self-note:** a ctx without `telemetry` is handled, and the debug record still carries the label.
- **Action retry:**
  - a plain `RuntimeError` gives the generic label;
  - `pending_action_id` rides both chunks.
- **Background store:**
  - failure → one `log_interaction` with `db_id is None` and the label;
  - success → no key, `db_id == "mem-42"`;
  - a real `ConversationLogger(tmp_path, "text")` shows exactly one "Storage failed:" line, for the failed entry only.
- **Isolation:** fakes only (DocumentGenerator, DaemonNotesManager and the pending-actions store are patched); no real stores, models or log directories.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F12a.txt` after every chunk)

- **Focused 1** (MemAvailable 6,690 MB): `test_handler_storage_receipts`, `test_store_write_outcomes`, `test_doc_conversation_source`, `test_sep07_calendar_offer_continuation` and `test_handle_submit` gave **208 passed** (19.26 s, 1,730,372 KB).
- **Focused 2** (6,720 MB): `test_request_path_parity`, `test_action_guard_integration`, `test_section_outcome_receipts`, `test_calendar_turn_round3` and `test_orchestrator_storage_receipts` gave **74 passed** (13.51 s, 1,718,112 KB).
- **Sweep, the 40 files matched by the parent's grep:**
  - chunk 1: `test_adaptive_adopters`, `test_api_actions`, `test_api_chat`, `test_api_error_fail_fast`, `test_api_origin_security`, `test_calendar_turn_round2`, `test_calendar_update_delete`, `test_correction_gui_wiring`, `test_deferred_request_clarify` gave **147 passed, 1 skipped**;
  - chunk 2: `test_escalation_gui_wiring`, `test_evidence_transport`, `test_fetch_url`, `test_gate_debug_summary_robust`, `test_grounding_buffered_delivery`, `test_grounding_cancel_storage_gate`, `test_grounding_delivery_atomicity`, `test_grounding_log_only`, `test_grounding_wiring` gave **175 passed**;
  - chunk 3: `test_ingress_guard`, `test_insight_completion_fixes`, `test_insight_evidence_layout`, `test_insight_mode_handler`, `test_insight_pattern_facet`, `test_insight_sweep_coverage`, `test_retrospective_small_guards`, `test_review_gate_strict_parse`, `test_sep02_checkin_fixes` gave **252 passed**;
  - chunk 4: `test_sep08_agentic_answer_integrity`, `test_sep08_document_continuity`, `test_sep08_ops_fixes`, `test_sep09_speed_batch`, `test_sep10_probe_dump_actions`, `test_sep10_probe_dump_interpretation`, `test_sep12_action_claim_eligibility`, `test_sep12_followup_budget_outcomes`, `test_sep12_followup_calendar_attribution` gave **554 passed**;
  - chunk 5: `test_sep12_web_evidence_budget`, `test_stream_artifacts`, `test_turn_progress`, `test_wiki_citations` gave **111 passed**.
  - Every chunk ran between 6,688 and 6,743 MB available and peaked at ≤1,745,432 KB.
- **The two files only the worker's grep found:** `test_audit0831_fixes` and `test_pacing_metrics_fix` gave **45 passed** (6.02 s, 1,106,684 KB).
- **Total:** 282 focused + 1,284 sweep = **1,566 passed, 1 skipped, 0 failed** over 52 files.
  - This reconciles with the worker: 1,244 over its 39 sweep files = 1,284 − 40 (the three files only the parent ran) + 0 (the parent's figure already includes the two files it ran separately).
  - data/ was unchanged after every chunk and after the scan, and `logs/` is absent.
- **ruff:** clean on all three files.
- **Scan:** dm18 is unchanged at found 23 / base 79 / new 0 / stale 56 (rows identical to `$S/scan_parent_F13a.txt`). dm17's file count went from 645 to 646. Scan stderr is empty.

### Limitations carried forward

- **Status message:** none yet on any path (F13c-2).
- **Background failure reach:** it does not reach the turn record or the delivered debug record (F13c-1).
- **Import-in-except:** see finding 4.

### Class-guard status

- No anchor and no response file.
