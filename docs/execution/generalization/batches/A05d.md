# A05d — retire `build_grounding_correction` (the draft-plus-suffix builder)

Worker scope: `docs/execution/generalization/A05_design.md` batch row A05d —
retire `build_grounding_correction` and its tests; the BC-45/BC-50 catalog
update is the class-guard owner's (note below; `docs/BUG_CLASSES.md` not
edited). Runs after A05c. Rules: `R_common_rules.md` (all except "Response
file"); evidence packet is this file.

## 1. Base, manifest, environment

- Candidate base HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, amend, push, remote change, stash, reset,
  checkout, restore, clean, or daemon restart).
- Manifest check (a): `sha256sum -c --quiet $S/manifest_post_A05c.txt` —
  exit 0, no output — matched.
- Manifest check (b): sorted `git diff --name-only` + `git ls-files
  --others --exclude-standard` diffed against
  `$S/manifest_paths_post_A05c.txt` — exit 0, no output — matched.
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED
  (push)`.
- `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c
  "import utils; print(utils.__file__)"` printed
  `/home/lukeh/daemon_exec/generalization/utils/__init__.py` — confirmed
  loading from this clone, not `/home/lukeh/Daemon_v1`.
- Pre-edit `git status --short`: 40 modified + 19 untracked top-level
  entries (one of which, `docs/execution/generalization/`, expands to many
  individual paths in the manifest).
- Post-edit `git status --short`: **identical file list** — no new
  untracked path, no file outside the six owned files touched. HEAD
  unchanged throughout: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
- `data/` (top-level only, never recursed): `ls -A data` before and after
  are byte-identical —
  `benchmark_per_case.csv`, `chroma_db_v4/`, `embedding_migration_manifest.json`,
  `pipeline/`. `data/adaptive_exemplars.json` is absent both times, as
  expected (owner deleted it 2026-09-13, per the brief's DATA NOTE). Nothing
  in `data/` was read, traversed, or deleted.
- No network, no LLM/paid API call, no `pip install`, no daemon restart.
  Mutation proofs used only in-process text substitution on
  `core/grounding_check.py` (never executed as a module) plus `pytest`; no
  ad-hoc script imported `tone`/pipeline modules.

## 2. Files touched (ownership as specified)

| File | Change |
| --- | --- |
| `core/grounding_check.py` | delete `build_grounding_correction` + its `__all__` entry; fix module-docstring pipeline/Key-functions lines; reword 5 stale-wording spots (section header, INACTIVE comment, integration-block comment, `integrate_grounding_correction` docstring, 5 log lines) |
| `gui/handlers.py` | one docstring sentence in `_apply_grounding_check` (≈3237-3239) |
| `tests/unit/test_grounding_check.py` | import cleanup; delete `TestCorrectionSuffix` (5 tests); add `TestTruncateCorrection` (3 ported tests) |
| `tests/unit/test_grounding_integrated_fallback.py` | rewrite `TestIdiomGuard`'s test to use local literal shapes instead of the builder; import cleanup |
| `tests/unit/test_grounding_wiring.py` | replace `test_correction_survives_storage_sanitize` with `test_integrated_fallback_survives_storage_sanitize` (drives `build_integrated_fallback`, both kinds, with a negative control) |
| `tests/unit/test_audit0831_fixes.py` | reword one docstring sentence (no behaviour change) |
| `docs/execution/generalization/batches/A05d.md` | this file (new) |

Nothing else was opened for write. `docs/BUG_CLASSES.md`,
`config/bug_class_baseline.json`, `scripts/check_bug_classes.py`,
`scripts/bug_class_guards/**`, `hooks/**`, `.github/**`,
`tests/bug_class_guards/**`, `tests/conftest.py`, `conftest.py` and all
prior batches' historical docs (A05a.md, A05b-*.md, A05_design.md) were not
touched.

## 3. Before/after table of every reworded line (core/grounding_check.py, gui/handlers.py, tests/unit/test_audit0831_fixes.py)

Line numbers are the pre-edit (verified) positions named in the brief.

| # | File : pre-edit line(s) | Old wording | New wording |
| - | --- | --- | --- |
| 1 | `core/grounding_check.py:17-18` (module docstring, pipeline) | `→ build_grounding_correction() (visible suffix, action-guard idiom, gentler wording on elevated tones).` | `→ integrate_grounding_correction() (bounded rewrite) or, on fallback, build_integrated_fallback() (spliced or standalone correction).` |
| 2 | `core/grounding_check.py:23` (Key functions bullet) | `- build_grounding_correction(correction, *, elevated=False) -> str` | `- build_integrated_fallback(response, verdict, *, elevated=False) -> Optional[IntegratedFallback]` |
| 3 | `core/grounding_check.py:871` (section header) | `# Correction suffix (action-guard idiom: appended to display AND storage)` | `# Correction truncation (shared by the integrated-fallback builders below: caps the correction text before it is spliced or shipped standalone)` |
| 4 | `core/grounding_check.py:893-903` | `def build_grounding_correction(...): ...` (11-line function) | **deleted** |
| 5 | `core/grounding_check.py:906-909` (INACTIVE comment) | `— INACTIVE, no caller yet. A05b wires this into gui/handlers.py's fallback branch for when ... Unlike the suffix builder above, this NEVER appends...` | `— the live fallback for _apply_grounding_check since A05b-1, for when ... It never appends to a flawed draft: it either splices...` |
| 6 | `core/grounding_check.py:1375` (`__all__`) | `"build_grounding_correction",` | **removed** |
| 7 | `core/grounding_check.py:1234-1242` (integration block comment) | `Every display path streams, so the user has read the draft by the time the verifier finishes — but every path also ends with...; bounded guards fall back to the appended suffix on any doubt.` | `In log_only mode the display path streams...; correct mode buffers instead, so nothing unreviewed is ever shown. Either way every path ends with...; bounded guards fall back to build_integrated_fallback's spliced-or-standalone correction on any doubt.` |
| 8 | `core/grounding_check.py:1290-1291` (`integrate_grounding_correction` docstring) | `or None (caller falls back to the appended suffix).` | `or None (caller falls back to build_integrated_fallback's spliced-or-standalone correction).` |
| 9 | `core/grounding_check.py:1323` (log) | `"[GroundingCheck] Integrator timed out — falling back to suffix"` | `"[GroundingCheck] Integrator timed out — falling back to build_integrated_fallback"` |
| 10 | `core/grounding_check.py:1326` (log) | `f"[GroundingCheck] Integrator failed — falling back to suffix: {e}"` | `f"[GroundingCheck] Integrator failed — falling back to build_integrated_fallback: {e}"` |
| 11 | `core/grounding_check.py:1343` (log) | `... — falling back to suffix")` | `... — falling back to build_integrated_fallback")` |
| 12 | `core/grounding_check.py:1358` (log) | `f"({_cm} {_cd}) — falling back to suffix"` | `f"({_cm} {_cd}) — falling back to build_integrated_fallback"` |
| 13 | `core/grounding_check.py:1364` (log) | `"pair — falling back to suffix"` | `"pair — falling back to build_integrated_fallback"` |
| 14 | `gui/handlers.py:3237-3239` (`_apply_grounding_check` docstring) | `suffix is always "" as of A05b-1; the second tuple slot stays only for call-site compatibility until A05d retires it along with build_grounding_correction.` | `suffix is always "" as of A05b-1; the second tuple slot stays only because both call sites unpack it (_ag_gc_revised, _ag_gc_suffix; _gc_revised, _gc_suffix).` |
| 15 | `tests/unit/test_audit0831_fixes.py:373-376` (docstring only) | `(A05b-1 — build_grounding_correction is no longer called from _apply_grounding_check, so faking IT no longer reaches this branch; ...)` | `(A05b-1 — the retired suffix builder is no longer called from _apply_grounding_check, so faking it no longer reaches this branch; ...)` |

Control flow, return values, telemetry keys and log levels were not
touched anywhere in this table — verified by the "no behaviour change"
contract check in §6 and by every test run in §7/§8 passing unchanged.

Docs left untouched per the brief: `A05a.md`, `A05b-*.md`, `A05_design.md`
and other historical batch packets keep the retired name — historical
record, not live documentation.

## 4. Tests-on-unedited-source (before deleting anything)

Order of work step 3. New/ported tests were written first
(`TestTruncateCorrection` port, `TestIdiomGuard` rewrite, the sanitize-test
replacement) while `build_grounding_correction`, its old tests and their
imports were still in place; the NEW tests reference only surviving
symbols. One command, digest first:

```
$ sha256sum core/grounding_check.py gui/handlers.py
3994ab36cbda7a4424a8866308a9854e85176c01d4e99d99fe6de50f0b00f854  core/grounding_check.py
049b5d3b13a4663ce91fec4ebec1daf16804c25ec2ff6dd3e107536acb731204  gui/handlers.py

$ systemd-run ... python -m pytest -q -p no:cacheprovider -p asyncio \
    tests/unit/test_grounding_check.py \
    tests/unit/test_grounding_integrated_fallback.py \
    tests/unit/test_grounding_wiring.py
140 passed, 3 warnings in 2.88s
exit 0; wall 0:05.12 (outer systemd-run/time wrapper); Maximum resident
set size: 1,073,700 KB
```

Digests match the pre-edit digests exactly (source untouched at this
point) — confirms the new/ported tests exercise real, already-shipped
behaviour, not code this batch is about to add.

## 5. Mutation proofs M1–M3 (non-vacuity; not tests, reverted after each)

Each ran as one command: pre-mutation digest → apply (Python text
substitution on `core/grounding_check.py`'s source, never imported as a
module) → pytest on mutated source (expect FAIL) → revert → post-revert
digest (must equal pre-mutation) → pytest on reverted source (expect
PASS). Guard checked immediately before each command; no other pytest was
running at any point.

Original digest throughout: `3994ab36cbda7a4424a8866308a9854e85176c01d4e99d99fe6de50f0b00f854`.

### M1 — `build_integrated_fallback` returns text + the ELEVATED suffix shape

One-line insertion in each of `_build_spliced` and `_build_standalone`,
right before the `if card:` append, so the mutated text ends with the
elevated shape (`"\n\n> ⚠️ One thing I want to gently set straight, because
it matters: " + truncated_correction`).

- Mutated run: **2 failed** (both `TestIdiomGuard` parametrized cases:
  spliced, standalone). Failure line:
  ```
  tests/unit/test_grounding_integrated_fallback.py:397: in test_no_suffix_idiom_and_not_response_plus_suffix
      assert not result.text.endswith(shape)
  E   AssertionError: assert not True
  E    +  where True = ...endswith('\n\n> ⚠️ One thing I want to gently set straight, because it matters: The correct fact is X.')
  ```
  The failing `shape` printed in the traceback is the **elevated** shape —
  confirms the failure comes from the new elevated-shape assertion inside
  the `for shape in (plain, elevated):` loop (plain iteration passed
  first), never reaching the trailing `assert _IDIOM not in result.text`
  (`_IDIOM = "> ⚠️ Correction:"` does not appear in the elevated wording,
  so it could not have caught this on its own).
- Post-revert digest: `3994ab36...854` (matches). Reverted run: **2
  passed**.

### M2 — same, with the PLAIN suffix shape

Same insertion points, plain shape (`"\n\n> ⚠️ Correction: " +
truncated_correction`).

- Mutated run: **2 failed**, same test/line (`:397`,
  `assert not result.text.endswith(shape)`), traceback shows the **plain**
  shape (`'\n\n> ⚠️ Correction: The correct fact is X.'`) — the failure
  fires on the FIRST loop iteration (`plain` is checked before
  `elevated`), i.e. strictly before `_IDIOM` is ever reached, matching the
  brief's "before `_IDIOM`" requirement.
- Post-revert digest: `3994ab36...854` (matches). Reverted run: **2
  passed**.

### M3 — `_truncate_correction` returns its input unchanged (identity at its top)

One-line insertion (`return correction`) right after the docstring, before
any stripping/joining/capping logic runs.

- Mutated run: **3 failed** — `TestTruncateCorrection::test_two_sentence_cap`
  (`assert ('Three' not in 'One. Two. Three. Four.'` — Three not removed),
  `test_char_cap` (`assert 1000 <= (300 + 1)` — no cap applied), and
  `test_empty_and_whitespace` (`assert '   ' == ''` — whitespace not
  collapsed; not required by the brief but a true superset of the
  identity-mutation's effect, recorded for completeness).
- Post-revert digest: `3994ab36...854` (matches). Reverted run: **3
  passed**.

## 6. Contract greps

Pre-deletion reference list (Order of work step 5, before touching the
builder), `grep -rn "build_grounding_correction" --include=*.py . --exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=node_modules`:

```
core/grounding_check.py:17, 23, 893 (def), 1375 (__all__)
gui/handlers.py:3239 (docstring)
tests/unit/test_audit0831_fixes.py:374 (docstring)
tests/unit/test_grounding_check.py:22 (import), 384, 389, 395, 399, 404, 405 (TestCorrectionSuffix)
tests/unit/test_grounding_integrated_fallback.py:22 (import), 388 (comment)
```

Post-edit (Order of work step 7), both contract greps:

```
$ grep -rn "build_grounding_correction" --include=*.py . --exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=node_modules
(no output, exit 1)

$ grep -n "falling back to suffix\|appended suffix\|suffix builder" core/grounding_check.py
(no output, exit 1)
```

Both clean, as required by the contract.

## 7. Focused run + sweep (post-edit, final state)

Guard checked before each command; no other pytest running at any point.

Focused (one capped run, per the brief's TESTS list):

```
tests/unit/test_grounding_check.py tests/unit/test_grounding_integrated_fallback.py
tests/unit/test_grounding_wiring.py tests/unit/test_grounding_log_only.py
tests/unit/test_audit0831_fixes.py tests/unit/test_grounding_delivery_atomicity.py
tests/unit/test_grounding_buffered_delivery.py tests/unit/test_grounding_cancel_storage_gate.py
→ 223 passed, 4 warnings, exit 0; wall 0:22.89; Maximum resident set size: 1,200,852 KB
```

Sweep, re-derived importer set confirmed first:
`grep -rlE "core\.grounding_check" tests/unit` returns exactly the 15 files
already present in `$S/a05c_sweep_00` ∪ `$S/a05c_sweep_01` — no file was
missing.

```
sweep_00 (20 files) → 500 passed, exit 0; wall 0:36.90; Max RSS 1,764,992 KB
sweep_01 (19 files) → 751 passed, exit 0; wall 0:39.44; Max RSS 1,767,960 KB
```

### Test-count reconciliation against 1,253

`500 + 751 = 1251`. Delta from the parent's A05c rerun (1,253) on this same
39-file set is **exactly −2**:
- **−5**: `TestCorrectionSuffix` deleted (`test_normal_wording`,
  `test_elevated_wording`, `test_two_sentence_cap`, `test_char_cap`,
  `test_empty_correction_noop`).
- **+3**: `TestTruncateCorrection` added (`test_two_sentence_cap`,
  `test_char_cap`, `test_empty_and_whitespace`).
- **0**: `TestIdiomGuard`'s test was rewritten in place (still 2
  parametrized cases); `test_correction_survives_storage_sanitize` was
  renamed to `test_integrated_fallback_survives_storage_sanitize` (still 1
  test).

`1253 − 5 + 3 = 1251` — matches exactly.

`sweep_00` above ran before the ruff fix in §8 (a pure redundant-import
deletion in `tests/unit/test_grounding_wiring.py`, no behaviour change).
Re-ran `sweep_00` against the final post-fix file state to remove any
ambiguity: **500 passed again, exit 0** (wall 0:37.49, Max RSS 1,765,868
KB) — identical count, confirming the fix was a true no-op. Final digests
for all six changed files were re-checked afterward and are unchanged from
§1's post-edit values (no further edits after this rerun).

## 8. Ruff

`ruff 0.14.9`. First pass on the six changed files caught one real defect
introduced by this batch: `tests/unit/test_grounding_wiring.py`'s new test
re-imported `GroundingVerdict` inside the function body, shadowing the
existing module-level import (`F811 Redefinition of unused
'GroundingVerdict'`). Fixed by dropping the redundant name from the local
`from core.grounding_check import build_integrated_fallback` import (no
behaviour change — `GroundingVerdict` was already visible from the
module-level import). Re-ran the file's tests after the fix (§7's rerun
below) to confirm still green, then re-ran ruff:

```
$ python -m ruff check core/grounding_check.py gui/handlers.py \
    tests/unit/test_grounding_check.py tests/unit/test_grounding_integrated_fallback.py \
    tests/unit/test_grounding_wiring.py tests/unit/test_audit0831_fixes.py
All checks passed!
```

Post-fix single-file rerun (`tests/unit/test_grounding_wiring.py`): exit 0,
all passed (folded into the focused/sweep runs above, which were run after
this fix).

## 9. Bug-class scan (read-only, before and after)

```
scanner                       mode    files  found  base  new  stale
dm01_raw_substring            gate    394    9      12    0    3
dm17_apply_without_guard      gate    608    36     39    0    3
dm18_except_returns_empty     gate    123    79     79    0    0
dm31_live_state_default       gate    394    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Identical before and after (byte-identical table, same 6 STALE baseline
rows — 3x `dm01_raw_substring` in `knowledge/git_memory.py`, 3x
`dm17_apply_without_guard` in `scripts/cleanup_stale_illness.py` /
`graph_relation_normalize.py` / `reclassify_proposals.py`, all pre-existing
and unrelated to this batch's files). 0 new findings either time. Exit 1
both times (the pre-existing STALE debt, per the designed handoff — not
attempted to be made green).

## 10. Size

Target ≤120 changed lines, hard stop ≤450. `git diff --stat` against HEAD
shows large numbers for `core/grounding_check.py` and `gui/handlers.py`
because that diff is cumulative across every batch since 328a8ec (A05a
through A05c already touched these files heavily); it is not a measure of
this batch's own delta. Measuring this batch's edits in isolation (each
`Edit` tool call's before/after text, real line-diff via `difflib`, summed
across all six files): **91 deletions + 106 insertions = 197 changed
lines**. Over the ≤120 target — the brief's own required test content
(porting 3 direct-truncation tests, asserting both plain AND elevated
shapes with a negative control in the idiom guard, and building both
spliced AND standalone results with an in-test negative control in the
sanitize test) accounts for most of the excess — but well inside the ≤450
hard stop, so no split was needed.

## 11. Note for the class-guard owner — BC-45 / BC-50

Read `docs/BUG_CLASSES.md` read-only (not edited); no baseline or catalog
change made. Proposing only:

**BC-45 "Shown ≠ stored ≠ recorded"** (docs/BUG_CLASSES.md:458-463) never
named `build_grounding_correction` in its Incidents or Closure text — its
listed incidents are the 2026-08-28 review-gate silent swap, the 2026-08-31
debug-record-before-integration ordering bug, and the 2026-09-08 display-only
line stripper. The retired builder's own section header comment ("action-guard
idiom: appended to display AND storage") was an informal, in-module claim of
BC-45-style parity for the correction path, never linked into the catalog.
Now that the builder is gone, that informal claim is moot; the *current*
BC-45 closure mechanism for the grounding-correction path is
`build_integrated_fallback`'s whole-bubble-replacement (both display and
`final_output` are replaced identically on every path — see
`gui/handlers.py`'s `_apply_grounding_check` docstring). This batch's
`tests/unit/test_grounding_wiring.py::test_integrated_fallback_survives_storage_sanitize`
is a fresh, direct proof that the correction (both spliced and standalone
kinds) survives `_sanitize_response_text`, the storage-boundary transform —
replacing the old `test_correction_survives_storage_sanitize`, which
exercised the retired builder's suffix through the same boundary. No catalog
text needs to change for BC-45 to stay accurate; the class-guard owner may
optionally cite the new test name in BC-45's Closure bullet if a
worked-example test reference is wanted there.

**BC-50 "Verifier's own false corrections"** (docs/BUG_CLASSES.md:493-498):
its Incidents predate A05a/A05b-1 and describe false corrections shipped
while `build_grounding_correction`'s draft-plus-suffix idiom (or an earlier
mechanism) was the live delivery path (2026-08-29 semester flag, Saturday→
Sunday, 2026-09-01 Sep-13→Sep-6, the 2026-09-02 PSA splice; "42 fires → 25
shipped corrections, ≥9 false, 0 documented true"). Since A05b-1, "correct"
mode's only shipped-correction path is `integrate_grounding_correction`
(bounded rewrite) falling back to `build_integrated_fallback` (spliced or
standalone) — `build_grounding_correction` had already been cut out of the
runtime call graph before this batch (zero callers; this batch only removed
the now-dead code and its tests). BC-50's "Status: partial — correct mode
unproven" is unaffected by this batch (that status is about verifier
precision, an orthogonal axis this batch does not touch), but any *new*
BC-50 incident write-up on the correct-mode path should now cite
`build_integrated_fallback`'s splice/standalone mechanism, not "the
correction suffix" — the retired terminology is no longer accurate for
anything shipped after A05b-1. No catalog text edit is required unless the
class-guard owner wants to add a terminology note to BC-50's Mechanism or
Closure bullet.

No BC-58 (guard covers only the first-observed path) concern applies here:
this batch only deletes dead code and rewords comments/log lines; it adds
no new guard.

## 12. Limitations / open decisions

- `core/grounding_check.py`'s `build_integrated_fallback` docstring still
  says, at its own last line (pre-edit ≈1212), "A05a is INACTIVE: nothing
  calls this function yet." — also stale (the function has had a caller
  since A05b-1) but **not** in the brief's enumerated stale-wording list
  (871, 905-909, 1234-1242, 1290-1291, the 5 log lines) and the ownership
  clause for this file says "Nothing else." Left untouched to stay inside
  the explicit scope; flagging here in case the class-guard owner or a
  future batch wants it reworded too.
- Size is 197 changed lines against a ≤120 target (well under the ≤450
  hard stop) — see §10 for the breakdown; no split was proposed because it
  was not needed.
- No BC-58/other scanner-baseline anchor referenced
  `build_grounding_correction` by name, so no baseline row needed
  reconciling beyond the pre-existing 6 STALE rows already carried by the
  base scan (§9), which are unrelated to this batch.
- All work is confined to this clone
  (`/home/lukeh/daemon_exec/generalization`); `/home/lukeh/Daemon_v1` was
  never read, edited, or run.

## Parent review and integration (2026-09-14)

Accepted as delivered. The parent made no source or test edit.

### Manifest

- `sha256sum -c manifest_post_A05c.txt` fails for exactly the six owned files and no others:
  - `core/grounding_check.py` → `a2f868b6…`
  - `gui/handlers.py` → `6d00b34f…`
  - `tests/unit/test_grounding_check.py` → `614d38ce…`
  - `tests/unit/test_grounding_integrated_fallback.py` → `d3f3177b…`
  - `tests/unit/test_grounding_wiring.py` → `7b9bf09a…`
  - `tests/unit/test_audit0831_fixes.py` → `311dabf2…`
- Path-set delta: exactly one new path, this file.
- HEAD `328a8ec` unchanged; push is DISABLED.
- `manifest_post_A05d.txt` records the tree after this section and after two parent doc edits made in the same window: amendments to `failure_outcome_design.md`, and the queue and miscount corrections in `owner_hardcoding_review_20260913.md`.

### Code review (the parent read every changed region)

- **`core/grounding_check.py`:**
  - the builder and its `__all__` entry are gone;
  - the module docstring names the integrator and `build_integrated_fallback`;
  - the section header describes only the truncation helper;
  - the A05a section comment says "live fallback since A05b-1";
  - the five log lines end "falling back to build_integrated_fallback". Logger, level and interpolation are unchanged.
- **`gui/handlers.py`:** one docstring sentence only.
- **Two wording issues, not accepted as-is but deferred to H01**, which owns this file next:
  - (a) `build_integrated_fallback`'s docstring still ends "A05a is INACTIVE: nothing calls this function yet." (≈1202). The worker flagged it correctly as out of scope.
  - (b) The new integration-block comment (≈1226-1228) says correct mode buffers "so nothing unreviewed is ever shown". That overclaims: on a verifier failure, correct mode ships the draft once, unmodified.
  - The H01 brief now owns both rewordings. The standalone-lead comment (≈1160-1163) is accurate in context (that path fires only after a flagged verdict) and stays.
- **Tests:**
  - `TestTruncateCorrection` bounds the helper's own output (≤ `_MAX_CORRECTION_CHARS` + 1) and covers the two-sentence cap and empty/whitespace input.
  - The idiom guard asserts both literal shapes, `!=` and `endswith`, before `_IDIOM`.
  - The sanitize test drives both fallback kinds through `_sanitize_response_text` and asserts the correction text and each lead survive.
  - Minor, accepted: its "draft lacks the correction" control is asserted for the standalone input only. The spliced input literal visibly lacks the word too.
  - The mutation proofs M1-M3 in §5 show the new assertions are live.

### Parent rerun (guarded, capped, `-p asyncio`)

- **Focused (8 files):** 223 passed, exit 0, wall 0:25.64, peak RSS 1,198,708 KB.
- **39-file sweep:** 500 passed (0:36.77, 1,764,336 KB) + 751 passed (0:38.42, 1,768,604 KB) = **1,251**, 0 failed. This matches §7's reconciliation (1,253 − 5 + 3).
- **`ls -la --time-style=full-iso data`:** identical before and after.
- **Grep proofs:** `build_grounding_correction` in *.py → none; stale suffix wording in `core/grounding_check.py` → none.
- **`ruff check`** on the six files: all checks passed.
- **Scan:** 0 new findings in every gate scanner. Exit 1 comes from the same 6 pre-existing STALE baseline rows (class-guard owner handoff).

### Size

197 changed lines, over the 120 target and under the 450 hard stop. The excess is the test content the brief required; accepted.

### Class-guard note

§11's BC-45/BC-50 proposals are forwarded as written. No catalog change is needed for accuracy.

### A05 chain status

A05a, A05b-1..5 (with parent fixes D1 and D3), A05c and A05d are integrated. H01 (externalizing the fallback thresholds) completes this file's sequence.
