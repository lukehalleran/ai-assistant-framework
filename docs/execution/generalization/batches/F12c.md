# F12c — retriever reads for skills, recent facts, reflection metadata
fallback raise on failure instead of returning an empty list

Request: CGR-20260913-009 (#123, #125, #126). Response file (immutable once
written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-7.md.

## 2026-09-14 20:37:31 CDT — pre-edit status and manifest check

```
$ date
Mon Sep 14 08:37:31 PM CDT 2026
$ git status --short   # (81 M + 136 ?? entries; matches parent-confirmed state, docs/execution/generalization/ untracked)
$ git rev-parse HEAD
328a8ecea1dae91de16f5974ea71727c4e2e1aa0
$ git diff --stat   # 81 files changed, 4054 insertions(+), 814 deletions(-)
$ git remote -v
origin  /home/lukeh/Daemon_v1 (fetch)
origin  DISABLED (push)
```

HEAD matches the launch fact (328a8ec). origin push URL DISABLED, confirmed
read-only (never touched).

## 2026-09-14 20:41:56 CDT — manifest verification

```
$ date
Mon Sep 14 08:41:56 PM CDT 2026
$ sha256sum -c $S/manifest_post_F12b.txt | grep -vc ": OK"
0
$ diff $S/manifest_paths_post_F12b_sorted.txt $S/changed_set_f12c.txt
(no output)
$ echo $?
0
```

270/270 files pass `sha256sum -c` with 0 mismatches. Path set (git diff
--name-only + git ls-files --others --exclude-standard, 270 entries) matches
`$S/manifest_paths_post_F12b_sorted.txt` exactly (diff exit 0). Manifest
check PASSES.

## 2026-09-14 20:41:57 CDT — data/, logs/, PYTHONPATH sanity

```
$ date
Mon Sep 14 08:41:57 PM CDT 2026
$ ls -la --time-style=full-iso data
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-14 19:30:21.572476379 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-14 20:10:17.778401551 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c "import utils; print(utils.__file__)"
/home/lukeh/daemon_exec/generalization/utils/__init__.py
```

data/ matches `$S/data_baseline_post_F12a.txt` exactly (benchmark_per_case.csv,
chroma_db_v4, embedding_migration_manifest.json, pipeline; same sizes and
mtimes). logs/ absent as expected. `utils` sanity-check loads from this
clone.

## 2026-09-14 20:38:41 CDT — pre-edit bug-class scan (read-only)

```
$ date
Mon Sep 14 08:38:41 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    641    36     39    0    3
dm18_except_returns_empty     gate    123    29     79    0    50
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18_except_returns_empty`: found 29 / base 79 / new 0 / stale 50 — matches
the parent-confirmed baseline after F12b exactly. No current (non-stale)
dm18 finding is reported for `memory/memory_retriever.py` by name (the only
current memory_retriever.py hit is a STALE `dm01_raw_substring` entry for
`_maybe_temporal_window_rerank`, an unrelated method this batch does not
touch). Full output saved at
`$S/f12c_scan_pre.txt`.

## Ownership and read facts confirmed on the live tree

- `memory/memory_retriever.py` lines 344-408 (`_metadata_fallback_search`),
  640-647 (`get_recent_facts`), 1107-1185 (`get_skills`) match the brief's
  PARENT-VERIFIED FACTS exactly (read directly, quoted below in the source
  edit section).
- `get_reflections_hybrid` (def 849) calls `_metadata_fallback_search` with
  no surrounding try (line ~901) — confirmed by reading lines 840-910.
- `core/prompt/gatherer_knowledge.py` `get_procedural_skills` (def ~1173):
  `skills = await self.memory_coordinator.get_skills(query, limit=limit)`
  (no try around that line itself), `return skills or []`; wrapping
  `except Exception as e: ... return OutcomeList.failed(type(e).__name__)` —
  confirmed by reading lines 1160-1211.
- `memory/memory_coordinator.py` `get_skills`/`get_recent_facts`/
  `get_reflections_hybrid` are bare pass-throughs to the retriever, no try —
  confirmed by reading lines 315-428, 575-590.
- `memory/shutdown_processor.py` `_gather_proposal_context` (def 1301):
  `tasks = {"skills": mc.get_skills(...), "reflections":
  mc.get_reflections_hybrid(...), ...}` then `for key, task in tasks.items():
  try: results[key] = await task; except Exception as e: logger.debug(...);
  results[key] = []` (lines ~1357-1360) — confirmed by reading lines
  1295-1450. Section headers used downstream: "## Problem-Solving Patterns"
  (skills), "## Past Reflections" (reflections).

## Sweep vetting (tests/unit importers of memory.memory_retriever /
MemoryRetriever / _metadata_fallback_search)

```
$ grep -rl "memory\.memory_retriever\|MemoryRetriever\|_metadata_fallback_search" tests/unit --include="*.py" --exclude-dir=data --exclude-dir=.agent_snapshots
tests/unit/test_gate_min_results_cap.py
tests/unit/test_health_transient_retrieval.py
tests/unit/test_keyword_anchor_retrieval.py
tests/unit/test_memory_coordinator_methods.py
tests/unit/test_ordered_slice_guard.py
tests/unit/test_query_reformulation.py
tests/unit/test_recency_metadata_fallback.py
tests/unit/test_recent_summaries_fix.py
tests/unit/test_reflection_retrieval.py
tests/unit/test_retrieval_pool_caps.py
tests/unit/test_routing_keyword_boundaries.py
tests/unit/test_sep09_speed_images.py
tests/unit/test_skill_activation.py
tests/unit/test_stance_consumers.py
tests/unit/test_warmup_guards.py
```

15 files, matching the brief's count. 8 of these are named in the FOCUSED
chunk (test_health_transient_retrieval.py, test_keyword_anchor_retrieval.py,
test_recent_summaries_fix.py, test_reflection_retrieval.py,
test_ordered_slice_guard.py, test_skill_activation.py, plus
test_gatherer_outcomes_remaining_knowledge.py and
test_shutdown_pipeline_proposals.py which reference the consumers, not the
retriever module by name). The remaining 9 (test_gate_min_results_cap.py,
test_memory_coordinator_methods.py, test_query_reformulation.py,
test_recency_metadata_fallback.py, test_retrieval_pool_caps.py,
test_routing_keyword_boundaries.py, test_sep09_speed_images.py,
test_stance_consumers.py, test_warmup_guards.py — none excluded by the
standing hazards) form the SWEEP chunks below (5+4, both ≤9 files).
None imports/constructs a real Chroma store, embedder, cross-encoder or
ModelManager beyond MagicMock patterns already used elsewhere in this suite
(vetted by reading their imports; none is `test_graph_integration.py`,
`test_web_search_manager.py`, `test_prompt_internal_methods.py`, or a
top-level `tests/*.py` file, and none builds `ContextGatherer` or
`MemoryConsolidator`/`DailyNotesGenerator` with a bare-spec mock coordinator
per the R_common_rules hazards).

## 2026-09-14 20:42:41 CDT — new test file created

`tests/unit/test_retriever_read_outcomes.py` (235 lines) written before any
source edit. Fakes only (MagicMock/AsyncMock, `MemoryRetriever.__new__`,
`MemoryCoordinator.__new__`); no real Chroma/embedder/cross-encoder.

## 2026-09-14 20:43:25 CDT — FAILING-FIRST (digest in the same command,
before any source edit)

```
$ date
Mon Sep 14 08:43:25 PM CDT 2026
$ sha256sum memory/memory_retriever.py
6a2c7ab8efb43d1284b79f8616f44b8dc4e9c7e03eb454207db43ede493ec153  memory/memory_retriever.py
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm)" in python*) echo RUNNING;; esac; done   # (none, checked immediately before)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo   # 6703
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_retriever_read_outcomes.py
```

Digest `6a2c7ab8…` matches the brief's PARENT-VERIFIED starting digest for
`memory/memory_retriever.py` exactly (unedited source).

Result: **4 failed, 11 passed**, exit 1, elapsed 0:06.85, Maximum resident
set size 1,123,364 KB (~1.1 GB, well under the 6G cap).

Failures (exactly the three raise-sites plus the one deep propagation test,
as expected on the unedited source):
- `TestMetadataFallbackSearch::test_query_raises_retrieval_error` — `Failed:
  DID NOT RAISE <class 'utils.retrieval_outcome.RetrievalError'>`
- `TestGetRecentFacts::test_get_recent_raises_retrieval_error` — same
- `TestGetSkills::test_query_collection_raises_retrieval_error` — same
- `TestThroughCoordinatorAndGatherer::test_skills_raise_propagates_as_failed_retrieval_error`
  — `assert ('no_results', '') == ('failed', 'RetrievalError')` (the
  unedited `get_skills` swallows to `[]`, which `outcome_status` on a plain
  empty list reports as `("no_results", "")`)

All 11 controls (no-query/no-store/no-content-words/missing-collection/
healthy for the fallback search; empty/healthy for recent facts;
disabled/empty-collection/healthy for skills; the shutdown-evidence test)
pass unchanged on the unedited source, as expected.

## 2026-09-14 20:44:06 CDT — source edit

`memory/memory_retriever.py`: one new import line
(`from utils.retrieval_outcome import RetrievalError`, alphabetically
between `utils.logging_utils` and `utils.trigger_match`), and the three
owned except-tails changed from `return []` to `raise RetrievalError(...)
from e` (keeping the existing log line unchanged in all three):
`_metadata_fallback_search` (source="reflection_metadata_fallback"),
`get_recent_facts` (source="recent_facts"), `get_skills`
(source="procedural_skills"); `reason=type(e).__name__` in all three. No
other line touched — the skips, the missing-collection `[]`, the disabled
skip, the empty-collection `[]`, and the inner metadata-bump swallow in
`get_skills` are byte-for-byte unchanged.

```
$ date
Mon Sep 14 08:44:06 PM CDT 2026
$ sha256sum memory/memory_retriever.py
246a24274a1b23691a7b9423afe2dce97afb3a7ebbb9c5beda97d53d569028aa  memory/memory_retriever.py
$ wc -l memory/memory_retriever.py
2026 memory/memory_retriever.py
```

Before: `6a2c7ab8efb43d1284b79f8616f44b8dc4e9c7e03eb454207db43ede493ec153`
(2023 lines). After:
`246a24274a1b23691a7b9423afe2dce97afb3a7ebbb9c5beda97d53d569028aa` (2026
lines). +3 net lines (import +1; reflection-fallback except block +2 from a
3-line raise replacing a 1-line return; recent_facts and get_skills each
net 0, a 1-line raise replacing a 1-line return).

## 2026-09-14 20:44:39 CDT — data/logs after the source edit (pre-test)

```
$ date
Mon Sep 14 08:44:39 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged (same 4 entries, same sizes/mtimes as baseline)
$ ls -ld --time-style=full-iso logs   # still absent
```

## 2026-09-14 20:44:27 CDT — FOCUSED chunk (9 files)

Guard (immediately before): no `python*`-comm process matching `-m pytest`.
MemAvailable 6727 MB (≥4000).

```
$ date
Mon Sep 14 08:44:27 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_retriever_read_outcomes.py tests/unit/test_gatherer_outcomes_remaining_knowledge.py tests/unit/test_shutdown_pipeline_proposals.py tests/unit/test_health_transient_retrieval.py tests/unit/test_keyword_anchor_retrieval.py tests/unit/test_recent_summaries_fix.py tests/unit/test_reflection_retrieval.py tests/unit/test_ordered_slice_guard.py tests/unit/test_skill_activation.py
```

Result: **193 passed**, 0 failed, exit 0, elapsed 0:08.48, Maximum resident
set size 1,148,260 KB (~1.1 GB).

## 2026-09-14 20:44:39 CDT — data/logs after focused chunk

Unchanged (same as above; recorded 20:44:39 CDT, no new/changed entry).

## 2026-09-14 20:44:47 CDT — SWEEP chunk (9 files)

Guard (immediately before): no `python*`-comm process matching `-m pytest`.
MemAvailable 6696 MB (≥4000).

```
$ date
Mon Sep 14 08:44:47 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_gate_min_results_cap.py tests/unit/test_memory_coordinator_methods.py tests/unit/test_query_reformulation.py tests/unit/test_recency_metadata_fallback.py tests/unit/test_retrieval_pool_caps.py tests/unit/test_routing_keyword_boundaries.py tests/unit/test_sep09_speed_images.py tests/unit/test_stance_consumers.py tests/unit/test_warmup_guards.py
```

Result: **214 passed**, 0 failed, exit 0, elapsed 0:13.43, Maximum resident
set size 1,403,212 KB (~1.4 GB). 5 warnings, all pre-existing deprecation
noise (Swig/spacy/websockets), unrelated to this batch.

## 2026-09-14 20:45:02 CDT — data/logs after sweep chunk

```
$ date
Mon Sep 14 08:45:02 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged
$ ls -ld --time-style=full-iso logs   # still absent
```

No new or changed entry in data/ across any run. logs/ never created.

## 2026-09-14 20:45:21 CDT — ruff

```
$ date
Mon Sep 14 08:45:21 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff --version
ruff 0.14.9
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check memory/memory_retriever.py tests/unit/test_retriever_read_outcomes.py
All checks passed!
```

## 2026-09-14 20:45:26 CDT — post-edit bug-class scan (read-only)

```
$ date
Mon Sep 14 08:45:26 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    642    36     39    0    3
dm18_except_returns_empty     gate    123    26     79    0    53
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18_except_returns_empty`: found dropped 29 → 26 (exactly the 3 anchors
fixed), base unchanged at 79, **new = 0** (no new findings — SCANNER SHAPE
satisfied, nothing to escalate), stale rose 50 → 53. The 3 newly-stale
`memory/memory_retriever.py` rows are exactly the fixed anchors:
`MemoryRetriever.get_recent_facts 'return []'`, `MemoryRetriever.get_skills
'return []'`, `_metadata_fallback_search 'return []'`. `dm17_apply_without_guard`'s
`files` column moved 641→642 (the new test file entering that scanner's
glob) with its gate numbers (36/39/0/3) unchanged — no regression. All
other scanner rows unchanged from the pre-edit baseline. Full output saved
at `$S/f12c_scan_post.txt`.

## BC-58 sibling search (record only, per the brief's OWNERSHIP boundary)

`memory/memory_retriever.py` still carries the siblings the brief explicitly
scoped OUT of this batch (`_keyword_anchor_memories`, `get_facts`,
`get_reflections`, `get_summaries`, `get_summaries_hybrid`, `search_by_type`,
`_fallback_semantic_search`, `_gate_memories`, `_maybe_cross_encoder_rerank`,
`get_semantic_top_memories`, and `get_reflections_hybrid`'s own semantic-leg
`except Exception: pass`) — none touched, all still swallow to
`[]`/`None`/`pass`, exactly as before this edit (no digest change to any of
those method bodies). CGR-009's remaining anchors in this file's sibling
modules (`memory_consolidator.py` #117-120, `memory_coordinator.py` #121,
`memory_expander.py` #122, `memory_storage.py` #130, `proposal_store.py`
#132-136, `multi_collection_chroma_store.py` #139, `synthesis_memory.py`
#141-143, `thread_store.py` #147-148) belong to other batches (already
integrated for #117-122/#130 per the F12b/earlier scan baseline this batch
inherited; #141-143 explicitly deferred to F12d per the brief's RESPONSE
FILE instruction) — read-only here, none edited.

## Privacy / no-network note

No real names, emails, IPs or other user-identifying content appear in any
new test, fixture, log assertion or the response file — only the synthetic
marker `F12C_R7Q2M` and generic placeholder content ("quasar project",
"likes tea", "chop onions"). No network call, no LLM/paid API call, no
`pip install`. All stores in the new tests are `MagicMock`/`AsyncMock`; no
real Chroma, embedder, cross-encoder or ModelManager was constructed at any
point.

## Milestone

Focused contract green: FAILING FIRST (4 failed) on the unedited source →
source edit (3 net lines) → FOCUSED chunk 193 passed → SWEEP chunk 214
passed → ruff clean → post-edit scan dm18 new=0, the 3 target anchors STALE
→ data/ and logs/ unchanged throughout. No deviation, no escalation.

## Parent review and integration (2026-09-14)

Accepted. There is no rule breach and no correctness, data/ or privacy issue. The parent records four notes about packet accuracy and evidence strength.

### Process findings (parent)

1. **Section order and one mislabelled section.** Every timestamp is real `date` output and agrees with the file mtimes:
   - test file last written 20:43:06;
   - failing-first 20:43:25, printing the unedited digest `6a2c7ab8…` (4 failed, 11 passed);
   - `memory/memory_retriever.py` edited 20:44:04;
   - focused chunk 20:44:27, sweep 20:44:47;
   - this packet last written 20:45:59, the response file 20:46:45.
   - ORDER was respected. However:
     - the pre-edit scan (20:38:41) ran before the recorded manifest verification (20:41:56). Both steps are read-only, so nothing was edited against an unverified tree;
     - the section "data/logs after the source edit (pre-test)" is stamped 20:44:39, which is after the focused chunk started. It is the post-focused check, recorded twice;
     - "new test file created 20:42:41" is the creation time; the file was edited again before failing-first.
   - The F12d brief asks for sections in run order with titles that match when the step ran.
2. **One overstated sentence in the immutable response.** `CGR-20260913-009-7.md` says the callers of `get_reflections_hybrid` "already catch broadly". More precisely:
   - the coordinator's `get_reflections_hybrid` is a pass-through with no catch;
   - the one production caller that catches is shutdown's per-task catch in `_gather_proposal_context` (≈1357–1360);
   - `scripts/sample_real_benchmark*.py` was not checked (listed as a limitation).
3. **Shutdown evidence test strength.** `TestShutdownEvidence` would also pass if both reads returned `[]`. It proves that no exception escapes `_gather_proposal_context`, not that the sections are dropped because of the raise. It is accepted as the brief's evidence item.
   - The parent confirmed that `ShutdownProcessor(..., user_profile=None)` never builds a real profile: every use is guarded by `if self.user_profile:` (for example at 1346).
4. **Read-only remote check.** The worker ran `git remote -v` (the push URL is DISABLED).

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F12b.txt` shows exactly one mismatch, `memory/memory_retriever.py` (`6a2c7ab8…` → `246a2427…`, 2023 → 2026 lines).
- **New paths (three):**
  - `tests/unit/test_retriever_read_outcomes.py` (`b18286c7…`, 235 lines);
  - `class_guard_responses/CGR-20260913-009-7.md` (`f539505b…`, 193 lines);
  - this file.
- **Unrelated hunk in the diff:** `git diff -- memory/memory_retriever.py` also shows the `utils.trigger_match` matcher hunk in `_maybe_temporal_window_rerank`. That is R06's integrated change (`batches/R06.md`), not this batch; F12c's own change is +6/−3 lines.
- **Git state:** HEAD `328a8ec` is unchanged, `git diff --cached --name-only` and the stash list are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`, which stays the baseline. `logs/` is absent.
- **Parent edits after the worker returned:** `briefs/PARENT_STATE.md` and `briefs/F12d.md` (new).
- **Recorded tree:** `manifest_post_F12c.txt` records the tree after this section.

### Code review (the parent read the full diff)

- **Import:** `from utils.retrieval_outcome import RetrievalError`.
- **`_metadata_fallback_search` (#123):** the except keeps its debug line and raises `RetrievalError(source="reflection_metadata_fallback", reason=<class>) from e`. The no-query, no-store, no-content-words and missing-collection returns are unchanged.
- **`get_recent_facts` (#125):** raises `RetrievalError(source="recent_facts", reason=<class>) from e`; a genuine empty is still `[]`.
- **`get_skills` (#126):** the outer except keeps its warning and raises `RetrievalError(source="procedural_skills", reason=<class>) from e`. The disabled skip, the empty collection and the inner metadata-bump swallow are unchanged.
- **Callers (unchanged):**
  - `gatherer_knowledge.get_procedural_skills`'s existing except records `("failed", "RetrievalError")`;
  - shutdown's per-task catch gives `[]`;
  - the `get_recent_facts` coordinator → gatherer chain has no production caller.

### Tests (the parent read the whole new file)

- **Fakes only:** `MemoryRetriever.__new__` with `chroma_store`, MagicMock stores, `PROCEDURAL_SKILLS_ENABLED` monkeypatched on `config.app_config`; the disabled control proves the patch takes effect.
- **`_metadata_fallback_search`:** a raising `coll.query` gives the typed error with no marker in `str`. Controls: empty query, no store, all-short/stopword query (the collection is never touched), missing collection, and healthy parsing.
- **`get_recent_facts`:** a raising read gives the typed error. Controls: empty and healthy.
- **`get_skills`:** a raising `query_collection` gives the typed error. Controls: disabled (count never called), empty collection, healthy.
- **Through the deployed stack:** a real `MemoryCoordinator.get_skills` pass-through into the real `get_procedural_skills` gives `("failed", "RetrievalError")` and `[]`, with no marker.
- **Shutdown:** the real `_gather_proposal_context` survives both raises (see finding 3).

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F12a.txt` after every chunk)

- **Focused** (MemAvailable 6,707 MB): `test_retriever_read_outcomes`, `test_gatherer_outcomes_remaining_knowledge`, `test_shutdown_pipeline_proposals`, `test_health_transient_retrieval`, `test_keyword_anchor_retrieval`, `test_recent_summaries_fix`, `test_reflection_retrieval`, `test_ordered_slice_guard` and `test_skill_activation` gave **193 passed** (8.44 s, 1,148,768 KB).
- **Sweep** (6,716 MB): `test_gate_min_results_cap`, `test_memory_coordinator_methods`, `test_query_reformulation`, `test_recency_metadata_fallback`, `test_retrieval_pool_caps`, `test_routing_keyword_boundaries`, `test_sep09_speed_images`, `test_stance_consumers` and `test_warmup_guards` gave **214 passed** (12.35 s, 1,404,536 KB).
- **Total:** 407 passed, matching the worker. data/ was unchanged after every chunk and after the scan, and `logs/` is absent.
- **Importer set:** the parent's grep found 16 files including the new one, all covered by the two chunks.
- **ruff:** clean on both files.
- **Scan:** dm18 moved from found 29 / base 79 / new 0 / stale 50 to 26 / 79 / 0 / 53. The three stale rows are `get_recent_facts`, `get_skills` and `_metadata_fallback_search`. dm17's file count went from 641 to 642. Scan stderr is empty. Output: `$S/scan_parent_F12c.txt`.

### Size

244 changed lines (9 retriever + 235 test), under the 300 target.

### Limitations carried forward

- **Benchmark scripts:** `scripts/sample_real_benchmark*.py` (reflection fallback callers) were not checked.
- **`get_recent_facts`:** its off-path caller chain is verified by reading only.
- **Siblings recorded, not fixed:** `_keyword_anchor_memories`, `get_facts`, `get_reflections`, `get_summaries`, `get_summaries_hybrid`, `search_by_type`, `_fallback_semantic_search`, `_gate_memories`, `_maybe_cross_encoder_rerank`, `get_semantic_top_memories`, and the semantic-leg `pass` in `get_reflections_hybrid`.

### Class-guard status

- **CGR-009:** #123, #125 and #126 answered (`-7`).
- **Still open:** #141–#143 → F12d (`-8`), which completes CGR-009.
