# F4 — FAISS semantic search and visual text search report failure
# distinctly; doc_cooccurrence's no-try callers stop reading a failure as
# a confident negative (CGR-20260913-008 anchors #107, #108, #109)

Worker evidence packet. Design source:
`docs/execution/generalization/failure_outcome_design.md`, "Decisions per
request" → CGR-008, batch row F4, and the 2026-09-14 amendment ("Two
consumers outside CGR-007's list"). Request packet (read-only, the only
file read in that checkout):
`/home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-008.md`.
Response file: `docs/execution/generalization/class_guard_responses/CGR-20260913-008-4.md`
(immutable once written; covers #107-#109 — the base response (F2)
answered #110/#111, `-2` (F3a) answered #103-#106 plus the upload-refusal
defect, `-3` (F3b) answered #101/#102. This closes CGR-008: every candidate
anchor now has a response).

## Scope

`knowledge/semantic_search.py`, ONLY the return paths of
`SemanticSearchIndex.search`; `knowledge/visual_memory_store.py`, ONLY
`search_by_text`; `knowledge/visual_retrieval.py`, ONLY the `search_by_text`
call site in `retrieve_visual_memories` and its returned dict (additive
keys); `knowledge/doc_cooccurrence.py`, ONLY the two leg reads in
`doc_cooccurrence` (status check + raise). New
`tests/unit/test_semantic_visual_failure_outcomes.py`. Adopts F1's leaf
`utils/retrieval_outcome.py` (`OutcomeList`, `outcome_status`,
`RetrievalError`) exactly as F2/F3a/F3b did. Read-only: everything else,
including `utils/retrieval_outcome.py`, `knowledge/literature_oracle.py`,
the synthesis modules, gatherers, `core/agentic/tools.py`, `gui/launch.py`,
`scripts/`, `config/**`, every class-guard file.

## 1. Manifest check (before any edit)

```
$ sha256sum -c --quiet $S/manifest_post_F3b.txt          # exit 0, no output
$ { git diff --name-only; git ls-files --others --exclude-standard; } | \
    sort -u | diff - $S/manifest_paths_post_F3b.txt       # exit 0, no output
```

Both passed. The pre-edit tree matched the parent's post-F3b record exactly.

## 2. Pre-edit git state

- `git rev-parse HEAD`: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout this batch — no commit made).
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED
  (push)`. Not touched. `git stash list`: empty, not touched.
- `git status --short` (pre-edit): identical to the manifest's recorded
  path set — the carried-over modified/untracked paths through F3b, plus
  F3b's own new paths (`knowledge/obsidian_manager.py` modified,
  `tests/unit/test_obsidian_failure_outcomes.py`,
  `docs/execution/generalization/batches/F3b.md`,
  `docs/execution/generalization/class_guard_responses/CGR-20260913-008-3.md`).
  Neither `tests/unit/test_semantic_visual_failure_outcomes.py` (new) nor
  any of the four target source files (modified) present yet.
- `sha256sum` of the four target files, pre-edit:
  - `knowledge/semantic_search.py`: `326fd28990b26adb99eab1f3f8b6719b468c13b383a89f27d95448b371f22019`
  - `knowledge/visual_memory_store.py`: `8337b1dfb65e0ba584734befb9dffe017ae1c4d761080bdbfa1d1f8d66ea3be8`
  - `knowledge/visual_retrieval.py`: `1859b153abce50dc04950ed65c35c034f41b17a6a5c75283227c5a5bdb20b48f`
  - `knowledge/doc_cooccurrence.py`: `75c90e3c5a5e647d832c50c05afb670fa8bb824e0da3189332e24d921f5b4489`
  All four match both the request packet's recorded source SHA-256s for
  anchors #107-#109 and the brief's "parent-verified facts" exactly.

## 3. Parent-verified facts re-verified

All facts in the F4 brief's "PARENT-VERIFIED FACTS" section were read
directly from the deployed source before editing and matched exactly: the
`SemanticSearchIndex.search` shape (empty-query `return []`; not-loaded
`return []`; the FAISS-search `try/except` → `return []` at #107; the
no-hits `return []`; the assembly `try/except` → `return []` at #108;
`semantic_search_with_neighbors` as a thin `get_index().search(...)`
pass-through); `VisualMemoryStore.search_by_text` (`_chroma is None` →
`return []`; the `try/except` around `query_collection` → `return []` at
#109); `VisualRetriever.retrieve_visual_memories` (the `search_by_text`
call site, the merge/entity-filter/image-load pipeline untouched, the
returned `{"text_results", "images"}` dict); `doc_cooccurrence`'s two
no-try leg reads (`ra = semantic_search_with_neighbors(a, k=depth)` /
`rb = ...(b, ...)`) and `DocCooccurResult`'s exact shape
(`shared, shared_titles, mention, known`); `is_known`'s pass-through; the
literature-oracle consumer at `knowledge/literature_oracle.py:740-746`
(`from knowledge.doc_cooccurrence import doc_cooccurrence` inside the
`try`, `record.aux_doc_cooccurrence = {...}`, `except Exception as ex:
log.warning(...)` non-fatal); F1's exact implemented API read directly
from `utils/retrieval_outcome.py` — no drift.

One fact determined during implementation, not fully spelled out in the
brief: `VisualRetriever._merge_results` only adds a result into its `seen`
dict when `image_path` is truthy (`if path and ...`), for BOTH the CLIP and
text legs. This means the additive `text_search_status`/`text_search_reason`
keys (captured immediately after the `search_by_text` call, before any
merge) can legitimately read `"succeeded"` even when the final
`text_results`/`images` lists end up empty because every hit lacked an
`image_path` — this is pre-existing merge behaviour, unrelated to and
unaffected by this batch's edit, and not a new anchor (no `except` involved,
just a truthy-key dedup key). Recorded here since the new test's healthy
control needed a non-empty `image_path` to observe a non-empty
`text_results` for its own assertion.

A second, brief-requested determination: whether `_search_clip`/
`search_by_clip` can swallow failures (contract point 3's "record ... as a
sibling"). Read `knowledge/visual_memory_store.py`'s `search_by_clip`
(216-255) and `knowledge/visual_retrieval.py`'s `_search_clip` (142-147) in
full: **neither has a try/except**. Unlike `search_by_text`, a raise from
`self._clip.encode_text(...)` or `self._store.search_by_clip(...)`
propagates straight out of `retrieve_visual_memories` uncaught (there is no
top-level try around the `clip_results`/`text_results` calls — only a
narrow `try/except: pass` around the `max_images` clamp at the very top).
So the CLIP leg is not a dm18 instance at all (no swallow-to-`[]`), but it
is also NOT protected the way the text leg now is; a CLIP failure today
surfaces as an unhandled exception to `retrieve_visual_memories`'s own
caller rather than a status field. Proven directly by
`TestRetrieveVisualMemories::test_clip_failure_is_not_swallowed`
(deployed-function evidence, not asserted-only) and recorded in the
response file as a sibling, unchanged.

No drift found; no escalation needed.

## 4. Order followed

manifest → pre-edit scan → write test file → failing-first run (digests in
the same command) → edit the four source files → focused run (1 chunk) →
sweep (2 more chunks) → ruff → post-edit scan → data listing → response
file → this packet.

## 5. Pre-edit scan (read-only, `scripts/check_bug_classes.py scan --root .`)

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    619    36     39    0    3
dm18_except_returns_empty     gate    123    71     79    0    8
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` findings for the four target files (via `--json`):
`knowledge/semantic_search.py` `[SemanticSearchIndex.search]` at line 347
(#107) and line 378 (#108); `knowledge/visual_memory_store.py`
`[VisualMemoryStore.search_by_text]` at line 281 (#109) — exactly the three
candidate anchors, at exactly the lines the request packet and brief named.
`knowledge/visual_retrieval.py` and `knowledge/doc_cooccurrence.py` have no
dm18 findings (neither is a `return []`-on-except site — the request
packet does not list them; they are contract-point-3/4 no-try sites, not
dm18 candidates). The 8 pre-existing STALE rows are F2's
`_tavily_search`/`_tavily_extract`, F3a's four `ReferenceDocsManager` rows,
and F3b's two `ObsidianManager` rows — unrelated to this batch.

## 6. Failing-first proof

Guard checked first (`/proc/comm` pytest guard: clear); MemAvailable
5412 MB ≥ 4000. One command, before any source edit:

```
$ sha256sum knowledge/semantic_search.py knowledge/visual_memory_store.py \
    knowledge/visual_retrieval.py knowledge/doc_cooccurrence.py && \
  PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_semantic_visual_failure_outcomes.py
```

All four digests matched (see §2). Result: **20 failed, 6 passed**, exit 1.
Elapsed 0:02.03; Maximum resident set size 775,892 KB.

The 6 passing tests on the unedited source are exactly the
"unaffected-by-design" controls, confirmed by name:
`TestSemanticSearchWithNeighbors::test_passes_succeeded_status_through`
(a plain non-empty list already reads as `outcome_status(...) ==
("succeeded", "")`, unchanged by the wrap);
`TestRetrieveVisualMemories::test_clip_failure_is_not_swallowed` (the CLIP
leg's lack of a try/except is pre-existing, untouched by this batch);
`TestDocCooccurrence::test_both_legs_no_results_control` and
`::test_shared_titles_known_true_control` (both legs healthy — no raise
path is reached either before or after the edit); and both
`TestLiteratureOracleConsumer` tests (they patch
`knowledge.doc_cooccurrence.doc_cooccurrence` directly, so they exercise
only the untouched, read-only `literature_oracle.py`, independent of this
batch's source edits). All 20 other tests fail on the unedited source
exactly as expected: every `.status`/`.reason` assertion
(`AttributeError: 'list' object has no attribute 'status'/'reason'` — a
bare `[]` has neither), the two `text_search_status`/`text_search_reason`
key assertions (`KeyError`), and all four `doc_cooccurrence` raise
assertions plus the `is_known` and privacy-on-raise tests
(`Failed: DID NOT RAISE`).

## 7. Source edit, then focused run

Guard re-checked (clear) before each pytest; MemAvailable checked before
each chunk (all ≥ 4000, see §8).

- `knowledge/semantic_search.py` (18 changed lines: 10 insertions, 8
  deletions, net +2 lines): one new import
  (`utils.retrieval_outcome.OutcomeList`); `search`'s six return
  statements converted — empty query → `OutcomeList()`; not-loaded →
  `OutcomeList.unavailable("index_not_loaded")` (#107's *sibling* early-out,
  not itself a dm18 anchor, but part of contract point 1's full state
  table); FAISS-search exception (#107) → `OutcomeList.failed(type(e).__name__)`,
  existing `logger.error` line kept byte-for-byte; no-hits →
  `OutcomeList()`; assembly exception (#108) →
  `OutcomeList.failed(type(e).__name__)`, existing `logger.error` line kept;
  success → `OutcomeList(rows[:k])`. `semantic_search_with_neighbors` is
  UNCHANGED — it already passes `get_index().search(...)`'s return value
  through verbatim, so it forwards the `OutcomeList` automatically (proven
  by `TestSemanticSearchWithNeighbors`, not just asserted from reading).
- `knowledge/visual_memory_store.py` (7 changed lines: 4 insertions, 3
  deletions): one new import; `search_by_text`'s three return paths
  converted identically — `_chroma is None` →
  `OutcomeList.unavailable("chroma_unavailable")` (#109's early-out);
  `query_collection` exception (#109) →
  `OutcomeList.failed(type(e).__name__)`, existing `logger.warning` line
  kept byte-for-byte; success → `OutcomeList(results)`.
- `knowledge/visual_retrieval.py` (9 changed lines: 8 insertions, 1
  deletion): one new import (`utils.retrieval_outcome.outcome_status`);
  one new line capturing `ts_status, ts_reason = outcome_status(text_results)`
  immediately after the `search_by_text` call (before `_merge_results`,
  which would otherwise silently drop the status via list-subclass
  iteration); the final `return {"text_results": ..., "images": ...}`
  expanded to add two ADDITIVE keys, `"text_search_status"` and
  `"text_search_reason"` — every existing key and value byte-for-byte
  unchanged. No other line touched: the merge, entity hard-filter, image
  loading and `_search_clip`/`_compress_image` are untouched.
- `knowledge/doc_cooccurrence.py` (11 changed lines, all insertions): one
  new import (`utils.retrieval_outcome.RetrievalError, outcome_status`);
  a 9-line block inserted immediately after the two leg reads
  (`ra = ...`, `rb = ...`), before any title/mention computation: reads
  `outcome_status(ra)` and `outcome_status(rb)`; if either status is
  `"failed"` or `"unavailable"`, raises
  `RetrievalError(source="doc_cooccurrence", reason=f"semantic_{worse}")`
  where `worse` is `"failed"` if either leg failed, else `"unavailable"` —
  the reason names only the worse leg's status, never concept text. A
  `"no_results"` leg (a genuine empty read) does not raise and falls
  through to the unchanged title/mention/known computation exactly as
  today. `DocCooccurResult`'s shape is untouched (no new field); `is_known`
  is untouched (it calls `doc_cooccurrence(...).known`, so it inherits the
  raise automatically — proven, not just asserted, by
  `TestDocCooccurrence::test_is_known_inherits_the_raise`).

## 8. Focused run (1 chunk, per the brief)

`tests/unit/test_semantic_visual_failure_outcomes.py` plus the 5
brief-named files (MemAvailable 5390 MB ≥ 4000; one test-fixture fix made
between the first and second attempt — see §14):

```
$ PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_semantic_visual_failure_outcomes.py \
  tests/unit/test_doc_cooccurrence.py \
  tests/unit/test_semantic_search_metric.py \
  tests/unit/test_semantic_load_concurrency.py \
  tests/unit/test_visual_memory_store.py \
  tests/unit/test_visual_memory_pipeline.py
```

Result: **84 passed**, 0 failed, 0 skipped, exit 0. Elapsed 0:02.54;
Maximum resident set size 811,996 KB.

**No existing test required a FIXTURE RULE edit** — verified two ways
before running anything: (1) `test_doc_cooccurrence.py` monkeypatches
`semantic_search_with_neighbors` with a lambda returning a PLAIN list
(`store.get(q, [])`); per `outcome_status`'s contract, a plain list only
ever reads as `"succeeded"`/`"no_results"`, never `"failed"`/
`"unavailable"`, so `doc_cooccurrence`'s new raise-check is never triggered
— all 8 existing assertions (shared-title/mention/bidirectional behaviour)
are byte-for-byte preserved. `test_semantic_search_metric.py` and
`test_semantic_load_concurrency.py` only exercise `.search()`'s
hit-ordering/`.load()`'s concurrency behaviour with real hits or no calls
to `.search()` at all — no failure path, no `.status` introspection, and
`OutcomeList == []`/indexing/`len()` are identical to plain-list behaviour
for every assertion these files make. `test_visual_memory_store.py`'s
`test_search_by_text_no_chroma` asserts `results == []`, which stays TRUE
for `OutcomeList.unavailable(...)` (list-subclass equality compares
elements only, ignoring `.status`); `test_search_by_text_uses_chroma`
indexes/lens the result exactly as before. `test_visual_memory_pipeline.py`
does not call `search_by_text`/`retrieve_visual_memories` at all (grepped
and read — a different fixture entirely). (2) confirmed by the green run
itself — all 5 existing files passed unmodified against the edited source.

## 9. Sweep (2 more chunks)

Every `tests/` importer of the four modules, established by grep before
running anything (`semantic_search_with_neighbors`, `SemanticSearchIndex`,
`get_index`, `visual_memory_store`/`VisualMemoryStore`,
`visual_retrieval`/`VisualRetriever`, `doc_cooccurrence` — direct or via
the gatherer's copied name), reconciled against the brief's
"parent-verified" list: the UNION is exactly the same 15 existing `tests/`
files plus 2 non-unit files the brief already named — no extra file found,
none dropped. (`tests/unit/test_visual_memory_gating.py` also defines a
method literally named `retrieve_visual_memories`, but on a hand-rolled
`_FakeRetriever` test double that imports nothing from
`knowledge/visual_retrieval.py` — confirmed by reading it in full — so it
is not an importer of any of the four modules and is correctly excluded.)

Chunk B (MemAvailable 5335 MB ≥ 4000):

```
$ PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  ... python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_calendar_turn_round3.py \
  tests/unit/test_codex_followups.py \
  tests/unit/test_gatherer_latency_guards.py \
  tests/unit/test_hybrid_semantic_score.py \
  tests/unit/test_public_news_verification.py
```

Result: **83 passed**, 0 failed, exit 0. Elapsed 0:09.88; Maximum resident
set size 1,223,944 KB. All five patch `semantic_search_with_neighbors` (or
an unrelated same-named helper in `utils.web_search_trigger`) at the
consuming module's own attribute, bypassing `SemanticSearchIndex.search`'s
edited internals entirely, or (test_codex_followups.py) only call
`.load()`, never `.search()` — confirmed unaffected by reading before
running, then by the green rerun.

Chunk C (MemAvailable 5231 MB ≥ 4000; includes the one non-unit file this
batch runs — see §10):

```
$ PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  ... python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_sep09_latency_metrics.py \
  tests/unit/test_sep09_speed_images.py \
  tests/unit/test_sep10_web_search_gap.py \
  tests/unit/test_synthesis_generator.py \
  tests/unit/test_warmup_guards.py \
  tests/test_synthesis_calibration.py
```

Result: **93 passed**, 0 failed, exit 0. Elapsed 0:09.95; Maximum resident
set size 1,672,020 KB.

Combined focused+sweep: **260 passed, 0 failed, 0 skipped** across 17 files
(new file: 26 tests; 16 pre-existing/non-unit files).

## 10. Non-unit test decisions (brief-named)

- **`tests/test_synthesis_calibration.py` — RUN.** Read in full before
  running. `@patch("knowledge.semantic_search.semantic_search_with_neighbors",
  side_effect=_mock_faiss_wiki_search)` is a CLASS-level decorator on the
  whole `TestSynthesisCalibration` — the real `SemanticSearchIndex.search`
  is never reached at all, so this batch's edit cannot affect it either
  way. `store = _build_mock_store()` is a bare `MagicMock` (no real
  ChromaDB); `model_manager` is a `MagicMock` with `generate_once =
  AsyncMock(...)` (no real LLM, no network). No file/store in this test
  uses a non-tmp_path default path. Green (part of chunk C above).
- **`tests/test_memory_deep_integration.py` — NOT RUN.** Read in full
  before deciding. It does not import or exercise ANY of the four modules
  this batch owns (`memory.memory_coordinator`/`memory.corpus_manager`
  semantic search over a real `MultiCollectionChromaStore`, unrelated to
  FAISS wiki search or visual memory) — so running it would prove nothing
  about this batch's contract. More importantly, its `memory_coordinator`
  fixture constructs a REAL `MultiCollectionChromaStore(persist_directory=
  str(Path(tmpdir)/"chroma_db"))` — `tmp_path`-scoped, but still a real
  embedded ChromaDB collection, which needs a real embedding function/model
  to add or query documents. This violates the universal "No real Chroma
  store, CLIP model or embedder" constraint (NO REAL INDEX section applies
  to every test run in this batch, not only the new file), independent of
  the tmp_path directory. Recorded as a limitation, not run.

## 11. Ruff

`ruff 0.14.9`.

```
$ PYTHONPATH=.../scripts/bin python -m ruff check \
  knowledge/semantic_search.py knowledge/visual_memory_store.py \
  knowledge/visual_retrieval.py knowledge/doc_cooccurrence.py \
  tests/unit/test_semantic_visual_failure_outcomes.py
All checks passed!
```

## 12. Bug-class scan (read-only, `scripts/check_bug_classes.py scan --root .`)

Post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    620    36     39    0    3
dm18_except_returns_empty     gate    123    68     79    0    11
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped 71→68 (exactly the 3 anchors #107, #108, #109
fixed), `new` stayed 0 (confirmed via `--json`: no new finding anywhere in
the tree), `stale` rose 8→11: the 3 new STALE rows are exactly
`SemanticSearchIndex.search` (×2, lines 347 and 378) and
`VisualMemoryStore.search_by_text` (line 281) — the designed class-guard
handoff. `knowledge/semantic_search.py` and
`knowledge/visual_memory_store.py` have **zero** live dm18 findings after
this edit (confirmed via `--json`); `knowledge/visual_retrieval.py` and
`knowledge/doc_cooccurrence.py` had none before or after (they were never
dm18 candidates — contract points 3/4 are no-try sites, not
`except: return []`).

## 13. Data directory (top-level, sizes and mtimes)

Identical at every checkpoint (before the first pytest, after the
failing-first run, after each focused/sweep chunk, and after the scan) —
no change, nothing written, nothing deleted:

```
total 68
drwxr-xr-x. 1 lukeh lukeh   220 2026-09-14 04:35:38.766908925 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-13 18:07:54.727650398 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
drwxr-xr-x. 1 lukeh lukeh   100 2026-09-14 04:35:38.400904050 -0500 chroma_multi
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
-rw-------. 1 lukeh lukeh    50 2026-09-14 04:35:38.763134691 -0500 web_search_credits.json
```

(`chroma_multi/` and `web_search_credits.json` are the pre-existing F2
side-effect entries, carried over unchanged since F3b's snapshot — every
store this batch's own tests and the calibration non-unit test use is a
`MagicMock`/fake or `tmp_path`-scoped, confirmed to touch neither.)

## 14. BC-58 siblings found

- **`_search_clip`/`search_by_clip` do not swallow failures** — see §3.
  Recorded as a real, un-fixed-by-design sibling in the response file
  (contract point 3's explicit ask), proven by a deployed-function test,
  not this batch's file to change (out of scope: the brief's ownership
  line is only the `search_by_text` call site).
- **`VisualRetriever._merge_results`'s truthy-`image_path` dedup key** —
  see §3; pre-existing, unrelated to and unaffected by this edit.
- **Other `semantic_search_with_neighbors`/`retrieve_visual_memories`
  consumers**, all read-only per ownership, named as siblings per the
  brief: `core/prompt/gatherer_knowledge.py:1859,1504` (F7);
  `core/agentic/tools.py:1958,2131`; `knowledge/synthesis_filter.py:496`
  (a raise there already means "pass by default" — a failed `OutcomeList`
  would now ALSO read as novelty 1.0 → pass, since `not OutcomeList.failed(...)`
  is `True` exactly like `not []`; sibling for F12, not touched);
  `knowledge/synthesis_retriever.py:241`,
  `knowledge/synthesis_generator.py:277` (F12); `gui/launch.py:387`
  (warmup, read-only).
- **Production `doc_cooccurrence` consumer**: `knowledge/literature_oracle.py:740-746`
  (read-only) — proven, not just read, to absorb the new `RetrievalError`
  non-fatally via `TestLiteratureOracleConsumer` (§ Contract below).
  Offline scripts under `scripts/` (read-only, not touched, not driven —
  they are validation/POC tools, out of this batch's evidence scope per
  ownership).

## 15. Privacy / no-network note

No network access, no LLM/paid API call, no daemon restart, no `pip
install`. Every fake exception message, query and concept string used to
prove the privacy contract carries a distinctive marker substring
(`EXCZQX9`), and `TestPrivacyNoLeakedText` (3 tests) plus the two raise-path
assertions embedded in `TestSemanticSearchIndexSearch`/`TestSearchByText`
assert that marker is absent from every `.reason` string produced on
failure; `TestLiteratureOracleConsumer` additionally asserts it is absent
from the oracle's own non-fatal warning log line. The one standing
`python -c "import utils; print(utils.__file__)"` sanity check
`R_common_rules.md` itself instructs was run once at the start of this
batch (prints a path inside this clone) — no other non-pytest code was
run. Every file edit used the Edit or Write tool; `git add` was never
invoked; every pytest invocation (failing-first, the focused chunk, and
both sweep chunks) used the guarded, capped, `-p asyncio` command with a
`/proc/comm` guard check and a MemAvailable check immediately before it.
**No process deviation this batch.**

## 16. Size

`knowledge/semantic_search.py` 18 changed lines +
`knowledge/visual_memory_store.py` 7 + `knowledge/visual_retrieval.py` 9 +
`knowledge/doc_cooccurrence.py` 11 = 45 changed lines across the four
source files (`git diff --stat`: 33 insertions, 12 deletions) +
`tests/unit/test_semantic_visual_failure_outcomes.py` 332 lines (new,
`wc -l`) = **377 total changed lines** — over the 350-line soft target
(this batch touches four files' full contract, a dict-shape addition, a
raise-and-propagate path, two sibling-swallow determinations, and a fully
offline deployed-consumer drive of a fifth, read-only file, so the test
surface is proportionally wider than F3b's two-method single-file batch)
but comfortably under the 450-line hard cap. No split was needed.

## 17. Milestone

Focused contract green: 84/84 passed (new file's 26 tests + 5 existing
files' 58 tests). Sweep green: 83/83 (chunk B) + 93/93 (chunk C,
including the one non-unit file run) = 176/176. Combined: **260/260
passed, 0 failed, 0 skipped** across all 17 files this batch ran, plus the
one non-unit file (`test_memory_deep_integration.py`) correctly not run
(§10). Failing-first: 20/26 failed on the unedited source (all four
digests confirmed), the 6 passes being exactly the unchanged-behaviour
controls. Ruff clean on all five changed/new files. Scan: dm18 `new`
stayed 0 before and after; all 3 anchors (#107-#109) now STALE, 0 live
dm18 findings remain in either of the two files that ever had one. No
existing test required a FIXTURE RULE edit. No `data/` write at any
checkpoint. No process deviation.

## 18. Open items / limitations

- Production consumers of `semantic_search_with_neighbors` and
  `retrieve_visual_memories` (gatherers, `core/agentic/tools.py`,
  `gui/launch.py` warmup) still flatten `OutcomeList`/the dict to a plain
  falsy check — F7 migrates the gatherers; the others are named siblings,
  not this batch's file.
- `knowledge/synthesis_filter.py:496`'s "raise means pass by default"
  interacts with a failed `OutcomeList` reading as `novelty 1.0` — F12
  hand-off, not touched here.
- `_search_clip`/`search_by_clip` still have no failure protection at all
  (not swallowed, not surfaced as a status either) — named as a sibling
  with deployed-function evidence, not this batch's file to change (the
  brief's ownership line is `search_by_text` only).
- `tests/test_memory_deep_integration.py` not run — it tests unrelated
  code and would construct a real (if tmp_path-scoped) ChromaDB collection
  requiring a real embedding model, which the batch's universal "no real
  Chroma/CLIP/embedder" constraint forbids regardless of relevance.
- No escalation: the manifest check, pre/post scans, and every test run
  came back exactly as the brief predicted; no drift from the
  parent-verified facts beyond the two determinations recorded in §3, both
  documentation-only and neither requiring a design change.

## CGR-008 status

Answered: #110/#111 (F2), #103-#106 plus upload refusal (F3a), #101/#102
(F3b), #107-#109 (F4, this batch). **CGR-20260913-008 is now fully
answered** — every candidate anchor in the request packet has a response.

## Parent review and integration (2026-09-14)

Accepted as delivered. The parent made no edit to source, tests or the response file and found no process deviation.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F3b.txt` reports no failures.
- **New paths:** exactly seven (166 paths in total):
  - `knowledge/semantic_search.py` (`fe772c25…`);
  - `knowledge/visual_memory_store.py` (`dcbf5d25…`);
  - `knowledge/visual_retrieval.py` (`e0225732…`);
  - `knowledge/doc_cooccurrence.py` (`0e5f74f4…`);
  - `tests/unit/test_semantic_visual_failure_outcomes.py` (`4dd8c94d…`, 332 lines);
  - `class_guard_responses/CGR-20260913-008-4.md` (`d6be05be…`);
  - this file.
- **Git state:** `git diff --cached --name-only` is empty, HEAD `328a8ec` is unchanged, push is DISABLED, and `git stash list` is empty.
- **`data/`:** identical to the parent's post-F2 snapshot.
- **Recorded tree:** `manifest_post_F4.txt` records the tree after this section.

### Code review (the parent read the full 45-line source diff and the whole test file)

- **`SemanticSearchIndex.search`:**
  - The six return paths are as the packet lists them.
  - `_encode_query` and `_read_rows` stay outside the try blocks. A raise from either propagates, so neither is a swallow site.
- **`VisualMemoryStore.search_by_text`:** returns three states (unavailable, failed, succeeded or no_results).
- **`VisualRetriever.retrieve_visual_memories`:**
  - The status is read immediately after the call, before `_merge_results`.
  - The two new keys are additive.
  - Consumers checked by the parent: `core/prompt/formatter.py:1265`, `core/agentic/controller.py:2977` and `core/agentic/tools.py:2131` read only `text_results` and `images`. `core/prompt/gatherer_knowledge.py:1504` passes the dict to the builder. The new keys never reach the prompt.
- **`doc_cooccurrence`:**
  - The raise comes before any title work.
  - A `no_results` leg is unaffected.
  - `DocCooccurResult` is unchanged.
- **Docstring nit (accepted, carried to F7a):**
  - `search`'s docstring ("no_results if not loaded / resources missing were the whole story…") is inaccurate: a not-loaded index returns `unavailable`.
  - The return annotations still read `List[...]`, as in F3b.
- **Behaviour change to note:** three offline research scripts call `doc_cooccurrence`:
  - `scripts/synthesis_oracle_validation.py`;
  - `scripts/validate_oracle_bidirectional.py`;
  - `scripts/synthesis_poc_anchored_vs_random.py`.

  When the FAISS index is unavailable or a search fails, they now stop with `RetrievalError` instead of scoring every pair as unknown. This is intended (CM-05). The scripts were not run.

### Follow-up found in parent review (pre-existing, not a regression)

**Defect:** `SemanticSearchIndex._read_rows` catches row-group read errors, logs a warning and continues.

**Consequences:**
- **Every read fails:** FAISS returns hits, but `search` returns `OutcomeList()` (`no_results`), and `doc_cooccurrence` scores `known=False`. That is the confident negative this batch removes on the other paths.
- **Some reads fail:** `search` returns fewer rows, still labelled `succeeded`.

**Why F4 did not catch it:** the site is `except → continue`, not `except → return []`, so dm18 does not see it, and it was outside F4's brief.

**Assigned to F7a,** where the gatherer's `semantic` task starts reading this status. A possible shape: count the hits that could not be read and return `failed("row_read_failed")` when there were hits but none could be read. The same batch fixes the docstring nit. Recorded in `failure_outcome_design.md`.

### Tests

- **Failing-first:** 20 failed and 6 passed, with the digests printed in the same command.
  - The 6 passes are unchanged-behaviour controls.
  - The two literature-oracle consumer tests pass either way because they patch `doc_cooccurrence` directly. They prove the untouched consumer absorbs `RetrievalError`; they do not test the edit.
- **Existing tests:** none edited.

### Parent rerun (guarded, capped, `-p asyncio`)

- **Chunk 1** (MemAvailable 5,408 MB):
  - Files: the new file plus test_doc_cooccurrence, test_semantic_search_metric, test_semantic_load_concurrency, test_visual_memory_store, test_visual_memory_pipeline, test_calendar_turn_round3, test_codex_followups and test_gatherer_latency_guards.
  - Result: **131 passed**, exit 0 (0:08.89, 1,099,768 KB).
  - The 3 warnings are SWIG `DeprecationWarning`s at import, which predate this batch.
- **Chunk 2** (MemAvailable 5,392 MB):
  - Files: test_hybrid_semantic_score, test_public_news_verification, test_sep09_latency_metrics, test_sep09_speed_images, test_sep10_web_search_gap, test_synthesis_generator, test_warmup_guards and `tests/test_synthesis_calibration.py`.
  - Result: **129 passed**, exit 0 (0:14.62, 1,768,244 KB), 5 warnings.
- **Total:** **260 passed**, 0 failed, matching the worker.
- **Non-unit calibration test:** rerun only after the parent confirmed that:
  - `semantic_search_with_neighbors` is patched for the whole class;
  - the store is a MagicMock;
  - `SynthesisMemory` and `SynthesisFilter` contain no file-write calls.
- **`tests/test_memory_deep_integration.py`:** not run. The parent agrees with the worker's reason.
- **`data/`:** unchanged after each chunk.
- **ruff 0.14.9:** all 5 files pass.
- **Scan:**
  - dm18: found 68, base 79, new 0, stale 11. The exit 1 comes from the stale rows only.
  - The 3 new stale rows are exactly `knowledge/semantic_search.py` `SemanticSearchIndex.search` ×2 (#107/#108) and `knowledge/visual_memory_store.py` `VisualMemoryStore.search_by_text` (#109). The other 8 are F2's, F3a's and F3b's.
  - dm01 (stale 5) and dm17 (stale 3) are unchanged from earlier batches.
  - Every gate scanner shows 0 new.

### Size

377 changed lines (45 source + 332 test), within the 450 cap.

### Class-guard status

- **CGR-008:** fully answered: #110/#111 (F2), #103-#106 plus upload refusal (F3a), #101/#102 (F3b), #107-#109 (F4).
- **Still open:** CGR-007, -009 and -010.
