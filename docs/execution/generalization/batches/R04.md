# Batch R04 — response to class-guard request CGR-20260913-002 (BC-01, BC-02)

Status: **focused contract green** (deterministic unit-test evidence only;
not a synthetic assembled probe, owner canary, or population claim).

## Scope

This worker owns batch R04 only: the response to class-guard request
`CGR-20260913-002` (scanner `dm01_raw_substring`, BC-01, BC-02) — seven
`raw_membership` anchors in `core/agentic/gate.py`'s
`evaluate_agentic_gate`, all matching a keyword vocabulary with raw `in`
against `_lower` instead of through `utils.trigger_match`. Owned files:
`core/agentic/gate.py` (the seven anchored sites plus module-level compiled
matchers beside the existing ones), new
`tests/unit/test_gate_keyword_boundaries.py`, new
`docs/execution/generalization/class_guard_responses/CGR-20260913-002.md`,
and this file. `tests/unit/test_agentic_gate.py` was inspected but not
edited — no existing assertion there encodes any of the seven anchors'
containment/negation defects (confirmed by grep for the specific
counterexample words and by tracing every test that reaches an anchor's
code path; see "Contract implemented" below).

Seven anchors: review #3 (line 724 pristine / 749 patched, CONTINUATION_PHRASES,
tone-deferral affirmation), #4 (770 / 795, CONTINUATION_PHRASES, insight-offer
affirmation), #5 (973 / 998-999, email-intent verbs), #6 (1080 / 1106,
SEARCH_SIGNAL_WORDS, casual-skip filter), #7 (1107 / 1133, CONTINUATION_PHRASES,
continuation-override check), #8 (1185 / 1211, CONTINUATION_PHRASES,
file-retrieval affirmation), #9 (1514 / 1540, EXPLICIT_SEARCH_KEYWORDS,
intent-veto exemption).

## Base and manifest

- Base HEAD (unchanged throughout): `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
- `git status --short` at start matched the parent's manifest exactly (32
  modified + 9 untracked files/dirs from A01/A02/A03a/A03b-1/A04/R01/R02/R03);
  `core/agentic/gate.py` and `tests/unit/test_gate_keyword_boundaries.py`
  absent from status (pristine / not yet created).
- `git remote -v`: `origin` fetch = `/home/lukeh/Daemon_v1`, push =
  `DISABLED` (verified read-only; never touched, no remote operation
  attempted).
- No `git stash`/`reset`/`clean`/`checkout`/`restore` was run at any point.
  No `git show` output was ever written over a tracked file — the pristine
  `core/agentic/gate.py` text used for the failing-before/scan-comparison
  work was written only to files under this session's scratchpad directory.
  No commit or push was made.
- Environment sanity check:
  `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c
  "import sys, utils.trigger_match as t; print(sys.version.split()[0],
  t.__file__)"` → `3.11.8 /home/lukeh/daemon_exec/generalization/utils/trigger_match.py`.
- All 65 manifest files (32 modified + 9 untracked groups' files + the 4
  gate/test files listed in the parent's ready record) were verified via
  `sha256sum -c` against the parent's ready-record digests and matched
  exactly, `0` mismatches, before any edit.
- `ls -A data` before the first pytest run and after the last: identical —
  `benchmark_per_case.csv chroma_db_v4 embedding_migration_manifest.json
  pipeline`. No new entry; nothing deleted.

### Assigned-file digest (pre-edit, matched the request's recorded source SHA-256 exactly)

```
86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378  core/agentic/gate.py
57d7c44e2607062f018903e440706155928b25ec9078f2832a20f70d5996338c  tests/unit/test_agentic_gate.py  (READ-ONLY: run, never edited)
4ee4d0439ba8a07ba40a08be61f11611769d90ddfbe192688322867d7ce64def  tests/unit/test_gate_system_helpers.py  (READ-ONLY: run, never edited)
f7882ca01a775d6f32ddceaa065f4b6a19e2e23e5316ef14ad09556dd3853ced  tests/unit/test_gate_self_report_memory_backstop.py  (READ-ONLY: run, never edited)
```

### Changed-file manifest (after)

| File | Status | SHA-256 (after) |
|---|---|---|
| `core/agentic/gate.py` | modified (seven anchor sites + module-level constants/matchers only) | `15ae94e647522e7b24aee4556b47894c3aafd4ef64adf21e83d1957310bdcbd9` |
| `tests/unit/test_gate_keyword_boundaries.py` | new | `f473827256938e035c4a5125b679b8976207ff4dfb29f1ff931b89239598cf97` |
| `docs/execution/generalization/class_guard_responses/CGR-20260913-002.md` | new | see that file |
| `docs/execution/generalization/batches/R04.md` | new | see this file |

`git diff --stat` for `core/agentic/gate.py`: **33 insertions, 7
deletions — 40 changed lines**; the new test file is 347 lines. **387
changed lines total**, within the 450-line hard cap (no split needed). No
file outside this list was touched; `git status --short` after all edits
shows the identical A01-R03 set plus exactly these two new entries
(`M core/agentic/gate.py`, `?? tests/unit/test_gate_keyword_boundaries.py`).

## Requirement / defect ID, invariant, and why the source seam owns the fix

- Defect classes: BC-01 (bare-substring keyword matching) and BC-02
  (negation-blind trigger matching), catalogued in `docs/BUG_CLASSES.md`.
- Invariant (per the request's "Requested remedy," the source owner's
  design): every listed vocabulary is matched through ONE compiled
  chokepoint matcher per vocabulary (`utils.trigger_match.compile_keyword_matcher`),
  at every listed site, with word-boundary semantics; negation-aware
  matching (`has_non_negated_hit`/the file's existing `_hit_non_negated`
  wrapper around `find_hits`) applies only to the two REQUEST-CUE
  vocabularies (#5's email-intent verbs, #9's EXPLICIT_SEARCH_KEYWORDS), not
  to the four CONTINUATION_PHRASES sites or SEARCH_SIGNAL_WORDS — adding
  negation to an affirmation arm would change routing beyond containment,
  which the request explicitly scopes out (recorded instead as a
  characterization; see below).
- Why `core/agentic/gate.py` owns the fix: it is the sole source of the
  seven anchored candidates (BC-01/BC-02's Find method, DM-01) and the sole
  file named in the request's "Source paths (Plan 2 owned)" row; no other
  file computes or duplicates these vocabularies.

## Failing-before evidence

Failing-before proof was captured BEFORE any edit to `core/agentic/gate.py`
(new test file added first, run against the pristine source):

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v \
env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gate_keyword_boundaries.py tests/unit/test_agentic_gate.py \
  tests/unit/test_gate_system_helpers.py tests/unit/test_gate_self_report_memory_backstop.py
```

This was run twice: once while developing the test file (surfacing one
test-fixture confound in `TestAnchor5EmailIntentVerbs::test_rewrite_does_not_satisfy_write`
— its original counterexample text opened with "Can you", which independently
satisfies the UNRELATED email-search-cue arm's `_is_info_seeking` check via
the `"can you"` interrogative opener; fixed by rewording to "Please rewrite
..." with no other test affected), and once more as the authoritative
failing-before reproduction against the exact final test file content, via
a temporary plain-file revert of `core/agentic/gate.py` to its pristine text
(re-hashed to `86c5a65e...` before, restored and re-hashed to `15ae94e6...`
after — no `git stash`/`reset`/`checkout`/`restore`/`show`-redirect
involved, per the R01 precedent of "plain text replacement").

Result (final, authoritative): **22 failed, 142 passed**, exit 1, elapsed
0:06.03, max RSS 1,080,088 KB. The 22 failures, one-to-one with the seven
anchors' intended counterexamples/inflection-recognition tests:

- `TestAnchor3ToneDeferralAffirmation::test_yesterday_does_not_affirm[clean|wrapped]` (2)
- `TestAnchor4InsightOfferAffirmation::test_measure_does_not_affirm[clean|wrapped]` (2)
- `TestAnchor5EmailIntentVerbs::test_rewrite_does_not_satisfy_write[clean|wrapped]` (2)
- `TestAnchor5EmailIntentVerbs::test_sender_does_not_satisfy_send[clean|wrapped]` (2)
- `TestAnchor5NewlyMatchedInflections::test_writing_an_email_now_recognized`,
  `test_composing_an_email_now_recognized` (2)
- `TestAnchor6CasualSkipSearchSignal::test_research_is_not_a_search_signal[clean|wrapped]` (2)
- `TestAnchor6CasualSkipSearchSignal::test_outlook_is_not_a_search_signal[clean|wrapped]` (2)
- `TestAnchor7ContinuationOverride::test_yesterday_does_not_read_as_continuation[clean|wrapped]` (2)
- `TestAnchor8FileRetrievalAffirmation::test_pressure_is_not_an_affirmation[clean|wrapped]` (2)
- `TestAnchor9ExplicitSearchVetoExemption::test_research_does_not_grant_exemption[clean|wrapped]` (2)
- `TestAnchor9ExplicitSearchVetoExemption::test_negated_search_does_not_grant_exemption[clean|wrapped]` (2)

Note: `TestAnchor5EmailIntentVerbs::test_negated_send_does_not_grant_arm`
and the `TestContinuationNegationCharacterization` test both PASSED against
the unpatched source too (the negated-send arm was already reached via a
condition that happens not to fire on that exact text pre-patch in a way
distinguishable from post-patch by that assertion alone — its purpose is
regression coverage going forward, not a failing-before proof; the
characterization test is explicitly not asserting a defect). This is
expected and does not weaken the failing-before evidence for the 22 listed.

## Contract implemented

- Added, beside the existing module-level matchers (`_COMPUTATION_HIT`,
  `_WEB_SEARCH_HIT`, `_TOOL_HIT`, `_MEMORY_HIT`, `_KNOWLEDGE_HIT`,
  `_FILE_ACCESS_KEYWORD_HIT`):
  - `_CONTINUATION_PHRASE_HIT = _compile_keyword_matcher(CONTINUATION_PHRASES)`
  - `_SEARCH_SIGNAL_HIT = _compile_keyword_matcher(SEARCH_SIGNAL_WORDS)`
  - `EMAIL_INTENT_CUE_WORDS = ('email',)` /
    `EMAIL_INTENT_VERB_WORDS = ('send', 'draft', 'write', 'compose', 'fire off')`
    (named constants extracted verbatim from the anchor's previous inline
    tuple — no phrase added or removed) with
    `_EMAIL_INTENT_CUE_HIT`/`_EMAIL_INTENT_VERB_HIT` compiled from them.
  - `_EXPLICIT_SEARCH_HIT = _compile_keyword_matcher(EXPLICIT_SEARCH_KEYWORDS)`
- Anchors #3, #4, #7, #8 (all `any(p in _lower for p in CONTINUATION_PHRASES)`):
  replaced with `_CONTINUATION_PHRASE_HIT(_lower)` — boundary matching only,
  no negation added (per contract).
- Anchor #6 (`any(w in _lower for w in SEARCH_SIGNAL_WORDS)`): replaced with
  `_SEARCH_SIGNAL_HIT(_lower)` — boundary matching only.
- Anchor #5 (`'email' in _lower and any(w in _lower for w in (...))`):
  replaced with `_hit_non_negated(_lower, _EMAIL_INTENT_CUE_HIT) and
  _hit_non_negated(_lower, _EMAIL_INTENT_VERB_HIT)` — negation-aware on both
  the cue and the verb, using the file's existing `_hit_non_negated`
  wrapper (`bool(_find_trigger_hits(lower_text, matcher))`, itself a thin
  wrapper around `utils.trigger_match.find_hits` — semantically identical to
  `has_non_negated_hit(text, matcher)` at the default window; reused rather
  than adding a second, functionally-duplicate import/helper for the same
  concept, matching "follow that pattern" in the parent's ready record).
- Anchor #9 (`any(kw in _lower for kw in EXPLICIT_SEARCH_KEYWORDS)`):
  replaced with `_hit_non_negated(_lower, _EXPLICIT_SEARCH_HIT)` —
  negation-aware.
- No vocabulary entry was added to or removed from CONTINUATION_PHRASES,
  SEARCH_SIGNAL_WORDS, the email-intent verb tuple, or
  EXPLICIT_SEARCH_KEYWORDS (BC-76: no phrase-append).
- `'?' in user_text` and `_has_url` (`'http://' in _lower`/`'https://' in
  _lower`) were left untouched at anchor #6's site, per the request's
  explicit "Keep URL-scheme detection... as is; it is recorded separately
  as reviewed benign."

### Newly-matched inflections (chokepoint's e-drop rule)

Checked every bare word in the four affected vocabularies for the
final-e-drop branch (`utils.trigger_match._altered_stem`), the only branch
that can introduce a match the old raw substring check would have missed
(consonant-doubling and c→k branches always produce a string that still
contains the bare word as a literal prefix, so they can never be "new"):

| Vocabulary | Bare word ending in `e` | New form(s) | Real word? | Disposition |
|---|---|---|---|---|
| CONTINUATION_PHRASES | `sure` | `suring`/`sured`/`sures`/`surings` | No — not real English inflections of the adjective "sure" | Accepted; unrealizable, no real-text impact |
| EMAIL_INTENT_VERB_WORDS | `write` | `writing`/`writings` | **Yes** — present participle / gerund | **Accepted, sense-preserving** — see `TestAnchor5NewlyMatchedInflections::test_writing_an_email_now_recognized` |
| EMAIL_INTENT_VERB_WORDS | `compose` | `composing` | **Yes** — present participle | **Accepted, sense-preserving** — see `TestAnchor5NewlyMatchedInflections::test_composing_an_email_now_recognized` |

No bare word in SEARCH_SIGNAL_WORDS or EXPLICIT_SEARCH_KEYWORDS ends in `e`
(the only trigger for this branch), so neither vocabulary gains any new
match from the chokepoint's inflection rule.

### "please don't do it" characterization (follow-up sibling, not fixed)

Per the request's contract, CONTINUATION_PHRASES is NOT made negation-aware
in this batch (that would change routing beyond containment). Recorded in
`TestContinuationNegationCharacterization::test_please_dont_do_it_still_reads_as_an_affirmation`:
with a deferred request armed, `evaluate_agentic_gate("please don't do it")`
still returns `should_trigger=True` with `"deferred-request affirmation"` in
`reason` — `'do it'` (a CONTINUATION_PHRASES phrase, substring-matched since
it contains a space) matches regardless of the immediately-preceding
`"don't"`. This is an unchanged, pre-existing characteristic (true both
before and after this batch's patch); it is filed here as a candidate for a
future class-guard request, not claimed as fixed.

### Tone-marker siblings (report only, not anchors, not changed)

`_tone_is_elevated`/`_tone_is_acute` (~lines 596-641 pristine) use
`marker in _t` uppercase substring checks over `_ELEVATED_TONE_MARKERS`/
`_ACUTE_TONE_MARKERS`. Confirmed by the parent's ready record as NOT anchors
of this request (a report-only sibling from batch R02); left unchanged.

### Line 961 sibling (report only, not an anchor, not changed)

`if _has_email_addr and any(w in _lower for w in ('email', 'send',
'message', 'write', 'mail', 'contact')): needs_tools = True` (the "Email
address + action verb" arm, gated on an actual `\S+@\S+\.\S+` regex match)
is the same raw-membership AST shape as anchor #5 but is not one of the
seven candidate anchors in the request packet, and the class-guard owner's
scan comparison in this batch (see below) confirms this checkout's scanner
cannot see it either way (whole-module exemption). Per the plan's routine
failure contingency ("Generalization scan exposes a baseline finding in a
file the batch edits... Do not fix it outside a request... the class-guard
owner decides whether to issue a request"), this line is left unchanged and
is named here for the class-guard owner's awareness, not fixed.

## Commands and results

Guard before each pytest run:
`for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm)" in
python*) echo RUNNING;; esac; done` — no concurrent run detected before any
invocation in this batch.

1. Failing-before (pristine `gate.py`): see above — 22 failed, 142 passed,
   exit 1, 0:06.07, 1,080,720 KB peak RSS.
2. After the patch, focused suite (same four files): **164 passed**, exit
   0, elapsed 0:05.79, max RSS 1,080,404 KB.
3. Broad sweep — every `tests/unit/*.py` importing `evaluate_agentic_gate`
   (29 files via `grep -rl evaluate_agentic_gate tests/unit/*.py`), after
   the patch: **1120 passed, 8 failed**, exit 1, elapsed 0:47.42, max RSS
   1,769,680 KB. The 8 failures are all
   `test_sep09_speed_batch.py::test_real_verifier_outcomes_reach_receipt_and_debug[...]`,
   asserting `row["grounding_status"] == "complete"` against
   `core/grounding_check.py`'s real verifier (a file dirtied by an earlier
   integrated-but-uncommitted batch, per the parent's manifest — `git
   status --short` shows `M core/grounding_check.py` already at session
   start). That test **monkeypatches** `core.agentic.gate.evaluate_agentic_gate`
   entirely (`monkeypatch.setattr(gate, "evaluate_agentic_gate",
   AsyncMock(return_value=decision))`), so this batch's edit cannot be the
   cause. Not repaired here (not this batch's file; routine failure
   contingency "Focused test fails ... for a known unrelated reason").
4. `PYTHONPATH=... python -m ruff --version` → `ruff 0.14.9`.
5. `PYTHONPATH=... python -m ruff check core/agentic/gate.py
   tests/unit/test_gate_keyword_boundaries.py` → **All checks passed!**
6. `ls -A data` before the first pytest run and after the last: identical
   (`benchmark_per_case.csv chroma_db_v4 embedding_migration_manifest.json
   pipeline`).

## Counterexamples, BC-58 sibling search, exemptions

Counterexamples implemented (all in clean AND wrapped/indented form, per
DEVELOPMENT_WORKFLOW §3):

- `'yes' ⊂ "yesterday"` at the tone-deferral affirmation (#3) and
  continuation-override check (#7) sites.
- `'sure' ⊂ "measure"` at the insight-offer affirmation site (#4).
- `'sure' ⊂ "pressure"` at the file-retrieval affirmation site (#8).
- `'write' ⊂ "rewrite"` and `'send' ⊂ "sender"` at the email-intent site (#5).
- `'search' ⊂ "research"` at the casual-skip filter (#6) and the
  intent-veto exemption (#9).
- `'look' ⊂ "outlook"` at the casual-skip filter (#6).
- A negated explicit keyword (`"don't send that email"`, `"don't search for
  that..."`) at the two request-cue sites (#5, #9), per BC-02.

Paired positive controls (task's exact phrasing, verbatim): `"yes please"`,
`"go ahead"`, `"send an email to Morgan about the meeting"`,
`"look up the latest developments on this"`,
`"search for the latest developments"`, `"pull up the article"`. All pass.

Every assertion drives the deployed `evaluate_agentic_gate` and checks the
decision's `should_trigger`, `modes`, `reason`, or `veto_exempt` — never a
bare helper — reusing the corpus-mock / offer-slot construction from
`tests/unit/test_agentic_gate.py` (`TestContinuationOverride`,
`TestFileAccessKeywords::test_affirmation_after_file_offer_triggers`) and
`tests/unit/test_deferred_request_clarify.py` (`gate._arm_deferred_request`).

BC-58 Find method (sibling-site search) run on every anchor's vocabulary and
helper touched this round:

```
grep -n "CONTINUATION_PHRASES\|SEARCH_SIGNAL_WORDS\|EXPLICIT_SEARCH_KEYWORDS" core/agentic/gate.py
grep -n "_hit_non_negated\|_CONTINUATION_PHRASE_HIT\|_SEARCH_SIGNAL_HIT\|_EXPLICIT_SEARCH_HIT\|_EMAIL_INTENT_.*_HIT" core/agentic/gate.py
```

Every consumer of these four vocabularies within `evaluate_agentic_gate` is
one of the seven anchors — confirmed by grep (no sibling raw-membership
consumer of the SAME vocabulary constants exists outside the anchor list).
The line-961 email-verb-tuple lookalike and the two tone-marker helpers are
a *different* vocabulary/shape each and are reported, not fixed, per the
"Sibling findings" section above and the request's exact anchor list.

Exemptions: `'?' in user_text` and `_has_url` at anchor #6's site (explicitly
named benign in the request); FILLER_WORDS (already compared as whole
tokens, confirmed not an anchor per the parent's ready record).

## Privacy scan result / no live store, network credential, or provider

No live store, network credential, or provider was used at any point. All
fixtures are synthetic strings and `unittest.mock.MagicMock`/`AsyncMock`
corpus managers (no real `CorpusManager`, no real Chroma access). `ls -A
data` unchanged before/after (see above). No secrets, tokens, or personal
data appear in the new test file, the response file, or this packet.

## Remaining milestone state

**Focused contract green** (deterministic unit-test evidence for the
deployed `evaluate_agentic_gate` function, all seven anchors and both
request-cue negation requirements). Not a synthetic assembled probe, owner
canary, or population claim (M5/M6 milestones, not this batch's scope).

## Open decisions for the parent / class-guard owner

1. This checkout's installed `dm01_raw_substring` scanner is the older
   ("contract v1") version and cannot see any of anchors #3-#9 (whole-module
   chokepoint-import exemption, verified directly against the scanner's
   source and empirically against the pristine and patched files in
   isolation — see the response file). The class-guard owner's own review
   process (presumably a newer scanner version or the manual
   `baseline_review_20260913.json` process that produced this request) is
   the one that must re-confirm this fix and mark the anchors
   `confirmed_fixed`.
2. Line 961's raw-membership email arm is a same-shape sibling, not
   requested; left unfixed pending a class-guard decision on whether to
   issue a follow-up request.
3. The `"please don't do it"` characterization and the tone-marker report
   are candidates for a future request; neither is claimed fixed here.

## Limitations

- Deterministic unit-test evidence only; no synthetic assembled probe,
  owner canary, or population claim.
- CONTINUATION_PHRASES and SEARCH_SIGNAL_WORDS are boundary-matched but
  deliberately not negation-aware in this batch (contract-scoped).
- This checkout's scanner cannot corroborate the fix (see "Open decisions"
  #1); its exit code after the patch is `1` for reasons unrelated to this
  batch (6 pre-existing STALE rows from R01/R03).
- 8 pre-existing, unrelated failures in `test_sep09_speed_batch.py` were
  observed in the broad sweep and are not this batch's responsibility (see
  "Commands and results" #3).
- No live Daemon process was started or exercised; `data/` unchanged
  throughout.

## Parent review and integration (parent session)

### Review

- Diff verified. `_CONTINUATION_PHRASE_HIT` serves #3/#4/#7/#8 and `_SEARCH_SIGNAL_HIT` serves #6, both boundary-only. #5 and #9 are negation-aware through the file's existing `_hit_non_negated` → `find_hits` wrapper. The inline email tuple was extracted verbatim into named constants. No vocabulary changes. The `?` and URL checks at #6 are untouched.
- Tests verified. They drive the deployed `evaluate_agentic_gate` with clean and wrapped forms, a paired control for every counterexample, and a newly-matched-inflection class (`writing`/`composing`: sense-preserving). The `"please don't do it"` characterization is recorded as a follow-up sibling.
- Failing-before: 22 failed / 142 passed, matching the counterexample list one-to-one.
- The scan finding is accepted as reported. This checkout's base `dm01` scanner exempts any module that imports `utils.trigger_match`, so it cannot see anchors #3–#9 either before or after. The class-guard owner's newer contract confirms closure on that side.

### Parent rerun on the integrated candidate

Run with `PYTHONPATH` set to this clone's `scripts/bin`, under `MemoryMax=6G`, with no other pytest process running.

- Focused battery (4 files): **164 passed, 0 failed**, exit 0; wall 0:05.49; peak RSS 1,080,472 KB.
- `ls -A data` before and after: unchanged.

### Cross-batch regression surfaced by this batch's sweep

The 8 `test_sep09_speed_batch.py::test_real_verifier_outcomes_reach_receipt_and_debug` failures are **not** pre-existing at base. The parent reproduced them on the integrated tree: `Rejected verdict JSON: why_false missing` → `grounding_status == "failed"` instead of `"complete"`. They were introduced by **A04's** strict verifier contract, whose focused battery did not include this file. The worker was right that R04 did not cause them; the "known unrelated reason" label was wrong. The regression is handled by the parent as an A04 correction (see `A04.md`) and by a cross-batch sweep of every unit-test file importing a module changed in this candidate.

### Final R04 manifest (sha256)

```
15ae94e647522e7b24aee4556b47894c3aafd4ef64adf21e83d1957310bdcbd9  core/agentic/gate.py
f473827256938e035c4a5125b679b8976207ff4dfb29f1ff931b89239598cf97  tests/unit/test_gate_keyword_boundaries.py
71f00c051fdb74618f9dabd5d2db124cbadfacf3b5356f334c40423f7b355596  docs/execution/generalization/class_guard_responses/CGR-20260913-002.md
```

### Integration state

- Integrated into the uncommitted candidate tree on base `328a8ec` (no commit, push, or restart).
- Milestone: **focused contract green** for CGR-20260913-002 anchors #3–#9.
- Follow-up siblings, report only: line 961's email-address arm, the tone-marker helpers, and continuation negation.
