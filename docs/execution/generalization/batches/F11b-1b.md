# F11b-1b: proposal batch loops count a failed write and keep going; check-proposals reports a proposal read failure cleanly

(Worker evidence packet, created incrementally per R_common_rules INTERRUPTION. Base commit 328a8ec, post-F11b-1 tree.)

Brief: `docs/execution/generalization/briefs/F11b-1b.md`. Completes F11b-1's deferred Part B (the shutdown `_generate_proposals` loop, `ingest_survivors`, and the owner-approved `main.py check-proposals` fix), drafted and reverted in F11b-1 for SIZE (see `class_guard_responses/CGR-20260913-010-4.md`, "Batch-loop continuations: drafted, verified, then DEFERRED"). This batch's failing-first proof is independent, per that section's own instruction.

## Manifest check (2026-09-14)

- `sha256sum -c $S/manifest_post_F11b-1.txt`: **253 files checked, 0 mismatches.**
- Path diff: `{git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard} | sort -u` (253 paths) vs `$S/manifest_paths_post_F11b-1.txt` (253 paths, sorted): **empty diff.** (`git status --short` alone collapses the untracked `docs/execution/generalization/` tree into one directory line; the ls-files-based reconstruction expands it to match the recorded manifest paths.)
- HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged).
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED (push)`.
- Starting digests (all match the brief's PARENT-VERIFIED FACTS exactly):
  - `memory/shutdown_processor.py` `74c9ed30ccdc5bc159e57c8f84f0c712b1b13e9a8f4ec6f7cffbdeea6f8ced13`
  - `agent_branch/proposal_bridge.py` `3c7c23a29d550e7e9c55a23863d64484134eaed9bdd36697b160346e751a78e8`
  - `main.py` `6ef42c51327a26d4d070e09d412365507cdea4cc22137127edeafc05270a5eef`
  - read-only `memory/proposal_store.py` `f9262df198a49ce798dbd6cb2ce69dc180eb37851e93a385aba54a7d015cd419`
  - read-only `agent_branch/portfolio.py` `436b0be589b9c809605a6158a5ac42f23262a7eb73ae38758761f36f8beb992e`
- data/ baseline: `ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F11b-1.txt`: **empty diff.** `logs/` absent (as expected).

Manifest check PASSES. Proceeding.

## Pre-edit scan (read-only, before any source edit)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (stale entries present, expected).

Summary line: `dm18_except_returns_empty gate 123 33 79 0 46` — found 33 / base 79 / new 0 / stale 46, matching the brief's recorded post-F11b-1 baseline exactly. Scan stderr empty. (The new test file tests/unit/test_proposal_loop_outcomes.py was created moments before this scan run, per the write-up below; it does not affect scanner output — the scanners inspect memory/shutdown_processor.py, agent_branch/proposal_bridge.py and main.py, none of which this file touches.)

## New test file (before failing-first)

`tests/unit/test_proposal_loop_outcomes.py` created: 233 lines. `wc -l` used (not `git diff --stat`, per GIT INDEX rule for new files).

## Failing-first (2026-09-14)

Guard: no RUNNING pytest process found via the /proc comm check. MemAvailable: 6602 MB (>=4000, OK).

Command (digest line first, in the same command):
```
sha256sum memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py && \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_proposal_loop_outcomes.py
```
Digests printed (unchanged, matching the manifest-check values exactly):
- `memory/shutdown_processor.py` `74c9ed30…`
- `agent_branch/proposal_bridge.py` `3c7c23a2…`
- `main.py` `6ef42c51…`

Result: **4 failed, 1 passed** in 0.35s (elapsed wall 1.90s, Maximum resident set size 764,280 KB). Exit status 1.
- FAILED `TestShutdownGenerateProposalsBatchLoop::test_one_failed_write_is_counted_second_still_stored` — `assert 1 == 2` (store_proposal.call_count): the unpatched loop's first `StoreWriteError` propagates uncaught out of the whole method to the pre-existing outer `except Exception as e: logger.warning(f"[Shutdown] Proposal generation failed: {e}")`, so the second proposal is never attempted. Captured log confirms: `[Shutdown] Proposal generation failed: proposal_store: OperationalError:SYNTH_MARKER_f11b1b_4e2a91` — this is the documented F11b-1 interim-window behavior.
- FAILED `TestShutdownGenerateProposalsBatchLoop::test_similarity_skip_control_not_counted_as_failed` — old message format `"[Shutdown] Generated 1 proposal(s)"` lacks the new `", 0 failed"` suffix (message-format assertion fails pre-edit as expected for a brand-new test file; the underlying dedup-skip/store behavior itself is unaffected by this batch).
- FAILED `TestShutdownGenerateProposalsBatchLoop::test_all_healthy_control_two_kept_zero_failed` — same old-message-format reason (`"Generated 2 proposal(s)"` without the `", 0 failed"` suffix).
- FAILED `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored` — `utils.retrieval_outcome.StoreWriteError: proposal_store: OperationalError:SYNTH_MARKER_f11b1b_4e2a91` propagates uncaught out of `ingest_survivors` (no local try today), confirming the documented interim behavior for this caller too.
- PASSED `TestIngestSurvivorsBatchLoop::test_all_healthy_control_both_ids_returned` — the happy path is unaffected by this batch's change, as expected for a paired control.

Failing-first proof recorded. Proceeding to source edits.

## Focused chunk, attempt 1: a test-fixture defect found and fixed (not a source bug)

Guard: no RUNNING pytest process. MemAvailable: 6609 MB (OK).

First focused run (the 9-file chunk listed above) gave **1 failed, 156 passed**: `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored` failed its own `assert _MARKER not in caplog.text` — the marker leaked into the captured warning (`ingest: store failed for survivor a (OperationalError:SYNTH_MARKER_f11b1b_4e2a91)`).

Root cause: this batch's own fixture, not the delivered source. CONTRACT item 2 requires `ingest_survivors`'s warning to log `e.reason` (`logger.warning("ingest: store failed for survivor %s (%s)", sb.branch_id, e.reason)`) — that is correct, deliberate behavior, matching the CGR-20260913-010-4 draft verbatim. `StoreWriteError.reason`'s own docstring says it "must be a short constant-style label... never free-form user-facing text", and the real call site (`memory/proposal_store.py:108`, read-only) sets `reason=type(e).__name__` -- always a clean exception-class name, never the underlying message. The test's `_RaisingFirstStore` had put the marker directly inside `reason` (`f"OperationalError:{_MARKER}"`), which is not a shape `reason` can take in the deployed code, so the assertion was testing an unrealistic input.

Fix (test file only, `tests/unit/test_proposal_loop_outcomes.py`): added `_raising_write_error()`, which mirrors the real call site exactly -- raises a real `RuntimeError` carrying the marker in its message, catches it, and constructs `StoreWriteError(source="proposal_store", reason=type(underlying).__name__)` with `__cause__` set to the underlying exception, so `reason` is the clean label `"RuntimeError"` and the marker lives only in the (never-logged) underlying message. Both `_RaisingFirstStore.store_proposal` and the shutdown-loop test's `side_effect` now use this shared helper instead of hand-rolling a marker-in-reason exception. This makes the `_MARKER not in caplog.text` assertion a real privacy check on what `e.reason`/the count-only log line expose, not a tautology about what the fixture typed into a field the source never controls the content of. No change to `memory/shutdown_processor.py`, `agent_branch/proposal_bridge.py` or `main.py` was needed or made.

## Focused chunk, attempt 2 (after the fixture fix)

Guard: no RUNNING pytest process. MemAvailable: 6606 MB (OK).

Command (same 9 files as above):
```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_proposal_loop_outcomes.py tests/unit/test_shutdown_pipeline_proposals.py tests/unit/test_proposal_store_outcomes.py tests/unit/test_proposal_store.py tests/unit/test_ordered_slice_guard.py tests/unit/test_skill_and_summary_outcomes.py tests/unit/test_thread_outcomes.py tests/unit/test_retrieval_outcome.py tests/unit/test_store_write_outcomes.py
```
Result: **157 passed, 0 failed** in 5.30s (elapsed wall 8.16s, Maximum resident set size 1,134,516 KB). Exit status 0.

data/ check: `ls -la --time-style=full-iso data` vs `$S/data_baseline_post_cleanup.txt`: only the `..` mtime line differs (this batch's `main.py` edit, per the brief's DATA NOTE — not a data/ change). `logs/`: absent.

## Sweep

Importer search: `grep -rl --include="*.py" -E "shutdown_processor|ShutdownProcessor" tests/unit` (13 files) and `grep -rl --include="*.py" -E "proposal_bridge" tests/unit` (1 file, this batch's own new test). A looser `\bProposalStore\b` grep also matched `tests/unit/test_proposal_store.py` (already in the focused chunk) — no `PendingProposalStore`-only false positives found.

13 shutdown_processor/ShutdownProcessor importers minus the 6 already run in the focused chunk (`test_ordered_slice_guard.py`, `test_proposal_loop_outcomes.py`, `test_proposal_store_outcomes.py`, `test_shutdown_pipeline_proposals.py`, `test_skill_and_summary_outcomes.py`, `test_thread_outcomes.py`) leaves **7 sweep files**: `test_entity_facts.py`, `test_knowledge_graph.py`, `test_retrospective_small_guards.py`, `test_sep09_storage_repairs.py`, `test_source_excerpt_pipeline.py`, `test_stance_write_path.py`, `test_stream_artifacts.py` — one chunk, ≤9. This is the same file set the parent's own F11b-1 rerun used for its sweep (parent packet, `batches/F11b-1.md` "Parent rerun" section), independently re-derived here from the current importer grep.

Vetted before running (grep for `ContextGatherer`, `UserProfile()`, `MagicMock(spec=[])`, `chromadb.`, `EphemeralClient`/`PersistentClient`, `CHROMA_PATH`, `OBSIDIAN_VAULT_PATH`, `ModelManager()`, `MemoryConsolidator`, `DailyNotesGenerator`, and every `data/`-path string):
- No file constructs a real `ContextGatherer`/`UserProfile()`/`ModelManager()`/`MemoryConsolidator`/`DailyNotesGenerator`, and no `OBSIDIAN_VAULT_PATH` use.
- `test_sep09_storage_repairs.py` uses `chromadb.EphemeralClient(Settings(anonymized_telemetry=False))` — the sanctioned DM-07 real-driver-in-memory pattern (BUG_CLASSES.md DM-07), not a persisted store; no `data/` write.
- `test_knowledge_graph.py`, `test_stance_write_path.py`, `test_stream_artifacts.py` use `tmp_path`-rooted paths for every on-disk fixture.
- `test_retrospective_small_guards.py`'s `data/...` string literals are inputs to `utils.python_fs_guard._check_and_maybe_block(op_name, path)` -- a pure decision check (raises `PermissionError` or returns) that never performs the named `os.replace`/`os.remove` operation itself; no real file I/O.
- `test_entity_facts.py` and `test_source_excerpt_pipeline.py` had no matches at all for the hazard greps.

Guard: no RUNNING pytest process. MemAvailable: 6604 MB (OK).

Command:
```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_entity_facts.py tests/unit/test_knowledge_graph.py tests/unit/test_retrospective_small_guards.py tests/unit/test_sep09_storage_repairs.py tests/unit/test_source_excerpt_pipeline.py tests/unit/test_stance_write_path.py tests/unit/test_stream_artifacts.py
```
Result: **254 passed, 0 failed** in 8.17s (elapsed wall 11.39s, Maximum resident set size 3,225,636 KB). Exit status 0. (115 warnings, all pre-existing deprecation noise from chromadb/swig, unrelated to this batch.)

data/ check: only the `..` mtime line differs from `$S/data_baseline_post_cleanup.txt` (same line as after the focused chunk — no new data/ writes from the sweep). `logs/`: absent.

**Combined test total this batch: 157 (focused) + 254 (sweep) = 411 passed, 0 failed.**

## ruff

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff --version` → **ruff 0.14.9**.
`python -m ruff check memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py tests/unit/test_proposal_loop_outcomes.py` → **All checks passed!**

## Final manifest re-check

`sha256sum -c $S/manifest_post_F11b-1.txt`: 4 checksums did not match, 1 listed file could not be read — all expected:
- `main.py`, `memory/shutdown_processor.py`: this batch's own edits (digests recorded above).
- `docs/execution/generalization/briefs/PARENT_STATE.md`, `docs/execution/generalization/LANDING_NOTES.md`: NOT this batch's edits — concurrent parent-maintained files (the same files F11b-1's own packet recorded the parent editing after that batch returned).
- `scan_pre.stderr`: listed in the manifest but absent on disk — per the parent's mid-task note (above), removed by the same owner cleanup that removed the three data/ leftovers; this batch never created or read this file.

`agent_branch/proposal_bridge.py` is NOT in `manifest_post_F11b-1.txt` at all (confirmed by grep) — it was untouched by every batch through F11b-1 (byte-identical to HEAD 328a8ec, per its matching starting digest `3c7c23a2…` recorded at the manifest check), so it was never a tracked "M" path in that snapshot. `sha256sum -c` has nothing to compare it against; its post-edit digest (`6cd8ae73…`) is recorded above from a direct `sha256sum` run instead.

Path diff (`{git diff --name-only; git diff --cached --name-only; git ls-files --others --exclude-standard} | sort -u`, 255 paths) vs `$S/manifest_paths_post_F11b-1.txt` (253 paths): exactly 3 new paths (`agent_branch/proposal_bridge.py`, `docs/execution/generalization/batches/F11b-1b.md`, `tests/unit/test_proposal_loop_outcomes.py` — all this batch's own) and 1 removed path (`scan_pre.stderr`, the owner's cleanup, as above). No unexplained path.

## BC-58 sibling search (guard covers only the first-observed path)

`grep -rn --include="*.py" --exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=tests` for `\.store_proposal(`, `\.get_proposal(`, `\.get_pending_and_approved(` across the repo, to check for other callers this batch's per-item degrade does not cover:

- `.store_proposal(`: `agent_branch/proposal_bridge.py:244` (fixed this batch), `memory/shutdown_processor.py:1262` (fixed this batch), `gui/tabs/proposals.py:208` (GUI, read-only per OWNERSHIP — not touched), `memory/proposal_store.py:294` (`update_status`'s internal re-store, read-only per OWNERSHIP — already caught by that method's own outer `except Exception: ... return False`; explicitly scheduled for **F11b-2** per `class_guard_responses/CGR-20260913-010-4.md`'s "Remaining CGR-010 anchors").
- `.get_proposal(`: `main.py:1566` (fixed this batch), `gui/tabs/proposals.py:275,316,429` (GUI, read-only — not touched).
- `.get_pending_and_approved(`: `main.py:1572` (fixed this batch), `gui/tabs/proposals.py:228` (GUI, read-only — not touched), `memory/shutdown_processor.py:1478` (`_check_implementation_tracking`, a DIFFERENT method from `_generate_proposals` — read only at 1455-1489: it already has its own method-level outer `try` (1464) that catches a `RetrievalError` here and logs "Implementation tracking failed", per F11b-1's own delivered test coverage — already handled by F11b-1 Part A, not this batch's job, and not a batch-loop-over-writes shape so a whole-method abort is the documented, accepted degrade there).

No sibling was silently missed: the two batch-loop callers (`_generate_proposals`, `ingest_survivors`) and the one CLI read site are exactly CONTRACT items 1-3, and every other call site is either already covered (F11b-1's `_check_implementation_tracking`) or explicitly routed to a named follow-up batch (`gui/tabs/proposals.py` -> unassigned GUI batch; `update_status`'s internal re-store -> F11b-2) rather than left unrecorded.

## Post-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (stale entries present, same as pre-edit; expected).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---:|---:|---:|---:|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 638 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | 33 | 79 | 0 | 46 |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |
| catalog | gate | 1 | 0 | 0 | 0 | 0 |

Identical to the pre-edit scan on every gated scanner, including `dm18_except_returns_empty` (33/79/0/46, matching the brief's expectation exactly). **new = 0 everywhere** — this batch's edits (wrapping caller sites in try/except) did not trip any scanner as a new finding; the five `ProposalStore` STALE rows from F11b-1 remain STALE (this batch touched callers, not the anchored `return None`/`return []` sites themselves). Scan stderr empty both times. No SCANNER SHAPE escalation needed.

## Final data/ and logs/ listing

`ls -la --time-style=full-iso data` vs `$S/data_baseline_post_cleanup.txt`: only the `..` mtime line differs (this batch's `main.py` edit at the repository root, per the brief's DATA NOTE — not a data/ change). No new, removed or modified data/ entry across the whole batch. `logs/`: absent throughout (`ls: cannot access 'logs': No such file or directory`).

## main.py `check-proposals`: verified by reading only

Per R_common_rules and the brief, `main.py` opens the real `CHROMA_PATH` store in this mode and is never executed by this batch. The edit is verified by reading the file after the change (offsets 1530-1579 read above). Exact final lines of the wrapped region:

```python
            from utils.retrieval_outcome import RetrievalError
            try:
                if specific_id:
                    proposal = store.get_proposal(specific_id)
                    if not proposal:
                        print(f"Proposal {specific_id} not found.")
                        sys.exit(1)
                    proposals = [proposal]
                else:
                    proposals = store.get_pending_and_approved()
            except RetrievalError as e:
                print(f"Could not read proposals: {e}")
                sys.exit(2)

            if not proposals:
                print("No pending/approved proposals to check.")
                sys.exit(0)
```

`sys.exit(1)` (not found) and `sys.exit(0)` ("No pending/approved proposals to check.") are byte-identical to before the edit and sit outside the `except RetrievalError` clause (a `SystemExit` raised inside the `try` is not a `RetrievalError` and is not intercepted by it), matching CONTRACT item 3 exactly. No automated test covers this CLI path (limitation, recorded below); the underlying `RetrievalError` raises from `ProposalStore.get_proposal`/`get_pending_and_approved` are already covered by F11b-1's `tests/unit/test_proposal_store_outcomes.py`.

## Privacy / no-network note

No real institution names or real IPs anywhere in this batch's edits or new test file. The synthetic marker `SYNTH_MARKER_f11b1b_4e2a91` (`_MARKER` in the new test file) is asserted absent from every new-log-line `caplog.text` capture in the raising tests (`test_one_failed_write_is_counted_second_still_stored`, `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored`); it is deliberately placed only inside an underlying (never-logged) exception's message, and the assertions confirm it never reaches `caplog.text` even though `e.reason` (a clean type-name label) is legitimately logged by `ingest_survivors`. No network call, live store, real Chroma client, real embedder or real model was constructed anywhere in this batch's new or modified tests — every fake is a `MagicMock`/`AsyncMock`/local hand-rolled class, and the sweep's one real-driver use (`chromadb.EphemeralClient` in `test_sep09_storage_repairs.py`) is in-memory only, not this batch's own fixture, and pre-existing. No pip install, no LLM/provider call, no daemon restart, no commit/push.

## Milestone

Focused contract green: 411 passed, 0 failed across the focused (157) and sweep (254) chunks; ruff clean; scan shows new = 0 on every gated scanner; data/ and logs/ unaffected beyond the owner's own cleanup and the recorded `main.py` root-mtime line. This is the ceiling this packet claims, per R_common_rules ("the milestone reached ('focused contract green' at most)").

## Limitations

- **No automated test for `main.py check-proposals`'s CLI output/exit code.** Verified by reading only, per R_common_rules (it opens the real `CHROMA_PATH` store and is never executed by any worker). The underlying `RetrievalError` raise paths are already covered by F11b-1's `tests/unit/test_proposal_store_outcomes.py`.
- **`ProposalStore.update_status`'s internal `self.store_proposal(...)` re-store** (`memory/proposal_store.py:294`, read-only) is unaffected by this batch — a raised `StoreWriteError` there is still caught only by that method's own outer `except Exception: ... return False`. Explicitly scheduled for **F11b-2** per `CGR-20260913-010-4.md`'s "Remaining CGR-010 anchors" (the "staged replacement, following the F11a-2 precedent" item), not this batch's OWNERSHIP.
- **`gui/tabs/proposals.py`'s four call sites** (`store_proposal`, `get_proposal` x3, `get_pending_and_approved`) are unaffected — `gui/**` is read-only per this batch's OWNERSHIP; recorded as a BC-58 sibling only, no batch currently assigned.
- **`memory/shutdown_processor.py`'s `_check_implementation_tracking`** (line 1478, read-only outside this batch's owned region) already has its own method-level outer-`try` degrade from F11b-1 Part A (whole-method abort with a logged warning, not a per-item degrade) — unaffected by, and out of scope for, this batch.
- This batch's own SIZE came in at 286 changed lines (12 + 7 + 21 source + 246 test), above the brief's ≤250 target though well under the ≤450 hard stop; no docstring, comment or assertion was trimmed to compress it.

## Response file

`docs/execution/generalization/class_guard_responses/CGR-20260913-010-5.md` written last, per ORDER — the #131 caller completion (the shutdown loop and `ingest_survivors` per-item degrade with deployed-function evidence, closing F11b-1's disclosed interim loop-abort window). `main.py`'s `check-proposals` CLI wrap has no anchor and is recorded in this packet only, per the brief. Test file digest recorded there: `eef2df15c278b809365efe1b5e1e09dc6f61b6fc087a18230accc13e62bf422e` (246 lines).

This packet (`batches/F11b-1b.md`) and the response file are now complete. No breach to disclose.

## Owner data/ cleanup observed (2026-09-14 19:30, mid-batch)

After the failing-first run, `ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F11b-1.txt` showed exactly: the `total` line (68 → 60), the `.`/`..` mtime lines, and three removed entries — `data/chroma_multi`, `data/user_profile.json`, `data/web_search_credits.json`. No new or modified entry. This matches the brief's owner-approved-cleanup exception exactly (F2-era leftovers). Recorded as the owner's cleanup; CONTINUING per the brief's instruction.

The parent (coordinator) independently confirmed this same cleanup mid-task and pointed to a refreshed baseline, `$S/data_baseline_post_cleanup.txt`. Before trusting it, this batch independently verified: (1) the file's content is byte-for-byte identical to this batch's own `ls -la --time-style=full-iso data` capture taken immediately after the failing-first run; (2) no stray `scan_pre.stderr` or other unexpected file exists at the repository root (the coordinator's note about a removed repo-root `scan_pre.stderr` refers to a file this batch never created — its absence is not a concern for this batch's own manifest, which already showed 0 mismatches). From this point on, subsequent data/ checks in this packet are diffed against `$S/data_baseline_post_cleanup.txt`.

## Source edits (2026-09-14)

All three edits applied via the Edit tool (no scripts), each within the OWNERSHIP-restricted region confirmed at the manifest-check reading:

- `memory/shutdown_processor.py` (`_generate_proposals`, ≈1253-1270): added `failed = 0` beside `kept = 0`; wrapped `doc_id = proposal_store.store_proposal(proposal)` in `try/except StoreWriteError: failed += 1; continue` (local import `from utils.retrieval_outcome import StoreWriteError` right before the try, matching the established F10b skill-loop / F11a-1 thread-loop local-import shape in this same file); changed `if kept:` to `if kept or failed:` and the message to `f"[Shutdown] Generated {kept} proposal(s), {failed} failed"`. The similarity-skip `continue` above it is untouched, so a dedup skip still never reaches the try. Any other exception is not caught here and still reaches the method's pre-existing outer `except Exception as e: logger.warning(f"[Shutdown] Proposal generation failed: {e}")`, per CONTRACT item 1.
- `agent_branch/proposal_bridge.py` (`ingest_survivors`, ≈242-248): added a module-level `from utils.retrieval_outcome import StoreWriteError` (this file's own convention is top-level imports, unlike shutdown_processor.py's local-import style); wrapped `doc_id = store.store_proposal(proposal)` in `try/except StoreWriteError as e: logger.warning("ingest: store failed for survivor %s (%s)", sb.branch_id, e.reason); continue`. A failed survivor's `sig` is never added to `seen` and its id never appended to `stored`, matching CONTRACT item 2 exactly.
- `main.py` (`check-proposals`, ≈1563-1575): added a local `from utils.retrieval_outcome import RetrievalError` and wrapped both read sites (`store.get_proposal(specific_id)` / `store.get_pending_and_approved()`, including the not-found `sys.exit(1)`) in `try/except RetrievalError as e: print(f"Could not read proposals: {e}"); sys.exit(2)`. `sys.exit()` raises `SystemExit`, which `except RetrievalError` does not intercept, so the not-found (exit 1) and "No pending/approved proposals" (exit 0) paths are unchanged, per CONTRACT item 3.

Digests after edit:
- `memory/shutdown_processor.py` `9aa08ee56f13dcb753861ba25153a60aa48817df844928f81acd2360292bae52`
- `agent_branch/proposal_bridge.py` `6cd8ae73fa484a5f8b172cd8db0f4e3b770a26f1cf9ad6fdc1f0485f2571f03e`
- `main.py` `18dd3b912585b6a0eb5eaadd4126abca91b248c8552e8c23fda01a34ae0c6598`

### Size (own hunks isolated from other batches' pre-existing changes in the same shared files)

`memory/shutdown_processor.py` and `main.py` were already "M" (modified, uncommitted) by earlier batches (F10b, F11a-1, F11a-2, F10c, F11b-1) before this batch started. `git diff -U0` was used to locate every hunk by line number and isolate only the ones this batch introduced:

- `memory/shutdown_processor.py`: 3 hunks belong to this batch (`@@ -1247,0 +1254 @@` +1; `@@ -1253 +1260,6 @@` -1/+6; `@@ -1257,2 +1269,2 @@` -2/+2) = **12 changed lines**. (Other hunks at old-lines 1135, 1160-1165, 1543-1547, 1606, 1629-1632 are pre-existing F10b/F11a-1/F11a-2 edits, not this batch's.)
- `agent_branch/proposal_bridge.py`: 2 hunks, both this batch's (this file had no prior modification) (`@@ -45,0 +46 @@` +1; `@@ -242 +243,5 @@` -1/+5) = **7 changed lines**.
- `main.py`: 1 hunk belongs to this batch (`@@ -1563,8 +1563,15 @@` -8/+13 = 21). (The other hunk at old-line 469 is a pre-existing F10c edit, not this batch's.) = **21 changed lines**.
- New test file `tests/unit/test_proposal_loop_outcomes.py`: **246 lines** (all new).

**Total this batch: 12 + 7 + 21 + 246 = 286 changed lines.** Above the brief's ≤250 target but well under the ≤450 HARD stop. Nothing was trimmed to fit — the test file's docstrings, comments and the "any other exception keeps today's path" contract note were kept in full; the one addition after the failing-first run (fixing the injected-exception fixture per below) added lines rather than removing any.

## Parent review and integration (2026-09-14)

Accepted as delivered. No COMPLIANCE breach: typed catches only, nothing trimmed, and each pytest invocation had its own guard check.

This batch closes F11b-1's interim window. A failed proposal write no longer aborts the rest of its loop, and `check-proposals` now reports a proposal read failure cleanly (owner decision 2026-09-14).

### Process findings (parent)

- **Test-fixture correction:** accepted. The first fixture put the privacy marker inside `StoreWriteError.reason`, but by contract `reason` is a class-name label. The corrected `_raising_write_error()` builds the error the way the real `ProposalStore` does: the marker sits only in the chained cause, and `reason` is the exception class name. The leak check now tests what callers actually log.
- **Owner cleanup mid-batch:** handled correctly.
  - The worker saw `data/chroma_multi`, `data/web_search_credits.json` and `data/user_profile.json` removed, plus the `total`/`.`/`..` lines changing, and continued as the brief allowed.
  - It checked the parent's new baseline against its own capture before switching to it.
  - `scan_pre.stderr` is gone from the manifest for the same reason.
- **Repository-root mtime:** after the cleanup, only the `..` line changed, from this batch's `main.py` edit at 19:33:17. The parent saved a refreshed baseline, `$S/data_baseline_post_F11b-1b.txt`.

### Manifest

- **Checksum mismatches against `manifest_post_F11b-1.txt`:**
  - `memory/shutdown_processor.py` (`74c9ed30…` → `9aa08ee5…`) and `main.py` (`6ef42c51…` → `18dd3b91…`), this batch's edits;
  - `briefs/PARENT_STATE.md` and `LANDING_NOTES.md`, parent edits recording the owner cleanup;
  - `scan_pre.stderr` could not be read, because the owner deleted it.
- **New paths (four):**
  - `agent_branch/proposal_bridge.py` (`3c7c23a2…` → `6cd8ae73…`, its first modification);
  - `tests/unit/test_proposal_loop_outcomes.py` (`eef2df15…`, 246 lines);
  - `class_guard_responses/CGR-20260913-010-5.md` (`7f6218c4…`);
  - this file.
- **Removed path:** `scan_pre.stderr`.
- **Unchanged:** `memory/proposal_store.py` is still `f9262df1…`.
- **Git state:** `git diff --cached --name-only` is empty, HEAD `328a8ec` is unchanged, the reflog shows only the clone entry, and the stash list is empty. No untracked files remain at the repository root.
- **`logs/`:** absent.
- **Parent edits after the worker returned:** `briefs/F11b-2.md` (with post-F11b-1b facts) and `briefs/PARENT_STATE.md`.
- **Recorded tree:** `manifest_post_F11b-1b.txt` records the tree after this section.

### Code review (the parent read every changed region)

- **`_generate_proposals`:**
  - A `failed = 0` counter sits beside `kept`.
  - A similarity match still `continue`s before any store.
  - The store call is wrapped in `try: doc_id = proposal_store.store_proposal(proposal)` / `except StoreWriteError: failed += 1; continue`.
  - `kept` counts only truthy ids.
  - The log is `if kept or failed: logger.info(f"[Shutdown] Generated {kept} proposal(s), {failed} failed")`.
  - Any other exception still reaches the outer "Proposal generation failed" warning.
- **`ingest_survivors`:**
  - A module-level `StoreWriteError` import was added.
  - The store call is wrapped in `try … except StoreWriteError as e: logger.warning("ingest: store failed for survivor %s (%s)", sb.branch_id, e.reason); continue`.
  - A failed survivor never reaches `seen` or `stored`.
- **`main.py check-proposals`:**
  - A local `RetrievalError` import was added.
  - `try:` wraps both reads, including the not-found `sys.exit(1)`, which is a `SystemExit` and so is not intercepted.
  - `except RetrievalError as e: print(f"Could not read proposals: {e}"); sys.exit(2)`.
  - The "No pending/approved proposals" exit 0 is unchanged.
- **Nit, not blocking:** as in F10b and F11a-1, the local import in `shutdown_processor.py` sits inside the loop body.

### Tests (the parent read the whole new file)

- **`_generate_proposals`:**
  - one `StoreWriteError` then success: 2 attempts, "Generated 1 proposal(s), 1 failed", marker absent;
  - control: a similarity skip means `store_proposal` is called once and the log shows "1 …, 0 failed";
  - control: all healthy gives "2 …, 0 failed".
- **`ingest_survivors`:** built from a local copy of the `_report` fixture plus real `RankedPortfolio`/`ScoredBranch`, with no real store.
  - first write raises: 2 attempts, only the second id returned, marker absent;
  - control: all healthy returns both ids.
- **Failing-first:** 4 failed and 1 passed on the unedited sources.
- **`main.py`:** verified by reading only; that limitation is recorded.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F11b-1b.txt`)

- **Focused** (MemAvailable 6,566 MB): `test_proposal_loop_outcomes`, `test_shutdown_pipeline_proposals`, `test_proposal_store_outcomes`, `test_proposal_store`, `test_ordered_slice_guard`, `test_skill_and_summary_outcomes`, `test_thread_outcomes`, `test_retrieval_outcome` and `test_store_write_outcomes` gave **157 passed** (8.39 s, 1,130,792 KB).
- **Sweep** (6,574 MB): `test_entity_facts`, `test_knowledge_graph`, `test_retrospective_small_guards`, `test_sep09_storage_repairs`, `test_source_excerpt_pipeline`, `test_stance_write_path` and `test_stream_artifacts` gave **254 passed** (11.32 s, 3,226,020 KB).
- **Total:** **411 passed, 0 failed**, matching the worker.
- **Importer set:** the parent's independent grep for `memory.shutdown_processor`, `ShutdownProcessor` and `agent_branch.proposal_bridge` found 13 files: 6 importers in the focused chunk plus the 7 sweep files.
- **`data/`:** unchanged after both chunks, and `logs/` is absent.
- **ruff 0.14.9:** clean on all four files.
- **Scan:** the dm18 row is unchanged (33/79/0/46). The only table change is dm17's file count (637 → 638). Scan stderr is empty.

### Size

286 changed lines (40 source + 246 test), under the cap.

### Limitations carried forward

- **`main.py check-proposals`:** no automated test.
- **`gui/tabs/proposals.py` callers:** each GUI callback's own except already shows a failure message. Recorded as siblings; no batch is assigned.
- **`ProposalStore.update_status`:** its delete-then-store is F11b-2's.

### Class-guard status

- **CGR-010:** #127–#129, #131, #140 and #146 are answered, including the #131 caller completion in `-5`.
  - Open: the F13a/F13b/F13c receipt work (no anchor).
- **CGR-009:**
  - Open: #117–#120 → F12a; #123, #125, #126 → F12c; #141–#143 → F12d.

