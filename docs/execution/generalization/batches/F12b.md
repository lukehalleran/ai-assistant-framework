# F12b: daily-notes status guard sibling + test_daily_notes_auto_update.py hermeticity

Worker packet. Written incrementally per R_common_rules INTERRUPTION. Base commit 328a8ec (uncommitted deliverable tree).

## Manifest check

Mon Sep 14 08:18:58 PM CDT 2026

- `git status --short | wc -l` = 132 tracked-modified/untracked top-level entries (one of them the untracked directory `docs/execution/generalization/`, expanded below); `git rev-parse HEAD` = `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged); `git diff --stat` tail: `79 files changed, 4032 insertions(+), 809 deletions(-)`; `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED (push)`.
- `sha256sum -c $S/manifest_post_F12a.txt --quiet`: exit 0, 0 mismatches, empty stderr.
- Path check: `git status --short` (untracked-directory line expanded via `find docs/execution/generalization -type f`) sorted and diffed against `$S/manifest_paths_post_F12a.txt` (265 paths each): diff exit 0, empty.
- Assigned/read-only digests match the brief exactly:
  - `utils/daily_notes_generator.py` `9964c54649ddc6b23cc048a113a251f7d4b2ada6c17851cf95d7ad41bfa9f906` (1009 lines)
  - `tests/unit/test_daily_notes_auto_update.py` `5eeeae8f70ff7cb231a4f2193668313242a44a85fee449bdb2e1552ecf233b77` (337 lines)
  - `memory/memory_consolidator.py` `60b05ebaab6e4a27995d5c7b5be27294e931ae450c0c5787223b4b78eada9317` (read-only, F12a state)
  - `utils/status_claims.py` `f7801d1e00e1a93513a2646a3fb2f8eef0a4b6bcdc9117d36ab537c1a1c17a18` (read-only)
  - `utils/retrieval_outcome.py` `1abc1586534171b05e3a06fd976d08eba76cb6ecdfef565944f4dcdebf4c95eb` (read-only)
- MemAvailable at manifest time: 6706 MB (>=4000 required).

Manifest check PASSED. No stop condition triggered. Proceeding.

## Pre-edit scan

Mon Sep 14 08:19:48 PM CDT 2026

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (gate scanners with findings), stderr empty.

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    640    36     39    0    3
dm18_except_returns_empty     gate    123    29     79    0    50
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

dm18: found 29 / base 79 / new 0 / stale 50 — exactly matches the F12a post-integration baseline in the brief. `utils/daily_notes_generator.py` has no dm18 anchor row currently (grep empty) — consistent with the design doc calling the sibling at ≈440-452 a duplicate "not in packet", i.e. not scanner-caught pre-edit.

Saved full output at /tmp/scan_pre_f12b.txt (not in repo).

## New test file

`stat -c '%y %n' tests/unit/test_daily_note_status_guard.py` → `2026-09-14 20:24:52.283854601 -0500` (real mtime, captured after the fact — the section header above was corrected to stop using an estimated time; see the note in "deviations" below).

`tests/unit/test_daily_note_status_guard.py` created, 180 lines. Fakes only: `_FakeModelManager` (stub `generate_once`), `_RaisingProfile`/`_HealthyProfile` (fake profiles, never a real `UserProfile()`), `_gen()` helper builds the real `DailyNotesGenerator` via its explicit-kwarg constructor (`vault_path=str(tmp_path/"vault")` so `OBSIDIAN_VAULT_PATH` is never read, `user_profile=` explicit, `tag_generation_enabled=False` so the `tag_generator` lazy-load property is never touched, `_trigger_narrative_refresh` replaced with an `AsyncMock` since it is not under test here). No `MemoryConsolidator` is ever constructed by this file.

- `TestCurrentStatusFacts` (unit-level, the read site): profile-None-via-property-monkeypatch → `profile_unavailable`; raising `get_current_view` → `RetrievalError(reason="ValueError")` with a privacy marker check; two healthy controls (`[]` and a returned fact).
- `TestGenerateForDateStatusGuard` (integration-level): raising profile and profile-None both → `success is False`, `error == "status_guard_unavailable"`, `generate_once` never awaited (`mm.calls == 0`), `_trigger_narrative_refresh` never awaited, no file at the expected note path, one WARNING carrying labels only (`status_facts`/`ValueError`), no marker in the result or the log; one healthy control → note written, `AUTHORITATIVE CURRENT FACTS` block present in the prompt sent to the model, `_trigger_narrative_refresh` awaited once.

## Failing-first (unedited source)

Mon Sep 14 08:25:04 PM CDT 2026

Guard: no other `-m pytest` process running (comm check empty). MemAvailable: 6708 MB.

```
sha256sum utils/daily_notes_generator.py tests/unit/test_daily_notes_auto_update.py
9964c54649ddc6b23cc048a113a251f7d4b2ada6c17851cf95d7ad41bfa9f906  utils/daily_notes_generator.py
5eeeae8f70ff7cb231a4f2193668313242a44a85fee449bdb2e1552ecf233b77  tests/unit/test_daily_notes_auto_update.py
```
(digests unchanged from the manifest check — this run is against the UNEDITED source, in the same command as the digest line.)

`systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_daily_note_status_guard.py`

Result: **4 failed, 3 passed** in 0.33s. Max RSS 762,952 KB. Exit status 1 (expected — /usr/bin/time -v reports the wrapped command's exit code).

Failures (all on the unedited source, exactly as expected):
- `TestCurrentStatusFacts::test_profile_none_raises_profile_unavailable` — `Failed: DID NOT RAISE RetrievalError` (old code returns `[]` silently).
- `TestCurrentStatusFacts::test_get_current_view_raising_raises_retrieval_error` — same, `DID NOT RAISE`.
- `TestGenerateForDateStatusGuard::test_raising_profile_blocks_generation_before_any_write` — `assert True is False` (`result.success` was `True`: the guard-less note was written and the LLM was called, exactly the real defect named in failure_outcome_design.md).
- `TestGenerateForDateStatusGuard::test_profile_unavailable_blocks_generation` — same.

Passed (controls, unaffected by the edit): both `TestCurrentStatusFacts` healthy-profile tests, and `TestGenerateForDateStatusGuard::test_healthy_status_facts_writes_note_with_guard_block`.

data/ check immediately after: `ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F12a.txt` — empty diff, unchanged. `logs/`: still absent (`ls -ld logs` → No such file or directory).

## Source edit: utils/daily_notes_generator.py

Mon Sep 14 08:25:54 PM CDT 2026

Two regions plus one import line, exactly the OWNERSHIP grant:
1. One import line added: `from utils.retrieval_outcome import RetrievalError` (next to the existing `utils.status_claims` import).
2. `_current_status_facts` body + docstring: mirrors `memory/memory_consolidator.py`'s F12a shape exactly (only the log-line prefix differs, `[DailyNotes]` vs `[Consolidator]`) — `profile is None` → `raise RetrievalError(source="status_facts", reason="profile_unavailable")`; a raising `get_current_view()` keeps the existing debug log line, then `raise RetrievalError(source="status_facts", reason=type(e).__name__) from e`; the genuine-empty filter result is unchanged (`[]`).
3. `generate_for_date`'s status-facts read (≈801-811 after the edit, was ≈801-802): wrapped in `try/except RetrievalError as e:` — one `logger.warning` carrying only `e.source`/`e.reason` (labels, per SCANNER SHAPE/privacy), then `result.error = "status_guard_unavailable"` (a new constant label, `skipped_reason` untouched) and `return result` — before `authoritative_facts_block`, the `get_user_display_name` prompt build, the LLM fallback loop, tag generation, `_write_note`, and `_trigger_narrative_refresh`. No exception escapes the method, so main.py's shutdown/CLI/catch-up/date-range callers (none of which wrap this call in a try except the shutdown step) see only a `GenerationResult(success=False, error="status_guard_unavailable")`, same as any other pre-existing failure return from this method.

`sha256sum utils/daily_notes_generator.py` → `ef9fdf270167a90f22004d062317309b1e385d5fe3b8da8a7bc9488a1a187cd9` (was `9964c546…`). `wc -l` → 1020 (was 1009). `git diff --stat`: 1 file changed, 16 insertions(+), 5 deletions(-) (21 changed lines).

## Hermeticity edit: tests/unit/test_daily_notes_auto_update.py

Mon Sep 14 08:26:21 PM CDT 2026

Read the whole file first (337 lines) to confirm scope: `grep -n "DailyNotesGenerator\|_trigger_narrative_refresh\|def generator\|@pytest.fixture"` shows exactly one fixture, `generator` (def ≈41), constructs the only `DailyNotesGenerator` in the file (via the `__new__`/`__init__`-bypass pattern at ≈49-50, pre-existing, not touched), and every test in the file uses that fixture — so a single fixture-level stub covers both success-path tests (`test_auto_update_proceeds_when_threshold_met` ≈280(+6), `test_force_overrides_auto_update_check` ≈326(+6)).

Edit (one line plus a 5-line comment, right after the existing `gen._user_profile = mock_profile` at ≈58): `gen._trigger_narrative_refresh = AsyncMock(return_value=None)`. `AsyncMock` was already imported (line 11, unedited). No assertion in any test was changed (FIXTURE RULE) — this is the hermeticity edit alone, a separate recorded kind of edit from the new-test-file work.

Why this is sufficient: `_trigger_narrative_refresh` is the ONLY place in `generate_for_date`'s success path that can build a `MemoryConsolidator` (`memory/memory_consolidator.py:952`, `MemoryConsolidator(self.model_manager)`, no profile arg) — read ≈939-970 confirmed no other call site. Replacing the bound method outright means that call is never made at all: no `MemoryConsolidator` is ever constructed (so it can never lazily reach a real default-path `UserProfile()`), and `OBSIDIAN_VAULT_PATH` is never read (the constructor's own env-provided `NARRATIVE_CONTEXT_ENABLED` check and the vault-path readers inside `generate_narrative_context` are also never reached). The generator's OWN status-claim guard already used a fake profile (`gen._user_profile = mock_profile`, pre-existing, line ≈58) which covers `_current_status_facts` on the generator itself — unrelated to this hazard but already hermetic. `vault_path`/`output_dir` already use `tmp_path` (pre-existing, ≈31-33, 59-61). No other path in the file reaches a real profile, vault path or default data path (confirmed by reading every test and the shared `_write_note` helper: pure tmp_path file writes).

`sha256sum tests/unit/test_daily_notes_auto_update.py` → `4da30d80db339a0f7c5443c03842a1a0d0c13b69e33e3b48201085b5810fd84a` (was `5eeeae8f…`). `wc -l` → 343 (was 337). `git diff --stat`: 1 file changed, 6 insertions(+) (6 changed lines).

## Focused chunk 1

Mon Sep 14 08:26:47 PM CDT 2026

Guard: no other `-m pytest` process running. MemAvailable: 6708 MB.

Files (5, ≤9): `tests/unit/test_daily_note_status_guard.py`, `tests/unit/test_consolidator_read_outcomes.py`, `tests/unit/test_narrative_status_claims.py`, `tests/unit/test_sep03_followups_gating.py`, `tests/unit/test_retrieval_outcome.py`.

`systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio <files above>`

Result: **94 passed**, 3 warnings (pre-existing SWIG deprecation warnings, unrelated), exit 0, elapsed 5.54s, Max RSS 1,100,700 KB.

data/ check immediately after: empty diff against `$S/data_baseline_post_F12a.txt`. `logs/` absent.

## Focused chunk 2 (first-ever run of test_daily_notes_auto_update.py)

Mon Sep 14 08:27:14 PM CDT 2026

Guard: no other `-m pytest` process running. MemAvailable: 6679 MB.

Pre-run confirmation (per DATA NOTE): read the whole file (see "Hermeticity edit" section above) — both `generate_for_date` success-path tests use the shared `generator` fixture, which now stubs `_trigger_narrative_refresh` (so no real `MemoryConsolidator` is ever built) and already set a fake `_user_profile`; `vault_path`/`output_dir` use `tmp_path`; no test in the file constructs a `DailyNotesGenerator` any other way; no test reads `OBSIDIAN_VAULT_PATH` or a default data path.

File (1, alone, ≤9): `tests/unit/test_daily_notes_auto_update.py`.

`systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_daily_notes_auto_update.py`

Result: **23 passed** (all — this file had never been run before), exit 0, elapsed 1.82s, Max RSS 762,948 KB. No assertion was changed by the hermeticity edit; every pre-existing assertion in the file passed unmodified.

data/ check immediately after: empty diff against `$S/data_baseline_post_F12a.txt` — confirms the hermeticity edit worked (no real `UserProfile()`/`MemoryConsolidator` reached a real path). `logs/` absent.

## Sweep

`grep -rl --exclude-dir=data --exclude-dir=.agent_snapshots -E "utils\.daily_notes_generator|DailyNotesGenerator|GenerationResult" tests/unit` → 7 files: `test_daily_notes_auto_update.py`, `test_daily_note_status_guard.py` (both already run above), `test_narrative_status_claims.py`, `test_sep03_followups_gating.py` (both already run in focused chunk 1), and three new: `test_sep05_evening_turn_audit.py`, `test_temporal_claim_audit.py`, `test_user_identity.py`.

Vetting (before running): none of the three constructs a `DailyNotesGenerator` — each only imports a pure helper (`build_temporal_claim_audit`, `SYSTEM_PROMPT_TEMPLATE`) or a class attribute (`MemoryConsolidator.NARRATIVE_SYNTHESIS_PROMPT`), so `_current_status_facts`/`generate_for_date` (the two changed regions) are never reached. `test_sep05_evening_turn_audit.py`'s one `MemoryConsolidator(mm, user_profile=profile)` construction (≈399) already passes an explicit fake profile (`MagicMock` with `get_current_view` stubbed); its `CorpusManager`/`GraphMemory`/`TimeManager` constructions all use `tmp_path`; grep for `ContextGatherer|UserProfile(|WebSearchRateLimiter|WebSearchCache|CorpusManager(|CORPUS_FILE|ChromaStore|chroma` found only the one tmp_path `CorpusManager(...)` line. None of the three is `test_graph_integration.py`, `test_web_search_manager.py`, `test_prompt_internal_methods.py`, or a top-level `tests/` file.

Mon Sep 14 08:28:02 PM CDT 2026

Guard: no other `-m pytest` process running. MemAvailable: 6700 MB.

File (3, ≤9): `tests/unit/test_sep05_evening_turn_audit.py`, `tests/unit/test_temporal_claim_audit.py`, `tests/unit/test_user_identity.py`.

`systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio <files above>`

Result: **84 passed**, 3 warnings (same pre-existing SWIG deprecations), exit 0, elapsed 5.09s, Max RSS 1,091,548 KB.

data/ check immediately after: empty diff against `$S/data_baseline_post_F12a.txt`. `logs/` absent.

No further sweep chunks needed — the importer grep found exactly the 7 files above and all are accounted for.

## BC-58 sibling search (DM-15: enumerate every read/write of the same primitive)

`grep -rn` (repo-wide, `--exclude-dir=data --exclude-dir=.agent_snapshots`) for each fixed symbol:

- **`_current_status_facts` definitions:** exactly two in the whole repo — `memory/memory_consolidator.py:205` (fixed by F12a) and `utils/daily_notes_generator.py:441` (fixed by this batch). No third sibling exists; the design doc's "duplicate ... sibling, not in packet" is now closed.
- **`_current_status_facts` call sites:** `memory/memory_consolidator.py:716` (F12a's own, unrelated to me) and `utils/daily_notes_generator.py:806` (mine, the one I wrapped). No other call site anywhere (docs/tests hits are text mentions or the new test files' own calls on the method under test).
- **`generate_for_date` call sites (4, all read):**
  - `main.py:633` (shutdown step) — inside `try: ... except Exception`; checks `result.success` → `result.skipped_reason` → `else: logger.warning(f"...{result.error}")`. My `error="status_guard_unavailable"`/`skipped_reason=None` combination routes into that existing `else` branch exactly like the pre-existing "All LLM models failed" error path. No exception, no behavior change needed.
  - `main.py:1077` (CLI `daily-note` mode, no try) — same three-way `if/elif/else` (≈1083-1092: `else: print(f"FAILED: {result.error}"); sys.exit(0)`). No crash.
  - `utils/daily_notes_generator.py:997` (`generate_yesterday_if_missing`, no try) and `:1016` (`generate_date_range` loop, no try) — both simply return/collect the `GenerationResult`; neither inspects `.error`/`.success` itself, so both are unaffected.
- **Callers of `generate_yesterday_if_missing`/`generate_date_range`:** `gui/launch.py:147` (inside `try/finally`, checks `result.success`/`skipped_reason`/`error` — same safe three-way pattern) and `scripts/daily_note_catchup.py:60` (no try, same pattern, confirmed by reading ≈50-68). Neither can observe an exception from this batch's change — `generate_for_date` still never raises, exactly as before.
- **Key finding:** every caller already treats "not success, no skipped_reason" as an ordinary (non-exceptional) failure to report — the SAME shape the method already used for "All LLM models failed" and "note write failed". This batch adds one more label to an existing, already-handled failure channel; it does not introduce a new control-flow shape for any caller.

## Privacy / no-network note

No real institution, personal, or IP-address text appears anywhere in the new test file, the source docstrings, or any new/touched log line. All fixture data is synthetic (`"Course XYZ"`, generic note bodies, `date(2026, 5, 20)`). The one synthetic marker (`_MARKER = "SYNTH_MARKER_f12b_7c3a1d"`) is asserted ABSENT from the `RetrievalError` string, the `result.error` field, and `caplog.text` — confirming `reason` stays a label/class name, never the raw injected exception text. No network call, LLM call, or paid API was made: every `model_manager` in every new/edited test is a stub (`_FakeModelManager`/`MagicMock`/`AsyncMock`); no real `ModelManager()`, `UserProfile()`, `MemoryConsolidator` (without an explicit fake profile), or Obsidian vault path was ever constructed or read.

## ruff

Mon Sep 14 08:29:17 PM CDT 2026

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check utils/daily_notes_generator.py tests/unit/test_daily_notes_auto_update.py tests/unit/test_daily_note_status_guard.py` → **All checks passed!**, exit 0. ruff 0.14.9.

## Post-edit scan

Mon Sep 14 08:29:21 PM CDT 2026

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` — exit 1 (gate scanners with pre-existing findings, unrelated to this batch), stderr empty.

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    641    36     39    0    3
dm18_except_returns_empty     gate    123    29     79    0    50
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

dm18: found 29 / base 79 / new 0 / stale 50 — **unchanged** from the pre-edit scan, exactly as the brief expects ("no dm18 anchor change and new = 0"). No `daily_notes_generator` row appears (grep empty), matching pre-edit. This makes sense structurally: the edit replaced the `except ...: return []` shape dm18 matches with `except ...: raise RetrievalError(...)`, so there was never a matching anchor to gain or lose here — consistent with the pre-edit scan already showing zero `daily_notes_generator` rows. dm17's file count moved 640→641, the same benign scan-traversal artifact noted in the F10c/F12a reviews (not a new/base/stale change). dm01/dm31/catalog unchanged. **No new gate finding was produced by this batch's edits — nothing to escalate under SCANNER SHAPE.**

Saved full output at /tmp/scan_post_f12b.txt (not in repo).

## Final data/logs check

Mon Sep 14 08:29:43 PM CDT 2026

`ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F12a.txt`: **empty diff** — not even the `..` line changed this time (no file under the checkout root was rewritten by this batch's runs). `logs/`: still absent.

## Size

22 changed lines in `utils/daily_notes_generator.py` (16 insertions, 5 deletions — one import line, the `_current_status_facts` raise-shape, the `generate_for_date` try/except) + 6 changed lines in `tests/unit/test_daily_notes_auto_update.py` (the hermeticity stub) + 180 lines in the new `tests/unit/test_daily_note_status_guard.py` = **208 changed lines total**, under the ≤300 target and well under the ≤450 hard stop. No compression or trimming was applied to fit any number — the SIZE estimate was checked before each later edit (test file → source → hermeticity edit) and the whole contract fit comfortably at every step, so no split was needed.

## Deviations (self-disclosed)

- **Estimated timestamp, caught and corrected in-flight.** The "New test file" section originally used a placeholder ("Mon Sep 14 08:23:xx PM CDT 2026") instead of real `date` output — exactly the F12a parent finding #2 the brief said not to repeat. Caught before any further section was written; corrected using `stat -c '%y %n'` (real captured mtime evidence, not a re-estimate) rather than left as an estimate. Every other timestamp in this packet is a `date` command run in the same or an adjacent command as the step it records. No `python -c` or other non-pytest script was ever run (the only `python` invocations in this batch were `-m pytest`, `-m ruff check`, and `scripts/check_bug_classes.py scan`).

## Limitations

- `main.py:633`, `main.py:1077`, `gui/launch.py:147`, `scripts/daily_note_catchup.py:60` (the four real callers of `generate_for_date`/`generate_yesterday_if_missing`/`generate_date_range`): verified by reading only, per OWNERSHIP (all four files are read-only for this batch); not exercised by a test.
- `get_user_display_name()` (called inside `generate_for_date`'s healthy path, unedited, out of ownership): reads `os.path.getmtime`/`open` on a computed default profile path (`utils/bootstrap.py:get_user_profile_path()`) if `USER_PROFILE_PATH` is unset. This is pre-existing behavior on every success path in both the new and the sibling test file, unrelated to this batch's edit; it is read-only (never writes) and degrades safely via `OSError`/broad-except to `"the user"` when the file does not exist, which the data/ baseline confirms it does not in this clone. Not an instance of the profile/vault/data-path hazard the DATA NOTE warns about (that hazard is specifically about a real `UserProfile()`/`MemoryConsolidator` object being constructed, which this batch's stub of `_trigger_narrative_refresh` and explicit `user_profile=` construction prevent everywhere). Recorded here for the parent's visibility rather than silently accepted.
- The stale `user_profile` property docstring on `DailyNotesGenerator` (still describes the old "swallow → None" fallback as unconditional, now slightly imprecise given callers of `_current_status_facts` treat a None profile as a raise) was left unchanged — only `_current_status_facts`'s own docstring was in OWNERSHIP; same limitation F12a recorded for the consolidator's identical property.
- No class-guard request packet or response file exists for this batch (by design, per the brief) — F12a's CGR-20260913-009-6 response already names F12b as the sibling batch; nothing further to file here.

## Milestone

**Focused contract green.** All 7 new tests (of which 4 failed on the unedited source) pass; the full focused set (94 tests across 5 files, chunk 1) plus the first-ever hermetic run of `test_daily_notes_auto_update.py` (23 tests, chunk 2) pass with 0 failures; the sweep (84 tests across 3 files) passes with 0 failures; ruff is clean on both changed files; the post-edit scan shows dm18 `new=0`, unchanged from pre-edit, with no new gate finding; data/ and logs/ are unchanged through every chunk.

## Pre-handoff git state

Mon Sep 14 08:30:15 PM CDT 2026

`git rev-parse HEAD` → `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged). `git status --short | wc -l` → 135 top-level entries; `git diff --stat` tail: `81 files changed, 4054 insertions(+), 814 deletions(-)`. `git diff --cached --name-only` empty (no index writes). `git stash list` empty. Untracked files new to this batch: exactly `docs/execution/generalization/batches/F12b.md` and `tests/unit/test_daily_note_status_guard.py` (both within OWNERSHIP's "New" grant); every other untracked path predates this batch (accumulated deliverable tree from earlier integrated batches).

## Parent review and integration (2026-09-14)

Accepted. The worker self-disclosed one process deviation and corrected it in-flight. It does not affect correctness, data/ or privacy.

### Process findings (parent)

1. **Placeholder timestamp (self-disclosed, corrected).** The "New test file" section first carried an estimated time; the worker replaced it with the file's mtime. The parent checked the remaining packet timestamps against file mtimes, and they follow the real order:
   - new test file last written 20:24:52;
   - failing-first section 20:25:04; the command printed the unedited digests (`9964c546…`, `5eeeae8f…`); 4 failed, 3 passed;
   - `utils/daily_notes_generator.py` edited 20:25:52 (section 20:25:54);
   - `tests/unit/test_daily_notes_auto_update.py` edited 20:26:15 (section 20:26:21);
   - the packet was last written 20:30:24, after its final section (20:30:15).
   - ORDER was respected. No non-pytest `python` was run.
2. **Size arithmetic.** The Size section says 22 generator lines (208 total). `git diff --numstat` gives 16 + 5 = 21, as the packet's own edit section says, so the total is 207. This is immaterial.
3. **Worker note accepted as a limitation.** On the healthy path, `get_user_display_name()` may read the default profile path. The read is read-only and falls back to "the user" when the file is missing. It predates this batch, and no run changed data/.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F12a.txt` shows exactly one mismatch, `briefs/R_common_rules.md`, a parent rules edit made before F12b launched.
- **New paths (four):**
  - `utils/daily_notes_generator.py` (`9964c546…` → `ef9fdf27…`, 1009 → 1020 lines);
  - `tests/unit/test_daily_notes_auto_update.py` (`5eeeae8f…` → `4da30d80…`, its first modification);
  - `tests/unit/test_daily_note_status_guard.py` (`1bb5d295…`, 180 lines);
  - this file.
- **Git state:** HEAD `328a8ec` is unchanged, `git diff --cached --name-only` is empty, and there are no untracked files at the repository root.
- **`data/`:** the listing is identical to `$S/data_baseline_post_F12a.txt`, including the `..` line, and stays the baseline. `logs/` is absent.
- **Parent edits after the worker returned:**
  - `briefs/R_common_rules.md`: `test_daily_notes_auto_update.py` is now runnable, with a stub-present check;
  - `briefs/PARENT_STATE.md`;
  - `briefs/F12c.md` (new).
- **Recorded tree:** `manifest_post_F12b.txt` records the tree after this section.

### Code review (the parent read both diffs in full)

- **Import:** `RetrievalError`.
- **`_current_status_facts`:**
  - a None profile raises `RetrievalError(source="status_facts", reason="profile_unavailable")`;
  - if `get_current_view` raises, the debug line is kept and the method raises with the exception class as reason (`from e`);
  - the filter's `[]` stays a genuine empty.
- **`generate_for_date`:**
  - `except RetrievalError as e:` catches only the typed failure, logs a warning with the labels, sets `result.error = "status_guard_unavailable"` and returns the result;
  - this happens before the LLM call, the note write and the narrative refresh.
  - The four callers (`main.py:633`, `main.py:1077`, `gui/launch.py:147`, `scripts/daily_note_catchup.py:60`) already report "not success, no skipped_reason" as an ordinary failure. The worker verified this by reading; the parent did not exercise it.
- **Fixture:** one line, `gen._trigger_narrative_refresh = AsyncMock(return_value=None)`, plus a 5-line comment. No assertion changed.
  - The parent's hazard grep on the file (`MemoryConsolidator`, `UserProfile`, `OBSIDIAN_VAULT_PATH`, `_trigger_narrative_refresh`) found only comments and the stub.

### Tests (the parent read the whole new file)

- **Construction:** every generator is built with an explicit fake profile, corpus and model manager, a tmp_path vault, tag generation off, and `_trigger_narrative_refresh` stubbed.
- **`_current_status_facts`:**
  - a None profile (property monkeypatch) gives `profile_unavailable`;
  - a raising `get_current_view` gives `ValueError`, with no marker in `str`;
  - controls: no status relations gives `[]`, and a status fact is returned.
- **`generate_for_date`:**
  - a raising profile gives `status_guard_unavailable`, with 0 LLM calls, no refresh, no note file, no marker in the error or the log, and labels in the log;
  - a None profile is blocked the same way;
  - control: a healthy read writes the note, puts the guard block in the prompt and awaits the refresh once.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F12a.txt` after every chunk)

- **Chunk 1** (MemAvailable 6,585 MB): `test_daily_note_status_guard`, `test_consolidator_read_outcomes`, `test_narrative_status_claims`, `test_sep03_followups_gating` and `test_retrieval_outcome` gave **94 passed** (5.64 s, 1,100,664 KB).
- **Chunk 2** (6,566 MB): `test_daily_notes_auto_update` alone gave **23 passed** (1.84 s, 762,584 KB). This is the file's first run in this lane.
- **Sweep** (6,520 MB): `test_sep05_evening_turn_audit`, `test_temporal_claim_audit` and `test_user_identity` gave **84 passed** (5.27 s, 1,091,732 KB).
- **Total:** 201 passed, matching the worker. data/ was unchanged after every chunk, and `logs/` is absent.
- **Importer set:** the parent's grep found 7 files, all in the runs above.
- **ruff:** clean.
- **Scan:** dm18 is unchanged at found 29 / base 79 / new 0 / stale 50 (this batch has no anchor). dm17's file count went from 640 to 641 (the new test file).

### Size

207 changed lines (21 generator + 6 fixture + 180 test), under the 300 target.

### Limitations carried forward

- **The four `generate_for_date` callers:** verified by reading only.
- **Stale docstring:** the `user_profile` property docstring still describes the old fallback (read-only here, as in F12a).
- **`get_user_display_name()`:** reads the default profile path read-only (predates this batch).

### Class-guard status

- **No request anchor** for this batch.
- **CGR-009 still open:** #123, #125 and #126 → F12c (`-7`); #141–#143 → F12d (`-8`).
