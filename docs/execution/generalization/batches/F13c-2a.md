# F13c-2a: web "Memory save failed" status-bar notice

Worker evidence packet. TypeScript only, no Python edits, no pytest. Written incrementally in run order; every dated entry's time comes from `date` run in the same command as the step.

## Step: manifest, git status, HEAD, digests, remotes (pre-edit)

`date` (same command as checks below): Mon Sep 14 11:11:09 PM CDT 2026 (manifest sha256 check) / 11:11:14 PM (web status diff) / 11:11:19 PM (data/logs listing) / 11:11:50 PM (path-set match).

- HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED (push)`.
- `sha256sum -c $S/manifest_post_F13c-1.txt`: 292 files, 0 mismatches (no output other than the header, exit 0).
- Path-set check: `{ git diff --name-only; git ls-files --others --exclude-standard; } | sort` vs `sort $S/manifest_paths_post_F13c-1.txt`: identical, 292 lines each. (Note: `git status --short` alone collapses the wholly-untracked `docs/execution/generalization/` directory to one line; the diff-name-only + ls-files-others form was needed to expand it and match the manifest, which lists individual files.)
- `git status --short web` vs `$S/web_status_post_F13c-1.txt`: identical (6 lines: ` M web/src/api/client.ts`, ` M web/src/api/useChatStream.test.ts`, ` M web/src/api/useChatStream.ts`, ` M web/src/components/debug/DebugPage.tsx`, `?? web/src/api/client.security.test.ts`, `?? web/src/api/launchAuth.ts`).
- `ls -la --time-style=full-iso data` vs `$S/data_baseline_post_F12a.txt`: identical (benchmark_per_case.csv, chroma_db_v4/, embedding_migration_manifest.json, pipeline/).
- `ls -ld --time-style=full-iso logs`: "No such file or directory" (absent, as expected).
- `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c "import utils; print(utils.__file__)"` -> `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (inside this clone, correct).
- `node`/`npm` on PATH: `/usr/bin/node` v22.21.1, `/usr/bin/npm` 10.9.4. `web/node_modules` exists.

All pre-edit checks pass. Proceeding per ORDER: pre-edit scan next.

## Step: pre-edit read-only scan

`date`: Mon Sep 14 11:12:13 PM CDT 2026.

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .` (exit 0):

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm16_config_key_reachability | report | 395 | 89 | - | - | - |
| dm17_apply_without_guard | gate | 647 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | 23 | 79 | 0 | 56 |
| dm29_phrase_append_signature | report | 0 | 0 | - | - | - |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |
| catalog | gate | 1 | 0 | 0 | 0 | 0 |

Matches the brief's expectation exactly: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 647. TypeScript is outside the scanners (no scanner rows reference web/); recorded per the brief anyway.

Saved to `$S/f13c2a_scan_pre.txt`. (Correction, made during the post-edit scan below: the "exit: 0" I recorded for this command was `tee`'s exit status, not the scanner's — the command was piped through `tee` without `pipefail`. The scanner's own exit code was not actually captured here; see the post-edit scan for the true value, which is identical output and — given the scanner's designed non-zero-on-stale-entries behavior — the same true exit code.)

## Step: WEB BASELINE pre-copy and digest verification

`date`: Mon Sep 14 11:12:29 PM CDT 2026.

Copied the five owned/read-for-diff existing files into `$S/f13c2a_pre/` before any edit and verified sha256 + `wc -l` against the brief's PARENT-VERIFIED MAP digests — all five match exactly:
- `web/src/api/useChatStream.ts` `dddb661719cb4a04e872a6919608219fe16cb325eee4c1b708d0797bfb4e4a04` (292 lines) — matches `dddb6617…`.
- `web/src/api/useChatStream.test.ts` `b242d01cbaf01863645af6fed1fc84c626220a2b93fe3779d1ea3045b227a4f8` (266 lines) — matches `b242d01c…`.
- `web/src/components/chat/ProgressIndicator.tsx` `6b9533ccb893d0fcc167e908ceba78d9d97a7425de2620a252220f6f8312a339` (37 lines) — matches `6b9533cc…`.
- `web/src/App.tsx` `d3c447e11ca42aa0be4b9fbb3f8a7edf16a6b07d1f91e76966d61e2bb9982dc8` (271 lines) — matches `d3c447e1…`.
- `web/src/api/types.ts` `c7d95f64d0c637e7d13c08edeac295d2b746d9d4737fbb9b59391ae078f9440c` (187 lines) — matches `c7d95f64…`.

## Step: read owned files and dependencies

`date`: Mon Sep 14 11:12:29 PM CDT 2026 (files read immediately after, same session).

Read useChatStream.ts, ProgressIndicator.tsx, types.ts, App.tsx, useChatStream.test.ts, client.ts, launchAuth.ts, client.security.test.ts (REPRESENTATIVE_ARGS hazard), ActionApprovalCard.test.tsx (component-test/MantineProvider pattern), web/src/test/setup.ts (confirms `cleanup()` runs afterEach; no global fake-timer reset, so every new test that schedules a `setTimeout` must wrap itself in `vi.useFakeTimers()` / `vi.useRealTimers()` — a real, un-advanced timer from an `it()` would otherwise dangle past the test).

Design decision: capture the `complete` event's debug object in a local `let completeDebug` inside `send()`'s closure (no new state field beyond `storageNotice`, per OWNERSHIP). Two named refs: `sendIdRef` (per-send counter) and `storageTimersRef` (`{followUp, clear}` timeout handles), plus a `clearStorageTimers()` helper called at the top of `send()` and `clearAll()`, plus one unmount-cleanup effect. Follow-up fetch reuses `api.getDebugRecords()` (imported from `./client`, read-only — no new client method, per the HAZARD note on `client.security.test.ts`'s `REPRESENTATIVE_ARGS`).

## Step: write the new tests (before any source edit)

`date`: Mon Sep 14 11:18:21 PM CDT 2026 (measured right after writing, same command as the diff/wc below).

Added a new `describe('useChatStream storage-failure notice (F13c-2a)', ...)` block to `web/src/api/useChatStream.test.ts` (no existing test case touched) covering the 9 contract scenarios 1–9 (test 8 split into "8" (a new send) and "8b" (`clearAll`), both sub-cases of the contract's bullet 8, "`clearAll` OR a new send"). Added a new file `web/src/components/chat/ProgressIndicator.test.tsx` (4 cases: notice shown/role=status, no-notice renders nothing, streaming shows the typing label not the notice, a server-shaped string never appears).

Measured (own lines only, per WEB BASELINE — `diff` against the `$S/f13c2a_pre/` copy, new file via `wc -l`):
- `web/src/api/useChatStream.test.ts`: 329 added lines (`diff $S/f13c2a_pre/useChatStream.test.ts web/src/api/useChatStream.test.ts | grep -c '^>'`).
- `web/src/components/chat/ProgressIndicator.test.tsx`: 51 lines (new file, `wc -l`).
- **Tests subtotal: 380 lines.**

## Step: SIZE estimate BEFORE any source edit — STOP, split proposal

`date`: Mon Sep 14 11:21:08 PM CDT 2026.

Per SIZE (R_common_rules, F13c-1 clarification) and the brief's own line "Estimate tests plus source BEFORE the first source edit. If over, stop and return a split": estimating planned source lines against the confirmed 380 test lines, BEFORE touching any source file.

Itemized planned-source estimate (additions unless noted; no source file has been edited — this is a plan, not a measurement):
- `web/src/api/types.ts`: +1 (`storage_failed?: string` on `DebugRecord`).
- `web/src/api/useChatStream.ts` (all pure insertions next to existing lines, no line rewrites): +1 import (`api` from `./client`), +2 constants, +1 state field, +1 initialState field, +1 Action union member, +1 `stream_started` reducer field, +2 new `storage_notice` reducer case, +2 refs (`sendIdRef`, `storageTimersRef`), +7 `clearStorageTimers()` helper, +3 unmount-cleanup effect, +2 top-of-`send` (`clearStorageTimers()` + `mySendId`), +1 `completeDebug` declaration, +1 capture in the `onmessage` `'complete'` case, +~24 the post-`stream_ended` notice/follow-up block in `finally`, +2 top-of-`clearAll` (`clearStorageTimers()` + counter bump) ≈ **51**.
- `web/src/components/chat/ProgressIndicator.tsx`: +1 new prop on `Props`, ~2 changed (destructure line), ~10 changed (rewriting `if (!streaming) return null` into the streaming/notice/null branch) ≈ **13**.
- `web/src/App.tsx`: +1 (`storageNotice={chat.storageNotice}`).
- **Source subtotal estimate: ≈66 lines.**

**Grand total estimate: 380 + 66 ≈ 446 lines** — within 4 lines of the 450 HARD stop, and far over both this brief's ≤300 target and the map's own ≈235–285 whole-contract estimate (the tests alone, at 380 measured lines, already exceed the map's whole-contract range by 95–145 lines). The source estimate above is a pre-edit plan, not a measurement, and this codebase's existing files (useChatStream.ts, launchAuth.ts, App.tsx, client.security.test.ts) carry dense inline commenting as a strong, consistent convention; matching it would plausibly add 10–20 more lines than the bare-minimum plan above, which would push the total over 450.

Per SIZE: "estimate the size BEFORE editing each later part of the contract... If the whole contract will not fit, stop and return the split proposal before editing the rest. Do not implement everything and then revert parts to fit" and "Never delete or shrink tests to fit" (dropping contract-required test coverage to buy margin is not an option). Given the razor-thin, uncertain margin, continuing into the source edits risks landing over the 450 hard cap mid-contract, which under this system's rules cannot be resolved by reverting.

**STOPPING before any source edit.** No source file has been touched (verified: `git status --short web` this run shows only the pre-existing baseline `M` lines plus the new `ProgressIndicator.test.tsx`; `useChatStream.ts`'s ` M` predates this batch, per the WEB BASELINE digest match above).

### Split proposal (matches the brief's own suggested split)

- **Part 1 (hook)**: `web/src/api/useChatStream.ts` (state field, action, reducer cases, `send`/`clearAll` logic, refs, unmount-cleanup effect — as already scoped by OWNERSHIP) + the new `describe` block already written in `web/src/api/useChatStream.test.ts` (9 contract scenarios; test 8/8b can be merged into one test to save ~15–20 lines if a tighter margin is wanted). Estimated size: ≈51 source + 329 tests ≈ **380 lines**, comfortably under 450. `web/src/api/types.ts` is NOT required for this part: `DebugRecord`'s existing `[key: string]: unknown` index signature already lets `completeDebug?.storage_failed` / `last?.storage_failed` type-check (as `unknown`, narrowed at runtime by the `typeof … === 'string'` check already in the plan), so the hook compiles standalone without the new field.
- **Part 2 (UI)**: `web/src/api/types.ts` (`storage_failed?: string` on `DebugRecord`) + `web/src/components/chat/ProgressIndicator.tsx` (new prop + notice branch) + `web/src/App.tsx` (one prop) + the new `web/src/components/chat/ProgressIndicator.test.tsx` (already written, 4 cases). Estimated size: ≈15 source + 51 tests ≈ **66 lines**, well under 450.
- **Dependency**: Part 2's `App.tsx` change (`storageNotice={chat.storageNotice}`) reads a field that only exists on the hook's return type after Part 1 lands — Part 2 must follow Part 1, not run in parallel.

Both new test files are already written in full (no coverage was cut to produce this proposal) and can be handed to whichever batch(es) the parent dispatches next; they do not need to be rewritten.

**Limitation / escalation**: stopping per SIZE before any source edit; returning this split proposal to the parent instead of proceeding to `web/src/api/useChatStream.ts`, `web/src/components/chat/ProgressIndicator.tsx`, `web/src/App.tsx`, `web/src/api/types.ts`. No `npm` test/typecheck run was performed (ORDER places the failing-first vitest run — and therefore any `npm` invocation — after this size gate, and the gate did not clear).

## Step: final status

`date`: Mon Sep 14 11:21:55 PM CDT 2026.

- `git status --short web`: only the pre-existing baseline lines (` M client.ts`, ` M useChatStream.test.ts`, ` M useChatStream.ts`, ` M DebugPage.tsx`, `?? client.security.test.ts`, `?? launchAuth.ts`) plus the one new `?? web/src/components/chat/ProgressIndicator.test.tsx` line — exactly the allowed set (this batch's owned test additions only; no other web/ path touched).
- HEAD unchanged: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
- `git diff --stat -- web`: `useChatStream.test.ts` shows `526 ++++`/`0 --` against HEAD (includes earlier batches' prior edits to that file; this batch's OWN addition, measured against the `$S/f13c2a_pre/` copy, is the 329 lines recorded above). `client.ts` and `DebugPage.tsx` diffs are unchanged from before this batch (untouched by this batch). `useChatStream.ts` diff (`33 ++/--`) is also entirely pre-existing (F13a/F13c-1), confirmed by the digest match above — this batch made no edit to it.
- sha256 of this batch's changed/new files:
  - `web/src/api/useChatStream.test.ts` `573115c78cf839205362153aa9b045b5c39a468ffdd92cdbf89b6927758e00a0` (595 lines total; 329 own lines added).
  - `web/src/components/chat/ProgressIndicator.test.tsx` `f58db30b853b73d55c36e274ee93ceae00f8185ce11198122b19284e31e982f1` (51 lines, new file).
  - `docs/execution/generalization/batches/F13c-2a.md` `467ce511a712cbd7ecbbcf2217b42cd55e25f6cca719396eed9fc22dd3e45d2b` (this packet, mid-write at capture time).
- `data/`: unchanged from `$S/data_baseline_post_F12a.txt` (benchmark_per_case.csv, chroma_db_v4/, embedding_migration_manifest.json, pipeline/ — identical).
- `logs/`: still absent ("No such file or directory").
- No `npm`/`node` command of any kind was run this batch (the SIZE gate stopped before the failing-first vitest run in ORDER).
- No pip/npm install, no build/dev/preview, no daemon restart, no LLM/paid API call, no git add/commit/push/stash/reset/checkout/restore/clean, no `mv`/`rm`. Only `Read`/`Edit`/`Write` tools touched files; `Bash` was used only for read-only inspection (`sha256sum -c`, `git status`/`diff`/`rev-parse`/`remote -v`, `ls`, `diff`, `wc -l`, `date`, the one `import utils` sanity check, and the one read-only `scripts/check_bug_classes.py scan`).

**Milestone reached: tests written and measured; SIZE gate stopped the batch before the failing-first run and before any source edit. Split proposal returned above.**

## Step: parent decision — split NOT adopted, resume whole contract

`date`: Mon Sep 14 11:24:05 PM CDT 2026.

Parent decision, received via message: the split proposed above is NOT adopted. Both new test files are already in the tree; `npm --prefix web run typecheck` type-checks `src/` including both test files, and the full vitest suite runs both — so either "half" batch on its own would fail typecheck and the full suite because of the other half's tests referencing fields/props that don't exist yet. The honest projection (380 test lines + ≈66 source ≈ 446) is under the 450 hard stop, and stopping to check first was the correct call — but the whole contract now resumes in THIS batch, starting at failing-first.

Instructions from the parent for the remainder of this batch:
1. Record this decision (this section) — done.
2. Re-run the guard/MemAvailable check, reconfirm the web/ status baseline and the four owned source digests unchanged, then run failing-first exactly per the brief: the digest line plus the two-file focused vitest command in ONE command (WEB TEST LANE form). Record the failures.
3. Source edits per CONTRACT: comments proportionate to surrounding code; measure the running total after EACH source file (diff vs `$S/f13c2a_pre/` copies + the test file's `wc -l`); if the running total would exceed 450 before the contract is complete, STOP immediately, record it, and return — do NOT revert, do NOT trim/delete tests.
4. Test fixes discovered at run time are allowed only if they do not weaken what the test proves; disclose each with before/after lines.
5. Continue ORDER: focused vitest (both files) → full vitest suite → typecheck → post-edit scan → `git status --short web` vs baseline, data/, logs/ → packet.

Proceeding now.

## Step: remaining source edits and final running-total

`date`: Mon Sep 14 11:28:38 PM CDT 2026.

Applied the two remaining planned edits, exactly as scoped by OWNERSHIP, no other source touched:
- `web/src/components/chat/ProgressIndicator.tsx`: added the optional `storageNotice?: boolean` prop (with a 2-line comment matching the file's existing density), destructured it, and replaced `if (!streaming) return null` with a branch that returns the existing `Group` position (no `Loader`, no elapsed timer) holding a dimmed `Text` "Memory save failed" with `role="status"` when `storageNotice` is true, else still `return null`. Streaming branch unchanged.
- `web/src/App.tsx`: added `storageNotice={chat.storageNotice}` to the `<ProgressIndicator>` element (one line).

Measured (diff vs `$S/f13c2a_pre/` copies, added+removed):
- `web/src/components/chat/ProgressIndicator.tsx`: 22 changed (comment+prop+destructure+branch rewrite).
- `web/src/App.tsx`: 1 changed (added-only, no removal).

**Final running total**: types.ts 3 + useChatStream.ts 81 + ProgressIndicator.tsx 22 + App.tsx 1 + useChatStream.test.ts (own) 329 + ProgressIndicator.test.tsx (new file) 56 = **492 lines** — under the parent's one-time 500 exception. Proceeding to the test runs.

## Step: post-edit test runs

Guard (no pytest process, no node vitest/tsc process) and MemAvailable checked before each run; `git status --short web` diffed against `$S/web_status_post_F13c-1.txt` before/after each — every diff was exactly the four expected lines (` M App.tsx`, ` M types.ts`, ` M ProgressIndicator.tsx`, `?? ProgressIndicator.test.tsx`) beyond the pre-existing baseline, no other web/ path ever changed. `data/` and `logs/` checked after each run: `data/` identical to `$S/data_baseline_post_F12a.txt` throughout; `logs/` absent throughout.

**Focused vitest** (`date` 11:28:52 PM guard, 11:28:55 PM run):
```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M env CI=1 npm --prefix web test -- src/api/useChatStream.test.ts src/components/chat/ProgressIndicator.test.tsx
```
Result: **2 test files passed (2), 25 tests passed (25)**, exit 0. No test fix and no source bug fix were needed at this step — everything passed on the first run after the two remaining source edits.

**Full vitest suite** (`date` 11:29:10 PM guard, 11:29:13 PM run):
```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M env CI=1 npm --prefix web test
```
Result: **4 test files passed (4), 42 tests passed (42)**, exit 0 (includes `client.security.test.ts` and `ActionApprovalCard.test.tsx`, per the brief).

**Typecheck** (`date` 11:29:25 PM guard, 11:29:28 PM run):
```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M env CI=1 npm --prefix web run typecheck
```
Result: `tsc --noEmit` — clean, **no output, exit 0**.

No source bug was found (nothing to fix); no additional test change was needed beyond the one MantineProvider fix already disclosed above.

## Step: post-edit read-only scan

`date`: Mon Sep 14 11:29:49 PM CDT 2026.

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .`:

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm16_config_key_reachability | report | 395 | 89 | - | - | - |
| dm17_apply_without_guard | gate | 647 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | 23 | 79 | 0 | 56 |
| dm29_phrase_append_signature | report | 0 | 0 | - | - | - |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |
| catalog | gate | 1 | 0 | 0 | 0 | 0 |

**Byte-identical to the pre-edit table** — expected: this batch touched only `web/` (TypeScript), which no scanner covers. dm18: found 23 / base 79 / new 0 / stale 56 (matches brief expectation exactly); dm17 files 647 (matches). True exit code (checked directly, not through `tee`): **1** — the designed non-zero-on-stale-entries exit (R_common_rules: "the base scan reports it STALE and exits non-zero. That is the designed handoff... Record it; do not try to make it green"); the 56 stale dm18 rows and 5/3 stale dm01/dm17 rows all pre-date this batch (identical count pre- and post-edit) and are not this batch's to fix.

## Step: final web status, data/, logs/

`date`: Mon Sep 14 11:29:36 PM CDT 2026 (last checked, immediately before the scan above; unchanged after it — the scan is read-only and touches no `web/`/`data/`/`logs/` path).

`git status --short web` vs `$S/web_status_post_F13c-1.txt`: exactly four differences beyond the pre-existing baseline — ` M web/src/App.tsx`, ` M web/src/api/types.ts`, ` M web/src/components/chat/ProgressIndicator.tsx`, `?? web/src/components/chat/ProgressIndicator.test.tsx` — precisely the set the brief names as allowed ("the new ProgressIndicator.test.tsx line, plus ` M` lines for ProgressIndicator.tsx, App.tsx and types.ts"). `useChatStream.ts`/`useChatStream.test.ts` already showed ` M` in the baseline. No other web/ path ever changed across the whole batch.

`data/`: unchanged from `$S/data_baseline_post_F12a.txt` throughout every run (benchmark_per_case.csv, chroma_db_v4/, embedding_migration_manifest.json, pipeline/ — identical every check).

`logs/`: absent throughout ("No such file or directory" every check).

## Step: final size table

| file | own changed lines | measure |
|---|---|---|
| `web/src/api/types.ts` | 3 | diff vs `$S/f13c2a_pre/types.ts` (added+removed) |
| `web/src/api/useChatStream.ts` | 81 | diff vs `$S/f13c2a_pre/useChatStream.ts` (added+removed) |
| `web/src/components/chat/ProgressIndicator.tsx` | 22 | diff vs `$S/f13c2a_pre/ProgressIndicator.tsx` (added+removed) |
| `web/src/App.tsx` | 1 | diff vs `$S/f13c2a_pre/App.tsx` (added+removed) |
| `web/src/api/useChatStream.test.ts` (own addition) | 329 | diff vs `$S/f13c2a_pre/useChatStream.test.ts`, added lines only (pure append + the one disclosed fix is in the other test file) |
| `web/src/components/chat/ProgressIndicator.test.tsx` (new file) | 56 | `wc -l` |
| **Total** | **492** | under the parent's one-time 500-line exception (over the brief's original 450 hard stop and far over its ≤300 target, both explicitly superseded by parent decision #2 for this batch only) |

## Step: deviations and limitations

- Deviation from the original brief: final size (492) exceeds the brief's stated ≤300 target and its 450 hard stop. Authorized explicitly by parent decision #2 (one-time, documented, this batch only, raising the hard stop to 500) — see that section above.
- One disclosed test-assertion fix (not a coverage change): `ProgressIndicator.test.tsx`'s "renders nothing" case, `toBeEmptyDOMElement()` → explicit `queryByRole('status')`/`queryByText('Memory save failed')` absence checks, because `MantineProvider` injects its own `<style>` elements regardless of the component's own output. Before/after recorded above.
- No source bug was found; no other test case was touched.
- No pip/npm install, no build/dev/preview, no npx, no daemon restart, no LLM/paid API call, no git add/commit/push/stash/reset/checkout/restore/clean, no mv/rm, at any point in this batch.
- No Python source file was touched; the only Python invocations were the two read-only scans and the one `import utils` sanity check.

**Milestone reached: focused contract green.** Focused vitest 25/25 passed; full vitest suite 42/42 passed; `tsc --noEmit` clean; post-edit scan identical to pre-edit (no new findings); web/data/logs match the allowed set throughout.

## Step: guard/baseline reconfirm and failing-first

`date`: Mon Sep 14 11:24:18 PM CDT 2026 (guard), 11:24:24 PM (baseline reconfirm), 11:24:28 PM (failing-first command).

- pytest guard: no python process running `-m pytest` (empty result).
- node guard: no `node` process with `vitest`/`tsc` in its cmdline (empty result).
- MemAvailable: 6759 MB (≥4000 required).
- `git status --short web` vs `$S/web_status_post_F13c-1.txt`: only the one expected new line (`?? web/src/components/chat/ProgressIndicator.test.tsx`); no other divergence.
- Four owned source digests reconfirmed unchanged (still match the pre-copy digests, i.e. still unedited):
  - `useChatStream.ts` `dddb6617…`
  - `ProgressIndicator.tsx` `6b9533cc…`
  - `App.tsx` `d3c447e1…`
  - `types.ts` `c7d95f64…`

**Failing-first** (digest line + focused vitest, ONE command, WEB TEST LANE form):
```
sha256sum web/src/api/useChatStream.ts web/src/components/chat/ProgressIndicator.tsx web/src/App.tsx web/src/api/types.ts && systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M env CI=1 npm --prefix web test -- src/api/useChatStream.test.ts src/components/chat/ProgressIndicator.test.tsx
```
Digests printed (same as above, unedited source). Result: **2 test files failed, 12 failed / 13 passed (25 total)**, exit code 1.
- `useChatStream.test.ts`: 10/21 failed — all 10 new (1–9, 8b) fail with `expected undefined to be true/false` on `result.current.storageNotice` (the field does not exist yet on `StreamState`). The pre-existing 11 tests in that file all still pass.
- `ProgressIndicator.test.tsx`: 2/4 failed — "shows the fixed text with role=status" fails (`Unable to find an accessible element with the role "status"` — the prop/branch doesn't exist yet); "renders nothing (paired control)" fails on `toBeEmptyDOMElement()` because `MantineProvider` injects two `<style data-mantine-styles>` elements into `document.head`/body context even when the component itself renders `null` — an environment artifact unrelated to the contract, exactly as the parent's note anticipated. The other 2 (streaming-shows-typing-label, never-renders-server-string) pass already since they don't depend on the new prop's absence.

This confirms the tests genuinely exercise not-yet-implemented behavior (proof of a valid failing-first), plus the one environment-shaped assertion the parent flagged.

## Step: run-time test fix (disclosed, does not weaken the assertion)

`date`: Mon Sep 14 11:24:28 PM CDT 2026 (same session as the failing-first read above; applied immediately after).

`web/src/components/chat/ProgressIndicator.test.tsx`, test "not streaming without storageNotice: renders nothing (paired control)" — `toBeEmptyDOMElement()` fails not because the component renders anything, but because `MantineProvider` injects its own `<style data-mantine-styles="true">`/`<style data-mantine-styles="classes">` elements into the render container regardless of what the component under test does — an environment artifact, not a contract violation (the failing-first output above shows the injected content is exactly two `<style>` tags, nothing from `ProgressIndicator` itself). Fixed by asserting the component's own observable output instead — still a real, un-weakened check that nothing renders for the notice:

Before:
```
  it('not streaming without storageNotice: renders nothing (paired control)', () => {
    const { container } = renderIndicator({ storageNotice: false })
    expect(container).toBeEmptyDOMElement()
  })
```
After:
```
  it('not streaming without storageNotice: renders nothing (paired control)', () => {
    // Not `toBeEmptyDOMElement()`: MantineProvider injects its own
    // <style data-mantine-styles> elements regardless of what the component
    // renders, so the container is never truly empty. Assert the component's
    // own output instead: no status role and no notice text.
    renderIndicator({ storageNotice: false })
    expect(screen.queryByRole('status')).not.toBeInTheDocument()
    expect(screen.queryByText('Memory save failed')).not.toBeInTheDocument()
  })
```
File is now 56 lines (was 51; +5 net). No other test case touched.

## Step: source edits — types.ts, then useChatStream.ts — running-total measurements

`date`: Mon Sep 14 11:25:15 PM CDT 2026 (after `types.ts`), Mon Sep 14 11:26:18 PM CDT 2026 (after `useChatStream.ts`).

Edited `web/src/api/types.ts` first (smallest, no dependency): added a `storage_failed?: string` field (plus a 2-line comment) to `DebugRecord`, right before its existing `[key: string]: unknown` index signature. Measured (diff vs `$S/f13c2a_pre/types.ts`, added+removed): **3 lines** (0 removed, 3 added). Running total after this file: 3 (types.ts) + 329 (useChatStream.test.ts) + 56 (ProgressIndicator.test.tsx) = **388** — under 450, proceeded.

Edited `web/src/api/useChatStream.ts` next (the hook: state field, action, reducer cases, refs, `clearStorageTimers` helper, one unmount-cleanup effect, and the `send`/`clearAll` logic — exactly the OWNERSHIP scope), following the design recorded earlier (per-send counter `sendIdRef`, `storageTimersRef` for the follow-up/clear timeout handles, `completeDebug` captured locally in `send`'s closure from the `complete` SSE event, the post-`stream_ended` notice/follow-up block in `finally`, `api.getDebugRecords()` reused for the follow-up per the HAZARD note). Comments added are proportionate to the file's existing density (one line or a short block per new construct, matching the surrounding style — e.g. the existing `pendingActionId` "Approval chaining" comment, the R2/R3 comments in `send`).

Measured (diff vs `$S/f13c2a_pre/useChatStream.ts`): **2 removed, 79 added = 81 changed lines** (added+removed); 79 using the added-only convention used for the test files. File is now 369 lines (was 292).

**Running total after `useChatStream.ts`:**
- Added+removed convention: 3 (types.ts) + 81 (useChatStream.ts) + 329 (useChatStream.test.ts) + 56 (ProgressIndicator.test.tsx) = **469**.
- Added-only convention (matching how the test files were measured, since they were pure appends): 3 + 79 + 329 + 56 = **467**.

Both readings are **over the 450 hard cap**, with `web/src/components/chat/ProgressIndicator.tsx` and `web/src/App.tsx` not yet touched (the contract is not complete).

**STOPPING IMMEDIATELY per the parent's instruction 3**: "If the running total would exceed 450 before the contract is complete, STOP immediately, record it, and return. Do NOT revert anything and do NOT trim or delete tests; the parent will decide."

No revert performed. `web/src/api/types.ts` and `web/src/api/useChatStream.ts` are left edited as measured above. `web/src/components/chat/ProgressIndicator.tsx` and `web/src/App.tsx` are untouched (still match their pre-copy digests). No `npm` run was performed after this point (focused/full vitest and typecheck were not reached — the running-total gate stopped the batch mid-CONTRACT, between source edits, per instruction 3's ORDER).

Awaiting parent decision on how to proceed (e.g. adjust the cap, accept the overage, or split the remaining two files into a follow-up batch now that the hook half is already in the tree and measured).

## Step: final status at this stop

`date`: Mon Sep 14 11:26:59 PM CDT 2026.

- `git status --short web` vs `$S/web_status_post_F13c-1.txt`: exactly two new differences beyond the allowed baseline — ` M web/src/api/types.ts` and `?? web/src/components/chat/ProgressIndicator.test.tsx` (the new file). `web/src/api/useChatStream.ts` and `web/src/api/useChatStream.test.ts` already showed ` M` in the baseline (per the brief's own note) and remain ` M`. No other web/ path changed.
- sha256 of every file this batch has touched:
  - `web/src/api/types.ts` `c55cf031b898bac53f7028a9dc7fc68f08df1a8fb73c5c7a5efa48c1549bba23` (190 lines; was 187).
  - `web/src/api/useChatStream.ts` `bb23f2d850dd0136baa6342b84a8c66f62aa258a52c3c41f2677a23b544778ff` (369 lines; was 292).
  - `web/src/api/useChatStream.test.ts` `573115c78cf839205362153aa9b045b5c39a468ffdd92cdbf89b6927758e00a0` (595 lines; was 266).
  - `web/src/components/chat/ProgressIndicator.test.tsx` `c12924d77691213452779227f823285a0135b46d87acef4849f3e8591d3e9cf3` (56 lines; new).
  - `web/src/components/chat/ProgressIndicator.tsx` `6b9533ccb893d0fcc167e908ceba78d9d97a7425de2620a252220f6f8312a339` — **unchanged**, matches the pre-copy digest exactly (not yet edited).
  - `web/src/App.tsx` `d3c447e11ca42aa0be4b9fbb3f8a7edf16a6b07d1f91e76966d61e2bb9982dc8` — **unchanged**, matches the pre-copy digest exactly (not yet edited).
- `data/`: unchanged from `$S/data_baseline_post_F12a.txt`.
- `logs/`: still absent.
- No `npm`/`node` run since the failing-first command (focused vitest with these new edits, full vitest suite, and typecheck were NOT run — the running-total gate stopped the batch first, per instruction 3's explicit "STOP immediately... before the contract is complete").
- No revert, no git add/commit/push/stash/reset/checkout/restore/clean, no mv/rm, no install/build/dev/preview/npx, no daemon restart, no LLM/paid API call.

**Batch paused here awaiting parent decision** (per instruction 3: do not revert, do not trim/delete tests, the parent decides). `web/src/api/types.ts` and `web/src/api/useChatStream.ts` are left in their edited state; `web/src/components/chat/ProgressIndicator.tsx` and `web/src/App.tsx` are untouched.

## Step: parent decision #2 — one-time size exception, hard stop 500

`date`: Mon Sep 14 11:28:13 PM CDT 2026.

Parent decision, received via message: a one-time, documented SIZE EXCEPTION for THIS batch only — the hard stop is raised from 450 to 500. Reasoning given: splitting is not viable (`ProgressIndicator.test.tsx` is already in the tree and uses `storageNotice`, so typecheck and the full vitest suite cannot pass until `ProgressIndicator.tsx` has the prop; removing that test would be a deletion/trimming, both blocked); the overage is almost all tests (385 lines: 329 + 56), source is ≈97 (81 useChatStream.ts + 3 types.ts, both already applied); the earlier stop was correct.

Instructions: (1) record this decision — done, this section; (2) make ONLY the two remaining planned edits (`ProgressIndicator.tsx` prop+branch, `App.tsx` one prop), no other source changes, comments proportionate, re-measure the running total after them — stop (no revert, no trimming) if it would exceed 500; (3) focused vitest → full vitest suite → typecheck, each with guard/MemAvailable/web-status-diff/data-logs around it, fixing a real source bug inside ownership if found (disclosed), test changes only if they don't weaken the assertion (disclosed with before/after); (4) post-edit scan, final web/data/logs listing, packet's final size table + deviations + limitations.

Proceeding now.

## Parent review and integration (2026-09-14)

Accepted under a one-time, documented SIZE exception: the hard stop was raised to 500 for this batch only, and the final size is 492. There is no rule breach, and no data/, privacy or `web/dist` issue.

### Parent decisions during the batch (both recorded above by the worker)

1. **23:23 — split not adopted.**
   - The worker stopped before any source edit, with tests at 380 lines and a projected total of ≈446.
   - Its proposed hook/UI split could not work: both test files were already in the tree, `tsc --noEmit` type-checks tests, and the full vitest suite runs both. So either half alone would fail.
   - The worker resumed the whole contract.
2. **23:28 — one-time exception to 500.** The worker stopped again at a running total of 469, with `ProgressIndicator.tsx` and `App.tsx` still untouched.
   - Splitting at that point was still not viable, because the component test references the new prop.
   - The overage is almost all tests: 385 lines of tests against ≈107 lines of source.
   - The worker made only the two remaining edits.

### Process findings (parent)

1. **Correct stops.** Both stops were correct applications of SIZE: one before the first source edit, one mid-contract with no revert and no trimming.
2. **Packet out of run order.** The sections written after decision #2 (remaining edits, test runs, scan, final status, size table, deviations) were inserted BEFORE the earlier "guard/baseline reconfirm and failing-first" and "types.ts/useChatStream.ts" sections. Every `date` is real and consistent with the mtimes:
   - tests last written 23:17:55 (hook) and 23:24:51 (component, the disclosed Mantine fix);
   - failing-first 23:24:28, printing unedited digests (12 failed, 13 passed);
   - `types.ts` 23:25:10, `useChatStream.ts` 23:26:11, `ProgressIndicator.tsx` 23:28:27, `App.tsx` 23:28:32;
   - runs 23:28:55–23:29:28.
3. **Disclosed test fix.** `toBeEmptyDOMElement()` became no `role=status` and no notice text, because MantineProvider injects `<style>` elements. This does not weaken the test. The parent anticipated it.
4. **Scan exit-code correction.** The worker corrected an earlier mis-recorded scan exit code (it had recorded `tee`'s).

### Manifest and web baseline

- **Checksums:** `sha256sum -c manifest_post_F13c-1.txt` shows exactly two mismatches:
  - `web/src/api/useChatStream.ts` (`dddb6617…` → `bb23f2d8…`, 292 → 369 lines);
  - `web/src/api/useChatStream.test.ts` (`b242d01c…` → `573115c7…`, 266 → 595).
- **New paths (five):**
  - `web/src/api/types.ts` (`c7d95f64…` → `c55cf031…`, 187 → 190);
  - `web/src/App.tsx` (`d3c447e1…` → `ed362188…`, 271 → 272);
  - `web/src/components/chat/ProgressIndicator.tsx` (`6b9533cc…` → `5bec561a…`, 37 → 55);
  - `web/src/components/chat/ProgressIndicator.test.tsx` (new, `c12924d7…`, 56 lines);
  - this file.
- **`git status --short web` vs `$S/web_status_post_F13c-1.txt`:** exactly the four allowed additions (` M App.tsx`, ` M types.ts`, ` M ProgressIndicator.tsx`, `?? ProgressIndicator.test.tsx`).
- **`web/dist`:** mtime 2026-09-14 14:32:05, not touched.
- **Git state:** HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`. `logs/` is absent.
- **Pre-edit copies:** `$S/f13c2a_pre/` digests match the brief (`d3c447e1`, `6b9533cc`, `c7d95f64`, `b242d01c`, `dddb6617`).
- **Parent edits after the worker returned:**
  - `briefs/PARENT_STATE.md`;
  - `briefs/F13c-2b.md` (new);
  - `failure_outcome_design.md` (legacy Gradio is logs-only);
  - `LANDING_NOTES.md` (the SPA rebuild note).
- **Recorded tree:** `manifest_post_F13c-2a.txt` and `$S/web_status_post_F13c-2a.txt` record the tree after this section.

### Code review (the parent read the exact own diffs against `$S/f13c2a_pre/`)

- **`types.ts`:** `storage_failed?: string` on `DebugRecord`, with a 2-line comment.
- **`useChatStream.ts`:**
  - `import { api } from './client'`; no new client method.
  - `storageNotice: boolean` in state, false initially.
  - Constants: `STORAGE_NOTICE_CLEAR_MS = 4000`, `STORAGE_NOTICE_FOLLOWUP_MS = 2500`.
  - The `storage_notice` action and reducer case; `stream_started` sets false; `cleared` resets via `initialState`.
  - Refs `sendIdRef` and `storageTimersRef`, a `clearStorageTimers` callback, and an unmount-only cleanup effect (no timers started in effects).
  - In `send`: `clearStorageTimers()` and `mySendId = ++sendIdRef.current`; `completeDebug` captured in the `complete` handler.
  - In `finally`, after `stream_ended`, only when `!ctrl.signal.aborted` and the send is still current:
    - a non-empty string `storage_failed` on `complete.debug` → `showNotice()`, which dispatches true and schedules a 4000 ms clear guarded by the send id;
    - otherwise, if `completeDebug` is truthy → one 2500 ms timer that calls `api.getDebugRecords()`, takes the last record, and shows the notice if the send is still current and the key is a non-empty string; errors are swallowed.
  - `clearAll` clears the timers and bumps the send id.
- **`ProgressIndicator.tsx`:** optional `storageNotice`. When not streaming, it renders `<Text size="xs" c="dimmed" role="status">Memory save failed</Text>` in a right-aligned Group if the notice is set, otherwise null. Streaming is unchanged.
- **`App.tsx`:** `storageNotice={chat.storageNotice}`.
- **Contract:** matches. The text is fixed; the server label is never rendered; there is no toast, no chat change and no Python change.

### Tests (the parent read both new test blocks before and after the fix)

- **Hook tests (10 cases):**
  1. an immediate notice with no follow-up fetch;
  2. the 4 s clear;
  3. a keyless follow-up after exactly 2500 ms, with no repeat;
  4. a follow-up that finds the key;
  5. a rejected or 500 follow-up: no notice, `error` null;
  6. a reset on the next send;
  7. `debug: null` → no fetch;
  8. a new send makes the follow-up stale;
  8b. `clearAll` makes it stale;
  9. an aborted stream shows nothing.
  - They use the file's `fetchEventSource` and `fetch` stubs, with fake timers after the mount `waitFor`.
- **Component tests (4):** the notice with `role=status`; nothing without it; streaming shows the typing label only; no server-like label.
  - The last one is weak: no label is passed, so it passes trivially.

### Parent rerun (WEB TEST LANE: guard, MemAvailable, `git status --short web` unchanged by each run, data/ and logs/)

- **Focused:** `env CI=1 npm --prefix web test -- src/api/useChatStream.test.ts src/components/chat/ProgressIndicator.test.tsx` gave **2 files, 25 passed** (1.85 s, 186,884 KB).
- **Full suite:** `env CI=1 npm --prefix web test` gave **4 files, 42 passed** (2.20 s, 186,576 KB).
- **Typecheck:** `env CI=1 npm --prefix web run typecheck` (`tsc --noEmit`) was clean (2.42 s, 401,492 KB).
- **After every run:** the web status was unchanged by the run, data/ was the same, and `logs/` was absent.
- **Scan:** the worker's post-edit scan is identical to pre-edit (TypeScript is outside the scanners). The parent did not re-run it, because no Python changed.

### Size

- **Total:** 492 changed lines. Tests: 329 hook + 56 component. Source: 3 `types.ts` + 81 `useChatStream.ts` + 22 `ProgressIndicator.tsx` + 1 `App.tsx`.
- **Exception:** a one-time exception (hard stop 500) by parent decision #2.

### Limitations carried forward

- **Late failures:** a background failure that settles more than 2.5 s after the stream ends (per-turn fact extraction or consolidation) stays logs-only.
- **Other failures:** non-`StoreWriteError` failures stay logs-only.
- **Duel and insight-assembly:** these have no delivered debug record until F13c-2b.
- **Hidden notice:** the notice is not visible while another view hides the chat column.
- **`/api/debug` cost:** the follow-up reads every redacted record once per turn with no `storage_failed` on `complete`.
- **Rebuild needed:** the deployed SPA (`web/dist`) must be rebuilt by the owner for the notice to appear (LANDING_NOTES).
- **Legacy Gradio:** logs-only (parent decision; its `submit_chat` is untestable without building the UI).
