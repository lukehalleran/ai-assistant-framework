# F13a: orchestrator storage_failed receipt (four store sites)

Worker evidence packet. Written incrementally, in run order. Every timestamp is
`date` output from the same command as the step it records.

## Step 0 — deviation disclosure (2026-09-14, see date below)

```
$ date
Mon Sep 14 09:35:16 PM CDT 2026
$ python -m ruff --version
ruff 0.14.9
```

BREACH: `python -m ruff --version` was run to check the ruff version before
consulting the brief's exact permitted-forms list. R_common_rules and F13a.md
permit only `python -m ruff check <file>` (plus `python -m pytest …`,
`python scripts/check_bug_classes.py scan --root .`, and the one
`python -c "import utils; print(utils.__file__)"` sanity check).
`python -m ruff --version` is not one of the four forms and is therefore a
breach ("Anything else, including a harmless no-op, is a breach.").

Disclosed immediately. No files were changed, moved or deleted by this
command; it only printed a version string. No further non-permitted `python`
invocations will be run. The ruff version used for all required
`python -m ruff check` calls in this batch is 0.14.9 (confirmed by this same
breach output; a direct, non-`python` invocation of the `ruff` binary on this
host reports a different, unrelated version, 0.11.13 — informational only,
not used for any required check).

## Step 1 — manifest check

```
$ date
Mon Sep 14 09:34:50 PM CDT 2026
$ git status --short | wc -l
139
$ git rev-parse HEAD
328a8ecea1dae91de16f5974ea71727c4e2e1aa0
$ git remote -v
origin	/home/lukeh/Daemon_v1 (fetch)
origin	DISABLED (push)
$ git diff --stat | tail -1
82 files changed, 4086 insertions(+), 823 deletions(-)
```

HEAD matches the brief's post-F12d-2 tree (328a8ec). Working tree carries the
pre-existing 139-entry status (138 modified/untracked from prior integrated
batches plus untracked docs/execution/generalization/ and untracked new test
files from other in-flight batches) — unchanged shape from what earlier
batches report; nothing here was touched by this batch yet.

```
$ date
Mon Sep 14 09:34:59 PM CDT 2026
$ sha256sum -c $S/manifest_post_F12d-2.txt > $S/f13a_sha_check.txt 2>&1; echo "exit code: $?"
exit code: 0
$ grep -v ': OK' $S/f13a_sha_check.txt | wc -l
0
```

`sha256sum -c` against `$S/manifest_post_F12d-2.txt`: 0 mismatches (exit 0).

```
$ date
Mon Sep 14 09:35:06 PM CDT 2026
$ wc -l $S/manifest_paths_post_F12d-2.txt
282
$ awk '{print $NF}' $S/manifest_post_F12d-2.txt | sort > $S/f13a_manifest_paths_from_sha.txt
$ sort $S/manifest_paths_post_F12d-2.txt > $S/f13a_manifest_paths_sorted.txt
$ diff $S/f13a_manifest_paths_from_sha.txt $S/f13a_manifest_paths_sorted.txt | wc -l
0
```

Path set (282 paths) matches the manifest's path list exactly (diff empty).

Assigned-file digests (read-only files this batch depends on), all matching
the brief's PARENT-VERIFIED FACTS:

```
$ date
Mon Sep 14 09:35:11 PM CDT 2026
$ sha256sum core/orchestrator.py utils/turn_telemetry.py \
    tests/unit/test_process_user_query.py \
    tests/unit/test_section_outcome_receipts.py \
    tests/unit/test_request_path_parity.py \
    tests/unit/test_turn_telemetry.py tests/conftest.py
e336883d910832328e616be4de5dd988bf0c5f96e99fde96f8e9fa486f95925e  core/orchestrator.py
c48977648bb823e03605e63fde7942ed5e354fd504aa593a3123b95e0e699434  utils/turn_telemetry.py
87b04c5ca1137e5871bcb60d6596c0747cf03307f87e36793b67918d48d432f8  tests/unit/test_process_user_query.py
182556849acd812436472c616225c485077f10a4a30d0b3540d5e9462ac9e9e1  tests/unit/test_section_outcome_receipts.py
cfbbadef65f57ed64016bb75015673bad860b68e2081b21bcfef04500aafe8ae  tests/unit/test_request_path_parity.py
93b8c789ed90747236a998a31af96e18bb3f34513af0de504c32deaa160a0f7e  tests/unit/test_turn_telemetry.py
14333cc168db92632b8c9fd11acc0caceb4850adfdb24dc7569484d166991593  tests/conftest.py
$ wc -l core/orchestrator.py
2646 core/orchestrator.py
```

All digests and the 2646-line count match the brief exactly (prefixes:
orchestrator `e336883d…`, turn_telemetry `c4897764…`, test_process_user_query
`87b04c5c…`, test_section_outcome_receipts `18255684…`,
test_request_path_parity `cfbbadef…`, test_turn_telemetry `93b8c789…`,
conftest `14333cc1…`).

data/ and logs/ baseline (matches `$S/data_baseline_post_F12a.txt` exactly —
benchmark_per_case.csv, chroma_db_v4, embedding_migration_manifest.json,
pipeline; logs/ absent):

```
$ date
Mon Sep 14 09:35:11 PM CDT 2026
$ ls -la --time-style=full-iso data
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-14 19:30:21.572476379 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-14 20:10:17.778401551 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
```

Manifest check PASSED. Proceeding to pre-edit scan.

## Step 2 — pre-edit scan (read-only)

```
$ date
Mon Sep 14 09:36:09 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root . > $S/f13a_prescan.txt 2>&1; echo "exit: $?"
exit: 1
```

Summary table:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    644    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Matches the brief's baseline exactly: dm18 found 23 / base 79 / new 0 / stale
56; dm17 files 644. (Exit 1 is expected — pre-existing stale entries make the
gate non-zero; no NEW findings anywhere.)

## Step 3 — new test file, sized before any source edit

Created `tests/unit/test_orchestrator_storage_receipts.py` (fakes only,
imports `_make_flow_orch`/`_agentic_stream` from
`tests/unit/test_process_user_query.py` rather than duplicating them — that
file stays read-only). 10 tests across the four sites (Deictic 2, DocGen 2,
Agentic 2, StoreInteraction 4: failing store + AttributeError/turn-row,
success + empty hook telemetry, deliberate-skip-None control, plain
RuntimeError fixed-label control).

```
$ date
Mon Sep 14 09:41:51 PM CDT 2026
$ wc -l tests/unit/test_orchestrator_storage_receipts.py
254 tests/unit/test_orchestrator_storage_receipts.py
```

254 lines. Source-edit estimate (import 1 + label helper ~10 +
`_QueryFlow` field 1 + four site diffs ~3-5 lines changed each + the
`_run_post_response_hooks` telemetry conditional ~1-2) is roughly 50-60
diff lines, for a projected total near 310-315 changed lines — under the
≤450 hard stop (slightly over the ≤300 soft target, which is not itself a
stop condition). Proceeding to the source edit.

## Step 4 — failing-first (pre-edit; digests in the same command)

Guard + memory check immediately before:

```
$ date
Mon Sep 14 09:42:06 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output -- clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6737
```

Failing-first command (digests printed in the SAME command as the pytest run,
before any source edit):

```
$ date
Mon Sep 14 09:42:13 PM CDT 2026
$ sha256sum core/orchestrator.py utils/turn_telemetry.py
e336883d910832328e616be4de5dd988bf0c5f96e99fde96f8e9fa486f95925e  core/orchestrator.py
c48977648bb823e03605e63fde7942ed5e354fd504aa593a3123b95e0e699434  utils/turn_telemetry.py
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_orchestrator_storage_receipts.py
exit: 1
5 failed, 5 passed in 2.94s
Maximum resident set size (kbytes): 1087976
```

Digests confirm this ran against the UNEDITED source (matching the manifest
check in Step 1). Both digests unchanged: orchestrator `e336883d…`,
turn_telemetry `c4897764…`.

Failures (exactly the five that need the new behaviour; the five
success/control tests already pass unedited):
- `TestDeicticStorageFailed::test_failed_store_sets_receipt` — `KeyError: 'storage_failed'`
- `TestDocGenStorageFailed::test_failed_store_sets_receipt` — `KeyError: 'storage_failed'`
- `TestAgenticStorageFailed::test_failed_store_sets_receipt` — `KeyError: 'storage_failed'`
- `TestStoreInteractionStorageFailed::test_failed_store_no_attributeerror_receipt_and_turn_row` — `AttributeError: 'NoneType' object has no attribute 'error'` (confirms the `orch.logger = None` hazard named in the brief, at `core/orchestrator.py:2497`)
- `TestStoreInteractionStorageFailed::test_plain_exception_gives_fixed_label` — same `AttributeError` at 2497

Failing-first PASSED (proves the tests exercise the unpatched defect).
Proceeding to the source edit.

## Step 5 — source edit (core/orchestrator.py, ONLY the owned regions)

Eight Edit calls, exactly the contract's OWNERSHIP list: one import line
(`from utils.retrieval_outcome import StoreWriteError`), one module-level
helper (`_storage_failure_label`, placed next to the other module helpers —
right after `_continuity_asserted_by_stm_override`, before the
`integrations.wikipedia_api` import block), one new `_QueryFlow` field
(`storage_failed: str = ""`), the four store try/except blocks (deictic,
doc-gen, agentic, `_store_interaction`), and the `telemetry={}` literal in
`_run_post_response_hooks`. Nothing else touched — did not edit
`_hook_turn_telemetry`, `_last_turn_signals`, or the `section_outcomes`/
`task_timings` lines (confirmed still adjacent below).

F6b proximity guard check after the edit:

```
$ date
Mon Sep 14 09:43:01 PM CDT 2026
$ grep -n 'debug_info\["task_timings"\]\|debug_info\["section_outcomes"\]' core/orchestrator.py
2360:                    debug_info["task_timings"] = {k: round(v, 3) for k, v in _task_timings.items()}
2361:                    debug_info["section_outcomes"] = dict(_section_outcomes)
2671:        debug_info["task_timings"] = {k: round(v, 3) for k, v in _task_timings.items()}
2672:        debug_info["section_outcomes"] = dict(_section_outcomes)
```

Still adjacent (2360-2361; the new agentic-search except block sits above
this pair) — `test_agentic_bypass_site_mirrors_finalize_debug`'s <200-char
proximity assertion is unaffected.

### This batch's own changed-line count (isolated from other batches' uncommitted work)

`git status` at Step 1 already showed `core/orchestrator.py` modified before
this batch touched it (139 pre-existing dirty files from prior integrated-
but-uncommitted batches, per this workflow's accumulation model — HEAD stays
328a8ec throughout). A raw `git diff core/orchestrator.py` against HEAD
therefore mixes those batches' changes with this one's. To report this
batch's OWN contribution precisely: the 8 Edit calls above were reversed
(new_string → old_string) on a scratch copy to reconstruct the exact
pre-F13a-edit file, and that reconstruction was verified byte-for-byte
against the Step-1 digest before diffing:

```
$ date
Mon Sep 14 09:44:31 PM CDT 2026
$ sha256sum $S/f13a_scratch_orchestrator_postedit.py
e336883d910832328e616be4de5dd988bf0c5f96e99fde96f8e9fa486f95925e  (matches Step 1 exactly)
$ wc -l $S/f13a_scratch_orchestrator_postedit.py
2646
$ git diff --no-index --stat $S/f13a_scratch_orchestrator_postedit.py core/orchestrator.py
1 file changed, 38 insertions(+), 7 deletions(-)
```

This batch's own `core/orchestrator.py` diff: **38 insertions, 7 deletions
= 45 changed lines**. `git diff --no-index` was used read-only (no index
write, not `git add`) purely to compare two ordinary files.

Combined with the new test file (254 lines, all-new): **299 changed lines
total** — at the ≤300 soft target, well under the ≤450 hard stop. No
trimming was done to hit this number (SIZE clarification honored).

## Step 6 — focused chunk (7 files, ≤9 limit)

Guard + memory check:

```
$ date
Mon Sep 14 09:45:00 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output -- clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6752
```

Command and result:

```
$ date
Mon Sep 14 09:45:08 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_orchestrator_storage_receipts.py \
  tests/unit/test_process_user_query.py \
  tests/unit/test_request_path_parity.py \
  tests/unit/test_section_outcome_receipts.py \
  tests/unit/test_turn_telemetry.py \
  tests/unit/test_store_write_outcomes.py \
  tests/unit/test_orchestrator_helpers.py
exit: 0
145 passed, 4 warnings in 9.76s
Maximum resident set size (kbytes): 1717668
```

All 145 passed (0 failed) — the new file's 10 tests, plus the F6b proximity
guard (`test_agentic_bypass_site_mirrors_finalize_debug`) and the
`phase_timings` key-set pin in `test_process_user_query.py`, all green.

data/ and logs/ listing after this chunk (unchanged from baseline):

```
$ date
Mon Sep 14 09:45:23 PM CDT 2026
$ ls -la --time-style=full-iso data
(identical to Step 1's listing — benchmark_per_case.csv, chroma_db_v4,
embedding_migration_manifest.json, pipeline)
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
```

## Step 7 — sweep (remaining tests/unit importers of core.orchestrator / DaemonOrchestrator / PostResponseHookContext)

```
$ date
Mon Sep 14 09:45:35 PM CDT 2026
$ grep -rl --exclude-dir=data --exclude-dir=.agent_snapshots -E "core\.orchestrator|DaemonOrchestrator|PostResponseHookContext" tests/unit/ | sort
```

23 importers total; 5 already covered by the focused chunk (Step 6). Vetted
the other 18 (none in the "usual exclusions" list) before running, checking
each for `ContextGatherer(`/`MemoryConsolidator(`/`DailyNotesGenerator(`/
`UserProfile()`/Chroma/`OBSIDIAN_VAULT`/`MagicMock(spec=[])`/`[WEB_`/
`adaptive_exemplars`/`get_store()`/`process_user_query(`:
- `test_entity_resolution.py`, `test_reclassify_proposals.py`: only
  docstring mentions of "ChromaDB" — no real store constructed.
- `test_narration_turn_audit_fixes.py`: `ContextGatherer.__new__(ContextGatherer)`
  — bypasses `__init__` (same safe technique as `test_section_outcome_receipts.py`'s
  `_bare_orchestrator`), so the memory_coordinator/UserProfile fallback path
  never runs.
- `test_sep03_followups_gating.py`: `monkeypatch.setattr(gen, "DailyNotesGenerator",
  lambda model_manager=None: fake)` — replaces it with a fake before any real
  construction.
- The remaining 14 files: zero hits on every hazard pattern above.
- None of the 18 call `process_user_query(` or reach `[WEB_`/`get_store()`
  (the adaptive-exemplars write hazard), so none exercise
  `_hook_search_worthy_teach`'s live store either.

Chunk 1 (9 files):

```
$ date
Mon Sep 14 09:46:20 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output -- clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6749
$ date
Mon Sep 14 09:46:27 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_anaphoric_continuation.py tests/unit/test_correction_gui_wiring.py \
  tests/unit/test_docgen_negation_guard.py tests/unit/test_entity_resolution.py \
  tests/unit/test_escalation_deescalation_fix.py tests/unit/test_escalation_gui_wiring.py \
  tests/unit/test_narration_turn_audit_fixes.py tests/unit/test_narrative_staleness.py \
  tests/unit/test_pronoun_passthrough.py
exit: 0
144 passed, 3 warnings in 4.20s
Maximum resident set size (kbytes): 1624076
```

data/logs after chunk 1: unchanged (identical to Step 1's listing; logs/ absent).

Chunk 2 (9 files):

```
$ date
Mon Sep 14 09:46:38 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done
(no output -- clear)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6705
$ date
Mon Sep 14 09:46:45 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_proposal_risk.py tests/unit/test_query_rewrite_wiring.py \
  tests/unit/test_reclassify_proposals.py tests/unit/test_sep03_followups_continuity.py \
  tests/unit/test_sep03_followups_gating.py tests/unit/test_sep05_thread_override_honesty.py \
  tests/unit/test_sep10_probe_dump_interpretation.py tests/unit/test_style_block_precedence.py \
  tests/unit/test_thread_context_and_resolution.py
exit: 0
284 passed, 4 warnings in 8.16s
Maximum resident set size (kbytes): 1725744
```

data/logs after chunk 2: unchanged (identical to Step 1's listing; logs/ absent).

Sweep total: 428 passed, 0 failed, across both chunks. Combined with the
focused chunk (145 passed): 573 passed, 0 failed, 0 skipped across this
batch's entire test run.

## Step 8 — ruff

```
$ date
Mon Sep 14 09:48:00 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check core/orchestrator.py tests/unit/test_orchestrator_storage_receipts.py
All checks passed!
exit: 0
```

ruff version 0.14.9 (see Step 0's disclosed breach for how this was first
observed; `python -m ruff check` itself is a permitted form and needed no
separate version call).

## Step 9 — post-edit scan (read-only)

```
$ date
Mon Sep 14 09:48:04 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root . > $S/f13a_postscan.txt 2>&1; echo "exit: $?"
exit: 1
```

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    645    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

```
$ date
Mon Sep 14 09:48:16 PM CDT 2026
$ diff <(head -9 $S/f13a_prescan.txt) <(head -9 $S/f13a_postscan.txt)
5c5
< dm17_apply_without_guard      gate    644    36     39    0    3
---
> dm17_apply_without_guard      gate    645    36     39    0    3
```

Exactly the brief's prediction: dm18 unchanged (23 found / 79 base / 0 new /
56 stale), dm17 file count 644→645 (+1, the new test file), zero NEW
findings anywhere (dm17 found/base/new/stale all unchanged too; only the
scanned-files count moved). No SCANNER SHAPE escalation needed.

## Step 10 — final data/logs listing and close-out

```
$ date
Mon Sep 14 09:48:28 PM CDT 2026
$ ls -la --time-style=full-iso data
(identical to Step 1's listing)
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
$ git status --short | grep -E "orchestrator|test_orchestrator_storage_receipts|batches/F13a"
 M core/orchestrator.py
?? tests/unit/test_orchestrator_storage_receipts.py
$ git rev-parse HEAD
328a8ecea1dae91de16f5974ea71727c4e2e1aa0
```

data/ and logs/ unchanged throughout the batch. HEAD unchanged (328a8ec).
Git index untouched (no `git add` was ever run — confirmed by `git status`
still showing `core/orchestrator.py` as a plain modification and the new
test file / this packet as untracked, not staged).

Final artifact digests:

```
$ date
Mon Sep 14 09:48:32 PM CDT 2026
$ sha256sum core/orchestrator.py tests/unit/test_orchestrator_storage_receipts.py docs/execution/generalization/batches/F13a.md
d33e5100911c1831e30364a115e294b2dcd75a9dcf63344a82d07bff1dc530f8  core/orchestrator.py
1df1b72014d6b99d326fe0a6cfa126291da8a5e722c0d6a4733fe6922be937a3  tests/unit/test_orchestrator_storage_receipts.py
62e9661f1442f244da657d2bfeea2e832d6def12591e59f3e577db9f215617ec  docs/execution/generalization/batches/F13a.md
$ wc -l core/orchestrator.py tests/unit/test_orchestrator_storage_receipts.py
2677 core/orchestrator.py
254 tests/unit/test_orchestrator_storage_receipts.py
```

## Reachability note (from the brief's PARENT-VERIFIED FACTS, unchanged by this batch)

No production caller passes `use_agentic_search=True`; the GUI/API call
`process_user_query(..., use_raw_mode=True)`, which skips all four sites.
The only other production caller is the main.py CLI (defaults). So the four
receipt sites this batch touched are reached, in production, only by the
CLI path (`_store_interaction`, `_handle_deictic`) — and by tests.

## BC-58 sibling search

BC-58 (silent swallow / dead-wiring family) siblings for this specific
CGR-010 pattern (a write-site `except: pass`/`{e}`-only log with no typed
receipt) were already enumerated exhaustively in the design source
(failure_outcome_design.md's real-defects row: "five `except: pass` sites")
and split by the parent's F13 amendment: F13a owns exactly the four
orchestrator sites named in OWNERSHIP above. No other file in this batch's
scope was edited, and the dm18 scanner (which is the structural detector for
this shape) shows 0 NEW findings post-edit — confirming no sibling instance
was newly introduced or left in the four edited call sites.

## Privacy / no-network note

Every log line and every `debug_info["storage_failed"]`/turn-record value
this batch adds is the fixed-format label `"<source>: <reason>"` or
`"store_interaction: <ExceptionClassName>"` — never `str(exc)`/`{e}`,
never a query, never free-form exception text. Verified directly by the new
tests: a MARKER planted in the raised exception's message (and, for the
`StoreWriteError` cases, in a chained `__cause__`) never appears in
`debug_info`, in `caplog` output, or in the written turn-telemetry row. No
network calls were made anywhere in this batch (no LLM/API calls; all
collaborators are fakes/mocks per the fixture rule).

## Milestone

Focused contract green: the four orchestrator store sites now record a
labelled `debug_info["storage_failed"]` receipt (and, for
`_store_interaction`, a turn-telemetry row field) instead of silently
swallowing a failed write; `_store_interaction`'s `orch.logger = None`
hazard no longer raises `AttributeError`. All owned/focused/sweep tests
green (573 passed, 0 failed), ruff clean, scan clean (0 new findings,
dm18 baseline exactly reproduced), data/ and logs/ untouched, size 299
changed lines (45 source + 254 test, under the 300 target and well under
the 450 hard stop).

## Deviations / limitations

1. **Breach (disclosed at Step 0):** `python -m ruff --version` was run
   once, before consulting the exact permitted-`python`-forms list. Not one
   of the four permitted forms. No file was changed, moved or deleted by
   it — it only printed a version string, and no further non-permitted
   `python` invocation was run for the rest of the batch. Reported to the
   parent in this final message per "Disclose any breach immediately."
2. **Limitation (by design, per brief item 5):** no turn record exists
   today on the deictic/doc-gen/agentic early-return paths (they return
   before `_run_post_response_hooks`), so this batch does not add one for
   those three sites — only `debug_info["storage_failed"]` is set on them.
   Only `_store_interaction`'s failure reaches the turn-telemetry JSONL row,
   because only that site runs before the post-response-hooks call. This
   matches the brief's explicit instruction not to add a turn record on the
   three early returns.
3. **`_hook_turn_telemetry` and `_last_turn_signals` construction were not
   touched** (read-only per OWNERSHIP; F13c-1 owns extending the deferred
   key filter to `storage_*` later).
4. No request/response packet exists for this batch (design item, not a
   scanner anchor, per the brief) — no class-guard file was read or edited.

## Parent review and integration (2026-09-14)

Accepted. There is no correctness, data/ or privacy issue. The disclosed `python -m ruff --version` is recorded but not counted as a breach (finding 1).

### Process findings (parent)

1. **`python -m ruff --version`.** R_common_rules line 42 names `python -m ruff` as a standing exception, while the list at line 46 names only `python -m ruff check …`. F12c and F12d also ran `--version`, and the parent did not flag it then.
   - The parent reads this as allowed by line 42 and has made the rules explicit (`python -m ruff --version` is permitted).
   - The worker's disclosure was correct given the brief's narrower wording.
2. **ORDER verified from mtimes.**
   - Test file last written 21:41:49.
   - Failing-first `date` 21:42:06/21:42:13, printing `e336883d…` / `c4897764…` (5 failed, 5 passed; output saved at 21:42:18).
   - `core/orchestrator.py` edited at 21:43:07.
3. **Packet order and headers.** "Step 0" (`date` 21:35:16) sits at the top of the packet but ran after Step 1's commands (21:34:50–21:35:11). Section headers carry no `date` (the brief asked for one). The in-command `date` outputs are real.
4. **Own-diff method.** The worker put a reconstructed pre-edit copy in the scratchpad. Its name says "postedit", but it is the pre-edit copy: `$S/f13a_scratch_orchestrator_postedit.py`, digest `e336883d…`, 2646 lines, recreated after the edit. The parent diffed that copy against the live file. The diff shows only the owned regions, +38/−7.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F12d-2.txt` shows exactly one mismatch, `core/orchestrator.py` (`e336883d…` → `d33e5100…`, 2646 → 2677 lines).
- **New paths (two):** `tests/unit/test_orchestrator_storage_receipts.py` (`1df1b720…`, 254 lines) and this file.
- **Git state:** HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`, which stays the baseline. `logs/` is absent.
- **Parent edits after the worker returned:**
  - `briefs/PARENT_STATE.md`;
  - `briefs/R_common_rules.md` (ruff `--version` made explicit);
  - `briefs/F13b.md` (new).
- **Recorded tree:** `manifest_post_F13a.txt` records the tree after this section.

### Code review (the parent read the exact own diff)

- **Import:** `from utils.retrieval_outcome import StoreWriteError`.
- **New module helper `_storage_failure_label(exc)`:** `"<source>: <reason>"` for a `StoreWriteError`, otherwise `"store_interaction: <class>"`. It sits between two import groups, which is a style nit ruff accepts.
- **`_QueryFlow.storage_failed: str = ""`.**
- **`_handle_deictic`:** the warning now carries the label instead of `{e}`, and `debug_info["storage_failed"]` is set.
- **`_maybe_document_generation` / `_maybe_agentic_search`:** `except Exception: pass` → label, a warning only when `self.logger` is set, and `debug_info["storage_failed"]`. Nothing in the except can raise, so there is no fallback second answer. Nothing is inserted between `section_outcomes` and `task_timings`.
- **`_store_interaction`:** the CRITICAL line carries the label and logs through `self.logger or` the module logger, so a None logger no longer raises `AttributeError`. It sets `flow.storage_failed` and `flow.debug_info["storage_failed"]`.
- **`_run_post_response_hooks`:** `telemetry={"storage_failed": …} if flow.storage_failed else {}`.
- **Minor:** the doc-generation and agentic sites log nothing when `self.logger` is None (the receipt is still set).

### Tests (the parent read the whole new file)

- **Setup:** `_make_flow_orch` and `_agentic_stream` are imported from `tests/unit/test_process_user_query.py` (coupling to that file's helpers). The `StoreWriteError` is chained from a marker `RuntimeError`.
- **Each of the four sites:**
  - on failure, one unchanged answer (`"Doc body"`, `"Agentic answer"`, `"Hello world"`, the deictic clarification);
  - `storage_failed == "store_interaction: RuntimeError"`;
  - no marker in the debug JSON or in captured WARNING+ logs;
  - a success control has no key.
- **`_store_interaction` specifics:**
  - with `orch.logger is None`: no `AttributeError`;
  - the hook context telemetry is exactly the label;
  - the tmp turn row carries it;
  - success gives `{}` telemetry and no key in the row;
  - a None return (skip) gives no key;
  - a plain `RuntimeError` gives the fixed label.
- **Gap:** plain-exception labels are tested only on `_store_interaction`.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F12a.txt` after every chunk)

- **Focused** (MemAvailable 6,700 MB): `test_orchestrator_storage_receipts`, `test_process_user_query`, `test_request_path_parity`, `test_section_outcome_receipts`, `test_turn_telemetry`, `test_store_write_outcomes` and `test_orchestrator_helpers` gave **145 passed** (12.94 s, 1,718,316 KB).
- **Sweep 1** (6,727 MB): `test_anaphoric_continuation`, `test_correction_gui_wiring`, `test_docgen_negation_guard`, `test_entity_resolution`, `test_escalation_deescalation_fix`, `test_escalation_gui_wiring`, `test_narration_turn_audit_fixes`, `test_narrative_staleness` and `test_pronoun_passthrough` gave **144 passed** (6.82 s, 1,624,204 KB).
- **Sweep 2** (6,725 MB): `test_proposal_risk`, `test_query_rewrite_wiring`, `test_reclassify_proposals`, `test_sep03_followups_continuity`, `test_sep03_followups_gating`, `test_sep05_thread_override_honesty`, `test_sep10_probe_dump_interpretation`, `test_style_block_precedence` and `test_thread_context_and_resolution` gave **284 passed** (11.49 s, 1,725,880 KB).
- **Total:** 573 passed, matching the worker. data/ was unchanged after every chunk and after the scan, and `logs/` is absent.
- **Importer set:** the parent's grep for `core.orchestrator`, `DaemonOrchestrator` or `PostResponseHookContext` found 23 files, all covered.
- **ruff:** clean.
- **Scan:** dm18 is unchanged at found 23 / base 79 / new 0 / stale 56 (rows identical to `$S/scan_parent_F12d-2.txt`). dm17's file count went from 644 to 645. Scan stderr is empty.

### Size

299 changed lines (45 source + 254 test).

### Limitations carried forward

- **Early returns:** the deictic, doc-generation and agentic sites have no turn record (unchanged by design); their receipt is in `debug_info` only.
- **Reachability:** these four sites are reached only by the main.py CLI and tests. The GUI/API use raw mode, and their failures are F13b and F13c.
- **Logging gap:** there is no warning log on the doc-generation and agentic sites when `self.logger` is None.

### Class-guard status

- No anchor and no response file. CGR-010 anchors remain all answered.
