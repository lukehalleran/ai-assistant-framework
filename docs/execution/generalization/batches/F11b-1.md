# F11b-1: proposal reads raise on a failed store read; a failed proposal write raises; batch loops count a failed write and keep going; check-proposals CLI reports a read failure cleanly

Worker evidence packet. Brief: `docs/execution/generalization/briefs/F11b-1.md`. Request packets:
`CGR-20260913-009.md` (#132, #133, #134, #136) and `CGR-20260913-010.md` (#131), read only from
`/home/lukeh/daemon_exec/class_guards/`. Response files:
`docs/execution/generalization/class_guard_responses/CGR-20260913-009-5.md` and
`docs/execution/generalization/class_guard_responses/CGR-20260913-010-4.md`.

## Manifest check (before)

- `sha256sum -c $S/manifest_post_F10c.txt` over the 247-path manifest: exit 0, **0 mismatches**.
- Path listing: `git status --short` expanded (`M` lines as-is; the one `??` directory,
  `docs/execution/generalization/`, walked with `find -type f`; other `??` files as-is), sorted, diffed
  against `$S/manifest_paths_post_F10c.txt` (247 lines): **empty diff**, 247 == 247.
- `git remote -v`: `origin  /home/lukeh/Daemon_v1 (fetch)`, `origin  DISABLED (push)`.
- HEAD: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged, matches the brief's base commit).
- `git stash list`: empty. `git reflog`: only the clone entry (`328a8ec HEAD@{0}: clone: from
  /home/lukeh/Daemon_v1`).
- Starting digests, verified against the brief:
  - `memory/proposal_store.py` `f26ba6a5cd14ebd0ea3c5d06f6555fe83a85728cb2bbf52bf1206331890877d3` — matches (449 lines expected).
  - `memory/shutdown_processor.py` `74c9ed30ccdc5bc159e57c8f84f0c712b1b13e9a8f4ec6f7cffbdeea6f8ced13` — matches.
  - `agent_branch/proposal_bridge.py` `3c7c23a29d550e7e9c55a23863d64484134eaed9bdd36697b160346e751a78e8` — matches.
  - `main.py` `6ef42c51327a26d4d070e09d412365507cdea4cc22137127edeafc05270a5eef` — matches (F10c's state).
  - `core/prompt/proposal_filter.py` `e54d1c2b938830b10341e1d7b739c64b4e4cfe1fd50830bb43b8c372451bc028` — matches (read-only).
  - `core/prompt/gatherer_knowledge.py` `00870a5bd0447c7b02cdfd9d993c68698d16f9f20b29dddbed2661319581f4a9` — matches (read-only).
  - `gui/tabs/proposals.py` `219a71fd7b67fa947d70fb04f5a21911e245cd036614fc6495a24e39cbe8114d` — matches (read-only).
  - `agent_branch/portfolio.py` `436b0be589b9c809605a6158a5ac42f23262a7eb73ae38758761f36f8beb992e` — matches (read-only).
- Sanity check: `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c "import utils; print(utils.__file__)"`
  → `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (inside this clone).
- `data/`: `ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F10c.txt`: **empty diff**
  (unchanged, 7 entries). `logs/`: absent (`ls -ld logs` → No such file or directory).

Manifest check clean. Proceeding.

## Pre-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .`
→ exit 1 (gate scanners with findings exit non-zero by design), stderr empty (`scan_pre_f11b1.stderr`, 0 lines,
kept under /tmp, never the repo).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 636 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **38** | **79** | **0** | **41** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

dm18 matches the brief's expected post-F10c baseline exactly (38 / 79 / 0 / 41).

## New test file

`tests/unit/test_proposal_store_outcomes.py` — 412 lines at failing-first (the version covering
ALL anchors, run on the unedited sources below). It was later trimmed and partially restored per
the "Size and split" section — final delivered version 317 lines, sha256 `21972a16…` (see that
section for the full history). A LOCAL `_RaisingChromaStore`/`_RaisingCollection` (copy of
`test_proposal_store.py`'s `MockChromaStore`/`MockChromaCollection` shape, extended with
`raise_on_add` / `raise_on_list` / `raise_on_query` switches), driving the DEPLOYED `ProposalStore`,
`ProposalFilter`, `KnowledgeRetrievalMixin.get_proposed_features` and (for the read-only
`_check_implementation_tracking` evidence) `ShutdownProcessor` directly. No real Chroma, embedder,
model, filesystem scan or data path anywhere.

## Failing-first (on the UNEDITED sources; digests in the same command)

```
sha256sum memory/proposal_store.py memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py && \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M \
  /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_proposal_store_outcomes.py
```

Guard: clear (no running pytest). MemAvailable: 6635 MB. data/: unchanged (empty diff vs
`$S/data_baseline_post_F10c.txt`). logs/: absent.

Digests (unedited): `memory/proposal_store.py` `f26ba6a5…`, `memory/shutdown_processor.py`
`74c9ed30…`, `agent_branch/proposal_bridge.py` `3c7c23a2…`, `main.py` `6ef42c51…` — all match the
brief's starting digests.

**Result: 8 failed, 10 passed** (exit 1), 4.66s wall / 1,070,936 KB peak RSS. Failures (exactly the
five anchors plus the two batch-loop callers, as expected — the corrupt-record sub-case of #133
counted separately):
- `TestStoreProposalRaises::test_add_raises_raises_store_write_error` — DID NOT RAISE (#131).
- `TestQueryProposalsRaises::test_query_raises_raises_retrieval_error` — DID NOT RAISE (#132).
- `TestGetProposalRaises::test_list_all_raises_raises_retrieval_error` — DID NOT RAISE (#133).
- `TestGetProposalRaises::test_corrupt_matching_record_raises_retrieval_error` — DID NOT RAISE (#133 sibling).
- `TestGetPendingRaises::test_list_all_raises_raises_retrieval_error` — DID NOT RAISE (#134).
- `TestGetPendingAndApprovedRaises::test_list_all_raises_raises_retrieval_error` — DID NOT RAISE (#136).
- `TestShutdownGenerateProposalsBatchLoop::test_one_failed_write_is_counted_second_still_stored` —
  `assert 1 == 2` (today's `store_proposal` call stops at the exception, since the broad
  `except Exception` in `_generate_proposals` swallows it and the whole loop is abandoned after the
  first raise — "1 failed" is never counted, it stops silently).
- `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored` — the
  `StoreWriteError` propagates uncaught out of `ingest_survivors` (no local try today), so the
  second survivor is never attempted.

Passing controls (10, evidence the fixtures are sound and existing behaviour is otherwise
unchanged): the five skip/genuine-empty/not-found/found controls, the two `get_pending`/
`get_pending_and_approved` empty controls, the filter/gatherer propagation test (already correct —
it exercises the EXISTING outer `except Exception` in `get_proposed_features`, independent of this
batch's edit), the `_check_implementation_tracking` evidence test (also already correct — its outer
`except Exception` already catches `RetrievalError`, a `RuntimeError` subclass), and the
similarity-skip control (`check_similarity` returning an id never reaches `store_proposal`).

data/ after this chunk: unchanged (empty diff). logs/: absent.

## Source edit (all four files, then SIZE discovery and split)

Edited all four owned files per CONTRACT 1-4 and 7: `memory/proposal_store.py` (import line +
five method bodies/docstrings), `memory/shutdown_processor.py` (`_generate_proposals` loop: `failed`
counter + typed catch), `agent_branch/proposal_bridge.py` (`ingest_survivors`: typed catch),
`main.py` (the two `check-proposals` reads wrapped in `except RetrievalError`).

**SIZE discovery.** `memory/shutdown_processor.py` and `main.py` were already modified by earlier
batches (F10b/F11a-1 thread-and-skill hunks; F10c's `main.py:469` one-liner). Isolating THIS batch's
own hunks from those (per the F10c sizing precedent — a file already `git diff`-modified by other
batches cannot be sized by the file's total diff) gives, via `git diff --numstat` cross-checked
against each hunk by hand:
- `memory/proposal_store.py` (untouched by any prior batch, full diff is mine): 47 + 9 = **56**.
- `agent_branch/proposal_bridge.py` (untouched by any prior batch, full diff is mine): 6 + 1 = **7**.
- `memory/shutdown_processor.py` (total diff 33+10=43; F10b's skill-loop hunk and F11a-1's two
  thread hunks are NOT mine): my one hunk (the `_generate_proposals` loop) = 3 removed + 9 added =
  **12**.
- `main.py` (total diff 14+9=23; F10c's `inspect_summaries` one-liner at 469 is NOT mine, -1+1=2):
  my one hunk (the two `check-proposals` reads) = 8 removed + 13 added = **21**.
- Source total: 56 + 7 + 12 + 21 = **96**.
- New test file (full `wc -l`, the new-file sizing convention per R_common_rules and the F11a-1
  precedent): **412**.
- **Grand total: 96 + 412 = 508 — over the 450 HARD cap** (and well over the 380 target).

**Split decision.** The brief pre-authorized exactly this split: "store + filter/gatherer evidence
vs the two loop callers + CLI" (briefs/F11b-1.md, SIZE line). Applying it literally:
- **Part A (delivered this batch):** `memory/proposal_store.py` (all five methods, #131-#134/#136)
  + the filter/gatherer propagation test. Source 56 + test (see below) = well under the cap.
- **Part B (reverted / deferred to a follow-up batch):** `memory/shutdown_processor.py`'s
  `_generate_proposals` loop, `agent_branch/proposal_bridge.py`'s `ingest_survivors` loop, and
  `main.py`'s `check-proposals` CLI wrap, plus their tests (`TestShutdownGenerateProposalsBatchLoop`,
  `TestCheckImplementationTrackingEvidence`, `TestIngestSurvivorsBatchLoop`).

**Reverts (Edit tool, restoring the exact pre-batch text — verified by digest):**
```
sha256sum memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py
```
→ `74c9ed30…` / `3c7c23a2…` / `6ef42c51…` — all three match the brief's starting digests exactly
(the manifest-check values), confirming a clean, complete revert with no residue.

**Test file trim.** `tests/unit/test_proposal_store_outcomes.py` truncated from 412 to 264 lines:
removed `TestShutdownGenerateProposalsBatchLoop` and `TestIngestSurvivorsBatchLoop` (both genuinely
depend on the deferred Part B loop-caller edits) and their now-unused fixtures/imports; rewrote the
module docstring to record the split. No assertion, docstring or comment was trimmed WITHIN any kept
test — only whole, out-of-scope test classes were removed (a scope cut, not a squeeze).

**`TestCheckImplementationTrackingEvidence` restored.** On review this class has NO Part B
dependency: `ShutdownProcessor._check_implementation_tracking` is read-only in BOTH parts of the
split (never edited by either), and its existing, unmodified outer `except Exception` already
catches the new `RetrievalError` from `get_pending_and_approved` (#136, fixed in Part A) exactly the
way it catches anything else. It was cut only for expediency when the whole Part B block was
removed; restored (with its `_shutdown_processor()` helper and the `datetime`/`AsyncMock`/
`MagicMock`/`patch` imports it needs) since it is genuine Part A evidence and fits comfortably
within budget. Final test file: **317 lines**, sha256 `21972a16…`.

**Final Part A size: 56 (source) + 317 (test) = 373 changed lines** — under the 380 target, well
under the 450 hard cap.

**Consequence disclosed (not a NEW class-guard instance, but a transient behaviour note):** until
the deferred Part B lands, `store_proposal`'s new `StoreWriteError` (now raised by this batch) is
NOT yet caught by its two loop callers. A single failed write during `_generate_proposals` or
`ingest_survivors` will propagate to each function's existing OUTER `except Exception` (unchanged,
pre-existing code) and abandon the REST of that batch/run instead of skipping just the one item —
coarser-grained than the eventual per-item degrade, but not silent: the existing outer handler
already logs it (`"[Shutdown] Proposal generation failed: {e}"` / the caller's
`"portfolio: proposal ingest failed"`). This is flagged for the parent as the reason Part B should
land promptly.

**Failing-first evidence for the delivered Part A subset** (drawn from the single failing-first run
above, on the then-fully-unedited sources — valid because `memory/proposal_store.py` was genuinely
unedited at that point, per its digest `f26ba6a5…` printed in that same command): of the 8 original
failures, 6 belong to Part A (`TestStoreProposalRaises`, `TestQueryProposalsRaises`,
`TestGetProposalRaises` x2, `TestGetPendingRaises`, `TestGetPendingAndApprovedRaises`); of the 10
original passes, 8 belong to Part A (the five skip/empty/not-found/found controls, the two
get_pending*/get_pending_and_approved* empty controls, and the filter/gatherer propagation test).
Of the 8 original failures, 6 belong to the delivered Part A scope; of the 10 original passes, 9
belong to Part A (the five skip/empty/not-found/found controls, the two get_pending*/
get_pending_and_approved* empty controls, the filter/gatherer propagation test, and the
`_check_implementation_tracking` evidence test — restored below since it has no Part B dependency).
The 2 remaining failures and 1 remaining pass (`test_similarity_skip_control_not_counted_as_failed`)
belonged to the removed `TestShutdownGenerateProposalsBatchLoop`/`TestIngestSurvivorsBatchLoop`
classes.

## Focused chunk (guarded, capped, `-p asyncio`)

Guard: clear. MemAvailable: 6616 MB.

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M \
  /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider \
  -p asyncio \
  tests/unit/test_proposal_store_outcomes.py tests/unit/test_proposal_store.py \
  tests/unit/test_proposal_filter.py tests/unit/test_shutdown_pipeline_proposals.py \
  tests/unit/test_gatherer_outcomes_remaining_knowledge.py tests/unit/test_ordered_slice_guard.py \
  tests/unit/test_retrieval_outcome.py tests/unit/test_store_write_outcomes.py \
  tests/unit/test_thread_outcomes.py
```

First run (264-line test file, before `TestCheckImplementationTrackingEvidence` was restored):
**218 passed**, 6.35s wall / 1,675,808 KB peak RSS, exit 0. Re-run after restoring that class (final
317-line test file): **219 passed**, 6.52s wall / 1,675,124 KB peak RSS, exit 0. `test_proposal_store.py`
passed UNEDITED (FIXTURE RULE: the skip/genuine-empty/not-found tests needed no repair — this batch
made no edits to that file at all). `test_shutdown_pipeline_proposals.py` and
`test_ordered_slice_guard.py` pass unchanged (evidence `memory/shutdown_processor.py` truly reverted
to its pre-batch behaviour).

data/ after each of these two runs: unchanged (only the repo-root `..` mtime line differs — my own
`main.py` edit-then-revert touches the root, the same benign effect F10c recorded; no entry inside
`data/` itself differs). logs/: absent.

## Sweep (guarded, capped, `-p asyncio`)

Importer grep: `grep -rl --include="*.py" -E "memory\.proposal_store|\bProposalStore\b|core\.prompt\.proposal_filter|\bProposalFilter\b|agent_branch\.proposal_bridge|memory\.shutdown_processor|\bShutdownProcessor\b" tests/unit` found 14 files; 6 are already in the focused chunk
(`test_ordered_slice_guard.py`, `test_proposal_filter.py`, `test_proposal_store_outcomes.py`,
`test_proposal_store.py`, `test_shutdown_pipeline_proposals.py`, `test_thread_outcomes.py`). The
remaining 8 (all match only `ShutdownProcessor`, unedited this batch — `memory/proposal_store.py`
is the only production module this batch actually changed) form one sweep chunk:
`test_entity_facts.py`, `test_knowledge_graph.py`, `test_retrospective_small_guards.py`,
`test_sep09_storage_repairs.py`, `test_skill_and_summary_outcomes.py`,
`test_source_excerpt_pipeline.py`, `test_stance_write_path.py`, `test_stream_artifacts.py`.

Vetted before running: all `ShutdownProcessor` construction sites pass explicit `user_profile`
(`None` or a `MagicMock`) and `MagicMock`/fixture stores — none builds a bare `ContextGatherer` or
calls `UserProfile()`; `test_sep09_storage_repairs.py`'s `_make_store` uses
`chromadb.EphemeralClient` (in-memory, no `CHROMA_PATH`/disk persistence, a pre-existing pattern in
that file, not introduced by this batch); `test_knowledge_graph.py`'s `graph`/`resolver` fixtures
are `tmp_path`-scoped; `test_stance_write_path.py` and `test_source_excerpt_pipeline.py` never
construct `ShutdownProcessor` at all (a staticmethod call / source introspection). None is in the
excluded list.

Guard: clear. MemAvailable: 6615 MB.

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M \
  /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider \
  -p asyncio \
  tests/unit/test_entity_facts.py tests/unit/test_knowledge_graph.py \
  tests/unit/test_retrospective_small_guards.py tests/unit/test_sep09_storage_repairs.py \
  tests/unit/test_skill_and_summary_outcomes.py tests/unit/test_source_excerpt_pipeline.py \
  tests/unit/test_stance_write_path.py tests/unit/test_stream_artifacts.py
```

**271 passed**, 7.83s wall / 3,251,916 KB peak RSS, exit 0 (115 warnings, all pre-existing
deprecation noise from spacy/chromadb/swig, unrelated to this batch).

**Total (final): 219 + 271 = 490 passed, 0 failed.**

data/ after this chunk: unchanged (only the same benign repo-root `..` mtime line). logs/: absent.

## ruff

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check memory/proposal_store.py tests/unit/test_proposal_store_outcomes.py --no-cache`
→ **All checks passed!** ruff **0.14.9**.

## Post-edit scan (read-only)

`PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .`
→ exit 1 (gate scanners with findings, by design), stderr empty (`scan_post_f11b1.stderr`, 0 lines,
kept under /tmp).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 637 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **33** | **79** | **0** | **46** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

dm18 moved **38 → 33** found, **41 → 46** stale (exactly -5/+5), **new = 0** — no SCANNER SHAPE
issue. The five new STALE rows are exactly the five `memory/proposal_store.py` anchors:
```
dm18_except_returns_empty: memory/proposal_store.py [ProposalStore.get_pending] 'return []'
dm18_except_returns_empty: memory/proposal_store.py [ProposalStore.get_pending_and_approved] 'return []'
dm18_except_returns_empty: memory/proposal_store.py [ProposalStore.get_proposal] 'return None'
dm18_except_returns_empty: memory/proposal_store.py [ProposalStore.query_proposals] 'return []'
dm18_except_returns_empty: memory/proposal_store.py [ProposalStore.store_proposal] 'return None'
```
**#131, #132, #133, #134 and #136 all went STALE, as expected.** dm17's file count moved 636→637
(a benign housekeeping count, `new` stays 0, unrelated to any finding). dm01/dm31 unchanged.

## Data/ and privacy/no-network note

`data/` never diverged from `$S/data_baseline_post_F10c.txt` beyond the repo-root `..` mtime line
(caused by this worker's own `main.py` edit-then-revert, exactly the F10c-documented mechanism —
not a test write). `logs/` stayed absent throughout. No network call was made (all tests are
in-process fakes or ephemeral in-memory Chroma); no real institution name or IP appears anywhere in
the new file — `MARKER = "F11BQZ7"` is the only injected string, and every assertion confirms it
never reaches a NEW error message.

## BC-58 sibling search

`_ensure_collection`'s `except Exception` at ≈65-67 (a failed `create_collection` disguised as the
"not configured" skip) is a recorded BC-58 sibling — read-only, not in OWNERSHIP, left unfixed with
this evidence. `check_similarity` (def 276, `except ... return None` 348, where `None` also means
"no duplicate" — fails open) and `get_for_dedup` (def 415, `except ... return ""` 447) are read-only
siblings per the brief, also left unfixed. `query_proposals`'s `coll is None` → `[]` branch (≈131)
is unchanged, per CONTRACT item 2 — a sibling, not this anchor.

## Owner data/ cleanup

Not observed during this batch. The three F2-era leftovers
(`data/chroma_multi`, `data/web_search_credits.json`, `data/user_profile.json`) are still present,
identical to `$S/data_baseline_post_F10c.txt`, at every check throughout this batch (matching F10c's
own note that the cleanup had "not [been] run by the end of this batch").

## Excluded test files

None of the standing exclusions (`tests/test_web_search_manager.py`,
`tests/test_prompt_internal_methods.py`, `tests/unit/test_graph_integration.py`,
`tests/unit/test_daily_notes_auto_update.py`, any top-level `tests/` file) were candidates for this
batch's focused or sweep sets — the importer grep returned no matches among them.

## Deviation summary (disclosed at once, per COMPLIANCE)

**SIZE hard-cap overrun, self-corrected via the brief's pre-authorized split.** Isolating this
batch's own hunks from earlier batches' overlapping edits to `memory/shutdown_processor.py` and
`main.py` put the full CONTRACT scope (all 5 `ProposalStore` methods + both batch-loop callers +
the CLI fix + the full test file) at 508 changed lines, over the 450 hard cap. Per the brief's own
"stop BEFORE exceeding it and return a split proposal" with its exact pre-authorized split line
("store + filter/gatherer evidence vs the two loop callers + CLI"), this batch:
1. delivered the full `memory/proposal_store.py` fix (#131-#134, #136) and the filter/gatherer
   propagation evidence, at 373 total changed lines (56 source + 317 test) — under the 380 target;
2. drafted, digest-verified, and test-verified (against the original failing-first run) the two
   batch-loop callers and the `main.py` CLI wrap, then REVERTED all three files to their exact
   pre-batch digests once the overrun was discovered, and removed the corresponding test classes;
3. recorded the deferred work verbatim in `CGR-20260913-010-4.md` ("Batch-loop continuations:
   drafted, verified, then DEFERRED") so a follow-up batch can apply it directly, subject to its own
   independent failing-first proof.

No comment, docstring or assertion was trimmed to fit; only whole out-of-scope test classes were
cut. No SCANNER SHAPE issue arose (dm18 new = 0 throughout). This is the batch's only deviation from
the brief; it is fully disclosed in both response files' "SIZE-driven split" sections.

## Milestone

Focused contract green for the delivered Part A scope (all tests pass; 0 failed).

## Parent review and integration (2026-09-14)

Accepted for Part A: the five `ProposalStore` anchors plus the filter/gatherer and implementation-tracking evidence. One deviation is recorded. The deferred Part B becomes batch **F11b-1b**, which runs next.

### Process findings (parent)

1. **SIZE: the split was self-applied after the cap was exceeded (deviation).**
   - What the rule required: the brief said "stop BEFORE exceeding it and return a split proposal". The worker instead implemented the whole contract, measured 508 changed lines, then applied the brief's pre-described split itself.
     - It reverted the three Part B files.
     - It removed the Part B test classes.
   - Mitigations, all verified by the parent:
     - the reverted files match their pre-batch digests exactly (`74c9ed30…`, `3c7c23a2…`, `6ef42c51…`; the manifest re-check has 0 mismatches);
     - only whole out-of-scope test classes were removed, and nothing inside a kept test was trimmed;
     - the deferred edits are recorded verbatim in `CGR-20260913-010-4.md` for the follow-up.
   - Accepted. `R_common_rules.md` SIZE now also says to measure before editing the later parts, and never to implement and then revert.
2. **Interim behaviour until F11b-1b lands.** `store_proposal` and the proposal reads now raise, but their loop callers and the CLI are not yet updated.
   - A failed write in shutdown `_generate_proposals` or `ingest_survivors` now aborts the rest of that loop. Each loop's existing outer handler logs it.
   - `main.py check-proposals` would show "Startup failed" plus a traceback on a store read failure.
   - None of this is silent or data-losing, and the tree is not deployed. F11b-1b is queued immediately to close the window.
3. **Repository-root mtime.** The `data/` listing's `..` line changed from 18:44:53 to 19:12:13 because of the worker's `main.py` edit-then-revert. `main.py` is back to `6ef42c51…`, and no stray file exists. The parent saved a refreshed baseline, `$S/data_baseline_post_F11b-1.txt`.
4. **Owner cleanup:** not run during this batch.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F10c.txt` gives 0 mismatches. The three reverted files match, and no parent edit happened during the run.
- **New paths (five):**
  - `memory/proposal_store.py` (`f26ba6a5…` → `f9262df1…`, 487 lines, its first modification);
  - `tests/unit/test_proposal_store_outcomes.py` (`21972a16…`, 317 lines);
  - `class_guard_responses/CGR-20260913-009-5.md` (`e0180808…`, 227 lines);
  - `class_guard_responses/CGR-20260913-010-4.md` (`9d74e5d5…`, 203 lines);
  - this file.
- **Git state:** `git diff --cached --name-only` is empty, HEAD `328a8ec` is unchanged, the reflog shows only the clone entry, and the stash list is empty.
- **`logs/`:** absent.
- **Parent edits after the worker returned:**
  - `briefs/F11b-1b.md` (new);
  - `briefs/PARENT_STATE.md`;
  - `briefs/R_common_rules.md` (SIZE clarification);
  - the design doc (F11b-1b added to the F11b split).
- **Recorded tree:** `manifest_post_F11b-1.txt` records the tree after this section.

### Code review (the parent read the full `proposal_store.py` diff)

- **Import:** `RetrievalError, StoreWriteError`.
- **`store_proposal` (#131):** the skip is unchanged. The except keeps its error line, then raises `StoreWriteError(source="proposal_store", reason=<class>)`.
- **`query_proposals` (#132):** raises `RetrievalError(reason="query:<class>")`. The skip, `coll is None`/`count() == 0` → [], the per-item skips and the status post-filter are unchanged.
- **`get_proposal` (#133):** raises `RetrievalError(reason="get:<class>")`. This also covers a corrupt matching record, which now lands in the same except. The skip and not-found → None are unchanged.
- **`get_pending` (#134) and `get_pending_and_approved` (#136):** raise `RetrievalError` with reasons `pending:<class>` and `pending_approved:<class>`. Skip, empty and per-item skips are unchanged.
- **Docstrings:** state the skip/raise contract.
- **Unchanged:** every other method (`update_status`, `update_tracking_metadata`, `check_similarity`, `get_for_dedup`, `_ensure_collection`).

### Tests (the parent read the whole new file)

- **`store_proposal`:** a raising add gives `StoreWriteError` with no marker; the no-chroma skip gives None.
- **`query_proposals`:** a raising query gives `query:`; controls: an empty collection and no chroma give [].
- **`get_proposal`:**
  - a raising `list_all` gives `get:`;
  - a corrupt matching record (invalid `steps_json`) raises;
  - controls: not found gives None, and found returns the proposal.
- **`get_pending` and `get_pending_and_approved`:** raising gives the matching prefix; empty gives [].
- **Through the filter and gatherer:** the real `ProposalFilter` over a raising store, driven through `get_proposed_features`, gives `("failed", "RetrievalError")` and `[]`.
- **`_check_implementation_tracking`:** built on a MagicMock-backed `ShutdownProcessor`, with `ProposalStore` and `ImplementationDetector` patched. A raise from `get_pending_and_approved` means the detector is never constructed and "Implementation tracking failed" is logged.
- **Failing-first:** 8 failed and 10 passed on fully unedited sources. 6 of the failures and 9 of the passes belong to the delivered scope.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F11b-1.txt`)

- **Focused** (MemAvailable 6,535 MB): the 9 files in §"Focused chunk" gave **219 passed** (9.43 s, 1,676,592 KB).
- **Sweep** (6,546 MB): `test_entity_facts`, `test_knowledge_graph`, `test_retrospective_small_guards`, `test_sep09_storage_repairs`, `test_skill_and_summary_outcomes`, `test_source_excerpt_pipeline`, `test_stance_write_path` and `test_stream_artifacts` gave **271 passed** (11.42 s, 3,251,596 KB).
- **Total:** **490 passed, 0 failed**, matching the worker.
- **Importer set:** 14 files, matching the worker. A looser grep also picks up four `PendingProposalStore` files, which are a different store.
- **`data/`:** unchanged after both chunks, and `logs/` is absent.
- **ruff 0.14.9:** clean.
- **Scan:**
  - dm18 moved from found 38 / base 79 / new 0 / stale 41 to 33 / 79 / 0 / 46.
  - All five `ProposalStore` rows (`store_proposal`, `query_proposals`, `get_proposal`, `get_pending`, `get_pending_and_approved`) are now in the STALE list, which has 54 rows in total.
  - dm17's file count went from 636 to 637. Scan stderr is empty.

### Size

373 changed lines (56 source + 317 test).

### Class-guard status

- **CGR-010:** #127–#129, #131, #140 and #146 are answered.
  - Open: F11b-1b's loop-caller completion (response `-5`) and the F13 receipts.
- **CGR-009:** #121, #122, #130, #132–#134, #136, #139, #147 and #148 are answered.
  - Open: #117–#120 → F12a (`-6`); #123, #125, #126 → F12c (`-7`); #141–#143 → F12d (`-8`).
