# H03b-1: remove real-school Wikipedia seeds; synthetic names in core identity/institution test fixtures

Labels only throughout (see `$S/H03b_map.md`). No real institution/person name, abbreviation, fragment, domain, snake_case form, or School A's real state word appears in this file.

## Manifest check

`date`: Tue Sep 15 12:13:28 AM CDT 2026 (repo status/HEAD/remote check); manifest sha256/path verification run immediately after.

- HEAD: 328a8ecea1dae91de16f5974ea71727c4e2e1aa0 (matches launch facts). Index empty (`git diff --cached --stat` empty).
- `git remote -v`: origin fetch=/home/lukeh/Daemon_v1, push=DISABLED. Confirmed, not changed.
- `sha256sum -c $S/manifest_post_H03.txt`: 304 entries, 0 mismatches (`grep -c -v ": OK"` = 0).
- Path set: `awk '{print $2}' manifest_post_H03.txt | sort` diffed against `sort manifest_paths_post_H03.txt` → 0 lines of diff (exact match).
- `git status --short web` diffed against `$S/web_status_post_F13c-2a.txt` → 0 lines of diff (exact match).
- `ls -la --time-style=full-iso data` diffed against `$S/data_baseline_post_F12a.txt` → 0 lines of diff (exact match).
- `ls -ld --time-style=full-iso logs`: No such file or directory (absent, as expected).

Manifest check clean. Proceeding to pre-edit scan.

## Pre-edit scan

`date`: Tue Sep 15 12:14:08 AM CDT 2026

`python scripts/check_bug_classes.py scan --root .` (read-only): exit=1 (expected; STALE entries present, designed handoff, not this batch's concern).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm16_config_key_reachability | report | 395 | 89 | - | - | - |
| dm17_apply_without_guard | gate | 648 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | 23 | 79 | 0 | 56 |

Matches launch facts exactly: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648. No new findings.

## Collision checks and pre-copies

`date`: Tue Sep 15 12:14:34 AM CDT 2026

- `grep -rniw` (excluding data/, .agent_snapshots/, node_modules/) for the mapping's synthetic forms (R-replacement, D-replacement, E/F/G-replacement tokens, the I-fallback phrase, the V-replacement) over *.py/*.md/*.json: 6 hits, all in `docs/execution/generalization/briefs/H03b-1.md` (the parent's own brief, read-only, pre-existing). 0 hits elsewhere — matches launch facts ("Collision checks already clean").
- Token-set re-verification (`date` same command, 12:15:04 AM CDT): A/A-frag forms absent (case-insensitive) from `_TEMPORAL_GENERIC_TOKENS` (core/agentic/gate.py ≈500-524: 0 hits). `_PRIVATE_SPHERE_GENERIC_TOKENS`/`_PRIVATE_GUARD_TIME_STOP_TOKENS` block (utils/web_search_trigger.py 1590-1625): 2 hits, both inside a comment at ≈1590-1591 — this is the H03-added source comment (read-only for this batch, out of scope, already noted in launch facts as H03's prior work). A-frag IS present in `utils/location_resolver.py` `_US_STATES` table (≈255-275: 1 hit) as required — generic code table, unchanged.

`date`: Tue Sep 15 12:14:34 AM CDT 2026

Copied the six assigned files to `$S/h03b1_pre/` and verified sha256 equality repo vs. copy (all six match, and match the launch-fact digests exactly):
- scripts/build_wiki_subset.py `42f937f5…`
- tests/unit/test_sep12_search_identity_scope.py `b7f26e6a…`
- tests/unit/test_sep12_followup_identity_names.py `97049059…`
- tests/unit/test_institution_resolver.py `4c7a7be2…`
- tests/unit/test_private_sphere_search_guard.py `4cc2a3b3…`
- tests/unit/test_personal_anchors.py `10d4ddf8…`

## Baseline test run (unedited tree)

`date`: Tue Sep 15 12:15:26 AM CDT 2026 (guard clear, MemAvailable 6970 MB)

Digests printed in the same command as the baseline run (all six match launch facts, confirmed above). Command: `systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=<clone>/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio -rA <5 files>`

Exit status 0. 156 passed, 0 failed, 0 errors. Elapsed 0:05.23-0:05.24, Maximum RSS ≈1.07 GB.

Per-file pass counts (baseline):
| File | Passed |
|---|---|
| test_sep12_search_identity_scope.py | 56 |
| test_sep12_followup_identity_names.py | 28 |
| test_institution_resolver.py | 49 |
| test_private_sphere_search_guard.py | 16 |
| test_personal_anchors.py | 7 |
| **Total** | **156** |

`date`: Tue Sep 15 12:15:59 AM CDT 2026 — data/ listing diffed against `$S/data_baseline_post_F12a.txt`: 0 lines (unchanged). `logs/`: absent (unchanged).

## Edits

`date`: Tue Sep 15 12:20:00 AM CDT 2026 (edits performed across the six files; per-file digests and diffs verified immediately after each file's edits — see below).

All edits used the Edit tool only (Write/Edit, no scripts). Label replacements applied via whole-string `replace_all` where safe, with compound (longer) forms replaced before any shorter substring that is also a component of a compound, to avoid corrupting a shorter form nested in a longer one; two-per-file docstring/comment fragments needed individually targeted (non-replace_all) edits after the bulk pass, since a naive bare-word replace_all would have corrupted an unrelated compound (a course-code label and an all-caps acronym label both contain a same two-letter substring as a real-name fragment). Every count below was independently grep-counted against the map's SCOPE numbers before editing and matched exactly.

### scripts/build_wiki_subset.py
- Line 103: removed the A-full seed string (with its trailing comma/space), kept the "Statistics" seed on the same line.
- Line 107 (B alone): the whole line deleted.
- Seed count verified: 78 remaining (was 80).
- Residual check (grep -c -i, both 0): A-full-fragment, B-fragment.
- Changed: 1 substitution (line 103) + 1 line deletion (line 107) = 2 removed / 1 added diff lines (3 total diff lines).
- sha256: `abdd15ae…` (was `42f937f5…`).

### tests/unit/test_sep12_search_identity_scope.py
- A → synthetic (79 exact-case + 4 lowercase = 83 total, matches map A=83).
- A-frag alone → synthetic state word (10 capitalized + 3 lowercase = 13, matches map).
- R → synthetic city (8, matches map "R on 8 lines").
- C → the mapping's synthetic form (6 exact-case + 2 lowercase = 8, matches map C=8).
- Lines 141/166-equivalent: A's lowercase second token in the two absence-asserts, replaced (2).
- One extra label fragment found by verification (a bare capitalized second-token word inside a docstring at the map's line ~273, "amputate '<A>' into '<2nd-token>'") — not separately itemized in the map's count table but carries a label form, so fixed too (1 targeted edit).
- Residual check: 0 hits for every real form (case-insensitive) after edits.
- Structural sanity preserved: the unrelated Springfield/Illinois/IL location-backstop fixture is untouched (25/31/13 occurrences unchanged).
- Changed lines: 92 (diff, 1:1 substitution) — matches map estimate (92) exactly.
- sha256: `56b7fd4d…` (was `b7f26e6a…`).

### tests/unit/test_sep12_followup_identity_names.py
- A → synthetic (3, matches map).
- A-frag + R → synthetic (combined "<R>, <A-frag>" string, 1 occurrence each, matches map).
- Residual check: 0.
- Changed lines: 3 — matches map estimate exactly.
- sha256: `679de762…` (was `97049059…`).

### tests/unit/test_institution_resolver.py
- A-full → the mapping's synthetic full-name form (1, matches map).
- A → synthetic (31, matches map).
- B → the mapping's synthetic form (2, matches map).
- C → the mapping's synthetic form (4: 3 exact-case compound + 1 possessive-shorthand in a docstring, matches map).
- R → synthetic city (3, matches map).
- D (all-caps acronym) → synthetic acronym (1, matches map).
- E ("St." college) → synthetic (1, matches map).
- I (generic "New … College" form) → left UNCHANGED per mapping instruction (2 occurrences; the brief says keep a generic form).
- Residual check: 0 for every real form.
- Changed lines: 40 — within map estimate range (36–42).
- sha256: `62a4ba31…` (was `4c7a7be2…`).

### tests/unit/test_private_sphere_search_guard.py
- A → synthetic (11, matches map).
- A-short: the list variable (10 uses: 1 definition + 9 call-sites) renamed to `SCHOOL_ANCHORS`; the separate line-6 prose abbreviation (1) replaced with the mapping's short form "XW". Total A-short occurrences handled: 11, matches map.
- V (course code) → "ABC 1234" (2, matches map). Verified the two-letter acronym embedded inside the course-code's OLD prefix was not itself present as the free-standing A-short token (the two are visually similar three/two-letter fragments but textually disjoint substrings, confirmed by separate grep counts before editing, so the targeted-context edits did not cross-contaminate).
- Residual check: 0 for every real form, 0 remaining bare A-short token, 0 remaining old course-code fragment.
- Changed lines: 17 — matches map estimate exactly.
- sha256: `549e37c0…` (was `4cc2a3b3…`).

### tests/unit/test_personal_anchors.py
- A → synthetic (7, matches map).
- D (all-caps acronym) → synthetic (1, matches map).
- F (the mapping's big-city form) → its synthetic replacement (1, matches map).
- G (two-word person-named university) → synthetic two-word place-named university (1, matches map).
- Residual check: 0 for every real form.
- Changed lines: 10 — within map estimate range (7–10).
- sha256: `c84dba34…` (was `10d4ddf8…`).

**Total changed lines across the batch: 92 + 3 + 40 + 17 + 10 + 3(build_wiki_subset diff lines, 2 source lines touched) = 165 diff lines** (well under the ≤300 target and the 450 hard cap).

## Privacy self-check

`date`: Tue Sep 15 12:24:49 AM CDT 2026

Method (the parent's, exactly as specified): for each of the six files, built the set of capitalised tokens present ONLY on removed lines of `diff $S/h03b1_pre/<file> <file>` (excluded if also present on an added line, via `comm -23` on sorted token lists from process substitution — the token set itself was never printed, echoed, or written to any file; only counts and, where a hit needed explaining, the already-known label were used). Unioned across the six files (16 distinct tokens), then `grep -c -F -f -` (process substitution) against the packet and each of the six files.

- **Packet (`docs/execution/generalization/batches/H03b-1.md`): 0 hits.** (An initial draft of this section had 5 hits, all from generic institutional vocabulary words used in prose while describing the mapping shape, not any real name; reworded to name only labels and drop the illustrative generic words — see below.)
- **Six edited files: non-zero hits in 4 of 6, all explained as generic-word or pre-existing-unrelated-content collisions, per file:line:**
  - `scripts/build_wiki_subset.py` — 2 tokens (5 occurrences): the `geography_places` seed list (lines 208, 223–224, 228), which the map marked explicitly out of scope. These are real, public city-name seeds unrelated to School A's identity, on lines never touched by this batch's edit (which only touched lines 103 and 107 in the `education_statistics` seeds).
  - `tests/unit/test_sep12_search_identity_scope.py` — 2 tokens (7 occurrences): (a) a 3-letter token is a substring inside a pre-existing, untouched rate-limiter identifier at line 94 (unrelated code); (b) a common institutional-naming word (6) is simply part of this batch's own chosen synthetic replacement for label C, which happens to share that word with a real name removed elsewhere in the batch — not a leak.
  - `tests/unit/test_sep12_followup_identity_names.py` — 1 token (3 occurrences): the pre-existing, untouched `PROPER_NAME_TERMS`/`INJECTED_LOCATION_TERMS` fixture at lines 26 and 66–67 (a named-source city+newspaper example, unrelated to School A, never edited by this batch).
  - `tests/unit/test_institution_resolver.py` — 2 tokens (7 occurrences): both are common institutional-naming words that are simply part of this batch's own synthetic replacements for A-full/B/C — not a leak.
  - `tests/unit/test_private_sphere_search_guard.py` — 0 hits.
  - `tests/unit/test_personal_anchors.py` — 2 tokens (4 occurrences): (a) A's second token is a substring of a pre-existing, untouched fixture org-name value at lines 82, 91, 174 (generic/fictional, unrelated to School A); (b) a common institutional-naming word (1) is part of this batch's own synthetic replacement for label F.
- No hit in any file traced to an actual leaked real name, abbreviation, fragment, domain, snake_case form, or School A's real state word. Every hit is either (i) pre-existing, untouched, unrelated content the map already flagged as out of scope or as a separate fixture, or (ii) a generic institutional word that is part of this batch's own chosen synthetic replacement and also happens to appear in a real name removed elsewhere in the batch.

Token self-check result: **packet hits = 0** (hard requirement met). Six-file hits are all explained above per file:line and label as generic-word or pre-existing-unrelated-content, per CONTRACT §3.

(Note: two intermediate drafts of this very section itself briefly reintroduced generic-word hits into the packet while explaining the six-file hits — common institutional-naming-word prose, and a rate-limiter identifier name containing a 3-letter label as a substring; both were caught by rerunning the self-check and reworded to avoid the literal words, with repeated fresh reruns after every subsequent edit confirming 0.)

## Post-edit test run

`date`: Tue Sep 15 12:30:12 AM CDT 2026 (guard clear, MemAvailable 6978 MB)

Same command as baseline, digests printed in the same command (all six match the post-edit sha256 values recorded above). Exit status 0. **156 passed, 0 failed, 0 errors — identical to baseline in total and per-file.**

| File | Baseline passed | Post-edit passed |
|---|---|---|
| test_sep12_search_identity_scope.py | 56 | 56 |
| test_sep12_followup_identity_names.py | 28 | 28 |
| test_institution_resolver.py | 49 | 49 |
| test_private_sphere_search_guard.py | 16 | 16 |
| test_personal_anchors.py | 7 | 7 |
| **Total** | **156** | **156** |

Elapsed 0:05.38, Maximum RSS ≈1.07 GB.

`date`: Tue Sep 15 12:30:33 AM CDT 2026 — data/ listing diffed against baseline: 0 lines (unchanged). `logs/`: absent (unchanged).

## Sweep

`date`: Tue Sep 15 12:31:07 AM CDT 2026

Importer greps (per IMPORTER GREPS, run over tests/unit, `--exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=node_modules`):
- `grep -rlE 'utils\.institution_resolver|from utils import (.*[ ,])?institution_resolver\b|institution_resolver\.[A-Za-z_]+' tests/unit`
- `grep -rlE 'utils\.location_resolver|from utils import (.*[ ,])?location_resolver\b|location_resolver\.[A-Za-z_]+' tests/unit`
- `grep -rlE 'utils\.web_search_trigger|from utils import (.*[ ,])?web_search_trigger\b|web_search_trigger\.[A-Za-z_]+' tests/unit`
- `grep -rlE 'core\.agentic\.gate|from core\.agentic import (.*[ ,])?gate\b|agentic\.gate\.[A-Za-z_]+' tests/unit`
- `grep -rlE 'test_sep12_search_identity_scope|test_sep12_followup_identity_names' tests/unit` (importers of the two edited test modules, for the shared `classify`/`decompose` helpers)

Union: 65 files. Minus the 5 already baseline/post-edit-tested unit files: 60 files remain. Hazard-vetted (`grep -lE 'ContextGatherer\(|MemoryConsolidator\(|DailyNotesGenerator\(|UserProfile\(\)'`): 2 hits, both confirmed hermetic before running —
- `tests/unit/test_profile_path_authority.py`: every scenario uses `tmp_path`/monkeypatched defaults (its own docstring states this and that all its fixture names are already synthetic); the bare `UserProfile()` calls occur only after `monkeypatch.chdir(tmp_path)` and/or an explicit `DEFAULT_PATH` patch.
- `tests/unit/test_sep05_evening_turn_audit.py`: `MemoryConsolidator(mm, user_profile=profile)` passes an explicit `MagicMock()` profile — not the lazy-real-`UserProfile()` hazard.

Split into 7 chunks (≤9 files each), run foreground, one at a time, guard + MemAvailable check before each, data/logs listing after each (all 0-diff / absent):

| Chunk | Files | Result | Elapsed |
|---|---|---|---|
| 0 | 9 | 231 passed, 1 skipped | 0:07.62 (`date` 12:32:21) |
| 1 | 9 | 191 passed | 28.74s (`date` 12:32:40) |
| 2 | 9 | 167 passed | 4.24s (`date` 12:33:23) |
| 3 | 9 | 330 passed | 10.90s (`date` 12:33:40) |
| 4 | 9 | 354 passed | 19.72s (`date` 12:34:04) |
| 5 | 9 | 589 passed | 14.87s (`date` 12:34:38) |
| 6 | 6 | 109 passed | 4.77s (`date` 12:35:06) |

Total: 60 files, **1971 passed, 1 skipped, 0 failed, 0 errors**. All exit codes 0. data/ unchanged and logs/ absent after every chunk.

## ruff

`date`: Tue Sep 15 12:35:40 AM CDT 2026

`python -m ruff --version`: ruff 0.14.9. `python -m ruff check` on the six changed files: **All checks passed!** (exit 0).

## Post-edit scan

`date`: Tue Sep 15 12:35:44 AM CDT 2026

`python scripts/check_bug_classes.py scan --root .` (read-only): exit=1 (same designed-handoff STALE-entries reason as pre-edit; unrelated to this batch).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm16_config_key_reachability | report | 395 | 89 | - | - | - |
| dm17_apply_without_guard | gate | 648 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | 23 | 79 | 0 | 56 |

**Identical to the pre-edit scan table in every column.** No new findings introduced by this batch.

## Final web / data / logs / git-status check

`date`: Tue Sep 15 12:35:54 AM CDT 2026

- `git status --short web` diffed against the F13c-2a baseline: 0 lines (unchanged; this batch touched no `web/` path).
- data/ listing diffed against baseline: 0 lines (unchanged). `logs/`: absent.
- `git diff --stat HEAD` restricted to the six assigned files: exactly those six, 163 insertions / 164 deletions, 6 files changed — no other tracked file touched.
- Repo-wide `git status --short` shows only this batch's six tracked-file edits as `M`, plus this new packet and the pre-existing untracked files from earlier integrated/in-flight batches (unrelated to this batch, not created or touched here).

## BC-58 sibling search

The map's EXTRA-files and spread-out-of-scope sections (test_ingest_turn_misfires.py, test_email_passive_context.py, test_pattern_engine.py, test_retrospective_small_guards.py, test_sep05_evening_turn_audit.py, and the T/V/L spread across many more files, plus a handful of production-comment sites) were identified by the parent's map as carrying the SAME label forms this batch fixes, but are explicitly assigned to H03b-2 or an owner question, not this batch. This batch's OWNERSHIP section restricts it to exactly the six named files; no sibling site was edited here. The sweep (60 files) ran several of those extra-listed files as importers/consumers (e.g. test_ingest_turn_misfires.py, test_music_session_misfires.py, test_sep05_evening_turn_audit.py) and confirmed they still pass unmodified — their own label content is untouched and out of this batch's scope.

## Privacy / no-network note

No network access was used or required. No LLM or paid API call was made. No `pip`/`npm` command was run. All test runs used `DAEMON_TEST_MODE=1`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1` and offline fakes/mocks per the deployed test fixtures — no live model, search, or calendar call occurred. The privacy self-check (token-set method) confirms 0 hits in this packet; the six edited files' residual hits are all pre-existing unrelated content or generic institutional-naming words, explained above per file:line.

## Milestone

**Focused contract green.** All four CONTRACT items met: (1) every label occurrence in the five test files replaced per mapping, structure preserved, identical per-file pass counts before/after; (2) build_wiki_subset.py seeds reduced 80→78 exactly as specified, no other change; (3) privacy verification complete (six-file residual real-form checks = 0 throughout editing; packet token self-check = 0); (4) no real name written to any file created or edited, or to this report.

## Deviations / escalations / limitations

None. No rule breach occurred. The only notable event was catching and correcting three self-inflicted false-positive privacy-self-check hits inside this very packet's own explanatory prose (generic institutional words and one rate-limiter identifier name), each caught immediately by rerunning the mechanical self-check after every packet edit and reworded before proceeding — recorded above in the Privacy self-check section for transparency. Final state: 0 hits, verified by a fresh rerun after every subsequent packet edit including this one.

## Parent review and integration (2026-09-15)

Accepted. There is no behaviour change, and no data/, web/ or privacy issue. Privacy was verified independently by the parent with a broader counts-only token set (below).

### Process findings (parent)

1. **ORDER verified from mtimes and packet dates.**
   - Baseline run `date` 00:15:26, with the six unedited digests (156 passed).
   - Edits: `tests/unit/test_sep12_search_identity_scope.py` 00:19:18, `test_sep12_followup_identity_names.py` 00:19:45, `test_institution_resolver.py` 00:21:00, `test_private_sphere_search_guard.py` 00:22:07, `test_personal_anchors.py` 00:22:30, `scripts/build_wiki_subset.py` 00:22:46.
   - Privacy self-check 00:24:49; post-edit run 00:30:12; sweep 00:31:07–00:35:06; packet last written 00:36:26.
2. **Typed timestamp again (minor; same slip as H03).** The "Edits" header reads "`date`: Tue Sep 15 12:20:00 AM CDT 2026 (edits performed across the six files …)". That is not a `date` output; the real edit mtimes are 00:19:18–00:22:46. The H03b-2 brief now spells out how to stamp the edits section.
3. **Self-corrected packet self-hits.** The worker's own token self-check caught generic-word hits it had introduced into this packet's explanatory prose three times, and reworded each time. Final packet hits: 0.

### Parent privacy verification (counts only; no name printed or written)

- **Token set:** 12 capitalised tokens (regex `\b[A-Z][A-Za-z.-]{2,}\b`) and 3 lowercase tokens found ONLY on removed lines of `diff $S/h03b1_pre/<file> <file>`.
- **In the six edited files** (masked-line review):
  - `scripts/build_wiki_subset.py:208/223/224/228`: pre-existing `geography_places` seeds and their comment (public place names, out of scope per the map, untouched);
  - `tests/unit/test_sep12_followup_identity_names.py:26/66/67`: a pre-existing public-newspaper fixture (untouched);
  - `tests/unit/test_personal_anchors.py:82/91/174`: the pre-existing synthetic "Local … Meetup" placeholder (untouched);
  - the other three files: 0.
  - Lowercase tokens in the six files: 0.
- **In `docs/execution/generalization/**/*.md`:**
  - `batches/A03b-1.md` and `batches/A03b-2.md`: a generic city word inside an `America/<city>` timezone ID and "timezone pin" prose (pre-existing A03b evidence);
  - `batches/H03.md` and `briefs/H03.md`: the generic lowercase word in "a large <word> company".
  - No real school, person, domain or School A state word in any lane doc, this packet included.
- **Synthetic names present as mapped** (case-sensitive counts, parent):
  - "Vermont Wrenfield": scope 78, followup 3, institution 31, private-sphere 11, anchors 7; "vermont wrenfield": scope 4.
  - "Marrowby": 8/1/3 (scope/followup/institution).
  - "Quellmoor University": 6/3 (scope/institution); "University of Tarnwick-Hollow": 2; "Wrenfield Institute of Science": 1; "St. Veldmoor College": 1.
  - "XQT": 1 institution + 1 anchors; "University of Dunmere": 1; "Brightwater Kestrel": 1.
  - `SCHOOL_ANCHORS`: 10; "ABC 1234": 2.
- **Education seeds:** the education_statistics seed list (lines 102–142) counts 80 in the pre-edit copy and 78 live. Line 103 still carries the "Statistics" seed.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_H03.txt` shows 0 mismatches. All six files were unmodified tracked files, so they appear as new paths.
- **New paths (seven):**
  - `scripts/build_wiki_subset.py` (`42f937f5…` → `abdd15ae…`, 784 → 783 lines; diff <2 >1);
  - `tests/unit/test_sep12_search_identity_scope.py` (`b7f26e6a…` → `56b7fd4d…`; <92 >92);
  - `tests/unit/test_sep12_followup_identity_names.py` (`97049059…` → `679de762…`; <3 >3);
  - `tests/unit/test_institution_resolver.py` (`4c7a7be2…` → `62a4ba31…`; <40 >40);
  - `tests/unit/test_private_sphere_search_guard.py` (`4cc2a3b3…` → `549e37c0…`; <17 >17);
  - `tests/unit/test_personal_anchors.py` (`10d4ddf8…` → `c84dba34…`; <10 >10);
  - this file.
- **Pre-edit copies:** `$S/h03b1_pre/` match the brief's digests.
- **Web and git state:** `git status --short web` equals `$S/web_status_post_F13c-2a.txt`. HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`. `logs/` is absent.
- **Parent edits after the worker returned:** `briefs/PARENT_STATE.md` and `briefs/H03b-2.md` (new).
- **Recorded tree:** `manifest_post_H03b-1.txt` records the tree after this section.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed after every chunk)

- **Focused** (MemAvailable 6,892 MB): `test_sep12_search_identity_scope`, `test_sep12_followup_identity_names`, `test_institution_resolver`, `test_private_sphere_search_guard` and `test_personal_anchors` gave **156 passed** (5.25 s, 1,073,384 KB). This matches the baseline and the worker's per-file 56/28/49/16/7.
- **Sweep:**
  - The IMPORTER GREPS union for institution_resolver, location_resolver, web_search_trigger, gate and the two edited test modules = 65 files; minus the 5 focused = 60.
  - Seven chunks: 231 passed + 1 skipped, 191, 167, 330, 354, 589, 109.
  - That is **1,971 passed, 1 skipped**. Every chunk ran with 6,894–6,957 MB available and peaked at ≤1,740,248 KB.
- **Total:** 2,127 passed, 1 skipped, 0 failed, matching the worker. data/ was unchanged after every chunk, `logs/` is absent, and web/ is unchanged.
- **ruff:** clean on all six files.
- **Scan:** byte-identical to `$S/scan_parent_H03.txt` (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648). Scan stderr is empty.

### Size

165 diff lines (92 + 3 + 40 + 17 + 10 + 3), under the 300 target.

### Limitations carried forward

- **Next batch:** H03b-2 covers the remaining 12 unit files plus tests/test_citation_system.py.
- **Owner question pending:** names outside the H03b files, including R in the geography seeds edited around here, and `docs/GENERALIZATION_AUDIT_20260901.md`.

