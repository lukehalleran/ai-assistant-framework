# Batch R03 — response to class-guard request CGR-20260913-006 (BC-01)

Status: **focused contract green** (deterministic unit-test evidence only;
not a synthetic assembled probe, owner canary, or population claim).

## Scope

This worker owns batch R03 only: the response to class-guard request
`CGR-20260913-006` (scanner `dm01_raw_substring`, BC-01 —
`GitMemoryExtractor._extract_tags` tags `work-in-progress`,
`breaking-change`, and `hotfix` by bare substring membership against the
lowered commit subject). Owned files: `knowledge/git_memory.py`
(`_extract_tags` plus one module-level compiled matcher and its import
only), new `tests/unit/test_git_memory_tags.py`, new
`docs/execution/generalization/class_guard_responses/CGR-20260913-006.md`,
and this file. Three anchors: review #15 (line 364, `"wip"`), #16 (line 366,
`"breaking"`), #17 (line 368, `"hotfix"`).

## Base and manifest

- Base HEAD (unchanged throughout): `328a8ecea1dae91de16f5974ea71727c4e2e1aa0`.
- `git status --short` at start matched the parent's manifest exactly:
  A01/A02/A03a, R01, and R02 integrated-uncommitted files present as listed;
  `knowledge/git_memory.py` and `tests/unit/test_git_memory_tags.py` absent
  from status (pristine / not yet created).
- `git remote -v`: `origin` fetch = `/home/lukeh/Daemon_v1`, push =
  `DISABLED` (verified read-only; never touched, no remote operation
  attempted).
- No `git stash`/`reset`/`clean`/`checkout`/`restore` was run. No commit or
  push was made.
- Environment sanity check: `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin
  python -c "import utils.trigger_match as t; print(t.__file__)"` →
  `/home/lukeh/daemon_exec/generalization/utils/trigger_match.py` (this
  clone, confirmed before any test run).
- Every dirty-tree file's digest and every `docs/execution/generalization/`
  file digest were reconfirmed against the parent's ready-record manifest
  via `sha256sum -c` and matched exactly (44 files, 0 mismatches); no
  additional untracked files existed beyond the manifest.
- `config/bug_class_baseline.json` (read-only) already carries the three
  anchors as pre-existing `dm01_raw_substring` entries on
  `knowledge/git_memory.py` / `GitMemoryExtractor._extract_tags` matching
  the request packet's three literals exactly.

### Assigned-file digests (pre-edit, matched the request's recorded source SHA-256 exactly)

```
e9d3ddbfa95a6bbbb35ff2ec89417e170be2d69367b52110014f8caaf3e93945  knowledge/git_memory.py
893f67000ff98ff0593992725f21d381f6aab9f8a8e5e73de4b74ece3d63dc1c  tests/test_git_memory.py  (READ-ONLY: run, never edited)
```

### Changed-file manifest (after)

| File | Status | SHA-256 (after) |
|---|---|---|
| `knowledge/git_memory.py` | modified (`_extract_tags` body + one import + one module-level matcher only) | `f796dd7a0bde05ad64da3ec83d966a79ac7c973d3c64a2a71d357cecbdee102c` |
| `tests/unit/test_git_memory_tags.py` | new | `d67e206195469dd9b8dbcc5568f24bf01b11fe3eb709d1bbdf259ecf4c55308e` |
| `docs/execution/generalization/class_guard_responses/CGR-20260913-006.md` | new | `3cde61c2122ad9fefd7b69eb2e14b4cb40e7c416c575ac9f194e59ef308b5c76` |
| `docs/execution/generalization/batches/R03.md` | new | see this file |

`git diff --stat` for `knowledge/git_memory.py`: **19 insertions, 3
deletions — 22 changed lines**; the new test file is 126 lines. **148
changed lines total**, well within the 450-line hard cap and the 350-line
high-risk guidance. No file outside this list was touched;
A01/A02/A03a/R01/R02's integrated-uncommitted files are unchanged
(re-confirmed by `git status --short` after all edits — identical set, only
`knowledge/git_memory.py` newly appears as modified and
`tests/unit/test_git_memory_tags.py` newly appears untracked).

## Requirement / defect ID, invariant, and why the source seam owns the fix

- Defect class: BC-01 "Bare-substring keyword matching" (`docs/BUG_CLASSES.md`).
  `_extract_tags` tested `"wip"/"breaking"/"hotfix" in subject_lower` — raw
  substring membership — so each literal fired inside an unrelated longer
  word: `'wip' ⊂ "swipe"`, `'breaking' ⊂ "groundbreaking"`, `'hotfix' ⊂
  "hotfixture"/"nonhotfix"`.
- Invariant (parent contract, `docs/PLAN_20260913_generalization_execution.md`
  routing decision recorded in the "ready record"): route the three literals
  through one compiled BC-01 chokepoint matcher
  (`utils.trigger_match.compile_keyword_matcher`); a bare word matches only
  itself or a sense-preserving inflection, bounded at both ends. Plain
  boundary matching only — no negation semantics, since tag metadata is not
  a request cue. Do not add conventional-commit `!:` breaking-marker
  semantics (new behavior, out of scope — recorded as a limitation instead).
  No phrase added or removed (BC-76): still exactly `wip`/`breaking`/
  `hotfix`.
- Why the source seam (this plan) owns the fix, not the class-guard plan:
  the class-guard plan owns scanner/detector/baseline; this plan owns the
  application code the request flags. `CGR-20260913-006` requested exactly
  this repair, naming `utils.trigger_match` as the expected remedy.

## Failing-before evidence

New parametrized tests (`tests/unit/test_git_memory_tags.py`) written first,
then run against `knowledge/git_memory.py` in its pristine pre-patch state
(digest confirmed `e9d3ddbfa95a6bbbb35ff2ec89417e170be2d69367b52110014f8caaf3e93945`
before running; the patch was applied only afterward):

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v \
env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_git_memory_tags.py tests/test_git_memory.py
```

Result: **10 failed, 47 passed**, exit 1. Elapsed 0:16.11. Maximum resident
set size: 730,028 KB. The 10 failures are exactly the containment
counterexamples for the three anchors:

- wip: `"swipe to dismiss"`, `"wipe the board"`, `"unwip"`,
  `"swipe\n  to dismiss"` (wrapped/indented) — each wrongly returned
  `work-in-progress`.
- breaking: `"groundbreaking results"`, `"unbreaking"`,
  `"groundbreaking\n  results"` (wrapped/indented) — each wrongly returned
  `breaking-change`.
- hotfix: `"hotfixture"`, `"hotfixation"`, `"nonhotfix"` — each wrongly
  returned `hotfix`.

This demonstrates the substring-containment defect on the deployed
`_extract_tags`, in this clone, before any patch.

## Contract implemented

`knowledge/git_memory.py` — one module-level compiled matcher plus its
import (per file ownership), routing all three literals through the BC-01
chokepoint:

```python
from utils.trigger_match import compile_keyword_matcher
...
_TAG_KEYWORD_MATCHER = compile_keyword_matcher(["wip", "breaking", "hotfix"])
```

`_extract_tags` (only the three tag lines changed):

```python
tag_hits = {hit.keyword for hit in _TAG_KEYWORD_MATCHER.iter_hits(subject_lower)}
if "wip" in tag_hits:
    tags.append("work-in-progress")
if "breaking" in tag_hits:
    tags.append("breaking-change")
if "hotfix" in tag_hits:
    tags.append("hotfix")
```

`iter_hits` (not `find_hits`/`has_non_negated_hit`) is used deliberately:
the boundary-matching half of the chokepoint applies, but not the
negation-lookback half — per the parent's "tag metadata is not a request
cue" instruction. No `!:` conventional-commit marker parsing was added.

## Commands and results

Guard check before every pytest run (never printed RUNNING):
```
for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm)" in python*) echo RUNNING;; esac; done
```

`ls -A data` before the first run and after the last run — both identical,
no new entries, nothing deleted:
```
benchmark_per_case.csv
chroma_db_v4
embedding_migration_manifest.json
pipeline
```

1. Failing-before (pristine `knowledge/git_memory.py`): **10 failed, 47
   passed**, exit 1. Elapsed 0:16.11. Max RSS 730,028 KB.
2. Patched, focused run: **57 passed**, exit 0. Elapsed 0:02.28. Max RSS
   765,112 KB.
3. `ruff --version`: `ruff 0.14.9`. `python -m ruff check
   knowledge/git_memory.py tests/unit/test_git_memory_tags.py` → **All
   checks passed!**
4. Bug-class scan, read-only, before editing (pristine `knowledge/git_memory.py`):
   ```
   scanner                       mode    files  found  base  new  stale
   dm01_raw_substring            gate    394    12     12    0    0
   dm17_apply_without_guard      gate    597    36     39    0    3
   dm18_except_returns_empty     gate    123    79     79    0    0
   dm31_live_state_default       gate    394    3      3     0    0
   catalog                       gate    1      0      0     0    0
   ```
   Exit **1** — the 3 `dm17` STALE rows are R01's pre-existing (uncommitted,
   already-integrated) handoff state in this checkout, unrelated to this
   batch.

   Bug-class scan, read-only, before handoff (patched tree, final state):
   ```
   scanner                       mode    files  found  base  new  stale
   dm01_raw_substring            gate    394    9      12    0    3
   dm17_apply_without_guard      gate    598    36     39    0    3
   dm18_except_returns_empty     gate    123    79     79    0    0
   dm31_live_state_default       gate    394    3      3     0    0
   catalog                       gate    1      0      0     0    0
   ```
   Exit **1** — `dm01` transitions exactly as expected: `found` 12 → 9
   (three fewer live findings), `new` stays 0, `stale` rises 0 → 3, and the
   three new STALE rows are precisely the three anchors (`knowledge/git_memory.py`
   `if "wip"/"breaking"/"hotfix" in subject_lower:`). The pre-existing R01
   `dm17` STALE rows are untouched (still exactly 3). `dm17`'s `files` count
   rose 597 → 598 only because its file walk also now sees the new test
   file; it produced no new finding. **`--write-baseline` was never
   passed.** No new finding was introduced anywhere by this batch's edits (0
   new in every gated scanner, both times).

## Counterexamples, BC-58 sibling search, exemptions

- Counterexamples (containment must not tag): `"swipe to dismiss"`,
  `"wipe the board"`, `"unwip"`; `"groundbreaking results"`, `"unbreaking"`;
  `"hotfixture"`, `"hotfixation"`, `"nonhotfix"` — all proven via the
  deployed `_extract_tags`, in clean and wrapped/indented forms.
- Positive controls: `"WIP: partial refactor"` / `"wip: quick patch"` /
  `"a wip feature"`; `"breaking change to the parser"` /
  `"feat!: BREAKING change to API"` (pre-existing `tests/test_git_memory.py`
  control); `"hotfix for login"` / `"a hotfix"` / `"hotfixes"`
  (sense-preserving inflection, verified against the deployed
  `utils.trigger_match` chokepoint before writing the fixture) — all still
  tag, clean and wrapped/indented.
- Contract control: `"do not merge, this is not wip"` still tags
  `work-in-progress` — no negation semantics, tag metadata is not a request
  cue.
- BC-58 sibling search:
  `grep -rnE '"[a-z]{3,}" in (subject|message|msg|title|line)_?lower' --include=*.py knowledge utils core memory`
  → one sibling outside this batch's ownership: `utils/tone_detector.py:1023`,
  `if "overwhelmed" in message_lower:`. Reported only, not fixed, per file
  ownership; the class-guard owner may want a separate request for it.
- Exemption: none. All three anchors were judged real BC-01 instances and
  fixed; no site in this batch's scope was left unfixed.

## Privacy scan result / no live store, network credential, or provider

- The new tests are pure in-process unit tests against literal subject
  strings; `GitMemoryExtractor(repo_path=str(tmp_path))` is constructed but
  `_extract_tags` never touches the filesystem or shells out to git
  (`__init__` only stores `repo_path`).
- `tests/test_git_memory.py` (run, not edited) does shell out to `git log` /
  `git rev-list` / `git show --numstat` against this checkout's own history
  via its default-`repo_path` tests — read-only, no write/mutating git
  command, consistent with the module's documented read-only side effects.
- `ls -A data` unchanged before and after every command in this batch (see
  above). `DAEMON_TEST_MODE=1`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`
  were set on every pytest invocation; no network call was made or
  attempted.
- No live Daemon process was started, stopped, or otherwise exercised.

## Remaining milestone state

**Focused contract green** for anchors #15, #16, and #17. Not a synthetic
assembled probe, owner canary, or E3/E4/E5 evidence. The class-guard request
stays open per its own contract until the class-guard owner integrates this
tree, reruns `python scripts/check_bug_classes.py scan --root .`, and
confirms the three anchors show as STALE there too (already demonstrated in
this checkout above), then marks their baseline records `confirmed_fixed`
with this request ID.

## Open decisions for the parent / class-guard owner

1. `utils/tone_detector.py:1023` (`"overwhelmed" in message_lower`) is a
   genuine BC-01-shaped sibling outside this request's file ownership,
   reported only. The class-guard owner may want a separate request for it.
2. The limitation that no new `!:` conventional-commit breaking-marker
   semantics was added is a deliberate scope boundary per the parent's
   contract, not an oversight — flagged here in case a future request wants
   that as new, separately-reviewed behavior.

## Limitations

- Deterministic unit-test evidence only; no synthetic assembled probe, owner
  canary, or population claim.
- No new conventional-commit `!:` breaking-marker semantics was added
  (deliberate, per contract) — a subject like `"feat!: refactor config"`
  with no literal "breaking" word still gets no `breaking-change` tag.
- Plain boundary matching only; `_extract_tags` still applies no negation
  semantics by design (tag metadata is not a request cue).
- `utils.trigger_match`'s bare-word morphology bound is a hand-written
  approximation, not a stemmer or lexicon (BC-01 catalog status: "partial");
  an inflection outside its declared suffix list would not tag. None was
  found for `wip`/`breaking`/`hotfix` during this review.
- `utils/tone_detector.py:1023` received no source change and no
  correctness judgment — reported only, per file ownership.
- `data/` was unchanged before and after every command in this batch; no
  live store, network credential, or provider was used.

## Parent review and integration (parent session)

### Review

- **Diff.** One module-level `compile_keyword_matcher(["wip", "breaking", "hotfix"])`. `_extract_tags` tests set membership over `iter_hits` keywords. No negation and no vocabulary change, per the contract.
- **Tests.** They drive the deployed `_extract_tags` (the constructor runs no git) with:
  - containment counterexamples for all three anchors, in clean and wrapped/indented forms;
  - positive controls, including the `hotfixes` inflection and the existing `feat!: BREAKING` control;
  - a no-negation characterization.
- **Failing-before.** 10 failed / 47 passed. The 10 failures are exactly the containment cases.
- **Sibling.** `utils/tone_detector.py:1023` is already anchor #30 of CGR-20260913-003 (queued batch R05); no new signal is needed.
- **Response file.** It carries every required field.

### Parent rerun on the integrated candidate

Run with `PYTHONPATH` set to this clone's `scripts/bin`, under `MemoryMax=6G`, with no other pytest process running.

- `tests/unit/test_git_memory_tags.py` + `tests/test_git_memory.py`: **57 passed, 0 failed**, exit 0; wall 0:14.70; peak RSS 729,264 KB.
- `python -m ruff check` (0.14.9) on both Python files: all passed.
- Bug-class scan (base scanner):
  - `dm01_raw_substring` 9 found / 12 base / **0 new / 3 stale** (anchors #15–#17, the designed handoff);
  - `dm17` still 3 stale (R01);
  - every other gate 0/0. Exit 1.
- `ls -A data` before and after: unchanged.

### Landing constraint

With R03 in the tree, the bug-class gate exits non-zero until the class-guard owner removes the three `dm01` baseline rows for anchors #15–#17. **R03 must land together with that baseline update** (as R01 must with its three `dm17` rows).

### Final R03 manifest (sha256)

```
f796dd7a0bde05ad64da3ec83d966a79ac7c973d3c64a2a71d357cecbdee102c  knowledge/git_memory.py
d67e206195469dd9b8dbcc5568f24bf01b11fe3eb709d1bbdf259ecf4c55308e  tests/unit/test_git_memory_tags.py
3cde61c2122ad9fefd7b69eb2e14b4cb40e7c416c575ac9f194e59ef308b5c76  docs/execution/generalization/class_guard_responses/CGR-20260913-006.md
```

### Integration state

- Integrated into the uncommitted candidate tree on base `328a8ec` (no commit, push, or restart).
- Milestone: **focused contract green** for CGR-20260913-006 anchors #15–#17.
