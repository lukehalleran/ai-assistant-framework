# F12d-2 — the shutdown synthesis audit check fails closed

Split off F12d on SIZE (batches/F12d.md "SIZE checkpoint"). Owner decision
2026-09-14, item 2 (briefs/PARENT_STATE.md "Owner decisions received
2026-09-14"): `memory/shutdown_processor.py`'s `_run_synthesis_dreaming`
audit-check block becomes fail-closed. No request packet; no response file
(CGR-009 is already fully answered by F12d). BUG_CLASSES: BC-20, BC-47,
CM-05.

## 2026-09-14 21:15:45 CDT — pre-edit status, manifest, digests

```
$ date
Mon Sep 14 09:15:45 PM CDT 2026
$ git status --short   # 82 M + 55 ?? (excluding docs/execution/generalization/, which is one untracked-dir line)
$ git rev-parse HEAD
328a8ecea1dae91de16f5974ea71727c4e2e1aa0
$ git diff --stat   # 82 files changed, 4070 insertions(+), 820 deletions(-)
$ git remote -v
origin  /home/lukeh/Daemon_v1 (fetch)
origin  DISABLED (push)
```

HEAD matches the launch fact (328a8ec). origin push URL DISABLED, confirmed
read-only (never touched, never will be).

```
$ date
Mon Sep 14 09:16:03 PM CDT 2026
$ sha256sum -c $S/manifest_post_F12d.txt 2>&1 | grep -v ': OK$' | wc -l
0
$ sha256sum -c $S/manifest_post_F12d.txt 2>&1 | grep -c ': OK$'
279
$ date
Mon Sep 14 09:16:09 PM CDT 2026
$ git ls-files -co --exclude-standard | grep -v -e '^data/' -e '^\.agent_snapshots/' | sort > $S/f12d2_current_paths.txt
$ wc -l $S/f12d2_current_paths.txt   # 1311 (every tracked+untracked file, not the changed set)
```

That first path-set attempt used the wrong basis (all repo files, not the
changed set) and correctly failed to match the 279-line manifest — no
mismatch was reported against the manifest's actual authoring method; this
was caught before drawing any conclusion, not a breach (read-only `git
ls-files`/`sort`/`diff`, no source touched). Re-derived the same way as
`git status --short --untracked-files=all` (which expands untracked
directories, unlike `git status --short`'s single collapsed-directory line
used above for the git-status readout):

```
$ date
Mon Sep 14 09:16:44 PM CDT 2026
$ git status --short --untracked-files=all | sed 's/^...//' | sort > $S/f12d2_current_paths.txt
$ sort $S/manifest_paths_post_F12d.txt -o $S/f12d2_manifest_paths_sorted.txt
$ diff $S/f12d2_current_paths.txt $S/f12d2_manifest_paths_sorted.txt && echo "PATH SETS MATCH EXACTLY"
PATH SETS MATCH EXACTLY
$ wc -l $S/f12d2_current_paths.txt $S/f12d2_manifest_paths_sorted.txt
279 / 279
```

279/279 files pass `sha256sum -c` with 0 mismatches. Path set (279 entries)
matches `$S/manifest_paths_post_F12d.txt` exactly. **Manifest check
PASSES.**

```
$ date
Mon Sep 14 09:16:49 PM CDT 2026
$ cat $S/data_baseline_post_F12a.txt
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-14 19:30:21.572476379 -0500 .
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
$ ls -la --time-style=full-iso data
(identical: same 4 entries, sizes, mtimes)
$ ls -ld --time-style=full-iso logs
ls: cannot access 'logs': No such file or directory
$ env | grep PYTHONPATH   # (the shell's own, points at Daemon_v1 -- must be overridden per-command)
PYTHONPATH=/home/lukeh/Daemon_v1/scripts/bin
$ sha256sum memory/shutdown_processor.py memory/synthesis_memory.py utils/retrieval_outcome.py tests/unit/test_synthesis_read_outcomes.py tests/unit/test_synthesis_audit.py
9aa08ee56f13dcb753861ba25153a60aa48817df844928f81acd2360292bae52  memory/shutdown_processor.py
1419e20e17be9458fa9ce7dfa3e5a9f3bb93f0827f227b40833ac07ad618f29a  memory/synthesis_memory.py
1abc1586534171b05e3a06fd976d08eba76cb6ecdfef565944f4dcdebf4c95eb  utils/retrieval_outcome.py
549e5546d9cb67eb8ed0661ea0d68fe3bd70a40e4c0c00c1f254ff2857473ead  tests/unit/test_synthesis_read_outcomes.py
d25b3d549f7a9c7c7705f934c34f5e9651469e1d0aeedfa56fb0bfd5aa0a6abc  tests/unit/test_synthesis_audit.py
```

data/ matches `$S/data_baseline_post_F12a.txt` exactly (same 4 entries,
sizes and mtimes). logs/ absent as expected. All five digests match the
brief's PARENT-VERIFIED FACTS exactly (unedited sources).

```
$ date
Mon Sep 14 09:16:54 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -c "import utils; print(utils.__file__)"
/home/lukeh/daemon_exec/generalization/utils/__init__.py
```

`utils` sanity check loads from this clone, not Daemon_v1.

## 2026-09-14 21:18:06 CDT — read facts confirmed on the live tree

```
$ date
Mon Sep 14 09:18:06 PM CDT 2026
$ grep -n "^class ShutdownProcessor\|def __init__" memory/shutdown_processor.py | head -5
114:class ShutdownProcessor:
132:    def __init__(
```

Read 1660-1840 directly (the full `run_synthesis_dreaming` /
`_run_synthesis_dreaming` region plus a few lines of context each side).
Confirmed line-for-line against the brief's PARENT-VERIFIED FACTS:
- public wrapper `run_synthesis_dreaming` (def 1668) → `await
  self._run_synthesis_dreaming()` (1676). Docstring already notes
  self-gating.
- `_run_synthesis_dreaming` (def 1678): outer `try:` (1688); flags imported
  from `config.app_config` inline (1689-1700); early return (1704-1705) if
  neither `SYNTHESIS_GENERATOR_ENABLED` nor `SYNTHESIS_POOLED_ENABLED`;
  `if SYNTHESIS_AUDIT_ENABLED:` (1708) wraps `from memory.synthesis_memory
  import SynthesisMemory` (1710) / `audit_mem = SynthesisMemory(self.chroma_store)`
  (1711) / `audit_stats = audit_mem.get_audit_stats()` (1712) in an inner
  `try:` (1709); `if audit_stats.get("auto_halt", False):` (1713) logs the
  HALTED warning (1714-1722) and `return`s (1723); `except Exception as e:`
  (1724) currently does `logger.debug(f"[Shutdown] Audit check skipped:
  {e}")` (1725) — FAIL-OPEN today, dreaming proceeds. Then `from
  knowledge.synthesis_filter import SynthesisFilter` (1727), `from
  memory.synthesis_memory import SynthesisMemory` (1728, re-import, same
  module), `mc = self.memory_coordinator` (1730); the pooled branch (1736,
  `if SYNTHESIS_POOLED_ENABLED:`) imports `knowledge.synthesis_pooled_generator.PooledConceptSynthesisGenerator`
  (1741) and is the ONLY branch when `SYNTHESIS_POOLED_ENABLED` is True (the
  retired retrieval/graph_walk/tier2 branch is the `else`, 1752-1808); `if
  not candidates: return` (1810-1812, before the filter is built); else
  `synthesis_memory = SynthesisMemory(self.chroma_store)` (1814) and
  `filter_pipeline = SynthesisFilter(...)` (1815-1821), `results = await
  filter_pipeline.process_batch(candidates)` (1822) — `results.get("accepted"/"rejected", 0)`
  used in the summary log (1824-1829). Outer `except Exception as e:`
  (1831) → `logger.warning("[Shutdown] Synthesis dreaming failed
  (non-fatal): %s", e)` (1832).
- No module-level `utils.retrieval_outcome` import in this file (confirmed:
  `grep -n "^import\|^from"` shows only stdlib/typing/`utils.logging_utils`
  at the top). `RetrievalError` is imported LOCALLY inside three other
  methods (1162, 1260, 1556, 1647 — the last three are `StoreWriteError`;
  1556 is `RetrievalError`, immediately before its own `try:` at 1557,
  `except RetrievalError as e:` at 1559) — this batch follows that exact
  local-import-immediately-before-the-try precedent, placed inside `if
  SYNTHESIS_AUDIT_ENABLED:` before the inner `try:`.
- `memory/synthesis_memory.py get_audit_stats` (def 508) calls
  `self.get_all_results(limit=500)` (521) with NO try; `get_all_results`
  (def 414) wraps `self.store.query_collection(...)` in `try/except
  Exception as e: logger.error(...); raise RetrievalError(source="synthesis_memory",
  reason=f"all_results:{type(e).__name__}") from e` (419-442, landed in
  F12d). So a failed read raises `RetrievalError(source="synthesis_memory",
  reason="all_results:<ExceptionClassName>")` — `reason` carries only the
  exception CLASS NAME, never its message, so no marker/free text can reach
  it. Confirmed read-only; this file is not edited by this batch.
- `utils/retrieval_outcome.py RetrievalError(source, reason)`: `.source`,
  `.reason` are plain attributes; `__str__` = `f"{source}: {reason}"`
  (labels only). Confirmed read-only.

```
$ date
Mon Sep 14 09:18:33 PM CDT 2026
$ grep -n "SYNTHESIS_GENERATOR_ENABLED\|SYNTHESIS_POOLED_ENABLED\|SYNTHESIS_RETRIEVAL_ENABLED\|GRAPH_WALK_ENABLED\|SYNTHESIS_AUDIT_ENABLED\|..." config/app_config.py
```

All flag names the method imports (`SYNTHESIS_GENERATOR_ENABLED`,
`SYNTHESIS_GENERATOR_CANDIDATES_PER_SESSION`, `SYNTHESIS_POOLED_ENABLED`,
`SYNTHESIS_POOLED_CANDIDATES_PER_SESSION`, `SYNTHESIS_POOLED_LLM_CONCURRENCY`,
`SYNTHESIS_RETRIEVAL_ENABLED`, `GRAPH_WALK_ENABLED`,
`GRAPH_WALK_MAX_CANDIDATES`, `GRAPH_WALK_MIN_BRIDGE_EDGES`,
`SYNTHESIS_AUDIT_ENABLED`) exist as `config.app_config` module attributes
(confirmed by grep, each with a concrete default), so monkeypatching them
with `monkeypatch.setattr("config.app_config.<NAME>", value)` before the
method's inline `from config.app_config import (...)` picks up the patched
value at call time (leaf module, no caching).

```
$ date
Mon Sep 14 09:18:37 PM CDT 2026
$ grep -n "class PooledConceptSynthesisGenerator\|def __init__\|def generate_candidates" knowledge/synthesis_pooled_generator.py
66:class PooledConceptSynthesisGenerator:
69:    def __init__(
146:    async def generate_candidates(self, count: int) -> List[SynthesisCandidate]:
```

Confirms the constructor/method shape the method calls
(`PooledConceptSynthesisGenerator(model_manager=..., concurrency=...)` then
`await pooled_gen.generate_candidates(count=...)`) — a spy `**kwargs`
`__init__` plus `async def generate_candidates(self, count)` covers it.

```
$ date
Mon Sep 14 09:19:06 PM CDT 2026
$ grep -n "def get_logger" -A 6 utils/logging_utils.py
137:def get_logger(name: str = "daemon_app") -> logging.Logger:
143:    return logging.getLogger(name)
```

Plain `logging.getLogger`, propagates to root — `caplog.at_level("WARNING")`
captures it without any logger-name argument (same pattern already used in
`tests/unit/test_thread_outcomes.py`, read for precedent at
`_make_processor`/`caplog.at_level` lines 266-302).

## 2026-09-14 21:19:14 CDT — importer re-grep and hazard vetting

```
$ date
Mon Sep 14 09:19:14 PM CDT 2026
$ grep -rl --exclude-dir=data --exclude-dir=.agent_snapshots -E "memory\.shutdown_processor|ShutdownProcessor" tests/unit/ | sort
tests/unit/test_entity_facts.py
tests/unit/test_knowledge_graph.py
tests/unit/test_ordered_slice_guard.py
tests/unit/test_proposal_loop_outcomes.py
tests/unit/test_proposal_store_outcomes.py
tests/unit/test_retriever_read_outcomes.py
tests/unit/test_retrospective_small_guards.py
tests/unit/test_sep09_storage_repairs.py
tests/unit/test_shutdown_pipeline_proposals.py
tests/unit/test_skill_and_summary_outcomes.py
tests/unit/test_source_excerpt_pipeline.py
tests/unit/test_stance_write_path.py
tests/unit/test_stream_artifacts.py
tests/unit/test_thread_outcomes.py
```

14 files (not 15 — the brief's list included `test_codex_followups.py`,
carried over from F12d's broader synthesis-importer grep). Confirmed:
`grep -n "shutdown_processor\|ShutdownProcessor" tests/unit/test_codex_followups.py`
returns nothing — it does not import `memory.shutdown_processor` /
`ShutdownProcessor` at all, so it is correctly excluded from THIS batch's
importer set (the brief itself flagged this possibility: "The set may be
smaller than the synthesis importer set F12d used; grep exactly.").

```
$ date
Mon Sep 14 09:22:42 PM CDT 2026
$ for f in <the 14 files>; do grep -n "MagicMock(spec=\[\])\|UserProfile(\|MemoryConsolidator(\|DailyNotesGenerator(\|ContextGatherer(" "tests/unit/$f"; done
(no output for any file)
```

None of the 14 hits a standing hazard (bare-spec `MagicMock`/`SimpleNamespace`
feeding `ContextGatherer`, `UserProfile()`, `MemoryConsolidator(`,
`DailyNotesGenerator(`). None is `test_graph_integration.py`,
`tests/test_web_search_manager.py`, `tests/test_prompt_internal_methods.py`,
or a top-level `tests/*.py` file (all 14 are under `tests/unit/`). All 14
are safe to run.

## 2026-09-14 21:22:48 CDT — pre-edit bug-class scan (read-only)

```
$ date
Mon Sep 14 09:22:48 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    643    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18_except_returns_empty`: found 23 / base 79 / new 0 / stale 56 — matches
the parent-confirmed baseline after F12d exactly (launch fact). `dm17`
files=643, also matching. Full output saved at `$S/f12d2_scan_pre.txt`.

## 2026-09-14 21:24:25 CDT — new test file written; SIZE checkpoint

Wrote `tests/unit/test_synthesis_audit_fail_closed.py` (fakes/spies only;
see the file's own docstring for exactly which names are patched and
where). No source file touched yet.

```
$ date
Mon Sep 14 09:24:25 PM CDT 2026
$ wc -l tests/unit/test_synthesis_audit_fail_closed.py
294 tests/unit/test_synthesis_audit_fail_closed.py
```

The owned source edit (the audit try/except block, 1708-1725, plus one
local `RetrievalError` import and an optional one-sentence docstring
addition) is estimated at roughly 15-25 changed lines from reading the
region (comparable to F12d's own 13-line `synthesis_memory.py` edit for a
similarly-shaped except-tail change). 294 + ~25 ≈ 319, comfortably under
the ≤450 hard stop, close to but a little over the ≤250 target — matching
F12d's own accepted precedent (301 test + 13 source = 314). Proceeding
without a split.

## 2026-09-14 21:24:37 CDT — guard, MemAvailable

```
$ date
Mon Sep 14 09:24:37 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done   # (none)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6782
```

No `python*`-comm process matching `-m pytest`. MemAvailable 6782 MB
(≥4000).

## 2026-09-14 21:24:41 CDT — FAILING-FIRST (digests in the same command,
before any source edit)

```
$ date
Mon Sep 14 09:24:41 PM CDT 2026
$ sha256sum memory/shutdown_processor.py memory/synthesis_memory.py
9aa08ee56f13dcb753861ba25153a60aa48817df844928f81acd2360292bae52  memory/shutdown_processor.py
1419e20e17be9458fa9ce7dfa3e5a9f3bb93f0827f227b40833ac07ad618f29a  memory/synthesis_memory.py
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_synthesis_audit_fail_closed.py
```

Both digests match the PARENT-VERIFIED starting digests exactly (confirmed
unedited source; `memory/synthesis_memory.py` is read-only for this batch
and is digested here as a drift check only, per the ORDER REMINDER's
pattern).

Result: **2 failed, 4 passed**, exit 1, elapsed 0:01.83, Maximum resident
set size 762,892 KB (~0.75 GB, well under the 6G cap).

Failures (exactly the two fail-open sites this batch's contract fixes):
- `TestAuditFailClosedTypedDegrade::test_retrieval_error_skips_dreaming_one_warning_labels_only`
  — `assert 1 == 0` on `filter_spy.construct_calls`. The unedited broad
  `except Exception as e: logger.debug(...)` swallows the typed
  `RetrievalError` and lets dreaming proceed to build the filter/generator,
  instead of returning after one warning.
- `TestAuditBroadFailurePropagatesToOuterExcept::test_other_exception_ends_dreaming_non_fatal`
  — `assert 1 == 0` on `gen_spy.construct_calls`. The unedited broad except
  also swallows the `RuntimeError` from a failed `SynthesisMemory`
  construction and lets dreaming proceed, instead of letting it propagate
  to the method's outer except.

Passes (4, the controls — already correct on unedited source, per contract
item 4 "unchanged"): `test_auto_halt_true_logs_halted_no_filter_or_generator`,
`test_auto_halt_false_healthy_stats_dreaming_proceeds`,
`test_audit_disabled_skips_read_dreaming_proceeds`,
`test_both_generator_flags_false_early_return_nothing_constructed`.

Captured log lines shown by pytest (pre-existing lines, unedited by this
batch, carrying `{e}` per the privacy carve-out): `memory/synthesis_memory.py:439
Error fetching all synthesis results: boom F12D2_H7RQXM` (ERROR, from the
already-shipped `get_all_results` except) and `shutdown_processor.py:1832
[Shutdown] Synthesis dreaming failed (non-fatal): boom F12D2_H7RQXM`
(WARNING, the existing OUTER except log line — this is expected to still
fire post-edit for the broad-failure test, since that except is unchanged;
its `{e}` carve-out is why the broad-failure test does not assert on the
marker).

## 2026-09-14 21:25:17 CDT — source edit

`memory/shutdown_processor.py`, `_run_synthesis_dreaming` ONLY (docstring +
the audit try/except, 1708-1725 in the pre-edit numbering): one sentence
added to the docstring (fail-closed note); the inner comment expanded to
explain the fail-closed contract; one local `from utils.retrieval_outcome
import RetrievalError` added inside `if SYNTHESIS_AUDIT_ENABLED:` (matching
the file's own local-import-before-the-try precedent at line 1556); the
inner `except Exception as e: logger.debug(f"[Shutdown] Audit check
skipped: {e}")` replaced with `except RetrievalError as e:` logging ONE
labels-only warning (`e.source`, `e.reason` — never `{e}`/`str(e)`) then
`return`. No broad except added; any non-`RetrievalError` failure in the
block now propagates to the method's pre-existing, unedited outer `except
Exception as e:` (contract items 1-3). No other line in the file touched.

```
$ date
Mon Sep 14 09:25:17 PM CDT 2026
$ sha256sum memory/shutdown_processor.py
ae1f9f80c38e0e44ca2aefab2e15301f45d2369c908efad250160447ff835bbc  memory/shutdown_processor.py
$ wc -l memory/shutdown_processor.py
2116 memory/shutdown_processor.py
$ git diff --stat -- memory/shutdown_processor.py
memory/shutdown_processor.py | 62 ++++++++++++++++++++++++++++++++++----------
 1 file changed, 49 insertions(+), 13 deletions(-)
$ sha256sum memory/synthesis_memory.py
1419e20e17be9458fa9ce7dfa3e5a9f3bb93f0827f227b40833ac07ad618f29a  memory/synthesis_memory.py
$ wc -l tests/unit/test_synthesis_audit_fail_closed.py
294 tests/unit/test_synthesis_audit_fail_closed.py
```

Before: `9aa08ee56f13dcb753861ba25153a60aa48817df844928f81acd2360292bae52`.
After: `ae1f9f80c38e0e44ca2aefab2e15301f45d2369c908efad250160447ff835bbc`
(2116 lines). `memory/synthesis_memory.py` confirmed byte-identical to its
starting digest (out of scope, read-only, untouched).

**Note on `git diff --stat` (49/13 = 62 total for the file):**
`shutdown_processor.py` was already `M` (modified relative to HEAD
328a8ec) before this batch started — the brief's launch fact says its
digest `9aa08ee5…` is "unchanged since F11b-1b", i.e. an EARLIER batch's
accumulated, uncommitted edit is already in this working-tree file.
`git diff --stat` reports the file's TOTAL delta from HEAD, which
necessarily includes that prior work, not just this batch's. This batch's
OWN delta was computed directly from the two Edit-tool calls actually made
(reconstructed old/new text, diffed with plain `diff -u` under `$S`, not a
`python` invocation): **3 deletions + 16 insertions = 19 changed lines**
(3 for the docstring addition, 16 for the comment/import/except-block
change). This matches exactly what was applied: the docstring gained 3
lines (a blank line + 2 sentence lines); the comment grew from 1 to 6 lines
(+5); one import line was added (+1); the `except Exception`/`logger.debug`
pair (2 lines) was replaced by `except RetrievalError`/`logger.warning`/
`return` (6 lines) (+4 net, i.e. -2/+6 line-for-line). Total: -3/+16 = 19.

**Size: 294 (test) + 19 (source) = 313 changed lines** — under the ≤450
hard stop, comparable to F12d's own accepted 314 (301 test + 13 source).

## 2026-09-14 21:26:36 CDT — data/logs after the source edit (pre-test)

```
$ date
Mon Sep 14 09:26:36 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged (same 4 entries, sizes, mtimes)
$ ls -ld --time-style=full-iso logs   # still absent
```

## 2026-09-14 21:26:40 CDT — guard, MemAvailable

```
$ date
Mon Sep 14 09:26:40 PM CDT 2026
$ for p in $(pgrep -f -- '-m pytest'); do case "$(cat /proc/$p/comm 2>/dev/null)" in python*) echo "RUNNING: $p";; esac; done   # (none)
$ awk '/MemAvailable/ {print int($2/1024)}' /proc/meminfo
6648
```

## 2026-09-14 21:26:45 CDT — FOCUSED chunk (6 files)

```
$ date
Mon Sep 14 09:26:45 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_synthesis_audit_fail_closed.py tests/unit/test_synthesis_audit.py tests/unit/test_synthesis_read_outcomes.py tests/unit/test_shutdown_pipeline_proposals.py tests/unit/test_codex_followups.py tests/unit/test_retrieval_outcome.py
```

Result: **137 passed**, 0 failed, exit 0, elapsed 0:07.13, Maximum resident
set size 1,135,224 KB (~1.1 GB). 4 warnings, all pre-existing deprecation
noise (websockets/Swig), unrelated to this batch.

```
$ date
Mon Sep 14 09:26:54 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged
$ ls -ld --time-style=full-iso logs   # still absent
```

## 2026-09-14 21:27:13 CDT — SWEEP chunks (13 remaining importer files, in
two chunks of 7 and 6)

The full re-grep (recorded above, "importer re-grep and hazard vetting")
found 14 files. 1 was used in FOCUSED (`test_shutdown_pipeline_proposals.py`;
`test_codex_followups.py` and `test_retrieval_outcome.py` were also in
FOCUSED but are not in the 14-file importer set — they were brief-mandated
FOCUSED files, not swept again here). The remaining 13 form the two SWEEP
chunks below (both ≤9 files): `test_entity_facts.py`,
`test_knowledge_graph.py`, `test_ordered_slice_guard.py`,
`test_proposal_loop_outcomes.py`, `test_proposal_store_outcomes.py`,
`test_retriever_read_outcomes.py`, `test_retrospective_small_guards.py`,
`test_sep09_storage_repairs.py`, `test_skill_and_summary_outcomes.py`,
`test_source_excerpt_pipeline.py`, `test_stance_write_path.py`,
`test_stream_artifacts.py`, `test_thread_outcomes.py`. None excluded by a
standing hazard (vetted above); none is `test_graph_integration.py`,
`tests/test_web_search_manager.py`, `tests/test_prompt_internal_methods.py`
or a top-level `tests/*.py` file; the Gradio synthesis tab is never
imported.

Guard (immediately before chunk 1): no `python*`-comm process matching
`-m pytest`. MemAvailable 6640 MB (≥4000).

```
$ date
Mon Sep 14 09:27:18 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_entity_facts.py tests/unit/test_knowledge_graph.py tests/unit/test_ordered_slice_guard.py tests/unit/test_proposal_loop_outcomes.py tests/unit/test_proposal_store_outcomes.py tests/unit/test_retriever_read_outcomes.py tests/unit/test_retrospective_small_guards.py
```

Result (chunk 1, 7 files): **177 passed**, 0 failed, exit 0, elapsed
0:11.54, Maximum resident set size 3,213,728 KB (~3.2 GB, under the 6G
cap). 4 warnings, pre-existing deprecation noise (Swig/spacy click),
unrelated to this batch.

```
$ date
Mon Sep 14 09:27:31 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged
$ ls -ld --time-style=full-iso logs   # still absent
```

Guard (immediately before chunk 2): no `python*`-comm process matching
`-m pytest`. MemAvailable 6728 MB (≥4000).

```
$ date
Mon Sep 14 09:27:39 PM CDT 2026
$ systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio tests/unit/test_sep09_storage_repairs.py tests/unit/test_skill_and_summary_outcomes.py tests/unit/test_source_excerpt_pipeline.py tests/unit/test_stance_write_path.py tests/unit/test_stream_artifacts.py tests/unit/test_thread_outcomes.py
```

Result (chunk 2, 6 files): **154 passed**, 0 failed, exit 0, elapsed
0:09.65, Maximum resident set size 1,702,044 KB (~1.7 GB). 114 warnings,
all pre-existing chromadb `legacy embedding function config`
`DeprecationWarning` noise from `test_sep09_storage_repairs.py`'s real
in-memory Chroma fixture (a pre-vetted, previously-passing importer file
this batch does not touch) plus the same Swig noise as elsewhere, unrelated
to this batch.

```
$ date
Mon Sep 14 09:27:51 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged
$ ls -ld --time-style=full-iso logs   # still absent
```

Total across FOCUSED + both SWEEP chunks: 137 + 177 + 154 = **468 passed**,
0 failed, across 19 distinct test files. data/ unchanged after every chunk;
logs/ never created.

## 2026-09-14 21:28:24 CDT — ruff

```
$ date
Mon Sep 14 09:28:24 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff --version
ruff 0.14.9
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m ruff check memory/shutdown_processor.py tests/unit/test_synthesis_audit_fail_closed.py
All checks passed!
```

## 2026-09-14 21:28:28 CDT — post-edit bug-class scan (read-only)

```
$ date
Mon Sep 14 09:28:28 PM CDT 2026
$ PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python scripts/check_bug_classes.py scan --root .
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    644    36     39    0    3
dm18_except_returns_empty     gate    123    23     79    0    56
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18_except_returns_empty`: found 23 / base 79 / new 0 / stale 56 —
UNCHANGED from the pre-edit scan (this edit replaced a debug-log-then-
fall-through `except Exception` with a typed `except RetrievalError: …
return`, not a `return []`/`return None`/`return ""` empty-value anchor, so
dm18's count is unaffected either way; **new = 0**, matching the brief's
expectation exactly — **SCANNER SHAPE satisfied, nothing to escalate**).
`dm17_apply_without_guard`'s `files` column moved 643→644 (the new test
file entering that scanner's glob, exactly the brief's expectation: "dm17
files +1 for the new test file"), gate numbers (36/39/0/3) unchanged — no
regression. Every other scanner row identical to the pre-edit baseline.
Full output saved at `$S/f12d2_scan_post.txt`.

## 2026-09-14 21:28:38 CDT — final data/logs listing

```
$ date
Mon Sep 14 09:28:38 PM CDT 2026
$ ls -la --time-style=full-iso data   # unchanged (same 4 entries throughout)
$ ls -ld --time-style=full-iso logs   # still absent throughout
```

No new or changed entry in data/ across any run in this batch. logs/ never
created.

## BC-58 sibling search (record only, per the OWNERSHIP boundary — only the
audit-check block 1708-1725 [pre-edit numbering] is owned)

```
$ date
Mon Sep 14 09:28:43 PM CDT 2026
$ grep -n "except Exception\|except RetrievalError\|except StoreWriteError" memory/shutdown_processor.py
(38 hits; the new `except RetrievalError as e:` at 1733 confirmed at its
edited location)
```

The one directly relevant sibling is `_run_synthesis_dreaming`'s OWN outer
`except Exception as e:`, now at line 1844 (shifted +13 lines by this
edit; was 1831 pre-edit) — read directly and confirmed UNCHANGED:

```
        except Exception as e:
            logger.warning("[Shutdown] Synthesis dreaming failed (non-fatal): %s", e)
```

This is exactly contract item 2's target ("any OTHER exception ... now
propagates to the method's existing outer except") — deliberately left
untouched, not a new anchor. Every other `except` in the file (37 more
hits) belongs to unrelated methods (`process_shutdown_memory`'s phases,
`_extract_*`, `_generate_proposals`, `_process_open_threads`'s own typed
`except RetrievalError as e:` at 1559 — the file's own pre-existing
precedent this batch's local-import placement follows — `_run_wiki_enrichment`,
etc.) and is out of this batch's OWNERSHIP (the audit-check block only);
none was read closely beyond confirming line 1844 is the direct sibling.

## Privacy / no-network note

No real names, emails, IPs or other user-identifying content appears in any
new test, fixture, log assertion, source edit or this packet — only the
synthetic marker `F12D2_H7RQXM` and generic placeholder content ("boom").
The marker never appears in the NEW warning lines this batch adds (asserted
directly in `TestAuditFailClosedTypedDegrade`, scoped to the single new
WARNING-level record, not the full `caplog.text`, since the pre-existing
`memory/synthesis_memory.py:439` ERROR line and the pre-existing outer
`except Exception` WARNING line at `shutdown_processor.py:1845` are BOTH
kept-unchanged lines that legitimately carry `{e}` per the brief's privacy
carve-out — visible unredacted in the captured-log output above, and
deliberately not asserted against). No network call, no LLM/paid API call,
no `pip install`. Every store/filter/generator/memory object in the new
tests is a local spy or `FakeChromaStore` (in-memory, no I/O, patched at
its production import site); no real Chroma, embedder, cross-encoder,
ModelManager or Gradio tab was constructed at any point.

## Milestone

Focused contract green: FAILING FIRST (2 failed, 4 passed) on the unedited
source → source edit (19 changed lines: 3 docstring + 16 comment/import/
except-block) → FOCUSED chunk 137 passed → SWEEP chunks 177 + 154 = 331
passed (468 total, 0 failed) → ruff clean → post-edit scan dm18 unchanged
(new=0), dm17 files+1 as expected → data/ and logs/ unchanged throughout.
The owner's fail-closed audit-check decision (briefs/PARENT_STATE.md item
2) is fully implemented and tested.

## Deviations, escalations, limitations

1. **None.** ORDER was followed exactly (manifest → this packet created →
   pre-edit scan → test file written → SIZE checkpoint → failing-first →
   source edit → focused → sweep → ruff → scan → data/logs → this packet),
   with no source write before the failing-first run was recorded, and no
   implement-then-revert.
2. **Path-set verification false start** (2026-09-14 21:16:09, see the
   first "pre-edit status, manifest, digests" section): the first attempt
   at the changed-path listing used `git ls-files -co --exclude-standard`
   (every tracked+untracked file, 1311 entries) instead of the changed-set
   basis the manifest actually uses. This was read-only (`git
   ls-files`/`sort`/`diff`, no source touched, no destructive command), was
   caught immediately by the line-count mismatch, and was corrected before
   drawing any conclusion — not a process breach, no recovery needed.
3. **Importer-set is 14, not the brief's 15**: `test_codex_followups.py` is
   in the brief's PARENT-VERIFIED FACTS list (carried over from F12d's
   broader synthesis-importer grep) but does not import
   `memory.shutdown_processor` / `ShutdownProcessor` at all — confirmed by
   direct grep (no hits). It was still run, in FOCUSED, exactly as the
   brief's TESTS section separately mandates by name. The brief itself
   flagged this possibility ("The set may be smaller... grep exactly.").
4. **`git diff --stat` for `memory/shutdown_processor.py` reports 62
   changed lines (49+13), not this batch's actual 19** — the file already
   carried an earlier batch's (F11b-1b's) uncommitted edit before this
   batch started (its start-of-batch digest `9aa08ee5…` differs from HEAD).
   This batch's own delta (19: 3 docstring + 16 comment/import/except-block,
   reconstructed from the two Edit-tool calls and verified with a plain
   `diff -u` under `$S`) is reported in "source edit" above and used for
   the SIZE total; `git diff --stat`'s file-level number is not.
5. **Limitation (carried forward from F12d):** the GUI synthesis tab
   (`gui/tabs/synthesis.py`) and `scripts/synthesis_validation.py` are
   unaffected by this batch (this batch's edit is inside
   `_run_synthesis_dreaming` only, which neither of those modules calls);
   not re-verified here since OWNERSHIP marks them read-only/out of scope
   and F12d already recorded their relationship to the audit-stats raise.
6. **Limitation:** only the `SYNTHESIS_POOLED_ENABLED=True` generator
   branch (`PooledConceptSynthesisGenerator`) was exercised in the
   "dreaming proceeds" control tests, per the file's own comment that this
   is now the sole non-retired generator path; the retired
   retrieval/graph_walk/tier2 branch (1752-1808, unedited, unreachable
   under these tests' flag set) was not separately driven — out of this
   batch's OWNERSHIP (the audit-check block only) and unaffected by the
   fail-closed change either way.

## Parent review and integration (2026-09-14)

Accepted. No rule breach, and ORDER was followed. With this batch, the owner's decision 2 (the synthesis audit auto-halt fails closed) is implemented.

### Process findings (parent)

1. **ORDER verified from mtimes.**
   - Test file last written 21:24:23.
   - Failing-first 21:24:41, printing the unedited `9aa08ee5…` (2 failed, 4 passed).
   - `memory/shutdown_processor.py` edited 21:25:15.
   - This packet last written 21:29:30.
2. **Size.** 313 changed lines (19 source + 294 test), over the brief's 250 target but under the 450 hard stop.
   - The worker got its own 19-line source delta by reconstructing the two Edit calls and running a plain `diff -u` under `$S`. The file-level `git diff --stat` (62) includes F11a-1 and F11b-1b.
3. **Parent brief error (caught by the worker).** The brief's importer list came from F12d's combined synthesis-plus-shutdown grep, and wrongly named `test_codex_followups.py` as a shutdown importer. The parent's grep confirms 14 shutdown importers before this batch.
4. **Control-test strength.** The two "dreaming proceeds" controls assert spy constructions but not the absence of the outer "Synthesis dreaming failed" warning, so a later failure after the filter would not fail them.
   - The parent read the path after the filter (1823–1842): `process_batch` on the spy, then one info log. Nothing real is reached.
   - The test module docstring cites pre-edit line numbers (1708–1725, 1831); the outer except is now at 1844.
5. **Self-corrected read-only false start** on the path-set listing (a `git ls-files -co` basis). Recorded by the worker.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F12d.txt` shows exactly one mismatch, `memory/shutdown_processor.py` (`9aa08ee5…` → `ae1f9f80…`, 2116 lines).
- **New paths (two):** `tests/unit/test_synthesis_audit_fail_closed.py` (`ebc81920…`, 294 lines) and this file.
- **Git state:** HEAD `328a8ec` is unchanged. The index and stash are empty, and there are no untracked files at the repository root.
- **`data/`:** identical to `$S/data_baseline_post_F12a.txt`, which stays the baseline. `logs/` is absent.
- **Parent edits after the worker returned:**
  - `briefs/PARENT_STATE.md`;
  - `briefs/F13a.md` (new);
  - `failure_outcome_design.md` (the "F13 split and receipt carriers" amendment).
- **Recorded tree:** `manifest_post_F12d-2.txt` records the tree after this section.

### Code review (the parent read 1678–1747 and 1822–1846)

- **Docstring:** one fail-closed sentence added. The audit comment is expanded.
- **Import:** `from utils.retrieval_outcome import RetrievalError` sits inside `if SYNTHESIS_AUDIT_ENABLED:` before the `try:`, following the file's local-import precedent at 1556.
- **The typed catch:** `except RetrievalError as e:` → `logger.warning("[Shutdown] Synthesis dreaming skipped: audit stats unavailable (%s: %s)", e.source, e.reason)` → `return`.
  - The old `except Exception` / `logger.debug("Audit check skipped")` is gone.
  - No new broad except. Any other failure (including a failing `SynthesisMemory(...)` construction) reaches the unchanged outer `except Exception` at 1844, which ends dreaming non-fatally.
- **Unchanged:** the auto-halt-true path, the disabled-audit path, the generator gates, and the generator/filter body.
- **Callers:** `run_synthesis_dreaming` and main.py 653/1728 are unchanged.

### Tests (the parent read the whole new file)

- **Processor:** built with `ShutdownProcessor.__new__` (chroma_store, memory_coordinator, model_manager only).
- **Patching:** flags are patched on `config.app_config`, set so only the pooled branch runs (generator False, pooled True, retrieval False, graph walk False). `SynthesisFilter`, `PooledConceptSynthesisGenerator` and `SynthesisMemory` are replaced by spies at their source modules (the method imports them at call time).
- **Typed degrade:** the REAL `SynthesisMemory` over a raising fake store gives no filter or generator construction, and exactly one WARNING carrying `synthesis_memory` and `all_results:RuntimeError`, with no marker. (The pre-existing ERROR line in `get_all_results` may carry `{e}`, as the brief allows.)
- **Broad failure:** a raising `SynthesisMemory` constructor gives no filter or generator, and the outer "Synthesis dreaming failed (non-fatal)" warning.
- **Controls:**
  - auto_halt True → HALTED, nothing constructed;
  - auto_halt False → one audit read, filter and generator constructed once;
  - audit disabled → no audit read, dreaming proceeds;
  - both generator flags False → nothing constructed.

### Parent rerun (guarded, capped, `-p asyncio`; data/ diffed against `$S/data_baseline_post_F12a.txt` after every chunk)

- **Focused** (MemAvailable 6,690 MB): `test_synthesis_audit_fail_closed`, `test_synthesis_audit`, `test_synthesis_read_outcomes`, `test_shutdown_pipeline_proposals`, `test_codex_followups` and `test_retrieval_outcome` gave **137 passed** (7.59 s, 1,134,980 KB).
- **Sweep 1** (6,698 MB): `test_entity_facts`, `test_knowledge_graph`, `test_ordered_slice_guard`, `test_proposal_loop_outcomes`, `test_proposal_store_outcomes`, `test_retriever_read_outcomes` and `test_retrospective_small_guards` gave **177 passed** (11.71 s, 3,212,132 KB).
- **Sweep 2** (6,718 MB): `test_sep09_storage_repairs`, `test_skill_and_summary_outcomes`, `test_source_excerpt_pipeline`, `test_stance_write_path`, `test_stream_artifacts` and `test_thread_outcomes` gave **154 passed** (9.08 s, 1,706,708 KB).
- **Total:** 468 passed, matching the worker. data/ was unchanged after every chunk and after the scan, and `logs/` is absent.
- **ruff:** clean.
- **Scan:**
  - dm18 is unchanged at found 23 / base 79 / new 0 / stale 56;
  - the full dm18 row listing is identical to `$S/scan_parent_F12d.txt`, so the typed `except RetrievalError: … return` added no finding;
  - dm17's file count went from 643 to 644; scan stderr is empty.

### Limitations carried forward

- **Generator branches:** only the pooled branch is driven. The retrieval, graph-walk and tier-2 branch (unchanged) is not.
- **Test gap:** the "dreaming proceeds" controls do not assert the absence of the non-fatal failure warning (finding 4).

### Class-guard status

- No anchor (owner decision); no response file. CGR-009 stays fully answered.
