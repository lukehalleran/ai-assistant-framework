# F11a-1: thread reads report failure instead of an empty list; a failed thread write raises; the shutdown thread pass never re-stores duplicates after a failed open-threads read

Worker evidence packet. Written incrementally per R_common_rules.md
"INTERRUPTION". Brief: `docs/execution/generalization/briefs/F11a-1.md`.
Base commit `328a8ec` (clone `/home/lukeh/daemon_exec/generalization`).

## Manifest check (2026-09-14)

- `sha256sum -c $S/manifest_post_F10b.txt` (S = parent scratchpad):
  233 files checked, **0 mismatches** (all `OK`).
- Path diff: `sort($S/manifest_paths_post_F10b.txt)` vs
  `awk '{print $2}' $S/manifest_post_F10b.txt | sort` (233 paths each
  side): **empty diff**.
- `git status --short`: 73 modified + untracked files, matching the
  session's gitStatus snapshot. `git rev-parse HEAD` =
  `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged).
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED (push)`.
- Starting digests (match the brief exactly):
  - `memory/thread_store.py` `79233e7e602f46fe287e78330c5b8a37264c2192b4337fd056f889c838517ad5`
  - `memory/memory_coordinator.py` `726b1d2a560048a998965983a466867a4df057e4bcb6c54ea2d57d88675d630f`
  - `memory/shutdown_processor.py` `eb091a082d5ea0fa9ce4623660c8c3d605a6e191118f3100e25273d2fd3aa716`
  - `tests/unit/test_thread_store.py` `e4007612f27e7f45a5853b089340a5f03e35e33419f6f5cf51334f0804230656`
  - `utils/retrieval_outcome.py` `1abc1586534171b05e3a06fd976d08eba76cb6ecdfef565944f4dcdebf4c95eb` (read-only)
  - `core/prompt/gatherer_knowledge.py` `00870a5bd0447c7b02cdfd9d993c68698d16f9f20b29dddbed2661319581f4a9` (read-only)
- `data/` baseline (`$S/data_baseline_f7a_rerun.txt`, 7 entries) vs current
  `ls -la --time-style=full-iso data`: **no diff**. `logs/`: absent
  (confirmed, `ls -ld` errors "No such file or directory").
- Sanity check: `PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin
  python -c "import utils; print(utils.__file__)"` ->
  `/home/lukeh/daemon_exec/generalization/utils/__init__.py` (this clone).
- MemAvailable at manifest-check time: 6,593 MB.

Manifest check clean. Proceeding.

## Pre-edit scan

`PYTHONPATH=.../scripts/bin python scripts/check_bug_classes.py scan --root .`
(stdout -> `/tmp/f11a1_scan_pre.stdout`, stderr -> `/tmp/f11a1_scan_pre.stderr`,
both under /tmp per the shell-guard rule; exit 1, expected for a gate
scanner with accepted debt).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 633 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **42** | **79** | **0** | **37** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

Matches the brief's stated post-F10b baseline exactly (found 42 / base 79 /
new 0 / stale 37). stderr empty. `core/prompt/gatherer_knowledge.py
[KnowledgeRetrievalMixin.get_unresolved_threads] 'return []'` already
appears in the STALE list here (pre-existing, unrelated to this batch — that
file is read-only per OWNERSHIP).

## Tests (written before any source edit)

New file `tests/unit/test_thread_outcomes.py` (348 lines; fakes only -- no
real Chroma, embedder, model or data path). 18 tests across 8 groups:
`TestStoreThreadOutcomes` (2), `TestListOpenThreadsOutcomes` (3),
`TestGetTopThreadsPropagation` (1), `TestQueryThreadsOutcomes` (4),
`TestCoordinatorUnresolvedThreads` (4), `TestGathererThroughRealCoordinator`
(1), `TestShutdownOpenThreadsReadDegrade` (3). `MockChromaStore`/
`MockCollection` are a LOCAL copy of `test_thread_store.py`'s shape (FIXTURE
RULE: that file's own fixture is untouched). `_make_coordinator` builds
`MemoryCoordinator` via `__new__` with only `thread_store` set;
`_make_processor` builds `ShutdownProcessor` via `__new__` with only
`thread_store` and a stub `model_manager.generate_once` set (the branch
taken passes `session_conversations` as an explicit list, so no
`corpus_manager`/`session_start` is needed). `ThreadExtractor.extract_new_threads`/
`detect_resolutions` are monkeypatched at the class level (no real model
call). The gatherer test binds the REAL, deployed
`MemoryCoordinator.get_unresolved_threads` (not a fake) to a fake
`thread_store`, through the deployed `KnowledgeRetrievalMixin` mixed into a
bare local host class.

FIXTURE RULE repair (the only edit to an existing test file):
`tests/unit/test_thread_store.py` `test_returns_none_when_add_raises`
(def 189) renamed to `test_add_raises_raises_store_write_error` and its
assertion changed from `store_thread(...) is None` to
`pytest.raises(StoreWriteError)`; one import line added
(`from utils.retrieval_outcome import StoreWriteError`). Its paired
control, `test_returns_none_when_collection_unavailable` (≈184-187,
`chroma_store=None` -> None), already existed and is untouched.

## Failing-first (digest line in the same command)

```
sha256sum memory/thread_store.py memory/memory_coordinator.py memory/shutdown_processor.py && \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q \
  -p no:cacheprovider -p asyncio tests/unit/test_thread_outcomes.py
```

Digest line (proves the tests ran against the pre-edit source, matching the
manifest check above):
```
79233e7e602f46fe287e78330c5b8a37264c2192b4337fd056f889c838517ad5  memory/thread_store.py
726b1d2a560048a998965983a466867a4df057e4bcb6c54ea2d57d88675d630f  memory/memory_coordinator.py
eb091a082d5ea0fa9ce4623660c8c3d605a6e191118f3100e25273d2fd3aa716  memory/shutdown_processor.py
```

**Result: 11 failed, 7 passed** in 4.02s, exit 1, peak RSS 1,130,892 KB.

Failures, all explained by the unedited source's swallow-to-None/[] and the
shutdown loop's lack of a try around `store_thread` (exactly the bugs this
batch fixes):
- `test_add_to_collection_raises_store_write_error` — DID NOT RAISE (old
  `store_thread` returns `None`).
- `test_list_all_raises_retrieval_error` — DID NOT RAISE (old
  `list_open_threads` returns `[]`).
- `test_raising_list_open_threads_propagates_unchanged` — DID NOT RAISE
  (same; `get_top_threads` itself has no try, so this failure is entirely
  `list_open_threads`'s unfixed swallow).
- `test_query_collection_raises_failed_outcome_list` — old `query_threads`
  returns plain `[]`, not `OutcomeList.failed(...)`.
- `test_collection_missing_returns_unavailable` — old code has no `coll is
  None` branch; falls into `if coll is None or coll.count() == 0: return
  []`, status `no_results` not `unavailable`.
- `test_retrieval_error_propagates_unchanged` / `test_other_exception_wrapped_as_retrieval_error`
  (coordinator) — DID NOT RAISE (old `get_unresolved_threads` swallows to
  `[]`).
- `test_coordinator_raise_becomes_failed_outcome` (gatherer) — status
  `no_results` not `failed`: the old coordinator swallows, so the
  gatherer's `threads or []` sees a healthy-looking empty list.
- `test_retrieval_error_skips_extraction_and_store_no_exception` — old code
  continues past the read failure straight into Phase 3;
  `thread_store.enforce_cap` WAS called (`assert not True` fails) — this is
  the duplicate-storing defect itself.
- `test_control_genuinely_empty_open_list_extracts_and_stores` — old log
  line is `"[Shutdown] Stored 2 new thread(s)"`, no `", 0 failed"` suffix.
- `test_one_write_failure_counted_second_still_stored` — old code has no
  try/except around `store_thread`, so the injected `StoreWriteError`
  propagates out of `_process_open_threads` uncaught instead of being
  counted.

Passing (7): all paired deliberate-skip/empty/malformed/healthy controls —
`test_control_chroma_store_none_returns_none`,
`test_control_empty_collection_returns_empty_list`,
`test_control_malformed_item_is_skipped`,
`test_control_empty_collection_count_zero_returns_empty_list`,
`test_control_healthy_query_returns_threads`,
`test_control_no_thread_store_returns_empty`,
`test_control_healthy_returns_dicts` — unaffected by the bug, as expected.

`data/` unchanged after this run (diffed clean against the 7-entry
baseline); `logs/` still absent.

## Source edit

`memory/thread_store.py`:
- Import line: `from utils.logging_utils import get_logger` gains a new
  line right after it: `from utils.retrieval_outcome import OutcomeList,
  RetrievalError, StoreWriteError` (one import line, per OWNERSHIP).
- `store_thread` (#146, def 227): docstring's Returns clause split into
  "None if unavailable (skip)" plus a new Raises clause; the except keeps
  its error log line, then `raise StoreWriteError(source="thread_store",
  reason=type(e).__name__) from e` instead of `return None`. The skip
  (`not self._ensure_collection()` -> `return None`) and success
  (`return doc_id`) paths are unchanged.
- `list_open_threads` (#147, def 259): docstring gains a Returns/Raises
  split; the except keeps its error log line, then `raise
  RetrievalError(source="thread_store",
  reason=f"list_open:{type(e).__name__}") from e` instead of `return []`.
  The skip -> `[]`, the per-item parse `except Exception: continue`
  (malformed-row skip), and success are all unchanged.
- `query_threads` (#148, def 330, no production caller): docstring
  rewritten to say callers read `.status` before slicing. `if coll is None
  or coll.count() == 0: return []` split into `if coll is None: return
  OutcomeList.unavailable("collection_missing")` then `if coll.count() ==
  0: return []` (count-zero unchanged); the except keeps its error log
  line, then `return OutcomeList.failed(type(e).__name__)` instead of
  `return []`. The `_ensure_collection` skip and success paths are
  unchanged.

`memory/memory_coordinator.py`, `get_unresolved_threads` (#121, def 589,
body+docstring only, per OWNERSHIP -- no top-level import added): docstring
gains a Raises clause. A local `from utils.retrieval_outcome import
RetrievalError` sits right before the try (mirroring the file's existing
local-import style for `memory.thread_store` symbols two methods above, in
`store_interaction`'s quick-resolution block). A new `except
RetrievalError: raise` sits before the broad except, so a `RetrievalError`
surfacing from `get_top_threads` (e.g. through `list_open_threads`)
propagates unchanged. The broad `except Exception as e:` keeps its debug
log line, then `raise RetrievalError(source="unresolved_threads",
reason=type(e).__name__) from e` instead of `return []`. The `not
self.thread_store` skip and success paths are unchanged.

`memory/shutdown_processor.py`, `_process_open_threads`, two regions only
(per OWNERSHIP):
- The open-threads read (≈1549-1554, now ≈1549-1558): a local `from
  utils.retrieval_outcome import RetrievalError` sits right before the try
  (same local-import style already used twice more in this method, for
  `ThreadExtractor` and `memory.thread_store`'s duplicate helpers). `except
  Exception as e:` narrowed to `except RetrievalError as e:` (the CONTRACT-
  required TYPED catch, not broad -- see SCANNER SHAPE below); it keeps its
  warning log line, then adds `return` so the pass stops BEFORE
  resolution/extraction/store. A short comment explains why (no de-dup
  target survives a failed read). A non-`RetrievalError` failure from
  `list_open_threads` (not expected per its own contract) would now
  propagate out of `_process_open_threads` uncaught -- unchanged from
  today's behavior for any exception `list_all`'s per-item skip doesn't
  already absorb, since the pass runs inside `asyncio.gather(...,
  return_exceptions=True)` at its Phase-B call site (unowned, unchanged).
- The write loop (≈1635-1640, now ≈1635-1646): `failed = 0` added next to
  the existing `stored = 0` (both are the write loop's own counters -- the
  minimal companion line the CONTRACT's count change requires). The
  `doc_id = self.thread_store.store_thread(thread)` call is now wrapped in
  `try: ... except StoreWriteError: failed += 1; continue` (a local `from
  utils.retrieval_outcome import StoreWriteError` sits right before the
  try, mirroring F10b's identical pattern in `_extract_procedural_skills`).
  `stored` still counts only truthy ids. The final log line changed from
  `if stored: logger.info(f"... {stored} new thread(s)")` to `if stored or
  failed: logger.info(f"... {stored} new thread(s), {failed} failed")` so a
  failure is visible even when nothing was stored.

No other files touched. `resolve_thread`, `_update_thread`, `touch_thread`,
`enforce_cap`, `get_top_threads`, `_ensure_collection` and every other
region of both files are unedited, per OWNERSHIP.

### SCANNER SHAPE note

Both degrades use a narrow, TYPED except (`except RetrievalError`/`except
StoreWriteError`), never a broad `except Exception` with a bare
return/continue -- the F10b lesson this brief's "Scanner shape" section
warns against. No restructuring was needed to avoid a scanner match; see
the post-edit scan below for the actual result (new = 0).

## Post-edit test run (guarded, capped, `-p asyncio`)

Guard: no `-m pytest` process running. MemAvailable: 6,582 MB.

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q \
  -p no:cacheprovider -p asyncio tests/unit/test_thread_outcomes.py tests/unit/test_thread_store.py
```

**73 passed, 0 failed**, 4.09s, exit 0, peak RSS 1,125,116 KB (18 new tests
+ 55 existing `test_thread_store.py` tests, including the one FIXTURE RULE
repair). `data/` unchanged after this run (diffed clean); `logs/` absent.

## Post-edit scan

`PYTHONPATH=.../scripts/bin python scripts/check_bug_classes.py scan --root .`
(stdout -> `/tmp/f11a1_scan_post.stdout`, stderr -> `/tmp/f11a1_scan_post.stderr`;
exit 1, expected -- gate scanners with remaining accepted debt elsewhere).

| scanner | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| dm01_raw_substring | gate | 396 | 7 | 12 | 0 | 5 |
| dm17_apply_without_guard | gate | 634 | 36 | 39 | 0 | 3 |
| dm18_except_returns_empty | gate | 123 | **38** | **79** | **0** | **41** |
| dm31_live_state_default | gate | 396 | 3 | 3 | 0 | 0 |

dm18: found 42 -> **38** (net -4), stale 37 -> **41** (net +4), new
0 -> **0** (unchanged -- no new finding from this edit). stderr empty.

**STALE (the fix retired these 4 rows):**
`memory/thread_store.py [ThreadStore.store_thread] 'return None'` (#146),
`memory/thread_store.py [ThreadStore.list_open_threads] 'return []'`
(#147), `memory/thread_store.py [ThreadStore.query_threads] 'return []'`
(#148), `memory/memory_coordinator.py [MemoryCoordinator.get_unresolved_threads]
'return []'` (#121) -- confirmed by grepping the post-edit STALE list.
(`core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_unresolved_threads]
'return []'` was already STALE before this batch -- pre-existing, unrelated,
read-only file.)

No new dm18 (or other) finding anywhere. The typed-except shape (per the
SCANNER SHAPE note above) needed no escalation.

## Focused run (chunks of <=9, guard + MemAvailable checked before each)

9 files, exactly the brief's list -- one chunk. Guard: no `-m pytest`
running. MemAvailable: 6,605 MB.

Exact file list:
```
tests/unit/test_thread_outcomes.py
tests/unit/test_thread_store.py
tests/unit/test_gatherer_outcomes_remaining_knowledge.py
tests/unit/test_thread_context_and_resolution.py
tests/unit/test_thread_extractor.py
tests/unit/test_ordered_slice_guard.py
tests/unit/test_retrieval_outcome.py
tests/unit/test_store_write_outcomes.py
tests/unit/test_skill_and_summary_outcomes.py
```

VET (before running): grepped all 9 for
`MemoryConsolidator\(|UserProfile\(\)|ContextGatherer\(|spec=\[\]|OBSIDIAN_VAULT_PATH|DailyNotesGenerator|CorpusManager\(\)`
-- one hit, `test_skill_and_summary_outcomes.py` lines 278/306
(`_make_storage(corpus_manager=_ProductionSignatureCorpusManager())` /
`_HealthyCorpusManager()`), both LOCAL fake `CorpusManager` classes, not
the real one -- already vetted and run by F10b. No other hit. None is
`test_daily_notes_auto_update.py`, `test_graph_integration.py`, or a
top-level `tests/` file.

**231 passed, 0 failed**, 5.34s, exit 0, peak RSS 1,137,508 KB. `data/`
diffed clean against the baseline afterward; `logs/` absent.

## Sweep

Every remaining `tests/unit/` importer of `memory.thread_store` /
`ThreadStore` / `memory.memory_coordinator` / `MemoryCoordinator` /
`memory.shutdown_processor` / `ShutdownProcessor` (grep), minus the 9
already in the focused set: 15 files (neither
`tests/unit/test_graph_integration.py` nor
`tests/unit/test_daily_notes_auto_update.py` matched the grep at all).

VET (before running, same hazard grep as the focused set): one hit,
`tests/unit/test_memory_coordinator_methods.py` lines 399/433/847
(`Mock(spec=[])` on `coordinator.topic_manager`, `coordinator.time_manager`
and a bare `memory` object) -- none is a `ContextGatherer`'s
`memory_coordinator`; the file never constructs a `ContextGatherer`
(grepped, zero hits) -- the documented fallback-to-real-`UserProfile()`
hazard does not apply. Extra check (not in the brief's own hazard list, but
run out of caution): grepped all 15 for
`chromadb\.|PersistentClient|requests\.(get|post)|urllib\.request|httpx\.|openai\.|anthropic\.`
-- one hit, `test_sep09_storage_repairs.py`, which uses
`chromadb.EphemeralClient` (in-memory only, no disk path, no network) for
deployed-function tests against a real Chroma collection API -- an
established pre-existing pattern in this file, not a default-path or
network hazard.

Chunked <=9 (2 chunks: 8/7).

| Chunk | Files | MemAvailable before | Passed | Time | Peak RSS (KB) |
|---|---|---|---|---|---|
| Sweep 1 | `test_entity_facts.py`, `test_gate_min_results_cap.py`, `test_knowledge_graph.py`, `test_memory_coordinator_methods.py`, `test_orchestrator_helpers.py`, `test_prompt_builder_self_report_trim.py`, `test_proposal_generator.py`, `test_provenance.py` | 6,589 MB | **358 passed** | 8.10s | 3,237,320 |
| Sweep 2 | `test_retrospective_small_guards.py`, `test_sep03_followups_continuity.py`, `test_sep09_storage_repairs.py`, `test_shutdown_pipeline_proposals.py`, `test_source_excerpt_pipeline.py`, `test_stance_write_path.py`, `test_stream_artifacts.py` | 6,615 MB | **201 passed** | 6.11s | 1,734,904 |

**Sweep total: 559 passed, 0 failed.** `data/` diffed clean against the
baseline after every chunk; `logs/` absent throughout.

**Grand total (this batch's own runs): 73 (post-edit re-run) + 231
(focused) + 559 (sweep) = 863 passed, 0 failed** across every command run
in this batch (the 73-count re-run is a subset of the 231-count focused
run's 9 files, not additional coverage -- both are reported for the
record, per ORDER).

## Size

Changed lines counted via before/after `diff -u` against the /tmp copies
saved right after the manifest check (`grep -cE '^[+-][^+-]'`, excluding
the `---`/`+++` header lines), plus the new file's full line count:

| File | Changed lines |
|---|---|
| `memory/thread_store.py` | 29 |
| `memory/memory_coordinator.py` | 10 |
| `memory/shutdown_processor.py` | 19 |
| `tests/unit/test_thread_store.py` (FIXTURE RULE edit) | 10 |
| `tests/unit/test_thread_outcomes.py` (new file, full count) | 348 |
| **Total** | **416** |

**416 changed lines** -- over the 380 target, under the 450 hard cap (34
lines of margin). The test file was designed compactly from the first
draft (short one-line docstrings, no redundant assertions) specifically to
stay clear of the cap; no trim was needed or performed after measuring
(unlike F10b's deviation 1 -- this draft never measured over the hard cap
at any point, so no split proposal was triggered).

## Changed files (SHA-256, after)

| File | After |
|---|---|
| `memory/thread_store.py` | `efe7f863c61bfe402a4acbf76fa25b7a710088f11cceb442b6b51748f0845a37` |
| `memory/memory_coordinator.py` | `d1787423d93dad6d9cab4a80f3d930eb64f6a30eedd788a9031d1ac3fc2750b4` |
| `memory/shutdown_processor.py` | `74c9ed30ccdc5bc159e57c8f84f0c712b1b13e9a8f4ec6f7cffbdeea6f8ced13` |
| `tests/unit/test_thread_store.py` | `39bab8f3a2cff1d51da950410a9df6dd81c7887f245efe6a5c27d2547f4cbedf` |
| `tests/unit/test_thread_outcomes.py` (new) | `d52ab153e6b1cd1f2d90b0bcb1333332cc96ed060bcd2bdcd211c90166d102bf` |

Before digests: see "Manifest check" above (`thread_store.py`
`79233e7e...`, `memory_coordinator.py` `726b1d2a...`,
`shutdown_processor.py` `eb091a08...`, `test_thread_store.py`
`e4007612...`). `test_thread_outcomes.py` is new (no before digest).

## Milestone

Focused contract green (per the brief's scope: `store_thread` #146,
`list_open_threads` #147, `query_threads` #148, `MemoryCoordinator.get_unresolved_threads`
#121, and the shutdown thread pass's read-degrade + write-loop count
change).

## BC-58 sibling search

`resolve_thread`, `_update_thread`, `touch_thread`, `enforce_cap` and
`_ensure_collection` in `memory/thread_store.py` are explicitly read-only
per OWNERSHIP (F11a-2 or recorded siblings) -- confirmed unedited by the
diffs above (only `store_thread`/`list_open_threads`/`query_threads` and
the one import line changed). `_ensure_collection`'s own
`except Exception as e: ... return False` (≈219-221, "failure disguised as
not-configured") is a BC-58 sibling per the brief -- recorded only, not
fixed (F11a-2/owner decision). No sibling of #121/#146/#147/#148's exact
shape exists elsewhere in `memory/memory_coordinator.py` or
`memory/thread_store.py` outside this batch's four anchors -- confirmed by
the post-edit STALE-list grep above showing exactly those four rows
retired and no others in either file. CGR-20260913-009's remaining
anchors (#117-120, #122-126, #132-134, #136, #139, #141-143) and
CGR-20260913-010's remaining anchor (#131) are in files this batch does
not own (`memory/memory_consolidator.py`, `memory/memory_expander.py`,
`memory/memory_retriever.py`, `memory/proposal_store.py`,
`memory/synthesis_memory.py`, `memory/storage/multi_collection_chroma_store.py`)
-- per the brief's RESPONSE FILES section: F11b-1, F12a-F12d, F13.

## Privacy / no-network note

No real institution names or real IPs anywhere in source or tests; only
the synthetic marker `F11A1QZ7` (distinct from every earlier batch's
marker). Every test uses fakes (`MagicMock`, `AsyncMock`, `SimpleNamespace`,
`__new__`-built hosts, a local `MockChromaStore`/`MockCollection`); no
network call, no real Chroma client, no real model, no real consolidator,
no real profile, no default-path store anywhere in this batch's own tests
or in any focused/sweep file exercised (`test_sep09_storage_repairs.py`'s
`chromadb.EphemeralClient` is in-memory only). Every new
`StoreWriteError`/`RetrievalError`/`OutcomeList.failed(...)` uses only
`type(e).__name__` as its reason -- never the raw exception message -- so
the marker cannot reach `.reason`/`str()`; explicit assertions
(`MARKER not in str(err)`, `MARKER not in err.reason`,
`MARKER not in caplog.text`) check this directly in every raising test.

## Deviations / escalations

No COMPLIANCE breach affecting correctness, safety, data/ or privacy. One
minor sequencing note: the brief's ORDER lists `ruff` and the post-edit
`scan` after `focused`/`sweep`; this packet instead ran ruff and the
post-edit scan immediately after the source edit (both are static/read-only
checks independent of test execution), then the post-edit test re-run,
then focused, then sweep. Every REQUIRED ordering was still followed
exactly: the new tests were written and failing-first was proven before
any source edit, and every pytest invocation (including this one) was
preceded by its own fresh guard + MemAvailable check. No evidence step was
skipped or reordered relative to another step it depends on.

Both degrades use a TYPED except from the
first draft (never a broad `except Exception`), so no SCANNER SHAPE
restructuring was needed (post-edit scan: new = 0, confirmed above). The
test file was designed compactly from the outset and never measured over
the 450 hard cap, so no SIZE trimming occurred. The guard/MemAvailable
check was run fresh immediately before every single pytest invocation in
this batch, including every verification re-run (no shared-check gap, per
the F11a-1 brief's explicit reminder after F10b's deviation 3).

## Open limitations (carried forward, matching the brief)

- `_ensure_collection`'s own `except Exception: ... return False` ("failure
  disguised as not-configured") is unchanged -- a BC-58 sibling, recorded
  only, per OWNERSHIP (F11a-2/owner decision).
- `resolve_thread` and `_update_thread` still use the delete-then-store
  order (write loses the record if the store fails after the delete
  succeeds) -- F11a-2's scope, unedited here. Because `store_thread` now
  raises instead of returning `None`, a `StoreWriteError` inside either
  method's loop now surfaces through their own existing broad
  `except Exception as e: logger.error(...); return False` (unowned,
  unchanged) -- so a failed re-store now correctly reports `False` instead
  of the prior silent `True` after a successful delete. This is a
  side-effect of #146's fix flowing through unowned code, not a new
  edit, and does not close F11a-2 (the write-before-delete reordering and
  rollback are still unimplemented).
- `query_threads` has no production caller (test-only, per the brief) --
  the `OutcomeList` contract is validated by tests only.
- `enforce_cap` (unowned, unchanged) has no try around its own
  `list_open_threads()` call either -- the same "no code change" shape as
  `get_top_threads` (tested), so a `RetrievalError` now propagates out of
  `enforce_cap` too. Its only caller in this batch's scope, Phase 3 of
  `_process_open_threads` (`try: self.thread_store.enforce_cap() except
  Exception as e: logger.debug(...)`), already wraps the call in its own
  unowned, unchanged broad except, so this still degrades quietly (a debug
  log, not a crash) rather than needing a new guard.
- The five `except Exception: pass` sites in `core/orchestrator.py`/
  `gui/handlers.py` and the four `except Exception: pass`-shaped
  swallow sites noted in earlier batches are outside this batch's scope;
  no code in this batch's OWNERSHIP is reached by them (thread reads/writes
  have no caller there).
- CGR-20260913-009's remaining anchors (#117-120, #122-126, #132-134,
  #136, #139, #141-143) and CGR-20260913-010's remaining anchor (#131) are
  unedited, read-only per OWNERSHIP; assigned to F11b-1/F12a-F12d/F13 per
  the brief.

## Parent review and integration (2026-09-14)

Accepted as delivered. There was no COMPLIANCE breach. Both degrades used typed excepts from the first draft, so no scanner-shape issue arose. The draft never measured over the cap, so nothing was trimmed. A guard check ran before every pytest invocation, and every chunk's file list is recorded. The only process note is the self-disclosed one: ruff and the scan ran before the test chunks, which is harmless because both are static checks.

### Manifest

- **Checksums:** `sha256sum -c manifest_post_F10b.txt` gives two mismatches, both this batch's edits:
  - `memory/memory_coordinator.py` (`726b1d2a…` → `d1787423…`);
  - `memory/shutdown_processor.py` (`eb091a08…` → `74c9ed30…`).
- **New paths (six):**
  - `memory/thread_store.py` (`79233e7e…` → `efe7f863…`, its first modification);
  - `tests/unit/test_thread_store.py` (`e4007612…` → `39bab8f3…`, the FIXTURE RULE edit, its first modification in this lane);
  - `tests/unit/test_thread_outcomes.py` (`d52ab153…`, 348 lines);
  - `class_guard_responses/CGR-20260913-009-4.md` (`b02c732f…`, 226 lines);
  - `class_guard_responses/CGR-20260913-010-3.md` (`5a8de369…`, 158 lines);
  - this file.
- **Git state:** `git diff --cached --name-only` is empty, HEAD `328a8ec` is unchanged, the reflog shows only the clone entry, and the stash list is empty.
- **Stray files:** none new. The only untracked file at the repository root is `scan_pre.stderr`.
- **`data/`:** the 7-entry baseline is unchanged, and `logs/` is absent.
- **Parent edits after the worker returned:**
  - `briefs/F11a-2.md` was written with post-F11a-1 facts.
  - `briefs/PARENT_STATE.md` was updated.
- **Parent process disclosure:** while re-verifying F11a-2, the parent ran `python -m pip show chromadb` (read-only package metadata; no install) to locate the installed chromadb source. It then read `chromadb/api/types.py` (`GetResult` at 385) read-only, outside the repository. This was a Python invocation beyond ruff and the scan, and is recorded for completeness.
- **Recorded tree:** `manifest_post_F11a-1.txt` records the tree after this section.

### Code review (the parent read every changed region)

- **`store_thread` (#146):** the skip still returns None. The except keeps its error line, then raises `StoreWriteError(source="thread_store", reason=<class>) from e`. The docstring is updated.
- **`list_open_threads` (#147):** the skip still returns [] and the per-item parse skip is kept. The outer except keeps its line, then raises `RetrievalError(source="thread_store", reason="list_open:<class>")`.
- **`query_threads` (#148):**
  - `coll is None` → `OutcomeList.unavailable("collection_missing")`;
  - `count() == 0` → `[]`;
  - the except keeps its line, then returns `OutcomeList.failed(<class>)`.
  - The docstring tells callers to read `.status` before slicing.
- **Coordinator `get_unresolved_threads` (#121):**
  - the `not self.thread_store` skip still returns [];
  - `except RetrievalError: raise`;
  - `except Exception as e` keeps its debug line, then raises `RetrievalError(source="unresolved_threads", reason=<class>)`.
- **`_process_open_threads`:**
  - The read is wrapped in `except RetrievalError as e:`, which logs the existing warning (labels only) and returns before resolution, extraction and any store.
  - In the write loop, `try: doc_id = self.thread_store.store_thread(thread)` / `except StoreWriteError: failed += 1; continue`, and `stored` counts only truthy ids.
  - It logs "Stored {stored} new thread(s), {failed} failed", and Phase 3 `enforce_cap` still runs.
- **Nit, not blocking:** as in F10b, the `utils.retrieval_outcome` imports sit inside function bodies. That file already imports `StoreWriteError` inside `_extract_procedural_skills`.
- **Callers (parent grep of production code):**
  - `list_open_threads`: coordinator quick resolution at 390 (debug-caught); shutdown at 1552 (typed catch); `get_top_threads` at 326 (propagates); `enforce_cap` at 460 (propagates to shutdown Phase 3's debug catch).
  - `get_top_threads`: coordinator at 605.
  - `store_thread`: shutdown at 1643 (typed catch); `resolve_thread` at 415 and `_update_thread` at 501, whose own broad excepts now return False after the delete (F11a-2).
  - `get_unresolved_threads`: the gatherer at 1297, reached from builder 1414.
  - `query_threads`: no production caller.
  - No api/ or gui/ caller.

### Tests (the parent read the whole new file and the fixture hunk)

- **`store_thread`:** a raising write gives `StoreWriteError` with labels and no marker; control: `chroma_store=None` gives None.
- **`list_open_threads`:** a raising `list_all` gives `RetrievalError("list_open:RuntimeError")`; controls: empty gives [] and a malformed item is skipped.
- **`get_top_threads`:** propagation evidence.
- **`query_threads`:**
  - a raising query gives `failed` (`== []`, an `OutcomeList`);
  - `coll is None` gives `unavailable/collection_missing`, via a small dict subclass whose `.get` misses while `in` still sees the collection;
  - controls: count 0 gives [], and healthy returns threads.
- **Coordinator:** a `RetrievalError` propagates as the same object; a `ValueError` is wrapped as `unresolved_threads`; no store gives []; healthy gives dicts.
- **Through the gatherer:** the real coordinator method bound to a raising fake store gives `outcome_status == ("failed", "RetrievalError")`.
- **Shutdown:**
  - a typed read failure means extraction methods are never called (they are patched to raise if called), no store, no cap and no marker;
  - control: a genuinely empty open list gives 2 stored, cap run, "Stored 2 …, 0 failed";
  - one `StoreWriteError` gives 2 attempts, cap run, "Stored 1 …, 1 failed".
- **FIXTURE RULE:** `test_returns_none_when_add_raises` was renamed to `test_add_raises_raises_store_write_error`, now asserting `pytest.raises(StoreWriteError)`. The paired skip control (`chroma_store=None` → None) directly above it is unchanged.
- **Failing-first:** 11 failed and 7 passed.

### Parent rerun (guarded, capped, `-p asyncio`)

| Chunk | Files | MemAvailable | Passed | Time | Peak RSS (KB) |
|---|---|---|---|---|---|
| Focused | the 9 files listed above | 6,578 MB | **231** | 8.30 s | 1,137,428 |
| Sweep 1 | the 8 files in the sweep table | 6,561 MB | **358** | 12.19 s | 3,236,792 |
| Sweep 2 | the 7 files in the sweep table | 6,557 MB | **201** | 8.89 s | 1,727,648 |

- **Total:** **790 passed, 0 failed**, matching the worker (231 + 559).
- **Importer set:** the parent's independent importer grep found 21 files. They are the 6 importers inside the focused chunk plus the 15 sweep files. The remaining 3 focused files do not import these modules.
- **`data/`:** unchanged after every chunk, and `logs/` is absent.
- **ruff 0.14.9:** clean on all five files.
- **Scan:**
  - dm18 moved from found 42 / base 79 / new 0 / stale 37 to 38 / 79 / 0 / 41.
  - `ThreadStore.store_thread 'return None'`, `ThreadStore.list_open_threads 'return []'`, `ThreadStore.query_threads 'return []'` and `MemoryCoordinator.get_unresolved_threads 'return []'` are now in the STALE list, which has 49 rows in total.
  - dm17's file count went from 633 to 634. dm01 and dm31 are unchanged, and scan stderr is empty.

### Size

416 changed lines (68 source and fixture plus 348 test), under the cap.

### Limitations carried forward

- **`resolve_thread` / `_update_thread`:** they still delete before re-storing. A failed re-store now reports False instead of True, but the record is still lost. F11a-2 fixes this.
- **`_ensure_collection`:** its failure disguised as "not configured" is a recorded sibling.
- **`enforce_cap`:** a `RetrievalError` propagates to its only caller, shutdown Phase 3, which catches it at debug level.

### Class-guard status

- **CGR-009:** #139, #122, #130, #121, #147 and #148 are answered (base, `-2`, `-3`, `-4`).
  - Still open: #132–#134 and #136 → F11b-1 (`-5`); #117–#120 → F12a (`-6`); #123, #125, #126 → F12c (`-7`); #141–#143 → F12d (`-8`).
- **CGR-010:** #127, #140, #128, #129 and #146 are answered (base, `-2`, `-3`).
  - Still open: #131 → F11b-1 (`-4`); receipts → F13a/F13b.
