# F2 — Tavily wrappers stop collapsing provider failure into `[]` (CGR-20260913-008 #110, #111)

Worker evidence packet. Design source:
`docs/execution/generalization/failure_outcome_design.md`, "Decisions per
request" → CGR-008 → `_tavily_search`/`_tavily_extract`, batch table row F2,
and the 2026-09-14 parent amendment "F1 Ready decision: one status
vocabulary". Request packet (read-only, the only file read in that
checkout): `/home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-008.md`.
Response file: `docs/execution/generalization/class_guard_responses/CGR-20260913-008.md`
(immutable once written; covers only #110/#111).

## Scope

`knowledge/web_search_manager.py`, at exactly the regions the F2 brief
named: `_tavily_search`/`_tavily_extract` (no-client and except branches),
the three wrapper call sites inside `_execute_search`, the extract call in
`fetch_url_content`, and one new optional field on `WebSearchResult`
(`extract_error`). New `tests/unit/test_tavily_failure_outcomes.py`. Adopts
F1's leaf `utils/retrieval_outcome.py` (`RetrievalError`, `OutcomeList`,
`outcome_status`) — F2 is its first production caller. Read-only:
`utils/retrieval_outcome.py`, `core/prompt/gatherer_web.py`,
`core/agentic/tools.py`, `multi_search`, `config/**`, every class-guard file.

## 1. Manifest check (before any edit)

Run from the checkout root, `S=<scratchpad>`:

```
$ sha256sum -c --quiet $S/manifest_post_F1.txt
$ { git diff --name-only; git ls-files --others --exclude-standard; } | sort -u | diff - $S/manifest_paths_post_F1.txt
```

Both printed nothing, exit 0. The pre-edit tree matched the parent's
post-F1 record exactly.

## 2. Pre-edit git state

- `git rev-parse HEAD`: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit made).
- `git remote -v`: `origin /home/lukeh/Daemon_v1 (fetch)`, `origin DISABLED
  (push)`. Not touched.
- `git status --short` (pre-edit): the same carried-over modified/untracked
  paths from prior integrated batches, plus F1's three new paths
  (`utils/retrieval_outcome.py`, `tests/unit/test_retrieval_outcome.py`,
  `docs/execution/generalization/batches/F1.md`); neither
  `knowledge/web_search_manager.py` (modified) nor
  `tests/unit/test_tavily_failure_outcomes.py` (new) present yet.
- `sha256sum knowledge/web_search_manager.py`:
  `2f5d005b959ae3dd28dddadce9891568719ecb7ce8954a4ac36bb7e5f1e647fe` — matches
  both the request packet's recorded source SHA-256 and the brief's
  parent-verified fact.

## 3. Parent-verified facts re-verified

All facts in the F2 brief's "PARENT-VERIFIED FACTS" section were read
directly from the deployed source before editing and matched exactly:
`FetchedPages`/`WebSearchResult`/`_execute_search`'s charge-on-dispatch
shape (lines 1361–1458), `_tavily_search` (1460–1538, anchor #110 at 1538),
`_tavily_extract` (1708–1744, anchor #111 at 1744), `fetch_url_content`
(1558–1610), the two read-only consumer sites (`core/agentic/tools.py:2073`,
`core/prompt/gatherer_web.py:297`), and F1's exact implemented API
(`RetrievalError(*, source, reason)`, `OutcomeList.failed(reason,
items=())`, `outcome_status(value)` — read from `utils/retrieval_outcome.py`
and F1.md directly, no drift). One addition found during implementation, not
in the brief's fact list: `_tavily_search`'s except block's existing log
line (`log.error(f"[WebSearch] Tavily search failed: {e}")`) is pre-existing
behavior, unchanged by this batch — the privacy constraint applies to the
NEW `RetrievalError.reason`/`str()`/`WebSearchResult.error`/`extract_error`
fields, not to this pre-existing log statement, per the brief's explicit
"keep... the log lines exactly."

No drift found; no escalation needed.

## 4. Order followed

manifest → pre-edit scan → write test file → failing-first run (digest in
the same command) → edit source → focused run (one test bug found and
fixed, rerun) → sweep (9 files, one chunk) → ruff → post-edit scan → data
listing → response file → this packet.

## 5. Pre-edit scan (read-only, `scripts/check_bug_classes.py scan --root .`)

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    617    36     39    0    3
dm18_except_returns_empty     gate    123    79     79    0    0
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` findings for `knowledge/web_search_manager.py` (via `--json`, digest
`2f5d005…`): `_tavily_search` (line 1538, anchor #110), `_tavily_extract`
(line 1744, anchor #111), and `_select_links_for_following` (line 1810 —
same file, same scanner, **not** a candidate anchor in the request packet's
table and **not** touched by F2; recorded as a sibling observation in the
response file).

## 6. Failing-first proof

Guard checked first (`/proc/comm` pytest guard: clear); MemAvailable 5439 MB
≥ 4000. One command, before any source edit:

```
$ sha256sum knowledge/web_search_manager.py && \
  PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_tavily_failure_outcomes.py
```

Digest: `2f5d005b959ae3dd28dddadce9891568719ecb7ce8954a4ac36bb7e5f1e647fe`
(matches). Result: **12 failed, 7 passed**, exit 1. Elapsed 0:04.86; Maximum
resident set size 1,080,772 KB.

Failing tests (all rejection-shaped, as expected on the unedited source —
today's wrappers return `[]`/raise nothing, `WebSearchResult` has no
`extract_error`):

- `TestTavilyWrappers::test_provider_exception_raises_retrieval_error[tavily_search]`
- `TestTavilyWrappers::test_invalid_key_shaped_exception[tavily_search]`
- `TestTavilyWrappers::test_no_client_raises_client_unavailable[tavily_search]`
- `TestTavilyWrappers::test_provider_exception_raises_retrieval_error[tavily_extract]`
- `TestTavilyWrappers::test_invalid_key_shaped_exception[tavily_extract]`
- `TestTavilyWrappers::test_no_client_raises_client_unavailable[tavily_extract]`
- `TestSearchEndToEnd::test_provider_exception_becomes_error_not_cached_reservation_settled`
- `TestStandardDepthExtractFailure::test_extract_raises_keeps_search_pages_and_sets_extract_error`
- `TestStandardDepthExtractFailure::test_control_extract_succeeds_no_extract_error`
- `TestFetchUrlContent::test_direct_empty_extract_raises_returns_failed_outcome_list`
- `TestFetchUrlContent::test_direct_thin_extract_raises_returns_the_direct_page`
- `TestDeployedConsumers::test_gatherer_receipt_records_provider_failure`

(At this point in the batch the test file used two separate
`TestTavilySearchWrapper`/`TestTavilyExtractWrapper` classes with 9 total
test functions; a later de-duplication pass — see §10, "Size" — merged them
into one parametrized `TestTavilyWrappers` class with identical assertions
and identical case count, purely to reduce line count. The failing-first
digest and 12/7 split are unaffected by that later restructuring — verified
by rerunning the full focused command after the restructuring, §8.)

The 7 passing controls on the unedited source: both genuine-empty-result
wrapper controls, the extract-empty-`urls` control, `test_invalid_key_error_text_unchanged`
(today's `_api_key_invalid` early return in `_execute_search` already
produces the right text as a side effect of `_tavily_search` setting the
flag before returning `[]` — a pre-existing regression guard, not a
rejection-shaped assertion), the zero-result `search()` control, the budget
control, and the agentic-consumer control (an `OutcomeList` is already
falsy with no `blocked` attribute by construction, independent of this
batch's source edit).

## 7. Source edit, then focused run

Guard re-checked (clear) before each pytest; MemAvailable checked before
every chunk (all ≥ 4000, see §8/§9 per-run readings).

`knowledge/web_search_manager.py` — 89 changed lines (73 insertions, 16
deletions): one new import (`utils.retrieval_outcome`), one new
`WebSearchResult.extract_error` field, `_tavily_search`/`_tavily_extract`
raise `RetrievalError` instead of returning `[]` (no-client and except
branches; existing invalid-key detection/flag/log lines preserved
byte-for-byte), `_execute_search`'s three wrapper call sites each wrapped in
`try/except RetrievalError`, and `fetch_url_content`'s extract call
similarly wrapped, returning `OutcomeList.failed(...)` on failure with
nothing free to fall back to. Full per-region rationale and the exact
diff shape are in the response file (`CGR-20260913-008.md`), "What changed
(summary)" — not duplicated here.

## 8. Focused run

MemAvailable 5427 MB ≥ 4000 (first run, before the test-bug fix below).

```
$ PYTHONPATH=.../scripts/bin systemd-run --user --scope -p MemoryMax=6G \
  -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
  OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q -p no:cacheprovider \
  -p asyncio tests/unit/test_tavily_failure_outcomes.py \
  tests/test_web_search_manager.py \
  tests/unit/test_sep12_followup_budget_outcomes.py \
  tests/unit/test_sep12_search_budget_reservation.py \
  tests/unit/test_sep12_web_evidence_budget.py tests/unit/test_url_fetch_layers.py
```

Result: **1 failed, 174 passed**, exit 1. The one failure was a bug in the
new test itself (`TestFetchUrlContent::test_direct_thin_extract_raises_returns_the_direct_page`
called the imported `_page(content=...)` helper positionally, passing the
content string as `url` instead) — not a source defect. Fixed in the test
file (keyword argument), then re-ran.

MemAvailable 5396 MB ≥ 4000 (rerun, post test-fix and post the size-driven
de-duplication in §10 — same command):

Result: **175 passed**, 0 failed, exit 0. Elapsed 0:15.16; Maximum resident
set size 1,768,480 KB. One chunk, 6 files — within the ≤9-file memory-chunk
rule.

## 9. Sweep

Every `tests/` importer of `knowledge.web_search_manager`, by actual import
statement (`grep -rnE "^\s*(import knowledge\.web_search_manager|from
knowledge\.web_search_manager|from knowledge import .*web_search_manager)"
tests --include=*.py`) — 14 files total; 5 already covered by the focused
run above. Remaining 9, one chunk (MemAvailable 5402 MB ≥ 4000, final rerun
after the size-driven edit):

```
tests/unit/test_evidence_transport.py
tests/unit/test_literature_oracle.py
tests/unit/test_web_fallback_general_intent.py
tests/unit/test_sep05_web_cache_same_query.py
tests/unit/test_audit0831_fixes.py
tests/unit/test_sep09_live_controls.py
tests/unit/test_sep12_keyword_boundary_and_budget.py
tests/unit/test_sep12_search_identity_scope.py
tests/unit/test_web_search_citations.py
```

Result: **287 passed**, 0 failed, exit 0. Elapsed 0:07.88; Maximum resident
set size 1,634,156 KB.

Combined sweep + focused: **462 tests passed, 0 failed, 0 skipped** across
15 files (the new file plus 14 pre-existing importers). **No existing test
required a FIXTURE RULE edit** — every pre-existing test that touches
`_tavily_search`/`_tavily_extract` either monkeypatches those wrapper
methods directly (bypassing this batch's raise/except machinery entirely,
so a bare `RuntimeError` a fake still raises reaches `search()`'s unchanged
outer backstop exactly as before — e.g.
`test_sep12_search_budget_reservation.py::TestChargeOnDispatch::test_provider_exception_still_charges_the_dispatched_base_cost`),
or never exercises the no-client/exception paths at all (uses `AsyncMock`
return values, or a genuinely reachable client). Verified by inspection
(grep for `_tavily_search`/`_tavily_extract`/`_tavily_client =`/`side_effect`
across all 9 sweep files before running) and confirmed by the green run.

`tests/test_web_search_manager.py` (non-unit, patches `tavily.TavilyClient`)
was read in full first, confirmed fully offline (`unittest.mock.patch` +
canned `MagicMock` return values, no real client ever raises), and run —
included in the focused chunk above, all its tests pass unmodified.

## 10. Ruff

`ruff 0.14.9`.

```
$ PYTHONPATH=.../scripts/bin python -m ruff check knowledge/web_search_manager.py \
  tests/unit/test_tavily_failure_outcomes.py
All checks passed!
```

## 11. Size

Initial draft: source edit 89 lines + a 389-line test file = 453 total
changed lines — **over the 450 hard stop**, discovered via `git diff --stat`
after writing both files and before handoff. Per "do not compress code or
comments to hit the number," the fix was structural de-duplication, not
compression: `TestTavilySearchWrapper` (4 tests) and `TestTavilyExtractWrapper`
(5 tests) — nine near-identical test bodies differing only in which wrapper
method and source label they exercise — were merged into one
`TestTavilyWrappers` class with `@pytest.mark.parametrize("source,call",
WRAPPER_CASES)` across four shared assertions plus the one extract-only
empty-`urls` control that has no search-side analogue. This preserves every
assertion and case count exactly (verified: 175 passed both before and after
the restructuring, §8). Seven purely decorative `# ---...---` ASCII divider
lines (zero words, immediately followed by a descriptive comment line that
already carried all the information) were removed, keeping every
descriptive comment's prose intact — not comment compression, removal of
content-free formatting. One test docstring was tightened from 8 lines to 5
without dropping its rationale. Final: `knowledge/web_search_manager.py` 89
lines (73 insertions, 16 deletions) + `tests/unit/test_tavily_failure_outcomes.py`
339 lines (new) = **428 total changed lines** — over the 300-line target
(driven by the brief's own enumerated TESTS list across wrapper-level,
`search()`-level, STANDARD-depth-partial, `fetch_url_content`, and two
deployed-consumer scenarios, each with a paired control), within the
450-line hard stop, confirmed by `git diff --stat` after the final edit.

## 12. Data directory (top-level, sizes and mtimes)

Before the first pytest:

```
total 60
drwxr-xr-x. 1 lukeh lukeh   150 2026-09-13 22:50:31.815565329 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-13 18:07:54.727650398 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
```

After the last pytest (the final sweep rerun):

```
total 68
drwxr-xr-x. 1 lukeh lukeh   220 2026-09-14 04:35:38.766908925 -0500 .
drwxr-xr-x. 1 lukeh lukeh   996 2026-09-13 18:07:54.727650398 -0500 ..
-rw-r--r--. 1 lukeh lukeh 53235 2026-09-13 12:28:07.844792269 -0500 benchmark_per_case.csv
drwxr-xr-x. 1 lukeh lukeh     0 2026-09-13 13:09:16.898881228 -0500 chroma_db_v4
drwxr-xr-x. 1 lukeh lukeh   100 2026-09-14 04:35:38.400904050 -0500 chroma_multi
-rw-r--r--. 1 lukeh lukeh   753 2026-09-13 12:28:07.846270384 -0500 embedding_migration_manifest.json
drwxr-xr-x. 1 lukeh lukeh   304 2026-09-13 12:28:07.846364181 -0500 pipeline
-rw-------. 1 lukeh lukeh    50 2026-09-14 04:35:38.763134691 -0500 web_search_credits.json
```

Two new entries: `chroma_multi/` and `web_search_credits.json`. Neither is
written by `tests/unit/test_tavily_failure_outcomes.py` (its own manager
constructions either use `object.__new__(WebSearchManager)` with no
`rate_limiter`/`cache` attribute at all, or `_make_manager(tmp_path, ...)`
from `test_sep12_search_budget_reservation.py`, whose rate limiter always
uses a `tmp_path` state file and whose cache is a `SimpleNamespace` stub —
neither ever touches `data/`). Both are the default-path side effects of
**pre-existing** tests in the required sweep set that construct a
`WebSearchManager()`/`WebSearchRateLimiter()` with no explicit
`state_file`/`chroma_store` override (default paths
`data/web_search_credits.json` and a default `MultiCollectionChromaStore()`
→ `chroma_multi/`) — unrelated to, and unchanged by, this batch's source
edit; those defaults exist in the unedited tree too. Nothing was deleted.

## 13. `multi_search` sibling and other siblings found

`multi_search`'s aggregation (≈2472, `error="; ".join(errors) if errors and
not all_pages else None`) drops a per-sub-query `RetrievalError` the instant
any other sub-query succeeds — the same BC-47 "aggregation keeps an error
only when the merged result is empty" shape the request packet's "Find"
column names. Out of scope for F2 (not a candidate anchor; `multi_search` is
explicitly read-only per ownership); recorded in the response file as a
sibling for a later batch, not fixed here.

`WebSearchManager._select_links_for_following` (dm18, same file, `except
Exception as e: log.debug(...); return []`, line 1810→1867) is a live
scanner finding in this file that is **not** in the request packet's
candidate-anchor table and **not** touched by F2 — out of ownership.
Recorded in the response file (§6 above); not fixed.

No BC-58 "first-observed path, enumerate every read/write site of the same
primitive" search applies beyond the above: `_tavily_search`/`_tavily_extract`
have exactly the callers the brief already enumerated (three sites in
`_execute_search`, one in `fetch_url_content`), all within this batch's own
file and ownership.

## 14. Privacy / no-network note

No network access, no LLM/paid API call, no daemon restart, no `pip
install`. Every fake exception message used in the new tests carries a
distinctive `..._MARKER` substring, and every assertion on
`RetrievalError.reason`/`str()`, `WebSearchResult.error`, and
`WebSearchResult.extract_error` checks that marker (and the literal
query/URL text) is absent — proving the privacy contract at both the
wrapper boundary and the `search()`/`fetch_url_content`/gatherer-receipt
boundaries, not just asserting it in prose. The one `test_gatherer_receipt_records_provider_failure`
consumer test deliberately stubs `manager._is_broad_news_query` to `False`
and supplies a fixed sync `trigger` decision with a single non-news search
term, specifically so `multi_search`'s LLM-decompose and semantic
broad-news-detection paths (`_semantic_broad_news_check`, an embedding
comparison — not gated by `auto_decompose=False`) are never reached; this
was verified by tracing the deployed `multi_search` control flow, not
assumed. `pytest` itself is the only subprocess spawned, via the required
`systemd-run`/`/usr/bin/time` wrapper. One process-hygiene note: during
initial exploration a single empty `python - <<'EOF' ... EOF` heredoc was
run (no code inside, no output, no effect) before realizing this counts as
non-pytest script execution requiring prior approval under the NON-PYTEST
CODE rule; it was not repeated, and all subsequent JSON inspection used
`jq`/`grep` via Bash instead. Disclosed here in full; no code executed, no
side effect, but the process rule was still not followed for that one call.

## 15. Milestone

Focused contract green: 175/175 passed (new file: 19 test functions,
23 with parametrize expansion, covering both wrappers × 4 shared cases +
1 extract-only control + 3 `search()`-level + 2 STANDARD-depth + 3
`fetch_url_content` + 2 deployed-consumer scenarios), 0 failed, 0 skipped,
exit 0. Sweep green: 287/287 passed across the remaining 9 importers, exit
0. Ruff clean on both changed files. Scan: 0 new findings anywhere; #110/#111
STALE as designed; `_select_links_for_following` and `multi_search`
recorded as siblings, not fixed. No `data/` deletion; two new entries are a
pre-existing-test side effect, not this batch's.

## 16. Open items / limitations

- `extract_error` is additive (`None` default); **no consumer reads it in
  F2** — F6 (per the design's batch table) wires it into a receipt.
- `multi_search`'s BC-47 aggregation sibling (§13) is recorded, not fixed.
- `WebSearchManager._select_links_for_following`'s own dm18 finding (§13) is
  recorded, not fixed — it is not a candidate anchor in this request.
- Anchors #101–#109 remain open, owned by F3a (`-2`), F3b (`-3`), and F4
  (`-4`).
- The one non-pytest heredoc process-hygiene note in §14: no code executed
  and no effect, but flagged transparently as a process deviation.
- No escalation: the manifest check, pre/post scans, and all test runs came
  back exactly as expected; the one test-authoring bug found in §8 was
  caught by the focused run itself and fixed within this batch, not a
  source-of-truth defect.

## Parent review and integration (2026-09-14)

Accepted. The parent made no source or test edit. One `data/` incident is recorded below; its root cause is a gap in the parent's brief, not the worker's code.

### Manifest

- `sha256sum -c manifest_post_F1.txt` reports no failures.
- The path-set delta is exactly four new paths: `knowledge/web_search_manager.py` (`6376dcb3…`, tracked and newly modified), `tests/unit/test_tavily_failure_outcomes.py` (`63b036a6…`), `class_guard_responses/CGR-20260913-008.md` (`1c832612…`) and this file.
- HEAD `328a8ec` is unchanged. Push is DISABLED, and `git stash list` is empty.
- The parent recorded the F6a/F6b split in `failure_outcome_design.md`'s batch table in the same window.
- `manifest_post_F2.txt` records the tree after this section.

### `data/` incident (recorded; nothing deleted)

**What changed.** `ls -la --time-style=full-iso data` before F2 (the parent's post-F1 snapshot) and after F2 differ by two NEW gitignored entries:
- `data/chroma_multi/` (dir, mtime 2026-09-14 04:35:38.40);
- `data/web_search_credits.json` (50 bytes, mtime 04:35:38.76).

No existing entry changed size or mtime.

**Cause, established by reading the code.**
- The existing non-unit file `tests/test_web_search_manager.py` constructs `WebSearchManager(api_key="test_key")` with no rate-limiter state file. That falls back to `WEB_SEARCH_CREDITS_PATH`, which defaults to `data/web_search_credits.json`.
- It then calls `search()` against a mock client that returns results. `WebSearchCache._ensure_initialized` lazily builds a default `MultiCollectionChromaStore()` at `data/chroma_multi` (`knowledge/web_search_manager.py` ≈953-955).
- F2's own new tests are clean. They use `tests/unit/test_sep12_search_budget_reservation.py::_make_manager`, which gives a `tmp_path` state file and a stub cache.

**Why the file ran.** The F2 brief said to read `tests/test_web_search_manager.py` "and run it if it stays offline". Offline was the wrong criterion: the file never touches the network but does write `data/`. That is the parent's error.

**Remedy.**
- `R_common_rules.md` now has a NON-UNIT TESTS rule. It names this file as a never-run offender and requires reading any non-unit test to confirm tmp paths or fakes first. It also requires a stop-and-report on any `data/` change.
- A persistent memory note records the offender.
- The parent's rerun below excludes the file and checks `data/` after every chunk.
- The two entries are left in place for the owner. They are clone-local and gitignored; deleting or keeping them is the owner's call.

### Process note (worker)

The worker ran one empty `python - <<'EOF'` heredoc (no code, no effect) before recognising the NON-PYTEST CODE rule. It was disclosed in §14 and not repeated. Recorded.

### Code review (the parent read the whole diff)

- **Import and new field.**
  - New import of `OutcomeList` and `RetrievalError` from the F1 leaf.
  - New `WebSearchResult.extract_error: Optional[str] = None`, additive.
- **`_tavily_search` / `_tavily_extract`.**
  - No client → `RetrievalError(reason="client_unavailable")`. `_tavily_extract([])` still returns `[]`, and that check comes before the client check.
  - Provider exception → the invalid-key detection, flag and log lines are kept byte for byte, then `raise RetrievalError(source=…, reason="invalid_api_key" | type(e).__name__) from e`.
  - A genuine empty provider result still returns `[]`.
- **`_execute_search`.**
  - Step 1 `except RetrievalError`: with `_api_key_invalid` set it returns the unchanged "Tavily API key is invalid"; otherwise it returns `error=f"Web search provider failed ({e.reason})"`. There is no query text, and both paths skip `record_usage` like today's invalid-key return.
  - Steps 2 and 3 `except RetrievalError`: the search pages are kept, `extract_error = "tavily_extract:<reason>"` is recorded, one warning without URL or query is logged, credits are still counted (the call was dispatched), and `error` stays None.
- **`fetch_url_content`.**
  - The extract call is wrapped, and the reservation still settles in `finally`.
  - If the direct fetch has content, it is returned. Otherwise the result is `OutcomeList.failed("tavily_extract:<reason>")`.
  - The budget paths are unchanged.
- **`search()`.** Unchanged. It still caches only `has_results`, and its outer handlers and settle are untouched.
- **Response file.** It is accurate: #110 and #111 fixed with deployed-function evidence; #101-#109 mapped to F3a/F3b/F4 (`-2`/`-3`/`-4`); the `multi_search` and `_select_links_for_following` siblings recorded.

### Parent rerun (guarded, capped, `-p asyncio`, chunks of ≤9, MemAvailable checked, `data/` diffed after EVERY chunk)

- **Set:** the new file plus every `tests/unit` importer of `knowledge.web_search_manager` / `WebSearchManager`, 17 files. `tests/test_web_search_manager.py` was deliberately excluded (above).
- **Result:** 229 (0:12.27, 1,722,864 KB) + 263 (0:14.03, 1,731,948 KB) = **492 passed**, 0 failed. MemAvailable was 5,351-5,385 MB.
- **`data/`:** identical to the post-F2 snapshot after each chunk. No further writes.
- **ruff** on both files: all checks passed.
- **Scan:**
  - dm18 now reads found 77, base 79, new 0, stale 2. The 2 new STALE rows are exactly `WebSearchManager._tavily_search` and `_tavily_extract` (#110/#111), the designed class-guard handoff.
  - Every other gate scanner shows 0 new.
  - Exit 1 comes from 10 STALE rows: 5 dm01, 3 dm17, 2 dm18.

### Size

428 changed lines (89 source + 339 test), under the 450 hard stop. The worker parametrized to get there, with no prose compression.

### Follow-ups

- `extract_error` is surfaced by F6b/F8.
- The `multi_search` BC-47 aggregation sibling.
- `_select_links_for_following`'s dm18 finding.
- Owner decision on the two clone-local `data/` entries.
