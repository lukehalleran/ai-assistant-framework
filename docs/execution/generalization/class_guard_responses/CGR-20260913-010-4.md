# CGR-20260913-010-4 — ProposalStore.store_proposal's failure-as-skip return (#131)

Request: CGR-20260913-010. Owning batch: F11b-1 (this is the `-4` follow-up
to the F10a base response `CGR-20260913-010.md` (#127, #140), the F10b
follow-up `CGR-20260913-010-2.md` (#128, #129) and the F11a-1 follow-up
`CGR-20260913-010-3.md` (#146)). Scanner: `dm18_except_returns_empty` (gate;
BC-47 on the write side).

## SIZE-driven split (disclosed)

The brief's SIZE line pre-authorized a split if the full batch scope overran
the 450-line hard cap: "store + filter/gatherer evidence vs the two loop
callers + CLI." Isolating this batch's own edits from earlier batches'
overlapping hunks in `memory/shutdown_processor.py` and `main.py` put the
full scope at 508 changed lines — over the cap. **#131 itself (the store's
own raise) IS fixed and delivered in this batch** — it lives in
`memory/proposal_store.py`, which no earlier batch had touched, so it carries
none of the overrun. What is DEFERRED is the brief's planned hardening of
#131's TWO batch-loop callers (`ShutdownProcessor._generate_proposals` and
`agent_branch.proposal_bridge.ingest_survivors`) to catch the newly-typed
`StoreWriteError` per item, plus the related `main.py` `check-proposals` CLI
read-side wrap (that one belongs with `CGR-20260913-009-5.md`'s #132-#136,
not this write-side anchor, but is tracked here too since the brief grouped
it with the loop callers). Full accounting: `batches/F11b-1.md`, "Size and
split" / "Source edit" sections.

## Changed files (SHA-256, before -> after)

Before = the digests recorded at this batch's manifest check (matching
`manifest_post_F10c.txt`).

| File | Before | After |
|---|---|---|
| `memory/proposal_store.py` | `f26ba6a5cd14ebd0ea3c5d06f6555fe83a85728cb2bbf52bf1206331890877d3` | `f9262df198a49ce798dbd6cb2ce69dc180eb37851e93a385aba54a7d015cd419` |

New file (not a "changed" file, but part of this fix):
`tests/unit/test_proposal_store_outcomes.py`,
`21972a1605f21ce70705b7390d4b32aa8cc13f69013de2733775412746c8534f` (317
lines). Shared with `CGR-20260913-009-5.md` above (the read anchors' tests
live in the same file).

`memory/shutdown_processor.py`, `agent_branch/proposal_bridge.py` and
`main.py` were edited (adding the typed catches described below) and then
REVERTED to their exact pre-batch digests (`74c9ed30…`, `3c7c23a2…`,
`6ef42c51…` respectively — verified by `sha256sum` after the revert) once
the SIZE overrun was discovered. They are NOT changed files in this
response; their intended edits are recorded here for the follow-up batch.

## Focused deployed-function test command

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q \
  -p no:cacheprovider -p asyncio tests/unit/test_proposal_store_outcomes.py tests/unit/test_proposal_store.py
```

Exit code 0. Full focused set (9 files, 1 chunk): **219 passed, 0 failed**.
Sweep set (8 files, 1 chunk): **271 passed, 0 failed**. `data/` unchanged
after every chunk (only the benign repo-root `..` mtime line); `logs/`
absent throughout.

Failing-first (on the unedited, post-F10c source, digests printed in the
same command, full 412-line test file covering every anchor including the
deferred loop-caller ones): **8 failed, 10 passed** — of the 8, the failure
relevant to THIS response (#131) is
`TestStoreProposalRaises::test_add_raises_raises_store_write_error`
("DID NOT RAISE StoreWriteError" — the old code returned `None`). The two
loop-caller failures observed in that same failing-first run
(`TestShutdownGenerateProposalsBatchLoop::test_one_failed_write_is_counted_second_still_stored`
and `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored`)
are evidence for the DEFERRED work below; their tests were removed from the
delivered file along with the reverted source edits (they will need an
independent failing-first re-run in the follow-up batch, per the standard
rule that a relaunched/continued worker's failing-first proof is
self-contained). Full failing-first detail in `batches/F11b-1.md`.

## Anchor disposition

| Review # | Symbol | Kind | Disposition | Evidence |
|---:|---|---|---|---|
| 131 | `ProposalStore.store_proposal` | `broad_except_returns_empty` | **Fixed** | The `except Exception as e:` keeps its error log line, then raises `StoreWriteError(source="proposal_store", reason=type(e).__name__) from e` instead of `return None`. Deployed-function test: `test_add_raises_raises_store_write_error` drives the real `ProposalStore.store_proposal` with `chroma_store.add_to_collection` raising `RuntimeError`, asserts `pytest.raises(StoreWriteError)` with `.source == "proposal_store"` and that the injected marker text never reaches `str(err)`/`err.reason`. Paired deliberate-skip control (unchanged, still returns `None`): `test_skip_control_returns_none` (`chroma_store=None`). |

## Batch-loop continuations: drafted, verified, then DEFERRED (not delivered)

Both edits were written, digest-verified against the brief's exact anchors,
and exercised against a failing-first run before being reverted for SIZE.
They are recorded here in full so the follow-up batch can apply them
verbatim (subject to its own independent failing-first proof, per
R_common_rules' INTERRUPTION-adjacent guidance that a continuation's proof
must be self-contained).

- **`ShutdownProcessor._generate_proposals`** (`memory/shutdown_processor.py`,
  the `store_proposal` call, `kept` count and its log line, ≈1259-1264 per
  the brief — confirmed at that location before editing): add a
  `failed = 0` counter next to `kept = 0`; wrap
  `doc_id = proposal_store.store_proposal(proposal)` in
  `try: ... except StoreWriteError: failed += 1; continue`; change the
  summary log from `if kept: logger.info(f"... Generated {kept}
  proposal(s)")` to `if kept or failed: logger.info(f"... Generated {kept}
  proposal(s), {failed} failed")` — the same shape F10b used for the skill
  loop and F11a-1 used for the thread-store loop. Drafted test (removed with
  the source revert, to be reinstated by the follow-up batch):
  `TestShutdownGenerateProposalsBatchLoop::test_one_failed_write_is_counted_second_still_stored`
  (two proposals, `store_proposal.side_effect = [StoreWriteError(...), "doc-2"]`
  -> both calls attempted, log shows "1 failed") and the paired control
  `test_similarity_skip_control_not_counted_as_failed` (a dedup skip via
  `check_similarity` never reaches `store_proposal` at all).
- **`agent_branch.proposal_bridge.ingest_survivors`** (the `store_proposal`
  call and the lines that use its result, ≈242-246 per the brief — confirmed
  at that location before editing): wrap
  `doc_id = store.store_proposal(proposal)` in `try: ... except
  StoreWriteError as e: logger.warning("ingest: store failed for survivor
  %s (%s)", sb.branch_id, e.reason); continue` — a failed survivor reaches
  neither `seen` nor `stored`; the return shape (`List[str]`) is unchanged.
  Drafted test (removed with the source revert):
  `TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored`
  (two survivors via a local fake store whose `store_proposal` raises
  `StoreWriteError` on the first call only — asserts both calls were
  attempted and exactly one id was returned), built entirely from fakes
  (a `SimpleNamespace`-based `BranchReport` stand-in matching
  `tests/agent_branch/test_proposal_bridge.py`'s established `_report()`
  shape, `agent_branch.scoring.RankedPortfolio`/`ScoredBranch`) — no real
  store, model or filesystem was needed, so no limitation applies to this
  drafted test itself.

**Consequence disclosed.** Until the follow-up batch lands these two
continuations, `store_proposal`'s new `StoreWriteError` (raised as of THIS
batch) is not yet caught per item by either loop caller. A single failed
write during `_generate_proposals` now propagates to that method's own
pre-existing, unchanged outer `except Exception as e: logger.warning(f"...
Proposal generation failed: {e}")`, abandoning the REST of that session's
proposal batch instead of skipping just the one item; a single failed write
during `ingest_survivors` now propagates uncaught to its only caller,
`agent_branch/portfolio.py`'s own pre-existing try/except
(`"portfolio: proposal ingest failed"`), abandoning the rest of that
portfolio's survivors. Both are coarser-grained than the eventual per-item
degrade, but NOT silent — both existing outer handlers already log the
failure today, so this is a visible, logged behaviour change (a batch that
used to silently under-count now visibly aborts early), not a new class of
invisible failure. The parent should prioritize the follow-up batch to close
this window.

## `main.py check-proposals` CLI: drafted, verified by reading, then DEFERRED

Owner decision, CONTRACT item 7 of the brief. Anchors confirmed exactly (as
in the brief) before editing: `elif mode == "check-proposals":` at 1530,
`store.get_proposal(specific_id)` at 1564, `store.get_pending_and_approved()`
at 1570. Drafted edit: wrap both reads in one
`try: ... except RetrievalError as e: print(f"Could not read proposals:
{e}"); sys.exit(2)`; not-found (exit 1) and "No pending/approved proposals"
(exit 0) unchanged. Reverted with the rest of Part B. `main.py` opens the
real `CHROMA_PATH` store in this branch and is never run either way, per
R_common_rules — the eventual fix is verified by reading only, exactly as
planned. Full detail: `CGR-20260913-009-5.md`.

## Scan (dm18_except_returns_empty)

| When | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| Pre-edit (post-F10c) | gate | 123 | 38 | 79 | 0 | 41 |
| Post-edit (final) | gate | 123 | 33 | 79 | 0 | 46 |

`memory/proposal_store.py [ProposalStore.store_proposal] 'return None'`
(#131) now appears in the scanner's STALE list — the class-guard owner
should remove that row and mark #131 `confirmed_fixed` with this request ID
(alongside #132/#133/#134/#136 in `CGR-20260913-009-5.md`, the other four new
STALE rows from the same file — all five moved together, `found` 38 -> 33).
No new finding anywhere (new = 0 before and after). Full detail in
`batches/F11b-1.md`.

## Privacy

No real institution names or real IPs. The injected-exception marker
`F11BQZ7` never reaches `StoreWriteError.reason`, `str(StoreWriteError(...))`,
or any caplog-captured NEW log line in the delivered test suite (explicit
assertions in the raising test).

## Remaining CGR-010 anchors (not this response)

- The two batch-loop continuations and the `main.py` CLI wrap (drafted,
  verified, reverted for SIZE — see above) are DEFERRED to a follow-up
  batch, which should also re-run an independent failing-first proof before
  reapplying them.
- `update_status` (staged replacement, following the F11a-2 precedent — a
  missing collection handle returns `False` instead of storing a duplicate)
  and `update_tracking_metadata` (returns `update_metadata`'s bool instead
  of always `True`) — no anchor, scheduled for F11b-2 per the design doc's
  "F11b split" amendment.
- Receipts (`_generate_proposals`'s counterpart across other write sites) ->
  F13a/F13b/F13c per the brief's original routing.

## Limitations

- The two batch-loop continuations and the `main.py` CLI wrap are DEFERRED
  (see above) — this is the batch's one disclosed SIZE-driven deviation from
  the brief.
- `ProposalStore.check_similarity` and `get_for_dedup` are unchanged
  (F11b-2's territory per the design doc) — recorded siblings only, detailed
  in `CGR-20260913-009-5.md`.
- `ProposalStore._ensure_collection`'s "failure disguised as not-configured"
  is unchanged — a BC-58 sibling, recorded only.
