# CGR-20260913-009-6 — Consolidator status-facts and Obsidian readers report failure instead of an empty list (#117, #118, #119, #120)

Request: CGR-20260913-009. Owning batch: F12a (this is the `-6` follow-up to
the base response `CGR-20260913-009.md` (#139), `-2` (#122), `-3` (#130),
`-4` (#121, #147, #148) and `-5` (#132, #133, #134, #136)). Scanner:
`dm18_except_returns_empty` (gate; BC-20, BC-47).

## Changed files (SHA-256, before -> after)

Before = the digests recorded at this batch's manifest check (matching
`manifest_post_F11b-2.txt`, re-verified against the brief's own stated
digests).

| File | Before | After |
|---|---|---|
| `memory/memory_consolidator.py` | `c00177c75e428643c94b75a2df68efdf49ba33935f0f8dd8d1671d7c37f4f628` | `60b05ebaab6e4a27995d5c7b5be27294e931ae450c0c5787223b4b78eada9317` |
| `main.py` | `18dd3b912585b6a0eb5eaadd4126abca91b248c8552e8c23fda01a34ae0c6598` | `f44cf7d617eea91ac78c018d1af5601f19e998baa7522c225b4666c491189c6b` |

`main.py` already carried a 23-line diff from an earlier batch (F10c's
one-line `inspect_summaries` fix plus other prior hunks); my own hunk was
isolated by diffing a pre-edit copy of the file and measures 8
insertions/3 deletions (11 lines), confined to the three direct reader calls
inside `refresh_narrative_context` (lines 1336-1347).

New file (not a "changed" file, but part of this fix):
`tests/unit/test_consolidator_read_outcomes.py`,
`9383eda4bb3376bb3d108e770941bb2027f0a6d77cbadc2099269a04a1aaa23e` (228
lines).

## Focused deployed-function test command

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_consolidator_read_outcomes.py \
  tests/unit/test_narrative_status_claims.py \
  tests/unit/test_sep05_evening_turn_audit.py \
  tests/unit/test_completed_plan_claims.py \
  tests/unit/test_temporal_claim_audit.py \
  tests/unit/test_user_identity.py \
  tests/unit/test_retrieval_outcome.py \
  tests/unit/test_summaries_read_fix.py \
  tests/unit/test_skill_and_summary_outcomes.py
```

Exit code **0**, **187 passed, 0 failed**, 0:05.99 elapsed, Maximum resident
set size 1,106,028 KB. The sweep (`grep -rl "memory_consolidator\|
MemoryConsolidator\|status_claims" tests/unit`) matched exactly these same 5
consolidator/status-claims files (already in the focused set above) — the
sweep set was empty; no other `tests/unit` file imports the changed symbols.

Failing-before (unedited source, same test file added): 7 failed, 10 passed.
Full command and per-line output in `batches/F12a.md`, "New test file +
failing-first".

## Per-anchor disposition (deployed-function evidence)

- **#117 `MemoryConsolidator._current_status_facts`** (was line 216 `return
  []`, now raises): `profile is self.user_profile is None` ->
  `RetrievalError(source="status_facts", reason="profile_unavailable")`;
  `profile.get_current_view()` raising -> keeps its `logger.debug` line,
  then `RetrievalError(source="status_facts", reason=type(e).__name__) from
  e`. A genuine empty filter result (healthy profile, no `STATUS_RELATIONS`
  match) is unchanged: `[]`. Tests:
  `TestCurrentStatusFacts::test_profile_unavailable_raises_retrieval_error`,
  `::test_get_current_view_raising_raises_retrieval_error` (both were DID
  NOT RAISE before the fix, pass after), paired with
  `::test_healthy_profile_no_status_relations_returns_empty` and
  `::test_healthy_profile_with_status_fact_returns_it` (controls, passed
  before and after — the empty/healthy shape is unchanged).
- **#118 `MemoryConsolidator._read_obsidian_weekly_summaries`** (was line
  490 `return []`, now raises `RetrievalError(source="obsidian_weekly",
  reason=type(e).__name__) from e` in the outer except; keeps its
  `logger.debug` line). The no-path skip (`return []` when
  `_get_obsidian_notes_path()` returns `None`) and the inner per-file
  frontmatter `except Exception: pass` are unchanged.
- **#119 `MemoryConsolidator._read_obsidian_monthly_summaries`** (was line
  552 `return []`, now raises `RetrievalError(source="obsidian_monthly",
  reason=type(e).__name__) from e`), same shape as #118.
- **#120 `MemoryConsolidator._read_obsidian_daily_notes`** (was line 618
  `return []`, now raises `RetrievalError(source="obsidian_daily",
  reason=type(e).__name__) from e`), same shape as #118.
  - All three readers tested with one parametrized set
    (`TestObsidianReaders`, `READER_CASES` over the 3 methods):
    `test_read_failure_raises_retrieval_error` (a `tmp_path` note layout
    with `Path.read_text` monkeypatched to raise `OSError` — DID NOT RAISE
    before the fix for all 3, passes after, with the correct per-reader
    `source` label and `reason == "OSError"`), paired with
    `test_no_notes_path_returns_empty` (control: `[]`, unaffected, passed
    before and after) and `test_healthy_tmp_note_is_parsed` (control: a real
    `tmp_path` note is discovered and parsed with the right `source` and
    `timestamp`, unaffected, passed before and after).
- **The narrative "not regenerated" path** (`generate_narrative_context`,
  not itself an anchor but the CONTRACT's item 3): one new
  `except RetrievalError as e:` clause added immediately before the
  pre-existing `except Exception as e:` (same enclosing `try`, no
  restructuring) — logs
  `"[NarrativeSynthesis] Narrative not regenerated: inputs unavailable
  ({e.source}: {e.reason})"` then `return ""`, BEFORE any LLM call or
  partial render, because the readers (lines ~667-669) and
  `_current_status_facts` (line ~716) sit inside that same `try` and now
  raise before reaching the prompt-building or LLM-call code below them.
  Tested: `test_raising_reader_returns_empty_and_skips_llm` and
  `test_raising_status_facts_returns_empty_and_skips_llm` — both asserted
  `result == ""`, the stub `generate_once` never called (`mm.calls == 0`),
  and the NEW distinguishing log text `"Narrative not regenerated"` present
  in caplog (this specific assertion failed on the unedited source, which
  only emitted the old generic `"Failed to generate narrative: {e}"`
  message — a first draft of these two tests used a looser caplog check
  that happened to also match the old message by coincidence, since the
  injected `RetrievalError.__str__` is `"source: reason"`; tightened before
  treating it as failing-first proof — see `batches/F12a.md`). Paired
  controls: `test_all_sources_empty_returns_empty` (all three readers
  return `[]`, no `RetrievalError` involved — unchanged `""` short-circuit,
  passed before and after) and
  `test_healthy_inputs_keep_status_guard_and_remove_conflict` (a healthy
  profile with a current `enrolled_in` fact — the authoritative-facts block
  reaches the prompt and a contradicting generated sentence is removed by
  the existing `remove_conflicting_claims` guard, passed before and after —
  confirms the new except clause does not touch the healthy path).
  **No caller persists an empty result** (`main.py:1391-1406`,
  `memory/memory_storage.py:1610` `_maybe_regenerate_narrative`,
  `utils/daily_notes_generator.py:962` `_trigger_narrative_refresh` all only
  save a truthy narrative), so no guard-less or partial narrative is ever
  written to `NARRATIVE_CONTEXT_PATH` on this path — this is
  "`status_guard=unavailable`" per the design doc's CGR-009 decision. The
  external return-value contract of `generate_narrative_context` (`""` on
  any internal failure, a string on success) is UNCHANGED for every one of
  its 3 callers (grep-verified, see "BC-58 sibling search" in
  `batches/F12a.md`) — this batch only replaces a silent partial-input path
  with an explicit, correctly labelled short-circuit that was already the
  return type callers expect.
- **`main.py refresh_narrative_context`** (CLI, verified by reading only —
  NEVER run per the brief): the three direct reader calls (≈1337-1341,
  outside `generate_narrative_context`, used only for the CLI's own
  diagnostic print lines) are now wrapped in
  `try: ... except RetrievalError as e: print(f"\n✗ Could not read Obsidian
  notes ({e.source}: {e.reason})"); return False`. `return False` flows
  into `success = asyncio.run(refresh_narrative_context())` ->
  `sys.exit(0 if success else 1)`, i.e. exit 1 — the SAME non-zero exit
  status the pre-existing `"✗ Failed to generate narrative context"` /
  `return False` path already used, with no Python traceback. The
  `generate_narrative_context` call site (≈1391) and its own "Failed"
  branch (≈1404-1406) needed no edit, for the reason above.

## Before/after scan tables (dm18_except_returns_empty)

| | found | base | new | stale |
|---|---:|---:|---:|---:|
| Pre-edit | 33 | 79 | 0 | 46 |
| Post-edit | 29 | 79 | 0 | 50 |

`new=0` both times — the new `except RetrievalError` clause did not trip the
gate as a new finding (same typed-catch shape accepted for F10b/F10c). All
four anchors (#117-#120) moved into the STALE list after the edit,
confirming the fix removed the exact `return []` pattern the scanner
matched. Per the request's "Class-guard acceptance" section, these four rows
should be removed and marked `confirmed_fixed` with this request ID once
integrated into a tree the class-guard checkout can scan.

## Siblings (record only — not fixed by this response)

- **`user_profile` property** (`memory/memory_consolidator.py:189-202`):
  still swallows a `UserProfile()` constructor failure and returns `None`.
  Read-only for this batch (not in OWNERSHIP); its docstring ("Failure is
  non-critical: callers fall back to an empty status-facts list...") is now
  slightly stale, since `_current_status_facts` no longer falls back
  silently — it raises `RetrievalError(reason="profile_unavailable")`
  instead. Not edited; flagged here rather than left undocumented.
- **`_get_obsidian_notes_path`** (`:408-424`): still returns `None` on any
  exception (a deliberate "vault not found" skip, distinct from a read
  failure once the path IS found). Unchanged, per the brief.
- **The streak ledger swallow** inside `generate_narrative_context`
  (`except Exception as e: ... streak_claims = []`): unchanged, per the
  brief — a genuine best-effort fallback, not one of #117-#120.
- **`utils/daily_notes_generator.py`'s own duplicate `_current_status_facts`**
  (`:440`, called `:801`) and its own narrative-refresh trigger
  (`_trigger_narrative_refresh`, `:962`): confirmed by grep to be a
  structurally identical sibling (BC-58), explicitly assigned to **F12b** by
  the parent's F12 split design decision. Not touched here.
- Remaining CGR-009 anchors: `memory/memory_retriever.py` #123, #125, #126
  → **F12c** (response `-7`); `memory/synthesis_memory.py` #141-#143 →
  **F12d** (response `-8`, which completes CGR-009).

## Limitations

- `main.py refresh_narrative_context` was verified by reading only; per the
  brief, it is never executed (constructs a real `ModelManager()` and
  `CorpusManager()`).
- The `user_profile` property's docstring is now slightly stale (see
  "Siblings" above) but is read-only for this batch.
- The sweep set for this batch was empty (every `tests/unit` importer of
  the changed symbols was already in the focused set); no broader
  regression sweep across all of `tests/unit` was run, per the brief's
  narrower sweep definition (grep on the specific changed symbols).
- One self-disclosed process breach: a stray non-pytest `python -c
  "print('noop')"` (piped to `head -0`, no output, no project import) was
  run once while checking diff size. Recorded in `batches/F12a.md`; no
  rework needed.
