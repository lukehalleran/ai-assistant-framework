# Response to CGR-20260913-007 (follow-up -4) — recent conversations,
# semantic memories, user profile and upcoming schedule report failure
# distinctly; facts/reflections answered off-path

Immutable response file, written 2026-09-14 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch (this
response): **F8a** (`docs/execution/generalization/batches/F8a.md`,
`core/prompt/gatherer_memory.py`, and new
`tests/unit/test_gatherer_outcomes_memory.py`). This `-4` response answers
anchors **#87, #88, #90 and #91** with code
(`_get_recent_conversations`, `_get_semantic_memories`,
`get_user_profile_context`, `get_upcoming_schedule`) and **#85, #86 and
#89** with off-path evidence only, no code (`get_recent_facts`,
`get_facts`, `_get_reflections`) — the base response
(`CGR-20260913-007.md`, F7a) answered #71-#74; `-2` (F7b) answered #80;
`-3` (F7c) answered #75-#79 and #81-#84. **#92**
(`core/prompt/gatherer_web.py` `_get_web_search_results`) remains open for
**F8b**, response `CGR-20260913-007-5`.

## Request ID / owning batch

- Request: `CGR-20260913-007` (scanner `dm18_except_returns_empty`; BC-20,
  BC-47, CM-05)
- Owning batch (this response): `F8a`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, no rebase; HEAD confirmed identical before and
  after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `core/prompt/gatherer_memory.py` | `2cbe777580a97aa06a35b9d224ca5e56f86ff57b6ff73b59595504d8e34efd68` | `b994315ce47f5102d7b05d43a8bc0e3a1d7641c6e2418d8384e3098f4677e640` |
| `tests/unit/test_gatherer_outcomes_memory.py` | (new file) | `c21be821b64a3da2270611df239a5acf67438eab8f0b0c63075dbb30277157df` (350 lines) |

The before-digest matches the request packet's recorded source SHA-256 for
#85-#91 exactly (`2cbe7775…`), reconfirmed by `sha256sum` immediately
before any edit and again inside the same failing-first command that
produced the counts below.

## What changed (summary)

`core/prompt/gatherer_memory.py` (25 changed lines: 21 insertions, 4
deletions — ownership scope only: one import line plus the bodies of
`_get_recent_conversations`, `_get_semantic_memories`,
`get_user_profile_context` and `get_upcoming_schedule`):

- One new import: `from utils.retrieval_outcome import OutcomeList`.
- **`_get_recent_conversations`** (#87): the ANCHOR except keeps its
  existing `logger.warning` line and returns
  `OutcomeList.failed(type(e).__name__)` instead of `[]`. The
  FALLBACK-only inner try/except (183-194, a distinct site) is unchanged
  — a sibling, recorded below.
- **`_get_semantic_memories`** (#88): `retrieval_err = None` is
  initialised right after `semantic_memories = []`, before the inner
  `try:`. The inner `except Exception as e:` (the coordinator-read
  swallow) keeps its existing `logger.warning` line and gains one line,
  `retrieval_err = type(e).__name__`. At the success return, when
  `retrieval_err` is set, the method returns `OutcomeList(<final list>,
  status="failed", reason=f"retrieval:{retrieval_err}")` (the items, if
  any survived a partial pool, are kept); otherwise the final list is
  returned exactly as today. The outer ANCHOR except keeps its existing
  `logger.error`/traceback-debug lines and returns
  `OutcomeList.failed(type(e).__name__)` instead of `[]`. The `if not
  query: return []` gate is untouched.
- **`get_user_profile_context`** (#90, `str`-returning): the ANCHOR
  except keeps its existing `logger.warning` line, then `raise`s instead
  of `return ""` (mirrors F7c's `get_narrative_context` precedent — the
  builder's own gather loop, F5-integrated and unedited by this batch,
  already records the raise as `_section_outcomes["user_profile"] =
  {"status": "failed", "reason": type(e).__name__}`, and the gathered
  value becomes `[]`, which every consumer already treats as "no
  profile"). The `if not self.user_profile: return ""` gate above the try
  is untouched.
- **`get_upcoming_schedule`** (#91): the ANCHOR except keeps its existing
  `logger.warning` line and returns `OutcomeList.failed(type(e).__name__)`
  instead of `[]`. All four legit early returns above the try
  (`SCHEDULE_EXTRACTION_ENABLED` off, no chroma store, empty collection,
  no schedule facts) are untouched, still `return []`.
- `tests/unit/test_gatherer_outcomes_memory.py` (new, 350 lines) — drives
  the DEPLOYED `MemoryRetrievalMixin._get_recent_conversations`,
  `_get_semantic_memories`, `get_user_profile_context` and
  `get_upcoming_schedule` directly through a bare-host `_G(coordinator,
  user_profile=...)` helper (`MemoryRetrievalMixin.__new__` + attributes —
  never a real `UserProfile()`/`ContextGatherer()`), plus the DEPLOYED
  `UnifiedPromptBuilder.build_prompt`/`_build_lightweight_context` through
  the `full_builder`/`retrieval_limits` fake-builder pattern.
  `SimpleNamespace`/`AsyncMock` fakes only — no real Chroma, corpus store,
  UserProfile file, embedder or network anywhere.

No other line in `core/prompt/gatherer_memory.py` changed.
`get_recent_facts`, `get_facts`, `_get_reflections`,
`_get_summaries_separate`, `_get_reflections_separate` and every other
method in the file are byte-for-byte unchanged.

## Per-anchor disposition (deployed-function evidence)

All four code-fixed anchors are real BC-20/BC-47 instances (not
"not-an-instance" dispositions); the new test file proves the pre-patch
defect directly on the unedited source (6 failed / 10 passed at digest
`2cbe7775…` — see "Focused test command" below).

- **#87** `_get_recent_conversations` (line 229), `return []`. Fixed.
  Evidence: `TestRecentConversationsOutcomes::test_raising_corpus_manager_is_failed_and_empty`
  (the anchor), `::test_healthy_nonempty_returns_todays_items`,
  `::test_healthy_empty_is_no_results` (unaffected controls),
  `::test_fallback_only_swallow_is_unchanged_sibling` (the sibling).
  The contract's "#87 must return typed, never raise" requirement is
  proven at the builder level by
  `TestLightPathUnchangedByTypedReturn::test_build_lightweight_context_stays_on_success_path_when_store_raises`:
  through the DEPLOYED `_build_lightweight_context` (builder.py:2309,
  the direct caller at builder.py:2314), a raising recent-memories store
  still produces the NORMAL success dict, never the except's DIFFERENT
  fallback dict.
- **#88** `_get_semantic_memories` (line 634), `return []`. Fixed as
  described above. Evidence:
  `TestSemanticMemoriesOutcomes::test_raising_coordinator_get_memories_is_failed_retrieval_prefixed`
  (inner swallow → `failed`/`"retrieval:ValueError"`),
  `::test_exception_outside_inner_try_is_failed_class` (a raising
  `_deduplicate_memories`, outside the inner try, → `failed`/`RuntimeError`
  via the outer anchor), `::test_empty_query_is_no_results`,
  `::test_healthy_returns_todays_items`.
- **#90** `get_user_profile_context` (line 964), `return ""`. Fixed: the
  except now re-raises. Evidence:
  `TestUserProfileOutcomes::test_raising_get_context_injection_reraises`
  (direct: `pytest.raises(RuntimeError)`),
  `::test_no_profile_returns_empty_string` (the legit gate, untouched),
  `::test_through_builder_raise_recorded_failed_and_prompt_still_builds`
  (the brief's explicit builder-level requirement: the DEPLOYED
  `build_prompt` still builds, `_section_outcomes["user_profile"] ==
  {"status": "failed", "reason": "RuntimeError"}`, and
  `result["user_profile"]` is falsy).
- **#91** `get_upcoming_schedule` (line 1070), `return []`. Fixed as
  described above. Evidence:
  `TestUpcomingScheduleOutcomes::test_raising_query_collection_is_failed_class`,
  `::test_disabled_flag_is_no_results` (legit gate, untouched),
  `::test_healthy_returns_todays_slice` (an all-7-weekday recurring
  fixture, deterministic regardless of the run date).

## #85, #86, #89: answered as NOT on the prompt path (no code)

- **#85** `get_recent_facts` (line 155/156): `try` 146-154; producer
  `await self.memory_coordinator.get_recent_facts(limit)` (148); capability
  fallback `return await self.get_facts(limit)` (152); `except Exception`
  153 → `return []` 154-155.
- **#86** `get_facts` (line 167/168): `try` 159-166; producer
  `memory_coordinator.get_facts` (161); capability gate 163-164; except
  165 → `return []` 167.
- **#89** `_get_reflections` (line 799/810): except 797-799 → `return []`.
- **Grep evidence** (`grep -rn "\.get_recent_facts(\|\.get_facts(\|\._get_reflections(" --include=*.py .`,
  excluding `tests/` and `gatherer_memory.py`): no production caller
  invokes the MIXIN's `get_recent_facts`, `get_facts` or `_get_reflections`.
  - `core/context_pipeline.py:973` calls `self.memory_system.get_facts` —
    the memory COORDINATOR's own method (a different class).
  - `memory/shutdown_processor.py:1313` calls `mc.get_facts` — same
    coordinator method.
  - `memory/memory_coordinator.py:328/335` (`get_recent_facts`/`get_facts`)
    delegate to `self._retriever` (`MemoryRetriever`, a different class).
  - `scripts/sample_real_benchmark.py:552` / `sample_real_benchmark_v2.py:555`
    call a retriever's `get_facts`.
  - The builder (`core/prompt/builder.py`) has no task named "facts" or
    bare "reflections" — the gather loop uses `_get_summaries_separate`
    (builder.py:1289) and `_get_reflections_separate` (builder.py:1307)
    instead.
- **Deployed builder test** (contract point 5, brief-mandated):
  `TestOffPathAnchorsEvidence::test_facts_and_reflections_mixin_methods_never_called_through_builder`
  monkeypatches the three MIXIN methods on the class to record a call and
  raise `AssertionError`, wires a bare `MemoryRetrievalMixin` instance as
  `context_gatherer` (with a coordinator that answers the REAL,
  unedited-by-this-batch `_get_summaries_separate`/`_get_reflections_separate`
  the builder actually calls), enables `max_reflections`/`max_summaries`
  in the retrieval overrides, and asserts the deployed `build_prompt`
  succeeds (`"_build_time" in result`) with the spy call list staying
  empty. This test passes both before and after this batch's edit (by
  design — none of the code paths it exercises touch the bare
  facts/reflections methods either way).
- **Class-guard decision:** these three anchors are left `return []`
  unchanged; the class-guard owner decides between a detector change
  (excluding structurally off-path methods from dm18) and continued
  `accepted_debt`, per the request packet's own "Class-guard acceptance"
  section. The post-edit scan (below) confirms all three stay LIVE, by
  design, with `new = 0`.

## Siblings recorded (BC-58)

- **The fallback-only inner swallow** in `_get_recent_conversations`
  (183-194, `except Exception as e: logger.warning("Fallback memory
  retrieval failed: ..."); ` — no `return`, falls through): a distinct
  site from the ANCHOR except, left unchanged — proven unaffected by
  `TestRecentConversationsOutcomes::test_fallback_only_swallow_is_unchanged_sibling`
  (a raising fallback with a healthy partial corpus_manager result still
  succeeds, not `failed`).
- **`_get_summaries_separate`** (312, except 386-388) and
  **`_get_reflections_separate`** (819, except 893-895) remain
  always-truthy dict shapes (`{"recent": [...], "semantic": [...]}`) even
  on their own except — the builder's `_section_outcomes` reads a failure
  inside either as `succeeded`/`no_results` today. Neither is a packet
  anchor. **Decision (parent, recorded at F8 Ready): not fixed in F8a;
  recorded as a limitation** — fixing this needs a builder change to read
  a dict section's status, deferred to whichever batch next reopens the
  builder.

## Before/after dm18 scan rows (`scripts/check_bug_classes.py scan --root .`)

Pre-edit (`core/prompt/gatherer_memory.py`'s 7 findings, digest
`2cbe7775…`):

```
MemoryRetrievalMixin.get_recent_facts line=155           -- #85 (off-path, unchanged)
MemoryRetrievalMixin.get_facts line=167                  -- #86 (off-path, unchanged)
MemoryRetrievalMixin._get_recent_conversations line=229  -- #87 (fixed)
MemoryRetrievalMixin._get_semantic_memories line=634     -- #88 (fixed)
MemoryRetrievalMixin._get_reflections line=799           -- #89 (off-path, unchanged)
MemoryRetrievalMixin.get_user_profile_context line=964   -- #90 (fixed)
MemoryRetrievalMixin.get_upcoming_schedule line=1070     -- #91 (fixed)
```

Post-edit (digest `b994315c…`): #87, #88, #90 and #91 have **no finding**
in the live dm18 report (confirmed via `jq` — 0 matches each); they now
appear in the base scanner's STALE list instead — the designed handoff:

```
dm18_except_returns_empty: core/prompt/gatherer_memory.py [MemoryRetrievalMixin._get_recent_conversations] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_memory.py [MemoryRetrievalMixin._get_semantic_memories] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_memory.py [MemoryRetrievalMixin.get_upcoming_schedule] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_memory.py [MemoryRetrievalMixin.get_user_profile_context] 'return ""'
```

#85, #86 and #89 remain **LIVE**, unchanged, by design:

```
MemoryRetrievalMixin.get_recent_facts line=156   -- #85
MemoryRetrievalMixin.get_facts line=168          -- #86
MemoryRetrievalMixin._get_reflections line=810   -- #89
```

Full scanner table, pre-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    627    36     39    0    3
dm18_except_returns_empty     gate    123    54     79    0    25
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Full scanner table, post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    628    36     39    0    3
dm18_except_returns_empty     gate    123    50     79    0    29
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped **54 → 50** (exactly the 4 code-fixed anchors:
#87, #88, #90, #91), `new` is **0** both times (no new finding introduced
anywhere in the tree by this edit), `stale` rose **25 → 29** (the 4 new
rows above, plus the 25 pre-existing rows unrelated to this batch —
F2/F3a/F3b/F4/F7a/F7b/F7c's carried-over STALE rows). `dm17_apply_without_guard`'s
`files` count ticked 627→628 (this scanner's file selection includes
`tests/`, and this batch adds one new test file; its own
`found`/`new`/`stale` are unchanged, a benign files-processed count
effect, not a finding). The overall scan exit code stays 1 both times,
driven by the pre-existing STALE rows across dm01/dm17/dm18 (unrelated to
this batch — full listing in `batches/F8a.md` §2/§10).

## Focused test command

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin systemd-run --user --scope \
  -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gatherer_outcomes_memory.py \
  tests/unit/test_section_outcomes.py \
  tests/unit/test_prompt_timeout.py \
  tests/unit/test_sep10_probe_dump_interpretation.py \
  tests/unit/test_independent_prompt_audit.py \
  tests/unit/test_light_prompt_path.py
```

**228 passed**, 0 failed, 0 skipped, exit 0, wall 0:15.20, peak RSS
1,713,848 KB — the new file's 16 test cases plus the 5 brief-named
existing files.

Sweep (all remaining `tests/unit` importers of `core.prompt.gatherer_memory`/
`MemoryRetrievalMixin`/`core.prompt.context_gatherer`/`ContextGatherer`, 10
files in 2 chunks): **185 passed, 0 failed, 0 skipped**. One file,
`tests/unit/test_graph_integration.py`, permanently excluded (known
`data/` writer via the `ContextGatherer` → `UserProfile()` fallback,
parent decision from F7a, unrelated to this batch). Two grep candidates
confirmed NOT genuine importers and excluded:
`tests/unit/test_upload_retrieval_pool.py` (docstring mention only),
`tests/unit/test_ordered_slice_guard.py` (allowlist string only). Full
chunk-by-chunk breakdown in `batches/F8a.md` §7-§8.

Failing-first (the new test file alone, run on the unedited source, digest
`2cbe7775…` printed in the same command and confirmed matching): **6
failed, 10 passed**, exit 1 — the 10 passes are the "existing behaviour
is unchanged" controls (healthy/empty/sibling for #87, the #87 light-path
control, empty-query/healthy for #88, no-profile for #90,
disabled-flag/healthy for #91, and the off-path evidence test — the last
two pass identically before and after by design). All 6 other tests fail
on the unedited source exactly as expected (`outcome_status(result) ==
("no_results", "")` instead of `("failed", ...)`; `Failed: DID NOT RAISE
<class 'RuntimeError'>` for #90's direct-raise test). One test-fixture-only
correction to this batch's OWN new file was made and re-verified by a
clean digest-confirmed rerun before this authoritative run shipped (full
detail in `batches/F8a.md` §4).

## Limitations

- **The fallback-only inner swallow** in `_get_recent_conversations`
  (183-194) is a read-only sibling, out of this batch's ownership — its
  own producer failure still reads as `succeeded` at the
  `_get_recent_conversations` boundary whenever the primary corpus_manager
  already returned something (proven unaffected, `batches/F8a.md` §11).
- **`_get_summaries_separate`/`_get_reflections_separate`** remain
  always-truthy dict shapes on their own except — a parent-decided
  limitation, not fixed in F8a; needs a builder change to read a dict
  section's status (see "Siblings recorded" above).
- **#92** (`core/prompt/gatherer_web.py` `_get_web_search_results`) remains
  unaddressed — F8b's scope, response `CGR-20260913-007-5`.
- No process deviation touching source or test correctness: every file
  edit used the Edit or Write tool; `git add` was never invoked; every
  pytest invocation used the guarded, capped, `-p asyncio` command with a
  `/proc/comm` guard check and a MemAvailable check immediately before
  it; `data/` was identical at every checkpoint, and `logs/` stayed
  absent throughout. One disclosed test-fixture-only correction to this
  batch's own new file (not an existing test — the FIXTURE RULE does not
  apply) is recorded in full in `batches/F8a.md` §4/§12, not in this
  immutable response.

## CGR-007 status

Answered: #71-#74 (F7a), #80 (F7b), #75-#79 and #81-#84 (F7c), #87, #88,
#90 and #91 with code plus #85, #86 and #89 with off-path evidence (F8a,
this response). **Still open:** #92 (F8b, response `CGR-20260913-007-5`).
CGR-009 and CGR-010 are untouched by any F7/F8 sub-batch.
