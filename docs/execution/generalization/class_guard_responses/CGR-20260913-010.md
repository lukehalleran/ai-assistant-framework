# CGR-20260913-010 — BASE response

Request ID: CGR-20260913-010. Owning batch ID: F10a.
This is the BASE response (dm18_except_returns_empty; BC-20, BC-47, CM-05).
Written 2026-09-14 by the generalization worker for F10a
(docs/execution/generalization/batches/F10a.md holds the full evidence trail).

## Changed files

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `memory/storage/multi_collection_chroma_store.py` | `66833584f2dfe725c27e3c4fb39af2e71e28b822d341e3da12667bca6e0992fe` | `5af28e2bbf5f2d3de968a8398e095cae9a2e1265aca68a8fe93c57d2101c6327` |
| `memory/memory_storage.py` | `efadf44fa77f97c1191f9c2733c1211277263ea75f3ccc6701696cedf56205c6` | `e8f3b6e8844e601653005cc4b2b8ad4f8ecf89afd702e0cb06629fc00f20e018` |
| `memory/memory_coordinator.py` | `dcac512839c67a68b08b99d7f3a9ff7f31aa4ea1ec8892f2583bedf81bda9006` | `726b1d2a560048a998965983a466867a4df057e4bcb6c54ea2d57d88675d630f` |
| `memory/memory_interface.py` | `f29c967c16ca5d979c58e744f8821c17075ed5a41f38da81cc001f5d2a9e7518` | `d17701d99af670cea2914f1210ef5929ebb24247a1118e44503628d23f7abb1c` |
| `gui/handlers.py` | `017d85383b0e073591c3e209fad654c683c0770333012c2c3a295ae28552869c` | `9cf718e47c2672b1e926da3e217d66048af5a59a13d2e2ab8d56fe51c5391e31` |

New leaf file (untracked before this batch too — created by F1, first modified
here): `utils/retrieval_outcome.py`, SHA-256 before `16f7bcb93e44885f353e679067eb96a8407ba9e316f00503432c021ad2ea2c3f`,
after `1abc1586534171b05e3a06fd976d08eba76cb6ecdfef565944f4dcdebf4c95eb`.

New test file (not a change to an existing file):
`tests/unit/test_store_write_outcomes.py`, SHA-256 `98c9c180492f88c6683a826c8fd43a13b2d2174695c822e7b26197f196ac58da`
(330 lines).

`core/orchestrator.py` is READ-ONLY in this batch (interim state recorded
below; F13 owns it) — not listed as changed.

Changed-lines summary (source only, 96 total; see `batches/F10a.md` §4 "Size
accounting" for the isolation methodology — several of these files were
already modified by earlier batches, so a raw `git diff --stat` against the
base commit overcounts): `utils/retrieval_outcome.py` 17,
`memory/storage/multi_collection_chroma_store.py` 4, `memory/memory_storage.py`
10, `memory/memory_coordinator.py` 26, `memory/memory_interface.py` 4,
`gui/handlers.py` 35.

## Focused deployed-function test command

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_store_write_outcomes.py \
  tests/unit/test_retrieval_outcome.py \
  tests/unit/test_api_error_storage_guard.py \
  tests/unit/test_thinking_leak_storage.py \
  tests/unit/test_memory_file_error_filter.py \
  tests/unit/test_provenance.py \
  tests/unit/test_entity_facts.py \
  tests/unit/test_calendar_turn_round3.py \
  tests/unit/test_handle_submit.py
```
Exit code: 0. Result: **238 passed**, 0 failed (5 warnings, all pre-existing
spacy/websockets/SWIG deprecation noise, unrelated to this change). Two more
focused chunks (18 more files, `batches/F10a.md` §5) add **276 passed** and
**39 passed** — **553 passed, 0 failed** total across the focused contract.

Failing-first (digest line + the new test file, in one command, run on the
UNEDITED sources): **6 failed, 8 passed** — every failure was
`ImportError: cannot import name 'StoreWriteError' from
'utils.retrieval_outcome'` (the local import inside the 6 tests that need the
new type); the 8 deliberate-skip/success controls already passed on the
unedited tree. Full command and output: `batches/F10a.md` §3.

Sweep (every remaining `tests/unit` importer of `memory.memory_storage` /
`MemoryStorage` / `memory.memory_coordinator` / `MemoryCoordinator` /
`memory.memory_interface` / `utils.retrieval_outcome` /
`multi_collection_chroma_store`, or referencing `store_interaction` /
`_background_store_interaction`; vetted and run in four chunks of ≤9):
**794 passed, 0 failed** (179 + 247 + 235 + 133). `tests/unit/test_graph_integration.py`
was excluded (the `ContextGatherer`/`UserProfile` default-path hazard, per the
standing rule) and never run. Full list, vetting notes and chunk results:
`batches/F10a.md` §6-7.

## Anchor disposition

| # | Symbol | Disposition | Evidence |
|---:|---|---|---|
| 127 | `MemoryStorage.store_interaction` | **Fixed** | See below. |
| 140 | `MultiCollectionChromaStore.add_conversation_memory` | **Fixed** | See below. |

### #140 — fixed, with deployed-function evidence

Before: `add_conversation_memory` (annotated `-> str`) had
`except Exception as e: logger.error(...); traceback.print_exc(); return None`
(previously 572-576) — a failed Chroma `.add(...)` collapsed into the same
`None` a caller could never distinguish from a legitimate (non-existent)
falsy return. Deployed-function proof:
`TestAddConversationMemoryOutcomes::test_failed_add_raises_store_write_error`
drives the deployed method against a fake `conversations` collection (via
`MultiCollectionChromaStore.__new__` + a manual `.collections` dict, the F9a
pattern — never a real Chroma client) whose `.add(...)` raises; on the
unedited source this test fails (`ImportError` on the not-yet-existing
`StoreWriteError`, structured so this was a recorded, expected failing-first
result, not a false pass); after the fix it raises
`StoreWriteError(source="chroma_conversations", reason=type(e).__name__) from e`.
The log line and `traceback.print_exc()` are unchanged (dropped nothing).
Privacy: `test_failed_add_raises_store_write_error` asserts a distinctive
marker placed in the underlying exception's message never reaches
`err.reason` or `str(err)` — only the exception's class name (`"RuntimeError"`)
survives. Success control (`test_success_returns_doc_id`) proves the
success path — the returned doc id and the collection's recorded `.add` call
— is unchanged.

### #127 — fixed, with deployed-function evidence

Before: `store_interaction`'s outer
`except Exception as e: logger.error(...); traceback.print_exc(); return None`
(previously 1020-1024) collapsed EVERY failure in the body — including a
Chroma write failure, a corpus write failure, thread detection, or metadata
build — into the same `None` returned by the four DELIBERATE skips above it
(thinking-only response, empty response, file-error response, API-error
sentinel). A turn could silently fail to persist while the caller (which
never checked the return value) proceeded as if it had.

After, two deployed-function proofs (a local copy of
`test_api_error_storage_guard.py`'s `storage` fixture — that file's own
fixture and tests are unedited, per the FIXTURE RULE):
1. `TestStoreInteractionFailureModes::test_store_write_error_from_chroma_propagates_unchanged`
   — a `StoreWriteError` raised by the (mocked) Chroma write propagates OUT OF
   `store_interaction` as the SAME object (`exc_info.value is swe`), not
   re-wrapped under a different `source` — the new `except StoreWriteError:
   raise` clause runs before the generic `except Exception`.
2. `TestStoreInteractionFailureModes::test_corpus_add_entry_failure_wraps_as_store_write_error`
   — a plain `RuntimeError` from `corpus_manager.add_entry` (a non-Chroma body
   exception, never reaching the Chroma write) is caught by the generic
   `except Exception as e:` and re-raised as
   `StoreWriteError(source="store_interaction", reason=type(e).__name__) from e`;
   the marker in the underlying exception's message never reaches `err.reason`
   or `str(err)`.

Deliberate-skip paired controls (`test_deliberate_skips_return_none_and_never_write`,
parametrized over the 4 skip responses): each still returns `None` BEFORE any
write, with neither `corpus_manager.add_entry` nor
`chroma_store.add_conversation_memory` called — unedited behaviour, proven
alongside the new raise behaviour, not merely asserted separately. Success
control (`test_success_control_returns_id`): a normal turn still returns the
Chroma-assigned id and still calls `corpus_manager.add_entry`.

### Coordinator continuity — `MemoryCoordinator.store_interaction`

`memory/memory_coordinator.py` is not itself a CGR-010 anchor, but a raise
propagating out of `MemoryStorage.store_interaction` passes straight through
it (line 369, previously no `try`). Before this batch, the two lines
syncing `conversation_context`/`interactions_since_consolidation` BACK from
storage ran only after a non-raising call; a failed write would have skipped
them, so the NEXT turn's sync-IN (`self._storage.conversation_context =
self.conversation_context`, still holding the STALE pre-turn context) would
silently overwrite whatever the storage layer had already appended —
dropping the failed turn from in-session context on top of it failing to
persist.

After: the storage call and the two sync-back lines are now
`try: ... finally: ...`, so the sync-back ALWAYS runs, even when storage
raises; the exception then propagates unchanged, and the per-turn thread
resolution below is unreached on a raise (ordinary control flow — a `raise`
inside `try`/`finally` still propagates after the `finally` body runs).
Deployed-function proof: `TestCoordinatorStoreInteraction::test_raise_still_syncs_context_back`
drives the real `MemoryCoordinator.store_interaction` (via
`MemoryCoordinator.__new__`, the `test_provenance.py` pattern) with a fake
`_storage` whose `store_interaction` mutates its own state THEN raises;
asserts the SAME `StoreWriteError` propagates, the coordinator's
`conversation_context`/`interactions_since_consolidation` reflect storage's
state (proving the sync-back ran), and `thread_store.list_open_threads` was
never called (proving thread resolution did not run). Success control
(`test_success_control_syncs_and_returns`) proves the sync-back and the
thread-resolution call still happen exactly as before on success.

### Handler continuity — `gui.handlers._background_store_interaction`

Before: the whole call was one `try`/`except Exception: logger.error(...)`;
a raise (never happened before this batch, since the write path always
returned `None`) would have skipped the transcript `conversation_logger.log_interaction(...)`
call entirely — a regression relative to the `None` path, which DOES log the
transcript (with `db_id=None`).

After: the write call is now inside an INNER `try`/`except StoreWriteError
as e: logger.error(...)` (the same log line and message format as before),
which falls through to the SAME transcript `log_interaction` call below with
`memory_id` left at its initialized `None` — so a failed write is logged AND
still recorded in the transcript, exactly like today's deliberate-skip
`None` path. The outer `except Exception as e: logger.error(...)` is
unchanged and still catches everything else (a non-`StoreWriteError`
exception, or a failure in `log_interaction` itself), so nothing escapes the
background task — no regression from today's swallow-everything behaviour.
Deployed-function proof:
`TestBackgroundStoreInteraction::test_store_write_error_logged_and_transcript_still_recorded`
drives the real `_background_store_interaction` with a memory system whose
`store_interaction` raises `StoreWriteError`; asserts (via `caplog`) the
existing "Background storage failed" log line fires, a distinctive marker
placed in the exception chain never reaches the log text, and
`conversation_logger.log_interaction` is called exactly once with
`metadata['db_id'] is None` and the same sanitized `assistant_response`.
Success control (`test_success_control_unchanged`) proves the success path
(info log, `db_id` set to the real id, one `log_interaction` call) is
byte-for-byte unchanged.

### Interim caller state — read-only, until F13

Per the brief's parent-verified map, re-confirmed unedited:
- `core/orchestrator.py` `_handle_deictic` (≈2018, `except` → warning),
  `_store_interaction` (≈2490, `except` → **the "CRITICAL: Failed to store
  interaction - data loss" log is now LIVE**, since `self.logger` is always
  set) both already catch broadly — a raise is caught exactly where a `None`
  was silently ignored before, so the CRITICAL path now actually fires on a
  real failure instead of never firing.
- `_maybe_document_generation` (≈2181), `_maybe_agentic_search` (≈2313),
  `gui/handlers.py` `_run_doc_generation` (≈1703), `_save_daemon_note`
  (≈2597), `_run_action_retry` (≈3498) are five bare `except Exception: pass`
  sites (F13's anchors) — a raise there is swallowed silently, EXACTLY as a
  `None` return is ignored today; no regression, and no improvement until
  F13 adds a turn-record receipt.
- `scripts/migrate_corpus_to_chromadb.py:136` (read-only): counted a `None`
  return from `add_conversation_memory` as "migrated" at line 137, with its
  own per-row `except Exception` at 142-145 already counting failures
  separately. A raise is now caught by that same per-row `except` and
  counted as a failure — correcting a pre-existing miscount, not a new
  hazard.
- Fallback coordinators that always return `None` (`core/orchestrator.py`
  ≈233, `core/prompt/base.py` ≈147) are unrelated call paths, out of scope,
  unchanged.

### Siblings — recorded, unedited (BC-58 sibling-site search)

- `MemoryStorage.store_skill` (#128, #129) — same file, same class, deferred
  to F10b (its own response `-2`).
- `memory/proposal_store.py ProposalStore.store_proposal` (#131) — deferred
  to F11b-1 (`-4`).
- `memory/thread_store.py ThreadStore.store_thread` (#146) — deferred to
  F11a-1 (`-3`).
- `corpus_manager.save_corpus`'s own swallow of its disk-write failure (a
  sibling noted in the brief, called from inside `add_entry`, itself called
  from inside `store_interaction`): unedited, out of OWNERSHIP. If it raises,
  that exception is now caught by `store_interaction`'s generic
  `except Exception` and wrapped as `StoreWriteError(source="store_interaction",
  ...)` — `save_corpus`'s own internal handling of ITS OWN disk failure is
  untouched.
- The store's own `search_all`, `query_multiple_collections`,
  `update_metadata`, `get_collection_stats` still swallow to an
  empty/false/typed-ish result — same BC-20/BC-47 shape, no CGR-010 anchor,
  unedited, out of OWNERSHIP.

## Scan tables

Before (dm18 summary row, post-F9b baseline):
```
dm18_except_returns_empty     gate    123    47     79    0    32
```
After:
```
dm18_except_returns_empty     gate    123    45     79    0    34
```
`found` 47→45 (both fixed anchors leave the accepted-debt count), `base` 79
unchanged, `new` **0** (no new dm18 finding introduced anywhere in the tree),
`stale` 32→34. The two new STALE lines, confirming anchors #127 and #140:
```
dm18_except_returns_empty: memory/memory_storage.py [MemoryStorage.store_interaction] 'return None'
dm18_except_returns_empty: memory/storage/multi_collection_chroma_store.py [MultiCollectionChromaStore.add_conversation_memory] 'return None'
```
(next to F9a's `get_by_id` and F9b's `_fetch_conversations_in_range`, already
STALE from earlier batches). `dm01` (7/12/0/5) and `dm31` (3/3/0/0) are
unchanged; `dm17` (36/39/0/3) is unchanged except its "files scanned" count
(631→632, solely the new test file existing in the tree). Scan stderr empty
both times. Full pre/post scan output: `batches/F10a.md` §2, §8.

## Remaining CGR-010 anchors — owning batches

#128, #129 → F10b (`-2`); #146 → F11a-1 (`-3`); #131 → F11b-1 (`-4`).
Turn-record receipts and the five `except: pass` sites named above → F13.

## Limitations

- The five `except: pass` call sites in `core/orchestrator.py` and
  `gui/handlers.py` (listed under "Interim caller state" above) still
  silently swallow a raised `StoreWriteError`, exactly as they silently
  ignored a `None` return before — no regression, but also no receipt of a
  failed write at those sites until F13.
- `corpus_manager.save_corpus`'s own internal handling of its own disk-write
  failure is unedited (out of OWNERSHIP); only the fact that `add_entry`
  propagating out of it now becomes a `StoreWriteError` from
  `store_interaction` is new.
- The store siblings (`search_all`, `query_multiple_collections`,
  `update_metadata`, `get_collection_stats`) keep the same
  swallow-to-empty/False shape today; out of this batch's ownership,
  tracked above.
- `store_skill` (#128/#129), `store_proposal` (#131) and `store_thread`
  (#146) are separate symbols in the same BC-20/BC-47 class, explicitly
  deferred to F10b/F11b-1/F11a-1 per the parent's F10 split — not silently
  left unmentioned.
- No FIXTURE RULE edits: no existing test's `assert result is None`
  assertion was touched; `test_api_error_storage_guard.py` and
  `test_thinking_leak_storage.py` are unedited and re-verified green in the
  focused run.
