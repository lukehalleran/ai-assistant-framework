# CGR-20260913-010-5 — #131 caller completion: proposal batch loops count a failed write and keep going

Request: CGR-20260913-010. Owning batch: F11b-1b (this is the `-5` follow-up
to the F10a base response `CGR-20260913-010.md` (#127, #140), the F10b
follow-up `CGR-20260913-010-2.md` (#128, #129), the F11a-1 follow-up
`CGR-20260913-010-3.md` (#146), and the F11b-1 follow-up
`CGR-20260913-010-4.md` (#131's write-side fix in
`memory/proposal_store.py`, plus the two batch-loop caller edits drafted,
digest-verified, exercised failing-first, and then reverted for SIZE).
Scanner: `dm18_except_returns_empty` (gate; BC-47 on the write side).

This response does not introduce a new anchor (the pre- and post-edit scans
both show `new = 0` on every gated scanner; see "Scan" below). It closes the
CALLER-side interim window that F11b-1's own packet disclosed: `#131`'s
write-side fix (`ProposalStore.store_proposal` now raises
`StoreWriteError` instead of returning `None` on a failed write) shipped in
F11b-1, but neither of its two batch-loop callers caught it per item — a
single failed write aborted the REST of that loop via each caller's
pre-existing, unrelated outer exception handler. This batch (F11b-1b) closes
that window by giving both callers the same typed per-item catch the F10b
skill loop and the F11a-1 thread-store loop already established.

The `main.py check-proposals` CLI read-side wrap (also drafted and reverted
alongside these two callers in F11b-1) has no anchor of its own — it is an
owner-decision UX fix layered on top of the already-answered read-side
anchors (#132-#134, #136 in `CGR-20260913-009-5.md`). Per this batch's brief
(`briefs/F11b-1b.md`), it is recorded in the batch packet
(`batches/F11b-1b.md`) only, not in this response.

## Changed files (SHA-256, before -> after)

Before = the digests recorded at this batch's manifest check (matching
`manifest_post_F11b-1.txt` and the brief's PARENT-VERIFIED FACTS).

| File | Before | After |
|---|---|---|
| `memory/shutdown_processor.py` | `74c9ed30ccdc5bc159e57c8f84f0c712b1b13e9a8f4ec6f7cffbdeea6f8ced13` | `9aa08ee56f13dcb753861ba25153a60aa48817df844928f81acd2360292bae52` |
| `agent_branch/proposal_bridge.py` | `3c7c23a29d550e7e9c55a23863d64484134eaed9bdd36697b160346e751a78e8` | `6cd8ae73fa484a5f8b172cd8db0f4e3b770a26f1cf9ad6fdc1f0485f2571f03e` |

New file (not a "changed" file, but part of this fix):
`tests/unit/test_proposal_loop_outcomes.py`,
`eef2df15c278b809365efe1b5e1e09dc6f61b6fc087a18230accc13e62bf422e` (246
lines).

`main.py` was also edited this batch (the `check-proposals` CLI wrap,
`6ef42c51327a26d4d070e09d412365507cdea4cc22137127edeafc05270a5eef` ->
`18dd3b912585b6a0eb5eaadd4126abca91b248c8552e8c23fda01a34ae0c6598`), but per
the brief carries no anchor and is recorded in `batches/F11b-1b.md` only, not
listed as a "changed file" of this response.

## Focused deployed-function test command

```
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env \
  DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin python -m pytest -q \
  -p no:cacheprovider -p asyncio tests/unit/test_proposal_loop_outcomes.py \
  tests/unit/test_shutdown_pipeline_proposals.py tests/unit/test_proposal_store_outcomes.py \
  tests/unit/test_proposal_store.py tests/unit/test_ordered_slice_guard.py \
  tests/unit/test_skill_and_summary_outcomes.py tests/unit/test_thread_outcomes.py \
  tests/unit/test_retrieval_outcome.py tests/unit/test_store_write_outcomes.py
```

Exit code 0. Focused set (9 files, 1 chunk): **157 passed, 0 failed** (5.30s,
1,134,516 KB peak RSS). Sweep set (7 files, 1 chunk, grep-derived importers
of `shutdown_processor`/`ShutdownProcessor`/`agent_branch.proposal_bridge`
not already in the focused set): **254 passed, 0 failed** (8.17s, 3,225,636
KB peak RSS). Combined: **411 passed, 0 failed**. `data/` unchanged after
every chunk (only the benign repo-root `..` mtime line from this batch's
`main.py` edit, plus a separately-recorded, owner-approved cleanup of three
unrelated F2-era `data/` leftovers mid-batch); `logs/` absent throughout.
Full detail in `batches/F11b-1b.md`.

Failing-first (on the unedited post-F11b-1 source, digests printed in the
same command): **4 failed, 1 passed**. The 4 failures are the two callers'
new tests plus their two message-format controls:
`TestShutdownGenerateProposalsBatchLoop::test_one_failed_write_is_counted_second_still_stored`
(`store_proposal.call_count` 1 instead of 2 — the unpatched loop's first
`StoreWriteError` propagated uncaught to `_generate_proposals`' own
pre-existing outer `except Exception`, aborting the rest of the batch, with
the captured warning
`[Shutdown] Proposal generation failed: proposal_store: OperationalError:...`
as direct evidence of the interim-window behavior);
`test_similarity_skip_control_not_counted_as_failed` and
`test_all_healthy_control_two_kept_zero_failed` (both failed only on the old
message format lacking the new `, N failed` suffix — the underlying
dedup-skip/store behavior was already correct and unaffected by this batch);
`TestIngestSurvivorsBatchLoop::test_one_failed_write_is_skipped_second_still_stored`
(the injected `StoreWriteError` propagated uncaught out of `ingest_survivors`
entirely — no local try existed). The 5th new test,
`TestIngestSurvivorsBatchLoop::test_all_healthy_control_both_ids_returned`,
passed pre-edit as expected (the happy path is unaffected by this batch).
Full failing-first detail, including the command and the digest line
matching the manifest-check values exactly, in `batches/F11b-1b.md`.

## Anchor status (no new anchor; caller-side interim window closed)

| Review # | Symbol | Kind | Disposition | Evidence |
|---:|---|---|---|---|
| 131 | `ProposalStore.store_proposal` | `broad_except_returns_empty` | Already **Fixed** (write side, `CGR-20260913-010-4.md`) — unchanged by this batch | This response adds the CALLER-side completion only; see below. |

**Caller-side completion (this batch, no anchor of its own):**

- `ShutdownProcessor._generate_proposals` (`memory/shutdown_processor.py`,
  `_generate_proposals`, ≈1253-1270): a `failed = 0` counter beside
  `kept = 0`; `try: doc_id = proposal_store.store_proposal(proposal) /
  except StoreWriteError: failed += 1; continue`; the similarity-dedup skip
  above the try is unaffected and is never counted as failed; the summary
  log changed from `if kept: logger.info(f"...Generated {kept}
  proposal(s)")` to `if kept or failed: logger.info(f"...Generated {kept}
  proposal(s), {failed} failed")`. Deployed-function evidence:
  `test_one_failed_write_is_counted_second_still_stored` drives the real
  `ShutdownProcessor._generate_proposals` with `store_proposal.side_effect`
  raising `StoreWriteError` on the first of two proposals — asserts both
  calls were attempted (`call_count == 2`) and the log reports `1 kept, 1
  failed`; the injected marker never reaches `caplog.text`. Paired controls:
  `test_similarity_skip_control_not_counted_as_failed` (a `check_similarity`
  match skips before ever calling `store_proposal`, so it is never counted
  as failed) and `test_all_healthy_control_two_kept_zero_failed` (both
  writes succeed -> `2 kept, 0 failed`).
- `agent_branch.proposal_bridge.ingest_survivors` (`agent_branch/proposal_bridge.py`,
  ≈242-248): `try: doc_id = store.store_proposal(proposal) / except
  StoreWriteError as e: logger.warning("ingest: store failed for survivor
  %s (%s)", sb.branch_id, e.reason); continue` — a failed survivor is added
  to neither `seen` nor `stored`; the return shape (`List[str]`) is
  unchanged. Deployed-function evidence:
  `test_one_failed_write_is_skipped_second_still_stored` drives the real
  `ingest_survivors` with a local fake store whose `store_proposal` raises
  `StoreWriteError` on the first of two survivors — asserts both were
  attempted (`store.calls == 2`) and exactly the second survivor's id was
  returned; the injected marker (carried only in the underlying exception
  chained under a clean `reason` label, matching `memory/proposal_store.py`'s
  real raise shape) never reaches `caplog.text` even though `e.reason`
  itself is legitimately logged. Paired control:
  `test_all_healthy_control_both_ids_returned` (both survivors succeed ->
  both ids returned).

**Consequence closed.** F11b-1's disclosed interim window — "a failed
write during `_generate_proposals` now propagates to that method's own
pre-existing, unchanged outer `except Exception`... abandoning the REST of
that session's proposal batch instead of skipping just the one item; a
single failed write during `ingest_survivors` now propagates uncaught to its
only caller... abandoning the rest of that portfolio's survivors" — is
closed by this batch. Both loops now attempt every item in the batch and
report or log per-item outcomes, matching the F10b skill-loop and F11a-1
thread-loop precedent exactly.

## Scan (dm18_except_returns_empty)

| When | mode | files | found | base | new | stale |
|---|---|---|---|---|---|---|
| Pre-edit (post-F11b-1) | gate | 123 | 33 | 79 | 0 | 46 |
| Post-edit (final) | gate | 123 | 33 | 79 | 0 | 46 |

No change. `new = 0` before and after — this batch's edits wrap caller sites
in try/except; they do not touch the anchored `return None`/`return []`
sites themselves, so no scanner row moved. No SCANNER SHAPE escalation was
needed. Full detail in `batches/F11b-1b.md`.

## Privacy

No real institution names or real IPs. The synthetic marker
`SYNTH_MARKER_f11b1b_4e2a91` never reaches `caplog.text` in either raising
test, including through `e.reason` (a clean type-name label per
`memory/proposal_store.py`'s real raise shape) in the `ingest_survivors`
warning — the marker lives only in an underlying, never-logged exception's
message. Pre-existing log lines kept unchanged (e.g. `_generate_proposals`'s
outer `except Exception as e: logger.warning(f"...{e}")`) may still carry
`{e}`; no test asserts on those, per the brief.

## Remaining CGR-010 anchors (not this response)

- `main.py check-proposals`'s CLI wrap (no anchor; owner-decision UX fix) —
  delivered this batch, recorded in `batches/F11b-1b.md` only.
- `ProposalStore.update_status`'s internal `self.store_proposal(...)`
  re-store (`memory/proposal_store.py:294`) — a raised `StoreWriteError`
  there is still caught only by that method's own outer
  `except Exception: ... return False`. Scheduled for **F11b-2** per the
  design doc's "F11b split" amendment (the "staged replacement, following
  the F11a-2 precedent" item), alongside `update_tracking_metadata`.
- `gui/tabs/proposals.py`'s four call sites (`store_proposal`,
  `get_proposal` x3, `get_pending_and_approved`) — read-only for this
  batch's OWNERSHIP; recorded as a BC-58 sibling only in
  `batches/F11b-1b.md`, no batch currently assigned.
- Receipts (`_generate_proposals`'s counterpart across other write sites) ->
  F13a/F13b/F13c per the brief's original routing.

## Limitations

- No automated test for `main.py check-proposals`'s CLI output/exit code —
  verified by reading only (it opens the real `CHROMA_PATH` store and is
  never executed by any worker, per R_common_rules); the underlying
  `RetrievalError` raise paths are already covered by F11b-1's
  `tests/unit/test_proposal_store_outcomes.py`. Full detail in
  `batches/F11b-1b.md`.
- `ProposalStore.update_status` and `update_tracking_metadata` are unchanged
  (F11b-2's territory) — recorded sibling only.
- `ProposalStore.check_similarity` and `get_for_dedup` are unchanged
  (F11b-2's territory per the design doc) — recorded siblings only, detailed
  in `CGR-20260913-009-5.md`.
- `ProposalStore._ensure_collection`'s "failure disguised as not-configured"
  is unchanged — a BC-58 sibling, recorded only.
