# Response to CGR-20260913-003 — Tone-detector keyword tests outside utils.trigger_match

Immutable response file, written 2026-09-13 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch: **R05**
(this response file, `docs/execution/generalization/batches/R05.md`, new
`tests/unit/test_tone_keyword_boundaries.py`, and `utils/tone_detector.py`).
This file is immutable once written; any follow-up is a new file with a `-2`
suffix, per the class-guard request contract.

## Request ID / owning batch

- Request: `CGR-20260913-003` (scanner `dm01_raw_substring`, BC-01, BC-28)
- Owning batch: `R05`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged throughout
  — no commit, no rebase, HEAD confirmed identical before and after)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `utils/tone_detector.py` | `8b2c70ad3428b03057d828cb9ee9952ba7a6d478974402b819c5afe5789af07c` (matches the request's recorded Source SHA-256 exactly) | `ceaa925370d43851069aa00983ba7a1bb3da2adf88f3aba5fd1cc288e73d73bf` |
| `tests/unit/test_tone_keyword_boundaries.py` | (new) | `e034c81b12bffad787ce7c1f1a16803425be3b91e3c7afa48fcb8422c5903651` |

`git diff --stat` for `utils/tone_detector.py`: **36 insertions, 9
deletions — 45 changed lines**; the new test file is 174 lines. **219
changed lines total**, well within the 450-line hard cap. No file outside
this list was touched.

## Changed files (summary)

- `utils/tone_detector.py`:
  - Import line: added `is_negated` beside the existing
    `compile_keyword_matcher` import from `utils.trigger_match`.
  - Added three module-level compiled matchers beside the existing four
    (`_HIGH_MATCHER`/`_MEDIUM_MATCHER`/`_CONCERN_MATCHER`/`_EVENT_MATCHER`):
    `_OVERWHELMED_MATCHER`, `_OVERWHELMED_POSITIVE_MATCHER` (both next to the
    existing harm-score matchers), and `_OBSERVATIONAL_MATCHER` (next to the
    `OBSERVATIONAL_MARKERS` set it compiles).
  - Anchor #29 (`_check_observational_language`, line 941 pristine): replaced
    `sum(1 for marker in OBSERVATIONAL_MARKERS if marker in message_lower)`
    with `sum(1 for _ in _OBSERVATIONAL_MATCHER.iter_hits(message_lower))`.
  - Anchor #30 (`_calculate_harm_score`, line 1023 pristine): replaced the
    raw `if "overwhelmed" in message_lower:` / `any(marker in message_lower
    for marker in positive_markers)` block with a chokepoint lookup
    (`_OVERWHELMED_MATCHER.iter_hits`), a chokepoint negation check
    (`is_negated` at the match position), and the exemption list routed
    through `_OVERWHELMED_POSITIVE_MATCHER`.
  - No vocabulary entry was added to or removed from `OBSERVATIONAL_MARKERS`
    or the positive-marker exemption list (BC-76); `utils/trigger_match.py`
    was not edited.
- `tests/unit/test_tone_keyword_boundaries.py` — new; deployed-function
  outcome fixtures for both anchors, clean and wrapped/indented, with
  counterexamples and paired positive controls (see
  `docs/execution/generalization/batches/R05.md`).

## Focused test command, exit code, counts

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v \
env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_tone_keyword_boundaries.py tests/unit/test_tone_detector.py \
  tests/unit/test_sep08_homework_tone_misfires.py tests/unit/test_tone_borderline_fallback.py \
  tests/unit/test_anti_amplification.py tests/unit/test_length_invariance.py
```

- **Failing-before**: the new test file was run against the pristine
  `utils/tone_detector.py` text (temporary plain-file text revert of the two
  anchor blocks only — re-hashed to `8b2c70ad...`-equivalent content before
  the run, restored and re-hashed to `ceaa9253...` after; no `git`
  stash/reset/checkout/restore/show was used): **9 failed, 9 passed**, exit
  1, elapsed 0:06.03, max RSS 1,605,460 KB. All 9 failures are exactly the
  two anchors' counterexamples/level assertions (see per-anchor disposition).
- **After the patch**, full focused suite (6 files): **150 passed**, exit 0,
  elapsed 0:04.44-0:07.26 (repeated 3x for stability), max RSS ~1,622,448 KB.

Cross-batch sweep (required, not edited) — every other unit test importing
`tone_detector`, one process:

```
PYTHONPATH=... python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_adaptive_exemplars.py tests/unit/test_escalation_deescalation_fix.py \
  tests/unit/test_grounding_check.py tests/unit/test_header_anchor_and_embed_cache.py \
  tests/unit/test_narration_turn_audit_fixes.py tests/unit/test_orchestrator_helpers.py \
  tests/unit/test_safety_canary.py tests/unit/test_sep05_heavy_history_first_person.py \
  tests/unit/test_sep09_speed_images.py tests/unit/test_sep10_probe_dump_interpretation.py \
  tests/unit/test_sep12_keyword_boundary_and_budget.py tests/unit/test_session_distress_enum_fix.py \
  tests/unit/test_style_block_precedence.py tests/unit/test_tone_arbiter_hardening.py \
  tests/unit/test_tone_floor_self_latch.py tests/unit/test_tonelevel_from_string.py \
  tests/unit/test_tone_stickiness_reset.py tests/unit/test_warmup_guards.py
```

Result: **549 passed**, exit 0, elapsed 0:14.12, max RSS 1,719,668 KB. No
failures — no fix required in any file outside this batch's ownership.

`ruff 0.14.9`; `python -m ruff check utils/tone_detector.py
tests/unit/test_tone_keyword_boundaries.py`: **All checks passed!**

## Per-anchor disposition

| # | Line (patched) | Vocabulary | Disposition | Evidence |
|---:|---:|---|---|---|
| 29 | 960 | `OBSERVATIONAL_MARKERS` (world-event vs. personal-crisis filter) | **fixed** | `TestObservationalMarkerContainment` — `'news'` ⊂ "newsletter" and `'article'` ⊂ "particle" no longer count toward the ≥2 threshold (clean + wrapped); genuine observational messages ("According to officials, millions of people are struggling with the new policies") keep count ≥2 (paired control, clean + wrapped). BC-28: `detect_crisis_level` on the containment message resolves `CrisisLevel.HIGH` (pre-fix it was short-circuited to `CONVERSATIONAL` at Stage 0 before the two HIGH keyword hits — "want to die", "can't go on" — were ever scanned, masking a real crisis signal); the genuine-observational control stays `CONVERSATIONAL` with `trigger="observational_language"`. |
| 30 | 1041-1058 | `'overwhelmed'` + its positive-marker exemption list | **partially fixed** | `TestOverwhelmedBoundaryAndNegation` — boundary: `'happy'` ⊂ "unhappy" no longer exempts ("unhappy and overwhelmed" now scores 2.0, clean + wrapped); paired controls unchanged ("overwhelmed by all the birthday gifts" stays 0.0; a genuinely overwhelmed message with no positive marker stays 2.0). Negation: forms the chokepoint's `NEGATION_CUE_RE` recognizes ("I won't be overwhelmed", "I will never feel overwhelmed again", "without feeling overwhelmed", "I'm not going to be overwhelmed") now add 0.0 harm, clean + wrapped. BC-28: the exemption fix changes the resulting `detect_crisis_level` LEVEL, not just the score — "I feel so alone and unhappy, completely overwhelmed" now reaches `CrisisLevel.CONCERN` via the deterministic keyword route (score 2+2=4 ≥ 4), vs. `CrisisLevel.MEDIUM` via the model-dependent semantic path pre-fix (score stuck at 2 because "unhappy" exempted "overwhelmed"). **Not fixed**: bare "I'm not overwhelmed" and "I'm no longer overwhelmed" still add 2.0 harm — `utils.trigger_match.NEGATION_CUE_RE`'s own docstring states it "deliberately does NOT include bare 'not'" and it has no "no longer" cue form either (verified directly: `is_negated("i'm not overwhelmed", ...)` and `is_negated("i'm no longer overwhelmed", ...)` both return `False`, both before and after this patch). Recorded honestly per the contract ("if it does not cover a form, record that honestly rather than extending the chokepoint") — `utils/trigger_match.py` is out of this batch's ownership and BC-76 rules out papering over the gap with a local phrase in `tone_detector.py` instead. |

## Before/after dm01 scan tables

Both runs use the checkout's currently-installed (older/"contract v1")
`dm01_raw_substring` scanner, which exempts an entire module outright once
it imports `utils.trigger_match` anywhere. `utils/tone_detector.py` already
carried that import (line 71) before this batch (it is the same module the
four existing HIGH/MEDIUM/CONCERN/EVENT matchers live in), so it was — and
remains — fully exempt from this scanner regardless of anchors #29/#30's
fix; this checkout's scanner cannot see either the pre-existing defect or
this batch's remedy, exactly as the request packet's own "SCANNER NOTE"
anticipates.

```
BEFORE (pristine anchor text, plain-file revert, re-hashed):
  python scripts/check_bug_classes.py scan --root . --json
  → tone_detector.py findings: 0 (dm01 or any other scanner); "new": []; exit 1
    (exit 1 is pre-existing, unrelated STALE rows on knowledge/git_memory.py
    and three scripts/*.py files from earlier batches — see below)

AFTER (this batch's patch restored):
  python scripts/check_bug_classes.py scan --root . --json
  → tone_detector.py findings: 0 (dm01 or any other scanner); "new": []; exit 1
    (same pre-existing STALE rows, untouched by this batch)
```

Structural proof of removal (independent of the scanner, which cannot see
either state): grep for the raw membership expressions at both anchor sites.

```
BEFORE: grep -n 'marker in message_lower\|if "overwhelmed" in message_lower' utils/tone_detector.py
  959:    marker_count = sum(1 for marker in OBSERVATIONAL_MARKERS if marker in message_lower)
  1041:    if "overwhelmed" in message_lower:
  1043:        if not any(marker in message_lower for marker in positive_markers):

AFTER:  grep -n 'marker in message_lower\|if "overwhelmed" in message_lower' utils/tone_detector.py
  (no output — both raw membership expressions are gone)
```

Per the request's acceptance contract: since this checkout's installed
scanner cannot see anchors #29/#30 at all (whole-module exemption), there is
no `dm01` row for `utils/tone_detector.py` to go STALE here. Acceptance
happens on the class-guard owner's own (newer/"contract v2") scanner after
this fix is integrated into a tree that checkout can scan; no attempt was
made to make any scanner here report these anchors.

## Limitations

- This is deterministic unit-test evidence only ("focused contract green");
  it is not a synthetic assembled probe, owner canary, or population claim.
- This checkout's scanner is the older ("contract v1") version that exempts
  a module wholesale once it imports the chokepoint anywhere; it cannot
  independently corroborate this fix or ever show anchors #29/#30 as `new`,
  `stale`, or `confirmed_fixed` — see the scan section above.
- Anchor #30 is partially fixed: bare "not overwhelmed"/"no longer
  overwhelmed" are a known, honestly-recorded chokepoint gap, not fixed here
  (see per-anchor disposition).
- BC-58 sibling raw-substring vocabulary checks remain in
  `_check_observational_language` (third-/first-person pronoun lists) and in
  `_calculate_harm_score`'s twelve pattern-multiplier keyword lists
  (abuse/distress, self-harm/crying, substance/crisis, sleep/mental,
  dissociation/trauma, hopelessness/suicidal). Neither is an anchor of this
  request; reported only, not fixed — see `docs/execution/generalization/batches/R05.md`.
- The non-unit importers `tests/integration/test_golden_distress_replay.py`,
  `tests/test_llm_fallback.py`, `tests/test_orchestrator_profile_injection.py`,
  `tests/test_tone_detection.py` were not run (CI-owned, out of this batch's
  test lane).
- No live Daemon process was started or exercised; `data/`
  (`adaptive_exemplars.json`, `benchmark_per_case.csv`, `chroma_db_v4`,
  `embedding_migration_manifest.json`, `pipeline`) was unchanged before and
  after every command in this batch; no live store, network credential, or
  provider was used (HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE=1 throughout; one
  local, previously-cached sentence-transformers embedder loaded offline
  during the cross-batch sweep — no network call).
