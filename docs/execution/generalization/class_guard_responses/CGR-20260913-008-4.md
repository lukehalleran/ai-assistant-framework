# Response to CGR-20260913-008 (follow-up -4) — FAISS semantic search and
# visual text search report failure distinctly; doc_cooccurrence stops
# reading a failure as a confident negative

Immutable response file, written 2026-09-14 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch (this
response): **F4** (`docs/execution/generalization/batches/F4.md`,
`knowledge/semantic_search.py`, `knowledge/visual_memory_store.py`,
`knowledge/visual_retrieval.py`, `knowledge/doc_cooccurrence.py`, and new
`tests/unit/test_semantic_visual_failure_outcomes.py`). This file is
immutable once written. This `-4` response answers anchors **#107, #108,
#109** — the base response (`CGR-20260913-008.md`, F2) answered #110/#111
(Tavily wrappers); `-2` (F3a) answered #103-#106 (`ReferenceDocsManager`)
plus the upload-refusal defect; `-3` (F3b) answered #101/#102 (Obsidian).
**With this response, every candidate anchor in the request packet has a
disposition — CGR-20260913-008 is fully answered.**

## Request ID / owning batch

- Request: `CGR-20260913-008` (scanner `dm18_except_returns_empty`; BC-20,
  BC-47)
- Owning batch (this response): `F4`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, no rebase; HEAD confirmed identical before and
  after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `knowledge/semantic_search.py` | `326fd28990b26adb99eab1f3f8b6719b468c13b383a89f27d95448b371f22019` | `fe772c2531072fac3ba579856cabf75bfffee4ee9511c7112068a1287142200b` |
| `knowledge/visual_memory_store.py` | `8337b1dfb65e0ba584734befb9dffe017ae1c4d761080bdbfa1d1f8d66ea3be8` | `dcbf5d25e361cbba9e899902a974b881e5c9de6ef11e2daaa204c8df6857de7d` |
| `knowledge/visual_retrieval.py` | `1859b153abce50dc04950ed65c35c034f41b17a6a5c75283227c5a5bdb20b48f` | `e0225732184901a9715290850e674e6bcd81aaf06afdea57d581913fccfc9b77` |
| `knowledge/doc_cooccurrence.py` | `75c90e3c5a5e647d832c50c05afb670fa8bb824e0da3189332e24d921f5b4489` | `0e5f74f40486cc7a3a965bb4de6c2209ad952d80152c71e6b9380822bc46cc55` |
| `tests/unit/test_semantic_visual_failure_outcomes.py` | (new file) | `4dd8c94da5573bf6b43282514f38c1be8a0c186f364e66aae34c5944814dd60f` |

All four before-digests match the request packet's recorded source
SHA-256s for #107-#109 and the F4 brief's "parent-verified facts" exactly,
reconfirmed by `sha256sum` immediately before any edit and again inside the
same failing-first command (see "Focused test command" below).

## What changed (summary)

- **`knowledge/semantic_search.py`** (18 changed lines: 10 insertions, 8
  deletions): one new import (`utils.retrieval_outcome.OutcomeList`).
  `SemanticSearchIndex.search`'s six return statements converted: empty
  query → `OutcomeList()`; not-loaded → `OutcomeList.unavailable("index_not_loaded")`;
  FAISS-search exception (**#107**) → `OutcomeList.failed(type(e).__name__)`,
  the existing `logger.error` line kept byte-for-byte; no-hits →
  `OutcomeList()`; result-assembly exception (**#108**) →
  `OutcomeList.failed(type(e).__name__)`, existing `logger.error` line kept;
  success → `OutcomeList(rows[:k])`. `semantic_search_with_neighbors` is
  UNCHANGED — it already returns `get_index().search(...)`'s value
  verbatim, so the `OutcomeList` passes through it automatically.
- **`knowledge/visual_memory_store.py`** (7 changed lines: 4 insertions, 3
  deletions): one new import. `search_by_text`'s three return paths
  converted identically: `_chroma is None` →
  `OutcomeList.unavailable("chroma_unavailable")`; `query_collection`
  exception (**#109**) → `OutcomeList.failed(type(e).__name__)`, existing
  `logger.warning` line kept byte-for-byte; success → `OutcomeList(results)`.
- **`knowledge/visual_retrieval.py`** (9 changed lines: 8 insertions, 1
  deletion): one new import (`utils.retrieval_outcome.outcome_status`);
  `retrieve_visual_memories` now reads `ts_status, ts_reason =
  outcome_status(text_results)` immediately after the `search_by_text`
  call (before `_merge_results`, which would otherwise drop the status via
  list-subclass iteration); the returned dict gains two ADDITIVE keys,
  `"text_search_status"` and `"text_search_reason"` — every existing key
  and value (`"text_results"`, `"images"`) is byte-for-byte unchanged. No
  other line touched: merge, entity hard-filter, image loading and
  `_search_clip` are untouched.
- **`knowledge/doc_cooccurrence.py`** (11 changed lines, all insertions):
  one new import (`utils.retrieval_outcome.RetrievalError, outcome_status`);
  a 9-line block inserted immediately after the two leg reads (`ra =
  semantic_search_with_neighbors(a, k=depth)`, `rb = ...(b, ...)`), before
  any title/mention computation: reads `outcome_status(ra)` and
  `outcome_status(rb)`; if either is `"failed"` or `"unavailable"`, raises
  `RetrievalError(source="doc_cooccurrence", reason=f"semantic_{worse}")`
  where `worse` is `"failed"` if either leg failed, else `"unavailable"` —
  the reason names only the worse leg's status, never concept text. A
  `"no_results"` leg (genuinely empty) does not raise; the rest of the
  function computes exactly as today. `DocCooccurResult`'s shape is
  unchanged (no new field). `is_known` is untouched — it inherits the raise
  because it simply reads `doc_cooccurrence(...).known`.
- `tests/unit/test_semantic_visual_failure_outcomes.py` (new, 332 lines,
  26 tests) — drives the DEPLOYED `SemanticSearchIndex.search`,
  `semantic_search_with_neighbors`, `VisualMemoryStore.search_by_text`,
  `VisualRetriever.retrieve_visual_memories` and
  `knowledge.doc_cooccurrence.doc_cooccurrence`/`is_known` directly, with
  paired failed/unavailable/succeeded/no_results cases, a dedicated
  privacy-leak test class, a sibling test proving `_search_clip` does not
  swallow failures, and a fully-offline deployed-consumer drive of
  `knowledge/literature_oracle.py`'s (read-only) `score_candidate`. No
  real FAISS index, ChromaDB, CLIP model, LLM or network anywhere —
  `knowledge.semantic_search.get_index()` is never called.

## Per-anchor disposition (deployed-function evidence)

All three are real BC-20/BC-47 instances (not "not-an-instance"
dispositions); the new test file proves the pre-patch defect directly on
the unedited source (see "Focused test command" below for the actual
failing-before run).

- **#107** `SemanticSearchIndex.search`, FAISS search error (was line 347),
  `return []`. Fixed: `self.index.search(...)` raising now returns
  `OutcomeList.failed(type(e).__name__)` instead of a bare `[]`. Deployed-
  function evidence: `TestSemanticSearchIndexSearch::test_index_search_raises_returns_failed_with_exception_class_name`,
  `TestSemanticSearchWithNeighbors::test_passes_failed_status_through`,
  `TestPrivacyNoLeakedText::test_semantic_search_index_reason_has_no_query_or_exception_text`.
- **#108** `SemanticSearchIndex.search`, result-assembly error (was line
  378), `return []`. Fixed identically: a raise while building/sorting
  result rows now returns `OutcomeList.failed(type(e).__name__)`. Deployed-
  function evidence: `TestSemanticSearchIndexSearch::test_row_to_result_raises_returns_failed`.
  Both #107 and #108 share the same control evidence:
  `test_hits_return_succeeded_with_rows`, `test_no_hits_returns_no_results`,
  `test_empty_query_returns_no_results` (three states that must stay
  exactly as before).
- **#109** `VisualMemoryStore.search_by_text`, ChromaDB text search error
  (was line 281), `return []`. Fixed: `self._chroma.query_collection(...)`
  raising now returns `OutcomeList.failed(type(e).__name__)` instead of
  `[]`; the `_chroma is None` early-out (not itself an anchor, but the
  state table's other unavailable path) now returns
  `OutcomeList.unavailable("chroma_unavailable")`. Deployed-function
  evidence: `TestSearchByText::test_query_collection_raises_returns_failed`,
  `::test_chroma_none_returns_unavailable`, controls
  `::test_results_return_succeeded`/`::test_empty_results_return_no_results`,
  `TestPrivacyNoLeakedText::test_search_by_text_reason_has_no_query_or_exception_text`.

## `doc_cooccurrence` and `visual_retrieval` — handled (contract points 3 and 4)

Neither `knowledge/visual_retrieval.py` nor `knowledge/doc_cooccurrence.py`
is a dm18 candidate (neither has an `except: return []`/`[]`-shaped site);
both are the request packet's named "no-try callers" that the design doc
explicitly asks this batch to close alongside the scanner anchors:

- **`VisualRetriever.retrieve_visual_memories`** now surfaces the text-search
  leg's outcome as two additive dict keys (`"text_search_status"`,
  `"text_search_reason"`), captured immediately after the call so a
  downstream transform (merge, slicing) cannot silently drop it. Deployed-
  function evidence: `TestRetrieveVisualMemories::test_failed_text_search_surfaces_status_keys`
  (failure → `"failed"`, existing keys unchanged),
  `::test_healthy_text_search_status_succeeded` (control).
- **`doc_cooccurrence(a, b, ...)`** previously called
  `semantic_search_with_neighbors` for both legs with NO try — a failed or
  unavailable leg used to read as an ordinary empty result, silently
  producing `known=False` (the exact defect the request packet's "Observed
  shape and semantic risk" describes: failure indistinguishable from "no
  matches"). It now raises `RetrievalError(source="doc_cooccurrence",
  reason="semantic_failed"|"semantic_unavailable")` when either leg's
  status is `"failed"`/`"unavailable"`, naming only the worse leg's status
  — never a concept string. A genuinely empty (`"no_results"`) leg is
  unaffected and computes exactly as today. Deployed-function evidence:
  `TestDocCooccurrence::test_failed_leg_raises_retrieval_error`,
  `::test_unavailable_leg_raises_retrieval_error`,
  `::test_failed_outranks_unavailable_in_reason`,
  `::test_is_known_inherits_the_raise`; controls
  `::test_both_legs_no_results_control` (`DocCooccurResult(0, [], False,
  False)`, byte-identical to today) and `::test_shared_titles_known_true_control`;
  privacy: `TestPrivacyNoLeakedText::test_doc_cooccurrence_raise_reason_has_no_concept_text`.
- **Deployed consumer, proven not asserted**: `knowledge/literature_oracle.py:740-746`
  (read-only, untouched) wraps its `doc_cooccurrence(a, b, depth=40)` call
  in `try: ... except Exception as ex: log.warning(...)`, leaving
  `record.aux_doc_cooccurrence` at its `None` default instead of a false
  `known=False`. Since `RetrievalError` is a `RuntimeError` subclass, the
  oracle's existing, generic `except Exception` absorbs it exactly like any
  other exception — no oracle code needed to change. Proven by a fully
  offline drive of the real `LiteratureOracle.score_candidate` orchestrator
  (mocked `model_manager.generate_once`, `retrieve_arxiv` stubbed to `[]`,
  `web_search_manager=None` so `retrieve_tavily` short-circuits with no
  network, `enable_aux=True`, `knowledge.doc_cooccurrence.doc_cooccurrence`
  patched to raise): `TestLiteratureOracleConsumer::test_failed_leg_leaves_aux_none_and_logs_nonfatal`
  asserts `rec.aux_doc_cooccurrence is None`, the non-fatal warning line
  fired, and the warning carries no marker text; the control
  `::test_healthy_leg_populates_aux_control` asserts a successful
  `DocCooccurResult` still populates `aux_doc_cooccurrence` exactly as
  before. No real model, no network, no LLM call — see "Privacy /
  no-network" in the batch packet.

## Siblings (named, not fixed by this batch)

- **`synthesis_filter.py:496`, `synthesis_retriever.py:241`,
  `synthesis_generator.py:277`** (F12): all call
  `semantic_search_with_neighbors`/consume its result; `synthesis_filter.py:496`'s
  "a raise already means passing by default" now also applies to a
  *failed* `OutcomeList`, since `not OutcomeList.failed(...)` is `True`
  exactly like `not []` — a failed novelty check would read as novelty 1.0
  → pass. Named for F12's hand-off, not touched here (read-only, out of
  ownership).
- **`core/prompt/gatherer_knowledge.py:1859,1504`** (F7): read-only
  consumers of `semantic_search_with_neighbors`/`retrieve_visual_memories`;
  both already treat their results as plain lists/dicts (falsy checks,
  iteration) — behaviour unaffected by this batch's edits (an empty
  `OutcomeList` stays falsy; the two new dict keys are additive). F7
  migrates them to read `.status`/the new keys.
- **`core/agentic/tools.py:1958,2131`**: same pattern, read-only, same
  unaffected-today / migrate-later status.
- **`gui/launch.py:387`** (warmup): read-only, calls into the same
  pipeline at process warmup; unaffected.
- **`search_by_clip`/`VisualRetriever._search_clip`**: read in full for
  this batch (contract point 3's explicit ask). Neither has a try/except
  of any kind — unlike `search_by_text`, a CLIP-leg exception is not
  swallowed to `[]` at all; it propagates straight out of
  `retrieve_visual_memories` uncaught (there is no top-level try around the
  `clip_results`/`text_results` calls, only a narrow `try/except: pass`
  around the `max_images` clamp). So this leg is not a dm18 instance, but
  it is also not protected the way the text leg now is. Proven by
  `TestRetrieveVisualMemories::test_clip_failure_is_not_swallowed`
  (deployed-function evidence, not just read). Out of this batch's
  ownership (`search_by_text` call site only); recorded for whichever
  batch later hardens the CLIP leg.

## Before/after dm18 scan rows (`scripts/check_bug_classes.py scan --root .`)

Pre-edit (target-file findings only, digests as in "Changed files" above):

```
dm18_except_returns_empty: knowledge/semantic_search.py [SemanticSearchIndex.search] 'return []' (line 347)
dm18_except_returns_empty: knowledge/semantic_search.py [SemanticSearchIndex.search] 'return []' (line 378)
dm18_except_returns_empty: knowledge/visual_memory_store.py [VisualMemoryStore.search_by_text] 'return []' (line 281)
```

Post-edit: **no findings** for any of the four target files in the live
dm18 report (confirmed via `--json`). All three now appear in the base
scanner's STALE list instead — the designed handoff:

```
dm18_except_returns_empty: knowledge/semantic_search.py [SemanticSearchIndex.search] 'return []'
dm18_except_returns_empty: knowledge/semantic_search.py [SemanticSearchIndex.search] 'return []'
dm18_except_returns_empty: knowledge/visual_memory_store.py [VisualMemoryStore.search_by_text] 'return []'
```

Full scanner table, pre-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    619    36     39    0    3
dm18_except_returns_empty     gate    123    71     79    0    8
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Full scanner table, post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    620    36     39    0    3
dm18_except_returns_empty     gate    123    68     79    0    11
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped 71→68 (the 3 fixed anchors), `new` is `0` both times
(no new finding introduced anywhere in the tree by this edit), `stale` rose
8→11 (the 2 pre-existing F2 rows, 4 F3a rows and 2 F3b rows, plus the 3 new
ones above). The overall scan exit code stays 1 both times, driven by the
pre-existing STALE rows across dm01/dm17/dm18 (unrelated to this batch —
full listing in F4.md §5/§12).

## Focused test command

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin systemd-run --user --scope \
  -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_semantic_visual_failure_outcomes.py \
  tests/unit/test_doc_cooccurrence.py \
  tests/unit/test_semantic_search_metric.py \
  tests/unit/test_semantic_load_concurrency.py \
  tests/unit/test_visual_memory_store.py \
  tests/unit/test_visual_memory_pipeline.py
```

**84 passed**, 0 failed, 0 skipped, exit 0, wall 0:02.54, peak RSS 811,996
KB — the new file's 26 tests plus the 5 brief-named existing files.

Sweep (all remaining `tests/` importers of the four modules, in 2 more
chunks): **83 passed** (`test_calendar_turn_round3.py`,
`test_codex_followups.py`, `test_gatherer_latency_guards.py`,
`test_hybrid_semantic_score.py`, `test_public_news_verification.py`;
0:09.88, 1,223,944 KB) + **93 passed** (`test_sep09_latency_metrics.py`,
`test_sep09_speed_images.py`, `test_sep10_web_search_gap.py`,
`test_synthesis_generator.py`, `test_warmup_guards.py`, and the one
non-unit file this batch runs, `tests/test_synthesis_calibration.py`;
0:09.95, 1,672,020 KB). Combined: **260 passed, 0 failed, 0 skipped**
across 17 files (full disposition and non-unit-test read-before-run
decisions in F4.md §9-§10).

Failing-first (same new-test file, run alone on the unedited source, all
four digests printed in the same command and confirmed matching): **20
failed, 6 passed**, exit 1 — the 6 passes are the "existing behaviour is
unchanged" controls (`semantic_search_with_neighbors` pass-through on
success, the CLIP-not-swallowed sibling, both `doc_cooccurrence`
healthy-legs controls, both oracle-consumer tests which patch
`doc_cooccurrence` directly and so exercise only the untouched
`literature_oracle.py`); all 20 other tests fail on the unedited source
exactly as expected (bare `[]`/`list` instead of `OutcomeList`, missing
dict keys, no raise where one is now required).

## Limitations

- Production consumers of `semantic_search_with_neighbors` and
  `retrieve_visual_memories` (`core/prompt/gatherer_knowledge.py`,
  `core/agentic/tools.py`, `gui/launch.py` warmup) still flatten
  `OutcomeList`/the returned dict to a plain falsy check — F7 migrates the
  gatherers to read `.status`/the new `text_search_status` key; the others
  are named siblings above, not this batch's file.
- `knowledge/synthesis_filter.py:496` and the other F12-owned synthesis
  modules still read a failed `OutcomeList` as "pass by default" (a raise
  or a failure both currently mean "novelty accepted") — F12 hand-off, not
  fixed here.
- `_search_clip`/`search_by_clip` remain unprotected (neither swallowed
  nor surfaced as a status) — named as a sibling with deployed-function
  evidence, out of this batch's ownership (the brief's line is
  `search_by_text` only).
- `tests/test_memory_deep_integration.py` (non-unit, brief-named) was read
  in full and NOT run: it does not exercise any of this batch's four
  modules, and its `memory_coordinator` fixture constructs a real (if
  tmp_path-scoped) `MultiCollectionChromaStore`, which needs a real
  embedding model — forbidden by the universal "no real Chroma/CLIP/
  embedder" constraint regardless of relevance.
- No process deviation this batch: every file edit used the Edit or Write
  tool; `git add` was never invoked; every pytest invocation (failing-first
  plus 3 chunks) used the guarded, capped, `-p asyncio` command with a
  `/proc/comm` guard check and a MemAvailable check immediately before it;
  `data/` was identical at every checkpoint.
