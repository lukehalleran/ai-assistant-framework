# Response to CGR-20260913-007 (follow-up -2) — background-knowledge legs
# (wiki, semantic chunks, FAISS row reads) report failure, timeout and busy
# distinctly

Immutable response file, written 2026-09-14 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch (this
response): **F7b** (`docs/execution/generalization/batches/F7b.md`,
`core/prompt/gatherer_knowledge.py`, `knowledge/semantic_search.py`, and new
`tests/unit/test_gatherer_outcomes_background_knowledge.py`). This `-2`
response answers anchor **#80** (`_get_wiki_content_timed`) — the base
response (`CGR-20260913-007.md`, F7a) answered #71-#74. It also records two
BC-58 siblings with deployed-function evidence, per the brief:
`_get_semantic_chunks_timed` (not itself a dm18 finding) and
`SemanticSearchIndex.search`'s total row-read failure (F4 parent-review
follow-up, not itself a dm18 finding).

## Request ID / owning batch

- Request: `CGR-20260913-007` (scanner `dm18_except_returns_empty`; BC-20,
  BC-47, CM-05)
- Owning batch (this response): `F7b`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, no rebase; HEAD confirmed identical before and
  after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `core/prompt/gatherer_knowledge.py` | `5d79ddf2861853575078a155985d7f174ecff4132b6ec002f35cd9461cc5c8df` | `1839020273dcf804b2be94ef67a65159fd71c89f0e3addd2a1657c82cbab98b3` |
| `knowledge/semantic_search.py` | `fe772c2531072fac3ba579856cabf75bfffee4ee9511c7112068a1287142200b` | `f2f1625dced9b2b728c19c2f3071dde0d96267edb1fa914247e1164366b054bd` |
| `tests/unit/test_gatherer_outcomes_background_knowledge.py` | (new file) | `45253ef576f2988d55f0e589279f5234b5fcb6637a02c8578125ac8002edf478` (399 lines) |

The before-digest for `core/prompt/gatherer_knowledge.py` matches F7a's
post-edit digest (the request packet's/brief's parent-verified base for
this batch) exactly, and `knowledge/semantic_search.py`'s before-digest
matches F4's post-edit digest exactly — both reconfirmed by `sha256sum`
immediately before any edit and again inside the same failing-first
command that produced the counts below.

## What changed (summary)

`core/prompt/gatherer_knowledge.py` (20 changed lines, ownership scope
only — the bodies of `_get_wiki_content_timed` and
`_get_semantic_chunks_timed`; the `OutcomeList`/`outcome_status` import at
line 65 was already present from F7a):

- **`_get_wiki_content_timed`**:
  - `chroma_err = None` initialized before the `if chroma:` block.
  - In-flight guard: `return []` → `return
    OutcomeList.unavailable("in_flight")`.
  - Chroma `except asyncio.TimeoutError:` → `return
    OutcomeList.unavailable("timeout")` (the `timings["timed_out"] = True`
    assignment and the existing `logger.warning` line are unchanged).
  - Chroma `except Exception as e:` gains one line, `chroma_err =
    type(e).__name__`, after the existing `logger.debug(...)` line
    (unchanged) — falls through to the live fallback exactly as before.
  - After the fallback's per-term loop and before the unchanged `return
    wiki_results`: `if chroma_err and not wiki_results: return
    OutcomeList.failed(f"chroma:{chroma_err}")` — the primary source
    (chroma) having errored, an EMPTY fallback is not a confident
    no_results.
  - Fallback outer `except Exception as e:` (**ANCHOR #80**) → `return
    OutcomeList.failed(type(e).__name__)` (existing `logger.warning` line
    unchanged).
  - Empty query and `_should_skip_wikipedia` are untouched — still bare
    `return []` (deliberate no_results, per the brief).
- **`_get_semantic_chunks_timed`**:
  - In-flight guard: `return []` → `return
    OutcomeList.unavailable("in_flight")`.
  - Immediately after the awaited search's `finally:` block and BEFORE
    the existing `if not results: return []` falsy check: `sem_status,
    sem_reason = outcome_status(results); if sem_status in ("failed",
    "unavailable"): return OutcomeList(status=sem_status,
    reason=sem_reason)` — reads the FAISS producer's (F4) status before
    any transform can flatten it.
  - `except asyncio.TimeoutError:` gains `return
    OutcomeList.unavailable("timeout")`; `except Exception as e:` gains
    `return OutcomeList.failed(type(e).__name__)` (both existing log
    lines unchanged).
  - The trailing `return []` after both excepts (not a packet anchor,
    same class as #80 but not scanner-detected — see "Siblings" below) is
    left untouched; it is unreachable in practice now (every path above
    it returns explicitly) but removing genuinely dead code was judged a
    larger, non-contract-required diff than leaving it, and ruff raised
    no finding either way.
  - Healthy paths (threshold gate, disambiguation filter, stitching)
    return exactly today's chunks — untouched.

`knowledge/semantic_search.py` (15 changed lines, ownership scope only —
`SemanticSearchIndex.search`, body and docstring; `_read_rows` UNCHANGED):

- Docstring rewritten to list the four actual states (`no_results`,
  `unavailable`, `failed`, `succeeded`-including-partial) instead of the
  inaccurate "no_results if not loaded" claim F4's parent review flagged
  as a nit and assigned to this batch.
- After `row_data_map = self._read_rows([i for i, _ in hits])` and before
  the existing `# 5) Assemble result dicts` comment: `if hits and not
  row_data_map: if self._pq_file: return
  OutcomeList.failed("row_read_failed"); return
  OutcomeList.unavailable("metadata_unavailable")`. A PARTIAL read (some
  rows present in `row_data_map`) skips this branch and falls through to
  the unchanged assembly loop, which already drops unreadable rows one at
  a time (`if not data: continue`) — so it stays `succeeded` with
  whatever rows WERE read. There is no separate "partial" state, matching
  the brief's explicit instruction: a single bad row group must not turn
  every hit query into a failure.

`tests/unit/test_gatherer_outcomes_background_knowledge.py` (new, 399
lines, 12 test functions / 18 test cases via parametrization) — drives the
DEPLOYED `KnowledgeRetrievalMixin._get_wiki_content`/`_get_semantic_chunks`
wrappers and the DEPLOYED `SemanticSearchIndex.search` directly,
`Mock`/`AsyncMock`/`threading.Semaphore` fakes only; a hand-built fake
`_pq_file` whose `read_row_group` raises drives the REAL, unchanged
`_read_rows`. No real ChromaDB, embedder, FAISS index, Wikipedia API or
network anywhere — `knowledge.semantic_search.get_index()` is never
called.

## Per-anchor disposition (deployed-function evidence)

- **#80** `_get_wiki_content_timed`, live-Wikipedia fallback outer except
  (previously `return []`). A real BC-20/BC-47 instance (not a
  "not-an-instance" disposition); the new test file proves the pre-patch
  defect directly on the unedited source (13 failed / 5 passed, digests
  confirmed matching — see "Focused test command" below). Fixed: the
  except now returns `OutcomeList.failed(type(e).__name__)`. Deployed-
  function evidence: `TestWikiOutcomes::test_fallback_raising_is_failed_with_exception_class`
  (the anchor site directly, with a marker query/exception proving no
  leaked text), `test_busy_inflight_guard_is_unavailable[wiki]` and
  `test_timeout_is_unavailable_and_timed_out_recorded[wiki]` (the two
  guard paths at the SAME method that the contract also requires),
  `TestWikiOutcomes::test_chroma_raises_fallback_outcome` (both
  parametrized cases: chroma-raised-with-empty-fallback → `failed` /
  `"chroma:<class>"`, and chroma-raised-with-fallback-items → `succeeded`,
  the brief-mandated control), `TestWikiOutcomes::test_healthy_chroma_returns_todays_results_control`
  (healthy chroma → unchanged today's results).

## BC-58 siblings (recorded with deployed-function evidence, per the brief)

- **`_get_semantic_chunks_timed`** (not a dm18 scanner finding: its
  `except` blocks fall through to a single shared `return []` outside any
  `except`, not the `except: return []` shape dm18 matches — confirmed
  absent from the live report and the STALE baseline both before and
  after this batch's edit). It never read the FAISS producer's
  (`SemanticSearchIndex.search`, F4) `.status` before the falsy check that
  follows the awaited search, so a failed/unavailable producer read was
  indistinguishable from a genuinely empty one. Fixed identically to the
  wiki leg's contract: the in-flight guard now returns
  `OutcomeList.unavailable("in_flight")`; the producer's status is read
  immediately and returned as-is on failed/unavailable; the timeout and
  generic-exception excepts now return typed `OutcomeList`s. Deployed-
  function evidence: `test_busy_inflight_guard_is_unavailable[semantic]`,
  `test_timeout_is_unavailable_and_timed_out_recorded[semantic]`,
  `TestSemanticOutcomes::test_producer_status_is_read_before_falsy_check`
  (both parametrized cases: producer `failed`/`unavailable`),
  `TestSemanticOutcomes::test_unexpected_pipeline_exception_is_failed`
  (an exception inside the pipeline, not the producer call, not a
  timeout — the disambiguation filter), `TestSemanticOutcomes::test_healthy_threshold_gate`
  (both parametrized cases: above-threshold succeeded, below-threshold
  no_results — unaffected healthy behaviour), and
  `TestThroughBuilder::test_busy_semantic_slot_marks_section_unavailable_in_flight`
  (through the deployed F5-integrated builder: a busy semantic slot marks
  `_section_outcomes["semantic"] == {"status": "unavailable", "reason":
  "in_flight"}`).
- **`SemanticSearchIndex.search`'s total row-read failure** (F4
  parent-review follow-up, not a dm18 scanner finding: `_read_rows` is
  `except → continue`, not `except → return []`). When FAISS returns hits
  but every row-group read fails, `search` used to return `OutcomeList()`
  (`no_results`) — the same confident-negative defect F4 removed from the
  other return paths — and `doc_cooccurrence` would score `known=False`
  from it. Fixed: `failed("row_read_failed")` when hits existed, nothing
  was readable, and a parquet handle IS present; `unavailable("metadata_
  unavailable")` when the parquet handle itself is absent; a PARTIAL read
  stays `succeeded` with the readable rows (no separate partial state — a
  single bad row group must not fail the whole query). The docstring is
  corrected to list these four states accurately. Deployed-function
  evidence, driving the REAL, unchanged `_read_rows` through a fake
  `_pq_file.read_row_group` that raises (not a mock of `_read_rows`
  itself, per the brief): `TestSemanticSearchIndexRowReadFailure::test_total_vs_partial_row_read_failure`
  (both parametrized cases: all-hits-in-one-raising-group → `failed`/
  `row_read_failed`; hits split across two groups with only one raising →
  `succeeded` with the readable row, the brief-mandated control),
  `::test_pq_file_none_with_hits_is_unavailable_metadata_unavailable`, and
  `::test_end_to_end_doc_cooccurrence_raises_semantic_failed` (monkeypatches
  `knowledge.semantic_search.get_index` to the fake failing index, then
  drives the DEPLOYED `knowledge.doc_cooccurrence.doc_cooccurrence` — it
  raises `RetrievalError(source="doc_cooccurrence", reason="semantic_
  failed")` instead of returning a confident `known=False`, exactly as F4
  designed for a failed leg).
- **`_get_wiki_snippet_cached`** (read-only sibling, brief-named). Read in
  full (def at line 560, `KnowledgeRetrievalMixin`). It wraps its own
  `asyncio.wait_for`/`get_wiki_snippet` call in a try/except (`except
  asyncio.TimeoutError:` at 588, `except Exception as e:` at 597) that
  returns `None` on ANY failure — the same dm18-shaped swallow-to-empty
  pattern as the anchors, but scoped to a single search term's snippet,
  not a whole section. Not a dm18 scanner finding (returns
  `None`/`Optional[Dict]`, not `[]`/`list`). Not fixed by this batch
  (ownership is `_get_wiki_content_timed`'s and `_get_semantic_chunks_
  timed`'s bodies only) — its swallow is why the live-fallback loop can
  never see an individual term's raw exception through the real
  implementation; only a raise from `_wiki_keywords_from_query`, or (as
  this batch's own tests do, by replacing the whole method with a raising
  fake) a propagated exception, reaches ANCHOR #80's except today.

## Before/after dm18 scan rows (`scripts/check_bug_classes.py scan --root .`)

Pre-edit (`core/prompt/gatherer_knowledge.py`'s #80 finding only, digest
`5d79ddf2…`):

```
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin._get_wiki_content_timed] 'return []' (line 1849)
```

(`knowledge/semantic_search.py` had zero live dm18 findings both before
and after this batch — both of its sites were already STALE since F4;
neither this batch's docstring edit nor its row-read-failure addition is
an `except: return []` shape.)

Post-edit (digest `1839020273…`): **no finding** for `_get_wiki_content_
timed` in the live dm18 report (confirmed via `jq` — 0 matches). It now
appears in the base scanner's STALE list instead — the designed handoff:

```
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin._get_wiki_content_timed] 'return []'
```

`_get_semantic_chunks_timed`'s trailing tail site (the sibling site) was
confirmed NOT a dm18 finding either before or after this batch (same `jq`
query pattern, 0 matches both times) — it was never in the baseline or
the live report, at any point, because the site is `except → (falls
through, no return)`, not `except → return []`.

Full scanner table, pre-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    625    36     39    0    3
dm18_except_returns_empty     gate    123    64     79    0    15
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Full scanner table, post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    626    36     39    0    3
dm18_except_returns_empty     gate    123    63     79    0    16
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped 64→63 (the 1 fixed anchor, #80), `new` is `0` both
times (no new finding introduced anywhere in the tree by this edit),
`stale` rose 15→16 (the 1 new row above, plus the 15 pre-existing rows
unrelated to this batch — F2/F3a/F3b/F4/F7a's carried-over STALE rows).
`dm17_apply_without_guard`'s `files` count ticked 625→626 because its file
selection includes `tests/` and this batch adds one new test file; its own
`found`/`new`/`stale` are unchanged, a benign files-processed count
effect, not a finding. The overall scan exit code stays 1 both times,
driven by the pre-existing STALE rows across dm01/dm17/dm18 (unrelated to
this batch — full listing in `batches/F7b.md` §3/§11).

## Focused test command

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin systemd-run --user --scope \
  -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gatherer_outcomes_background_knowledge.py \
  tests/unit/test_sep09_latency_metrics.py \
  tests/unit/test_audit0831_fixes.py \
  tests/unit/test_semantic_visual_failure_outcomes.py \
  tests/unit/test_semantic_search_metric.py \
  tests/unit/test_semantic_load_concurrency.py \
  tests/unit/test_doc_cooccurrence.py \
  tests/unit/test_hybrid_semantic_score.py
```

**120 passed**, 0 failed, 0 skipped, exit 0, wall 0:10.42, peak RSS
1,233,400 KB — the new file's 18 test cases plus the 7 brief-named
existing files.

Sweep (all remaining `tests/unit` importers of the two modules, 37 files
in 5 chunks of ≤9): **1,142 passed, 0 failed, 0 skipped**. One file,
`tests/unit/test_graph_integration.py`, permanently excluded (parent
decision recorded during F7a; known `data/` writer via the
`ContextGatherer` → `UserProfile()` fallback, unrelated to this batch).
Full chunk-by-chunk breakdown in `batches/F7b.md` §9.

Failing-first (the new test file alone, run on the unedited source,
digests printed in the same command and confirmed matching): **13
failed, 5 passed**, exit 1 — the 5 passes are the "existing behaviour is
unchanged" controls (chroma-raises-with-fallback-items already reads
succeeded today; healthy threshold gate above/below threshold; a partial
row-read failure already returns the readable rows as succeeded today;
healthy chroma already returns today's results). All 13 other tests fail
on the unedited source exactly as expected (`outcome_status(result) ==
("no_results", "")` instead of `("failed", ...)`/`("unavailable", ...)`;
`AttributeError`/`KeyError` where a typed status or a `_section_outcomes`
entry was expected; `Failed: DID NOT RAISE` for the doc_cooccurrence
end-to-end case). Two bugs in this batch's OWN new test file (not the
source) were found and fixed during this step — a query containing a
hidden substring match against `_should_skip_wikipedia`'s pattern list,
and a too-short builder query that tripped an unrelated continuation-
suppression gate — each fix re-verified by a full digest-confirmed revert/
rerun cycle before the count above shipped; full detail in
`batches/F7b.md` §5.

## Limitations

- `_get_wiki_snippet_cached`'s per-term swallow-to-`None` (see "BC-58
  siblings" above) is read-only, out of this batch's ownership; a future
  batch hardening the live-Wikipedia fallback's per-term granularity would
  need to touch it.
- Producer-caller siblings of `semantic_search_with_neighbors`
  (`core/agentic/tools.py:1958`, `knowledge/synthesis_retriever.py:241`,
  `knowledge/synthesis_filter.py:496`, `knowledge/synthesis_generator.py:277`
  — F12 hand-off — and `gui/launch.py:387` warmup) still flatten a
  failed/unavailable `OutcomeList` to a plain falsy check — unaffected by
  this batch, matching F4's original sibling list exactly.
- `#75-#79, #81-#84` (`get_git_commits` through `get_relevant_emails` ×2)
  remain unaddressed in `core/prompt/gatherer_knowledge.py` — F7c's scope
  (response `CGR-20260913-007-3`), confirmed still present and unchanged
  by this batch's post-edit scan.
- No process deviation touching source or test correctness: every file
  edit used the Edit or Write tool; `git add` was never invoked; every
  pytest invocation used the guarded, capped, `-p asyncio` command with a
  `/proc/comm` guard check and a MemAvailable check immediately before it;
  `data/` was identical at every checkpoint. Two disclosed NON-PYTEST CODE
  breaches (one `python3 -c` one-liner, one empty `python3 -<<EOF`
  heredoc — both touched only `/tmp`, neither imported a project module or
  wrote a repository file) and one disclosed ORDER deviation (the source
  was briefly edited before the failing-first proof, caught and corrected
  via a clean digest-verified revert before any test ran against it) are
  recorded in full in `batches/F7b.md` §2/§5/§6, not in this immutable
  response.

## CGR-007 status

Answered: #71-#74 (F7a), #80 (F7b, this response), plus the
`_get_semantic_chunks_timed` and `SemanticSearchIndex.search` BC-58
siblings. **Still open:** #75-#79, #81-#84 (F7c, response
`CGR-20260913-007-3`) and #85-#92 (F8, response `CGR-20260913-007-4`).
