# Response to CGR-20260913-007 (follow-up -5) — web search results report
# a typed failure for an exception, a provider error and a budget refusal,
# instead of collapsing all three into the same empty section

Immutable response file, written 2026-09-14 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch (this
response): **F8b** (`docs/execution/generalization/batches/F8b.md`,
`core/prompt/gatherer_web.py`, and new
`tests/unit/test_gatherer_outcomes_web.py`). This `-5` response answers
anchor **#92** (`WebSearchMixin._get_web_search_results`), the LAST open
anchor in CGR-20260913-007 — the base response (`CGR-20260913-007.md`, F7a)
answered #71-#74; `-2` (F7b) answered #80; `-3` (F7c) answered #75-#79 and
#81-#84; `-4` (F8a) answered #87, #88, #90 and #91 with code, plus #85, #86
and #89 with off-path evidence. **CGR-20260913-007 is now fully answered.**

## Request ID / owning batch

- Request: `CGR-20260913-007` (scanner `dm18_except_returns_empty`; BC-20,
  BC-47, CM-05)
- Owning batch (this response): `F8b`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, no rebase; HEAD confirmed identical before and
  after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `core/prompt/gatherer_web.py` | `270b6208e97519812e91d88c235dd5eea5845167e58c478b13c60a1eef85a005` | `b6dae37f5096dc41e8e06ac8a1d13f6975221da18d9d084988a8f6bf7130c37f` |
| `tests/unit/test_gatherer_outcomes_web.py` | (new file) | `1e57548301725e56610675f8d8647c8bc2d25661f1e1f87a20ad5194eb0658f9` (292 lines) |
| `tests/unit/test_sep10_web_search_gap.py` (FIXTURE RULE) | `5b8c77b90e8979076f3dc6a53554e4c522b9b1b2a07c709b8d3b411c0234b6f0` | `12f2f3494da16770356d73233562d45e500cf4b352e8374dac2d55b6e88b2ae1` |
| `tests/unit/test_tavily_failure_outcomes.py` (FIXTURE RULE) | `63b036a6c843f800d78f9646deec3219f5143628a179db02cfeadc070d722f12` | `a12919f6a53ee410ebb21123c23c13254014f7f4006efa2400deb9a7f3a68656` |

The `core/prompt/gatherer_web.py` before-digest matches the request
packet's/brief's recorded source SHA-256 for #92 exactly (`270b6208…`),
reconfirmed by `sha256sum` immediately before any edit and again inside the
same failing-first command that produced the counts below.
`tests/unit/test_tavily_failure_outcomes.py` is untracked (added by F2,
never `git add`ed by any batch); its pre-edit digest was reconstructed
byte-for-byte from this session's own earlier full read (before any edit)
and independently matches F2's own parent-review-recorded digest for this
file — see `batches/F8b.md` §14 for the full note.

## What changed (summary)

`core/prompt/gatherer_web.py` (20 changed lines: 19 insertions, 1
deletion — ownership scope only: one import line plus the body of
`_get_web_search_results`):

- One new import: `from utils.retrieval_outcome import OutcomeList`.
- In the `else:` branch (not `result.has_results`), after the unchanged
  receipt write (`last_web_decision["error"] = str(result.error)` when
  `result.error` is set) and the unchanged debug log, two new checks were
  added before the existing `return None`:
  - `if getattr(result, "blocked", None) == "budget": return
    OutcomeList.unavailable("budget")` — checked FIRST, because a budget
    refusal sets BOTH `error` and `blocked="budget"`
    (`knowledge/web_search_manager.py` `search()`, read-only, 1317-1322
    reservation refused, 1401-1406 spend refused);
  - `if getattr(result, "error", None): return
    OutcomeList.failed("provider_error")` — a constant label, never
    `str(result.error)`, so "client not available"/"invalid key"/"provider
    failed (<reason>)" are never split into separate states (BC-76);
  - the genuine-empty case (no error, not blocked) falls through to the
    unchanged `return None`.
- The outer `except Exception as e:` (ANCHOR #92) keeps its existing
  `last_web_decision["error"] = type(e).__name__` write and its
  `logger.warning` line, and returns `OutcomeList.failed(type(e).__name__)`
  instead of `None`.
- The success path (`if result.has_results:` block) and every deliberate
  non-search early return (disabled, intent veto, crisis veto, no manager,
  manager unavailable, trigger unavailable, not-triggered) are
  byte-for-byte unchanged. `_cached_web_evidence` and
  `should_trigger_web_search` are untouched.
- `tests/unit/test_gatherer_outcomes_web.py` (new, 292 lines) — drives the
  DEPLOYED `WebSearchMixin._get_web_search_results` directly through a
  bare `WebSearchMixin.__new__` host, plus one builder-level case through
  the DEPLOYED `UnifiedPromptBuilder.build_prompt` with the REAL bound
  method wired via `types.MethodType` (not a mock). `MagicMock`/
  `AsyncMock`/`SimpleNamespace` fakes only — no real WebSearchManager, rate
  limiter, cache, Tavily client or network anywhere.
- Two existing tests updated under the FIXTURE RULE (see below).

No other line in `core/prompt/gatherer_web.py` changed.

## Per-anchor disposition (deployed-function evidence)

**#92** `_get_web_search_results` (outer except, line 304), `return None`.
A real BC-20/BC-47 instance (not a "not-an-instance" disposition); the new
test file proves the pre-patch defect directly on the unedited source (5
failed / 4 passed at digest `270b6208…` — see "Focused test command"
below). Fixed as described above. Deployed-function evidence:

- `TestTypedFailures::test_raising_multi_search_is_failed_with_exception_class`
  (the ANCHOR #92 site directly: `outcome_status(result) == ("failed",
  "RuntimeError")`, `result == []`, receipt `error == "RuntimeError"`,
  marker absent);
- the FIXTURE RULE edit to
  `test_sep10_web_search_gap.py::test_gatherer_exposes_search_exception`
  (the identical except site, reached with a real `TimeoutError`, every
  pre-existing receipt assertion kept);
- `TestThroughBuilder::test_raising_search_is_failed_and_label_reads_error_web_section_absent`
  (the builder-level proof: through the DEPLOYED
  `UnifiedPromptBuilder.build_prompt` with the REAL bound
  `_get_web_search_results`, `result["_section_outcomes"]["web_search"] ==
  {"status": "failed", "reason": "RuntimeError"}`,
  `result["web_search_results"] == []` — the web section absent — and the
  DEPLOYED `PromptFormatter._build_feature_inventory` on that same result
  still renders `web_search=ON(error)`, confirming the receipt-only label
  is unaffected).

## Provider-error and budget paths — BC-58 siblings fixed in the same method

Two more sites in the same `not result.has_results` branch shared the
identical "collapses into the same `None` a genuine empty search returns"
defect as ANCHOR #92, though neither is itself a separate dm18 finding
(dm18 matches only the `except: return X` shape; these are plain `if`
branches inside the `try`). Both are fixed by this batch's single edit,
recorded here as BC-58 siblings per the request packet's own note for #92
("confirm that the web evidence receipt distinguishes this outcome"):

- **Budget-refused empty result** (`result.blocked == "budget"`, checked
  FIRST): fixed → `OutcomeList.unavailable("budget")`. Deployed-function
  evidence: `TestTypedFailures::test_budget_refused_empty_result_is_unavailable_budget`,
  and — the PARENT-VERIFIED FACT that a budget refusal sets BOTH `error`
  and `blocked="budget"` together —
  `::test_budget_and_error_both_set_checks_budget_first` (the budget-first-
  ordering proof).
- **Provider-error result** (`result.error` set, not budget-blocked): fixed
  → `OutcomeList.failed("provider_error")`, a constant label. Deployed-
  function evidence: `TestTypedFailures::test_provider_error_result_is_failed_provider_error`,
  and the FIXTURE RULE edit to
  `test_tavily_failure_outcomes.py::TestDeployedConsumers::test_gatherer_receipt_records_provider_failure`
  (the real F2-produced `WebSearchResult(error=...)` shape, reached end to
  end through `_tavily_search` raising → F2's `RetrievalError` →
  `_execute_search`'s Step-1 `except RetrievalError` → a `WebSearchResult`
  with `error` set and no `blocked` — `multi_search` never raises for this
  case, so this test exercises the PROVIDER-ERROR branch, not the outer
  except).

**Limitation, per contract point 3's explicit instruction (BC-76):** the
single `"provider_error"` label does not distinguish "client not
available" from "invalid key" from "provider failed (<reason>)" — no
string-matching was added to split those states, since
`knowledge/web_search_manager.py` is read-only in this batch's ownership.

`grep -rn "_get_web_search_results(" --include=*.py .` (excluding
`tests/`/`docs/`) confirms the only production caller is
`core/prompt/builder.py:1513` — no sibling call site elsewhere.
`multi_search`'s own BC-47 aggregation sibling and
`WebSearchManager._select_links_for_following`'s dm18 finding remain
recorded (not fixed) by F2's response `CGR-20260913-008.md`, unchanged and
not rediscovered here.

## Before/after dm18 scan rows (`scripts/check_bug_classes.py scan --root .`)

Pre-edit (`core/prompt/gatherer_web.py`'s #92 finding, digest `270b6208…`):

```
dm18_except_returns_empty: core/prompt/gatherer_web.py [WebSearchMixin._get_web_search_results] 'return None' (line 304)
```

Post-edit (digest `b6dae37f…`): **no finding** for
`_get_web_search_results` in the live dm18 report (confirmed via `jq` — 0
matches). It now appears in the base scanner's STALE list instead — the
designed handoff:

```
dm18_except_returns_empty: core/prompt/gatherer_web.py [WebSearchMixin._get_web_search_results] 'return None'
```

Full scanner table, pre-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    628    36     39    0    3
dm18_except_returns_empty     gate    123    50     79    0    29
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Full scanner table, post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    629    36     39    0    3
dm18_except_returns_empty     gate    123    49     79    0    30
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped 50→49 (exactly the 1 fixed anchor, #92), `new` is
`0` both times (no new finding introduced anywhere in the tree by this
edit), `stale` rose 29→30 (the 1 new row above, plus the 29 pre-existing
rows unrelated to this batch — F2/F3a/F3b/F4/F7a/F7b/F7c/F8a's
carried-over STALE rows). `dm17_apply_without_guard`'s `files` count
ticked 628→629 (this scanner's file selection includes `tests/`, and this
batch adds one new test file; its own `found`/`new`/`stale` are
unchanged, a benign files-processed count effect, not a finding). The
overall scan exit code stays 1 both times, driven by the pre-existing
STALE rows across dm01/dm17/dm18 (unrelated to this batch — full listing
in `batches/F8b.md` §2/§12).

## Focused test command

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin systemd-run --user --scope \
  -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gatherer_outcomes_web.py \
  tests/unit/test_sep10_web_search_gap.py \
  tests/unit/test_tavily_failure_outcomes.py \
  tests/unit/test_sep12_web_evidence_budget.py \
  tests/unit/test_sep12_followup_budget_outcomes.py \
  tests/unit/test_sep09_live_controls.py \
  tests/unit/test_web_fallback_general_intent.py \
  tests/unit/test_sep12_repository_status_context.py \
  tests/unit/test_independent_prompt_audit.py
```

**186 passed**, 0 failed, 0 skipped, exit 0, wall 0:15.32, peak RSS
1,728,808 KB — plus a second chunk,
`tests/unit/test_sep12_search_budget_reservation.py` (the source of the
`_make_manager`/`_page` fixtures), **13 passed**, 0 failed, exit 0, wall
0:01.90, peak RSS 761,884 KB. Combined: **199 passed, 0 failed, 0
skipped**, across the new file (9 tests) and all 9 brief-named web test
files.

Sweep (all remaining `tests/unit` importers of `core.prompt.gatherer_web`/
`WebSearchMixin`/`core.prompt.context_gatherer`, 7 files in 1 chunk):
**140 passed, 0 failed, 0 skipped**. Full chunk breakdown, including the
4 candidates confirmed NOT genuine importers and excluded
(`test_proposal_risk.py`, `test_ordered_slice_guard.py`,
`test_upload_retrieval_pool.py`, `test_codex_followups.py`), in
`batches/F8b.md` §9-§10.

Failing-first (the new test file alone, run on the unedited source, digest
`270b6208…` printed in the same command): **5 failed, 4 passed**, exit 1 —
the 4 passes are the "existing behaviour is unchanged" controls (a genuine
empty result stays `None`; a not-triggered decision stays `None`; the
disabled toggle stays `None`; success returns the same result object). All
5 other tests fail on the unedited source exactly as expected
(`outcome_status(result) == ("no_results", "")` instead of `("failed",
...)`/`("unavailable", ...)`). No test-file bug was found — every failure
and every pass landed exactly where designed on the first attempt.

## FIXTURE RULE edits (exact)

- **`tests/unit/test_sep10_web_search_gap.py::test_gatherer_exposes_search_exception`**
  (the ANCHOR #92 except path). Added one import
  (`from utils.retrieval_outcome import outcome_status`). In the test
  body, replaced `assert await gatherer._get_web_search_results(T1) is
  None` with `result = await gatherer._get_web_search_results(T1)`
  followed by `assert outcome_status(result) == ("failed",
  "TimeoutError")` and `assert result == []`. Every existing receipt
  assertion below it (`triggered`, `source`, `results`, `error`) is kept,
  unchanged. 7 changed lines (6 insertions, 1 deletion).
- **`tests/unit/test_tavily_failure_outcomes.py::TestDeployedConsumers::test_gatherer_receipt_records_provider_failure`**
  (the provider-error result path). `outcome_status` was already imported
  (F2). Replaced `assert result is None` with `assert outcome_status(result)
  == ("failed", "provider_error")` and `assert result == []`, immediately
  above the two unchanged receipt/marker assertions (`last_web_decision["error"]`
  truthy, marker absent from it). 7 changed lines (6 insertions, 1
  deletion).

## Limitations

- **The single "provider_error" label** does not distinguish the three
  underlying provider-error shapes ("client not available", "invalid
  key", "provider failed (<reason>)") — per contract point 3's explicit
  instruction against string-matching error text (BC-76). A future batch
  wanting finer granularity needs a typed field from
  `knowledge/web_search_manager.py` (read-only here).
- **`multi_search`'s own BC-47 aggregation sibling** (a joined `errors`
  string dropped once any sub-query succeeds) and
  **`WebSearchManager._select_links_for_following`'s dm18 finding** remain
  recorded, not fixed, by F2's response `CGR-20260913-008.md` — outside
  this batch's ownership.
- **The trigger-level budget veto** (`not decision.should_search` with
  `_blocked == "budget"`, before `multi_search` is ever called) is a
  deliberate non-search per contract point 4, unchanged — it already
  returned `None` (or a cached result) before this batch, and stays that
  way; only the POST-`multi_search` budget-refused-with-no-pages result
  is newly typed.
- No process deviation touching source or test correctness: every file
  edit used the Edit or Write tool; `git add` was never invoked; every
  pytest invocation used the guarded, capped, `-p asyncio` command with a
  `/proc/comm` guard check and a MemAvailable check immediately before it;
  `data/` was identical at every checkpoint, and `logs/` stayed absent
  throughout. One disclosed process note (reconstructing the untracked
  `test_tavily_failure_outcomes.py`'s pre-edit SHA-256 from this session's
  own earlier read, since `git add`/`git diff` cannot see untracked-file
  history — independently confirmed against F2's own recorded digest for
  the same file) is recorded in full in `batches/F8b.md` §14, not in this
  immutable response.

## CGR-007 status

Answered: #71-#74 (F7a), #80 (F7b), #75-#79 and #81-#84 (F7c), #87, #88,
#90 and #91 with code plus #85, #86 and #89 with off-path evidence (F8a),
#92 with code plus its two BC-58 siblings (F8b, this response).
**CGR-20260913-007 is now fully answered (#71-#92, -1 through -5).**
CGR-009 and CGR-010 are untouched by any F7/F8 sub-batch.
