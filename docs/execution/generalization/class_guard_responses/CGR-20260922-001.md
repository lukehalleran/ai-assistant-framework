# CGR-20260922-001 — response (landed 2026-09-22, combined review clone)

| Field | Value |
|---|---|
| Request | `docs/execution/class_guards/requests/CGR-20260922-001.md` |
| Owning batch | 2026-09-22 combined review (`fix/sep22-reviewed-guards-smoke`, base `2d563f0`) |
| Detector landed | `dm38_machinery_consumers` — gate, contract v2, classes BC-91/BC-20/BC-45, leg `dm38_consumers` = python_tree over `core`, `gui`, `utils` (157 files) |
| Policy | `config/bug_class_policy.json` 2026-09-22.1; harness `expected_test_cases` 312 → 326 (13 DM-38 mutation controls + one coverage assertion) |
| Baseline | 0 occurrences at landing; no accepted debt; ceiling 0 |
| DM-39 | NOT a scanner: `tests/unit/test_sep22_graph_function_words.py` in the ordinary unit lane (spaCy lexicon required, no silent skip); reported separately from the structural scan |

## Rules as implemented (`scripts/bug_class_guards/scanners/dm38_machinery_consumers.py`)

1. **builder** — `_topup_filler` and `_recency_floor_filler` in `core/prompt/builder.py`
   canonicalize every candidate read from their declared inputs through
   `_canonical_turn_key` (set comprehension element, or the loop's comparison).
2. **delivery pipeline** — in `gui/handlers.py`, `_run_agentic_search` and `_run_enhanced`
   each call `_apply_delivery_revisions` with a snapshot whose binding chain
   (plain copies, `a or b`, suffix-free sanitizer calls, `+=` of a suffix-free
   expression) never passes through a `*_suffix`/`*_notice`/`NOTICE*` name or a
   `delivery_notice(...)` call; neither site calls a checker directly; inside the
   pipeline both checkers run on clean text and `_apply_personal_claim_check_for_delivery`
   reads a name bound from `_apply_grounding_check_for_delivery`'s result.
3. **render** — `_format_memory`, `mem_parts`, `_assemble_prompt` in
   `core/prompt/formatter.py` rebind their text through `strip_delivery_notices`.
4. **emitter registry** — anywhere in the leg except `utils/read_time_markers.py`:
   no string constant containing `> ⚠️`, and every `delivery_notice(...)` opening
   is a `NOTICE_*` name/attribute.

Differences from the request: rule 2 follows the pipeline helper the GF lane
introduced (`_apply_delivery_revisions`) rather than the two direct checker
calls the request described; the request's "supplement with rg" lines are
kept in the catalog's Find. The registry (rule 4) is stronger than requested:
the openings are ONE set of constants that both the emitters and the stripper
use, so the stripper's vocabulary cannot drift from what is emitted.

## Mutation controls (`tests/bug_class_guards/test_scanners.py::TestDM38*`)

Green: audited sites + registered emitter → 0 findings. Red: raw identity in
either filler (2); site passes the suffixed display value; site calls a
checker directly; site drops the pipeline; second revision reads the original
draft; pipeline drops a checker; pipeline receives a suffixed name; render
bypass (3); notice literal outside the leaf; unregistered opening.

## Evidence

- `python scripts/check_bug_classes.py scan --root .` → OK (dm38: 157 files, 0 found).
- Isolated harness → 326 passed; `verify-receipts --scan … --harness-junit …` → OK.
- Changed files: `scripts/bug_class_guards/scanners/{__init__,dm38_machinery_consumers}.py`,
  `config/bug_class_policy.json`, `tests/bug_class_guards/{fixtures/__init__,test_policy,test_coverage,test_scanners}.py`.
  SHA-256 before/after are in the commit (`git show --stat`); the pre-integration
  copies are archived owner-locally under `.agent_snapshots/review_handoff_20260922/`.

## Limitations

Bounded to the declared sites and the notice prefix literal. A helper that
hides a suffix under an unrelated name, or a consumer outside `core/prompt/`
and `gui/handlers.py`, is not detected; the BC-58 sibling review remains the
control for new shapes. Not whole-program taint analysis.
