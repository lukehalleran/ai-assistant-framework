# Response to CGR-20260913-007 (follow-up -3) — git commits, proposed
# features, procedural skills, graph context, unresolved threads,
# narrative, daemon self-notes and relevant emails report failure
# distinctly

Immutable response file, written 2026-09-14 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch (this
response): **F7c** (`docs/execution/generalization/batches/F7c.md`,
`core/prompt/gatherer_knowledge.py`, and new
`tests/unit/test_gatherer_outcomes_remaining_knowledge.py`). This `-3`
response answers anchors **#75, #76, #77, #78, #79, #81, #82, #83 and
#84** — the base response (`CGR-20260913-007.md`, F7a) answered #71-#74;
`-2` (F7b) answered #80. It also records the six producer-internal-swallow
BC-58 siblings named by the brief's CONTRACT point 5, each with
deployed-function evidence and its owning batch.

## Request ID / owning batch

- Request: `CGR-20260913-007` (scanner `dm18_except_returns_empty`; BC-20,
  BC-47, CM-05)
- Owning batch (this response): `F7c`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged
  throughout — no commit, no rebase; HEAD confirmed identical before and
  after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `core/prompt/gatherer_knowledge.py` | `1839020273dcf804b2be94ef67a65159fd71c89f0e3addd2a1657c82cbab98b3` | `00870a5bd0447c7b02cdfd9d993c68698d16f9f20b29dddbed2661319581f4a9` |
| `tests/unit/test_gatherer_outcomes_remaining_knowledge.py` | (new file) | `0a3d61d7ece8082b18ec50ac2b185eca570c512280060e4ddaf959a37c86fde6` |

The before-digest for `core/prompt/gatherer_knowledge.py` matches F7b's
post-edit digest (the request packet's/brief's parent-verified base for
this batch, `18390202…`) exactly, reconfirmed by `sha256sum` immediately
before any edit and again inside the same failing-first command that
produced the counts below.

## What changed (summary)

`core/prompt/gatherer_knowledge.py`, exactly the 9 except-sites named by
the brief's CONTRACT — "direct swaps," no leg-status-reading
restructuring needed (unlike F7a's producer-consuming methods; these 8
methods have no already-typed producer to read from). This batch's own
isolated contribution is 23 changed lines (the cumulative `git diff
--stat` for the file, 58 insertions/17 deletions, conflates this batch's
edit with F7a's and F7b's prior uncommitted edits to the same file — see
`batches/F7c.md` §6 for the isolated-diff cross-check):

- **`get_git_commits`** (#75): except `return []` → `return
  OutcomeList.failed(type(e).__name__)`; `logger.warning` line unchanged.
- **`get_proposed_features`** (#76): same shape.
- **`get_procedural_skills`** (#77): same shape.
- **`get_graph_context`** (#78): same shape.
- **`get_unresolved_threads`** (#79): same shape.
- **`get_narrative_context`** (#81, `str`-returning, SYNC): except
  `return ""` → a 5-line comment plus `raise` — the disabled-flag and
  missing-corpus-manager early returns above the try are untouched, still
  `return ""`. The only caller, `core/prompt/builder.py:1072`, already
  wraps this call in a try/except (F5-integrated, `builder.py:1071-1079`)
  that records `section_outcomes["narrative"] = {"status": "failed",
  "reason": type(e).__name__}` — previously dead code, since the gatherer
  never raised; this batch's edit is what makes that except branch
  actually reachable for a real producer failure.
- **`get_daemon_self_notes`** (#82): same shape as the first five
  (`logger.debug` line unchanged); the inner per-item annotation-failure
  swallow (2083-2095) is untouched.
- **`get_relevant_emails`** inner except (#83): `return []` → `return
  OutcomeList.failed("relevance_unavailable")` — the two-line "Fail closed
  rather than injecting arbitrary inbox content" comment stays
  byte-for-byte; still carries zero items.
- **`get_relevant_emails`** outer except (#84): same shape as the first
  five.
- `tests/unit/test_gatherer_outcomes_remaining_knowledge.py` (new, 409
  lines) — drives the DEPLOYED `KnowledgeRetrievalMixin.get_git_commits`,
  `get_proposed_features`, `get_procedural_skills`, `get_graph_context`,
  `get_unresolved_threads`, `get_daemon_self_notes` and
  `get_relevant_emails` directly through a minimal `_G(KnowledgeRetrievalMixin)`
  host, plus `get_narrative_context` both directly and through the
  DEPLOYED `UnifiedPromptBuilder.build_prompt` (a local copy of the
  `full_builder`/`retrieval_limits` fake-builder pattern established by
  `tests/unit/test_independent_prompt_audit.py`, also used by F5/F7a/F7b).
  `Mock`/`AsyncMock`/`SimpleNamespace` fakes only — no real ChromaDB,
  embedder, graph store, email service or network anywhere. The six
  direct-swap sections share an identical (raise/empty/nonempty
  [/disabled]) contract shape, parametrized across sections instead of
  repeated per method.

No other line in `core/prompt/gatherer_knowledge.py` changed. Every legit
early return (feature-flag gates, `hasattr` gates, missing-corpus-manager)
stays untouched.

## Per-anchor disposition (deployed-function evidence)

All nine are real BC-20/BC-47 instances (not "not-an-instance"
dispositions); the new test file proves the pre-patch defect directly on
the unedited source (10 failed / 22 passed at digest `18390202…` — see
"Focused test command" below).

- **#75** `get_git_commits` (line 1116), `return []`. Fixed. Evidence:
  `test_raising_producer_is_failed[git_commits]` (index-branch chroma
  raises), `test_control_healthy_empty_is_no_results[git_commits]`,
  `test_control_healthy_nonempty_is_succeeded[git_commits]`,
  `test_control_disabled_flag_returns_empty_unchanged[git_commits]`.
- **#76** `get_proposed_features` (line 1171), `return []`. Fixed.
  Evidence: the same four-test pattern with `[proposed_features]` (a
  raising `_proposal_filter.get_proposals`).
- **#77** `get_procedural_skills` (line 1214), `return []`. Fixed.
  Evidence: `[procedural_skills]` (a raising
  `memory_coordinator.get_skills`).
- **#78** `get_graph_context` (line 1276), `return []`. Fixed. Evidence:
  `[graph_context]` (a raising `graph.get_context_sentences`, reached
  through the real `memory.graph_utils.extract_graph_entities`).
- **#79** `get_unresolved_threads` (line 1303), `return []`. Fixed.
  Evidence: `[unresolved_threads]` (a raising
  `memory_coordinator.get_unresolved_threads`).
- **#81** `get_narrative_context` (line 2036), `return ""`. Fixed (the
  except now re-raises instead of swallowing). Evidence:
  `test_narrative_producer_raise_reraises` (direct: `pytest.raises`),
  `test_narrative_disabled_flag_returns_empty_string`,
  `test_narrative_missing_corpus_manager_returns_empty_string` (both
  legit-early-return controls, contract point 4),
  `test_through_builder_narrative_raise_recorded_failed` (the brief's
  required builder-level proof: the DEPLOYED `build_prompt` still builds,
  `narrative_state == ""`, `_section_outcomes["narrative"] == {"status":
  "failed", "reason": "RuntimeError"}`),
  `test_through_builder_max_narrative_zero_skips_entry_control` (F5
  sibling control: no entry, producer never called).
- **#82** `get_daemon_self_notes` (line 2101), `return []`. Fixed.
  Evidence: `[daemon_self_notes]` (a raising `chroma.query_collection`).
- **#83** `get_relevant_emails` ranking-stage except (line 2293), `return
  []`. Fixed: `OutcomeList.failed("relevance_unavailable")`, still zero
  items, comment unchanged. Evidence:
  `test_raising_embedder_is_failed_relevance_unavailable` (a raising
  `ModelManager._get_cached_embedder`).
- **#84** `get_relevant_emails` outer except (line 2297), `return []`.
  Fixed: `OutcomeList.failed(type(e).__name__)`. Evidence:
  `test_raising_service_search_is_failed` (a raising `service.search`).

## BC-58 siblings (producer-internal swallows, contract point 5)

Recorded with deployed-function evidence, per the brief — none edited
(read-only, out of this batch's ownership):

| Producer | File:line | Swallow | Other production callers | Owning batch |
|---|---|---|---|---|
| `GitMemoryExtractor.extract_commits` | `knowledge/git_memory.py:67,103-109` | `except (FileNotFoundError, OSError, TimeoutExpired)` / non-zero returncode → `[]` | `knowledge/git_memory_loader.py:67,130` | unowned |
| `ProposalStore.query_proposals` | `memory/proposal_store.py:105,161-163` | `except Exception` → `[]` | none besides `proposal_filter.py:661` (itself only called by our gatherer) | F11b |
| `MemoryRetriever.get_skills` | `memory/memory_retriever.py:1107,1183-1185` | `except Exception` → `[]` | `memory/shutdown_processor.py:1312` | F12 |
| `MemoryCoordinator.get_unresolved_threads` | `memory/memory_coordinator.py:585,598-600` | `except Exception` → `[]` | none | F11a |
| `CorpusManager.get_narrative_context` | `memory/corpus_manager.py:605,660-662` | `except Exception` → `""` | none | F12 |
| `EmailService._fan_out` | `core/email/service.py:53,57-60` | per-provider `except Exception` → `continue` (total outage reduces to `[]`/partial without raising) | `core/agentic/tools.py`, `core/agentic/controller.py`, `gui/handlers.py` | unowned |

Also recorded (read-only, this batch's own file): the self-notes inner
per-item annotation-failure swallow (`get_daemon_self_notes`, 2083-2095)
— untouched, only the outer except (2099-2101) was edited. And one
off-path caller, `debug_graph_context.py:43` (a standalone manual debug
script, not imported by any production or test module), confirmed
unaffected — it only inspects truthiness/length/iteration of
`get_graph_context`'s result, identical for a plain `list` and an
`OutcomeList`.

**Consequence, stated as a limitation per section (contract point 5):**
until F9-F12 land, a failure INSIDE any of these six producers still
reads as `no_results` at this batch's gatherer boundary, not `failed` —
this batch's fix converts only the GATHERER's own except (an exception
that escapes the producer, e.g. a misconfigured/absent producer, or the
producer's own code raising past its internal swallow).

## Before/after dm18 scan rows (`scripts/check_bug_classes.py scan --root .`)

Pre-edit (`core/prompt/gatherer_knowledge.py`'s 9 findings for this
batch's anchors, digest `18390202…`):

```
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_git_commits] line=1116          -- #75
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_proposed_features] line=1171    -- #76
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_procedural_skills] line=1214    -- #77
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_graph_context] line=1276        -- #78
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_unresolved_threads] line=1303   -- #79
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_narrative_context] line=2036    -- #81
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_daemon_self_notes] line=2101    -- #82
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_relevant_emails] line=2293      -- #83
core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_relevant_emails] line=2297      -- #84
```

Post-edit (digest `00870a5b…`): **no findings** for these 9 symbol/line
pairs in the live dm18 report (confirmed via `jq` — a query for
`select(.path=="core/prompt/gatherer_knowledge.py")` over the live
findings returns **zero rows total**, meaning every method in this file,
across F7a+F7b+F7c combined, is now gone from the live report). They now
appear in the base scanner's STALE list instead — the designed handoff:

```
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_daemon_self_notes] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_git_commits] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_graph_context] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_narrative_context] 'return ""'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_procedural_skills] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_proposed_features] 'return []'
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_relevant_emails] 'return []'   (×2, #83 and #84)
dm18_except_returns_empty: core/prompt/gatherer_knowledge.py [KnowledgeRetrievalMixin.get_unresolved_threads] 'return []'
```

**The re-raising `get_narrative_context` except is confirmed NOT a dm18
finding**, before or after this batch (0 matches in the live report both
times, via the same `jq` query): dm18 matches an `except: return X`
shape; a bare `raise` returns nothing and structurally cannot match.

Full scanner table, pre-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    626    36     39    0    3
dm18_except_returns_empty     gate    123    63     79    0    16
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

Full scanner table, post-edit:

```
scanner                       mode    files  found  base  new  stale
----------------------------  ------  -----  -----  ----  ---  -----
dm01_raw_substring            gate    396    7      12    0    5
dm16_config_key_reachability  report  395    89     -     -    -
dm17_apply_without_guard      gate    627    36     39    0    3
dm18_except_returns_empty     gate    123    54     79    0    25
dm29_phrase_append_signature  report  0      0      -     -    -
dm31_live_state_default       gate    396    3      3     0    0
catalog                       gate    1      0      0     0    0
```

`dm18` `found` dropped 63→54 (exactly the 9 fixed anchors), `new` is `0`
both times (no new finding introduced anywhere in the tree by this edit),
`stale` rose 16→25 (the 9 new rows above, plus the 16 pre-existing rows
unrelated to this batch — F2/F3a/F3b/F4/F7a/F7b's carried-over STALE
rows). `dm17_apply_without_guard`'s `files` count ticked 626→627 (this
scanner's file selection includes `tests/`, and this batch adds one new
test file; its own `found`/`new`/`stale` are unchanged, a benign
files-processed count effect, not a finding). The overall scan exit code
stays 1 both times, driven by the pre-existing STALE rows across
dm01/dm17/dm18 (unrelated to this batch — full listing in `batches/F7c.md`
§2/§11).

## Focused test command

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin systemd-run --user --scope \
  -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v env DAEMON_TEST_MODE=1 \
  CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 \
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
  python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gatherer_outcomes_remaining_knowledge.py \
  tests/unit/test_sep12_repository_status_context.py \
  tests/unit/test_proposal_filter.py \
  tests/unit/test_independent_prompt_audit.py \
  tests/unit/test_narrative_staleness.py \
  tests/unit/test_sep10_probe_dump_actions.py \
  tests/unit/test_sep10_probe_dump_interpretation.py \
  tests/unit/test_email_passive_context.py \
  tests/unit/test_sep03_followups_gating.py
```

**534 passed**, 0 failed, 0 skipped, exit 0, wall 0:15.64, peak RSS
1,754,272 KB — the new file's 32 test cases plus the 8 brief-named
existing files.

Sweep (all remaining `tests/unit` importers of `core.prompt.
gatherer_knowledge`/`KnowledgeRetrievalMixin`/`core.prompt.context_gatherer`,
30 files in 4 chunks of ≤9, permanently excluding `test_graph_
integration.py`): **633 passed, 0 failed, 0 skipped**. Full chunk-by-chunk
breakdown in `batches/F7c.md` §9.

Failing-first (the new test file alone, run on the unedited source, digest
`18390202…` printed in the same command): **10 failed, 22 passed**, exit
1 — the 22 passes are the "existing behaviour is unchanged" controls
(healthy-empty/healthy-nonempty/disabled-flag for all 6 sections; the
no-cue and healthy-ranked email controls; the disabled-flag and
missing-corpus-manager narrative controls; the `max_narrative=0` control).
All 10 other tests fail on the unedited source exactly as expected
(`outcome_status(result) == ('no_results', '')` instead of `('failed',
...)`; `Failed: DID NOT RAISE <class 'RuntimeError'>` for the narrative
direct-raise test). No test-file bug was found during failing-first this
time — every failure and every pass landed exactly where designed on the
first attempt.

## Limitations

- The six producer-internal swallows (see "BC-58 siblings" above) mean
  end-to-end failure detection for those sections waits for F9-F12; this
  batch closes only the gatherer-boundary anchors.
- **#85-#92 are explicitly not addressed** — `core/prompt/gatherer_
  memory.py`'s `get_recent_facts`, `get_facts`, `_get_recent_conversations`,
  `_get_semantic_memories`, `_get_reflections`, `get_user_profile_context`,
  `get_upcoming_schedule`, and `core/prompt/gatherer_web.py`'s `_get_web_
  search_results` — owned by **F8**, response `CGR-20260913-007-4`.
- `memory/shutdown_processor.py:1312`'s `get_skills` call and
  `core/agentic/tools.py`/`core/agentic/controller.py`/`gui/handlers.py`'s
  `get_email_service()` calls are separate production consumers of
  producers this batch's siblings touch — unaffected, out of this
  batch's ownership.
- `debug_graph_context.py:43` is an off-path manual debug script,
  confirmed unaffected, not part of the class-guard's scope.
- No process deviation: every file edit used the Edit or Write tool;
  `git add` was never invoked; every pytest invocation used the guarded,
  capped, `-p asyncio` command with a `/proc/comm` guard check and a
  MemAvailable check immediately before it; `data/` was identical at
  every one of 13 explicit checkpoints, and `logs/` stayed absent
  throughout. No non-pytest Python ran beyond the three standing
  exceptions (ruff, the read-only scan, the one `import utils` sanity
  check). One INTERRUPTION occurred (an API session limit at ≈14:32,
  resumed at 15:01 after independent parent verification) — disclosed in
  full in `batches/F7c.md` §3; it is the rule-anticipated scenario, not a
  process-rule breach, and the resumption re-verified the manifest,
  digest, and `data/`/`logs/` state independently before continuing.

## CGR-007 status

Answered: #71-#74 (F7a), #80 (F7b), #75-#79 and #81-#84 (F7c, this
response). **Still open:** #85-#92 (F8, response `CGR-20260913-007-4`).
CGR-009 and CGR-010 are untouched by any F7 sub-batch.
