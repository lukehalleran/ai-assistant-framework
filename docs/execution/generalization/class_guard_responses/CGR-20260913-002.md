# Response to CGR-20260913-002 — Agentic gate keyword tests outside utils.trigger_match

Immutable response file, written 2026-09-13 by the generalization plan under
`docs/PLAN_20260913_generalization_execution.md`. Owning batch: **R04**
(this response file, `docs/execution/generalization/batches/R04.md`, new
`tests/unit/test_gate_keyword_boundaries.py`, and `core/agentic/gate.py`).
This file is immutable once written; any follow-up is a new file with a `-2`
suffix, per the class-guard request contract.

## Request ID / owning batch

- Request: `CGR-20260913-002` (scanner `dm01_raw_substring`, BC-01, BC-02)
- Owning batch: `R04`
- Base tree: `328a8ecea1dae91de16f5974ea71727c4e2e1aa0` (unchanged throughout —
  no commit, no rebase, HEAD confirmed identical before and after this batch)

## Changed files and SHA-256 before/after

| File | SHA-256 before | SHA-256 after |
|---|---|---|
| `core/agentic/gate.py` | `86c5a65efa03cb48d7a5a20a125fa8f0739311c37efd03ef9d3807cf52cee378` (matches the request's recorded source SHA-256 exactly) | `15ae94e647522e7b24aee4556b47894c3aafd4ef64adf21e83d1957310bdcbd9` |
| `tests/unit/test_gate_keyword_boundaries.py` | (new) | `f473827256938e035c4a5125b679b8976207ff4dfb29f1ff931b89239598cf97` |

`git diff --stat` for `core/agentic/gate.py`: **33 insertions, 7
deletions — 40 changed lines**; the new test file is 347 lines. **387
changed lines total**, within the 450-line hard cap. No file outside this
list was touched.

## Changed files (summary)

- `core/agentic/gate.py`:
  - Added three module-level vocabulary constants (`EMAIL_INTENT_CUE_WORDS`,
    `EMAIL_INTENT_VERB_WORDS`) and four module-level compiled chokepoint
    matchers beside the existing ones (`_CONTINUATION_PHRASE_HIT`,
    `_SEARCH_SIGNAL_HIT`, `_EMAIL_INTENT_CUE_HIT`, `_EMAIL_INTENT_VERB_HIT`,
    `_EXPLICIT_SEARCH_HIT`), each `_compile_keyword_matcher(...)` over the
    existing vocabulary — no phrase added to or removed from any vocabulary.
  - Replaced the raw `any(p in _lower for p in CONTINUATION_PHRASES)` /
    `any(w in _lower for w in SEARCH_SIGNAL_WORDS)` /
    `any(kw in _lower for kw in EXPLICIT_SEARCH_KEYWORDS)` / `'email' in
    _lower and any(w in _lower for w in (...))` expressions at the seven
    anchored call sites (lines 749, 795, 998-999, 1106, 1133, 1211, 1540 in
    the patched file) with calls to the compiled matchers, negation-aware
    (`_hit_non_negated`, the file's existing wrapper around
    `utils.trigger_match.find_hits` — semantically identical to
    `has_non_negated_hit`) at the two request-cue sites (#5, #9) only.
- `tests/unit/test_gate_keyword_boundaries.py` — new; deployed-function
  outcome fixtures for all seven anchors plus two follow-up sibling classes
  (see `docs/execution/generalization/batches/R04.md`).

## Focused test command, exit code, counts

```
PYTHONPATH=/home/lukeh/daemon_exec/generalization/scripts/bin \
systemd-run --user --scope -p MemoryMax=6G -p MemorySwapMax=512M /usr/bin/time -v \
env DAEMON_TEST_MODE=1 CHROMA_DEVICE=cpu OMP_NUM_THREADS=2 MKL_NUM_THREADS=2 \
OPENBLAS_NUM_THREADS=2 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 \
python -m pytest -q -p no:cacheprovider -p asyncio \
  tests/unit/test_gate_keyword_boundaries.py tests/unit/test_agentic_gate.py \
  tests/unit/test_gate_system_helpers.py tests/unit/test_gate_self_report_memory_backstop.py
```

- **Failing-before** (pristine `core/agentic/gate.py`, i.e. before this
  batch's patch; new test file added first per the plan's state sequence).
  Reconfirmed against the exact final test file content by temporarily
  reverting `core/agentic/gate.py` to its pristine text (plain file
  replacement, re-hashed to `86c5a65e...` before the run and restored/
  re-hashed to `15ae94e6...` after — no `git` command involved): **22
  failed, 142 passed**, exit 1, elapsed 0:06.03, max RSS 1,080,088 KB.
  All 22 failures are exactly the seven anchors' counterexamples/newly-
  matched-inflection assertions (see per-anchor disposition below); zero
  unexpected failures.
- **After the patch**: **164 passed**, exit 0, elapsed 0:05.79, max RSS
  1,080,404 KB.

Broader sweep (report-only, not edited): every `tests/unit/*.py` file that
imports `evaluate_agentic_gate` (29 files, found via
`grep -rl evaluate_agentic_gate tests/unit/*.py`), run together after the
patch: **1120 passed, 8 failed**, elapsed 0:47.42, max RSS 1,769,680 KB. The
8 failures are all `test_sep09_speed_batch.py::test_real_verifier_outcomes_reach_receipt_and_debug[...]`,
asserting on `core/grounding_check.py`'s verifier-receipt path with
`evaluate_agentic_gate` **fully monkeypatched**
(`monkeypatch.setattr(gate, "evaluate_agentic_gate", AsyncMock(...))`) —
confirmed pre-existing and unrelated to this batch (that file is dirty from
an earlier integrated-but-uncommitted batch per the parent's manifest; this
response does not touch it, per the plan's routine-failure contingency for
"a known unrelated reason").

`ruff 0.14.9`; `python -m ruff check core/agentic/gate.py
tests/unit/test_gate_keyword_boundaries.py`: **All checks passed!**

## Per-anchor disposition

| # | Line (patched) | Vocabulary | Disposition | Evidence |
|---:|---:|---|---|---|
| 3 | 749 | CONTINUATION_PHRASES (tone-deferral affirmation) | **fixed** | `TestAnchor3ToneDeferralAffirmation` — `'yesterday'` (⊃ `'yes'`) no longer affirms; `'yes please'` still does (clean + wrapped). |
| 4 | 795 | CONTINUATION_PHRASES (insight-offer affirmation) | **fixed** | `TestAnchor4InsightOfferAffirmation` — `'measure'` (⊃ `'sure'`) no longer affirms; `'go ahead'` still does (clean + wrapped). |
| 5 | 998-999 | email-intent verbs (inline tuple, now `EMAIL_INTENT_CUE_WORDS`/`EMAIL_INTENT_VERB_WORDS`) | **fixed** | `TestAnchor5EmailIntentVerbs` — `'rewrite'` (⊃ `'write'`), `'sender'` (⊃ `'send'`), and negated `"Don't send that email"` (BC-02) no longer grant the arm; `"write an email for the professor"` and `"send an email to Morgan..."` still do (clean + wrapped). |
| 6 | 1106 | SEARCH_SIGNAL_WORDS (casual-skip filter) | **fixed** | `TestAnchor6CasualSkipSearchSignal` — `'research'` (⊃ `'search'`) and `'outlook'` (⊃ `'look'`) no longer supply a search signal (deployed `reason` flips from `'no trigger'` to `'casual/short message'`); `"look up the latest developments..."` still does (clean + wrapped). |
| 7 | 1133 | CONTINUATION_PHRASES (continuation-override check) | **fixed** | `TestAnchor7ContinuationOverride` — `'yesterday'` no longer marks the prior turn inherited (`reason` stays `'casual/short message'` instead of falling to `'no trigger'`); `'yes please'` still overrides (clean + wrapped). |
| 8 | 1211 | CONTINUATION_PHRASES (file-retrieval affirmation) | **fixed** | `TestAnchor8FileRetrievalAffirmation` — `'pressure'` (⊃ `'sure'`) no longer consumes a file offer; `'go ahead'` still does (clean + wrapped). |
| 9 | 1540 | EXPLICIT_SEARCH_KEYWORDS (intent-veto exemption) | **fixed** | `TestAnchor9ExplicitSearchVetoExemption` — `'research'` (⊃ `'search'`) and a negated `"don't search for that..."` (BC-02) no longer grant `veto_exempt`; `"search for the latest developments"` and `"pull up the article"` still do. |

No anchor was left unfixed.

## Sibling findings (report only — see `docs/execution/generalization/batches/R04.md` for full detail)

- Line 961's `any(w in _lower for w in ('email', 'send', 'message', 'write', 'mail', 'contact'))`
  (the "Email address + action verb" arm, gated on an actual `\S+@\S+\.\S+`
  address) is the same raw-membership shape but is **not** one of the seven
  requested anchors and was left unchanged, per the plan's contingency not
  to fix a baseline item outside the request that issued it.
- `_tone_is_elevated`/`_tone_is_acute` (uppercase substring checks over
  `_ELEVATED_TONE_MARKERS`/`_ACUTE_TONE_MARKERS`, ~lines 596-641) are
  confirmed NOT anchors of this request (per the parent's ready record) and
  were not changed.
- Two newly-matched, sense-preserving inflections from the chokepoint's
  e-drop rule: `'write'` now also matches `'writing'`/`'writings'` and
  `'compose'` now also matches `'composing'` (the old raw substring check
  missed these entirely — no literal `'write'`/`'compose'` substring
  survives dropping the final `'e'`). Accepted; both were failing-before and
  are now green (`TestAnchor5NewlyMatchedInflections`).
- A characterization (not a fix) for `"please don't do it"`: CONTINUATION_PHRASES
  is deliberately NOT made negation-aware in this batch (per the request's
  contract), so this text still reads as an affirmation despite the leading
  `"don't"`. Recorded in `TestContinuationNegationCharacterization` as a
  follow-up sibling for a future request.

## Before/after dm01 scan tables

Both runs use the checkout's currently-installed (older/"contract v1")
`dm01_raw_substring` scanner, which skips an entire module outright once it
imports `utils.trigger_match` anywhere (`_imports_chokepoint`,
`scripts/bug_class_guards/scanners/dm01_raw_substring.py:105-116`).
`core/agentic/gate.py` already carried that import (lines 108-110) before
this batch, so it was — and remains — fully exempt from this scanner
regardless of the seven anchors' fix; this checkout's scanner cannot see
either the pre-existing defect or this batch's remedy. Verified directly by
invoking `dm01_raw_substring.scan()` on the file in isolation:

```
PRISTINE (HEAD, 86c5a65e...): files_processed=1, findings=0
PATCHED  (this batch, 15ae94e6...): files_processed=1, findings=0
```

Full-repo scan, before (pristine snapshot: `core/agentic/gate.py` reverted
to HEAD byte-for-byte, `tests/unit/test_gate_keyword_boundaries.py` absent)
vs after (this batch's actual tree):

```
                               BEFORE                          AFTER
scanner                       files  found  base  new  stale   files  found  base  new  stale
dm01_raw_substring             394      9    12    0     3       394     9    12    0     3
dm17_apply_without_guard       599     36    39    0     3       600    36    39    0     3
dm18_except_returns_empty      123     79    79    0     0       123    79    79    0     0
dm31_live_state_default        394      3     3    0     0       394     3     3    0     0
```

`found`/`base`/`new`/`stale` are byte-identical before and after in every
gated scanner (`new: 0` everywhere in both). The only delta is
`dm17_apply_without_guard`'s file-walk count (599 → 600), from this batch's
one new test file existing on disk — it produces zero new `dm17` findings.
**dm01's counts do not change** as a direct result of this batch, for the
reason above (gate.py's whole-module exemption predates and survives this
batch). The 6 STALE rows (3 `dm01` on `knowledge/git_memory.py`, 3 `dm17` on
three `scripts/*.py` files) are R01's and R03's designed pre-existing debt,
untouched by this batch, present identically before and after. Exit code
after the patch: **1** (expected — the 6 pre-existing STALE rows; not a
regression introduced here). `--write-baseline` was never passed.

Per the request's acceptance contract: since this checkout's installed
scanner cannot see anchors #3-#9 at all (whole-module exemption), there is
no `dm01` row for `core/agentic/gate.py` to go STALE here — the class-guard
owner's own review process (the one that produced this request, presumably
against a newer/"contract v2" scanner or manual review) is what should
re-confirm the fix and mark anchors #3-#9 `confirmed_fixed` with this
request ID once integrated into a tree that checkout can scan.

## Limitations

- This is deterministic unit-test evidence only ("focused contract green");
  it is not a synthetic assembled probe, owner canary, or population claim.
- This checkout's scanner is the older ("contract v1") version that exempts
  a module wholesale once it imports the chokepoint anywhere; it cannot
  independently corroborate this fix or ever show anchors #3-#9 as `new`,
  `stale`, or `confirmed_fixed` — see the scan section above.
- Negation semantics were deliberately NOT added to CONTINUATION_PHRASES
  (#3, #4, #7, #8) or SEARCH_SIGNAL_WORDS (#6) in this batch, per the
  request's contract; `"please don't do it"` still reads as an affirmation
  (recorded characterization, not a defect of this batch).
- Line 961's raw-membership email arm and the two tone-marker helper
  functions are confirmed non-anchors and were left unchanged; they are not
  re-reviewed by this response.
- No live Daemon process was started or exercised; `data/`
  (`benchmark_per_case.csv`, `chroma_db_v4`, `embedding_migration_manifest.json`,
  `pipeline`) was unchanged before and after every command in this batch; no
  live store, network credential, or provider was used.
