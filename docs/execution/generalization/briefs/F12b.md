=== F12b: a daily note is never written without its status guard; the daily-notes auto-update test stops reaching the real profile and vault ===
(Parent brief, 2026-09-14. Drafted while F10a ran; re-verified during F11b-1b; launched after F12a was integrated. None of these files is edited by F10–F12a.)

Design source: docs/execution/generalization/failure_outcome_design.md
- Real-defects row: "The narrative status guard silently turns off on a read failure …; duplicate at `utils/daily_notes_generator.py:440-452` (sibling, not in packet)".
- "consolidation marks `status_guard=unavailable` and does not persist a guard-less narrative as authoritative" — applied here to the daily note, which is written to the vault.
- Amendment "F12 split (parent decision, 2026-09-14)": F12b = this sibling plus the test hermeticity fix. F12a made `MemoryConsolidator._current_status_facts` raise `RetrievalError` (see batches/F12a.md's parent section) — mirror its shape exactly.
Request packets: none. This is not a scanner anchor; do NOT read any class-guard file. There is no response file; the evidence lives in batches/F12b.md, and F12a's CGR-009-6 response names this batch.
BUG_CLASSES: BC-20, BC-47, CM-05.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE — measure before editing later parts; never implement then revert; never trim to fit — and SCANNER SHAPE — catch the TYPED failure for a contract-required degrade; escalate a new gate finding).

COMPLIANCE (read twice):
- Your first source write — to ANY file, including tests/unit/test_daily_notes_auto_update.py — comes only after the failing-first run is recorded in batches/F12b.md.
- NEVER put `python` in any Bash command except the three allowed forms: `python -m pytest …` (through the capped systemd-run), `python -m ruff check …`, `python scripts/check_bug_classes.py scan --root .`, plus the one `python -c "import utils; print(utils.__file__)"` sanity check. No other `python -c`, heredoc, REPL or script — not even a no-op. Build chunk lists with grep/sed or by hand.
- Check each fixture against the gates it must pass (auto-update threshold, force flag, existing-note check, model fallback list, tag generation flag, `NARRATIVE_CONTEXT_ENABLED`).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Timestamp packet entries from `date` output, not estimates.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F12a.txt` and `$S/manifest_paths_post_F12a.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_post_F12a.txt`. Re-check after every chunk; ANY difference means STOP and report. tests/unit/test_daily_notes_auto_update.py is EXCLUDED from every run in R_common_rules until this batch's hermeticity fix is in place: run it for the FIRST time only after your edit to it, and only after confirming by reading that every `generate_for_date` success path in it stubs `_trigger_narrative_refresh` (or sets `NARRATIVE_CONTEXT_ENABLED` False) and supplies a fake profile — then check data/ immediately after that chunk. For the failing-first proof of the hermeticity fix, do NOT run the unedited file; prove it by reading and recording the exact code path instead.
ORDER REMINDER: print `sha256sum utils/daily_notes_generator.py tests/unit/test_daily_notes_auto_update.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control; the hermeticity edit is a separate, recorded kind of edit (no assertion changes). List every edit.

PARENT-VERIFIED FACTS (re-verified 2026-09-14; confirm at the manifest check)
- Starting digests: utils/daily_notes_generator.py `9964c546…` (1009 lines), tests/unit/test_daily_notes_auto_update.py `5eeeae8f…`; read-only memory/memory_consolidator.py `60b05eba…` (F12a state), utils/status_claims.py `f7801d1e…`, utils/retrieval_outcome.py `1abc1586…`.
- utils/daily_notes_generator.py:
  - `GenerationResult` (dataclass, def 195): `success: bool = False`, `date`, `output_path`, `conversation_count`, `intensity`, `duration_hours`, `skipped_reason: Optional[str]` (documented values "already_exists", "no_conversations", "disabled"), `error: Optional[str]`.
  - `user_profile` property (def 426): the same lazy default-path `UserProfile()` with a swallow → None.
  - `_current_status_facts` (def 440): an exact copy of the consolidator's pre-F12a version — None profile → []; `except Exception as e: logger.debug(...); return []`.
  - `_write_note` (def 726).
  - `generate_for_date` (def 738), no outer try: `status_facts = self._current_status_facts()` (801) → `authoritative_facts_block` (802) → LLM `generate_once` over a model fallback list with per-model try → `remove_conflicting_claims(llm_response, status_facts)` (886) → optional `tag_generator.generate_tags` → try: `result.output_path = self._write_note(target_date, full_content)` (926) … `await self._trigger_narrative_refresh()` / except. A guard-less note is written to the vault today when the profile read fails. Read ≈738–937 before editing.
  - `_trigger_narrative_refresh` (def 939): gated by `NARRATIVE_CONTEXT_ENABLED` (default True); builds `MemoryConsolidator(self.model_manager)` WITHOUT a profile → `generate_narrative_context` → `save_narrative_context`; `except Exception as e: logger.warning(...)`.
  - Callers of `generate_for_date`: main.py shutdown step (inside a try, "non-critical"); main.py CLI `daily-note` mode (no try); gui/launch.py catch-up thread (caught); scripts/daily_note_catchup.py `run_catchup` (no try); `generate_yesterday_if_missing` and `generate_date_range` (no try).
- tests/unit/test_daily_notes_auto_update.py: the success-path tests (≈276–303 `test_auto_update_proceeds_when_threshold_met`, ≈330–337 `test_force_overrides_auto_update_check`) reach `_trigger_narrative_refresh` → a real `MemoryConsolidator` → a real default-path `UserProfile()` and the real `OBSIDIAN_VAULT_PATH`. The fixture sets `gen._user_profile` on the generator only (≈58); the vault fixture uses tmp_path (≈31–33). This is why the file is excluded.

OWNERSHIP
- utils/daily_notes_generator.py: the body and docstring of `_current_status_facts` and, inside `generate_for_date`, the status-facts read (≈801–802) ONLY (plus one import line).
- tests/unit/test_daily_notes_auto_update.py: the fixture/test setup needed to make it hermetic ONLY (stub `_trigger_narrative_refresh` or monkeypatch `NARRATIVE_CONTEXT_ENABLED` False where the refresh is not under test; supply a fake profile wherever a consolidator could be built); no assertion changes.
- New tests/unit/test_daily_note_status_guard.py; new docs/execution/generalization/batches/F12b.md.
- Read-only: every other generator method, main.py, gui/launch.py, scripts/**, memory/memory_consolidator.py (F12a), utils/status_claims.py, memory/user_profile.py, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `_current_status_facts`: mirror F12a exactly — profile None → `raise RetrievalError(source="status_facts", reason="profile_unavailable")`; `get_current_view` raising → keep the log line, then `raise RetrievalError(source="status_facts", reason=type(e).__name__) from e`; a genuine empty → [].
2. `generate_for_date`: catch the TYPED `RetrievalError` from the status-facts read → one warning (labels only) → return the method's result object with `success=False` and `error="status_guard_unavailable"` (a constant label; do not reuse `skipped_reason`), BEFORE any LLM call, tag generation, note write or narrative refresh. No exception escapes, so the no-try callers (CLI, catch-up script, date-range loop) are unchanged. Every other path is unchanged. Read how `result` is built and returned before this point and reuse that object.
3. The hermeticity edit makes test_daily_notes_auto_update.py reach no real profile, vault path or data path; its assertions are unchanged and still pass.
4. If any gate scanner reports a NEW finding on your edit, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_daily_note_status_guard.py; fakes only — fake profile, tmp_path vault/output directory, stub model manager, stubbed `_trigger_narrative_refresh`)
- FAILING FIRST in one command: the digest line above, then the new test file ALONE on the UNEDITED source. Record the failures.
- `_current_status_facts`: raising `get_current_view` → `RetrievalError(source="status_facts", reason=<class>)`; profile None → `profile_unavailable`; healthy → facts / [] (controls).
- `generate_for_date`: raising status facts → `success is False`, `error == "status_guard_unavailable"`, `generate_once` never awaited, no file written under the tmp output directory, `_trigger_narrative_refresh` never awaited; healthy → the note is written to tmp_path and the guard block appears in the prompt (control). Read the method's gates first (conversation lookup, existing-note check, force flag) and build the minimal fixture that reaches line ≈801.
- Privacy: markers in injected exception messages never appear in the new warning or result fields; pre-existing log lines kept unchanged may carry `{e}` — do not assert on those.
- Focused (≤9 per chunk, in this order): chunk 1 = the new file, test_consolidator_read_outcomes.py (F12a), test_narrative_status_claims.py, test_sep03_followups_gating.py, test_retrieval_outcome.py; chunk 2 = tests/unit/test_daily_notes_auto_update.py ALONE, only after its hermeticity edit and the read-through described in the DATA NOTE, with a data/ check immediately after.
- Sweep: every remaining tests/unit importer of utils.daily_notes_generator / DailyNotesGenerator / GenerationResult (grep and list), chunks of ≤9, vetted BEFORE running for real profile/vault/data paths; usual exclusions (test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file).

SCAN: pre- and post-edit read-only scan. Baseline after F12a: dm18 found 29 / base 79 / new 0 / stale 50. Expect no dm18 anchor change and new = 0 (confirm against the scan).
SIZE: target ≤300 changed lines; HARD stop at ≤450 (measure before editing later parts; never implement then revert; never trim to fit).
ORDER: manifest → create F12b.md → pre-edit scan → new test file → failing-first (digests in the same command) → source edit → test_daily_notes_auto_update.py hermeticity edit → focused → sweep → ruff → scan → data/logs listing → packet.
