=== H03c-3a: replace the real course code, subject prefix, professor surname, course and syllabus titles, and companion and contact first names in 10 calendar-family test files with synthetic ones ===
(Parent brief, 2026-09-15. Owner decisions 5, N4, R1 and R3-final (briefs/PARENT_STATE.md). Written from the read-only maps `$S/H03c_map.md`, `$S/H03c_residual_map.md` and `$S/H03c_EN_map.md` (labels only). Tests only; no production file. The parent re-verified the 10 digests below on the post-H03c-2 tree at 10:49:43. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write any real name, course code, subject prefix, or course or syllabus title into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy.
- The packet uses labels only:
  - V = course code (V-hyph = hyphenated form);
  - SP = V's bare subject prefix (SP-title TitleCase, SP-lower);
  - PS = professor surname;
  - ST = syllabus title (ST-1 = the week-1 sibling row);
  - CT = course title;
  - CN = companion first name;
  - EN = contact first name.
- Give `file:line` references. Never paste diffs, never quote "before" text, never describe a label with an example word.
- Self-check (the parent's method; counts only):
  - build the capitalised (`\b[A-Z][A-Za-z.-]{2,}\b`) and lowercase (`\b[a-z][a-z_.-]{3,}\b`) token sets found ONLY on removed lines of `diff $S/h03c3a_pre/<path> <path>`;
  - grep them via process substitution against batches/H03c-3a.md and every edited file;
  - the packet must have 0 hits apart from infrastructure paths (remote URL, sanity-check path), which you explain by line; explain every edited-file hit by `file:line` and label (for example, an ordinary English word that was also part of a title);
  - re-run after EVERY packet edit.

OWNER DECISIONS applied here (2026-09-15): decision 5 (V in tests), N4 (PS, CT, ST, SP in tests), R1 (CN everywhere), R3-final (EN everywhere).

Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE (estimate before editing);
- EVIDENCE TIMESTAMPS: `date; sha256sum <file>` right after each file's edit.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-3a.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c3a_pre/<same relative path>` and verify digests → baseline runs (digests in the same command as the first chunk) → ALL edits → privacy self-check → post-edit runs (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- Finish EVERY edit before the privacy self-check and the post-edit runs. (H03c-2 ran its post-edit tests before its last edits; do not repeat that.)
- No edit before the baseline is recorded. Never implement then revert.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- Run only tests/unit files. Run the guard and MemAvailable check before EVERY pytest invocation; ≤9 files per chunk; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-3a.txt` and `$S/manifest_paths_pre_H03c-3a.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

MAPPING (parent decision; synthetic)
- V → "ABC 1234" (V-hyph → "ABC-1234").
- SP → "ABC" (SP-title → "Abc", SP-lower → "abc"), following the original casing.
- PS → "Varnum".
- ST → "Fitted Curves (2): Past the Straight Line"; ST-1 → "Fitted Curves (1)". Keep every other part of each row's format.
- CT → "Predictive Modeling for Operations".
- CN → "Tamsin".
- EN → "Maren".
- Collision checks at launch (counts only; excludes as in R_common_rules):
  - "Varnum" and "Maren": 0 hits outside docs/execution expected.
  - "ABC 1234", "Fitted Curves", "Predictive Modeling for Operations" and "Tamsin" are already used by earlier H03c batches in OTHER files. Record their per-file counts.
  - Confirm that none of the 10 target files already contains any replacement string. If one does, record the file and why each swap stays unambiguous.

SCOPE (digest prefixes re-verified by the parent; re-verify at launch and record the full digests in the packet)
- tests/unit/test_sep10_probe_dump_actions.py `d1521c47…` (2625 lines):
  - V: 72, 686, 834, 838, 987, 1651, 1901, 1903, 1905, 1927, 1932, 1938, 2321, 2323, 2341, 2348 (fixtures); 995 (assertion).
  - PS: 684 (comment); 689, 728, 1654, 1901, 1905, 2341, 2348 (fixtures); 1895 (comment); 2351 (assertion input).
  - ST: 1903.
  - SP: 67, 201, 230, 241, 415, 430, 814, 1818, 2370, 2383, 2506, 2526, 2582, 2620 (fixtures); 1000 and 1077 (exact "SP study group" assertions); 1808 (comment).
  - EN: 1907 ("EN appointment" in `_LIVE_R1_CALENDAR`).
- tests/unit/test_calendar_turn_round3.py `d7d8bff2…` (622): V 229, 338, 440, 445, 471; SP 43, 61, 301 (fixtures); 68, 69, 308, 309 (exact topic assertions; SP-title).
- tests/unit/test_calendar_turn_misfires.py `dabb44e1…` (309): V 21 (docstring), 63, 194, 206; SP 2 (docstring), 54.
- tests/unit/test_sep07_calendar_offer_continuation.py `7ea6c736…` (533): V 59, 62, 308, 363, 366, 476; SP 4 (docstring), 44.
- tests/unit/test_sep10_calendar_forced_action_loop.py `79ab77bd…` (238): V 40, 43, 46, 182; SP 70.
- tests/unit/test_sep10_referee_followups.py `b0061891…` (192): V 80.
- tests/unit/test_grounding_wiring.py `7b9bf09a…` (356): V 292, 294 (assertion).
- tests/unit/test_syllabus_turn_misfires.py `6c2226cc…` (213): V-hyph 63 (comment); V 170.
- tests/unit/test_sep01_grounding_misfires.py `d6ed88af…` (383): V 284, 325; ST-1 285 and 303 (quoted in why_false); ST 286; CN 236.
- tests/unit/test_sep07_followups.py `d7b3d442…` (206): V 80, 91, 187; CT 91 (same line); SP 67.
  - V is split across lines 67–68, so a V regex will miss it; read those lines.
- Before editing, grep each file for every label's real form (case-insensitive, non-word-bounded) and reconcile against these lists. Record any extra or missing line by `file:line` and label. An occurrence the maps missed in these 10 files is in scope; one outside these files is reported only.

STRUCTURE THAT MUST SURVIVE (read before editing)
- **Calendar title-token matcher** (gui/handlers.py; `[a-z]+` tokens of 3+ letters minus stopwords and weekdays):
  - "Varnum" becomes a title token on the positive matches (689/728, 2348/2351), which only strengthens them;
  - the negative claims (1919, 1948, 2189, 2297, 2344, 2610) must still not match;
  - keep the "Dr. X's" possessive and "(Dr. X — Zoom)" shapes;
  - "Maren" must not share a token with any claim in the file (EN-lower appears nowhere else).
- **SP:** the 3→3-letter swap keeps line lengths and title-token counts. Change each query and its assertion together: sep10 67 with 1000/1077; round3 61/301 with 68/69/308/309 ("Abc").
- **Detectors that depend only on shape:** `extract_calendar_title` (keeps query casing), `is_personal_doc_search`, the calendar-typo action patterns, `is_task_directive`, and the content_type_detector wrapped-prose guard.
- **ST in sep01:**
  - `claim_date_in_source` counts content tokens of 3+ letters plus "hw";
  - the ST-1 row at 285 must share NO token with the counterfactual claim (the line-338 claim: hw, due, sunday, canvas);
  - no title may contain a date form;
  - keep 285/286/303 and sep10:1903 consistent;
  - in sep10, the title tokens must not include "tomorrow" or any weekday.
- **CT at sep07_followups:91:** keep the rest of the line ("Canvas quizzes", the roster, "1 admitted upload chunks").
- **Grounding tests** put V in both the claim and the source. Swap consistently within each file. Calendar summary equality holds under a consistent swap.

OWNERSHIP
- The 10 test files above: ONLY the label-carrying strings. No other line changes.
- New docs/execution/generalization/batches/H03c-3a.md.
- Read-only: every production file (including gui/handlers.py and core/grounding_check.py, whose comments belong to H03c-3b), every other test, and every class-guard path.

CONTRACT
1. Every listed occurrence is replaced per the mapping.
2. No behaviour change: the 10 files pass with identical per-file counts before and after.
3. Privacy verification:
   - `grep -c -i` per label over the 10 files is 0 for every real form;
   - the token self-check shows 0 packet hits (infrastructure paths explained).
4. No real name in any written file or report.

TESTS
- BASELINE FIRST: the 10 digests, printed in the same command as the first chunk. Then the 10 files in 2 chunks (≤9 each), with per-file counts (`-rA` summaries).
- Post-edit: the same chunks with identical per-file counts.
- Importer check: grep tests/ for imports of each of the 10 module names (`from tests.unit.<module> import`, `import tests.unit.<module>`, dotted patch strings). Run any importer not already run (≤9 per chunk).
- ruff on the 10 files.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: map estimate ≈87 changed lines; target ≤150; HARD stop 450.
- Measure before editing. If the estimate exceeds 150, stop and return a split proposal (sep10_probe_dump_actions alone vs the other 9).
ORDER: manifest → create H03c-3a.md → pre-edit scan → collision checks → pre-copies → baseline (digests in the same command) → ALL edits → privacy self-check → post-edit runs → importer check → ruff → scan → data/logs/web → packet.
