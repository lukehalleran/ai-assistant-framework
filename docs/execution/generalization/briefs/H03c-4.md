=== H03c-4: replace the companion's real first name (CN) in stance/insight tests and production comments, plus residual SP/V3/V4/IDP/A-short strings in files no other batch owns ===
(Parent brief, 2026-09-15. Owner decisions R1, R2, N4, 5 and N3 (briefs/PARENT_STATE.md). Written from the read-only map `$S/H03c_residual_map.md` (labels only), plus parent privacy checks after H03c-2 (fact_extractor.py:372) and H03c-3b (fact_source.py:188, status_claims.py:55, test_narrative_status_claims.py 47/50, BUG_RETROSPECTIVE:257). The parent re-verified every digest below on the post-H03c-3b tree at 12:07:12. The parent also checked that every listed production line is a `#` comment or inside a triple-quoted docstring, and that config/feature_registry.yaml:330 is a descriptive notes string. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write any real name, subject prefix, course code or number, school short form or ID label into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy. Never quote a grep pattern containing a real form in the packet; describe checks by label.
- Labels:
  - CN = companion first name (CN-lower in ids);
  - SP = subject prefix (SP-lower lowercase);
  - V3 = course token prefix (mixed-case bare form);
  - V4 = a third real course code (subject prefix plus number) that shares V2's prefix;
  - IDP = student-ID label;
  - A-short = School A's short form.
- Give `file:line` references. Never paste diffs, never quote "before" text, never describe a label with an example word.
- Self-check (the parent's method; counts only), with BOTH token families:
  - long: the capitalised (`\b[A-Z][A-Za-z.-]{2,}\b`) and lowercase (`\b[a-z][a-z_.-]{3,}\b`) token sets found ONLY on removed lines of `diff $S/h03c4_pre/<path> <path>`;
  - short/numeric: the `\b[0-9]{3,}\b` and `\b[A-Za-z]{2,3}\b` token sets found ONLY on removed lines (the long sets miss course numbers, 3-letter prefixes and 2-letter short forms);
  - grep both families via process substitution against batches/H03c-4.md and every edited file;
  - the packet must have 0 hits apart from infrastructure paths, which you explain by line; explain every edited-file hit by `file:line` and label (for example, an ordinary code token);
  - re-run after EVERY packet edit.

Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE (estimate before editing);
- EVIDENCE TIMESTAMPS: `date; sha256sum <file>` right after each file's edit.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-4.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c4_pre/<same relative path>` and verify digests → baseline runs (digests in the same command as the first chunk) → ALL edits → privacy self-check → post-edit runs (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- **BASELINE BEFORE ANY EDIT:**
  - Your FIRST Edit call on ANY scope file (test, production or doc) must come after the output of EVERY baseline chunk is recorded in the packet.
  - H03c-3b edited first and then reverted; that pattern is forbidden. If you ever find you edited a scope file before the baseline, STOP immediately and report. Do NOT revert on your own.
- Take a data/ listing BEFORE the first pytest chunk as well as after every chunk.
- Finish EVERY edit (tests, production comments AND docs) before the privacy self-check and the post-edit runs.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- Run only tests/unit files. Run the guard and MemAvailable check before EVERY pytest invocation; ≤9 files per chunk; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-4.txt` and `$S/manifest_paths_pre_H03c-4.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.
- A separate `main.py` process (outside this clone) may be running. Leave it alone. If MemAvailable is below 4000 MB before a chunk, wait and re-check; never lower the cap or skip the check.

MAPPING (parent decision; synthetic)
- CN → "Tamsin" / "tamsin".
- SP → "ABC" / "Abc" / "abc", following the original casing.
- V3 bare mixed-case → "Ktr".
- V4 → "QRS 7420" (same casing and spacing as the original).
- IDP routed → "Student ID 900123456".
- A-short → "XW" (keep the hyphenated suffix that follows it).
- The CN doc lines → "Tamsin".
- Collision checks at launch (counts only; excludes as in R_common_rules):
  - "QRS 7420": 0 hits outside docs/execution expected.
  - "Tamsin", "ABC", "Ktr", "XW" and "Student ID 900123456" are already used by earlier batches in OTHER files. Record per-file counts.
  - Confirm that no target file already contains "Tamsin" or "tamsin" (the entity-id and display-name pairing must stay unambiguous); record any that does.
- Do NOT touch any other synthetic course code already in the tree ("ABC 1234", "QRS 7310"). Their cross-file overlap is a recorded limitation.

SCOPE (digest prefixes re-verified by the parent; re-verify at launch and record the full digests in the packet)
CN tests:
- tests/unit/test_appraisal_settledness.py `4b11bd3f…`: 21, 27, 34, 36, 38, 84, 87, 93.
- tests/unit/test_insight_mode_handler.py `f55aa95c…`: 47, 140, 183, 186 (assertion; must equal 183).
- tests/unit/test_insight_provenance.py `e6e13aee…`: 38, 49, 60, 65, 74, 89.
- tests/unit/test_stance_write_path.py `0cde1b6d…`: 13 (docstring), 30, 59, 61 (assertion), 87 (comment), 88, 101, 104, 111 (comment), 112 (assertion), 119 (comment), 120, 136, 137, 145, 151, 172, 173.
  - CN is also inside Python variable names at 120/136/137. Rename them consistently, and keep the alias binding at 88/112.
- tests/unit/test_anaphoric_continuation.py `e6bfd146…`: 259.
- tests/unit/test_insight_gate.py `ab666a61…`: 51, 182.
- tests/unit/test_backfill_stance.py `eb7c3380…`: 34, 47, 72, 74, 76 (exact edge key "name|is|evil"), 80, 110.
- tests/unit/test_insight_sweep.py `9119474e…`: 107, 109, 113, 121, 138 (comment), 139 (assertion).
- tests/unit/test_sep03_followups_continuity.py `91115218…`: 32 (docstring). Its source-reading test at :350 must still pass.
- tests/unit/test_sep03_followups_graph.py `f77b174a…`: 112, 113, 114 (assertion).
- tests/unit/test_stance_classifier.py `126fc01a…`: 3 (docstring), 25, 36, 43, 56, 60, 65 (comment), 66, 75, 79, 112, 138, 141 (assertion; fake resolver id), 152.

CN production COMMENTS/DOCSTRINGS (verify each is not a code string before editing):
- memory/stance_classifier.py `049fb06a…`: 5, 18, 21 (docstring).
- memory/graph_models.py `e8161af9…`: 184, 185 (comments).
- memory/graph_utils.py `dbabd2bb…`: 20 (docstring), 274 (comment).
- memory/cross_deduplicator.py `ac51bd5a…`: 29 (docstring), 459 (comment).
- memory/memory_retriever.py `246a2427…`: 16, 17, 311, 312 (docstrings).
- scripts/backfill_stance.py `8a5585f3…`: 6, 17 (module docstring). The script passes its module `__doc__` to argparse, so this changes `--help` text only; record it.
- config/feature_registry.yaml `48d04d00…`: 330 (a descriptive notes string; confirm no code reads it as a key or pattern).
- memory/fact_extractor.py `bd4a3bda…`: 372 (comment, CN-lower). The 3 O comments in this file STAY.
- Already handled in H03c-3b: llm_fact_extractor 178, memory_storage 1169/1344. OUT (code string, keep): llm_fact_extractor 448–449.

SP tests:
- tests/unit/test_sep08_profile_fact_misfires.py `67bf9ca2…`: 32 (SP-lower), 107 (change together: support-span lookup).
- tests/unit/test_sep08_request_scope.py `39762f69…`: 50.
- tests/unit/test_sep08_homework_tone_misfires.py `52ae3793…`: 274.
- tests/unit/test_sep10_probe_dump_interpretation.py `915ce004…`: 1120, 1124 (calendar-typo patterns and `is_task_directive` depend only on shape). Its source-reading test at :1452 must still pass.

V3 test: tests/unit/test_thread_context_and_resolution.py `5e3e16f5…`: 76, 83 (no "6", "7", "hw" or "homework" in the prefix).

IDP routed tests (assertions only check that the digits are gone): tests/unit/test_api_chat.py `54a0d131…` 119; tests/unit/test_api_debug_settings.py `0274508e…` 65, 108.

Parent additions (found by the parent's short/numeric check after H03c-3b):
- memory/fact_source.py `4e230de6…`: 188 (comment): SP-lower → "abc". Every other line stays, including the T cue word at 262.
- utils/status_claims.py `f7801d1e…`: 55 (comment; the file has no digit regex): V4 → "QRS 7420".
- tests/unit/test_narrative_status_claims.py `86cf9cd7…`: 47 (narrative text) and 50 (claim value): V4 → "QRS 7420". Change them together; read the test's assertions first.

Docs:
- docs/AUTONOMOUS_CURATION_DESIGN.md `3d04a36f…`: 106 (CN).
- docs/QUICK_REFERENCE.md `08de6112…`: 1383 (CN).
- docs/BUG_RETROSPECTIVE_20260715_20260904.md `f2e33984…`: 257 (A-short in a hyphenated example; parent addition).
- NOT edited: docs/BUG_CLASSES.md (class-guard-owned), hooks/** (class-guard-owned), scripts/reflection_*.py (code strings; recorded by the parent).

- Before editing, grep each file in scope for every label's real form (case-insensitive, non-word-bounded; for SP-lower, V4 and A-short also word-bounded to avoid code-token noise) and reconcile against these lists. Record any extra or missing line by `file:line` and label. An extra occurrence in a scope file that the owner decisions cover (tests, comments, docstrings, docs) is in scope; a code string or a file outside scope is reported only.

STRUCTURE
- CN:
  - entity_id lowercase paired with display_name TitleCase;
  - exact checks at backfill_stance:76, insight_sweep:139, insight_mode_handler 186 == 183;
  - the stance_classifier 138/141 resolver id;
  - CN must not be a pronoun or role word.
- SP: the 3→3-letter swap; change each query and its assertion together.
- V4: the narrative mention (47) and the claim value (50) must stay identical strings, so any narrative/claim match still holds.

OWNERSHIP: the listed lines only (plus reconciled extras in scope files, as above); new batches/H03c-4.md. Read-only: everything else and every class-guard path.

CONTRACT
1. Every listed occurrence is replaced. Production edits are comment/docstring-only; state this per file after reading your own diff.
2. No behaviour change: identical per-file test counts before and after.
3. Privacy: per-label real-form counts over the edited files are 0, for both token families; packet token hits 0 (infrastructure paths explained). The CN code strings at llm_fact_extractor 448–449 are out of scope.
4. No real name in any written file or report.

TESTS
- BASELINE FIRST (digests in the same command as the first chunk), in 3 chunks of ≤9:
  - the 19 edited test files (the 18 above plus test_narrative_status_claims.py).
- Importers:
  - at least one importer each of memory.stance_classifier, memory.graph_models, memory.graph_utils, memory.cross_deduplicator, memory.memory_retriever, memory.fact_extractor, memory.fact_source, utils.status_claims and scripts.backfill_stance, if not already among the 19;
  - grep all import forms plus dotted patch strings, and record which file covers each module.
- Post-edit: identical per-file counts. ruff on every edited .py.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: ≈103 changed lines; target ≤200; HARD stop 450. Measure before editing; if the estimate exceeds 200, stop and return a split proposal.
ORDER: manifest → create H03c-4.md → pre-edit scan → collision checks → pre-copies → baseline (digests in the same command) → ALL edits → privacy self-check → post-edit runs → importer check → ruff → scan → data/logs/web → packet.
