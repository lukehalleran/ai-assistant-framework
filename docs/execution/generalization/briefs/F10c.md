=== F10c: fix the `get_summaries(limit=)` signature defect so the summaries read (and narrative regeneration after consolidation) actually works ===
(Parent brief, 2026-09-14. Owner decision 2026-09-14: "yes to all fixes" — fix the defect F10b pinned. Written while F11a-2 ran; launch after F11a-2 is integrated. None of the files below is edited by F11a-2.)

Design source: docs/execution/generalization/failure_outcome_design.md, amendment "F10 split and the write error type" ("[verified] Pre-existing defect, not fixed (owner decision)"), now decided: FIX. docs/execution/generalization/LANDING_NOTES.md records the defect and the pin test that must change. briefs/PARENT_STATE.md "Owner decisions received 2026-09-14", item 1.
Request packets: none; do NOT read any class-guard file. No response file; evidence in batches/F10c.md.
BUG_CLASSES: BC-20 (a read that silently never worked).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE and SCANNER SHAPE).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F10c.md.
- No non-pytest Python of any kind; the exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (`NARRATIVE_CONTEXT_ENABLED`, the weekly/monthly cutoffs, the both-empty early return, the consolidator guard).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F11a-2.txt` and `$S/manifest_paths_post_F11a-2.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: the listing named in batches/F11a-2.md's parent section (the owner may delete the F2-era leftovers between batches; the parent records any new baseline there). Re-check after every chunk and STOP on any change. NEVER construct a `CorpusManager` on its default path, a real `MemoryConsolidator`, a real `UserProfile()` or a real model; a `CorpusManager` is allowed ONLY with an explicit tmp_path `corpus_file` AND after you have read `save_narrative_context` and every other method your test reaches to confirm each writes only under that tmp path — otherwise stub those methods.
ORDER REMINDER: print `sha256sum memory/memory_storage.py main.py tests/unit/test_skill_and_summary_outcomes.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. Expected: the two summaries-read tests in tests/unit/test_skill_and_summary_outcomes.py (F10b) — see TESTS. List every edit.

PARENT-VERIFIED FACTS (2026-09-14, post-F11a-1 tree; re-verify at the manifest check)
- Starting digests: memory/memory_storage.py `ccd78048…`, main.py `d748d224…`, tests/unit/test_skill_and_summary_outcomes.py `05daea00…`, memory/corpus_manager.py `df3f4cd4…` (read-only).
- memory/corpus_manager.py `CorpusManager.get_summaries(self, count: int = 5)` (def ≈474): filters summary entries (type contains "summary" or tags `@summary`/`type:summary`, excluding reflections), sorts by a normalized naive-UTC key `_norm_ts` (accepts datetime or ISO string, tz-aware converted to naive UTC, unparseable → `datetime.min`), returns `summaries[:count]`. It has NO `limit` parameter. Timestamps in memory are normally `datetime` (loaded ISO strings are converted at ≈87–89; `add_entry` ≈188 and `add_summary` ≈384–419 default to `datetime.now()`), but legacy string or tz-aware values are possible.
- memory/memory_storage.py `_get_recent_summaries_by_timespan(self, span_type, limit=4)` (def ≈1625; F10b state): `all_summaries = self.corpus_manager.get_summaries(limit=50)` (1639) — with the production `CorpusManager` this raises `TypeError` on every call, now reported as `RetrievalError(source="corpus_summaries", reason="TypeError")`. The body: `[]` if empty; cutoff by `span_type` (unknown → []); per row, a string timestamp is parsed (`except Exception: continue`), a tz-aware datetime is made naive, rows at or after the cutoff are kept; then `filtered.sort(key=lambda x: x.get("timestamp", datetime.min), reverse=True)` sorts on the RAW stored values — a mix of `str`/`datetime` or tz-aware/naive values raises `TypeError` (→ `RetrievalError`); `return filtered[:limit]`; the outer except raises `RetrievalError`.
- Its only caller `_maybe_regenerate_narrative` (F10b) now degrades explicitly on `RetrievalError`; once the read works, a non-empty result reaches `self.consolidator.generate_narrative_context(...)` and `self.corpus_manager.save_narrative_context(narrative)` — the owner-approved behaviour change on the non-default `SUMMARIZE_AT_SHUTDOWN_ONLY=0` path.
- main.py `inspect_summaries()` (async def 465): `summaries = memory_system.corpus_manager.get_summaries(limit=10)` (469) — the same defect; that CLI helper crashes with `TypeError`. It builds a real orchestrator (`build_orchestrator()`), so it is not unit-testable; fix by reading and record evidence.
- main.py:1347 already calls `corpus.get_summaries(count=50)` (correct; unchanged).
- F10b's tests (tests/unit/test_skill_and_summary_outcomes.py): `test_production_signature_raises_type_error_reason` (a fake with `get_summaries(self, count=5)` → asserts `RetrievalError(reason="TypeError")`, pinning the defect) and `test_healthy_read_returns_filtered_sorted_rows` (a fake with `get_summaries(self, limit=50)`).

OWNERSHIP
- memory/memory_storage.py: the body and docstring of `_get_recent_summaries_by_timespan` ONLY.
- main.py: the `get_summaries(limit=10)` call in `inspect_summaries` (469) ONLY.
- tests/unit/test_skill_and_summary_outcomes.py: the two summaries-read tests named above ONLY (FIXTURE RULE).
- New tests/unit/test_summaries_read_fix.py; new docs/execution/generalization/batches/F10c.md.
- Read-only: every other memory_storage.py method (including `_maybe_regenerate_narrative`, `store_skill`, `store_interaction`), memory/corpus_manager.py, every other part of main.py, memory/memory_consolidator.py, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `_get_recent_summaries_by_timespan` calls `self.corpus_manager.get_summaries(50)` — the count passed POSITIONALLY, which matches `CorpusManager.get_summaries(count)` and any first-positional count/limit.
2. The sort uses a normalized naive datetime key computed from each kept row's timestamp (the same parse/naive conversion the filter already does; reuse it rather than re-deriving), so mixed `str`/`datetime`/tz-aware/naive rows sort without raising; rows whose timestamp cannot be parsed are still skipped exactly as today; the returned row dicts are unchanged objects.
3. Unchanged: empty → []; unknown span → []; a real read failure → `RetrievalError(source="corpus_summaries", reason=<class>)`; the docstring states the read now works against the production `CorpusManager`.
4. main.py `inspect_summaries`: `get_summaries(10)`.
5. No change to `_maybe_regenerate_narrative` or any other method. Record the behaviour change (narrative regeneration now runs on the `SUMMARIZE_AT_SHUTDOWN_ONLY=0` path) in the packet.
6. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_summaries_read_fix.py; fakes or a tmp_path `CorpusManager` only — see the DATA NOTE)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- A fake with the production signature `get_summaries(self, count=5)` returning recent summaries → the weekly read returns them (fails today with `RetrievalError(TypeError)`).
- Mixed timestamps (a naive datetime, a tz-aware datetime, an ISO string, all within the window) → returned most-recent-first with no exception (fails today); an unparseable string row is skipped (control).
- If (and only if) the DATA NOTE conditions are met: a tmp_path `CorpusManager` with two `add_summary` entries → `_get_recent_summaries_by_timespan("weekly")` returns them; otherwise record why it was not used.
- `_maybe_regenerate_narrative` with the production-signature fake returning recent summaries, a stub consolidator (AsyncMock `generate_narrative_context` → "a narrative") and a stubbed `get_recent_memories` / `save_narrative_context` → generate awaited once and save called once (fails today: the read raised and generation was skipped).
- FIXTURE RULE edits in tests/unit/test_skill_and_summary_outcomes.py: `test_production_signature_raises_type_error_reason` becomes the positive production-signature proof (renamed accordingly), paired with the existing raising-read test (`test_corpus_manager_raising_raises_retrieval_error`, unchanged) as the failure control; `test_healthy_read_returns_filtered_sorted_rows` keeps its assertion but its fake must accept the positional count (e.g. `def get_summaries(self, count=50)`). No other test in that file changes.
- Focused (≤9 per chunk): the new file, test_skill_and_summary_outcomes.py, test_recent_summaries_fix.py, test_corpus_manager.py, test_narrative_staleness.py, test_narrative_status_claims.py, test_retrieval_outcome.py, test_store_write_outcomes.py.
- Sweep: every remaining tests/unit importer of memory.memory_storage / MemoryStorage or file referencing `get_summaries` (grep and list), chunks of ≤9, vetted BEFORE running; usual exclusions (test_graph_integration.py, test_daily_notes_auto_update.py, the ContextGatherer/UserProfile hazard, top-level tests/).

SCAN: pre- and post-edit read-only scan. Expect no dm18 change (the #130 row is already STALE) and new = 0.
SIZE: target ≤250 changed lines; HARD stop at ≤450 (stop BEFORE exceeding it; never trim to fit).
ORDER: manifest → create F10c.md → pre-edit scan → tests → failing-first (digests in the same command) → edits → focused → sweep → ruff → scan → data/logs listing → packet.
