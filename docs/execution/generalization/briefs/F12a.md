=== F12a: the narrative is never regenerated from a failed status-facts or Obsidian read; the consolidator readers raise instead of returning an empty list ===
(Parent brief, 2026-09-14. Drafted from a read-only map while F10a ran; facts re-verified during F11b-1b (none of these files is edited by F10–F11b); launched after F11b-2 was integrated. The design-doc "F12 split" amendment was written at the same time.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-009: "Raise on failure (not-found stays None/[]): … the consolidator readers and `_current_status_facts` …"; "consolidation marks `status_guard=unavailable` and does not persist a guard-less narrative as authoritative".
- Real-defects rows: "The narrative status guard silently turns off on a read failure; the narrative is generated and persisted without it" (`memory/memory_consolidator.py:204-216,704-705,757`); "Consolidator input readers that fail render as '(No … available)'; the narrative is built from partial inputs" (`:455-618,654-699`).
- Amendment "F12 split (parent decision, 2026-09-14)": F12a = consolidator (#117–#120) + the `refresh-narrative` CLI; F12b = the daily_notes_generator status-guard sibling (no anchor) + making test_daily_notes_auto_update.py hermetic; F12c = retriever (#123, #125, #126); F12d = synthesis (#141–#143) + the owner-approved fail-closed audit check.
Request packet (the ONLY class-guard file you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#117, #118, #119, #120).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response file (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-6.md.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE — measure before editing later parts; never implement then revert; never trim to fit — and SCANNER SHAPE — catch the TYPED failure for a contract-required degrade; escalate a new gate finding).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F12a.md.
- No non-pytest Python of any kind — not even `python -c` for list slicing; build chunk lists with grep/sed or by hand. The exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (the Obsidian path lookup, the "all sources empty" early return, the model-manager guard, the streak ledger).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F11b-2.txt` and `$S/manifest_paths_post_F11b-2.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_post_F11b-1b.txt` (data/ holds `benchmark_per_case.csv`, `chroma_db_v4`, `embedding_migration_manifest.json`, `pipeline`). Editing main.py (at the repository root) changes only the `..` line of that listing — record it; any other difference means STOP. NEVER construct `MemoryConsolidator` without an explicit fake profile AND with the three readers or `_get_obsidian_notes_path` faked: the lazy `user_profile` property builds a real `UserProfile()` at its default path (the data/user_profile.json hazard), and the readers read the real `OBSIDIAN_VAULT_PATH`. tests/unit/test_daily_notes_auto_update.py stays excluded (F12b fixes it). NEVER run main.py.
ORDER REMINDER: print `sha256sum memory/memory_consolidator.py main.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit. (Map: no unit test asserts []/None on a failure path; tests/unit/test_completed_plan_claims.py (≈214–217) stubs `_current_status_facts = lambda: []` and the readers `lambda limit: []` and must pass unedited.)

PARENT-VERIFIED FACTS (re-verified 2026-09-14; confirm at the manifest check)
- Starting digests: memory/memory_consolidator.py `c00177c7…` (802 lines), main.py `18dd3b91…` (F11b-1b state); read-only utils/status_claims.py `f7801d1e…`, memory/memory_storage.py (F10c state), utils/daily_notes_generator.py `9964c546…`.
- memory/memory_consolidator.py:
  - `user_profile` property (def 190): lazily builds `UserProfile()` at its default path; constructor failure → debug log → `return None`. Every production construction omits `user_profile=` (main.py:1331, core/prompt/builder.py ≈684, utils/daily_notes_generator.py ≈952), so a None here means the profile could not be built.
  - `_current_status_facts` (def 204): `if profile is None: return []` (210–211); `try: current = profile.get_current_view()` (213) / `except Exception as e: logger.debug(...); return []` (214–216) → ANCHOR #117; the filter on `STATUS_RELATIONS` — a `[]` there is a genuine empty. `UserProfile.get_current_view` (memory/user_profile.py ≈566–579) has no try.
  - `_get_obsidian_notes_path` (def 408): returns the notes path if it exists, else None (the deliberate skip); `except Exception` → None — a sibling swallow (record only).
  - Readers (filesystem I/O only; each has an inner frontmatter-parse `except Exception: pass`, keep):
    - `_read_obsidian_weekly_summaries` (def 426): no path → []; `return weekly_summaries`; outer `except Exception as e:` (488) → `logger.debug(...)`; `return []` → ANCHOR #118.
    - `_read_obsidian_monthly_summaries` (def 492): skip; `return monthly_summaries[:limit]`; outer except (550) → ANCHOR #119.
    - `_read_obsidian_daily_notes` (def 554): skip; return; outer except (616) → ANCHOR #120.
  - `generate_narrative_context` (def 620, async): one outer `try:` / `except Exception as e: logger.warning(f"[NarrativeSynthesis] Failed to generate narrative: {e}")` (801) → `return ""`. Readers called near the top (≈654–656); "(No monthly summaries available)" renders at 665 (and similar for weekly/daily/corpus); all-empty → ""; `status_facts = self._current_status_facts()` (704) → `authoritative_facts_block`; the streak ledger `except Exception` → `streak_claims = []` (≈714–717, a sibling, record only); no `generate_once` → ""; LLM `self.model_manager.generate_once(...)` (737); empty output → ""; `remove_conflicting_claims(narrative, status_facts)` (757). With an empty fact list both guard steps are silent no-ops.
  - The consolidator persists nothing on this path. Callers save only a truthy result: main.py `refresh_narrative_context` (≈1391–1392), utils/daily_notes_generator.py `_trigger_narrative_refresh` (≈962–965, inside its own try/except), memory/memory_storage.py `_maybe_regenerate_narrative` (F10b/F10c; inside its own try).
- main.py `refresh_narrative_context` (async def 1322, nested): constructs `MemoryConsolidator(model_manager)` (1331); calls the three readers directly with no try and prints their counts (1337–1339); `narrative = await consolidator.generate_narrative_context(...)` (1386); `if narrative: corpus.save_narrative_context(narrative)` (≈1391–1392) else "Failed" → exit 1; run via `success = asyncio.run(refresh_narrative_context())` (1403) with no try — a reader raise would be an uncaught CLI traceback. Read ≈1318–1410 to see exactly how `success` maps to the exit status before editing.
- Existing tests: test_narrative_status_claims.py (≈131–175, FakeProfile + monkeypatched readers); test_sep05_evening_turn_audit.py (≈387–416, MagicMock profile + stubbed readers); test_completed_plan_claims.py (≈214–217); test_temporal_claim_audit.py; test_user_identity.py. All fakes.

OWNERSHIP
- memory/memory_consolidator.py: the bodies and docstrings of `_current_status_facts` and the three readers, and inside `generate_narrative_context` ONLY one new `except RetrievalError` clause before the outer `except Exception` (plus one import line).
- main.py: inside `refresh_narrative_context`, the three direct reader calls (≈1337–1339) and the `generate_narrative_context` call site ONLY (plus one local import if needed).
- New tests/unit/test_consolidator_read_outcomes.py; new docs/execution/generalization/batches/F12a.md; the response file.
- Read-only: `user_profile`, `_get_obsidian_notes_path`, the streak ledger, every other consolidator method, utils/daily_notes_generator.py (F12b), memory/memory_storage.py, utils/status_claims.py, memory/user_profile.py, every other part of main.py, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `_current_status_facts` (#117): profile None → `raise RetrievalError(source="status_facts", reason="profile_unavailable")`; `get_current_view` raising → keep the log line, then `raise RetrievalError(source="status_facts", reason=type(e).__name__) from e`; a genuine empty filter result → [].
2. Readers (#118–#120): no notes path → [] (unchanged skip); the outer except keeps its log line, then `raise RetrievalError(source="obsidian_weekly" | "obsidian_monthly" | "obsidian_daily", reason=type(e).__name__) from e`; the inner frontmatter skips are unchanged; a genuine empty → [].
3. `generate_narrative_context`: a `RetrievalError` from any reader or from the status facts → one warning "[NarrativeSynthesis] Narrative not regenerated: inputs unavailable (<source>: <reason>)" (labels only) and `return ""` BEFORE any LLM call or partial render — catch the TYPED `RetrievalError` (never a new broad except). No caller persists an empty result, so no guard-less or partial narrative is saved and the previous narrative stays in place. Every other path and the outer `except Exception` are unchanged.
4. main.py `refresh_narrative_context`: a `RetrievalError` from the direct reader calls → print one line naming the source/reason labels and end with the same non-zero exit status the existing "Failed" path uses (no traceback); the existing "Failed" path for an empty narrative is unchanged.
5. If any gate scanner reports a NEW finding on your edit, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only in every new error, reason, log line and CLI message.

TESTS (tests/unit/test_consolidator_read_outcomes.py; fakes only — an explicit fake profile on every consolidator, a faked notes path via tmp_path or a monkeypatched `_get_obsidian_notes_path`, a stub `model_manager.generate_once` AsyncMock; never a real profile, vault, model or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- `_current_status_facts`: a fake profile whose `get_current_view` raises → `RetrievalError(source="status_facts", reason=<class>)`; profile None (monkeypatch the property) → `reason="profile_unavailable"`; healthy with no status relations → [] (control); healthy with a status fact → the fact (control).
- Readers: a tmp_path notes directory where the read raises (e.g. monkeypatch `Path.read_text` or the iteration inside the reader's try) → `RetrievalError` with the per-reader source; no path → [] (control); a healthy tmp note → parsed (control).
- `generate_narrative_context`: a raising reader → returns "" and `generate_once` is NEVER awaited; raising status facts → "" and `generate_once` never awaited; healthy inputs with a status fact → the guard block is in the prompt and a contradicting sentence is removed (control, using the existing claims helpers); all sources genuinely empty → "" as today (control).
- main.py `refresh_narrative_context`: it is nested inside main.py's CLI dispatch and builds real services — verify by reading only (NEVER run main.py); record the exact final lines and the limitation in the packet.
- Privacy: markers in injected exception messages never appear in any NEW error str/reason or new warning (caplog); pre-existing log lines kept unchanged may still carry `{e}` — do not assert on those. Build injected `RetrievalError`s the way the real code does (reason = a label or exception class name).
- Focused (≤9 per chunk): the new file, test_narrative_status_claims.py, test_sep05_evening_turn_audit.py, test_completed_plan_claims.py, test_temporal_claim_audit.py, test_user_identity.py, test_retrieval_outcome.py, test_summaries_read_fix.py (F10c), test_skill_and_summary_outcomes.py (F10b).
- Sweep: every remaining tests/unit importer of memory.memory_consolidator / MemoryConsolidator / utils.status_claims (grep and list), chunks of ≤9, vetted BEFORE running (any file that constructs `MemoryConsolidator` without a fake profile or reaches the real vault path is excluded and recorded); never run tests/unit/test_daily_notes_auto_update.py, test_graph_integration.py, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py or any top-level tests/ file.

SCAN: pre- and post-edit read-only scan. Baseline after F11b-2: dm18 found 33 / base 79 / new 0 / stale 46. Expect the dm18 rows for #117–#120 to go STALE, new = 0.
RESPONSE FILE (`CGR-20260913-009-6.md`): #117–#120 with deployed-function evidence; the narrative "not regenerated" path and that no caller persists it; the CLI change (read-only verified); siblings (`user_profile` swallow, `_get_obsidian_notes_path` swallow, streak ledger swallow, the daily_notes_generator duplicate guard → F12b); remaining CGR-009 anchors → F12c (#123, #125, #126; `-7`), F12d (#141–#143; `-8`).
SIZE: target ≤380 changed lines; HARD stop at ≤450 (measure before editing later parts; never implement then revert; never trim to fit; split proposal: status facts + generate vs readers + CLI).
ORDER: manifest → create F12a.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → response file.
