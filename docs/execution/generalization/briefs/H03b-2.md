=== H03b-2: replace the remaining real school, person, program and course names in 12 unit test files plus tests/test_citation_system.py with synthetic ones (owner decision 5) ===
(Parent brief, 2026-09-15. Written from `$S/H03b_map.md` (labels only). The 13 digests were re-checked while H03b-1 ran; all are unmodified tracked files. This batch REUSES H03b-1's mapping for A/A-full/A-snake/A-frag/R, and H03 put the same names into source comments.)

PRIVACY (read three times):
- NEVER write any real institution, organization or person name, abbreviation, fragment, domain, email local-part, snake_case form or School A's real state word into ANY file you create or edit: tests, batches/H03b-2.md, or your final report.
- Real strings may appear ONLY inside Edit `old_string` inputs, or in grep commands whose OUTPUT you do not copy.
- The packet refers to them ONLY by the map's labels (A, A-full, A-snake, A-frag, A-domain, K, L, M, N, O, Q, R, S, T, U, V, W), with `file:line`. Never paste a diff. Never quote "before" text. Do not describe a label by an illustrative example word.
- Self-check before you finish (the parent's method; counts only):
  - build the sets of capitalised tokens (regex `\b[A-Z][A-Za-z.-]{2,}\b`) AND lowercase tokens (`\b[a-z][a-z_]{3,}\b`) that appear ONLY on removed lines of `diff $S/h03b2_pre/<file> <file>` (not on added lines);
  - grep both sets (process substitution, never printed) against batches/H03b-2.md and the 13 edited files.
  - The packet must have 0 hits. Explain every edited-file hit by `file:line` and label as generic or pre-existing unrelated text.
  - H03b-1's packet reintroduced generic-word hits three times while explaining hits. Re-run the check after EVERY packet edit.

Source: owner decision 5 (briefs/PARENT_STATE.md "Owner decisions received 2026-09-14"): replace School A/B and the other real-sounding fixture names in the tests with synthetic names that keep each test's structure; never write the real names anywhere.
- Scope note: an owner question is pending about names OUTSIDE these 13 files (L in more tests and production comments, T in a code regex, V and R elsewhere, and one mention in docs/GENERALIZATION_AUDIT_20260901.md). Do NOT touch anything outside the 13 files.
Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE;
- EVIDENCE TIMESTAMPS.
- TIMESTAMP RULE, repeated because H03 and H03b-1 both typed a time into an edits header ("12:00:00 … window", "12:20:00 … edits performed"): every section header's time is the `date` output of a command run in that step.
  - For the edits section, run `date; sha256sum <file>` right after each file's edit, and use the first such `date` as the header.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03b-2.md → pre-edit scan → collision checks → copy the 13 files to `$S/h03b2_pre/` and verify digests → T regex-coupling check (read-only) → baseline run of the 12 unit files (digests in the same command; chunks of ≤9) → edits → privacy self-check → post-edit run (identical per-file counts) → ruff → scan → data/logs/web listing → packet.
- No edit before the baseline is recorded. Never implement then revert. Estimate size before editing.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- NEVER run tests/test_citation_system.py; the lane never runs top-level tests. Verify its edit by reading.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_H03b-1.txt` and `$S/manifest_paths_post_H03b-1.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

SCOPE (digests re-checked 2026-09-15; all unmodified tracked files)
| sha256 | lines | file | labels (count) | structure notes (map §4) |
|---|---|---|---|---|
| 33a3bdf4 | 101 | tests/unit/test_profile_confirmation_recurrent.py | A 2, U 1 | quick-profile key round-trip; the U assertion needs only the word "Online" |
| 5f6c8ebe | 191 | tests/unit/test_sep02_checkin_fixes.py | A 1 | 107: must not match `_REPO_STATE_OBJECT_RE` (memory/fact_extractor.py:330) |
| a8a3b1a5 | 173 | tests/unit/test_query_reformulation.py | A 2 | 172–173: keep A as 2 tokens, each >2 chars (`_reformulate_for_embedding` ≈514) |
| c63240bb | 113 | tests/unit/test_music_session_misfires.py | A 1, A-frag 1 (comment), R 4, Q 5, S 1, V 1 | 87: A's tokens outside `_TEMPORAL_GENERIC_TOKENS` (gate.py ≈500); the comment lists A's lowercase tokens; R/Q must stay TitleCase for `_entity_mention_is_proper`; line 112 reads web_search_trigger source (untouched) |
| 50ed7ee5 | 961 | tests/unit/test_knowledge_graph.py | A 1, A-snake 1 | 669: the expected `_normalize_id` output |
| 03d570e0 | 510 | tests/unit/test_curation_engine.py | A 1, T 1 | 471: fact value "<T> at <A> since …" |
| 176434be | 370 | tests/unit/test_agentic_timeout_fallback.py | A 1 | 359: a plain query returned unchanged |
| 24f73c3c | 300 | tests/unit/test_ingest_turn_misfires.py | A-full 1 (72), A-domain 6 lines (59, 64, 65, 73, 117, 138), K 3, L 11, M 1, N 1, O 1, W 2, T 2, V 2 | `_EMAIL_COMMAND_RE` (gate.py ≈537) is anchored on a send verb, so addresses are irrelevant |
| 39ca5d96 | 254 | tests/unit/test_email_passive_context.py | A-domain 2 (198, 209), L 4, O 3 | L must match between sender (198) and contacts (209); 213 asserts `startswith(L)`; L's first name and O overlap a marketing email subject, so replace consistently throughout the file |
| d56d41a9 | 395 | tests/unit/test_pattern_engine.py | A-domain 1 (341), R 1 | only a clinic domain count and counts are asserted |
| ea86306c | 184 | tests/unit/test_retrospective_small_guards.py | A-domain 1 (53, a registrar URL) | the URL check is `'https://' in` (gate.py ≈738) |
| 14cb05c7 | 553 | tests/unit/test_sep05_evening_turn_audit.py | A lowercase 1 (133, a parametrize entry) | any non-temporal string |
| df1e574f | 281 | tests/test_citation_system.py | A 1 (25), T 3 (20, 25, 57) | TOP-LEVEL: never run; 57 is an exact-substring assertion, so T must change consistently at 20/25/57 |

MAPPING (parent decision; synthetic)
- A → "Vermont Wrenfield"; lowercase "vermont wrenfield"; A-snake → "vermont_wrenfield"; A-full → "Wrenfield Institute of Science"; A-frag → "Vermont"; R → "Marrowby".
- A-domain → "wrenfield.example"; K (continuing-ed subdomain word) → "extension", giving "extension.wrenfield.example"; the registrar URL → "https://registrar.wrenfield.example".
- S → "Lakemont" (keep "IL").
- U → "Online Master of Example Studies".
- V → "ABC 1234".
- W → "RegPortal".
- T → "MXS", ONLY where no test depends on T matching `memory/fact_source.py` ≈255–268 (a list of string literals; the map flags line 262).
  - Before editing, read that region and every T test: test_curation_engine.py 471, test_ingest_turn_misfires.py (2) and tests/test_citation_system.py 20/25/57.
  - If any test depends on the production list containing T, leave T unchanged in THAT file and record it (label plus `file:line`) for the pending owner question.
- Persons (invented; replace consistently within each file, including first-name-only and email local-part forms):
  - L → "Morgan Ashdown" (first name "Morgan"; an email local part like "morgan.ashdown");
  - M → "<M-first> Fenwright" (first name withheld by the parent 2026-09-15: it matched a real person's first name); N → "Jordan Pellow";
  - O → "Alex";
  - Q → "Tamsin".
- Collision checks at launch: `grep -rniw` over *.py/*.md/*.json (excluding data/, .agent_snapshots/, node_modules/) for Lakemont, MXS, RegPortal, "Morgan Ashdown", Ashdown, Fenwright, Pellow, Tamsin, "Online Master of Example Studies" and wrenfield.example must show 0 hits outside briefs.
  - "Alex" and "Morgan" alone may already exist as generic fixtures; check that the files you edit do not already use them for a DIFFERENT purpose. If they do, choose another invented first name.
- Re-verify that A's lowercase tokens ("vermont", "wrenfield") are absent from `_TEMPORAL_GENERIC_TOKENS` (gate.py ≈500), and that "Vermont Wrenfield" does not match `_REPO_STATE_OBJECT_RE` (memory/fact_extractor.py ≈330) in a way that changes test_sep02_checkin_fixes.py:107 (the baseline/post counts prove it).

OWNERSHIP
- The 13 files above: ONLY the strings carrying label forms (plus variable or fixture names that embed them). No other line changes.
- New docs/execution/generalization/batches/H03b-2.md.
- Read-only: every source file (including memory/fact_source.py and gate.py), every other test file, docs/GENERALIZATION_AUDIT_20260901.md, and every class-guard path.

CONTRACT
1. Every label occurrence in the 13 files is replaced per the mapping, except a T recorded as regex-coupled. Structure is preserved.
2. The 12 unit files pass with identical per-file counts before and after. tests/test_citation_system.py is verified by reading (T consistent at 20/25/57; no other change).
3. Privacy verification: for each real form, `grep -c -i` over the 13 files is 0 (except a recorded T), and the token self-check shows 0 hits in the packet. Record counts per LABEL only.
4. No real name in any written file, the packet or the report.

TESTS (no new test file)
- BASELINE FIRST: the 13 digests printed in the same command as the first baseline chunk; the 12 unit files in 2 chunks (≤9). Record per-file counts (`-rA` or per-file summaries).
- Post-edit: the same chunks with identical per-file counts.
- Importers of the edited test modules: grep tests/ for imports of each edited test module name and run any importer found (≤9 per chunk).
- ruff on the 13 files.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: map estimate ≈23 core / ≈50 with persons, programs and courses; target ≤150; HARD stop 450 (estimate before editing).
ORDER: manifest → create H03b-2.md → pre-edit scan → collision checks → pre-copies → T coupling check → baseline runs (digests in the same command) → edits → privacy self-check → post-edit runs (identical counts) → importer runs → ruff → scan → data/logs/web → packet.
