=== H03c-5: replace the real-looking example contact first name (EN) in the contacts/email tests and the agentic tool-schema example with "Maren" ===
(Parent brief, 2026-09-15. Owner decision R3-final (briefs/PARENT_STATE.md). Written from the read-only map `$S/H03c_EN_map.md` (labels only). The parent re-verified the 6 digests below on the post-H03c-4 tree at 12:34:54. H03c-3a/3b already handled the gate.py comment, the ARCHITECTURE_GUIDE line, test_ingest_turn_misfires.py:118 and test_sep10_probe_dump_actions.py:1907. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write the real EN form into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy. Never quote a grep pattern containing a real form in the packet; describe checks by label.
- Do not write ANY other real form either (for example a KEEP word you happen to see). The self-check only catches tokens this batch removes, so it cannot see those.
- Labels: EN = contact first name; EN-lower = email local parts; EN-poss = possessive.
- Give `file:line` references. Never paste diffs or quote "before" text.
- Self-check (the parent's method; counts only):
  - removed-only capitalised and lowercase token sets from `diff $S/h03c5_pre/<path> <path>`, PLUS removed-only `\b[0-9]{3,}\b` and `\b[A-Za-z]{2,3}\b` sets;
  - grep them case-insensitively against batches/H03c-5.md and every edited file;
  - the packet must have 0 hits apart from explained infrastructure paths;
  - re-run after EVERY packet edit.

Request packets: none. Do NOT read any class-guard file, not even with grep: docs/BUG_CLASSES.md, docs/DEVELOPMENT_WORKFLOW.md, docs/TEST_LANES.md, scripts/check_bug_classes.py (run it only via the permitted scan command), scripts/bug_class_guards/**, config/bug_class_*.json, tests/bug_class_guards/**, hooks/**, .github/**, docs/execution/class_guards/**. H03c-4's worker grep-read docs/BUG_CLASSES.md; do not repeat that. BUG_CLASSES: none, so the BC-58 sibling-search item is "N/A (privacy batch)" and needs no file.
Every repo-wide grep uses: `--exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=node_modules --exclude-dir=logs --exclude-dir=.git --exclude-dir=hooks --exclude-dir=.github --exclude-dir=bug_class_guards --exclude-dir=class_guards --exclude=BUG_CLASSES.md --exclude=DEVELOPMENT_WORKFLOW.md --exclude=TEST_LANES.md --exclude=check_bug_classes.py --exclude='bug_class_*.json'`.
No response file.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (permitted `python` forms, IMPORTER GREPS, SIZE, EVIDENCE TIMESTAMPS).

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-5.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c5_pre/<same relative path>` and verify digests → baseline runs (digests in the same command as the first chunk) → ALL edits → privacy self-check → post-edit runs (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- **BASELINE BEFORE ANY EDIT:** your FIRST Edit call on ANY scope file must come after the output of EVERY baseline chunk is recorded in the packet. If you ever find you edited a scope file before the baseline, STOP immediately and report. Do NOT revert on your own.
- Take a data/ listing BEFORE the first pytest chunk as well as after every chunk.
- A separate `main.py` process (outside this clone) may be running; leave it alone. If MemAvailable is below 4000 MB before a chunk, wait and re-check.
- Finish EVERY edit before the privacy self-check and the post-edit runs.
- `python` only as `python -m pytest …` (capped systemd-run), `python -m ruff check …` / `--version`, `python scripts/check_bug_classes.py scan --root .`, and the one `utils` sanity check. No npm.
- NEVER run tests/test_agentic_search.py or any other top-level tests/ file; verify it by reading. Run only tests/unit files, with the guard and MemAvailable check before EVERY pytest invocation, ≤9 files per chunk.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-5.txt` and `$S/manifest_paths_pre_H03c-5.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline `$S/data_baseline_post_F12a.txt`; `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

MAPPING: EN → "Maren"; EN-lower → "maren" (emails `maren@…`, dotted `maren.<surname>@…`); EN-poss → "Maren's".
- The replacement must differ, in any case, from the other first name queried at test_google_contacts.py:429.
- Collision re-check at launch: "Maren" appears only in the H03c-3a/3b edits and docs/execution; record per-file counts. None of the 5 target files may already contain it.

SCOPE (digest prefixes re-verified by the parent)
- core/agentic/types.py `98e1229c…`:1167: the `recipient` example in the tool-schema description. This is prompt text sent to the model; no test asserts on it. The owner approved this prompt-text change (R3-final). Record it as the one behaviour-visible string.
- tests/test_agentic_search.py `206c7b13…`: 421 (fixture) and 426 (exact `== EN`); change together. Top-level; read-verify only.
- tests/unit/test_agentic_gate.py `57d7c44e…`:84: keep the possessive plus " email" wording (the "'s email" keyword, gate.py:211).
- tests/unit/test_gmail_send.py `86e1f7bc…`: 310, 332, 362 (recipient); 315 (EN-lower@ email); 337 (error string with EN 3×, keep consistent); 367.
- tests/unit/test_google_contacts.py `e770e185…`:
  - fixtures 52, 53, 63, 64, 69, 70, 80, 263, 264, 271, 272, 312, 313;
  - queries 96, 103, 113, 122, 132, 142, 149, 164, 179, 193, 217, 232, 248, 299, 331, 359, 398, 399, 413, 415, 428;
  - exact assertions 167, 168, 182, 183, 303, 335, 441.
  - Pairings: 52/53 and 63/64 ↔ 167/168/182/441; 70 (dotted) ↔ 183; 264 ↔ 303; 313 ↔ 335.
  - The cache key is (endpoint, query.lower()). Keep the saved and other emails at 264/272 distinct, and 312/313 identical.
  - Optional: the comment identifier at 431 (substring use). Swap it only if it is plain comment text.
- tests/unit/test_sep08_ops_fixes.py `e7a4572a…`: 126, 145, 166, 178, 185, 206 (query arguments).
- Before editing, grep each file for the EN real form (case-insensitive, non-word-bounded) and reconcile against these lists; record extras or misses by `file:line`.

OWNERSHIP: the listed lines only; new batches/H03c-5.md. Read-only: everything else and every class-guard path.

CONTRACT
1. All listed occurrences are replaced.
2. Identical per-file counts for the 4 tests/unit files. test_agentic_search is consistent by reading (421 and 426 match).
3. Per-label real-form counts over the edited files are 0; packet token hits 0.
4. No real name in any written file or report.

TESTS
- BASELINE FIRST (digests in the same command): test_google_contacts.py, test_gmail_send.py, test_sep08_ops_fixes.py, test_agentic_gate.py, plus at least one tests/unit importer of core.agentic.types (grep all import forms and patch strings; record which).
- Post-edit identical counts. ruff on the edited .py files (types.py and test_agentic_search.py included).

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: ≈57 changed lines; target ≤150; HARD stop 450.
ORDER: manifest → create H03c-5.md → pre-edit scan → collision checks → pre-copies → baseline (digests in the same command) → ALL edits → privacy self-check → post-edit runs → importer check → ruff → scan → data/logs/web → packet.
AFTER THIS BATCH (parent only): a repo-wide privacy sweep (counts only) across every H03/H03b/H03c token set, recording the remaining KEEPs in LANDING_NOTES.
