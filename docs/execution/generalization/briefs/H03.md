=== H03: replace real institution names used as worked examples in source comments and docstrings with synthetic ones (no code or behaviour change) ===
(Parent brief, 2026-09-14. Re-derived from a read-only anchor map after the crash lost the first draft. Digests and line counts were re-checked on the post-F12b tree and again at launch. `core/agentic/gate.py:519` was added from the H03b map. Launched after the F-series was integrated.)

Source: docs/execution/generalization/owner_hardcoding_review_20260913.md, section "H03 — synthetic worked examples in resolver comments (owner item 3, low priority)": "Replace the comment and docstring examples in the two named files, plus the same-kind siblings, with synthetic institutions … No code or behaviour change."
Request packets: none; do NOT read any class-guard file. No response file; evidence in batches/H03.md.
BUG_CLASSES: none (privacy/hardcoding review item).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms (including `python -m ruff --version`);
- EVIDENCE TIMESTAMPS;
- IMPORTER GREPS;
- SIZE.

PRIVACY (read three times):
- NEVER write any real institution, organization or person name, abbreviation, fragment or state word taken from a real school's name into ANY file you create or edit: the source replacements, batches/H03.md, or your final report. Not partially, not abbreviated, not quoted as "before" text.
- Real strings may appear ONLY inside the tool inputs needed to do the work:
  - an Edit `old_string`;
  - a `grep -c`/`grep -n` whose output you do not copy into any file.
- The packet refers to the removed examples ONLY by these labels, with `file:line` anchors:
  - "School A" (the real current school; forms: A, A-short = two-letter form, A-snake = snake_case, A-frag = the state word that starts its name);
  - "School B" (a real past school);
  - "University C" (the "different school" example).
- Do NOT paste `diff -u` output into the packet. Record instead:
  - for each anchor, `file:line` → label → "replaced (comment|docstring)";
  - the changed-line count per file;
  - the statement that every `-`/`+` line of `diff -u $S/h03_pre/<file> <file>` lies inside a comment or docstring (you may read the diff; do not copy it).

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03.md → pre-edit scan → copy the five files to `$S/h03_pre/` and verify digests → baseline focused run (digests in the same command) → edits → post-edit diff check (read only) → privacy grep counts → focused (identical counts) → sweep → ruff → scan → data/logs/web listing → packet.
- No edit before the baseline run is recorded. Never implement then revert.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (inside the capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- Take every packet timestamp from `date` output in the same command as the step. Write sections in run order, with that `date` output in each header.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F13c-2b.txt` and `$S/manifest_paths_post_F13c-2b.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules.
- data/ baseline: `$S/data_baseline_post_F12a.txt`. Re-check data/ after every chunk and STOP on any change.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.
ORDER REMINDER: print `sha256sum utils/institution_resolver.py utils/location_resolver.py utils/web_search_trigger.py memory/entity_resolver.py core/agentic/gate.py` in the SAME command that runs the pre-edit baseline tests, BEFORE any edit.

PARENT-VERIFIED FACTS (anchor map 2026-09-14; digests re-checked 2026-09-14 on the post-F12b tree, and none of these files was edited by F12c–F13c-2b; re-verify every line)
- Digests:
  - utils/institution_resolver.py `2c70393a…` (468 lines);
  - utils/location_resolver.py `5392e2ad…` (611);
  - utils/web_search_trigger.py `8bb20c4f…` (2353);
  - memory/entity_resolver.py `f6b0b0d7…` (305);
  - core/agentic/gate.py `15ae94e6…` (2237; already modified by earlier batches, so its `git diff` includes their work).
- IN-SCOPE anchors (comments/docstrings only; nothing outside comments/docstrings in these files uses these names):
  - utils/institution_resolver.py:
    - `:10` (module docstring, School A as a profile value);
    - `:24-25` (module docstring, University C as the "query names a different school" example);
    - `:54` (comment above `_SCHOOL_RELATIONS`, School B as past next to School A as current);
    - `:123` (comment above `_GENERIC_SCHOOL_RE`, School A in a generic-word → school example);
    - `:328-329` (`query_justifies_institution` docstring, School A wrapped over two lines);
    - `:408`, `:410`, `:411` (`strip_unjustified_institution` docstring: School A at 408 and 411, University C at 410);
    - `:459` (`scope_identity_terms` docstring, School A).
  - utils/location_resolver.py:
    - `:310-311` (`_institution_protected_spans` docstring, University C wrapped);
    - `:313-314` (the same docstring, School A at 313; line 314 names the US state word in School A's name). Change both together: the example shows a state word inside a school name, so the replacement must be a synthetic school name that still CONTAINS a real US state word.
    - `:343` (`_sub_outside_spans` docstring, School A plus its state-word fragment);
    - `:523` (`query_justifies_location` docstring, School A);
    - `:567-568` (`strip_unjustified_location` docstring, School A at 567 and its state-word fragment at 568).
  - utils/web_search_trigger.py:
    - `:1420-1421` (`_build_llm_trigger_prompt` docstring Args section, School A wrapped);
    - `:1461` (comment inside `_build_llm_trigger_prompt`, before the import at 1464, NOT inside the prompt string, which closes at ≈1566);
    - `:1572` (module-level comment block 1569–1594 above `_PRIVATE_GUARD_TIME_STOP_TOKENS`, School A);
    - `:1578` (the same block, School A's two-letter short form);
    - `:1591-1592` (the same block, School A wrapped);
    - `:1739` (comment inside `_classify_with_llm_unified`, School A).
  - memory/entity_resolver.py: `:299` (`_normalize_id` docstring, School A and its snake_case form on one line; replace both forms consistently).
  - core/agentic/gate.py: `:519` (`_terms_are_temporal_generic` docstring, School A's two-letter short form; found by the H03b map).
- OUT OF SCOPE — record only (by `file:line` and kind), do not edit:
  - utils/institution_resolver.py `:77` (a government health-program name used as a cross-domain word; not a school);
  - utils/location_resolver.py `:359`, `:361`, `:452-453`, `:455-456`, `:543-544` (public newspapers, a company, a film character and a historical person used as "state word leading a proper name" examples);
  - utils/web_search_trigger.py comments naming the search vendor or a large tech company (≈879, 880, 909, 1258, 1577, 2105), and the website names inside prompt strings (≈1478–1526, code);
  - core/agentic/gate.py `:532-533` and `:584` (a real PERSON's name in comments; the owner will be asked at H03b launch);
  - scripts/build_wiki_subset.py seeds and every test fixture (batches H03b-1/H03b-2).
- Behaviour safety:
  - no doctests (`>>>`) in these files and no runtime `__doc__` use of them (re-verify, including gate.py);
  - tests/unit/test_music_session_misfires.py:112 reads web_search_trigger.py source for a prompt phrase at ≈1498 (no anchor touches it);
  - tests/unit/test_request_path_parity.py:198 reads source but skips `#` lines and matches call-site patterns absent from every anchor;
  - grep tests/unit for `inspect.getsource`, `__doc__` and source reads of these five modules, and confirm that none asserts on an anchor line.

SYNTHETIC REPLACEMENTS (parent decision; the SAME names batches H03b-1/H03b-2 use in test fixtures, so comments and tests agree)
- School A → "Vermont Wrenfield" (a state word plus one TitleCase token).
- A-frag (the state word alone) → "Vermont".
- A-snake → "vermont_wrenfield".
- A-short (two-letter form) → "XW".
- School B → "University of Tarnwick-Hollow".
- University C → "Quellmoor University".
- Collision check: at launch, `grep -rniw` over *.py/*.md/*.json (excluding data/, .agent_snapshots/, node_modules/) for Wrenfield, Tarnwick, Quellmoor and XW must show 0 hits before your edits. "Vermont" is expected only in utils/location_resolver.py's `_US_STATES` table (code, unchanged).
- Rewrap within the same comment or docstring if line length requires it; keep each example's point:
  - current vs past school;
  - "different school";
  - a state word inside a school name;
  - a name fragment;
  - the snake_case id form;
  - the two-letter short form.

OWNERSHIP
- The comment/docstring lines at the IN-SCOPE anchors above ONLY, in the five source files (rewrapping within the same comment/docstring is allowed).
- New docs/execution/generalization/batches/H03.md.
- Read-only: every code line in those files, scripts/**, every test file, config/**, briefs/**, LANDING_NOTES.md, the owner review doc, and every class-guard path.

CONTRACT
1. Every in-scope anchor's real name is replaced with its synthetic counterpart, preserving the example's point.
2. No code line changes: every `-`/`+` line of `diff -u $S/h03_pre/<file> <file>` lies inside a comment or docstring. Verify by reading; record the statement and per-file counts, never the diff text.
3. Privacy verification: for each real form of School A/B/University C, `grep -c -i` over the five files is 0 after the edits.
   - Exception: School A's state word, which legitimately stays in location_resolver.py's `_US_STATES` code table (and anywhere else the map records it as a generic state name). Record the count per file and confirm each remaining hit is in code, not in an anchor comment.
   - Record counts per LABEL only, never the pattern text.
4. The out-of-scope items are untouched and listed in the packet (by `file:line` and kind only).
5. No real name appears in any file this batch writes.

TESTS (no new test file: comment-only)
- Pre-edit baseline in one command: the digest line above, then the focused chunk on the UNEDITED sources; record counts.
- Post-edit: the same focused chunk with identical counts, then the sweep.
- Focused (≤9 per chunk): test_institution_resolver.py, test_location_resolver.py, test_music_session_misfires.py, test_request_path_parity.py, test_sep12_search_identity_scope.py, test_private_sphere_search_guard.py, test_personal_anchors.py, plus every tests/unit file named test_*entity_resolver* or test_*web_search_trigger* (grep and list; vet; chunk).
- Sweep: every remaining tests/unit consumer of utils.institution_resolver / utils.location_resolver / utils.web_search_trigger / memory.entity_resolver / core.agentic.gate, per IMPORTER GREPS (every import form plus dotted patch strings). Chunks of ≤9, each vetted.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
  - The gate.py consumer set is large; take it in as many chunks as needed.
- ruff on the five files.

SCAN: pre- and post-edit read-only scan. Expect identical tables (no new file, no new finding): dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648.
SIZE: expected ≈24–30 changed lines; target ≤100; HARD stop at ≤450.
ORDER: manifest → create H03.md → pre-edit scan → pre-copies → baseline focused run (digests in the same command) → edits → post-edit diff check (read only) → privacy grep counts → focused (identical counts) → sweep → ruff → scan → data/logs/web listing → packet.
