=== F10a: a failed turn write raises a typed StoreWriteError instead of returning the same None as a deliberate skip; callers keep the conversation context and the transcript ===
(Parent brief, 2026-09-14. Drafted from a read-only map while F9a ran; launched after F9b was integrated. Facts were re-verified against the post-F9b tree. The design-doc "F10 split" amendment was written at the same time.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-010: "A failure raises; None remains the documented deliberate skip. Callers record a failed write (turn-record receipt) instead of `except: pass`. A result object is rejected: it is always truthy."
- Real-defects row: "Write failures and deliberate skips both return None; 'CRITICAL … data loss' logging never fires; five `except: pass` sites".
- Batch row F10 ("Turn and skill writes: raise on failure, skips stay None", seam "store writer"); F13 owns the turn-record receipt and the five `except: pass` sites.
- Amendment "[verified] F9 and F10 share a file": multi_collection_chroma_store.py was edited F9a → F9b and now F10a.
- Amendment "F10 split" (parent decision, 2026-09-14): F10a = turn write (`add_conversation_memory` #140, `store_interaction` #127), response `CGR-20260913-010.md` (BASE); F10b = skill write (#128, #129) plus the summaries read (CGR-009 #130).
Request packet (the ONLY class-guard file you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-010.md. This batch answers anchors #127 and #140.
BUG_CLASSES: BC-20, BC-47, CM-05.
Response file (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-010.md (BASE; none exists yet).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL.

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F10a.md.
- No non-pytest Python of any kind; the exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (the four deliberate-skip guards run BEFORE any write).
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F9b.txt` and `$S/manifest_paths_post_F9b.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_f7a_rerun.txt` (7 entries; `logs/` absent). Re-check after every chunk and STOP on any change.
ORDER REMINDER: print `sha256sum utils/retrieval_outcome.py memory/storage/multi_collection_chroma_store.py memory/memory_storage.py memory/memory_coordinator.py memory/memory_interface.py gui/handlers.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): an existing test that pins the old swallow may be repaired only with the new assertion plus a paired control. List every edit. (Parent map: no unit test asserts None on a store FAILURE path; every `assert result is None` in test_api_error_storage_guard.py and test_thinking_leak_storage.py is a deliberate skip and must keep passing unedited.)

PARENT-VERIFIED FACTS (read-only map, re-verified on the post-F9b tree, 2026-09-14)
- Starting digests: utils/retrieval_outcome.py `16f7bcb9…`, memory/storage/multi_collection_chroma_store.py `66833584…`, memory/memory_storage.py `efadf44f…`, memory/memory_coordinator.py `dcac5128…`, memory/memory_interface.py `f29c967c…`, gui/handlers.py `017d8538…`, core/orchestrator.py `e336883d…` (read-only).
- utils/retrieval_outcome.py (stdlib-only leaf, F1): `RETRIEVAL_STATES`, `RetrievalError(RuntimeError)` with keyword-only `source`/`reason` and `str` = "source: reason" (docstring "The typed failure a read site raises"), `OutcomeList`, `outcome_status`. No write-error type exists anywhere (`CorruptStoreError`/`StoreVersionError` in utils/safe_json.py are load-time errors). Parity test: tests/unit/test_retrieval_outcome.py.
- memory/storage/multi_collection_chroma_store.py `add_conversation_memory` (def 536): annotated `-> str`; normalizes metadata, derives epoch, uuid, `self._get_collection('conversations').add(...)`, debug log, `return doc_id`; NO skip path; `except Exception as e: logger.error(f"Error adding conversation memory: {e}"); import traceback; traceback.print_exc(); return None` (≈566–570) → ANCHOR #140. The file already imports `RetrievalError` (F9a). Sibling `add_to_collection` has no try and already raises. Other production caller: scripts/migrate_corpus_to_chromadb.py:136 (counts None as migrated at 137; its per-row `except Exception` at 142–145 counts failures) — read-only; a raise is then counted correctly.
- memory/memory_storage.py `MemoryStorage.store_interaction` (def 790, ends 1024): the whole body is one try (818).
  - Deliberate skips, in order, all `return None`: thinking-only response (826–831), empty response (832–834), file-error response (838–840), API-error sentinel (845–847). No dedup skip.
  - Side effects before the Chroma write: thread detection (877–879), corpus `add_entry` (882–892; `save_corpus` swallows its own disk failure — sibling, record only), `conversation_context.append` (895–899), topic manager (906).
  - Inner best-effort excepts, keep: content-type detection (952–962 `except Exception: pass  # Non-fatal`), provenance config import (966–970).
  - Chroma write: `memory_id = self.chroma_store.add_conversation_memory(query, response, clean_metadata)` (1003); a None return is unchecked today.
  - After the write: per-turn fact extraction (1006–1008, flag default off), consolidation counter (1011–1015, off by default).
  - Success: `return memory_id` (1018); 1017 logs "Stored memory {memory_id}".
  - Outer except (1020–1024): `except Exception as e: logger.error(f"Error storing interaction: {e}"); traceback.print_exc(); return None` → ANCHOR #127.
  - Stale "None if storage failed" docstrings: memory_storage.py 816 and the module header (≈line 8); memory_coordinator.py 363; memory/memory_interface.py 196.
- memory/memory_coordinator.py `store_interaction` (347–396): syncs `current_topic`/`conversation_context` INTO storage (366–367), calls storage (369, no try), syncs `conversation_context` and `interactions_since_consolidation` BACK (376–378), then quick thread resolution in its own try (381–394), returns `memory_id` (396). A raise today would skip the sync-back: the next turn's sync-in would overwrite storage's appended context, so the in-session conversation would lose that turn.
- Production callers of `store_interaction` (all catch; the return value is used only by the handler below):
  - core/orchestrator.py `_handle_deictic` ≈2018 (except → warning at 2022); `_maybe_document_generation` ≈2181 and `_maybe_agentic_search` ≈2313 (`except Exception: pass`, F13 sites); `_store_interaction` ≈2490 (except → "CRITICAL: Failed to store interaction - data loss" at 2497; `self.logger = get_logger("orchestrator")` is always set at 491).
  - gui/handlers.py `_background_store_interaction` (def 143; try → `store_interaction` → "Background storage complete" log → `conversation_logger.log_interaction(..., metadata={..., 'db_id': memory_id})` (db_id at 183) → `except Exception as e: logger.error(f"[HANDLE_SUBMIT] Background storage failed: {e}")` (197)). A raise today would skip the transcript log, which a None does not. Runs as a background task after streaming.
  - gui/handlers.py `_run_doc_generation` ≈1703, `_save_daemon_note` ≈2597, `_run_action_retry` ≈3498 (`except Exception: pass`, F13 sites).
  - Interim until F13: at the five `pass` sites a raise is ignored silently, exactly as the None return is ignored today (no regression); at orchestrator 2022 and 2497 it is now logged.
- Fallback coordinators that always return None: core/orchestrator.py ≈233 and core/prompt/base.py ≈147 — out of scope, unchanged.
- Test fixtures: tests/unit/test_api_error_storage_guard.py `storage` fixture (`MemoryStorage(corpus_manager=MagicMock(), chroma_store=MagicMock(), fact_extractor=MagicMock())`); tests/unit/test_calendar_turn_round3.py drives `handlers._background_store_interaction` (≈609); tests/unit/test_store_get_by_id_outcomes.py (F9a) shows the `MultiCollectionChromaStore.__new__` + fake collection pattern.

OWNERSHIP
- utils/retrieval_outcome.py: ADD one class `StoreWriteError(RuntimeError)` after `RetrievalError` (keyword-only `source`, `reason`; `str` = "source: reason"; docstring: the typed failure a WRITE site raises; None stays the deliberate skip), plus a one-line addition to the module docstring. No other change.
- memory/storage/multi_collection_chroma_store.py: the body of `add_conversation_memory` ONLY, plus extending the existing `from utils.retrieval_outcome import RetrievalError` line.
- memory/memory_storage.py: the body and docstring of `store_interaction` ONLY, the module-header docstring line, plus one import line.
- memory/memory_coordinator.py: the body and docstring of `store_interaction` ONLY.
- memory/memory_interface.py: the `store_interaction` docstring ONLY (≈176–199).
- gui/handlers.py: the body of `_background_store_interaction` ONLY.
- New tests/unit/test_store_write_outcomes.py; new docs/execution/generalization/batches/F10a.md; the response file.
- Read-only: `store_skill`, `_get_recent_summaries_by_timespan`, `_maybe_regenerate_narrative` (F10b), every other method in those files (including `get_by_id` and `get_ids_by_timestamp_range` in the store), core/orchestrator.py (F13), every other handlers function (F13), scripts/**, memory/corpus_manager.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `StoreWriteError` exists in the leaf as above; `RETRIEVAL_STATES` and every existing name are unchanged (the existing parity test still passes).
2. `add_conversation_memory`: the except keeps its log line (drop nothing else), then `raise StoreWriteError(source="chroma_conversations", reason=type(e).__name__) from e`. Success returns the doc id string as today.
3. `store_interaction`:
   - the four deliberate skips return None exactly as today, before any write;
   - a `StoreWriteError` from the Chroma write propagates UNCHANGED (not re-wrapped);
   - any other exception in the body (thread detection, corpus write, metadata build, …) keeps the existing log line, then `raise StoreWriteError(source="store_interaction", reason=type(e).__name__) from e`;
   - success returns the id as today; the two inner best-effort excepts are unchanged;
   - docstrings say: returns the id; None only for a deliberate skip; raises StoreWriteError on a failed write.
4. `MemoryCoordinator.store_interaction`: the sync-back of `conversation_context` and `interactions_since_consolidation` runs even when storage raises (try/finally); the exception then propagates unchanged; quick thread resolution runs only after a non-raising call (as today on success). Docstring updated.
5. `_background_store_interaction`: a raised store failure is logged (the existing error line; `StoreWriteError`'s str carries labels only) and the transcript `log_interaction` call STILL happens with `db_id=None` and the same sanitized response and metadata as today's None path; nothing escapes the task. The success path is byte-for-byte unchanged in behaviour. No new metadata key (the receipt is F13's).
6. No change to orchestrator.py or any other handlers function (record the interim state above).
7. Privacy: `StoreWriteError` carries only a source label and an exception class name.

TESTS (tests/unit/test_store_write_outcomes.py; fakes only; never a real Chroma client, embedder or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Tests that import `StoreWriteError` fail on the unedited leaf — an expected, recorded failure mode; structure the file so the skip/success controls still run (e.g. import inside the tests that need it) so the record shows which tests fail and which pass.
- Leaf: `StoreWriteError(source=, reason=)` str and attributes; subclass of RuntimeError; not a `RetrievalError`.
- Chroma: `MultiCollectionChromaStore.__new__` with a fake `_get_collection` / collections (F9a pattern): `.add` raising → `StoreWriteError(source="chroma_conversations", reason=<class>)`, marker absent; success → the doc id.
- Storage (local copy of test_api_error_storage_guard.py's `storage` fixture):
  - chroma raising `StoreWriteError` → propagates unchanged (same object);
  - corpus `add_entry` raising → `StoreWriteError(source="store_interaction", reason=<class>)`;
  - each of the four deliberate skips → None and the chroma write is not called (paired controls);
  - success → the id.
- Coordinator: a coordinator whose `_storage.store_interaction` raises (build it the lightest way an existing unit test does; if none, `MemoryCoordinator.__new__` with the attributes the method reads) → re-raises, and `conversation_context` / `interactions_since_consolidation` were synced back; success control.
- Handler: `gui.handlers._background_store_interaction` (precedent tests/unit/test_calendar_turn_round3.py ≈609) with `store_interaction` raising `StoreWriteError` → no exception, error logged (caplog; marker absent), `log_interaction` called once with `metadata['db_id'] is None` and the sanitized response; success control unchanged.
- Privacy: a distinctive marker in every injected exception message never appears in any `StoreWriteError` str/reason or in the handler's error log.
- Focused (≤9 per chunk):
  - chunk 1: the new file, test_retrieval_outcome.py, test_api_error_storage_guard.py, test_thinking_leak_storage.py, test_memory_file_error_filter.py, test_provenance.py, test_entity_facts.py, test_calendar_turn_round3.py, test_handle_submit.py;
  - chunk 2: test_process_user_query.py, test_request_path_parity.py, test_api_chat.py, test_action_guard_integration.py, test_orchestrator_helpers.py, test_extraction_replay_provenance.py, test_knowledge_graph.py, test_fact_verification.py, test_stance_write_path.py;
  - chunk 3: test_store_get_by_id_outcomes.py, test_expander_outcomes.py, test_chroma_store_no_autodelete.py, test_recent_summaries_fix.py.
- Sweep: every remaining tests/unit importer of memory.memory_storage / MemoryStorage / memory.memory_coordinator / MemoryCoordinator / memory.memory_interface / utils.retrieval_outcome / multi_collection_chroma_store, and every tests/unit file referencing `store_interaction` or `_background_store_interaction` (grep and list), in chunks of ≤9; vet each for real stores, default paths and real embedders BEFORE running; usual exclusions (test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py). Top-level tests/ files are never run.

SCAN: pre- and post-edit read-only scan. Baseline after F9b: dm18 found 47 / base 79 / new 0 / stale 32. Expect the dm18 rows for #127 and #140 to go STALE, new = 0.
RESPONSE FILE (BASE `CGR-20260913-010.md`): #127 and #140 fixed with deployed-function evidence; the deliberate-skip controls; the coordinator/handler continuity changes; the interim caller state until F13 (five `pass` sites; the now-live CRITICAL log); the migrate script consequence; siblings recorded (`save_corpus` swallow; fallback coordinators); remaining CGR-010 anchors mapped: #128, #129 → F10b (`-2`); #146 → F11a-1 (`-3`); #131 → F11b-1 (`-4`); receipts and the five `pass` sites → F13.
SIZE: target ≤380 changed lines; HARD stop at ≤450 (stop before exceeding it and return a split proposal: leaf+chroma+storage vs coordinator+handler).
ORDER: manifest → create F10a.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → response file.
