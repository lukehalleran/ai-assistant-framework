=== F10b: a failed skill write raises instead of looking like a dedup skip; the narrative is never regenerated from a failed summaries read ===
(Parent brief, 2026-09-14. Drafted from a read-only map while F9a ran; launched after F10a was integrated. Facts were re-verified on the post-F10a tree.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-010: "A failure raises; None remains the documented deliberate skip." CGR-009: "Raise on failure (not-found stays None/[])".
- Batch table correction "[verified] Anchor #130 had no batch": assigned to F10; "`_maybe_regenerate_narrative` … must degrade explicitly: never regenerate a narrative from a failed read."
- Amendment "[map] More sequential seams": shutdown_processor.py regions in order — F10: the `store_skill` count at ≈1161; F11a: ≈1545/≈1629; F11b: ≈1253/≈1466; F12: ≈1312.
- Amendment "F10 split and the write error type" (parent decision, 2026-09-14): F10b = skill write (#128, #129) + summaries read (CGR-009 #130); the `get_summaries(limit=)` signature defect is an owner decision and is NOT fixed.
Request packets (the ONLY class-guard files you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-010.md (#128, #129) and /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#130).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response files (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-010-2.md (#128, #129) and docs/execution/generalization/class_guard_responses/CGR-20260913-009-3.md (#130).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (note the new tests/unit/test_daily_notes_auto_update.py exclusion and the MemoryConsolidator/DailyNotesGenerator profile-and-vault hazard).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F10b.md.
- No non-pytest Python of any kind; the exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (`PROCEDURAL_SKILLS_ENABLED`, `NARRATIVE_CONTEXT_ENABLED`, the dedup threshold, the model-manager guard in `_extract_procedural_skills`).
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F10a.txt` and `$S/manifest_paths_post_F10a.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_f7a_rerun.txt` (7 entries; `logs/` absent). Re-check after every chunk and STOP on any change. Never construct a real `MemoryConsolidator`, `CorpusManager` with a default path, or `UserProfile()`; stub the consolidator and corpus manager.
ORDER REMINDER: print `sha256sum memory/memory_storage.py memory/shutdown_processor.py memory/memory_interface.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit. (Parent map: no unit test references `store_skill`, `ProceduralSkill`, `_get_recent_summaries_by_timespan` or `_maybe_regenerate_narrative`; no unit test drives `_extract_procedural_skills`.)

PARENT-VERIFIED FACTS (read-only map, re-verified on the post-F10a tree, 2026-09-14)
- Starting digests: memory/memory_storage.py `e8f3b6e8…`, memory/shutdown_processor.py `01afac00…`, memory/memory_interface.py `d17701d9…`, utils/retrieval_outcome.py `1abc1586…` (read-only), memory/corpus_manager.py `df3f4cd4…` (read-only).
- After F10a: `utils.retrieval_outcome.StoreWriteError(source=, reason=)` exists next to `RetrievalError`; memory/memory_storage.py already imports `StoreWriteError` (F10a). See batches/F10a.md's parent section.
- memory/memory_storage.py `MemoryStorage.store_skill` (def 1500, ends ≈1556):
  - outer try (≈1510); `if not PROCEDURAL_SKILLS_ENABLED: return None` (1512–1513) — deliberate skip;
  - inner `try: coll = self.chroma_store._get_collection(collection_name)` / `except (ValueError, Exception) as e:` warning "procedural_skills collection not available" (1521) → `return None` → ANCHOR #128 (a failure disguised as a skip);
  - dedup (≈1527–1541): `coll.count()` and `query_collection(...)`; a match with `score >= SKILL_DEDUP_THRESHOLD` logs "Skill deduplicated" (1538) → `return None` — deliberate skip; the dedup read is NOT in the inner try (the outer except catches it); `query_collection` already raises (F9a store notes);
  - write: `doc_id = self.chroma_store.add_to_collection(...)` (1545; raises on failure) → `return doc_id`;
  - outer except: `logger.error(f"[MemoryStorage] Failed to store skill: {e}")` (1555) → `return None` → ANCHOR #129.
  - Docstrings "None if duplicate or failed": memory_storage.py 1508 and memory/memory_interface.py 229.
- Callers of `store_skill`: memory/memory_coordinator.py `store_skill` (≈571–576, pure pass-through, no try; unchanged, read-only); memory/shutdown_processor.py `_extract_procedural_skills` (def 1036):
  - early returns: skills disabled (≈1043–1048), no model manager (≈1050–1051); LLM output parsed line by line; invalid skills skipped (≈1147–1159);
  - `doc_id = await self._storage.store_skill(skill)` (1161, NOT in a try) → `if doc_id: kept += 1` (1162–1163) → `if kept: logger.info(f"[Shutdown] Extracted {kept} procedural skill(s)")` (1165–1166);
  - it runs in Phase A `asyncio.gather(..., return_exceptions=True)` (≈269–275), whose exceptions go to `_log_shutdown_llm_failure("extraction", r)` (≈276–278) — an LLM-failure label. A raise from `store_skill` would stop the remaining skills and be mislabeled.
- memory/memory_storage.py `_get_recent_summaries_by_timespan` (def 1604, try ≈1616, except ≈1654–1656) → ANCHOR #130:
  - `all_summaries = self.corpus_manager.get_summaries(limit=50)` (1618); `[]` if empty (≈1620–1621); `[]` for an unknown `span_type` (≈1632–1633); per-row timestamp parse failure `except Exception: continue` (≈1641–1642); sort (≈1651); `return filtered[:limit]` (≈1652);
  - except: `logger.debug(f"[MemoryStorage] Error getting {span_type} summaries: {e}")` (1655) → `return []`.
  - **Pre-existing defect (NOT fixed here; owner decision):** the production `CorpusManager.get_summaries(self, count: int = 5)` (memory/corpus_manager.py:474) has no `limit` parameter, so this call raises `TypeError` on every call and has always returned `[]`. Leave the call unchanged; after this batch the failure is reported instead of hidden.
  - Callers: only `_maybe_regenerate_narrative` (1570 `"weekly", limit=4`; 1571 `"monthly", limit=2`).
- `_maybe_regenerate_narrative` (def 1558, try ≈1564, except ≈1600–1602 warning): returns if `NARRATIVE_CONTEXT_ENABLED` is off (1565–1567); `if not recent_weeklies and not recent_monthlies:` → debug and return (1573–1575) — a failed and an empty read look the same, and one failed read plus one healthy read regenerates from partial input; returns if no consolidator (≈1577–1579); `user_statements = self.corpus_manager.get_recent_memories(60)` best-effort (1585; `except Exception as e: logger.debug` ≈1586–1587); `narrative = await self.consolidator.generate_narrative_context(...)` (1590); `if narrative: self.corpus_manager.save_narrative_context(narrative)` (≈1596–1597). Only caller: `consolidate_and_store_summary` (def 1450; reached only from `store_interaction` 1018 when `SUMMARIZE_AT_SHUTDOWN_ONLY` is "0"; default "1").

OWNERSHIP
- memory/memory_storage.py: the bodies and docstrings of `store_skill`, `_get_recent_summaries_by_timespan` and `_maybe_regenerate_narrative` ONLY (extend the existing `from utils.retrieval_outcome import StoreWriteError` line if `RetrievalError` is needed).
- memory/shutdown_processor.py: the `store_skill` call and `kept` count lines inside `_extract_procedural_skills` (≈1161–1166) ONLY.
- memory/memory_interface.py: the `store_skill` docstring ONLY (≈222–231).
- New tests/unit/test_skill_and_summary_outcomes.py; new docs/execution/generalization/batches/F10b.md; the two response files.
- Read-only: `store_interaction` and every other memory_storage method (F10a's state), memory/corpus_manager.py, memory/memory_coordinator.py, memory/memory_consolidator.py (F12a), every other shutdown_processor region (F11a/F11b/F12), utils/retrieval_outcome.py, core/**, gui/**, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `store_skill`:
   - disabled → None; dedup match → None (deliberate skips, unchanged);
   - collection unavailable (#128) → keep the warning line, then `raise StoreWriteError(source="procedural_skills", reason=f"collection:{type(e).__name__}") from e`;
   - any other failure in the body — the dedup read, the write (#129) → keep the error line, then `raise StoreWriteError(source="procedural_skills", reason=type(e).__name__) from e`; a `StoreWriteError` raised inside propagates unchanged (not re-wrapped);
   - success returns the doc id; docstrings updated.
2. `_extract_procedural_skills`: a `StoreWriteError` from one skill is counted (`failed += 1`) and the loop continues with the remaining skills; the final log reports kept and failed counts (counts only); any other exception propagates as today. A dedup skip is still "not kept", not "failed".
3. `_get_recent_summaries_by_timespan` (#130): the except keeps its log line, then `raise RetrievalError(source="corpus_summaries", reason=type(e).__name__) from e`; a genuinely empty corpus and an unknown `span_type` → [] (unchanged); the per-row parse skip is unchanged (record as a limitation); the `get_summaries(limit=50)` call is unchanged.
4. `_maybe_regenerate_narrative`: if either summaries read raises `RetrievalError`, or the `get_recent_memories` read fails, log one warning with labels only and return WITHOUT calling `generate_narrative_context` or `save_narrative_context`. Both reads genuinely empty → return as today. Healthy reads → generate and save as today.
5. Consequence to record: with the production `CorpusManager`, every call now logs "summaries unavailable: corpus_summaries: TypeError" on the non-default per-turn consolidation path, and no narrative is regenerated — the same outcome as today, now visible.
6. No other change. Privacy: labels and class names only.

TESTS (tests/unit/test_skill_and_summary_outcomes.py; fakes only; no real Chroma, embedder, model, consolidator, profile or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- `store_skill` (MemoryStorage built like test_api_error_storage_guard.py's fixture, a LOCAL copy; monkeypatch `PROCEDURAL_SKILLS_ENABLED` and the dedup threshold where they are read — read the method first): disabled → None and no write; dedup match → None and no write; `_get_collection` raising → `StoreWriteError` reason "collection:<class>"; dedup `query_collection` raising → `StoreWriteError` and no write; `add_to_collection` raising → `StoreWriteError`; success → the id.
- `_extract_procedural_skills` (ShutdownProcessor via `__new__` with the attributes it reads; a stub `model_manager.generate_once` AsyncMock returning two JSON skill lines; a stub `_storage.store_skill` that raises `StoreWriteError` for the first skill and returns an id for the second): both skills attempted, kept 1, failed 1, no exception; controls: a dedup None is not counted as failed; all healthy → kept 2. Read the elided body (≈1052–1146) first to build the stub output correctly; if the method cannot be driven without a real model or store, record that as a limitation and test only what is reachable.
- Summaries read: a fake corpus whose `get_summaries` raises → `RetrievalError(source="corpus_summaries")`; a fake with the production signature `get_summaries(self, count=5)` → `RetrievalError(reason="TypeError")` (pins the pre-existing defect); empty → []; healthy (a fake accepting `limit`) → the filtered rows.
- Narrative: weekly read raising + monthly healthy → no generate, no save; both empty → no generate (unchanged); `get_recent_memories` raising → no generate; healthy → generate and save called once (control). Stub the consolidator with an AsyncMock; never a real model.
- Privacy: markers in injected exception messages never appear in any error reason or warning.
- Focused (≤9 per chunk): the new file, test_store_write_outcomes.py (F10a), test_retrieval_outcome.py, test_api_error_storage_guard.py, test_thinking_leak_storage.py, test_memory_file_error_filter.py, plus every tests/unit file referencing `store_skill`, `ProceduralSkill`, `procedural_skills`, `shutdown_processor`, `ShutdownProcessor`, `narrative_context` or `get_summaries` (grep, list, VET, chunk — never tests/unit/test_daily_notes_auto_update.py).
- Sweep: every remaining tests/unit importer of memory.memory_storage / MemoryStorage / memory.shutdown_processor / memory.memory_interface (grep and list), chunks of ≤9, vetted; usual exclusions (test_graph_integration.py, test_daily_notes_auto_update.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file).

SCAN: pre- and post-edit read-only scan. Baseline after F10a: dm18 found 45 / base 79 / new 0 / stale 34. Expect the dm18 rows for #128, #129 and #130 to go STALE, new = 0.
RESPONSE FILES: `CGR-20260913-010-2.md` (#128, #129 fixed with deployed-function evidence; the shutdown count change; skip controls; remaining CGR-010 anchors → F11a-1 #146 (`-3`), F11b-1 #131 (`-4`), F13 receipts). `CGR-20260913-009-3.md` (#130 fixed; the narrative consumer's explicit degrade; the pre-existing `get_summaries(limit=)` signature defect recorded as an owner decision, not fixed; remaining CGR-009 anchors → F11a-1 #121/#147/#148 (`-4`), F11b-1 #132–#134/#136 (`-5`), F12a–F12d).
SIZE: target ≤380 changed lines; HARD stop at ≤450 (stop before exceeding it and return a split proposal: skill write vs summaries read).
ORDER: manifest → create F10b.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → both response files.
