=== F12d-2: the shutdown synthesis audit check fails closed — dreaming never runs when the audit stats cannot be read ===
(Parent brief, 2026-09-14. Split from F12d on SIZE; F12d shipped the synthesis reads. Launched after F12d was integrated. Owner decision 2026-09-14, item 2: the synthesis audit auto-halt becomes fail-closed.)

READ FIRST — the two process rules the last two batches broke:
- Your FIRST source write comes only AFTER the failing-first run is recorded in batches/F12d-2.md.
- Never implement then revert. Measure size before each later part.
- F12d edited the sources before its tests and had to revert them. Do not repeat that.

Design source: docs/execution/generalization/failure_outcome_design.md
- "Owner decisions and the F11b split (2026-09-14)" → "Synthesis audit auto-halt": the audit read catches the typed `RetrievalError` and skips dreaming; any other failure propagates to `_run_synthesis_dreaming`'s existing outer except, which ends dreaming.
- briefs/PARENT_STATE.md "Owner decisions received 2026-09-14", item 2.
Request packets: none; do NOT read any class-guard file. No response file; evidence in batches/F12d-2.md. CGR-009 is already fully answered.
BUG_CLASSES: BC-20, BC-47, CM-05.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- SIZE;
- SCANNER SHAPE: catch the TYPED failure for a contract-required degrade; never add a new broad except that returns; escalate a new gate finding;
- the exact permitted `python` forms;
- EVIDENCE TIMESTAMPS.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create F12d-2.md → pre-edit scan → write the test file → failing-first (digest in the same command) → source edit → focused → sweep → ruff → scan → data/logs listing → packet.
- NEVER put `python` in a Bash command except:
  - `python -m pytest …` (inside the capped systemd-run);
  - `python -m ruff check …`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `python -c "import utils; print(utils.__file__)"` sanity check.
  - No other `python -c`, heredoc, REPL or script.
- Take every packet timestamp from `date` output in the same command as the step. Write sections in run order. Put the `date` output in the section header; never type a time by hand.
- Check each fixture against the gates it must pass: `SYNTHESIS_GENERATOR_ENABLED`, `SYNTHESIS_POOLED_ENABLED`, `SYNTHESIS_RETRIEVAL_ENABLED`, `GRAPH_WALK_ENABLED` and `SYNTHESIS_AUDIT_ENABLED`, and the auto-halt thresholds. `_run_synthesis_dreaming` imports its flags from `config.app_config` INSIDE the method (1689–1700), so monkeypatch them on `config.app_config`.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F12d.txt` and `$S/manifest_paths_post_F12d.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`. Re-check data/ after every chunk; ANY difference means STOP.
- Never construct a real generator, filter, graph, embedder, ModelManager or Chroma store.
- Never import or build the Gradio synthesis tab. Never run main.py or any script.
ORDER REMINDER: print `sha256sum memory/shutdown_processor.py memory/synthesis_memory.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit.

PARENT-VERIFIED FACTS (post-F12d tree, 2026-09-14; re-verify every line)
- Starting digests:
  - memory/shutdown_processor.py `9aa08ee5…` (unchanged since F11b-1b; lines below are current);
  - read-only: memory/synthesis_memory.py `1419e20e…` (591 lines, after F12d), utils/retrieval_outcome.py `1abc1586…`, tests/unit/test_synthesis_read_outcomes.py `549e5546…`, tests/unit/test_synthesis_audit.py `d25b3d54…`.
- memory/shutdown_processor.py:
  - public wrapper `run_synthesis_dreaming` (def 1668) → `await self._run_synthesis_dreaming()`.
  - `_run_synthesis_dreaming` (def 1678):
    - outer `try:` (1688); the flag import from `config.app_config` (1689–1700); `if not (SYNTHESIS_GENERATOR_ENABLED or SYNTHESIS_POOLED_ENABLED): return` (1704–1705);
    - `if SYNTHESIS_AUDIT_ENABLED:` (1708) → inner `try:` (1709): `from memory.synthesis_memory import SynthesisMemory` (1710); `audit_mem = SynthesisMemory(self.chroma_store)` (1711); `audit_stats = audit_mem.get_audit_stats()` (1712);
    - `if audit_stats.get("auto_halt", False):` (1713) → `logger.warning("[Shutdown] Synthesis HALTED by audit queue — …")` (1714–1722) → `return` (1723);
    - `except Exception as e:` (1724) → `logger.debug(f"[Shutdown] Audit check skipped: {e}")` (1725). Dreaming then PROCEEDS (FAIL-OPEN today);
    - then `from knowledge.synthesis_filter import SynthesisFilter` (1727), `from memory.synthesis_memory import SynthesisMemory` (1728), `mc = self.memory_coordinator` (1730), and the generators from ≈1736 (read 1727–1832 and list every import and constructor the method reaches);
    - outer `except Exception as e:` (1831) → `logger.warning("[Shutdown] Synthesis dreaming failed (non-fatal): %s", e)` (1832).
  - Callers of `run_synthesis_dreaming`: main.py 653 and 1728, inside `asyncio.wait_for` with their own excepts (read-only).
- After F12d, `SynthesisMemory.get_audit_stats` (def ≈508) calls `get_all_results(limit=500)` with no try. `get_all_results` raises `RetrievalError(source="synthesis_memory", reason="all_results:<class>")` on a failed read. So a failed audit read now reaches the inner broad except as a `RetrievalError` and dreaming proceeds. Before F12d it saw zeros, `auto_halt=False`, and also proceeded.
- tests/unit importers of memory.shutdown_processor / ShutdownProcessor (parent grep; re-grep at the start and vet each):
  - test_codex_followups.py
  - test_entity_facts.py
  - test_knowledge_graph.py
  - test_ordered_slice_guard.py
  - test_proposal_loop_outcomes.py
  - test_proposal_store_outcomes.py
  - test_retriever_read_outcomes.py
  - test_retrospective_small_guards.py
  - test_sep09_storage_repairs.py
  - test_shutdown_pipeline_proposals.py
  - test_skill_and_summary_outcomes.py
  - test_source_excerpt_pipeline.py
  - test_stance_write_path.py
  - test_stream_artifacts.py
  - test_thread_outcomes.py
  - The set may be smaller than the synthesis importer set F12d used; grep exactly.

OWNERSHIP
- memory/shutdown_processor.py: inside `_run_synthesis_dreaming`, the audit-check block 1708–1725 ONLY, plus one import of `RetrievalError` (local inside the block, or at module level if the file already has a `utils.retrieval_outcome` import — check; record which). Docstring: one sentence in `_run_synthesis_dreaming`'s docstring about fail-closed may be added.
- New tests/unit/test_synthesis_audit_fail_closed.py; new docs/execution/generalization/batches/F12d-2.md.
- Read-only: every other shutdown_processor region, memory/synthesis_memory.py, knowledge/**, gui/**, scripts/**, main.py, config/**, utils/retrieval_outcome.py, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT (owner decision, FAIL-CLOSED)
1. Replace the inner `except Exception as e: logger.debug(f"[Shutdown] Audit check skipped: {e}")` with `except RetrievalError as e:`. It logs ONE warning with labels only, for example `logger.warning("[Shutdown] Synthesis dreaming skipped: audit stats unavailable (%s: %s)", e.source, e.reason)`, then `return`. This is the typed degrade.
2. Any OTHER exception in the audit check is no longer swallowed. It propagates to the method's existing outer `except Exception` (1831), which logs "Synthesis dreaming failed (non-fatal)" and ends the method. So dreaming never runs without a readable audit.
3. Do NOT add a new broad `except Exception` + `return`.
4. Unchanged: the auto-halt-true path, the `SYNTHESIS_AUDIT_ENABLED` False path (no audit read, dreaming proceeds), the disabled gates, and the healthy path.
5. No caller change. The public wrapper and main.py already treat the method as non-fatal.
6. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only; the new warning must not include `{e}` text.

TESTS (tests/unit/test_synthesis_audit_fail_closed.py; fakes only)
- Build the processor with `ShutdownProcessor.__new__(ShutdownProcessor)`, setting only the attributes the method reads before the generators (`chroma_store`, `memory_coordinator`), or with the constructor using MagicMocks and `user_profile=None`.
- Read 1678–1832 first. Monkeypatch every synthesis flag on `config.app_config`.
- Make "dreaming proceeded past the audit check" observable WITHOUT building anything real: patch `knowledge.synthesis_filter.SynthesisFilter` (the name imported at 1727) with a spy class that records construction. Patch EVERY generator class the method would construct at its import source (e.g. a spy that returns no candidates), so nothing real is built. Record in the packet each name you patched and where it is imported.
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED source. Record the failures.
- Required tests:
  - Typed degrade (deployed chain):
    - Setup: a REAL `SynthesisMemory` over a local fake store whose `query_collection` raises `RuntimeError(marker)`, so `get_audit_stats` raises `RetrievalError("all_results:RuntimeError")`.
    - Expect: no `SynthesisFilter` or generator constructed; exactly one warning containing `synthesis_memory` and `all_results:RuntimeError`; marker absent from every new log line; no exception escapes `run_synthesis_dreaming`.
  - Broad failure: patch `memory.synthesis_memory.SynthesisMemory` so its construction raises `RuntimeError`.
    - Expect: no filter or generator constructed; the outer "Synthesis dreaming failed (non-fatal)" warning logged; no exception escapes.
  - Controls:
    - `auto_halt` True (fake stats) → the HALTED warning, no filter or generator (as today);
    - `auto_halt` False with healthy fake stats → the filter spy IS constructed (dreaming proceeds);
    - `SYNTHESIS_AUDIT_ENABLED` False → no audit read (SynthesisMemory spy not called for the audit) and the filter spy IS constructed;
    - both generator flags False → early return, nothing constructed.
- Privacy: the marker never appears in any NEW log line. Pre-existing lines kept unchanged may carry `{e}`; do not assert on those.
- Focused (≤9 per chunk): the new file, test_synthesis_audit.py, test_synthesis_read_outcomes.py, test_shutdown_pipeline_proposals.py, test_codex_followups.py, test_retrieval_outcome.py.
- Sweep: every remaining tests/unit importer of memory.shutdown_processor / ShutdownProcessor (from the re-grep), in chunks of ≤9, each vetted BEFORE running.
  - Never build the Gradio tab.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.

SCAN: pre- and post-edit read-only scan.
- Baseline after F12d: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 643.
- Expect no dm18 change (dm17 files +1 for the new test file) and new = 0.
- A NEW dm18 finding on the typed `except RetrievalError: … return` means STOP and escalate. Do not restructure around it.
SIZE: target ≤250 changed lines; HARD stop at ≤450.
- Measure the test file before the source edit; never implement then revert; never trim to fit.
ORDER: manifest → create F12d-2.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet.
