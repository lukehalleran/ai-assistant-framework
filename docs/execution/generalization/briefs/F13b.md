=== F13b: GUI inline turn paths record a labelled `storage_failed` receipt instead of silently passing; the background store marks the transcript when a write failed ===
(Parent brief, 2026-09-14. Drafted while F10b ran from a read-only map. It includes owner decision 4 as the owner revised it on 2026-09-14: no chat text. Every anchor was re-checked on the post-F12b tree and again on the post-F13a tree, and all are unchanged. Launched after F13a was integrated.)

READ FIRST — the process rules recent batches broke:
- Your FIRST source write comes only AFTER the new test file exists AND the failing-first run (digests printed in the same command) is recorded in batches/F13b.md.
- Never implement then revert. Measure the test file's size before the source edit.
- If the whole contract will not fit under the hard stop, stop BEFORE any source edit and return the split proposal named under SIZE.

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-010: "Callers record a failed write (turn-record receipt) instead of `except: pass`."
- Amendment "F13 split and receipt carriers (parent decision, 2026-09-14)": F13b = the three gui/handlers.py inline `pass` sites plus the background store's transcript receipt. No chat text on any path.
- Owner decision 4 as revised 2026-09-14 (briefs/PARENT_STATE.md "Owner decisions"): a failed memory save is NEVER reported inside the chat reply text.
  - It goes to the logs (this batch) and to a small transient GUI status message (F13c-2).
  - Every chunk this batch yields stays byte-for-byte unchanged.
- The delivered-debug-record update and the deferred turn record for the background save are F13c-1, not this batch.
Request packets: none; do NOT read any class-guard file. No response file; evidence in batches/F13b.md.
BUG_CLASSES: BC-20, BC-47, CM-05.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- SIZE;
- SCANNER SHAPE;
- the exact permitted `python` forms (which now explicitly include `python -m ruff --version`);
- EVIDENCE TIMESTAMPS.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create F13b.md → pre-edit scan → write the test file → failing-first (digests in the same command) → source edits → focused → sweep → ruff → scan → data/logs listing → packet.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (inside the capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `python -c "import utils; print(utils.__file__)"` sanity check.
- Take every packet timestamp from `date` output in the same command as the step. Write sections in the order the steps ran, and put that `date` output in each section header.
- Check each fixture against the gates it must pass: the dispatcher intents, `ctx.handled`, partial `SimpleNamespace` ctx objects without `telemetry`, and `memory_system=None`.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F13a.txt` and `$S/manifest_paths_post_F13a.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`. Re-check data/ after every chunk; ANY difference means STOP.
- Turn telemetry and conversation logs must go to tmp paths in every test (the autouse sandbox or explicit patches). Never write logs/ or a real conversation-log directory.
- Never build the Gradio UI or start the API server.
ORDER REMINDER: print `sha256sum gui/handlers.py utils/conversation_logger.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit.

PARENT-VERIFIED FACTS (post-F13a tree, 2026-09-14; re-verify every line)
- Starting digests:
  - gui/handlers.py `9cf718e4…` (5861 lines);
  - utils/conversation_logger.py `52828cbf…`;
  - read-only tests: test_doc_conversation_source.py `631dbc0b…`, test_sep07_calendar_offer_continuation.py `7ea6c736…`, test_store_write_outcomes.py `98c9c180…`, test_section_outcome_receipts.py `18255684…`, test_handle_submit.py `4f4ca7c7…`, test_request_path_parity.py `cfbbadef…`, test_action_guard_integration.py `e4447444…`, test_calendar_turn_round3.py `d7d8bff2…`;
  - core/orchestrator.py `d33e5100…` (after F13a).
- The shared label helper: F13a added `core/orchestrator.py:_storage_failure_label(exc)`.
  - It returns `"<source>: <reason>"` for a `StoreWriteError`, and `"store_interaction: <class>"` for anything else.
  - gui/handlers.py has no module-level import of core.orchestrator, but already imports from it locally (`from core.orchestrator import PostResponseHookContext, run_post_response_hooks` at 912, inside `_write_turn_telemetry`).
  - It also imports `StoreWriteError` locally at 167 (inside `_background_store_interaction`).
- `ctx` = `SubmitContext` (≈1186) with `ctx.telemetry` (≈1248) and `ctx.debug_record` (≈1261).
- `_build_debug_record(..., extra=...)` (def 639) merges `extra` via `if extra: record.update(extra)` (705–706). NONE of the three inline sites passes `extra=` today.
- The three inline sites (each store completes before the reply is yielded):
  - `_run_doc_generation` (def 1644; dispatcher ≈5686–5691):
    - `_doc_response` built 1701–1708;
    - store 1712–1718 (`if orchestrator.memory_system:`, `tags=["document_generation"]` 1717);
    - `except Exception:` 1719 / `pass` 1720;
    - `_build_debug_record(mode='doc-generation', …)` 1730–1736 → `yield {"role": "assistant", "content": _doc_response, "debug": debug_record}` 1737 → `_write_turn_telemetry(...)` 1738–1742 → `ctx.handled = True` 1743.
    - Outer `except Exception as e:` 1746. A raise from receipt code leaves `ctx.handled` False, and the dispatcher falls through to the agentic path (a second answer).
  - `_save_daemon_note` (def 2563; callers `_run_self_note` ≈3405, `_run_pending_proposal` ≈3433, affirmation follow-through):
    - `_resp` built ≈2588–2603;
    - store 2606–2610 (`tags=["daemon_self_note"]` 2609); `except Exception:` 2611 / `pass` 2612;
    - `_build_debug_record(mode='self-note', …)` 2618–2624 → yield 2625 → `_write_turn_telemetry(...)` 2626.
    - It has no try of its own; the callers' excepts (≈3413, ≈3438) fall through to the normal flow.
  - `_run_action_retry` (def 3470; dispatcher ≈5502–5507):
    - `_resp` = text + `_format_action_proposal_card(new)` 3502–3506;
    - store 3507–3511 (`tags=["action_retry"]` 3510); `except Exception:` 3512 / `pass` 3513;
    - `_build_debug_record(mode='action-retry', …)` 3515–3521 → `yield {…, "pending_action_id": new.action_id}` 3522–3523 → `_write_turn_telemetry(...)` 3524 → `ctx.handled = True` 3525.
    - A raise lands in the except at 3526, after the card is queued.
- Background store: `_background_store_interaction` (def 143; F10a):
  - store ≈172;
  - `except StoreWriteError as e:` 184 → `logger.error(f"[HANDLE_SUBMIT] Background storage failed: {e}")` 186;
  - `log_metadata` ≈189–197 (`'db_id': memory_id` 194) → `conversation_logger.log_interaction(...)` 198;
  - outer `except Exception` ≈207–208 (logs at 208).
  - It is dispatched as `asyncio.create_task` by `_dispatch_storage` (def 822) after the final chunk.
- utils/conversation_logger.py:
  - `log_format` defaults to "text";
  - the handlers use `get_conversation_logger()` (imported 113; called ≈5275) with the default, so the TEXT format is in use;
  - `_log_text` (def 104) prints `Memory ID` from `db_id` (125–126, 133–134), and otherwise only topic, personality, files and mode;
  - `_log_json` (def 138) writes the whole metadata dict.
- Test hazards:
  - tests/unit/test_doc_conversation_source.py (≈204–300) calls `_run_doc_generation` with a `SimpleNamespace` ctx that has NO `telemetry` attribute and `memory_system=None`. An unconditional `ctx.telemetry[...]` would break `ctx.handled is True`.
  - tests/unit/test_sep07_calendar_offer_continuation.py (≈434–457) drives `_run_action_retry` with `memory_system=None`, `telemetry={}` and a patched `_build_debug_record`.
  - tests/unit/test_store_write_outcomes.py `TestBackgroundStoreInteraction` asserts:
    - the "Background storage failed" log text;
    - `log_interaction.assert_called_once()`;
    - `metadata["db_id"]` (None on failure, "mem-42" on success).
    - Keep the log text, one `log_interaction` call, and the success metadata unchanged.
  - tests/unit/test_section_outcome_receipts.py `test_two_callers_read_and_forward` (≈187–196) counts exactly 3 `section_outcomes=` and 2 `getattr(orchestrator, '_last_section_outcomes', {})` in the handlers source; both counts were confirmed today (3 and 2). Add neither.
  - tests/unit/test_request_path_parity.py (≈193–230) forbids `escalation_tracker.update(` and `.run_post_response_detectors(` in handlers. Add neither.
  - test_handle_submit.py:
    - `TestStorageOnce` ≈664–672 reads `call_args[0][-1]` of `_dispatch_storage`;
    - doc-gen ≈741–768; self-note ≈798–830.
  - test_request_path_parity.py and test_action_guard_integration.py both drive these functions with a `store_interaction` mock.
  - test_calendar_turn_round3.py ≈601–622 calls `_background_store_interaction` directly.
  - Parent grep: no existing unit test makes `store_interaction` raise at the three inline sites (confirm).

OWNERSHIP
- gui/handlers.py:
  - the three inline store try/except blocks and their `_build_debug_record(... extra=...)` arguments ONLY;
  - inside `_background_store_interaction`, the `except StoreWriteError` block and the `log_metadata` construction ONLY.
  - Do NOT change `_dispatch_storage`, its signature or any caller of it; what is passed to `store_interaction`; or any yielded chunk.
- utils/conversation_logger.py: inside `_log_text`, one line (or one `if` plus one line) that prints `storage_failed` when present ONLY.
- New tests/unit/test_handler_storage_receipts.py; new docs/execution/generalization/batches/F13b.md.
- Read-only: every other handlers function (including `_dispatch_storage`, `_write_turn_telemetry`, `_capture_delivery`, `_format_action_proposal_card`), core/orchestrator.py (including `_storage_failure_label`), gui/launch.py, api/**, web/**, memory/**, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. Label: reuse F13a's helper. Do not define the label format again.
   - Inside each except, `from core.orchestrator import _storage_failure_label` (a local import, following the 912 precedent), then `label = _storage_failure_label(e)`.
   - If that local import creates an import-cycle or test hazard you can show, STOP and report it rather than duplicating the format.
2. Each inline site:
   - `except Exception as e:` → label → one `logger.warning` (labels only, never `{e}`);
   - if `getattr(ctx, "telemetry", None)` is a dict, set `ctx.telemetry["storage_failed"] = label`;
   - the site's `_build_debug_record(...)` receives `extra={"storage_failed": label}` only when the store failed. On success, or when the store is skipped, the call is exactly as today (no `extra`, or `extra=None`).
   - The receipt code can never raise: a missing `telemetry` attribute or a non-dict is skipped.
3. No chat text changes: every yielded chunk's `content` and keys are byte-for-byte unchanged on failure, skip and success. The status message is F13c-2.
4. `_background_store_interaction`, on `StoreWriteError`:
   - keep the existing error log line text;
   - add `'storage_failed': label` to `log_metadata` ONLY on failure; the success metadata is byte-for-byte unchanged;
   - still exactly one `log_interaction` call.
   - The generic outer `except Exception` (207–208) is unchanged. It covers failures outside the store call, e.g. transcript logging.
5. `_log_text` prints the `storage_failed` label when present. No change to the JSON writer.
6. No change to `_dispatch_storage`, the turn-record path, or any other function. Record as limitations handed to F13c-1/F13c-2:
   - no status message yet on any path;
   - the background (main chat) failure does not reach the turn record or the already-delivered debug record.
7. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_handler_storage_receipts.py; fakes only)
- Use the smallest ctx each site needs: LOCAL copies of the fixture shapes from test_doc_conversation_source.py / test_sep07_calendar_offer_continuation.py / test_handle_submit.py; or import small helpers only if they are plain functions.
- AsyncMock stores, tmp telemetry and log paths, patched external managers.
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- Each inline site with `store_interaction` raising `StoreWriteError` (a marker chained in the underlying exception) must give:
  - exactly one final assistant chunk whose content equals what the success control yields for the same inputs;
  - `ctx.handled` True where applicable;
  - `ctx.telemetry["storage_failed"] == label`;
  - a debug record carrying the label;
  - one warning log with the label;
  - the marker absent everywhere: content, debug, telemetry, logs.
  - Also: a plain `RuntimeError(marker)` gives `"store_interaction: RuntimeError"`; a ctx lacking `telemetry` is still handled with no exception.
- Controls per site:
  - success → no key, same content;
  - `memory_system=None` → no key;
  - `store_interaction` returning None → no key.
  - Action retry: `pending_action_id` present in both the failure and success chunks.
- Background:
  - failure → one `log_interaction` call whose metadata has `db_id is None` and `storage_failed == label`, plus the existing log text;
  - success → metadata has no `storage_failed` key (control);
  - text format → a tmp `ConversationLogger(log_dir=tmp_path, log_format="text")` file shows the label when present and does not when absent.
- Focused (≤9 per chunk):
  - chunk 1: the new file, test_store_write_outcomes.py, test_doc_conversation_source.py, test_sep07_calendar_offer_continuation.py, test_handle_submit.py;
  - chunk 2: test_request_path_parity.py, test_action_guard_integration.py, test_section_outcome_receipts.py, test_calendar_turn_round3.py, test_orchestrator_storage_receipts.py.
- Sweep: every remaining tests/unit importer of gui.handlers / utils.conversation_logger (grep and list — expect a large set), in chunks of ≤9, each vetted BEFORE running (no Gradio build, no real stores, models, data or log paths).
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
  - tests/unit/test_daily_notes_auto_update.py is runnable since F12b.

SCAN: pre- and post-edit read-only scan.
- Baseline after F13a: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 645.
- Expect no dm18 change (dm17 files +1) and new = 0.
SIZE: target ≤350 changed lines; HARD stop at ≤450.
- Measure the test file before the source edit; never implement then revert; never trim to fit.
- If the estimate exceeds the cap, stop before editing and return a split proposal: inline sites vs background receipt plus `_log_text`.
ORDER: manifest → create F13b.md → pre-edit scan → tests → failing-first (digests in the same command) → edits → focused → sweep → ruff → scan → data/logs listing → packet.
