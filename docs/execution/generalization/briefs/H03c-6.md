=== H03c-6: replace a real course code in two dev-script gold-anchor tuples and a real registration-portal name in one production comment ===
(Parent brief, 2026-09-15. Owner decision R4, "Swap both" (briefs/PARENT_STATE.md). The parent's final repo-wide sweep found these as the only leftovers outside every earlier decision. The parent mapped the lines at 12:54:54: each real form occurs only on the line listed, case-insensitively, outside lane docs. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write any real form into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy. Never quote a grep pattern containing a real form in the packet; describe checks by label.
- Do not write ANY other real form you happen to see (the self-check cannot see words this batch does not remove).
- Labels:
  - V3 = a real course code (upper-case subject prefix plus number);
  - W = a real registration-portal name.
- Give `file:line` references. Never paste diffs or quote "before" text.
- Self-check (the parent's method; counts only):
  - removed-only capitalised and lowercase token sets from `diff $S/h03c6_pre/<path> <path>`, PLUS removed-only `\b[0-9]{3,}\b` and `\b[A-Za-z]{2,3}\b` sets;
  - grep them case-insensitively against batches/H03c-6.md and every edited file;
  - the packet must have 0 hits apart from explained infrastructure paths;
  - re-run after EVERY packet edit.

Request packets: none. Do NOT read any class-guard file, not even with grep: docs/BUG_CLASSES.md, docs/DEVELOPMENT_WORKFLOW.md, docs/TEST_LANES.md, scripts/check_bug_classes.py (run it only via the permitted scan command), scripts/bug_class_guards/**, config/bug_class_*.json, tests/bug_class_guards/**, hooks/**, .github/**, docs/execution/class_guards/**. BUG_CLASSES: none, so the BC-58 item is "N/A (privacy batch)".
Every repo-wide grep uses: `--exclude-dir=data --exclude-dir=.agent_snapshots --exclude-dir=node_modules --exclude-dir=logs --exclude-dir=.git --exclude-dir=hooks --exclude-dir=.github --exclude-dir=bug_class_guards --exclude-dir=class_guards --exclude=BUG_CLASSES.md --exclude=DEVELOPMENT_WORKFLOW.md --exclude=TEST_LANES.md --exclude=check_bug_classes.py --exclude='bug_class_*.json'`.
No response file.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (permitted `python` forms, IMPORTER GREPS, SIZE, EVIDENCE TIMESTAMPS).

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-6.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c6_pre/<same relative path>` and verify digests → usage reading (below) → baseline run (digests in the same command) → ALL edits → privacy self-check → post-edit run (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- **BASELINE BEFORE ANY EDIT:** your FIRST Edit call on ANY scope file must come after the baseline chunk's output is recorded in the packet. If you ever find you edited a scope file before the baseline, STOP immediately and report. Do NOT revert on your own.
- Take a data/ listing BEFORE the first pytest chunk as well as after it.
- A separate `main.py` process (outside this clone) may be running; leave it alone. If MemAvailable is below 4000 MB before a chunk, wait and re-check.
- **NEVER run either script** (`scripts/reflection_domain_clustering.py`, `scripts/reflection_validation_harness.py`) or any other file under scripts/. They may read data/ or call models. Verify them by reading plus ruff only.
- `python` only as `python -m pytest …` (capped systemd-run), `python -m ruff check …` / `--version`, `python scripts/check_bug_classes.py scan --root .`, and the one `utils` sanity check. No npm.
- Run only tests/unit files, with the guard and MemAvailable check before EVERY pytest invocation, ≤9 files per chunk.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-6.txt` and `$S/manifest_paths_pre_H03c-6.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline `$S/data_baseline_post_F12a.txt`; `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

MAPPING (parent decision; synthetic, already used elsewhere in the tree)
- V3 → "KTR 5520" (upper case, same single space).
- W → "RegPortal".
- Collision checks at launch (counts only): record the files already containing "KTR 5520" and "RegPortal". None of the 3 target files may already contain either.

SCOPE (digest prefixes re-verified by the parent)
- scripts/reflection_domain_clustering.py `99b8bca2…` (277 lines):47: the third string of one tuple in the `GOLD_ENTITY_ANCHORS` list (V3). Keep the other tuple elements and the trailing comment unchanged.
- scripts/reflection_validation_harness.py `d7ae1514…` (643 lines):100: the third string of one tuple in the `GOLD_ANCHORS` list (V3). Keep everything else unchanged.
- core/prompt/gatherer_knowledge.py `33889a95…` (2302 lines):346: a `#` comment (W). Comment text only.
- Before editing, grep the 3 files for each real form (case-insensitive, non-word-bounded) and reconcile: exactly one line each is expected. Record extras or misses by `file:line`.

USAGE READING (before the baseline; record in the packet, labels only)
- Read how `GOLD_ENTITY_ANCHORS` and `GOLD_ANCHORS` are used: grep scripts/ and tests/ for both names and for each module name, in all import forms. State whether the anchor strings are matched against stored memory text when a developer runs the script.
- If they are, record plainly: "after this swap, this one gold anchor will no longer match the owner's real data when the dev script is run; the app itself is unaffected." The parent will pass that note to the owner.
- If any non-script runtime code (core/, memory/, gui/, api/, utils/) imports either constant, STOP and report before editing.

OWNERSHIP: the 3 listed lines only; new batches/H03c-6.md. Read-only: everything else and every class-guard path.

CONTRACT
1. The 3 occurrences are replaced per the mapping; production edit is comment-only.
2. Identical per-file counts for the baseline test files.
3. Per-label real-form counts over the 3 edited files are 0 (case-insensitive); packet token hits 0.
4. No real form in any written file or report.

TESTS
- BASELINE FIRST (digests of the 3 scope files in the same command): tests/unit/test_gatherer_latency_guards.py and tests/unit/test_calendar_prompt.py (importers of core.prompt.gatherer_knowledge), plus any tests/unit file your usage grep finds importing either script.
- Post-edit identical counts. ruff on all 3 edited .py files.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: 3 changed lines; HARD stop 450.
ORDER: manifest → create H03c-6.md → pre-edit scan → collision checks → pre-copies → usage reading → baseline (digests in the same command) → ALL edits → privacy self-check → post-edit run → importer check → ruff → scan → data/logs/web → packet.
