=== F12d: synthesis reads raise on failure, so a failed similarity check never stores a duplicate and a failed result read never looks like "no results"; the shutdown synthesis audit check fails closed ===
(Parent brief, 2026-09-14. Drafted while F10a ran. Facts were re-verified on the post-F12a tree while F12b ran, and again on the post-F12b tree while F12c ran: all seven digests unchanged, and only the second main.py caller line moved. F12c changed only memory/memory_retriever.py, so nothing below moved. Launched after F12c was integrated. The owner decision of 2026-09-14 is folded in: the synthesis audit auto-halt becomes fail-closed.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-009: "Typed lists for the synthesis, retriever and `query_*` reads". Real-defects row: "Synthesis `find_similar` failure stores a duplicate instead of updating convergence" (`memory/synthesis_memory.py:99,107-115`).
- Amendment "F12 split (parent decision, 2026-09-14)":
  - F12d reads RAISE. Every consumer either flattens a typed list or already catches an exception in the fail-closed direction. The flattening consumers are `if similar:`, the list comprehensions and slices in `get_known_connections` / `get_ungraded` / `get_graded`, and `len(...)` in `get_stats`.
  - The shutdown audit check becomes fail-closed (owner decision).
- briefs/PARENT_STATE.md "Owner decisions received 2026-09-14", item 2.
Request packet (the ONLY class-guard file you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#141, #142, #143).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response file (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-8.md. With it, CGR-009 is fully answered.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL. That includes:
- SIZE: measure before editing later parts; never implement then revert; never trim to fit.
- SCANNER SHAPE: catch the TYPED failure for a contract-required degrade; never add a new broad except that returns; escalate a new gate finding.
- the exact permitted `python` command forms;
- EVIDENCE TIMESTAMPS from `date`.

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F12d.md.
- NEVER put `python` in a Bash command except: `python -m pytest …` (inside the capped systemd-run), `python -m ruff check …`, `python scripts/check_bug_classes.py scan --root .`, and the one `python -c "import utils; print(utils.__file__)"` sanity check. No other `python -c` (not even a no-op), heredoc, REPL or script.
- Take every packet timestamp from `date` output in the same command as the step — never estimate, never back-fill.
- PACKET ORDER (added after F12c): run the steps in the ORDER line below. Write packet sections in the order the steps ran, and give each section a title that matches when its step ran. The manifest check comes before the pre-edit scan. A data/ check recorded after a chunk is titled as after that chunk.
- Check each fixture against the gates it must pass: empty collection, similarity threshold, grading filters, and `SYNTHESIS_GENERATOR_ENABLED` / `SYNTHESIS_POOLED_ENABLED` / `SYNTHESIS_AUDIT_ENABLED`. `_run_synthesis_dreaming` imports those flags from `config.app_config` inside the method, so monkeypatch them on `config.app_config`.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F12c.txt` and `$S/manifest_paths_post_F12c.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules.
- The data/ baseline is `$S/data_baseline_post_F12a.txt`, unchanged by F12b and F12c. data/ holds `benchmark_per_case.csv`, `chroma_db_v4`, `embedding_migration_manifest.json` and `pipeline`.
- Re-check data/ after every chunk; ANY difference means STOP.
- Never import or build the Gradio synthesis tab in a test. Never run scripts/synthesis_validation.py or main.py.
ORDER REMINDER: print `sha256sum memory/synthesis_memory.py memory/shutdown_processor.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control, and list every edit. Map: tests/unit/test_synthesis_audit.py (`d25b3d54…`) has an in-file `FakeChromaStore` (74) and `test_get_all_results` (302). It has no failure-path assertions.

PARENT-VERIFIED FACTS (post-F12b tree, re-checked 2026-09-14; F12c did not touch these files; re-verify every line)
- Starting digests:
  - memory/synthesis_memory.py `32e445db…` (584 lines; no LLM calls);
  - memory/shutdown_processor.py `9aa08ee5…`;
  - read-only: knowledge/synthesis_filter.py `eb4de03a…`, gui/tabs/synthesis.py `4a341290…`, scripts/synthesis_validation.py `db206ba0…`, utils/retrieval_outcome.py `1abc1586…`, tests/unit/test_synthesis_audit.py `d25b3d54…`.
- memory/synthesis_memory.py (it does not import utils.retrieval_outcome yet; imports 33–42):
  - `find_similar` (def 61) → ANCHOR #141.
    - It calls `store.query_collection(...)`, which raises. `if not results: return []` (79). Then the threshold filter, and it returns sorted matches.
    - `except Exception as e:` (97) → `logger.error(...)` → `return []` (99).
  - `store_result` (def 101):
    - `similar = self.find_similar(result.candidate.connection_claim)` (107, no try);
    - `if similar:` (109) → `_update_convergence`;
    - otherwise it inserts via `self.store.add_to_collection(...)` (127). That insert's own `except Exception as e:` (134) logs and re-raises (136).
    - Today a failed similarity read takes the insert branch and stores a duplicate.
  - `get_recurring` (def 184) → ANCHOR #142.
    - It calls `query_collection(query_text="cross-domain connection", n_results=100)`. Empty → [] (193); otherwise it returns results.
    - `except Exception as e:` (206) → `return []` (208).
    - Only caller: `get_stats` (def 571; `converging = len(self.get_recurring())` 577). `get_stats` has its own `except Exception:` → a zeros dict (a sibling) and no production caller.
  - `get_all_results` (def 409) → ANCHOR #143.
    - It calls `query_collection(..., n_results=limit)`. Empty → [] (421); otherwise it returns results.
    - `except Exception as e:` (433) → `return []` (435).
    - Internal consumers, none with a try:
      - `get_known_connections` (def 335; a list comprehension over `self.get_all_results(limit=limit * 2)` 342–346);
      - `get_ungraded` (def 437; 450);
      - `get_graded` (def 465; 470);
      - `get_audit_stats` (def 501; `all_results = self.get_all_results(limit=500)` 514; with zeros it computes `auto_halt=False` at 553/564).
  - Siblings (record only): `_update_convergence` (`update_metadata` swallows; except → ""), `create_bridge_edge` (→ ""), `grade_result` (except 405 → False), `get_stats` (→ zeros).
- Production callers:
  - `find_similar`:
    - knowledge/synthesis_filter.py `_stage_4_novelty_internal` (def 565; `similar = self.memory.find_similar(...)` 571; `if not similar:` 573 → novel).
      - It runs inside `process_candidate`'s per-stage `except Exception as e:` (219) → `StageResult(stage_name=..., passed=False, reason=f"Stage error: {e}")` (220–224) → `result.reject(...)` → return.
      - So a raise REJECTS the candidate (fail-closed). `RetrievalError`'s str is labels only.
    - `store_result` is called at synthesis_filter.py 243 inside `except Exception as e: logger.error(f"Failed to store accepted synthesis result: {e}")` (250–251). A raise stores nothing and is logged.
    - scripts/load_discovery_batch_to_gui.py (try/print).
  - `get_all_results`, via its consumers:
    - gui/tabs/synthesis.py `_load_synth_stats` (`stats = sm.get_audit_stats()` 158) and `_load_synth_queue` (`get_ungraded` 192/194, `get_known_connections` 198, `get_graded` 200). These Gradio callbacks have NO try, so a raise surfaces as a Gradio error in that tab instead of "No items in this view." or stats "OK". Recorded, not changed.
    - scripts/synthesis_validation.py (`known = sm.get_known_connections(limit=limit)` 185, no try). A raise is a CLI traceback. Recorded, not changed.
  - memory/shutdown_processor.py `_run_synthesis_dreaming` (def 1678; public wrapper `run_synthesis_dreaming` def 1668):
    - Outer `try:` (1688), with the flags imported from `config.app_config` at 1689–1700 and `if not (SYNTHESIS_GENERATOR_ENABLED or SYNTHESIS_POOLED_ENABLED): return` (1704–1705).
    - `if SYNTHESIS_AUDIT_ENABLED:` (1708) → inner `try:` (1709): `from memory.synthesis_memory import SynthesisMemory` (1710); `audit_mem = SynthesisMemory(self.chroma_store)` (1711); `audit_stats = audit_mem.get_audit_stats()` (1712).
    - `if audit_stats.get("auto_halt", False):` (1713) → `logger.warning("[Shutdown] Synthesis HALTED by audit queue — …")` (1714–1722) → `return` (1723).
    - `except Exception as e:` (1724) → `logger.debug(f"[Shutdown] Audit check skipped: {e}")` (1725) → dreaming proceeds. This is FAIL-OPEN today.
    - Then `from knowledge.synthesis_filter import SynthesisFilter` (1727) and `from memory.synthesis_memory import SynthesisMemory` (1728), and the generators.
    - The method's outer `except Exception as e:` (1831) → `logger.warning("[Shutdown] Synthesis dreaming failed (non-fatal): %s", e)` (1832).
    - Callers of `run_synthesis_dreaming`: main.py 653 and 1728, inside `asyncio.wait_for` with their own excepts.
  - core/insight/* does not consume SynthesisMemory.
- tests/unit importers of memory.synthesis_memory / SynthesisMemory / knowledge.synthesis_filter / SynthesisFilter / memory.shutdown_processor / ShutdownProcessor (parent grep, post-F12b plus F12c's new file):
  - test_codex_followups.py
  - test_entity_facts.py
  - test_knowledge_graph.py
  - test_ordered_slice_guard.py
  - test_proposal_loop_outcomes.py
  - test_proposal_store_outcomes.py
  - test_retrospective_small_guards.py
  - test_sep09_storage_repairs.py
  - test_shutdown_pipeline_proposals.py
  - test_skill_and_summary_outcomes.py
  - test_source_excerpt_pipeline.py
  - test_stance_write_path.py
  - test_stream_artifacts.py
  - test_synthesis_audit.py
  - test_thread_outcomes.py
  - test_retriever_read_outcomes.py (F12c)
  - Re-grep at the start and vet each file.
  - test_codex_followups.py matches only through shutdown_processor, not SynthesisMemory.

OWNERSHIP
- memory/synthesis_memory.py: the bodies and docstrings of `find_similar`, `get_recurring` and `get_all_results` ONLY (plus one import line).
- memory/shutdown_processor.py: inside `_run_synthesis_dreaming`, the audit-check `try/except` block (1708–1725) ONLY (plus one local import if needed).
- New tests/unit/test_synthesis_read_outcomes.py; new docs/execution/generalization/batches/F12d.md; the response file.
- Read-only:
  - `store_result` (the raise from `find_similar` already prevents the duplicate — prove it) and every other synthesis method;
  - knowledge/synthesis_filter.py, gui/tabs/synthesis.py, scripts/**, main.py;
  - every other shutdown_processor region;
  - the store, utils/retrieval_outcome.py, config/**;
  - briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `find_similar` (#141), `get_recurring` (#142) and `get_all_results` (#143):
   - a genuine empty stays [] (unchanged);
   - the except keeps its log line, then raises `RetrievalError(source="synthesis_memory", reason=...) from e`;
   - the reasons are `f"find_similar:{type(e).__name__}"`, `f"recurring:{type(e).__name__}"` and `f"all_results:{type(e).__name__}"` respectively.
2. `store_result` is unchanged. With (1), it never inserts when the similarity read failed, because the raise leaves before the insert.
3. Shutdown audit check (owner decision, FAIL-CLOSED):
   - Replace the inner `except Exception as e: logger.debug(... Audit check skipped ...)` with `except RetrievalError as e:`. It logs one warning with labels only (e.g. "[Shutdown] Synthesis dreaming skipped: audit stats unavailable ({e.source}: {e.reason})") and then returns. This is the typed degrade.
   - Any OTHER exception in the audit check is no longer swallowed. It propagates to the method's existing outer `except Exception` (1831), which logs "Synthesis dreaming failed (non-fatal)" and ends the method. So dreaming never runs without a readable audit.
   - Do NOT add a new broad `except Exception` + `return`.
   - The auto-halt-true path, the disabled gates and the healthy path are unchanged.
4. No other caller changes. Record:
   - the synthesis filter rejects the candidate with a labelled stage error;
   - the GUI synthesis tab shows a Gradio error instead of an empty view;
   - scripts/synthesis_validation.py prints a traceback on a read failure.
5. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_synthesis_read_outcomes.py; fakes only — a LOCAL copy of test_synthesis_audit.py's `FakeChromaStore` with a raising mode; `ShutdownProcessor.__new__` or a constructor call with `user_profile=None` and MagicMocks, with patched synthesis classes)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- Each of the three reads:
  - `query_collection` raising → `RetrievalError` with its reason prefix;
  - empty collection → [] (control);
  - healthy → results (control).
- `store_result`:
  - a raising similarity read → it raises, and `add_to_collection` is NEVER called (no duplicate);
  - a healthy no-match → inserted once (control);
  - a healthy match → convergence updated, no insert (control).
- Through the deployed filter: `SynthesisFilter` stage 4 with a raising `find_similar`.
  - Read its constructor and `_stage_4_novelty_internal` / `process_candidate` first, and use a fake memory.
  - Expect: the candidate does not pass, and the stage reason carries labels only.
  - If the filter cannot be built without a model or a real store, record read-only evidence instead.
- `get_known_connections` / `get_ungraded` / `get_graded` / `get_audit_stats` with a raising `get_all_results` → the `RetrievalError` propagates. This is evidence that no consumer turns it back into an empty view.
- Shutdown audit check. Read 1678–1832 first.
  - Setup: monkeypatch the synthesis config flags on `config.app_config`. Patch `memory.synthesis_memory.SynthesisMemory` and the generator and filter classes at the names the method imports, so nothing real is built and every generator is a spy.
  - `get_audit_stats` raising `RetrievalError` → no generator constructed or called, and the labelled warning is logged.
  - `SynthesisMemory(...)` construction raising a plain `RuntimeError` → no generator called, and the outer "Synthesis dreaming failed" warning is logged.
  - `auto_halt` True → halted as today (control).
  - `auto_halt` False → generators called (control).
  - `SYNTHESIS_AUDIT_ENABLED` False → proceeds without an audit read (control).
- Privacy: markers never appear in any NEW error str/reason or new warning. Pre-existing log lines kept unchanged may carry `{e}`; do not assert on those.
- Focused (≤9 per chunk): the new file, test_synthesis_audit.py, test_codex_followups.py (after vetting), test_retrieval_outcome.py, test_shutdown_pipeline_proposals.py, test_ordered_slice_guard.py.
- Sweep: every remaining importer from the list above, in chunks of ≤9, each vetted BEFORE running.
  - Never build the Gradio tab.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
  - tests/unit/test_daily_notes_auto_update.py is runnable since F12b, but it is not an importer here.

SCAN: pre- and post-edit read-only scan.
- Baseline after F12c: dm18 found 26 / base 79 / new 0 / stale 53; dm17 files 642.
- Expect the dm18 rows for #141–#143 to go STALE, with new = 0.
RESPONSE FILE (`CGR-20260913-009-8.md`) covers:
- #141–#143 with deployed-function evidence;
- the duplicate-store proof;
- the filter, GUI and script consequences;
- the fail-closed audit check (owner decision 2026-09-14) with its tests;
- the siblings, listed;
- CGR-009 fully answered (base and `-2` … `-8`).
- Describe each caller's catch precisely (which caller catches, which is a pass-through); do not generalize.
SIZE: target ≤350 changed lines; HARD stop at ≤450.
- Measure before editing later parts; never implement then revert; never trim to fit.
- Split proposal if needed: synthesis reads plus the store_result proof vs the shutdown audit check.
ORDER: manifest → create F12d.md → pre-edit scan → tests → failing-first (digests in the same command) → edits → focused → sweep → ruff → scan → data/logs listing → packet → response file.
