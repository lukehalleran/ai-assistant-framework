=== F13a: the orchestrator records a labelled `storage_failed` receipt instead of silently passing on a failed turn write ===
(Parent brief, 2026-09-14. Drafted while F10b ran from a read-only map. Re-verified on the post-F12b tree and again on the post-F12d-2 tree: core/orchestrator.py `e336883d…`, 2646 lines, and every anchor below unchanged. Launched after F12d-2 was integrated.)

READ FIRST — the process rules recent batches broke:
- Your FIRST source write comes only AFTER the new test file exists AND the failing-first run (digests printed in the same command) is recorded in batches/F13a.md.
- Never implement then revert. Measure size before each later part.

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-010: "Callers record a failed write (turn-record receipt) instead of `except: pass`."
- Real-defects row: "Write failures and deliberate skips both return None; 'CRITICAL … data loss' logging never fires; five `except: pass` sites".
- Amendment "F13 split and receipt carriers (parent decision, 2026-09-14)": F13a = the four orchestrator store sites; no chat text on any path.
- F6b is the receipt precedent (`sections_not_checked` in `_last_turn_signals` and `debug_info`, labels only).
Request packets: none. CGR-010's anchors are all answered, and these sites are design items, not scanner anchors. Do NOT read any class-guard file. No response file; evidence in batches/F13a.md.
BUG_CLASSES: BC-20, BC-47, CM-05.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- SIZE;
- SCANNER SHAPE;
- the exact permitted `python` forms;
- EVIDENCE TIMESTAMPS.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create F13a.md → pre-edit scan → write the test file → failing-first (digests in the same command) → source edit → focused → sweep → ruff → scan → data/logs listing → packet.
- NEVER put `python` in a Bash command except:
  - `python -m pytest …` (inside the capped systemd-run);
  - `python -m ruff check …`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `python -c "import utils; print(utils.__file__)"` sanity check.
  - No other `python -c`, heredoc, REPL or script.
- Take every packet timestamp from `date` output in the same command as the step. Write sections in run order, with the `date` output in each header; never type a time by hand.
- Check each fixture against the gates it must pass: `use_raw_mode`, `use_agentic_search`, the deictic guard, `self.memory_system`, and `orch.logger = None`.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F12d-2.txt` and `$S/manifest_paths_post_F12d-2.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`. Re-check data/ after every chunk; ANY difference means STOP.
- Turn telemetry must go to tmp paths in every test: the autouse sandbox in tests/conftest.py (≈166–168) or `_patch_telemetry_path` from test_section_outcome_receipts.py. Never write `logs/turn_records.jsonl`.
- Never build a real ModelManager, Chroma store or embedder. Never run main.py.
ORDER REMINDER: print `sha256sum core/orchestrator.py utils/turn_telemetry.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit.

PARENT-VERIFIED FACTS (post-F12d-2 tree, 2026-09-14; re-verify every line)
- Starting digests:
  - core/orchestrator.py `e336883d…` (2646 lines);
  - read-only: utils/turn_telemetry.py `c4897764…`, tests/unit/test_process_user_query.py `87b04c5c…`, tests/unit/test_section_outcome_receipts.py `18255684…`, tests/unit/test_request_path_parity.py `cfbbadef…`, tests/unit/test_turn_telemetry.py `93b8c789…`, tests/conftest.py `14333cc1…`.
- `utils.retrieval_outcome.StoreWriteError(source=, reason=)` exists (F10a). `memory_system.store_interaction` raises it on a failed write; None is the deliberate skip.
- Reachability:
  - No production caller passes `use_agentic_search=True`.
  - The GUI and API call `process_user_query(..., use_raw_mode=True)` (gui/handlers.py `_run_raw` ≈1421), which skips all four sites.
  - The only other production caller is the main.py CLI (defaults).
  - So these four sites are reached by the CLI (`_store_interaction`, `_handle_deictic`) and by tests only. Record this in the packet.
- The four sites (`flow` = `_QueryFlow`, class at 267; `flow.debug_info` built ≈1943):
  - `_handle_deictic` (def 2007):
    - store ≈2018–2020 (`tags=["clarification"]` 2019);
    - `except Exception as e:` ≈2021 → `logger.warning(f"[Orchestrator] Failed to store clarification interaction: {e}")` 2022;
    - inline before `return` ≈2029. This is an early return: no post-response hooks, no turn record.
  - `_maybe_document_generation` (def 2119):
    - store ≈2181–2185 (`tags=["document_generation"]` 2184); `except Exception:` 2186 / `pass` 2187;
    - then `debug_info.update` ≈2189 and `return` ≈2198 (an early return: no hooks, no turn record).
    - The block is inside the outer try ≈2138 / except ≈2200. A raise from receipt code would take the "Document generation failed, falling back" path and produce a second answer.
  - `_maybe_agentic_search` (def 2206):
    - store ≈2313–2317 (`tags=["agentic_search"]` 2316); `except Exception:` 2318 / `pass` 2319;
    - then `debug_info.update` ≈2321 and `return` ≈2337;
    - the same outer-try hazard (≈2339).
  - `_store_interaction` (def 2482):
    - store ≈2490–2495; `except Exception as e:` ≈2496 → `self.logger.error(f"[Orchestrator] CRITICAL: Failed to store interaction - data loss: {e}")` 2497;
    - called inline at ≈1974, before `_run_post_response_hooks` (≈1979; builds `PostResponseHookContext(..., telemetry={}, ...)` with `telemetry={},` at 2521) and `_finalize_debug` (≈1980, ≈2613–2646).
- Turn record: `_hook_turn_telemetry` (def 344) merges `_last_turn_signals`, then `ctx.telemetry` (348), then query/mode/session/model, and calls `utils/turn_telemetry.py:record_turn` (def 112).
  - `record_turn` writes EVERY key of the record; there is no whitelist.
  - So utils/turn_telemetry.py needs NO edit and is read-only. The draft's conditional ownership of it is dropped.
- `self.logger = get_logger("orchestrator")` is always set in `__init__` (491). BUT tests/unit/test_process_user_query.py sets `orch.logger = None` in its fixtures (87 and 402), so a failing store in `_store_interaction` would raise `AttributeError` at 2497 and escape `process_user_query`.
- F6b hazards:
  - tests/unit/test_section_outcome_receipts.py `test_agentic_bypass_site_mirrors_finalize_debug` (139) needs `debug_info["section_outcomes"]` within 200 characters of `debug_info["task_timings"]` (2333–2334). Insert nothing between those lines.
  - tests/unit/test_process_user_query.py (≈212–215) pins `set(debug["phase_timings"].keys())`. Never put the receipt into `phase_timings`.
- Tests driving these paths: test_process_user_query.py (deictic ≈158–171, enhanced ≈201–210, doc-gen ≈290–303, agentic ≈324–343), test_request_path_parity.py, test_section_outcome_receipts.py, test_turn_telemetry.py.
- Future coupling (read-only here): F13c-1 will extend `_hook_turn_telemetry`'s deferred key filter to `storage_*`. Do not touch `_hook_turn_telemetry` in this batch.

OWNERSHIP
- core/orchestrator.py, ONLY:
  - the four store try/except blocks named above;
  - the `telemetry={}` literal in `_run_post_response_hooks`'s `PostResponseHookContext(...)` construction;
  - one new `_QueryFlow` field (e.g. `storage_failed: str = ""`);
  - one import line;
  - optionally one small module-level label helper (e.g. `_storage_failure_label(exc) -> str`) placed next to the other module helpers.
- New tests/unit/test_orchestrator_storage_receipts.py; new docs/execution/generalization/batches/F13a.md.
- Read-only: every other orchestrator region (including the `section_outcomes`/`task_timings` lines, `_hook_turn_telemetry` and `_last_turn_signals` construction), utils/turn_telemetry.py, gui/**, api/**, web/**, memory/**, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. A receipt label is `"<source>: <reason>"` from a `StoreWriteError`, or `"store_interaction: <exception class name>"` for any other exception. Never exception text.
2. `_maybe_document_generation` and `_maybe_agentic_search`: the `except Exception: pass` becomes `except Exception as e:` → compute the label → one warning log (labels only) → `debug_info["storage_failed"] = label`. The receipt code can never raise (no second answer). Success leaves no `storage_failed` key.
3. `_handle_deictic`: keep the warning but switch it to the label instead of `{e}`, and set `debug_info["storage_failed"] = label`.
4. `_store_interaction`:
   - keep the CRITICAL error line, with the label instead of `{e}`;
   - log it through `self.logger` when that is set and through the module logger otherwise, so a None logger never raises;
   - set `flow.storage_failed = label` and `flow.debug_info["storage_failed"] = label`;
   - `_run_post_response_hooks` passes `telemetry={"storage_failed": flow.storage_failed}` only when it is set, and otherwise `{}` exactly as today.
5. No user-visible change (owner decision 4, revised): the returned answer text, the number of answers and the returned structure are unchanged on failure. No turn record exists on the deictic, doc-gen or agentic early returns today; do not add one (record as a limitation).
6. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_orchestrator_storage_receipts.py; fakes only — `DaemonOrchestrator.__new__` as in test_process_user_query.py `_make_flow_orch`, AsyncMock memory_system, tmp turn-telemetry path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED source. Record the failures.
- For each of the four sites, `store_interaction` raising `StoreWriteError(source="store_interaction", reason="RuntimeError")` with a marker in any underlying message. Expect:
  - the turn still returns its normal answer, exactly one answer;
  - `debug_info["storage_failed"] == "store_interaction: RuntimeError"`;
  - the marker absent from the new log lines and from debug.
  - A plain `RuntimeError(marker)` gives the label `"store_interaction: RuntimeError"`.
- `_store_interaction` with `orch.logger = None` and a failing store → no `AttributeError`; the turn-record row (tmp path) carries `storage_failed`.
- Controls:
  - success on each site → no `storage_failed` key;
  - success on `_store_interaction` → no `storage_failed` key in the turn row, and the hook context's telemetry is `{}`;
  - a `store_interaction` returning None (skip) → no key.
  - The F6b proximity guard and the `phase_timings` key set still pass (run those files).
- Focused (≤9 per chunk): the new file, test_process_user_query.py, test_request_path_parity.py, test_section_outcome_receipts.py, test_turn_telemetry.py, test_store_write_outcomes.py, test_orchestrator_helpers.py.
- Sweep: every remaining tests/unit importer of core.orchestrator / DaemonOrchestrator / PostResponseHookContext (grep and list), in chunks of ≤9, each vetted for real stores, models and data paths BEFORE running.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
  - tests/unit/test_daily_notes_auto_update.py is runnable since F12b.

SCAN: pre- and post-edit read-only scan.
- Baseline after F12d-2: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 644.
- Expect no dm18 change (dm17 files +1) and new = 0.
SIZE: target ≤300 changed lines; HARD stop at ≤450.
- Measure the test file before the source edit; never implement then revert; never trim to fit.
- If the estimate exceeds the cap, stop BEFORE any source edit and return a split proposal: `_store_interaction` plus the hooks telemetry vs the three early-return sites.
ORDER: manifest → create F13a.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet.
