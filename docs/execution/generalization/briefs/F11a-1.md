=== F11a-1: thread reads report failure instead of an empty list; a failed thread write raises; the shutdown thread pass never re-stores duplicates after a failed open-threads read ===
(Parent brief, 2026-09-14. Drafted from a read-only map while F9b ran; launched after F10b was integrated. Facts were re-verified on the post-F10b tree. The design-doc "F11a split" amendment was written at the same time.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-009: "Raise on failure (not-found stays None/[]): … `ThreadStore.list_open_threads`"; "the shutdown thread pass skips thread extraction when open threads cannot be read"; "Typed lists for the synthesis, retriever and `query_*` reads". CGR-010: "A failure raises; None remains the documented deliberate skip."
- Real-defects row: "Shutdown thread pass: an empty open-thread list (failed read) skips resolution and de-dup, so duplicate threads are stored".
- Batch row F11a ("Thread writes + shutdown thread pass"); amendment "[map] More sequential seams": shutdown_processor.py F11a regions (now ≈1549 and ≈1635).
- Amendment "F11a split" (parent decision, 2026-09-14): F11a-1 = the four anchors plus the shutdown pass; F11a-2 = the delete-then-store staged replacement in `resolve_thread` / `_update_thread`.
Request packets (the ONLY class-guard files you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#121, #147, #148) and /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-010.md (#146).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response files (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-4.md (#121, #147, #148) and docs/execution/generalization/class_guard_responses/CGR-20260913-010-3.md (#146).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL — note the 2026-09-14 clarifications: SIZE (never trim comments, docstrings or assertions to fit; stop and propose a split) and SCANNER SHAPE (never restructure code only to stop a scanner matching; escalate).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F11a-1.md.
- No non-pytest Python of any kind; the exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (`THREAD_SURFACING_ENABLED`, `_ensure_collection`, ≥2 session items, the model-manager guard in `_process_open_threads`).
- Record the guard and MemAvailable check before EVERY pytest invocation, including verification re-runs.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F10b.txt` and `$S/manifest_paths_post_F10b.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_f7a_rerun.txt` (7 entries; `logs/` absent). Re-check after every chunk and STOP on any change. Record every chunk's exact file list in the packet (not only counts).
ORDER REMINDER: print `sha256sum memory/thread_store.py memory/memory_coordinator.py memory/shutdown_processor.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. Known: tests/unit/test_thread_store.py `test_returns_none_when_add_raises` (def 189; `add_to_collection` raising → asserts `store_thread(...) is None`) must become a `StoreWriteError` assertion; its paired control (`chroma_store=None` → None, ≈184–187) already exists. List every edit.

PARENT-VERIFIED FACTS (read-only map, re-verified on the post-F10b tree, 2026-09-14)
- Starting digests: memory/thread_store.py `79233e7e…`, memory/memory_coordinator.py `726b1d2a…`, memory/shutdown_processor.py `eb091a08…`, utils/retrieval_outcome.py `1abc1586…` (read-only), core/prompt/gatherer_knowledge.py `00870a5b…` (read-only).
- utils/retrieval_outcome.py: `RetrievalError`, `OutcomeList`, `outcome_status`, `StoreWriteError(source=, reason=)` (F10a).
- Store wrapper (memory/storage/multi_collection_chroma_store.py): `list_all` and `add_to_collection` have NO try and already raise (`add_to_collection` assigns a fresh `uuid4` doc id per write); `query_collection` raises `ValueError` for an unknown collection.
- memory/thread_store.py (488 lines):
  - `_ensure_collection` (def 206): returns False when `chroma_store` is falsy, `collections is None`, no `create_collection`, or `create_collection` raises (`except Exception` ≈219–221 logs an error) — the last is a failure disguised as "not configured": BC-58 sibling, record only.
  - `store_thread` (def 227): skip `if not self._ensure_collection(): logger.warning(...); return None` (≈237–239); success `return doc_id` from `add_to_collection` (≈253); `except Exception as e: logger.error(f"[ThreadStore] Failed to store thread: {e}")` (256) → `return None` (≈257) → ANCHOR #146.
  - `list_open_threads` (def 259): skip → [] (≈261–262); per-item parse `except Exception: continue` (≈272–273, a malformed-row skip: keep, record); success `return threads` (≈274); `except Exception as e: logger.error(...list_open_threads failed...)` (276) → `return []` (≈277) → ANCHOR #147.
  - `query_threads` (def 330): skip → [] (≈341–342); `if coll is None or coll.count() == 0: return []` (≈346–347); per-match skips (≈358–359, ≈362–363); success `return threads[:n_results]` (≈365); `except Exception as e: logger.error(f"[ThreadStore] Query failed: {e}")` (368) → `return []` (≈369) → ANCHOR #148. NO production caller (tests only).
  - Siblings (record; `resolve_thread` def 371 / `_update_thread` def 461 are F11a-2's): `get_top_threads` (def 279, no try; `open_threads = self.list_open_threads()`; `if not open_threads: return []`) — a raise passes through with no code change; `enforce_cap` (def 425, no try; calls `list_open_threads()`); `touch_thread` (def 408).
- memory/memory_coordinator.py:
  - quick thread resolution inside `store_interaction` (≈385–398): `open_threads = self.thread_store.list_open_threads()` (390) inside a try whose `except Exception as e: logger.debug(f"[MemoryCoordinator] Quick thread resolution failed (non-fatal): {e}")` (398) — a raise is swallowed at debug and quick resolution is skipped; acceptable, unchanged. (F10a's try/finally sync-back sits above it; read-only.)
  - `get_unresolved_threads` (def 589): skip `if not self.thread_store: return []` (≈597–598); `threads = self.thread_store.get_top_threads(max_results=max_results)` (600) → `return [t.to_dict() for t in threads]` (≈601); `except Exception as e: logger.debug(f"[MemoryCoordinator] get_unresolved_threads failed: {e}")` (603) → `return []` (≈604) → ANCHOR #121.
  - Its ONLY production caller: core/prompt/gatherer_knowledge.py `get_unresolved_threads` (def 1278; flag/hasattr guards → []; `threads = self.memory_coordinator.get_unresolved_threads(max_results=max_results)` (1297); `return threads or []`; `except Exception as e: logger.warning(...)` → `return OutcomeList.failed(type(e).__name__)`). So a RAISE from the coordinator is recorded by the builder as `failed` with reason `RetrievalError` (the class name) with no gatherer change; an `OutcomeList.failed` return would be flattened by `threads or []`. Hence the coordinator must RAISE.
- memory/shutdown_processor.py `_process_open_threads` (def 1494):
  - early returns: flag off, no thread_store, no `generate_once`, corpus errors, `len(sess_items) < 2` (read ≈1500–1545 for exact lines);
  - read: `existing_open = []` (1549) / `try: existing_open = self.thread_store.list_open_threads()` (1551) / `except Exception as e: logger.warning(f"[Shutdown] Listing open threads failed: {e}")` (1553), then the pass CONTINUES: resolution is skipped, extraction runs without the open list (`asyncio.gather(_detect_resolutions(), extractor.extract_new_threads(sess_items, open_threads=existing_open), return_exceptions=True)`, 1562), de-dup/`touch_thread` see nothing, and every extracted thread is stored again → duplicates;
  - write loop: `doc_id = self.thread_store.store_thread(thread)` (1635) / `if doc_id: stored += 1` (1637, no try); Phase 3 `try: self.thread_store.enforce_cap()` (1643) / `except Exception as e: logger.debug(...)`;
  - caller: `process_shutdown_memory` Phase B `asyncio.gather(..., self._process_open_threads(session_conversations), ..., return_exceptions=True)` (≈287–297; the call at 290) → `_log_shutdown_llm_failure("generation", r)` (def 84, an LLM-failure label).
  - LLM calls: memory/thread_extractor.py `extract_new_threads` and `detect_resolutions` via `model_manager.generate_once`; both catch their own errors and return []. Tests must stub them (monkeypatch the extractor methods or stub `generate_once`); never a real model.
  - F10b edited `_extract_procedural_skills` (≈1161–1172) above this region; read-only for you.
- No api/ or gui/ code lists or resolves threads: a raise cannot reach a request handler.
- Existing tests: tests/unit/test_thread_store.py (825 lines; `MockChromaStore`/`MockCollection` fakes ≈30–75); test_gatherer_outcomes_remaining_knowledge.py (F7c; coordinator faked; unaffected); test_thread_context_and_resolution.py; test_thread_extractor.py; test_ordered_slice_guard.py (anchors `ShutdownProcessor._gather_proposal_context` only; run it after editing shutdown_processor.py). No unit test covers `_process_open_threads` or coordinator `get_unresolved_threads`.

OWNERSHIP
- memory/thread_store.py: the bodies and docstrings of `store_thread`, `list_open_threads` and `query_threads` ONLY, plus one import line.
- memory/memory_coordinator.py: the body and docstring of `get_unresolved_threads` ONLY.
- memory/shutdown_processor.py: inside `_process_open_threads`, the open-threads read block (≈1549–1553) and the `store_thread` write loop plus its count log (≈1635–1640) ONLY.
- tests/unit/test_thread_store.py: the one FIXTURE RULE edit named above.
- New tests/unit/test_thread_outcomes.py; new docs/execution/generalization/batches/F11a-1.md; the two response files.
- Read-only: `resolve_thread`, `_update_thread`, `touch_thread`, `enforce_cap`, `get_top_threads`, `_ensure_collection`, every other coordinator and shutdown_processor region (F10a/F10b/F11b/F12), core/prompt/gatherer_knowledge.py, memory/thread_extractor.py, utils/retrieval_outcome.py, the store, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `store_thread` (#146): the skip returns None as today; the except keeps its log line, then `raise StoreWriteError(source="thread_store", reason=type(e).__name__) from e`; success returns the doc id. Docstring updated.
2. `list_open_threads` (#147): the skip → [] and the per-item parse skip are unchanged; the outer except keeps its log line, then `raise RetrievalError(source="thread_store", reason=f"list_open:{type(e).__name__}") from e`; genuine empty → [].
3. `query_threads` (#148, no production caller): the `_ensure_collection` skip → [] unchanged; `coll is None` → `OutcomeList.unavailable("collection_missing")`; `count() == 0` → [] unchanged; the except keeps its log line, then `return OutcomeList.failed(type(e).__name__)`; success unchanged. Docstring says callers read the status before slicing.
4. `MemoryCoordinator.get_unresolved_threads` (#121): the `not self.thread_store` skip → [] unchanged; the swallow is removed so a failure propagates — a `RetrievalError` (e.g. from `list_open_threads` through `get_top_threads`) propagates unchanged; any other exception keeps the log line, then `raise RetrievalError(source="unresolved_threads", reason=type(e).__name__) from e`; success unchanged. Evidence: the gatherer then records `failed` with no gatherer change.
5. `_process_open_threads`:
   - the open-threads read catches the TYPED failure: `except RetrievalError as e:` → keep the warning line (its `{e}` is labels only) → `return` BEFORE resolution, extraction and any store — no duplicates. Do not use a broad `except Exception` for this degrade. If any gate scanner still reports a NEW finding on your edit, STOP and escalate (R_common_rules SCANNER SHAPE) — do not restructure around it;
   - a genuinely empty open list → the pass runs as today;
   - the write loop: a `StoreWriteError` from one thread is counted (`failed += 1`) and the loop continues; `stored` counts only confirmed ids; the count log reports stored and failed; Phase 3 `enforce_cap` still runs;
   - no other change to the pass.
6. Unchanged: the coordinator's quick-resolution block, `get_top_threads`, `enforce_cap`, `resolve_thread`, `_update_thread`, the gatherer.
7. Privacy: labels and class names only in every new error, reason and log.

TESTS (tests/unit/test_thread_outcomes.py; fakes only; no real Chroma, embedder, model or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- ThreadStore (reuse the MockChromaStore shape from test_thread_store.py as a LOCAL copy): `add_to_collection` raising → `StoreWriteError(source="thread_store")`; `chroma_store=None` → None (control); `list_all` raising → `RetrievalError(reason="list_open:<class>")`; empty collection → [] (control); a malformed item is skipped (control); `query_collection` raising → `OutcomeList.failed(<class>)` equal to []; `coll is None` → `unavailable("collection_missing")`; count 0 → [] (control); healthy query → threads.
- `get_top_threads` through a raising `list_open_threads` → propagates (evidence for no code change).
- Coordinator: `MemoryCoordinator.__new__` with a fake `thread_store` whose `get_top_threads` raises `RetrievalError` → propagates unchanged; raising `ValueError` → `RetrievalError(source="unresolved_threads", reason="ValueError")`; `thread_store=None` → []; healthy → dicts.
- Through the gatherer (deployed `KnowledgeRetrievalMixin.get_unresolved_threads` on a bare host, as test_gatherer_outcomes_remaining_knowledge.py does, but with the REAL coordinator method bound to a fake thread store that raises): `outcome_status(result) == ("failed", "RetrievalError")`, result == [].
- Shutdown pass (`ShutdownProcessor.__new__`; patch `THREAD_SURFACING_ENABLED`; ≥2 session conversations; stub the extractor/model): `list_open_threads` raising `RetrievalError` → no extraction call, no `store_thread` call, no exception; genuinely empty open list → extraction runs and new threads are stored (control); `store_thread` raising `StoreWriteError` for the first of two new threads → the second is still stored, `enforce_cap` still called, no exception. Read ≈1494–1650 first to build the minimal host; if a path cannot be driven without a real model or store, record it as a limitation.
- Privacy: markers in injected exception messages never appear in any new error str/reason or in the new log lines (caplog) — pre-existing log lines that the contract keeps unchanged may still carry `{e}`; do not assert on those.
- Focused (≤9 per chunk): the new file, test_thread_store.py, test_gatherer_outcomes_remaining_knowledge.py, test_thread_context_and_resolution.py, test_thread_extractor.py, test_ordered_slice_guard.py, test_retrieval_outcome.py, test_store_write_outcomes.py (F10a), test_skill_and_summary_outcomes.py (F10b).
- Sweep: every remaining tests/unit importer of memory.thread_store / ThreadStore / memory.memory_coordinator / MemoryCoordinator / memory.shutdown_processor / ShutdownProcessor (grep and list), chunks of ≤9, vetted for real stores, default paths and real embedders BEFORE running; usual exclusions (test_graph_integration.py, test_daily_notes_auto_update.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file).

SCAN: pre- and post-edit read-only scan. Baseline after F10b: dm18 found 42 / base 79 / new 0 / stale 37. Expect the dm18 rows for #121, #146, #147 and #148 to go STALE, new = 0.
RESPONSE FILES: `CGR-20260913-009-4.md` (#121, #147, #148 with deployed-function evidence; the shutdown pass skip; the gatherer evidence; siblings `_ensure_collection`, `get_top_threads`, `enforce_cap`, the per-item parse skip; remaining CGR-009 anchors → F11b-1 #132–#134, #136 (`-5`); F12a–F12d). `CGR-20260913-010-3.md` (#146; the shutdown write loop; the FIXTURE RULE edit; the delete-then-store siblings scheduled for F11a-2; remaining CGR-010 anchors → F11b-1 #131 (`-4`); F13 receipts).
SIZE: target ≤380 changed lines; HARD stop at ≤450 (stop BEFORE exceeding it and return a split proposal: thread_store + coordinator vs shutdown pass; never trim to fit).
ORDER: manifest → create F11a-1.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → both response files.
