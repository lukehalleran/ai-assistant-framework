=== F11b-1b: proposal batch loops count a failed write and keep going; check-proposals reports a proposal read failure cleanly (completes F11b-1's deferred Part B) ===
(Parent brief, 2026-09-14. F11b-1 delivered the ProposalStore anchors but deferred these three caller edits for size; until this batch lands, a failed proposal write aborts the rest of its loop (logged) and `check-proposals` shows "Startup failed" plus a traceback. Launched immediately after F11b-1 was integrated.)

Design source: docs/execution/generalization/failure_outcome_design.md, "Owner decisions and the F11b split (2026-09-14)" (F11b-1b paragraph). briefs/F11b-1.md CONTRACT 5–7 (the original contract for these edits). docs/execution/generalization/class_guard_responses/CGR-20260913-010-4.md, section "Batch-loop continuations: drafted, verified, then DEFERRED" (the drafted edits and tests — use them as a reference, but prove them independently: your failing-first run must be your own).
Request packet (the ONLY class-guard file you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-010.md (context for #131; no new anchor).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response file (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-010-5.md (the #131 caller completion). The check-proposals CLI change is an owner-decision UX fix with no anchor: record it in the batch packet only.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE — measure before editing later parts; never implement then revert; never trim to fit — and SCANNER SHAPE).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F11b-1b.md.
- No non-pytest Python of any kind — not even `python -c` for list slicing; build chunk lists with grep/sed or by hand. The exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (`CODE_PROPOSALS_ENABLED`, `generate_once`, ≥3 session items, the similarity check before each store, the ingest de-dup `seen` set).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F11b-1.txt` and `$S/manifest_paths_post_F11b-1.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_post_F11b-1.txt`. Re-check after every chunk. The OWNER has approved deleting three F2-era data/ leftovers (data/chroma_multi, data/web_search_credits.json, data/user_profile.json) and may run that cleanup while you work: if a data/ check shows ONLY those three entries missing (plus a changed `.` directory mtime line), record it as the owner's cleanup and continue; any other difference means STOP and report. Editing main.py (at the repository root) changes the `..` line of the data/ listing; that line alone is not a data/ change — record it. NEVER run main.py, `agent_branch/run.py live_proposal_store()`, or the portfolio demo CLI — they open the real `CHROMA_PATH`.
ORDER REMINDER: print `sha256sum memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit.

PARENT-VERIFIED FACTS (post-F11b-1 tree, 2026-09-14; confirm the digests at the manifest check)
- Starting digests: memory/shutdown_processor.py `74c9ed30…`, agent_branch/proposal_bridge.py `3c7c23a2…`, main.py `6ef42c51…`; read-only: memory/proposal_store.py `f9262df1…` (F11b-1), agent_branch/portfolio.py `436b0be5…`.
- After F11b-1: `ProposalStore.store_proposal` raises `StoreWriteError(source="proposal_store", reason=<class>)` on a failed write (None only for the no-chroma skip); `get_proposal` and `get_pending_and_approved` raise `RetrievalError(source="proposal_store", reason="get:<class>" / "pending_approved:<class>")`; not-found stays None and a genuine empty stays []. See batches/F11b-1.md's parent section.
- memory/shutdown_processor.py `_generate_proposals` (def 1178): gates `CODE_PROPOSALS_ENABLED`, `generate_once`, ≥3 session items; outer try; `GoalDirectedGenerator(...)`; `get_for_dedup()`; LLM calls `generate_proposals_with_context` with a cold fallback `_generate_proposals_cold` (def 1269); loop: `existing_id = proposal_store.check_similarity(proposal); if existing_id: continue` (1255), `doc_id = proposal_store.store_proposal(proposal); if doc_id: kept += 1` (1259–1261), `if kept: logger.info(...)` (≈1263; read its exact text); `except Exception as e: logger.warning(f"[Shutdown] Proposal generation failed: {e}")` (1267). Read ≈1178–1268 before editing. The F10b skill loop (≈1161–1172) and the F11a-1 thread loop (≈1640–1650) already use the same typed-catch shape — follow it.
- agent_branch/proposal_bridge.py `ingest_survivors` (def 209, no local try): `doc_id = store.store_proposal(proposal)` (242); `if doc_id: seen.add(sig)` (245); `stored.append(proposal.id)` (246). Only caller: agent_branch/portfolio.py (`ids = ingest_survivors(objective, reports, portfolio, proposal_store)` 106 inside a try whose except logs "portfolio: proposal ingest failed" 109). Read the whole function (≈209–251) and its inputs (the report/portfolio types) before designing the test; tests/agent_branch/test_proposal_bridge.py (top-level, NEVER run it) shows an established `_report()` fixture shape you may read and copy locally.
- main.py `elif mode == "check-proposals":` (1530): builds `MultiCollectionChromaStore(persist_directory=CHROMA_PATH)` and `ProposalStore(...)`, then `if specific_id: proposal = store.get_proposal(specific_id)` (1564) → `if not proposal: print(f"Proposal {specific_id} not found."); sys.exit(1)` … `else: proposals = store.get_pending_and_approved()` (1570); `if not proposals: print("No pending/approved proposals to check."); sys.exit(0)`. No local try.
- Existing tests: tests/unit/test_shutdown_pipeline_proposals.py (patches `knowledge.proposal_generator.GoalDirectedGenerator` and `memory.proposal_store.ProposalStore`; `store_proposal.return_value` "doc-123"…), tests/unit/test_proposal_store_outcomes.py (F11b-1), tests/unit/test_ordered_slice_guard.py (anchors `ShutdownProcessor._gather_proposal_context`; run it after editing shutdown_processor.py).

OWNERSHIP
- memory/shutdown_processor.py: inside `_generate_proposals`, the `store_proposal` call, the `kept`/`failed` counters and the count log line (≈1255–1264) ONLY.
- agent_branch/proposal_bridge.py: inside `ingest_survivors`, the `store_proposal` call and the lines that use its result (≈242–246) ONLY, plus one import line.
- main.py: inside `check-proposals`, the two read sites (≈1563–1573) ONLY, plus one local import if needed.
- New tests/unit/test_proposal_loop_outcomes.py; new docs/execution/generalization/batches/F11b-1b.md; the response file.
- Read-only: every other region of those three files, memory/proposal_store.py, agent_branch/portfolio.py, gui/**, core/**, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `_generate_proposals`: a `failed = 0` counter next to `kept`; `try: doc_id = proposal_store.store_proposal(proposal)` / `except StoreWriteError: failed += 1; continue` (TYPED); `kept` counts only confirmed ids; the count log fires when `kept or failed` and reports both (labels/counts only); any other exception keeps today's path (the outer warning). A similarity match is still skipped before any store and is never counted as failed.
2. `ingest_survivors`: `try: doc_id = store.store_proposal(proposal)` / `except StoreWriteError as e:` → one warning with labels only (e.g. the survivor's branch id and `e.reason`) → `continue`; a failed survivor is added to neither `seen` nor `stored`; the return shape is unchanged.
3. main.py `check-proposals`: wrap exactly the two reads so `except RetrievalError as e:` → `print(f"Could not read proposals: {e}")` (labels only) → `sys.exit(2)`; not-found (exit 1) and "No pending/approved proposals" (exit 0) are unchanged.
4. No other change. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only in every new log line and CLI message.

TESTS (tests/unit/test_proposal_loop_outcomes.py; fakes only; no real Chroma, embedder, model, filesystem scan or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- Shutdown `_generate_proposals` (patch `GoalDirectedGenerator` and `ProposalStore` as test_shutdown_pipeline_proposals.py does; stub the LLM calls): two proposals, `store_proposal` raising `StoreWriteError` for the first → both attempted, the second stored, no exception, the count log shows 1 kept / 1 failed; control: a similarity match is skipped and not counted as failed; control: all healthy → 2 kept, 0 failed.
- `ingest_survivors`: with a local fake store whose `store_proposal` raises `StoreWriteError` for the first of two survivors → both attempted, exactly the second survivor's id returned; control: all healthy → both ids returned. If the function cannot run without a real store, model or filesystem, record that as a limitation and test what is reachable.
- main.py `check-proposals`: verify by reading only (it opens the real `CHROMA_PATH` store); record the exact final lines in the packet and the limitation (no automated test for the CLI message; the underlying raises are covered by F11b-1's store tests).
- Privacy: markers in injected exception messages never appear in the new log lines (caplog); pre-existing log lines kept unchanged may still carry `{e}` — do not assert on those.
- Focused (≤9 per chunk): the new file, test_shutdown_pipeline_proposals.py, test_proposal_store_outcomes.py, test_proposal_store.py, test_ordered_slice_guard.py, test_skill_and_summary_outcomes.py, test_thread_outcomes.py, test_retrieval_outcome.py, test_store_write_outcomes.py.
- Sweep: every remaining tests/unit importer of memory.shutdown_processor / ShutdownProcessor / agent_branch.proposal_bridge (grep and list; `\bProposalStore\b`-only matches that are really `PendingProposalStore` are not importers), chunks of ≤9, vetted BEFORE running; usual exclusions (test_graph_integration.py, test_daily_notes_auto_update.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file including tests/agent_branch/*).

SCAN: pre- and post-edit read-only scan. Baseline after F11b-1: dm18 found 33 / base 79 / new 0 / stale 46. Expect no dm18 change and new = 0.
RESPONSE FILE (`CGR-20260913-010-5.md`): the #131 caller completion — the shutdown loop and `ingest_survivors` per-item degrade with deployed-function evidence; that F11b-1's interim loop-abort window is closed; remaining CGR-010 items → F13a/F13b/F13c receipts.
SIZE: target ≤250 changed lines; HARD stop at ≤450 (measure BEFORE editing later parts; never implement then revert; never trim to fit).
ORDER: manifest → create F11b-1b.md → pre-edit scan → tests → failing-first (digests in the same command) → edits → focused → sweep → ruff → scan → data/logs listing → packet → response file.
