=== F12c: retriever reads for skills, recent facts and the reflection metadata fallback raise on failure instead of returning an empty list ===
(Parent brief, 2026-09-14. Drafted while F10a ran; facts re-verified on the post-F12a tree while F12b ran (F12b touched none of the files below); launched after F12b was integrated.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-009: "Typed lists for the synthesis, retriever and `query_*` reads". Amendment "F12 split (parent decision, 2026-09-14)" records the parent decision for F12c: these three reads RAISE, like F11a-1 and F11b-1, because their consumers flatten a typed list (`gatherer_knowledge.get_procedural_skills` does `return skills or []`; `get_reflections_hybrid` merges into a new list), while a raise already reaches an existing catch.
Request packet (the ONLY class-guard file you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#123, #125, #126).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response file (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-7.md.
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE — measure before editing later parts; never implement then revert; never trim to fit — SCANNER SHAPE — catch the TYPED failure for a contract-required degrade; escalate a new gate finding — the exact permitted `python` command forms, and EVIDENCE TIMESTAMPS from `date`).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F12c.md.
- NEVER put `python` in a Bash command except: `python -m pytest …` (inside the capped systemd-run), `python -m ruff check …`, `python scripts/check_bug_classes.py scan --root .`, and the one `python -c "import utils; print(utils.__file__)"` sanity check. No other `python -c` (not even a no-op), heredoc, REPL or script.
- Take every packet timestamp from `date` output in the same command as the step — never estimate, never back-fill.
- Check each fixture against the gates it must pass (`PROCEDURAL_SKILLS_ENABLED`, empty collection, no-content-words query, missing reflections collection).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F12b.txt` and `$S/manifest_paths_post_F12b.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_post_F12a.txt` (unchanged by F12b; data/ holds `benchmark_per_case.csv`, `chroma_db_v4`, `embedding_migration_manifest.json`, `pipeline`). Re-check after every chunk; ANY difference means STOP. Never build a real embedder or cross-encoder (several retriever tests patch `_cross_encoder`; keep that pattern).
ORDER REMINDER: print `sha256sum memory/memory_retriever.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit. (Map: no unit test asserts [] on these failure paths; tests/unit/test_ordered_slice_guard.py anchors `MemoryRetriever._keyword_anchor_memories` and `_maybe_cross_encoder_rerank` slices and `gatherer_memory._get_reflections_hybrid_filtered` — do not touch those methods.)

PARENT-VERIFIED FACTS (post-F12a tree, re-checked after F12b, 2026-09-14; re-verify every line)
- Starting digests: memory/memory_retriever.py `6a2c7ab8…` (2023 lines); read-only memory/memory_coordinator.py `d1787423…`, core/prompt/gatherer_knowledge.py `00870a5b…`, core/prompt/gatherer_memory.py `b994315c…`, memory/shutdown_processor.py `9aa08ee5…`, utils/retrieval_outcome.py `1abc1586…`.
- Store wrapper after F9a/F9b: `get_recent`, `list_all`, `query_collection`, `add_to_collection` have no try and raise; `get_by_id`, `get_ids_by_timestamp_range` raise `RetrievalError`; `search_all`, `query_multiple_collections`, `update_metadata`, `get_collection_stats` still swallow.
- memory/memory_retriever.py:
  - `_metadata_fallback_search` (module-level def 344): skips → [] (no query/store; no content words); `if not coll: return []` (371, collection missing — a sibling "unavailable", record only); raw `coll.query(...)`; `return out`; `except Exception as e: logger.debug(f"[ReflectionMetadataFallback] Failed: {e}")` (407) → `return []` → ANCHOR #123. Sole caller: `MemoryRetriever.get_reflections_hybrid` (def 849; `metadata_results = _metadata_fallback_search(` 901, no try; iterates and merges). Its own semantic leg swallows (`except Exception: pass` ≈878–896) — a sibling, record only.
    - Callers of `get_reflections_hybrid`: memory/memory_coordinator.py pass-through (418/423); memory/shutdown_processor.py `_gather_proposal_context` (def 1302; `"reflections": mc.get_reflections_hybrid(synthetic_query, limit=2)` 1326, gathered by the per-task `try: results[key] = await task` (1357) / `except Exception as e: logger.debug(...); results[key] = []` (≈1358–1360)); scripts/sample_real_benchmark*.py. Not on the prompt path.
  - `get_recent_facts` (def 640): `recent = self.chroma_store.get_recent("facts", limit=limit)` (≈643); `return recent or []`; `except Exception as e: logger.debug(...); return []` → ANCHOR #125. Only caller: memory/memory_coordinator.py `get_recent_facts` (323/328), whose only caller is core/prompt/gatherer_memory.py `get_recent_facts` (145/149, catches), which has no production caller (F8a evidence).
  - `get_skills` (async def 1107): `if not PROCEDURAL_SKILLS_ENABLED:` → [] (1124, skip); `if coll is None or coll.count() == 0: return []`; `get_recent` / `query_collection`; an in-memory metadata bump `except Exception: pass` (≈1164–1175, keep); `return merged[:limit]`; `except Exception as e: logger.warning(f"[MemoryRetriever] Failed to retrieve skills: {e}")` (1184) → `return []` → ANCHOR #126.
    - Callers: memory/memory_coordinator.py `get_skills` (582/587, pass-through, no try) → core/prompt/gatherer_knowledge.py `get_procedural_skills` (def 1173; `skills = await self.memory_coordinator.get_skills(query, limit=limit)` 1194; `return skills or []` 1210; its except → `OutcomeList.failed(type(e).__name__)`), so a raise is recorded by the builder as `failed` with reason `RetrievalError` with no gatherer change; memory/shutdown_processor.py `_gather_proposal_context` (`"skills": mc.get_skills(synthetic_query, limit=3)` 1324, the same per-task catch → []).
  - Other except → []/None siblings (record only): `_keyword_anchor_memories`, `get_facts`, `get_reflections`, `get_summaries`, `get_summaries_hybrid`, `search_by_type`, `_fallback_semantic_search`, `_gate_memories`, `_maybe_cross_encoder_rerank`, `get_semantic_top_memories`.
- 15 tests/unit files reference memory.memory_retriever / MemoryRetriever / `_metadata_fallback_search` (grep at the start; vet each).

OWNERSHIP
- memory/memory_retriever.py: the bodies and docstrings of `_metadata_fallback_search`, `get_recent_facts` and `get_skills` ONLY (plus one import line).
- New tests/unit/test_retriever_read_outcomes.py; new docs/execution/generalization/batches/F12c.md; the response file.
- Read-only: every other retriever method (including `get_reflections_hybrid` and its slices), memory/memory_coordinator.py, core/prompt/gatherer_knowledge.py, core/prompt/gatherer_memory.py, memory/shutdown_processor.py, scripts/**, the store, utils/retrieval_outcome.py, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `_metadata_fallback_search` (#123): the skips and the missing-collection [] are unchanged; the except keeps its log line, then `raise RetrievalError(source="reflection_metadata_fallback", reason=type(e).__name__) from e`. `get_reflections_hybrid` is unchanged, so the raise propagates to its callers (shutdown's per-task catch → [], as today but logged at debug).
2. `get_recent_facts` (#125): the except keeps its log line, then `raise RetrievalError(source="recent_facts", reason=type(e).__name__) from e`; a genuine empty → [].
3. `get_skills` (#126): the disabled skip and the empty collection are unchanged; the outer except keeps its log line, then `raise RetrievalError(source="procedural_skills", reason=type(e).__name__) from e`; the inner metadata-bump swallow is unchanged.
4. No caller changes (record the gatherer and shutdown evidence).
5. If any gate scanner reports a NEW finding, STOP and escalate (SCANNER SHAPE). Privacy: labels and class names only.

TESTS (tests/unit/test_retriever_read_outcomes.py; fakes only — `MemoryRetriever.__new__` with the attributes each method reads, MagicMock stores, no embedder/cross-encoder)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED source. Record the failures.
- `_metadata_fallback_search`: `coll.query` raising → `RetrievalError(source="reflection_metadata_fallback")`; no query / no content words / missing collection → [] (controls); healthy → results (control). Read the function first to satisfy its content-word gate.
- `get_recent_facts`: `get_recent` raising → `RetrievalError(source="recent_facts")`; empty → [] (control).
- `get_skills`: `query_collection` raising → `RetrievalError(source="procedural_skills")`; disabled → [] and empty collection → [] (controls); healthy → skills (control).
- Through the deployed coordinator and gatherer: the real `get_procedural_skills` on a bare gatherer host whose coordinator delegates to a retriever whose `get_skills` raises → `outcome_status(result) == ("failed", "RetrievalError")`, result == [].
- Shutdown evidence: `_gather_proposal_context` (read ≈1302–1450 first; `ShutdownProcessor.__new__`) with a coordinator whose `get_skills` / `get_reflections_hybrid` raise → no exception, those sections absent; if it cannot be driven with fakes, record read-only evidence.
- Privacy: markers never appear in any NEW error str/reason; pre-existing log lines kept unchanged may carry `{e}` — do not assert on those.
- Focused (≤9 per chunk): the new file, test_gatherer_outcomes_remaining_knowledge.py, test_shutdown_pipeline_proposals.py, test_health_transient_retrieval.py, test_keyword_anchor_retrieval.py, test_recent_summaries_fix.py, test_reflection_retrieval.py, test_ordered_slice_guard.py, test_skill_activation.py.
- Sweep: every remaining tests/unit importer of memory.memory_retriever / MemoryRetriever / _metadata_fallback_search (grep and list), chunks of ≤9, vetted for real embedders, cross-encoders, stores and data paths BEFORE running; usual exclusions (test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file).

SCAN: pre- and post-edit read-only scan. Baseline after F12b: dm18 found 29 / base 79 / new 0 / stale 50. Expect the dm18 rows for #123, #125 and #126 to go STALE, new = 0.
RESPONSE FILE (`CGR-20260913-009-7.md`): #123, #125, #126 with deployed-function evidence; why these reads raise instead of returning typed lists (the flattening consumers); the gatherer and shutdown evidence; `get_recent_facts`'s off-path callers; siblings listed; remaining CGR-009 anchors → F12d (#141–#143; `-8`).
SIZE: target ≤300 changed lines; HARD stop at ≤450 (measure before editing later parts; never implement then revert; never trim to fit).
ORDER: manifest → create F12c.md → pre-edit scan → tests → failing-first (digest in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → response file.
