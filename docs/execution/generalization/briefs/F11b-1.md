=== F11b-1: proposal reads raise on a failed store read (not-found stays None, genuine empty stays []); a failed proposal write raises; batch loops count a failed write and keep going; the check-proposals CLI reports a read failure cleanly ===
(Parent brief, 2026-09-14. Drafted from a read-only map while F9b ran; facts re-verified on the post-F11a-1 tree and re-checked after F10c (F10c changed only main.py:469, which does not move the check-proposals lines); launched after F10c was integrated. Owner decision 2026-09-14 folded in: fix the `main.py check-proposals` message. The design-doc "F11b split" amendment was written at the same time.)

Design source: docs/execution/generalization/failure_outcome_design.md
- "Decisions per request" → CGR-009: "Raise on failure (not-found stays None/[]): … `ProposalStore.get_proposal` …". CGR-010: "A failure raises; None remains the documented deliberate skip."
- Batch row F11b ("Proposal reads/writes + consumers (after F11a; shared shutdown_processor)"); amendment "[map] More sequential seams": shutdown_processor.py F11b regions (now ≈1259 and ≈1472).
- Amendment "F11b split" (parent decision, 2026-09-14): F11b-1 = the five anchors plus their batch-loop callers and the check-proposals CLI; F11b-2 = the `update_status` staged replacement ("[verified] Delete-then-store loses the record") and the `update_tracking_metadata` success-on-failure sibling.
- briefs/PARENT_STATE.md "Owner decisions received 2026-09-14", item 3.
Request packets (the ONLY class-guard files you may read): /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-009.md (#132, #133, #134, #136) and /home/lukeh/daemon_exec/class_guards/docs/execution/class_guards/requests/CGR-20260913-010.md (#131).
BUG_CLASSES: BC-20, BC-47, CM-05.
Response files (immutable once written): docs/execution/generalization/class_guard_responses/CGR-20260913-009-5.md (#132, #133, #134, #136) and docs/execution/generalization/class_guard_responses/CGR-20260913-010-4.md (#131).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL (including SIZE — never trim to fit — and SCANNER SHAPE — escalate a new gate finding, never restructure around it).

COMPLIANCE (read twice):
- Your first source write comes only after the failing-first run is recorded in batches/F11b-1.md.
- No non-pytest Python of any kind — not even `python -c` for list slicing; build chunk lists with grep/sed or by hand. The exceptions are ruff, the read-only scan and the one sanity check.
- Check each fixture against the gates it must pass (`_ensure_collection`, `CODE_PROPOSALS_ENABLED`, ≥3 session items, `IMPL_TRACKING_*` flags, the similarity check before each store).
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_F10c.txt` and `$S/manifest_paths_post_F10c.txt`.
GUARD, MEMORY, DATA NOTE: as in R_common_rules. data/ baseline: `$S/data_baseline_post_F10c.txt`. Re-check after every chunk. The OWNER has approved deleting three F2-era data/ leftovers (data/chroma_multi, data/web_search_credits.json, data/user_profile.json) and may run that cleanup while you work: if a data/ check shows ONLY those three entries missing (plus a changed `.` mtime line), record it as the owner's cleanup and continue; any other difference means STOP and report. NEVER call `agent_branch/run.py` `live_proposal_store()`, the portfolio demo CLI, or main.py — they open the real `CHROMA_PATH`.
ORDER REMINDER: print `sha256sum memory/proposal_store.py memory/shutdown_processor.py agent_branch/proposal_bridge.py main.py` in the SAME command that first runs your new tests, BEFORE any source edit.
FIXTURE RULE (S01 precedent): repair an existing test only with the new assertion plus a paired control. List every edit. (Map: no unit test asserts a failure-path value; the skip, genuine-empty and not-found tests in tests/unit/test_proposal_store.py must pass unedited.)

PARENT-VERIFIED FACTS (re-verified on the post-F11a-1 tree and re-checked after F10c, 2026-09-14; confirm the digests at the manifest check)
- Starting digests: memory/proposal_store.py `f26ba6a5…` (449 lines), memory/shutdown_processor.py `74c9ed30…`, agent_branch/proposal_bridge.py `3c7c23a2…`, main.py `6ef42c51…` (F10c's state), core/prompt/proposal_filter.py `e54d1c2b…` (read-only), core/prompt/gatherer_knowledge.py `00870a5b…` (read-only), gui/tabs/proposals.py `219a71fd…` (read-only), agent_branch/portfolio.py `436b0be5…` (read-only).
- utils/retrieval_outcome.py: `RetrievalError`, `OutcomeList`, `outcome_status`, `StoreWriteError` (F10a).
- Store wrapper: `add_to_collection` assigns a fresh `uuid4` doc id per write and has no try; `list_all` and `query_collection` raise; `update_metadata` returns False on failure.
- memory/proposal_store.py:
  - `_ensure_collection` (def 52): False when `chroma_store` is None (the deliberate skip), no `collections`, `create_collection` raises (`except Exception` ≈65–67, a failure disguised as the skip — BC-58 sibling, record only), or no `create_collection`.
  - `store_proposal` (def 73): skip → None (≈83–85); `doc_id = self.chroma_store.add_to_collection(...)` (≈91–93) → `return doc_id` (≈99); `except Exception as e: logger.error(...)` (101) → `return None` (103) → ANCHOR #131.
  - `query_proposals` (def 105): skip → [] (≈123–124); `coll = self.chroma_store.collections.get(...)` (≈130), `if coll is None or coll.count() == 0: return []` (≈131–132); `query_collection(...)` (≈134–138); items without `proposal_id` skipped (≈143–144); per-item `from_metadata` failure `except Exception: logger.debug; continue` (148–150, a malformed-row skip); status post-filter (≈152–153); `return proposals` (≈159); `except Exception as e: logger.error(f"... Query failed: {e}")` (161) → `return []` (163) → ANCHOR #132.
  - `get_proposal` (def 165): skip → None (≈172–173); `list_all` (≈176); first metadata match → `CodeProposal.from_metadata(meta)` (≈180); not found → None (181); `except ...` (182) → `return None` (184) → ANCHOR #133 (a corrupt matching record also lands here and looks like not-found).
  - `get_pending` (def 186): skip → [] (≈188–189); `list_all` (≈192); per-item `except Exception: continue` (199–200); `return proposals` (≈201); `except ...` (202) → `return []` (204) → ANCHOR #134. NO production caller (every production `get_pending()` belongs to the pending-ACTION store, `core/actions/types.py`).
  - `get_pending_and_approved` (def 394): skip → [] (≈396–397); `list_all` (≈400); per-item `continue` (408–409); return (≈410); `except ...` (411) → `return []` (413) → ANCHOR #136.
  - Siblings (record; F11b-2 owns `update_status` (def 206) and `update_tracking_metadata` (def 350)): `check_similarity` (def 276, `except ...` 346 → `return None` 348, where None also means "no duplicate" → fails open); `get_for_dedup` (def 415, `except ...` 447 → `return ""`).
- Callers and the effect of a raise:
  - `query_proposals` → core/prompt/proposal_filter.py `ProposalFilter.get_proposals` (def 610; `store.query_proposals(...)` 661, NOT inside a try; `if not proposals: logger.info("[ProposalFilter] No proposals found in store ..."); return []` ≈667–669) → core/prompt/gatherer_knowledge.py `get_proposed_features` (def 1118; `proposals = await self._proposal_filter.get_proposals(query, limit=limit)` 1150; `return proposals or []` 1167; its except → `OutcomeList.failed(type(e).__name__)` ≈1169–1171) → the builder records `failed` with reason `RetrievalError`. A raise needs NO filter or gatherer change; an `OutcomeList.failed` return would be flattened to `[]` by the filter. Hence RAISE.
  - `store_proposal`:
    - gui/tabs/proposals.py `_generate_proposals_now` (208 `if store.store_proposal(p): kept += 1`, inside a try whose except returns "Generation failed: {e}"): a raise is caught and shown (labels only); the stored-so-far count is lost from the message. Read-only; record.
    - agent_branch/proposal_bridge.py `ingest_survivors` (def 209, no local try; `doc_id = store.store_proposal(proposal)` 242; `if doc_id: seen.add(sig)` 245; `stored.append(proposal.id)` 246); only caller agent_branch/portfolio.py (`ids = ingest_survivors(...)` 106, inside a try whose except logs "portfolio: proposal ingest failed" 109): a raise stops the remaining survivors.
    - memory/shutdown_processor.py `_generate_proposals` (def 1178): gates `CODE_PROPOSALS_ENABLED`, `generate_once`, ≥3 session items; outer try; `GoalDirectedGenerator(...)`; `get_for_dedup()`; LLM calls `generate_proposals_with_context` with a cold fallback `_generate_proposals_cold` (def 1269); loop: `existing_id = proposal_store.check_similarity(proposal); if existing_id: continue` (1255), `doc_id = proposal_store.store_proposal(proposal); if doc_id: kept += 1` (1259–1261), `if kept: logger.info` (≈1263); `except Exception as e: logger.warning(f"[Shutdown] Proposal generation failed: {e}")` (1267): a raise stops the remaining proposals. Read ≈1178–1268 before editing.
  - `get_proposal`: gui/tabs/proposals.py 275, 316, 429 (each `if not proposal: return "... not found"`, each inside a try whose except returns a failure message) — a raise is shown as a failure, not "not found"; read-only.
  - `get_pending_and_approved`: gui/tabs/proposals.py 228 (`if not proposals: return "No pending/approved proposals to check."`, except → failure message; read-only); memory/shutdown_processor.py `_check_implementation_tracking` (def 1451; `proposals = store.get_pending_and_approved(); if not proposals: return` ≈1472–1474; inside a try → `except Exception as e: logger.warning(f"[Shutdown] Implementation tracking failed: {e}")` 1488) — a raise is logged as a failure instead of silently returning; no change.
  - main.py `elif mode == "check-proposals":` (≈1530): builds `MultiCollectionChromaStore(persist_directory=CHROMA_PATH)` and `ProposalStore(...)`, then `if specific_id: proposal = store.get_proposal(specific_id)` (1564) → `if not proposal: print(f"Proposal {specific_id} not found."); sys.exit(1)` … `else: proposals = store.get_pending_and_approved()` (1570); `if not proposals: print("No pending/approved proposals to check."); sys.exit(0)`. No local try: after this batch both reads raise `RetrievalError`, which would escape to the outer `except Exception as e: logger.error(f"Startup failed: {e}")` + traceback and then the `finally:` shutdown block. OWNER DECISION: fix it (CONTRACT 7).
  - `update_status` (F11b-2): gui/tabs/proposals.py 396, 445.
  - Phase B `asyncio.gather(..., return_exceptions=True)` → `_log_shutdown_llm_failure` — not reached, because both shutdown functions keep their own outer excepts.
- No api/ route uses ProposalStore.
- Existing tests in tests/unit referencing these modules: test_proposal_store.py (`MockChromaStore`/`MockChromaCollection` with uuid ids, delete, list_all, query_collection), test_proposal_filter.py (MagicMock store, tmp_path goals file), test_shutdown_pipeline_proposals.py (patches `knowledge.proposal_generator.GoalDirectedGenerator` and `memory.proposal_store.ProposalStore`), test_gatherer_outcomes_remaining_knowledge.py (F7c), test_proposal_risk.py (path string only). test_pending_proposal.py, test_action_guard_integration.py, test_sep07_calendar_offer_continuation.py and test_sep10_referee_followups.py use `PendingProposalStore` (a different store). test_ordered_slice_guard.py anchors `ShutdownProcessor._gather_proposal_context` — run it after editing shutdown_processor.py. Top-level tests/agent_branch/* are never run.

OWNERSHIP
- memory/proposal_store.py: the bodies and docstrings of `store_proposal`, `query_proposals`, `get_proposal`, `get_pending` and `get_pending_and_approved` ONLY, plus one import line.
- memory/shutdown_processor.py: inside `_generate_proposals`, the `store_proposal` call, the `kept` count and its log line (≈1259–1264) ONLY.
- agent_branch/proposal_bridge.py: inside `ingest_survivors`, the `store_proposal` call and the lines that use its result (≈242–246) ONLY.
- main.py: inside `check-proposals`, the two read sites (≈1563–1573) ONLY, plus one local import if needed.
- New tests/unit/test_proposal_store_outcomes.py; new docs/execution/generalization/batches/F11b-1.md; the two response files.
- Read-only: every other proposal_store method (F11b-2 or recorded siblings), gui/tabs/proposals.py, every other part of main.py, core/prompt/proposal_filter.py, core/prompt/gatherer_knowledge.py, agent_branch/portfolio.py, every other shutdown_processor region (including `_check_implementation_tracking`), utils/retrieval_outcome.py, the store, config/**, briefs/**, LANDING_NOTES.md, failure_outcome_design.md, and every class-guard path.

CONTRACT
1. `store_proposal` (#131): skip → None unchanged; the except keeps its log line, then `raise StoreWriteError(source="proposal_store", reason=type(e).__name__) from e`; success returns the doc id.
2. `query_proposals` (#132): skip → [], genuine empty (`count() == 0`) → [], the per-item skips and the status post-filter are unchanged; the outer except keeps its log line, then `raise RetrievalError(source="proposal_store", reason=f"query:{type(e).__name__}") from e`. The `coll is None` → [] branch is unchanged (record it as a sibling).
3. `get_proposal` (#133): skip → None and not-found → None unchanged; a failed read OR a corrupt matching record → keep the log line, then `raise RetrievalError(source="proposal_store", reason=f"get:{type(e).__name__}") from e`.
4. `get_pending` (#134) and `get_pending_and_approved` (#136): skip → [], genuine empty → [], the per-item skip unchanged; the outer except keeps its log line, then `raise RetrievalError(source="proposal_store", reason=f"pending:{type(e).__name__}") from e` (reason prefix `pending_approved:` for #136).
5. `_generate_proposals` loop: catch the TYPED `StoreWriteError` from one proposal → `failed += 1` → continue; `kept` counts only confirmed ids; the log reports kept and failed; any other exception keeps today's path (the outer warning).
6. `ingest_survivors`: catch the TYPED `StoreWriteError` from one survivor → log (labels only) → continue with the next; a failed survivor is neither added to `seen` nor to `stored`; the return shape is unchanged.
7. main.py `check-proposals` (owner decision): wrap exactly the two reads so `except RetrievalError as e:` → `print(f"Could not read proposals: {e}")` (labels only) → `sys.exit(2)`; not-found (exit 1) and "No pending/approved proposals" (exit 0) are unchanged.
8. No change to the filter, gatherer, GUI or `_check_implementation_tracking` (record each as read-only evidence).
9. If any gate scanner reports a NEW finding on your edit, STOP and escalate (SCANNER SHAPE).
10. Privacy: labels and class names only in every new error, reason, log line and CLI message.

TESTS (tests/unit/test_proposal_store_outcomes.py; fakes only; no real Chroma, embedder, model, filesystem scan or data path)
- FAILING FIRST in one command: the digest line above, then the new tests on the UNEDITED sources. Record the failures.
- Store (a LOCAL copy of test_proposal_store.py's `MockChromaStore` shape, able to raise on add / list_all / query on demand): `store_proposal` raising add → `StoreWriteError(source="proposal_store")`, no chroma → None (control); `query_proposals` raising query → `RetrievalError(reason="query:<class>")`, empty collection → [] (control), no chroma → [] (control); `get_proposal` raising list_all → `RetrievalError(reason="get:<class>")`, a corrupt matching record → `RetrievalError`, not found → None (control), found → the proposal (control); `get_pending` and `get_pending_and_approved` raising list_all → `RetrievalError` with their prefixes, empty → [] (control).
- Through the deployed filter and gatherer: the real `ProposalFilter.get_proposals` (constructed as in test_proposal_filter.py, tmp_path goals file) over a store whose `query_proposals` raises, driven via `KnowledgeRetrievalMixin.get_proposed_features` on a bare host → `outcome_status(result) == ("failed", "RetrievalError")`, result == [].
- Shutdown `_generate_proposals` (patch `GoalDirectedGenerator` and `ProposalStore` as test_shutdown_pipeline_proposals.py does; stub the LLM calls): two proposals, `store_proposal` raising `StoreWriteError` for the first → the second is still stored, no exception, log shows kept 1 / failed 1; control: a similarity match is skipped, not counted as failed.
- `_check_implementation_tracking` evidence (patch `knowledge.implementation_detector.ImplementationDetector`; enable the flags): `get_pending_and_approved` raising `RetrievalError` → the failure warning is logged and the detector is never constructed.
- `ingest_survivors`: read its signature first; with a fake store raising `StoreWriteError` for the first of two survivors → the second is stored and returned, the first is absent from the result. If the function cannot run without a real store, model or filesystem, record that as a limitation and test what is reachable.
- main.py `check-proposals`: this CLI branch opens the real `CHROMA_PATH` store and cannot be driven in a unit test — NEVER run main.py. Verify by reading, record the exact lines in the packet, and state the limitation (no automated test for the CLI message; the underlying raise is covered by the store tests).
- Privacy: markers in injected exception messages never appear in any NEW error str/reason or in new log lines (caplog); pre-existing log lines the contract keeps unchanged may still carry `{e}` — do not assert on those.
- Focused (≤9 per chunk): the new file, test_proposal_store.py, test_proposal_filter.py, test_shutdown_pipeline_proposals.py, test_gatherer_outcomes_remaining_knowledge.py, test_ordered_slice_guard.py, test_retrieval_outcome.py, test_store_write_outcomes.py (F10a), test_thread_outcomes.py (F11a-1).
- Sweep: every remaining tests/unit importer of memory.proposal_store / ProposalStore / core.prompt.proposal_filter / ProposalFilter / agent_branch.proposal_bridge / memory.shutdown_processor / ShutdownProcessor (grep and list), chunks of ≤9, vetted for real stores, default paths and real embedders BEFORE running (the `PendingProposalStore` tests are a different store and are not importers); usual exclusions (test_graph_integration.py, test_daily_notes_auto_update.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, every top-level tests/ file).

SCAN: pre- and post-edit read-only scan. Baseline after F10c: dm18 found 38 / base 79 / new 0 / stale 41. Expect the dm18 rows for #131, #132, #133, #134 and #136 to go STALE, new = 0.
RESPONSE FILES: `CGR-20260913-009-5.md` (#132, #133, #134, #136 with deployed-function evidence; the filter/gatherer evidence; the check-proposals CLI fix (read-only verified); GUI and `_check_implementation_tracking` read-only evidence; siblings `_ensure_collection`, `coll is None`, `check_similarity`, `get_for_dedup`, per-item skips; remaining CGR-009 anchors → F12a #117–#120 (`-6`), F12c #123/#125/#126 (`-7`), F12d #141–#143 (`-8`)). `CGR-20260913-010-4.md` (#131; the two batch-loop continuations; `update_status` / `update_tracking_metadata` scheduled for F11b-2; remaining CGR-010 → F13a/F13b/F13c receipts).
SIZE: target ≤380 changed lines; HARD stop at ≤450 (stop BEFORE exceeding it and return a split proposal: store + filter/gatherer evidence vs the two loop callers + CLI; never trim to fit).
ORDER: manifest → create F11b-1.md → pre-edit scan → tests → failing-first (digests in the same command) → edit → focused → sweep → ruff → scan → data/logs listing → packet → both response files.
