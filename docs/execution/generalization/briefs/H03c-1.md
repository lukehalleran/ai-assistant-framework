=== H03c-1: replace a friend's real first name (label P) in memory code comments, a curator self-check string and two tests; swap the co-owned R/V/V2/O labels in the same files ===
(Parent brief, 2026-09-15. Owner decisions 1, 2, 5 and 6 of 2026-09-15 plus N1 (briefs/PARENT_STATE.md). Written from the read-only H03c map (`$S/H03c_map.md`, labels only). Each file is edited whole in this batch.)

PRIVACY (read three times):
- NEVER write any real person name, city, course code or program abbreviation, or any label's real text, into ANY file you create or edit or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs and in grep commands whose output you do not copy.
- The packet uses labels only:
  - P = the friend's first name (plus its lowercase id form);
  - R = School A's city;
  - V and V2 = two course codes;
  - O = the owner's first name.
- Give `file:line` references. Never paste diffs, never quote "before" text, never describe a label by an example word.
- Do NOT restate the health detail in the packet. Call it "the care-team clause".
- Self-check (the parent's method; counts only):
  - build the capitalised (`\b[A-Z][A-Za-z.-]{2,}\b`) and lowercase (`\b[a-z][a-z_.-]{3,}\b`) token sets found ONLY on removed lines of `diff $S/h03c1_pre/<path> <path>`;
  - grep them via process substitution against batches/H03c-1.md and the five edited files;
  - the packet must have 0 hits; explain any edited-file hit by `file:line` and label;
  - re-run after EVERY packet edit.

Source:
- Owner decision 1, 2026-09-15: replace P everywhere, including the curator code string, after mapping it.
- The map settled the curator question. `memory/curation/curators/profile_junk_facts.py:42` is the input of the self-check sentinel `care_team_clause_flags` in `ProfileJunkFactCurator.sentinels()`. It is not a pattern matched against stored facts: `_is_junk_object` (memory/fact_extractor.py:229) has no name logic, and `scan()` selects facts by relation and clause shape only.
- So a NAME-ONLY swap changes nothing about what the curator purges.
- The rest of the care-team clause stays byte-identical, because tests rely on its shared tokens:
  - `test_sep05_evening_turn_audit.py` 204/218 exercise the care-team cue rule, not the overlap floor at `memory/fact_source.py:980`;
  - the comment at fact_source.py:979 documents that incident.
Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE;
- EVIDENCE TIMESTAMPS: for edits, run `date; sha256sum <file>` right after each file's edit and use it as the section time.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-1.md → pre-edit scan → collision check → copy the five files to `$S/h03c1_pre/<same relative path>` and verify digests → baseline run (digests in the same command) → edits → privacy self-check → post-edit run (identical per-file counts) → importer sweep → ruff → scan → data/logs/web listing → packet.
- No edit before the baseline is recorded. Never implement then revert. Estimate size before editing.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- NEVER run any top-level `tests/*.py` file (e.g. tests/test_fact_extractor_utils.py).
- Run the guard and MemAvailable check before EVERY pytest invocation; ≤9 files per chunk; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-1.txt` and `$S/manifest_paths_pre_H03c-1.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

SCOPE (digests from the map, 2026-09-15; re-verify)
| sha256 | lines | file | labels to replace |
|---|---|---|---|
| eb821f70 | 1064 | memory/fact_source.py | P comments :169, :979; R :15 (docstring), :146 (comment), :907 (docstring); V/V2 comments :155, :156, :639, :640. KEEP the T cue word in the `studies_at` code string at :262 |
| 4747b9c9 | 1989 | memory/fact_extractor.py | P comments :188 (lowercase graph id in an edge example), :213; R comment :1951 |
| 0e47a5e8 | 102 | memory/curation/curators/profile_junk_facts.py | P :7 (module docstring), :42 (the sentinel's code string; the name only) |
| 85be9084 | 171 | tests/unit/test_sep05_curation_wave2.py | P :51 (profile fact value, the care-team clause; name only), :107 (node id lowercase plus display name TitleCase), :110, :112 (edge ids lowercase) |
| 8592fd93 | 553 | tests/unit/test_sep05_evening_turn_audit.py | P :1 (docstring), :103, :143 (queries), :106 (assert `extract_rare_proper_nouns(q) == [P]`), :133 (parametrize, lowercase), :140 (resolver map, lowercase key and value), :146 (assert, lowercase in seeds), :166, :167 (edge ids), :192 (`_is_junk_object(clause, "has_doctor")`), :204 (`_FRIEND_TURN`), :218 (triple object); O :359, :361, :377 |

MAPPING (parent decision; synthetic; collision-checked by the map)
- P → "Ellery" (lowercase "ellery" in ids, keys and parametrize).
  - 0 hits repo-wide. TitleCase. Not in `_PROPER_NOUN_STOPWORDS`, not a when-word, not a clause verb, not a care-team cue.
- R → "Marrowby" (lowercase "marrowby" where the original is lowercase).
- V → "ABC 1234" (hyphenated "ABC-1234" where the original is hyphenated).
- V2 → "QRS 7310" (0 hits; avoids the existing bare-word "XYZ" fixtures). V2 and V must stay distinct.
- O → "Alex".
- Re-verify at launch (counts only):
  - `grep -rniw` for Ellery, Marrowby, "QRS 7310", "ABC 1234", Alex over *.py/*.md/*.json (excluding data/, .agent_snapshots/, node_modules/). Ellery and "QRS 7310" must show 0 hits outside briefs. The others are the lane's existing synthetic names.
  - "Alex" must not already appear in the two test files with a different meaning. The map found none; confirm.

STRUCTURE THAT MUST SURVIVE (read before editing)
- `utils/query_checker.py:772` `extract_rare_proper_nouns` matches `^[A-Z][a-zA-Z'’-]{2,}$`, excludes stopwords (:756) and sentence-initial tokens, and merges adjacent accepted tokens (evening audit :106).
- `memory/graph_utils.py:102` `is_temporal_deictic`: lowercase P must not match (evening audit :133). `extract_graph_entities` needs the resolver key to equal the query's lowercase token (:140–146).
- `memory/fact_extractor.py:229` `_is_junk_object` care-team branch (`_CARE_TEAM_HAS_RE`, `_CLAUSE_OBJECT_RE`): the clause's verbs and time words stay unchanged.
- `memory/fact_source.py` `_WORD_RE` (:110), stopwords, overlap floor (:980–984) and care-team cues (:175): the evening audit :204 and :218 must keep sharing every non-name token exactly.
- test_sep05_curation_wave2.py :67 (all sentinels pass), :74 (doc-id set driven by the :51 fixture), :86 (no sentinel failures).

OWNERSHIP
- The five files above: ONLY the label-carrying strings listed in SCOPE (name tokens only; all other clause text unchanged).
- New docs/execution/generalization/batches/H03c-1.md.
- Read-only: every other file, including utils/query_checker.py, memory/graph_utils.py, memory/curation/** except profile_junk_facts.py, and every class-guard path.

CONTRACT
1. Every listed P, R, V, V2 and O occurrence is replaced per the mapping. The T code word at fact_source.py:262 is unchanged.
2. No behaviour change: every test file run passes with identical per-file counts before and after, and the curator sentinels still pass.
3. Privacy verification:
   - `grep -c -i` for every real form of P, R, V, V2 and O over the five files is 0;
   - O counts only in the two test files (the production fact files may carry O outside this batch's decision scope; record their counts only);
   - the token self-check shows 0 packet hits.
4. No real name or health detail restated in any written file or report.

TESTS
- BASELINE FIRST (digests of the five files printed in the same command as the first chunk):
  - chunk 1: tests/unit/test_sep05_curation_wave2.py, tests/unit/test_sep05_evening_turn_audit.py, tests/unit/test_fact_source.py, tests/unit/test_fact_source_negation_scope.py, tests/unit/test_sep03_cat_session_fixes.py, tests/unit/test_fact_long_object_salvage.py, tests/unit/test_sep03_live_probe_fixes.py, tests/unit/test_curation_engine.py.
  - Record per-file counts.
- Post-edit: the same chunk with identical per-file counts.
- Importer sweep: every other tests/unit consumer of memory.fact_source / memory.fact_extractor / memory.curation (including `memory.curation.curators`), per IMPORTER GREPS. Chunks of ≤9, vetted.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
- ruff on the five files.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: map estimate ≈33 changed lines; target ≤100; HARD stop 450.
ORDER: manifest → create H03c-1.md → pre-edit scan → collision check → pre-copies → baseline (digests in the same command) → edits → privacy self-check → post-edit run → importer sweep → ruff → scan → data/logs/web → packet.
