=== H03b-1: remove the two real-school Wikipedia seeds and replace real school names in the core identity/institution test fixtures with synthetic ones (owner decision 5) ===
(Parent brief, 2026-09-15. Written from the read-only H03b map (`$S/H03b_map.md`, labels only). The six digests were re-checked after H03 was integrated, unchanged. H03 already put the SAME synthetic names into the source comments, so the fixtures and comments will agree. H03b-2 covers the remaining unit files plus tests/test_citation_system.py.)

PRIVACY (read three times):
- NEVER write any real institution, organization or person name, abbreviation, fragment, domain, snake_case form or School A's real state word into ANY file you create or edit: tests, script, batches/H03b-1.md, or your final report.
- Real strings may appear ONLY inside the tool inputs needed to do the work:
  - an Edit `old_string`;
  - a `grep -c`/`grep -n` pattern whose OUTPUT you do not copy into any file.
- The packet refers to them ONLY by the labels from the map: A, A-full, A-short, A-snake, A-frag, B, C, D, E, F, G, I, R, V.
  - Record `file:line` → label → the synthetic replacement, plus counts.
  - Never paste a diff. Never quote "before" text.
- Self-check before you finish (the parent's method, prints counts only):
  - build the set of capitalised tokens that appear ONLY on removed lines of `diff $S/h03b1_pre/<file> <file>` (not on added lines);
  - grep that set (via process substitution, never printed) against batches/H03b-1.md and the six edited files;
  - the packet must have 0 hits. Any hit in an edited file must be explained by `file:line` and label as a generic word or code (e.g. a state name in a code table).

Source: owner decision 5 (briefs/PARENT_STATE.md "Owner decisions received 2026-09-14"): "`scripts/build_wiki_subset.py:103/107` real-school seeds and the School A test fixture: FIX … replaced with synthetic names that keep each test's structure (state word, short form). Split by file under the size cap after a read-only map; never write the real names anywhere."
Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- EVIDENCE TIMESTAMPS (the `date` output in each section header, never a typed or "window" time);
- IMPORTER GREPS;
- SIZE.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03b-1.md → pre-edit scan → copy the six files to `$S/h03b1_pre/` and verify digests → baseline run of the 5 unit files (digests in the same command) → edits → privacy self-check → post-edit run (identical counts) → sweep → ruff → scan → data/logs/web listing → packet.
- No edit before the baseline is recorded. Never implement then revert.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- Never run scripts/build_wiki_subset.py.
- Run the guard and MemAvailable check before EVERY pytest invocation; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_post_H03.txt` and `$S/manifest_paths_post_H03.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.
ORDER REMINDER: print `sha256sum` of the six files in the SAME command that runs the baseline tests, BEFORE any edit.

MAPPING (parent decision; synthetic; the same names H03 used in source comments)
- A (state word + one TitleCase token) → "Vermont Wrenfield"; lowercase "vermont wrenfield"; A-snake → "vermont_wrenfield".
- A's second token alone → "Wrenfield" (lowercase "wrenfield" in the 141/166-style absence asserts).
- A-frag (the state word alone) → "Vermont"; lowercase "vermont".
- R (School A's city, used as "<R>, <A-frag>") → "Marrowby", giving "Marrowby, Vermont".
- A-full → "Wrenfield Institute of Science".
- A-short (only a list variable name in test_private_sphere_search_guard.py plus the comment on its line 6) → rename the variable to `SCHOOL_ANCHORS`. Where a short form is needed, use "XW".
- B → "University of Tarnwick-Hollow".
- C → "Quellmoor University" (possessive "Quellmoor University's").
- D (all-caps acronym) → "XQT" (collision-check first).
- E → "St. Veldmoor College"; F → "University of Dunmere"; G → "Brightwater Kestrel".
- I → keep if it is a generic "New … College" form; if it is a real college's name, "New Harrow College".
- V (course code) → "ABC 1234".
- Collision checks at launch:
  - `grep -rniw` over *.py/*.md/*.json (excluding data/, .agent_snapshots/, node_modules/) for Marrowby, XQT, Veldmoor, Dunmere, Brightwater, Kestrel, "New Harrow" and "ABC 1234" must show 0 hits.
  - Wrenfield, Tarnwick, Quellmoor and XW are expected only in the five H03 source files and briefs.
- Also re-verify that "vermont" and "wrenfield" are absent from `_PRIVATE_SPHERE_GENERIC_TOKENS` (web_search_trigger.py ≈1613), `_PRIVATE_GUARD_TIME_STOP_TOKENS` (≈1596) and `_TEMPORAL_GENERIC_TOKENS` (gate.py ≈500), and that "Vermont" is matched by the bare-state pattern (location_resolver.py `_US_STATES`, ≈263; pattern ≈507).

SCOPE (digests re-checked 2026-09-15 after H03; all six are unmodified tracked files)
- scripts/build_wiki_subset.py `42f937f5…` (784 lines), in `DOMAIN_MAP["education_statistics"]["seeds"]` (list opens at 102):
  - line 103 holds A-full AND the "Statistics" seed on the SAME line: remove ONLY the A-full string (with its comma/space), keep "Statistics";
  - line 107 holds B alone: delete that line.
  - 80 seeds become 78. No test imports the script; nothing asserts seed counts or titles.
- tests/unit/test_sep12_search_identity_scope.py `b7f26e6a…` (549): A 83, A-frag alone 13, C 8, R on 8 lines, plus lines 141/166 (A's second token, lowercase).
- tests/unit/test_sep12_followup_identity_names.py `97049059…` (158): A 3, A-frag 1, R 1. It imports `classify`/`decompose` from the scope test, so both files are in this batch.
- tests/unit/test_institution_resolver.py `4c7a7be2…` (291): A 31, A-full 1, B 2, C 4, R 3, D 1, E 1, I 2 (docstrings at 8, 89, 188).
- tests/unit/test_private_sphere_search_guard.py `4cc2a3b3…` (164): A 11, A-short 11 (variable name plus the line-6 comment), V 2.
- tests/unit/test_personal_anchors.py `10d4ddf8…` (191): A 7, D 1, F 1, G 1.

STRUCTURE THAT MUST SURVIVE (from the map; read each test before editing)
- Scope test:
  - A must start with the state word of the location fixture "<R>, <A-frag>" (271–279, 281–287, 328–334, 410–414, 423–429).
  - A's span stays protected (location_resolver `_institution_protected_spans` 308, `_sub_outside_spans` 340, `_proper_name_spans` 444, `query_justifies_location` 513, `strip_unjustified_location` 559).
  - Absence asserts on the lowercase state word (140/165/438) and on A's second token (141/166).
  - Exact strings: "User's school: <A>" (250, 269, 307, 326, 495, 520); list equality `f"{institution} {t}"` (268, 287, 325, 429, 494, 519, 526); possessive "<A>'s" (245, 302, 360; `_names_institution` institution_resolver.py:283).
  - A must not contain the Springfield fixture's city, state or abbreviation. R must not be a substring of A.
  - C keeps "<word> University" (254, 311, 375, 393; contains-asserts 259/316).
- test_institution_resolver.py:
  - parametrize 54–60 (`_INSTITUTION_VALUE_RE` 66 branches: two TitleCase tokens (A), all-caps acronym (D), "University of X-Y" (B), "X Institute of Y" (A-full), "St."-prefix (E));
  - 87–97 (B stale 0.85 vs A 1.0; B must match the value regex);
  - 228–236 (C must match `_NAMED_INSTITUTION_RE` 117 with a possessive);
  - 238–244; 212–222 (`_GENERIC_SCHOOL_RE` 124); 282–283.
- test_private_sphere_search_guard.py: A's tokens outside the two token sets; 85–88 stay False; 59–63 (Acme not an A token); V needs a letter token outside the vocabulary.
- test_personal_anchors.py: list equality 80/89 and 126/131; 150–156 (the vocabulary constraint); 169–174 (value-regex matches).
- test_sep12_followup_identity_names.py: `SCHOOL = <A>` (44); A avoids every word in `PROPER_NAME_TERMS`/`INJECTED_LOCATION_TERMS`; 122 has the same state/R dependency.

OWNERSHIP
- The six files above: ONLY the strings carrying label forms, the 141/166-style lines, and the renamed variable in the private-sphere test. No other line changes.
- New docs/execution/generalization/batches/H03b-1.md.
- Read-only: every source file (utils/**, core/**, memory/** — H03 already changed their comments), every other test file, tests/test_citation_system.py (H03b-2), config/**, briefs/**, and every class-guard path.

CONTRACT
1. Every label occurrence in the five test files is replaced per the mapping. The tests keep their structure and pass with identical per-file counts.
2. build_wiki_subset.py: the two seeds are removed as specified; no other change (78 seeds).
3. Privacy verification:
   - for every real form, `grep -c -i` over the six files is 0, except School A's state word where it legitimately remains in code unrelated to these files' fixtures (there should be none in these six files; explain any hit by `file:line` and label);
   - the parent's token self-check shows 0 hits in the packet.
   - Record counts per LABEL only.
4. No real name in any written file, the packet or the report.

TESTS (no new test file; behaviour-neutral fixture rename)
- BASELINE FIRST in one command: the six digests, then the 5 unit files on the UNEDITED tree. Record per-file pass counts (use `-rA` or per-file runs if needed to get per-file counts).
- Post-edit: the same 5 files with identical per-file counts.
- Sweep: every other tests/unit consumer of utils.institution_resolver / utils.location_resolver / utils.web_search_trigger / core.agentic.gate, per IMPORTER GREPS, plus any test importing the two edited test modules (`test_sep12_search_identity_scope`). Chunks of ≤9, vetted.
  - Usual exclusions: test_graph_integration.py, the ContextGatherer/UserProfile hazard, tests/test_web_search_manager.py, tests/test_prompt_internal_methods.py, and every top-level tests/ file.
- ruff on the six files.

SCAN: pre/post read-only scan. Expect identical tables: dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648 (no new file).
SIZE: map estimate ≈157–166 changed lines; target ≤300; HARD stop 450 (estimate before editing).
ORDER: manifest → create H03b-1.md → pre-edit scan → pre-copies → baseline run (digests in the same command) → edits → privacy self-check → post-edit run (identical counts) → sweep → ruff → scan → data/logs/web → packet.
