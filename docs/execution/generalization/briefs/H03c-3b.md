=== H03c-3b: replace real advisor, program, course, city, companion, contact and ID-label strings in the remaining tests, production COMMENTS/DOCSTRINGS and docs with synthetic ones (no code behaviour change) ===
(Parent brief, 2026-09-15. Owner decisions 3, 4, 5, 6, 7, N2, N3, N4, R1, R2 and R3-final (briefs/PARENT_STATE.md). Written from the read-only maps `$S/H03c_map.md`, `$S/H03c_residual_map.md` and `$S/H03c_EN_map.md` (labels only). The parent re-verified all 33 digests below on the post-H03c-3a tree at 11:19:54. The parent also checked that the 7 listed production lines not starting with `#` sit inside triple-quoted docstrings: registry.py:1202, builder.py:551, obsidian_manager.py:35, memory_storage.py:1344, grounding_check.py:766, query_checker.py:777, claim_tracker.py:135. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write any real name, program, course code, subject prefix, city, ID label or title into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy. Do not quote a grep pattern containing real forms in the packet; describe the check by label.
- Labels:
  - L = advisor full name (L-first stays unchanged; L-sur = surname);
  - T = program abbreviation;
  - V / V-hyph = course code; V2 = second course code; V3 = course token in the T title (full code; V3′ = same prefix, different number);
  - SP = subject prefix;
  - R = city;
  - CN = companion first name; CN-email = the synthetic email local part that coincides with CN;
  - EN = contact first name;
  - IDP = student-ID label;
  - ST-1 = syllabus week-1 row;
  - PS = professor surname;
  - A-frag = School A's state word;
  - UN = O-derived username.
- Give `file:line` references. Never paste diffs, never quote "before" text, never describe a label with an example word.
- Self-check (the parent's method; counts only):
  - build the capitalised (`\b[A-Z][A-Za-z.-]{2,}\b`) and lowercase (`\b[a-z][a-z_.-]{3,}\b`) token sets found ONLY on removed lines of `diff $S/h03c3b_pre/<path> <path>`;
  - grep them via process substitution against batches/H03c-3b.md and every edited file;
  - the packet must have 0 hits apart from infrastructure paths, which you explain by line; explain every edited-file hit by `file:line` and label (a recorded KEEP, or an ordinary English word);
  - re-run after EVERY packet edit.

CLASS-GUARD PATH: docs/BUG_CLASSES.md is class-guard-owned and is NEVER edited. Its R and SP lines stay; the parent records them for the owner.
Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE (estimate before editing);
- EVIDENCE TIMESTAMPS: `date; sha256sum <file>` right after each file's edit.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-3b.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c3b_pre/<same relative path>` and verify digests → baseline runs (digests in the same command as the first chunk) → ALL edits → privacy self-check → post-edit runs (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- Finish EVERY edit (tests, production comments AND docs) before the privacy self-check and the post-edit runs.
- No edit before the baseline is recorded. Never implement then revert.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- Run only tests/unit files. Run the guard and MemAvailable check before EVERY pytest invocation; ≤9 files per chunk; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-3b.txt` and `$S/manifest_paths_pre_H03c-3b.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.
- A separate `main.py` process (≈3.8 GB, outside this clone) may be running. Leave it alone. If MemAvailable is below 4000 MB before a chunk, wait and re-check; never lower the cap or skip the check.

MAPPING (parent decision; synthetic)
- L → "Morgan Ashdown" (L-sur → "Ashdown"; L-first unchanged).
- T → "MXS". The narration title fixture → "MXS_Ktr5520_M16L7", identical at 73/74/91.
- V → "ABC 1234" (V-hyph "ABC-1234"). V2 → "QRS 7310" (lowercase "qrs 7310"). V3 → "KTR 5520" (glued form "Ktr5520"; bare "KTR"); V3′ → "KTR 5740".
- SP → "ABC" / "Abc" / "abc", following the original casing.
- R → "Marrowby" / "marrowby".
- CN → "Tamsin"; CN-email local part → "odile" (backup "ysolde").
- EN → "Maren".
- IDP:
  - SWAP-SAFE `test_sep03_cat_session_fixes.py:211` → "WFID: 0000-0000";
  - routed `test_privacy_redaction.py:12` → "Student ID 900123456";
  - routed 48 and 55 → "Student ID: …" (exact; change together);
  - KEEP-REAL 111–112;
  - docs → "student ID".
- ST-1 → "Fitted Curves (1)". PS → "Varnum". A-frag → "Vermont" (audit line 36 → "The Vermont Wrenfield one?"). UN → "alexh".
- Collision checks at launch (counts only; excludes as in R_common_rules):
  - "odile", "ysolde", "Ktr5520", "KTR 5740": 0 hits outside docs/execution expected.
  - "MXS", "Morgan Ashdown", "Marrowby", "ABC 1234", "QRS 7310", "KTR", "Tamsin", "Maren", "Varnum", "Student ID 900123456", "WFID" and "Fitted Curves" are already used by earlier batches in OTHER files (H03c-3a included). Record per-file counts.
  - Confirm that none of the target files already contains a replacement string where that would make a swap ambiguous; record any such file.

SCOPE (digest prefixes re-verified by the parent; re-verify at launch and record the full digests in the packet)
Tests (edit and run):
- tests/unit/test_fact_source.py `cb2e2c11…`: R 6 (docstring); fixtures 34, 52, 55, 59, 146, 147, 156, 157, 161; assertions 35, 56, 61, 87, 88, 149, 158 (exact), 162 (endswith).
- tests/unit/test_sep03_cat_session_fixes.py `6548e664…`:
  - R 17 (docstring), 172, 174, 175 (lyrics; 174/175 identical), 182, 197, 239, 240 (assertions);
  - CN 172, 182;
  - V 205, 223, 248; V2 205, 223, 248, 250, plus substring assertions 245, 252, 257;
  - L 219 (signature);
  - IDP 211 (SWAP-SAFE; rerun the signature-block class).
- tests/unit/test_narration_turn_audit_fixes.py `b43c9961…`: T 6 (docstring); T+V3 title 73, 74 (path; UN on the same line), 91 (exact `not in titles`); UN 78/79 (read to confirm which).
- tests/unit/test_keyword_anchor_retrieval.py `8f4a4db7…`: L 9 (docstring), 240, 256 (substring assertion); V3′ 241 (must not contain appointment, scheduled or Friday).
- tests/unit/test_fact_source_negation_scope.py `9f21522e…`: R 84.
- tests/unit/test_privacy_redaction.py `2773c577…`: R 14; IDP 12, 48, 55 (routed); 111–112 KEEP-REAL.
- tests/unit/test_reflection_retrieval.py `4dccf898…`: T 135.
- tests/unit/test_ingest_turn_misfires.py `b9af1662…`: V2 54 (INGEST_PASTE); CN-email 64 → "odile"; EN 118.
- tests/unit/test_sep12_keyword_boundary_and_budget.py `92d455f4…`: V2 159 (lowercase; no concern stems).

Production COMMENTS/DOCSTRINGS ONLY (no code strings):
- knowledge/obsidian_manager.py `b9e6e8e8…`: L 35 (module docstring), 820 (comment); V3 865 (comment).
- utils/query_checker.py `e3dced06…`: L-sur 777 (docstring; the surname example token only); SP 845 (comment).
- core/prompt/gatherer_knowledge.py `00870a5b…`: T 758 (comment).
- core/action_claim_guard.py `905cb5c8…`: V 145.
- core/grounding_check.py `6b604790…`: V-hyph 201, V 299, 322; ST-1 766 (docstring).
- core/agentic/tools.py `19f2cfa6…`: V 1896.
- memory/claim_tracker.py `fbae71f1…`: R 135 (docstring). The O on the same line STAYS (production).
- memory/memory_storage.py `e2959253…`: R 104; CN 1169, 1344.
- memory/llm_fact_extractor.py `abc0b74f…`: R 349; CN 178. The CN code-string prompt example at 448–449 is OUT (keep).
- core/content_type_detector.py `52bd76ae…`: SP 169.
- core/actions/registry.py `cb801574…`: SP 71, 225, 1173, 1202 (docstring, twice), 1358, 1360.
- core/agentic/protocols.py `96a35d4c…`: SP 1225.
- core/agentic/controller.py `b6d40419…`: SP 1548.
- core/agentic/gate.py `e5dc0920…`: SP 818, 908; EN 989 (comment).
- core/prompt/builder.py `58f84975…`: SP 551 (docstring).
- utils/web_search_trigger.py `a3347385…`: SP 877, 1582.
- gui/handlers.py `ae327d02…`: PS 2971 (comment).
- For EVERY production line, verify it is a comment or docstring before editing. If it is a code string, STOP and report (labels only).

Docs:
- docs/GENERALIZATION_AUDIT_20260901.md `962056d0…`: A-frag 36.
- docs/CLAIM_SUPPORT_TRACE_20260902.md `56f8215f…`: R 123 (twice), 130; IDP 53, 60.
- docs/ARCHITECTURE_GUIDE.md `3474fdc2…`: R 1981; EN 2976; CN 3203.
- docs/MEMORY_SYSTEM.md `bdb58e2b…`: R 687; CN 791.
- docs/PROJECT_SKELETON.md `108d4f50…`: R 3641 (the O on that line STAYS); T 2401, 4475, 4479; V3 1405, 1411, 2401.
- docs/PROMPT_BUILDING_PIPELINE.md `cf668a4a…`: L 111; CN 417.
- docs/BUG_RETROSPECTIVE_20260715_20260904.md `c1042637…`: T 232.
- NOT edited: docs/BUG_CLASSES.md (class-guard-owned).

- Before editing, grep each file in scope for every label's real form (case-insensitive, non-word-bounded) and reconcile against these lists. Record any extra or missing line by `file:line` and label. An extra occurrence in a scope file that the owner decisions cover (tests, comments, docstrings, docs) is in scope; a code string or a file outside scope is reported only.

STRUCTURE (read before editing)
- test_fact_source: residence cues at 87/88; exact excerpts at 158/162.
- sep03_cat: 174/175/197 identical; 239/240 exact on both sides; "V2 and V" at 248 must be two distinct codes; object tokens must appear in the live span (fact_source `_WORD_RE`, overlap floor).
- privacy_redaction: the address rule consumes the rest of the line (R at 14 is free); 48 and 55 exact together.
- narration: 73/74/91 identical strings (the filter works on path origin).
- keyword_anchor: the `extract_rare_proper_nouns` assertions still hold for L.
- ingest: the INGEST_PASTE edits don't affect the gate or visual assertions (155–179). EN at 118 keys only on the verb (`_EMAIL_COMMAND_RE`, gate.py:537).
- Two source-reading tests must still pass: test_sep03_followups_continuity.py:350 (memory_storage call text) and test_sep10_probe_dump_interpretation.py:1452 (`get_daemon_self_notes`).
- Why comment and docstring edits cannot change behaviour here (parent check 10:58:08):
  - no module in scope reads its own `__doc__` (only scripts/ do);
  - the repo-wide maps found every test occurrence of each real form, so any test asserting on these comment texts would already be in scope;
  - line counts are unchanged, so line-proximity source tests are unaffected.

OWNERSHIP
- The listed lines only (plus reconciled extras in scope files, as above).
- New docs/execution/generalization/batches/H03c-3b.md.
- Read-only: everything else, including docs/BUG_CLASSES.md and every class-guard path.

CONTRACT
1. Every listed occurrence is replaced. Production edits are comment/docstring-only; state this per file after reading your own diff.
2. No behaviour change: identical per-file test counts before and after.
3. Privacy: per-label real-form counts over the edited files are 0, except the recorded KEEPs:
   - privacy_redaction 111–112;
   - O on claim_tracker:135 and PROJECT_SKELETON:3641;
   - llm_fact_extractor 448–449;
   - fact_source.py:262 (not in scope; unchanged).
   - Packet token hits 0 (infrastructure paths explained).
4. No real name in any written file or report.

TESTS
- BASELINE FIRST (digests in the same command as the first chunk), in 2 chunks of ≤9:
  - the 9 edited test files;
  - test_sep03_followups_continuity.py, test_sep10_probe_dump_interpretation.py, test_grounding_check.py, test_action_claim_guard.py, test_claim_tracker.py, test_music_session_misfires.py, test_sep12_search_identity_scope.py.
- Post-edit: the same chunks with identical per-file counts.
- Importer check: grep tests/ for imports of each edited test module name (all forms, dotted patch strings). Run any importer not already run (≤9 per chunk). Production edits are comment/docstring-only, so their importers need no run; ruff proves those files still parse.
- ruff on every edited .py, production files included.

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: map estimate ≈117 changed lines (tests ≈59, production comments ≈37, docs ≈21); target ≤200; HARD stop 450.
- Measure before editing. If the estimate exceeds 200, stop and return a split proposal (tests vs production comments plus docs).
ORDER: manifest → create H03c-3b.md → pre-edit scan → collision checks → pre-copies → baseline (digests in the same command) → ALL edits → privacy self-check → post-edit runs → importer check → ruff → scan → data/logs/web → packet.
