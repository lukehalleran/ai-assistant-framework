=== H03c-2: replace the owner's real first name (label O) in test fixtures with "Alex", keep the two real-name guard tests, swap the O-derived username in test paths, and swap the residual labels that share these files ===
(Parent brief, 2026-09-15. Owner decision 2, N1, N2, N4, R1 and the merged residual labels (briefs/PARENT_STATE.md). Written from the read-only maps `$S/H03c_map.md` and `$S/H03c_residual_map.md` (labels only). Each file is edited whole in this batch. Name swaps keep every line count.)

PRIVACY (read three times):
- NEVER write any real name, username, city, course code, course or syllabus title, subject prefix or ID prefix into ANY file you create or edit, or into your report, except as the required edits themselves.
- Real strings appear ONLY in Edit `old_string` inputs, and in grep commands whose output you do not copy.
- The packet uses labels only:
  - O = owner first name (O-lower, @O-lower handle, O-lower_h resolver id);
  - UN = the O-derived username in `/home/<UN>/` paths;
  - L = advisor full name;
  - R = city;
  - V = course code; V3 = second course token (bare prefix);
  - SP = V's subject prefix;
  - CT = course title; ST = syllabus title (bare stem);
  - CN = companion first name;
  - IDP = student-ID label/prefix.
- Give `file:line` references. Never paste diffs, never quote "before" text, never describe a label with an example word.
- Self-check (the parent's method; counts only):
  - build the capitalised (`\b[A-Z][A-Za-z.-]{2,}\b`) and lowercase (`\b[a-z][a-z_.-]{3,}\b`) token sets found ONLY on removed lines of `diff $S/h03c2_pre/<path> <path>`;
  - grep them via process substitution against batches/H03c-2.md and every edited file;
  - the packet must have 0 hits; explain every edited-file hit by `file:line` and label (e.g. a kept guard line);
  - re-run after EVERY packet edit.

OWNER DECISIONS applied here (2026-09-15):
- **Decision 2:** replace O in test fixtures.
- **N1 — guards:** KEEP the two real-name guard tests UNCHANGED, so they still catch real-name leaks:
  - `tests/test_system_prompt_placeholders.py` (the whole guard at 48–72, including its test function name, docstring, comments, probes and assert message): 0 edits;
  - `tests/unit/test_user_identity.py` 208/209/224/231: 0 edits.
  - Also add a short note next to the "no bulk replace" rule in `docs/generalization/02-owner-neutral-runtime.md` recording the owner decision, without any real name: fixtures now use a synthetic first name; the two guard tests keep the real name on purpose.
- **N2:** replace UN in test paths with "alexh" (`/home/alexh/...`). test_visual_memory_gating needs UN to begin with the replacement first name.
- **N4 and R1:** merged residual swaps in these files, listed per file below.

Request packets: none; do NOT read any class-guard file. No response file. BUG_CLASSES: none (privacy/hardcoding).
Rules: docs/execution/generalization/briefs/R_common_rules.md applies in FULL, including:
- the exact permitted `python` forms;
- IMPORTER GREPS;
- SIZE (estimate before editing);
- EVIDENCE TIMESTAMPS: `date; sha256sum <file>` right after each file's edit.

COMPLIANCE (read twice):
- ORDER is strict: manifest → create H03c-2.md → pre-edit scan → collision checks → copy every file in scope to `$S/h03c2_pre/<same relative path>` and verify digests → baseline runs of the tests/unit files (digests in the same command as the first chunk) → edits → privacy self-check → post-edit runs (identical per-file counts) → importer check → ruff → scan → data/logs/web listing → packet.
- No edit before the baseline is recorded. Never implement then revert.
- `python` may appear in a Bash command ONLY as:
  - `python -m pytest …` (capped systemd-run);
  - `python -m ruff check …` / `python -m ruff --version`;
  - `python scripts/check_bug_classes.py scan --root .`;
  - the one `utils` sanity check.
  - No npm.
- NEVER run any file outside tests/unit: no top-level tests/*.py, tests/test_eval/**, tests/benchmarks/** or tests/fixtures consumers. Those edits are verified by reading (consistent swaps, including every exact assertion in the same file) plus ruff.
- Run the guard and MemAvailable check before EVERY pytest invocation; ≤9 files per chunk; record each chunk's exact file list.
- Disclose any breach immediately.

MANIFEST CHECK: against `$S/manifest_pre_H03c-2.txt` and `$S/manifest_paths_pre_H03c-2.txt`.
GUARD, MEMORY, DATA NOTE:
- As in R_common_rules. The data/ baseline is `$S/data_baseline_post_F12a.txt`.
- `git status --short web` must equal `$S/web_status_post_F13c-2a.txt` throughout.

MAPPING (parent decision; synthetic)
- O → "Alex"; O-lower → "alex"; @O-lower → "@alex"; resolver id O-lower_h → "alex_h".
  - EXCEPTIONS: in `tests/unit/test_entity_facts.py` and `tests/unit/test_sep03_live_probe_fixes.py`, "Alex" already means a different person, so O → "Avery" (lowercase "avery") there.
- UN → "alexh".
- L → "Morgan Ashdown" (lowercase "morgan ashdown"); an L first-name-only form stays unchanged.
- R → "Marrowby" / "marrowby".
- V → "ABC 1234"; SP → "ABC" / "Abc" / "abc", following the original casing.
- V3 (bare prefix) → "KTR".
- CT → "Predictive Modeling for Operations"; ST bare stem → "Fitted Curves".
- CN → "Tamsin" (lowercase "tamsin").
- IDP (SWAP-SAFE use) → "WFID 0000-0000".
- Collision checks at launch (counts only; 0 hits outside briefs expected unless noted): alexh, Avery, KTR, WFID, "Predictive Modeling for Operations", "Fitted Curves".
  - "Tamsin" is expected only in tests/unit/test_music_session_misfires.py.
  - Confirm no target file other than the two exceptions already uses "Alex", and neither exception file uses "Avery".

STRUCTURE THAT MUST SURVIVE (read each test before editing)
- O must be a single TitleCase word with a lowercase tail:
  - memory/claim_tracker.py `_DECLARATIVE_RE` :108 and `_SUBJECT_VERB_RE` :118;
  - hashing is case- and whitespace-insensitive, and test_claim_tracker.py 67–73 pairs TitleCase with lowercase.
- Signature and salutation lines written with `\nO` escapes, which a `-w` grep misses: test_self_report_predicates 48/93, test_sep06_evidence_ownership 115, test_sep04_evening_fixes 114/121, test_action_claim_guard 187, test_fact_long_object_salvage 30. Search with a non-word-bounded pattern for these.
- Exact, prefix and possessive forms: swap consistently within each file.
  - Exact equality: test_knowledge_graph 118, test_source_excerpt_pipeline 113, tests/test_user_profile_schema_preferences.py 143, test_user_profile 27, test_stance_consumers 151, tests/test_wizard_force_flag_override.py 100, tests/test_eval/test_checks.py 161, test_cross_deduplicator 552–589 (subject parsed from "O | …", "O - …", "O is …"), tests/test_fact_extractor_utils.py 282/288/294.
- test_visual_memory_gating:
  - 107–117 need O-lower to be a strict prefix of a UN path segment (so "alex" / "/home/alexh/");
  - 139–142 need O-lower to match as a standalone word.
- `memory/fact_extractor.py:297` `_SALUTATION_RE`: L's replacement must be exactly "First Last" in TitleCase (test_fact_long_object_salvage 41–42); `_clean_triple` lowercases it (65–66).
- CN in test_stance_consumers:
  - entity_id lowercase paired with display_name TitleCase;
  - exact sentence at :93; startswith at :74/:80; not-in at :76.
  - CN must not be a pronoun or role word.
  - test_sep03_live_probe_fixes:95 needs a plain capitalised given name (the junk-object check).
- SP 3→3 letters keeps line lengths. round2:48 feeds `is_personal_doc_search` and the wrapped-prose guard. audit0831:480 feeds `is_personal_doc_search`.
- V in test_calendar_turn_round2: 81 is an exact summary assertion; keep 73/192 consistent with it.
- V3 in test_actions: 316 and 322 must be the identical plain string (XML attribute and text).
- CT/ST on test_sep04_evening_fixes:228: no `.csv`/`.pdf`, filename or junk tokens; the test still expects "Part 1".
- IDP at test_paste_turn_misfires:47: "WFID 0000-0000" matches no redaction rule. That test only checks gate and visual behaviour.
- tests/fixtures/retrieval_benchmarks.yaml: content strings only. Keep YAML quoting and indentation byte-exact apart from the name tokens. No marker, query or id contains O.

SCOPE (per file; lines from the maps; digests re-verified at launch and recorded in the packet)
Top-level (edit, verify by reading, NEVER run):
- tests/test_gated_prompt.py: O 11 (possessive).
- tests/test_agentic_search.py: O 958, 991, 1662 (the `\nO` form).
- tests/test_integration_fact_extraction.py: O 15, 16, 21 (comment).
- tests/test_user_profile_schema_preferences.py: O 132, 135, 143 (exact assertion).
- tests/test_fact_extractor_utils.py: O 116, 123, 129, 136, 178, 238, 244, 250, 257, 267 (padded and "O." forms), 281, 287, 293 (`user_name=`), 282, 288, 294 (assertions).
- tests/test_wizard_force_flag_override.py: O 21, 85, 100 (assertion).
- tests/test_tone_detection.py: O 109, 131, 151 (possessives), 198 (comment).
- tests/test_eval/test_checks.py: O 156, 161 (assertion).
- tests/fixtures/retrieval_benchmarks.yaml: O at 690, 710, 719, 737, 746, 755, 768, 777, 788, 797, 810, 819, 828, 837, 848, 857, 866, 875, 884, 893, 902, 911, 920, 929, 938; CN at 864, 866.
- tests/test_system_prompt_placeholders.py: GUARD, 0 edits (N1).

tests/unit (edit and run):
- test_action_claim_guard.py: O 187.
- test_code_proposal.py: O-lower 346, 489 (`reviewed_by`); assertions 354, 496.
- test_actions.py: @O-lower 297, 316; assertions 310, 311, 323; V3 316 and 322 (exact).
- test_fact_long_object_salvage.py: O 30 (signature); L 41, 42 (exact), 65, 66 (lowercased).
- test_claim_tracker.py:
  - O-lower ClaimKey/hash subject: 37, 38, 43, 47, 48, 52, 53, 62, 63, 68, 72, 73, 81, 213, 214, 286, 347, 348, 351, 353, 361, 369, 387, 400, 420, 438;
  - TitleCase / case-folding: 67, 91, 92, 99, 130, 134, 137, 147, 159, 168, 188, 194, 195, 196;
  - resolver id O-lower_h: 98, 100, 187, 190;
  - R on 130, 147, 159, 168, 188, 195.
- test_cross_deduplicator.py: O 516, 549, 559, 568, 577, 586; assertions 552, 562, 571, 580, 589.
- test_calendar_update_delete.py: O 214 (comment); @O-lower 274.
- test_calendar_turn_round2.py: O 111; @O-lower assertions 116, 118; SP 48; V 73, 81 (exact), 192.
- test_deliberation_coordinator.py: O 45.
- test_entity_facts.py: O 209 → "Avery".
- test_health_transient_retrieval.py: O 67, 132, 229, 231; assertions 69, 135, 165.
- test_paste_turn_misfires.py: O-lower 11 (docstring); O 47 (same line as IDP → "WFID 0000-0000"); UN 197.
- test_sep06_evidence_ownership.py: O 115 (`\nO`).
- test_shutdown_pipeline_proposals.py: O 67; assertion 147.
- test_self_report_predicates.py: O 48, 93 (signature inside the asserted call).
- test_user_profile.py: O fixtures 22, 60, 70, 80, 144, 190, 191, 192, 202, 203, 212, 268, 374, 424, 425, 429; assertions 27, 75, 85, 152.
- test_audit0831_fixes.py: O 96; @O-lower 99; SP 480.
- test_thinking_leak_storage.py: O 47, 83.
- test_retrieval_context_quality.py: O 73, 75.
- test_sep03_live_probe_fixes.py: O 185, 188 → "Avery"; CN 95 → "Tamsin".
- test_grounding_check.py: O 41.
- test_sep04_evening_fixes.py: O 114 (signature), 121 (salutation); R 99 (lowercase); CT+ST 228.
- test_source_excerpt_pipeline.py: O 50, 110, 111, 285; exact assertion 113.
- test_visual_memory_gating.py:
  - O-lower/UN docstrings 5, 11, 107, 139;
  - UN fixtures 93, 114;
  - entity lists 112, 121, 127, 140;
  - queries 122, 128, 141;
  - assertions 130, 142.
- test_stance_consumers.py: O assertions 150, 151; CN 29, 37, 40, 42, 47, 50, 55, 57, 63, 73, 74, 76, 79, 80, 83, 90, 93, 106, 123, 127.
- test_tone_carryover_persistence.py: O 5 (docstring).
- test_wiki_disambiguation_filter.py: O 42.
- test_store_get_by_id_outcomes.py: O-lower ClaimKey subject 70, 120, 135, 149, 167, 180.
- test_daily_notes_auto_update.py: O 18.
- test_knowledge_graph.py: O 117, 954; exact assertion 118; assertion 959.
- test_daemon_guard.py: UN 28.
- test_user_identity.py: GUARD, 0 edits (N1). Still run it; it must pass unchanged.
- NOT in this batch: test_narration_turn_audit_fixes.py (H03c-3) and test_sep05_evening_turn_audit.py (done in H03c-1).

Doc:
- docs/generalization/02-owner-neutral-runtime.md: one short note, 1–3 lines, next to the "no bulk replace" rule, with no real name.

OWNERSHIP
- The files above: ONLY the label-carrying strings listed, plus the one doc note. No other line changes.
- New docs/execution/generalization/batches/H03c-2.md.
- Read-only: every production file (including core/system_prompt.txt and memory/claim_tracker.py), the two guard tests, every other test file, and every class-guard path.

CONTRACT
1. Every listed occurrence is replaced per the mapping. The two guard tests are byte-identical to their pre-copies.
2. No behaviour change: every tests/unit file run passes with identical per-file counts before and after. The top-level and fixture edits are consistent by reading (each exact assertion updated with its fixture).
3. Privacy verification:
   - `grep -c -i` per label over the edited files is 0 for every real form, except the two guard files (O kept by N1; record their counts);
   - the token self-check shows 0 packet hits.
4. No real name in any written file or report.

TESTS
- BASELINE FIRST: the digests of all files in scope, printed in the same command as the first chunk. Then every tests/unit file listed (edited files plus test_user_identity.py) in chunks of ≤9. Record per-file counts (`-rA` summaries).
- Post-edit: the same chunks with identical per-file counts.
- Importer check: grep tests/ for imports of each edited tests/unit module name (`from tests.unit.<module> import` and friends) and run any importer not already run (≤9 per chunk).
- ruff on every edited .py file (top-level included).

SCAN: pre/post read-only scan; expect identical tables (dm18 found 23 / base 79 / new 0 / stale 56; dm17 files 648).
SIZE: map estimate ≈250–260 changed lines (O ≈221 after removing the guards, UN 7, residuals ≈30, doc note ≤3); target ≤300; HARD stop 450.
- Measure before editing. If the estimate exceeds 300, stop and return a split proposal (top-level plus fixtures vs tests/unit).
ORDER: manifest → create H03c-2.md → pre-edit scan → collision checks → pre-copies → baseline (digests in the same command) → edits → privacy self-check → post-edit runs → importer check → ruff → scan → data/logs/web → packet.
