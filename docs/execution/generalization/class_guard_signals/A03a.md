# Class-guard integration signal — batch A03a

Immutable once written (per the plan's "Generalization scan exposes a
baseline finding" contingency). This is a signal, not a request; the
class-guard owner decides whether to issue a request against it.

- Batch ID: A03a (F02 profile-path authority)
- Scanner: `dm01_raw_substring`
- Path: `memory/user_profile.py`
- Symbol: `UserProfile._is_temporal_query`
- Source SHA-256 before: `2cff4062a8340c20a5d36ba26a21df6c3eca9b56d06f73c926536278f6847f08`
- Source SHA-256 after: `33cbe429fbc7b900fc1e52c73ed11c208d7f11520d70f2fe47053c7641d267c6`
- Anchored expression: unchanged. `UserProfile._is_temporal_query` still reads:

  ```python
  return any(kw in query_lower for kw in self.TEMPORAL_KEYWORDS)
  ```

  Previously at memory/user_profile.py:686; now at memory/user_profile.py:684
  (the file shrank by two lines above this point — the `if sys.frozen` /
  `else` DEFAULT_PATH branch collapsed into a single `DEFAULT_PATH = None`
  line, and the now-unused `import sys` was removed). The expression text
  and its containing method are byte-for-byte identical to before.

- Reason: this batch edited `memory/user_profile.py` only for profile-path
  resolution (`DEFAULT_PATH` / `__init__`, per F02/BC-10/BC-58) — a change
  far from `_is_temporal_query` in both location and purpose. The file's
  source SHA-256 changed as a side effect, so a scanner that anchors on
  content hash (dm01_raw_substring, per CGR-20260913-005 review #23) will
  report `disposition_source_changed` until the class-guard owner re-reviews.
  CGR-20260913-005 is NOT fixed by this batch and remains open.
- Reproduction: `python scripts/check_bug_classes.py scan --root .`

No other file assigned to A03a (`utils/bootstrap.py`, `utils/user_identity.py`,
`utils/location_resolver.py`, `utils/institution_resolver.py`,
`utils/timezone_resolver.py`, `tests/unit/test_profile_path_authority.py`)
appears in any class-guard request as of this batch's HEAD
(328a8ecea1dae91de16f5974ea71727c4e2e1aa0). The post-edit
`check_bug_classes.py scan --root .` run below reported 0 new / 0 stale for
every gate, confirming no new baseline finding was introduced in this
checkout's (base-contract) scanner.
