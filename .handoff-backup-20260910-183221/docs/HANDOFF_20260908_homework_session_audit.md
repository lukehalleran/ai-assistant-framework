# Fable handoff — September 8 homework session

**STATE: Codex audit (2026-09-08 ~17:20) → Fable adversarial review DONE (same evening; see the review table). ALL SIX BATCHES (B1–B6) SHIPPED in the working tree the same evening — see RESULTS; owner: commit (`commit_message_10.txt`, combined with 8/9), restart, B6.4 live probes.** No application fixes, migrations, live model probes, commits, or restarts were performed by the audit. Do not interpret a proposed acceptance check as a passing result.

## STATE — evidence, baseline, and scope

- Source directory supplied by the owner: `~/daemon_dumps/`. `debug_records.json` contains **41 records**, indexed **0–40** throughout this document. Its SHA-256 is `a0f045122183fbf810b1e3e711a8bd50311dfed94001fade12b4663d24b081a7`. `last_prompt.txt` is 74,231 bytes / 1,108 lines and corresponds to record 40. The earlier `/tmp` exports were empty and are not evidence.
- Audit coverage: parsed the complete JSON; reviewed all 41 user requests, answers, plans, gate reasons, provenance receipts and timing fields; read the complete last-prompt export; inspected prompt structure across the session and the source-bearing sections relevant to the findings. Repeated histories and uploaded lecture prose were inspected selectively rather than repeatedly reproduced. This is a session audit, not proof that every possible defect in the repository has been found.
- Session window: September 8, approximately 14:57–17:05 local time. The JSON lacks a top-level turn timestamp; most times below come from the prompt's clock, checked against `daemon_debug.log`. Record 28 ran around 16:07 according to that log.
- Read the canonical `docs/DEVELOPMENT_WORKFLOW.md`, critical and session-safety doctrine in `CLAUDE.md`, and root `PLAN.md`. **`PLAN.md` is an implemented, historical `expand_memory` design**, not an unfinished task list. Consult `docs/HANDOFF_20260907_upload_reuse_contracts.md` for already shipped upload fixes and its live retest results. The September 6 context-integrity handoff remains relevant to deferred behavior and memory work.
- Baseline HEAD: `bd60cbe`, branch `master`. There were **13 modified tracked files, +805/−25 lines**, plus `tests/unit/test_sep07_calendar_offer_continuation.py` and `tests/unit/test_sep08_profile_fact_misfires.py` untracked. Existing edits touch `.gitignore`, action claim/proposal/calendar code, `core/agentic/{controller,gate,protocols,types}.py`, `gui/handlers.py`, `memory/fact_source.py`, and `utils/query_checker.py`. **Preserve these edits.** In particular, prospective-completion and fitness-relation support fixes already exist; do not implement them again.
- Historical logs establish paths taken during the incident; current source and read-only function probes establish what remains on disk. No running-process revision was verified. Do not claim HEAD alone identifies the incident runtime.
- Raw dumps contain personal data even though some fields are redacted. Keep them outside this public repository. Tests and tracked examples must use synthetic people, documents and events. Reference private evidence by record index and JSON field, not copied personal biographies.

## FABLE ADVERSARIAL REVIEW (2026-09-08 evening, Fable 5.1)

Every finding below was re-verified against `~/daemon_dumps/debug_records.json` (SHA matches), the incident log `daemon_debug_20260908_172904.log` (14:52–17:29), and the deployed functions (direct invocation, `DAEMON_TEST_MODE=1`). The tracked diff was not touched by this review.

| # | Verdict | Verification | Fable notes |
|---|---|---|---|
| F1 | **ACCEPTED, P0** | 47 `Stripped bare tool-call line` entries; **32 of them are the code fences themselves** (`` ```r `` ×16, `` ``` `` ×16). Storage sanitizes `final_output` (raw stream) while display/debug use the stripped `display_output` → stored history intact, user saw broken code. | The heuristic's original fixture (commit 16dbd81: `list_repos`, "Lines added…") is a bare TOOL NAME line. Replacement must be a closed set of tool names, never a word count. Codex's 1.3 "canonical body" is reduced: removing the heuristic restores parity; a parity test pins it. |
| F2 | **ACCEPTED** | `is_self_report(rec2)=True`, `is_request_shaped(rec2)=False`, `is_casual_acknowledgment(rec8)=True`. Record 8 prompt had no upload section (light path). | Root cause of both is that request predicates look only at the HEAD clause. Regex fixes restore the full path but cannot find Q2 — the PDF body was middle-out trimmed from history (`CONVERSATION_QUERY_MAX_TOKENS`=600, 09-04) and the upload leg needs a document cue + 0.62 relevance. Batch 5 is the actual fix. |
| F3 | **ACCEPTED** | `build_context_digest({'user_uploads':[…]})` → `('', [])`. Allocation is sequential first-come; `max_section_chars`=1200 per section, so exhaustion is possible but the omission is the defect. | Fix is two lines of key ordering plus a reserved allowance; no planner redesign. |
| F4 | **ACCEPTED** | `detect_insight_request(rec28).kind == 'pattern_temporal'`; match span = `"am i just finding the 5% perentile here given input of the t values? probably doing a bit more"` — `.{0,100}` with DOTALL crosses the `?`. 31.9 s evidence, 4 PubMed calls, `document_saved=False`. | Two-part fix: no sentence crossing in any arm, and bare `more/less` need a longitudinal anchor. Existing contracts (`test_pattern_routing_generic.py`) must stay green. |
| F5 | **ACCEPTED** | `controller.py:509-519` acquires before round 1 when `sandbox_manager.is_available()` (= `self.enabled`, cheap). Log: 26 `Created persistent sandbox session`, 13 `Closed … after 0 executions`, 5 `timed out, recreating`. | Lazy supplier at `_dispatch_sandbox` only. `is_available()` is already side-effect-free. |
| F6 | **ACCEPTED in part** | 19 agentic turns, 0 tool rounds. Records 23 AND 40 are LLM-flagged memory loops on statements (Codex isolated only 40). | Deterministic backstop generalized (below). The routing-reduction *evaluation* (Codex 2.4/6) is DEFERRED — no baseline exists. |
| F7 | **ACCEPTED** | Export = base `full_prompt`; controller keeps `_last_final_prompt`/`_last_final_system_prompt`; record 40 base prompt renders [GOOGLE CALENDAR] + [KNOWLEDGE GRAPH] while the receipt lists both omitted. | Add answer-call fields to the record + export; keep base fields. Cost roll-up by call site DEFERRED. |
| F8 | **ACCEPTED, narrowed** | `15:18:59 … Final prompt still over ceiling after trimming: 40559/40000`. Overflow is the merged query (lectures); trimmables exhausted. | Bounded fix only: middle-out the query section with an explicit omission marker naming `get_full_document`. Codex step 6 (global metering redesign) is DEFERRED. |
| F9 | **NOTED, not code** | Model-origin errors (nine vs ten predictors, OS assumption, Windows). | No deterministic fix. Eval set = owner decision; grounding verifier stays LOG-ONLY. |
| F10 | **ACCEPTED, already owner-gated** | The write-path fixes are in the dirty tree (`test_sep08_profile_fact_misfires.py`, 28 tests); `data/profile_junk_candidates_20260908.txt` awaits the owner. | Nothing new to build here. |
| F11 | **ACCEPTED** | `redact_text('abc123456789def') → 'abc[REDACTED ID]def'`; 7 plan hashes in the dump carry `[REDACTED ID]`. `birthday=`/`lives_in=`/`location=` uncovered. | Alphanumeric boundaries on the 9-digit rule (a format constraint on the ID, not a hash exemption) + structured profile-field rules. |
| F12 | **ACCEPTED, mixed** | 32 `[prompt_context_gatherer] Semantic search timeout after 1.5s` (wiki semantic path, `SEM_TIMEOUT_S`); contacts 403 at 14:57 + 15:31 (`google_contacts`); 15:33:20 turn: kimi-3 streamed one `<thinking>` chunk, visible answer empty, storage skipped, no debug record. | The 15:33 shape is a CONTENT-channel thinking-only stream (the reasoning-channel recovery does not cover it). Semantic timeouts: read-only investigation only. |

**Findings Codex did not isolate (added to the plan):**

- **N1 — `read` as an R function name routed to file tools.** Record 6 `"read not a function had been using read.delim…"`: `gate._REQUEST_SHAPED_RE` (`core/agentic/gate.py:1547`) matches the retrieval verb `read` at the message head → "File retrieval continuation — routing to tools" → an agentic turn with zero rounds. The verb needs an object-shaped continuation.
- **N2 — pasted R script comment fires the Tier-1 file/document arm.** `FILE_ACCESS_PATTERNS[0]` matches `"read csv data file"` inside the user's pasted `#read csv data file into data frame` comment (probe: records 16 and 30). Eight turns (16, 17, 30, 33, 34, 35, 37, 38) ran `computation, tools` with zero rounds on this alone. Code-shaped lines must be stripped before the file-intent test, and a hit buried mid-body of a >30-word message is incidental.
- **N3 — thinking-only content stream leaves no record.** The 15:33 turn: `parse_thinking_block` returned an empty answer, the handler neither recovered nor recorded. Contract in batch 6.

**Rejected / deferred from Codex's plan (with reasons):**

- Step 1.3 full "canonical answer body" refactor → reduced to heuristic removal + parity test (the raw→sanitize storage path is already the canonical source; guards append to both variables).
- Step 2.4 routing-reduction evaluation, step 6 (global metering, soft budgets, capability-based routing, guidance pruning), step 5.3 call-site cost aggregation → DEFERRED until batch 4 receipts exist to measure against.
- Step 3.1 "session document contract on `api/state.py` identity" → ACCEPTED in reduced form: an in-memory registry keyed by (display name, sha256), cleared with the chat session, no restart persistence (restart falls back to today's retrieval path; documented, not assumed).
- Step 7.1 synthetic behaviour eval set, 7.2 task-scoped "don't solve" preference → owner decisions, not defects.
- Step 8.2 contacts re-auth → only the reason string is surfaced; no auth changes.

## ACTIONS — measured findings

### F1. Valid answer code is deleted after generation — highest priority

**Confirmed in source, exported answers and runtime logs.** `gui/handlers.py:3283` applies a line-removal heuristic when the agentic session has no tool rounds. A line with fewer than four words is discarded unless its punctuation/prefix passes a whitelist. This is not evidence that the line is a tool call.

The log contains **47 removed lines across 13 replies**. Examples: record 12 loses `print(y_hat - resid)`; record 20 loses `coef(model)`; record 22 loses the assignment defining `coef_table` and its equality check; record 31 loses a degrees-of-freedom assignment and the second half of a data-frame expression; records 37–38 lose the opening assignment and closing parenthesis. Code fences are also removed. Relevant log anchors: 15:24:37, 15:45:31, 16:25:28 and 16:50:05, all tagged `Stripped bare tool-call line`.

There is a second defect: cleanup changes `display_output`, while persistence at `gui/handlers.py:3525–3537` normally starts from `final_output`. The final debug response is synchronized to display at line 3506, so **the chat/debug answer can be broken while subsequent retrieved conversation history contains the intact original**. This explains why an audit of only `last_prompt.txt` can miss the user's experience. Guard rewrites can sometimes resynchronize the variables; divergence is conditional, not universal.

### F2. Document continuations lose access through two independent routes

**Record 2, 15:01:** a status preface followed by an explicit request to show the first question is classified as a self-report. Direct invocation gives `is_self_report=True`, `is_request_shaped=False`. `daemon_debug.log:141706–141714` records the resulting retrieval trim, including `max_user_uploads=0`, and the memory-search backstop suppresses tool routing. The PDF question body has already been middle-out trimmed from history. Daemon asks for another upload.

Relevant seams: `utils/query_checker.py:480` (`is_self_report`), `core/agentic/gate.py:1440` / `:1547` (largely opener-based request tests), `core/prompt/builder.py:549–629` (self-report overrides), `core/agentic/gate.py:1264` (memory backstop). The current request predicate recognizes questions and selected cues, but misses an imperative later in this particular multi-clause message. Do not fix this by matching every quoted imperative anywhere in a paste.

**Record 8, 15:13:** “ok next q please” is classified as a casual acknowledgment. Direct invocation of `is_casual_acknowledgment` returns True. `builder.py:993` selects `_build_lightweight_context`, which retains three recent exchanges and explicitly empties `user_uploads` (`:2267`). The original document is outside that short window. Gate says `casual/short message`; the answer asks the user to paste it again and speculates about the next task.

The upload roster cue in `core/prompt/gatherer_knowledge.py:100` recognizes numbered `question/task/part/page` and document nouns, but not ordinal/elliptical continuations such as “first question” or “next q”. Fixing that regex alone cannot repair the earlier zero-upload/light-path decisions. Records 3 and 9 succeed after reattachment: useful positive controls. September 7's upload-only retrieval pool and title roster already shipped and should be preserved.

### F3. Planner cannot see the evidence it is directing the answer to use

**Confirmed.** `ResponsePlanner._CONTEXT_DIGEST_KEYS` (`core/response_planner.py:181`) omits `user_uploads`. The 6,000-character sequential digest can also exhaust its allowance on STM/history/profile/narrative before later source sections. `core/orchestrator.py:1638–1645` gives the planner the original request and this digest; current attachment bodies are not a dedicated planner evidence channel.

Direct invocation with only `{'user_uploads': [{'content': 'TASK_1_MARKER'}]}` yields **empty digest and empty section list**. In records 3 and 9, the full question text is attached, but the plan says to request the wording again. At 15:02 the review gate penalizes the correct answer for using that available text without asking for it. Record 26's plan also instructs the model to apply a nonexistent standard-error function to a coefficient vector; the answer fortunately corrects it.

This is both transport and planning-authority risk. A model-generated plan must not override the current source or turn a user's tentative understanding into a prescribed fact.

### F4. A statistics question triggers longitudinal personal analysis

**Reproduced by calling `detect_insight_request`.** Record 28 is a question about a t-distribution percentile. `_IMPLICIT_PERSONAL_COMPARISON_RE` (`core/insight/detector.py:252`) matches the span beginning “am i just finding” through “doing a bit more”, crossing the intervening question mark. `_detect_deliberation_shape` (`:289`) returns that broad match; `detect_insight_request` routes it to `pattern_temporal` (`:520`). Neither a personal-record source nor a longitudinal phenomenon is required by this arm.

Runtime: **31.865 seconds evidence assembly**, **45.579 seconds recorded wall time**, 36 evidence items including unrelated historical distress and four PubMed items. The final answer happens to explain the math correctly; that does not validate the routing. The insight timing omits the ordinary preparation that already occurred before `_run_insight_mode`, so it is not necessarily full ingress-to-delivery latency.

### F5. Remote sandboxes start even when no code will execute

**Confirmed.** `core/agentic/controller.py:509–519` acquires a persistent sandbox before the first decision whenever the backend is configured. In the incident window, logs show **13 sandbox creations and 12 closures explicitly reporting zero executions**. All 19 exported agentic records have empty tool rounds. Repeated E2B DELETE 404s are cleanup of expired handles, not proof that a requested computation failed. Creating/recycling unused sandboxes costs latency and potentially provider charges; dollar amounts cannot be inferred from this dump.

Creation belongs at actual sandbox dispatch. Preserve the session-reuse and dead-handle recovery contracts in `_get_sandbox_session` (`controller.py:263`) and `knowledge/sandbox_manager.py`; move the point where they are invoked.

### F6. Routing generates unnecessary decision/synthesis work

**Observed; savings require measurement.** There are 21 enhanced, 19 agentic and one insight records. All 19 agentic turns use `answer_call=final_synthesis`, all have zero recorded tool rounds, and all skip decision reuse because admitted retrieval evidence was absent from the decision prompt. Even ordinary local-language questions such as clearing an R environment or retrieving coefficients enter the tool loop.

Record 40 is a casual plans update. The entity/recall tier correctly stands down, but the LLM trigger sets memory intent. The deterministic backstop only applies when `is_self_report` recognizes the message; direct invocation returns False for this elliptical update. The query has 32 words, so this particular miss is **not** its 40-word cap.

Do not undo September 7's evidence-visibility reuse guard to save calls. Irrelevant retrieved notes triggering that guard are a source-selection problem. Prefer correct initial routing and a shared evidence-bearing answer path. A no-tool answer may still require admitted document evidence; zero rounds is not proof that context is unnecessary.

### F7. Exported prompt and token totals describe a different call

**Confirmed.** `gui/handlers.py:3390–3410` counts/exports the base `full_prompt` and system prompt. The controller constructs the actual answer prompt with `_build_final_prompt` (`controller.py:2676`) and already keeps `_last_final_prompt` / `_last_final_system_prompt`. Those actual payloads are not the prompt fields exported by this handler.

Example: record 40's exported base prompt includes calendar and graph sections, while its actual-answer receipt says both were omitted. Every agentic record lists calendar omission. Some list graph or email omissions. `_FINAL_PROMPT_DIRECT_RENDERED_KEYS` (`controller.py:1786`) and the renderer confirm these gaps. The receipt itself is useful evidence and should remain.

Every exported `token_count_kind` is `local_input_estimate`. The sum of `total_tokens` is **782,829**, but this is **not billed usage**: it sums base input estimates and does not include all decision, generation, verifier, STM, planner, or sandbox costs. Recorded wall times sum to **1,139.659 seconds**; these are handler measurements, not total user waiting time. Median recorded preparation is **10.152 seconds** and median base system estimate is **7,249 tokens**.

### F8. Large attachments and excess background compete with task evidence

**Observed with source support.** Records 10 and 14 have base input estimates of 53,548 and 44,993 tokens. Their current-query tails are approximately 165K and 125K characters, largely attached lectures. At 15:18:59 the actual agentic final prompt logs **40,559 / 40,000 tokens after trimming**. `controller.py:2888` warns and proceeds once its limited removable-section list is exhausted. A comment says 2× while the executable ceiling is 5× the tool-context budget.

The current turn merges full attachment text before generation (`gui/handlers.py:4455` onward); the context budget cannot bound arbitrary material appended as the current query. Conversely, following turns can lose the exact task passage through history compression. A hard global reduction would worsen F2. Use bounded, source-addressable active-document context and explicit completeness, keeping full source retrievable.

Record 40 also retrieves years-old notes with little connection to the query, a large profile, and unrelated graph edges. Similar excess background appears on code questions. This establishes relevance/cost candidates, not an experimentally justified replacement token budget. Preserve useful personalized memory; do not turn Daemon into a memory-free homework bot.

### F9. Answer quality and scope issues survive the current checks

- Record 0 assumes the user is on Windows despite the request expressing uncertainty; record 1 corrects it. The plan had explicitly warned against assuming the OS.
- Records 20, 32 and 37 claim ten predictors / eleven coefficients. The supplied formula names **nine predictors**, and record 27's actual R error reports ten rows. This is a model-origin count error; don't blame R or silently change the source formula.
- Record 35 calls the third flat matrix element HP; with the shown intercept/Age/KM ordering it is KM. Its “two issues” explanation describes one indexing error twice.
- Record 14 predicts exact equality of residual arithmetic with unjustified confidence, and claims knowledge of what graders want. Records 0/10 likewise infer grading restrictions beyond the quoted source. Treat these as grounding/evaluation cases, not reasons to hardcode R facts into the application.
- Record 9 was asked for the next question and supplied a worked code solution. Earlier the user had requested not to solve the assignment. Later requests do explicitly ask for code/debugging help: preserve that distinction rather than imposing an irreversible “never give code” flag.
- Records 39–40 add unsupported ease/time estimates and justify rest through prior productivity despite an existing contextual-grounding instruction. Record 40 introduces an unrelated appointment into a casual closing. Record 38's question 1–5 validation statement exceeds the execution evidence available to Daemon.
- Record 29's log-only grounding verdict restates the same correct formula as a correction; the log contains similar questionable flags for records 30 and 37. **Do not enable automatic verifier rewriting** as a shortcut. The planner/reviewer blind spot in F3 must be fixed and precision evaluated first.

### F10. Existing memory contamination is still visible

Record 40's profile includes an intended action stored as completed and office hours stored as a fitness schedule. The uncommitted `memory/fact_source.py` changes and existing `test_sep08_profile_fact_misfires.py` already target those write-path defects. A passing write-path test does not repair stored records.

The dump also contains stale deadlines, unrelated historical states, polluted graph identity edges, and lecture-derived statements presented as user facts in record 28. Its narrative misstates the assignment part/task count, although current direct messages correct it; a narrative-caution marker is present. Source ownership, completion status, event time and current relevance must survive retrieval. Do not infer new personal events from the erroneous records or automatically purge stores.

### F11. Export redaction damages hashes and misses rendered profile fields

**Reproduced.** Seven response-plan audit objects contain `[REDACTED ID]` inside hash values. The nine-digit regex at `utils/privacy_redaction.py:79` matches digit runs embedded in hexadecimal strings; `redact_text('abc123456789def')` becomes `abc[REDACTED ID]def`. This destroys reproducible planner receipts.

Meanwhile the rendered profile's `birthday=`, `location=` and `lives_in=` forms are not covered by the current `DOB`/`address` rules and remain in the export. Redaction is not anonymization: medical/personal prose and names also remain by the module's stated policy. Fix the structured-field mismatch and artifact integrity together without weakening existing credential/phone/ID protection or promising anonymity.

### F12. Operational anomalies and evidence gaps

- **32 semantic-search timeouts** appear in the incident window. The repeated 1.5-second path is worth investigating before raising any timeout: cancelled executor work can continue in its worker. Already implemented wiki isolation must be preserved. Inspect queue wait versus work duration and source usefulness.
- Contacts API returns 403 on both saved and other-contact surfaces early in the session. No contact lookup was requested by the reviewed turns. Record as degraded integration health; distinguish API enablement/scopes/permission responses locally before deciding whether reauthorization is needed.
- At 15:33:27, between exported records 15 and 16, the log says an empty assistant response was skipped by storage. This attempt is absent from the 41-record export. It may be cancellation, streaming failure or incomplete capture; **cause unconfirmed**. Inspect ingress/SSE termination and conversation logs, then create a regression for the established cause.
- Warnings about absent images occur on text-only requests. These are logging noise unless an image was actually expected. The massive handler/model payload logs already noted in prior handoffs also deserve bounded, opt-in diagnostic capture.
- Records 10/14 show successful duplicate-attachment suppression; do not label those warnings as failed ingestion. Reattachment alone does not prove persistence failed: F2 already explains two retrieval losses, and persistence is asynchronous.

## PLANNED — Fable's execution batches (replaces Codex's steps 0–8)

Order: **B1 → (B2 ‖ B3) → B4 → B5 → B6.** B1–B4 are deterministic, small, and independent of any design decision; B5 is the design batch (contract frozen below); B6 closes operational unknowns. Each batch = one commit boundary (workflow §3a): touched suites + five repo-wide guards + ruff, then the owner commits BEFORE the restart. File ownership is disjoint within a wave; anything shared is serialized.

Doctrine that holds for every batch: tests call THE deployed function; synthetic names/documents only; no store writes; preserve the dirty tree (calendar-offer + fact-source batches); `# lazy import:` reasons on any new function-body import.

### B1 — Stop answer corruption (F1) — `gui/handlers.py` only

1. Delete the no-round short-line loop at `gui/handlers.py:3283-3316` (the `if (not _had_real_rounds and display_output and not ctx.telemetry.get("agentic_narration_recovered"))` block). Keep `strip_thinking_tag_leaks`, `_strip_leaked_xml_blocks`, `strip_trailing_stream_artifact`, and the narration recovery exactly as they are.
2. Replace it with `_strip_bare_tool_name_lines(text)` (module-level, pure): remove a line ONLY when, outside a fenced code block, the whole stripped line is `<tool_name>` or `<tool_name>(…)`/`<tool_name>: …` where `<tool_name>` ∈ the closed set of agentic tool names (derive from `core/agentic/types.py` tool definitions + `DISPATCH_TABLE` handler names — build the set once at import; never a word count, never punctuation). Same no-round condition as before.
3. Parity: right before `_dispatch_storage`, assert-by-construction that the stored text derives from the same body the user saw: `final_output_sanitized` must contain every fenced block and every non-empty line of `display_output` minus display-only decorations (WEB_N link markup, Sources footer). Implement as a helper `_answer_bodies_agree(display, stored) -> bool` used by the test AND logged at WARNING on mismatch in production (log-only; never mutate).
4. Tests `tests/unit/test_sep08_agentic_answer_integrity.py` (drive the deployed `_run_agentic_search` through the `test_handle_submit.py` harness — `_make_orchestrator` + a fake controller whose final stream is fixed): (a) zero-round stream with `` ```r ``, `print(y_hat - resid)`, `coef(model)`, `)`, a one-word answer `TRUE`, a markdown table, `ls -la` — every line survives in SSE content, debug `response`, and the stored text; (b) a real leaked `list_repos` line and `web_search("x")` line are removed; (c) an action card and a grounding suffix still reach both display and storage; (d) `_strip_bare_tool_name_lines` leaves `coef(model)` alone. Replay record-12/22/31/37 shapes with synthetic variable names.
5. Regression suites: `test_handle_submit.py`, `test_stream_artifacts.py`, `test_grounding_wiring.py`, `test_narration_turn_audit_fixes.py`, `test_sep07_calendar_offer_continuation.py`.

### B2 — Request scope + routing precision (F2 predicates, F4, F6 backstop, N1, N2) — `utils/query_checker.py`, `core/agentic/gate.py`, `core/insight/detector.py`

1. **Clause-level request detection.** New `query_checker.request_clauses(text) -> list[str]`: split on `[.!?;]`/newlines after `fact_source.strip_quoted_correspondence` and after removing fenced/code-shaped lines (new shared helper `strip_code_shaped_lines`: fenced blocks, lines starting with `#`, `>`, `$ `, `>>>`, lines containing `<-`). `is_self_report` returns False when ANY clause is request-shaped (`is_request_shaped(clause)`, `_ADDRESSING_ASSISTANT_RE`, `_IMPERATIVE_REQUEST_RE.match`, or a clause beginning with `please`). Record 2 → False. Contracts in `test_self_report_predicates.py` stay green (Q1 has no request clause).
2. **Task navigation.** New `query_checker.is_task_navigation(text)`: `\b(next|first|previous|prev|last|following|second|third)\s+(q|question|task|part|problem|step|section|page|one)\b`, `\bq\s*\d+\b`, `\b(question|task|part|problem)\s+\d+\b`, word-bounded, negation-guarded via `utils/trigger_match`. `is_casual_acknowledgment` returns False when `is_task_navigation` hits or the message contains `please`. Record 8 → False; "ok", "ok cool", "hmm still broken", "got it sorry frustrated lol" stay True.
3. **N1.** `_REQUEST_SHAPED_RE` verb alternative gains `(?!\s*(?:not|is|isn'?t|was|wasn'?t|does|doesn'?t|did|didn'?t|=|<-|\(|\.))` after the verb. "read not a function…" → False; "read the file", "check it out now", "pull up the veto logic" → True.
4. **N2.** The Tier-1 file/document arm evaluates `_FILE_ACCESS_KEYWORD_HIT`/`FILE_ACCESS_PATTERNS` on `strip_code_shaped_lines(user_text)`, and for messages over `REQUEST_CONTINUATION_MAX_WORDS` a hit counts only in the head/tail 200 chars (mirror `core/insight/detector._trigger_is_incidental`). Record 16/30 shapes → no file intent; "can you pull up the notes doc? here's my script: …" → file intent.
5. **F4.** `_IMPLICIT_PERSONAL_COMPARISON_RE`: every `.{0,N}` becomes `[^.?!\n]{0,N}`; `more|less` leave the first alternation and return as `\b(?:more|less)\s+(?:than\s+(?:before|usual|normal|i\s+used\s+to)|lately|recently|these\s+days|over\s+time)\b`. Record 28 → None; all `test_pattern_routing_generic.py` positives stay `pattern_temporal`; "Has my sleep changed since I moved?" and "Does my mood track with exercise?" unchanged.
6. **F6 backstop.** In the gate's `needs_memory_search` branch (`gate.py:1264`): honor the LLM verdict only when `_recall_signal_hit(_lower)` OR `_is_info_seeking(user_text)` OR `_is_request_shaped(user_text)`; otherwise suppress (statement-shaped narration: records 23, 40). Nothing is taught. `test_gate_self_report_memory_backstop.py` contracts (recall cue, question) stay green; add records 23/40 shapes.
7. Tests: `tests/unit/test_sep08_request_scope.py` (each predicate on the record shapes + paired counterexamples: forwarded email with an imperative inside a blockquote → no request; a code comment `#read the file` → no file intent; "compare how I was before and after night shift" → still insight). Regression: `test_self_report_predicates.py`, `test_light_prompt_path.py`, `test_gate_self_report_memory_backstop.py`, `test_pattern_routing_generic.py`, `test_insight_gate.py`, `test_prompt_builder_self_report_trim.py`, `test_sep07_followups.py`, `test_sep07_calendar_offer_continuation.py`.

### B3 — Lazy sandbox acquisition (F5) — `core/agentic/controller.py`, `core/agentic/tools.py`

1. `controller.run()`: replace the eager `_get_sandbox_session()` at `:509-519` with `sandbox_session = self._lazy_sandbox_supplier() if sandbox_available else None`, where the supplier is a tiny `LazySandboxSession` (in `tools.py`, beside the dispatch table): `async def get()` calls the controller's existing `_get_sandbox_session` on first use and caches; `is_closed` proxies the underlying session when acquired.
2. `ToolExecutor._dispatch_sandbox` (`tools.py:465`): `if isinstance(sandbox_session, LazySandboxSession): sandbox_session = await sandbox_session.get()` before the existing `if sandbox_session and not sandbox_session.is_closed`. Both routers (`ToolExecutor.dispatch_single`, `controller._dispatch_single_inner`) pass the same object through `DISPATCH_TABLE`; no other handler touches it.
3. Preserve `_get_sandbox_session` recycling (age/is_alive), `close_sandbox`, and the "Using persistent sandbox session" semantics (log moves to first acquisition).
4. Tests `tests/unit/test_sep08_lazy_sandbox.py` with a fake `sandbox_manager` (counts `create_session`, fake session with `run/is_closed/is_alive/age_seconds/close`): configured + no sandbox decision → 0 creates; first sandbox decision → 1; two sandbox decisions in one run → 1; expired session → recreate once; `create_session` raising → honest tool error in the round; `close_sandbox` idempotent. Regression: `test_tool_wiring_parity.py`, `test_agentic_*`, `tests/unit/test_dispatch*`.

### B4 — Answer-call receipts + artifact-safe redaction (F7, F11) — `gui/handlers.py` (after B1), `api/routes/debug.py`, `utils/privacy_redaction.py`

1. **F11.** `(?<!\d)\d{9}(?!\d)` → `(?<![0-9A-Za-z])\d{9}(?![0-9A-Za-z])` (a standalone token — hex hashes untouched; `GTID 903123456` and `id: 903123456` still redacted). Add structured profile-field rules: `(?i)\b(birthday|birth\s*date|born)\s*[:=]\s*[^\r\n,;]+` → `\1: [REDACTED DOB]`; `(?i)\b(lives?_in|lives\s+in|location|home\s*town|hometown)\s*[:=]\s*[^\r\n,;]+` → `\1: [REDACTED LOCATION]`. Idempotence test on the replacement forms; existing `test_privacy_redaction.py` stays green; add a 64-hex SHA and a `plan_sha256` object that must survive byte-identical.
2. **F7.** Agentic debug record gains `answer_call` (session), `answer_prompt` (`controller._last_final_prompt`), `answer_system_prompt` (`_last_final_system_prompt`), `answer_prompt_tokens` (same `_safe_count_tokens`), `answer_prompt_hash`; base `prompt`/`system_prompt`/`total_tokens` keep their names, and `token_count_kind` stays `local_input_estimate`. `build_redacted_prompt_export` writes the ANSWERING CALL payload first (labelled with `answer_call`), then the BASE RETRIEVAL PROMPT; both system prompts obey `include_system`. Legacy records without the fields export unchanged. The `/api/debug` records carry the new fields through `redact_data`.
3. Tests: `tests/unit/test_sep08_answer_call_export.py` — fake controller with a distinct final prompt → record fields present, export contains both payloads with labels, hash intact after redaction, legacy record export unchanged. Regression: `test_api_debug_settings.py`, `test_evidence_transport.py`, `test_agentic_decision_answer_reuse.py`.

### B5 — Bounded active-document continuity (F2 access, F3, F8) — new `core/active_document.py`; wiring in `gui/handlers.py`, `core/response_planner.py`, `core/agentic/controller.py`, `utils/attachment_audit.py`

**Frozen contract (Fable):**

- `ActiveDocumentRegistry` (leaf module, in-memory, attached as `orchestrator.active_documents`; cleared by `ChatSession.clear()` in `api/state.py`; NOT persisted across restart — after a restart the existing roster/retrieval path applies, and the doc says so). Entry: `doc_id`, `display_name` (`attachment_display_name`), `kind` (extension), `sha256`, `char_count`, `text` (capped `ACTIVE_DOCUMENT_MAX_CHARS`=300_000), `registered_turn`, `last_selected_turn`, `items` = `split_numbered_items(text)` (line-anchored `Question N`, `Q N`, `Task N`, `Problem N`, `Part N`, `N.`/`N)`; label + span), `served` (set of labels). Register from `_handle_submit_inner` right after `process_files_structured` for every non-image, error-free document; same (name, sha) refreshes, never duplicates; LRU cap 12.
- `resolve_navigation(user_text, turn) -> ActivePassage | Ambiguous | None`, fired only when `query_checker.is_task_navigation` (B2) hits or the text names a registered filename. Candidates = documents with ≥2 numbered items; an explicit filename wins; exactly one candidate → it; several → `Ambiguous(names)`; none → None. Target item: explicit number → that; `first` → 1; `next` → last served + 1 (1 if none); `previous`/`last` accordingly; beyond range → `None` with `exhausted=True`. Passage bounded at `ACTIVE_PASSAGE_MAX_CHARS`=4000 with a `[passage truncated — full text via get_full_document(title="upload:<name>")]` tail; `complete` flag.
- Injection (handlers, before mode routing, after `ctx` is built): a resolved passage is appended to BOTH `merged_input` and `analysis_text` as `[ACTIVE DOCUMENT — <name>, <label> (<k> of <n>)]\n<passage>`; `Ambiguous` appends `[ACTIVE DOCUMENT NOTE] Several attached documents contain numbered items: A.pdf, B.docx — ask which one before answering.` Because the appended text is long, `is_casual_acknowledgment`/`is_self_report` no longer fire and the light path is bypassed without touching the builder; the passage lands in [CURRENT QUERY] for BOTH the enhanced and agentic routes. Never chooses by newest timestamp alone; explicit filename/part beats inferred continuity.
- **F3.** `ResponsePlanner._CONTEXT_DIGEST_KEYS` gains `user_uploads` FIRST with a reserved allowance (`max_section_chars` for that key = 1500, taken before the sequential fill), label `user_uploads — current attachments`. Probe: `build_context_digest({'user_uploads':[{'content':'TASK_1_MARKER'}]})` returns the marker.
- **F8.** `controller._build_final_prompt`: after the trimmable list is exhausted and the assembled prompt is still over `prompt_ceiling`, middle-out the `[CURRENT USER QUERY — RESPOND TO THIS]` part with `TokenManager` to fit, inserting `[… N characters of attached material omitted from this call; full text retrievable via get_full_document(title=…) …]`, and log exactly what was cut. The user's own words (head) and the tail survive; the ceiling multiplier is NOT changed.
- `audit_attachments(user_text, files, documents, available_documents=None)`: when a referenced file is not attached this turn but IS in `available_documents` (the registry's names), the note reads "previously attached this session" instead of "not attached"; pure helper, no store access.
- Tests `tests/unit/test_active_document.py`: item splitting on a synthetic 3-question doc; resolve first/next/explicit/previous/exhausted; ambiguity with two question-bearing docs; filename override; sha dedupe; LRU; passage bound + truncation marker; clear on session reset. `tests/unit/test_sep08_document_continuity.py` (handle_submit harness): upload a synthetic multi-question doc, ask "show me the first question", interleave two code questions, then "ok next q please" with no files → [CURRENT QUERY] carries Q2, no light path, no re-upload request in the injected note; two docs → the ambiguity note; planner digest carries the uploads marker; the F8 overflow test with a 200K-char synthetic query keeps the head and the omission marker. Regression: `test_sep04_attachment_turn.py`, `test_sep04_evening_fixes.py`, `test_upload_retrieval_pool.py`, `test_response_planner.py`, `test_light_prompt_path.py`, `test_handle_submit.py`.

### B6 — Operational unknowns (F12, N3) — `gui/handlers.py`, `core/actions/google_contacts.py`

1. **N3.** Enhanced path: when `parse_thinking_block` leaves an empty answer (content-channel thinking-only stream), run ONE recovery via `model_manager.generate_once(disable_reasoning=True)` (the same contract as `ResponseGenerator._recover_reasoning_only`); if still empty, yield the existing "Empty Response" notice, append a debug record with `response=""` and `answer_call="empty"`, and do NOT dispatch storage. Test with a fake stream of `<thinking>…</thinking>` only.
2. Contacts 403: include the Google error `status`/`message` in the warning and in `get_runtime_action_health()` so the owner can tell "API not enabled" from "insufficient scope"; no auth action.
3. Semantic timeouts (32×1.5 s): read-only report in this doc's results section — which executor, queue wait vs work, whether the in-flight semaphore skipped; no timeout raise, no extra workers.
4. Closeout per batch: `CLAUDE.md` one-liner, `CLAUDE_CHANGELOG.md` entry, memory note, `commit_message_10.txt`; owner commits, restarts, then the live probes: agentic reply with fenced R code intact; upload→interleaved→"next q please"; the record-28 sentence answers in enhanced mode; record-16 paste runs no tool loop; sandbox created only on a real code request.

## Verification commands

```bash
# per batch (add the batch's new test file), then the five guards
DAEMON_TEST_MODE=1 python -m pytest <batch suites> -q --color=no -p no:cacheprovider
DAEMON_TEST_MODE=1 python -m pytest tests/unit/test_no_git_state_in_tests.py tests/unit/test_ordered_slice_guard.py tests/unit/test_budget_meters_rendered_sections.py tests/unit/test_tool_wiring_parity.py tests/unit/test_model_capability_wiring.py -q --color=no -p no:cacheprovider
ruff check <changed files>
bash scripts/agent_session_audit.sh
```

Results are recorded below as they happen; a proposed check is not a passing result.

## RESULTS (filled in by the executing session)

### Wave 1 — B1, B2, B3, B4 SHIPPED in the working tree (2026-09-08 evening; two Sonnet subagents, Fable referee)

- **B1** `gui/handlers.py`: the word-count line stripper is gone; `_strip_bare_tool_name_lines` removes only a line that IS a bare agentic tool name/call (closed set from the tool schemas + `DISPATCH_TABLE`, fence-aware); `_answer_bodies_agree` log-only parity guard before storage. Referee probe on the record-12/22/31/37 shapes: fences, `print(y_hat - resid)`, `coef(model)`, `)`, `TRUE`, table rows all survive; `web_search("x")` and a bare `github` line are removed. Tests: `tests/unit/test_sep08_agentic_answer_integrity.py` (14).
- **B2** `utils/query_checker.py` (`strip_code_shaped_lines`, `request_clauses`, `is_task_navigation`; clause-level request test in `is_self_report`; `is_casual_acknowledgment` refuses navigation/"please"), `core/agentic/gate.py` (N1 lookahead on the retrieval verb; N2 file arm on code-stripped text with head/tail scope over 30 words; F6 backstop = LLM memory verdict needs a recall cue OR info-seeking/retrieval shape OR the broader `query_checker.is_request_shaped` imperative family), `core/insight/detector.py` (no sentence crossing; `more/less` need a longitudinal qualifier). Deployed-function probes after the change: R2 self_report=False, R6 request_shaped=False, R8 casual_ack=False, R16 no file intent, R23/R40 memory suppressed, R28 → None. Referee caught and fixed one regression in the first cut: imperative recall requests without "?" ("remind me about…", "describe my…", "give me a rundown of my…") were suppressed until the imperative family was added to the corroboration. Pre-existing, unchanged: a message under 10 words with no search signal never reaches the Tier-4 branch at all. Tests: `tests/unit/test_sep08_request_scope.py` (55).
- **B3** `core/agentic/tools.py` `LazySandboxSession` + resolve in `_dispatch_sandbox`; `controller.run()` no longer awaits `_get_sandbox_session` before round 1. Fake-backend tests: no sandbox decision → 0 creates; one/two sandbox rounds → 1 create; expired session recreated once; `create_session` failure degrades to the ephemeral path; `close_sandbox` idempotent. Tests: `tests/unit/test_sep08_lazy_sandbox.py` (11).
- **B4** `utils/privacy_redaction.py`: nine-digit ID rule bounded on `[0-9A-Za-z]` (a 64-hex SHA and `plan_sha256` survive byte-identical; `GTID 903123456` still redacts); `birthday|birth date|born` and `lives_in|lives in|location|home town|hometown` structured rules, idempotent. `gui/handlers.py`: agentic debug record gains `answer_call`, `answer_prompt`, `answer_system_prompt`, `answer_prompt_tokens`, `answer_prompt_hash` (base fields unchanged); `build_redacted_prompt_export` renders the ANSWERING CALL payload first, then BASE RETRIEVAL PROMPT; legacy records export unchanged; `/api/debug` needed no change. Tests: `tests/unit/test_sep08_answer_call_export.py` (6) + `test_privacy_redaction.py` extended.
- Combined referee run (all new + regression suites + the five repo-wide guards): **688 passed**, ruff clean on every touched file. `scripts/agent_session_audit.sh` clean for both agents.
- Commit note: the calendar-offer (commit_message_8) and profile-fact (commit_message_9) batches were still uncommitted in `gui/handlers.py`, `core/agentic/gate.py` and `utils/query_checker.py` when this wave landed, so their hunks now interleave with B1/B2/B4 — one combined commit is the honest history (workflow §3a.4).

### Wave 2 — B5, B6 SHIPPED in the working tree (2026-09-08, Sonnet subagent)

- **B5** new `core/active_document.py` (leaf module: stdlib + `utils.logging_utils` + `utils.trigger_match`): `split_numbered_items()` chooses the word-labelled family ("Question N"/"Task N"/"Part N"/bare "Q N") over the bare-number family ("N."/"N)") when it yields ≥2 items, keeping only the first strictly-increasing run (a restart ends the run — the nested-bullet-fragmentation guard); `ActiveDocumentRegistry` (dedupe by `(name.lower(), sha256)`, LRU cap 12, `next_turn()` counter) and `resolve_navigation()` (explicit number / first / next / previous / last, word-bounded + negation-guarded via `utils.trigger_match.is_negated`, candidates = registered docs with ≥2 items, a mentioned filename — boundary-checked, not a bare substring, so a one-letter stem like "A" can't match inside unrelated words — narrows the pool, several unnamed candidates → `Ambiguous`, out-of-range → `Exhausted`); passages bounded at 4000 chars with a `get_full_document(title="upload:<name>")` truncation marker. Wired into `gui/handlers.py._handle_submit_inner`: registers every text attachment from the turn, then ALWAYS attempts navigation (not gated on "no files this turn" — the doc just registered is already a candidate, so "please show me first question" in the SAME message as the upload resolves too, not just a later re-ask); the resolved note is appended to both `merged_input` and `analysis_text` (bypasses the light path / self-report trim without touching the builder) and recorded in `ctx.telemetry["active_document"]`. `api/state.py`: `AppState.__init__` creates the registry, attaches it to the orchestrator and to `ChatSession` (whose `clear()` now clears it too — session-scoped, never persisted). `utils/attachment_audit.audit_attachments()` gained `available_documents=`: a referenced-but-missing name that matches a previously-registered document reads "Previously attached this session (available on request)" instead of joining the generic missing-files list. F3: `ResponsePlanner._CONTEXT_DIGEST_KEYS` gained `user_uploads` FIRST with its own reserved 1500-char allowance rendered BEFORE the fair sequential loop (so a large STM/history/profile digest can't crowd out the current attachment); roster-only marker items (`metadata.type == "upload_roster"` with empty content) are skipped. F8: `AgenticSearchController._build_final_prompt` — when the section-trim ladder is exhausted and the prompt is still over the `*5` ceiling, `_squeeze_query_part_for_ceiling()` middle-outs ONLY the `[CURRENT USER QUERY]` part's body (via `TokenManager._middle_out` when attached, else `utils.text_budget.fit_text_to_tokens` over `_estimate_tokens`) and swaps in a `get_full_document`-aware omission marker in place of the generic "middle-out snipped" one — the marker's own token cost is reserved up front (worst-case 7-digit cut count) so swapping a LONGER custom marker in after the fit can't push the result back over budget (caught by the test at 40006/40000 on the first pass). The `*5` multiplier is unchanged; every other section is untouched. Tests: `tests/unit/test_active_document.py` (37), `tests/unit/test_sep08_document_continuity.py` (11, through the deployed `handle_submit` harness + direct planner/controller probes).
- **B6.1 (N3)** `gui/handlers.py` enhanced path: when the entire streamed response is a `<thinking>...</thinking>` block with nothing after it (content-channel thinking-only stream — the `elif not final_answer_stream:` branch that used to just set `final_output=""` and tell the user to retry), ONE recovery call via `model_manager.generate_once(disable_reasoning=True, max_tokens=1024)` runs first, using the exact prompt/system prompt the stream used; a non-empty, tag/XML-stripped result becomes the answer and the turn proceeds normally (storage dispatches as usual). A still-empty result yields the existing "Empty Response" friendly notice, appends a debug record via `_build_debug_record(..., extra={"answer_call": "empty"})`, and returns — `final_output` stays `""` so the pre-existing `is_error_response` storage gate in the `finally` block skips dispatch without any new gating logic. Note: an EARLIER, pre-existing "stuck thinking" recovery a few lines above (guarded by `thinking_started and not thinking_complete`) already intercepts the common single-block case with a non-empty body (it tag-strips and re-displays the raw reasoning text as the answer — unchanged, out of scope here) — the new branch is reached when that recovery finds nothing to salvage (verified with a realistic nested-tag leak shape, `<thinking><reasoning></reasoning></thinking>`, a documented real leak pattern per the module's own 2026-07-03 note). Tests: `tests/unit/test_sep08_ops_fixes.py::TestThinkingOnlyStreamRecovery` (3, incl. a `generate_once` exception degrading non-fatally).
- **B6.2 (Contacts 403)** `core/actions/google_contacts.py`: non-200 responses now parse `resp.json().get("error", {})` defensively (`_describe_api_error`, never raises on a missing/malformed body) into `"STATUS: message"` (message truncated to 160 chars) and store it as module state (`_last_error`/`get_last_error()`, cleared on success or `clear_cache()`); the warning line becomes `"[GoogleContacts] API error: HTTP 403 (saved) — PERMISSION_DENIED: People API has not been used…"`. `core/actions/registry.get_runtime_action_health()` surfaces a `"lookup_contact backend: DEGRADED (...)"` line whenever an error is on record (checked independently of Calendar's enabled state, before its early return; silent when no call has ever failed). No auth/retry changes. Tests: `tests/unit/test_sep08_ops_fixes.py::TestContactsErrorDetail` + `TestRuntimeActionHealthContacts` (7); pre-existing `tests/unit/test_google_contacts.py` (27) unaffected.
- **B6.3 (semantic-timeout report, read-only, no code change):** `ContextGatherer._get_semantic_chunks` (`core/prompt/gatherer_knowledge.py:1733`, named `"semantic"` in `builder.py`'s `_timed_task` accounting — the wiki/reference FAISS semantic-chunk leg of the knowledge gatherer mixin) is the task that times out, on the dedicated 2-worker `_WIKI_SEM_EXECUTOR` under `SEM_TIMEOUT_S=1.5s` (`SEM_K=50`). `daemon_debug_20260908_172904.log` (repo root) shows **32 of 44 total turns (73%)** logged "Semantic search timeout after 1.5s" — almost every homework/stats query in this session. The in-flight semaphore skip (`_WIKI_SEM_INFLIGHT`, "Wiki semantic search still running from a previous turn — skipping wiki chunks this turn") **never fired — zero occurrences** in the log. Gap analysis of the 32 timestamps: the minimum gap between consecutive timeouts was **80s** (max 752s) — far longer than any plausible abandoned-worker completion time — so the 2-slot semaphore was never saturated between calls; each of the 32 is therefore a genuine WORK-duration timeout (the search itself exceeding 1.5s), not queue-wait contention from a backed-up executor. The log does not record how long an abandoned worker eventually finished, so whether raising `SEM_TIMEOUT_S` would recover useful wiki chunks — versus these particular queries (long LLM-rewritten/expanded statistics questions) being structurally slow at `k=50` regardless of budget — remains unmeasured. No timeout raise, no extra workers, per instruction.
- Combined run: 353 targeted new+regression tests green (per-batch), 1446 passed / 1 pre-existing skip across a 63-file broad-net regression sweep touching every module in this wave's import graph, ruff clean on all 11 touched files, all five repo-wide guards green, `scripts/agent_session_audit.sh` clean (HEAD unchanged, no data/ writes, no suspicious large files).
- Deviation from the plan text: the B5 wiring prose said navigation resolution runs "ONLY when this turn attached no documents," but the B5 TEST SPEC (turn 1: attach + "please show me first question" → the marker must appear in that SAME turn's prompt) requires the opposite — the freshly-registered document must be a navigable candidate immediately. Implemented as: register first, then ALWAYS attempt navigation (still gated by `is_task_navigation`/a filename mention, so it's a no-op on an ordinary attach-only turn); the test suite encodes this as the source of truth. Not committed, not restarted, per instruction — see OWNER below.

### Live probes after the 19:29 restart (commit 21a914e; owner-run, debug records reviewed by Fable)

| Probe | Outcome |
|---|---|
| 1 fenced R code | PASS — enhanced route, fence + every code line intact in the bubble and stored history. |
| 2 active document | PASS — attach + "please show me the first question…" injected `[ACTIVE DOCUMENT — Homework1-1.pdf, Task 1 (1 of 4)]` and quoted Q1/Q2; two interleaved code questions; "ok next q please" ran the FULL path (no light path, no re-upload request), injected Task 2 and quoted Q3–Q6 verbatim. |
| 3 record-28 sentence | PASS on the fix (no insight-assembly, no PubMed). Still ran agentic `knowledge` with zero rounds, 33.7 s — the deferred F6 class, unchanged. |
| 4 pasted script | PASS — enhanced, "no trigger". |
| 5 sandbox | PASS — one E2B session created (two log lines, one event), output 45. |
| N1 "read not a function" | PASS — enhanced, "no trigger". |

**New findings from the same records — B7 SHIPPED (`tests/unit/test_sep08_homework_tone_misfires.py`, 41; 759 + guards green; `commit_message_11.txt`):**
- **Homework was a "heavy topic" all day.** `HEAVY_KEYWORDS` holds `"ice"` and `_is_heavy_topic_heuristic` matches bare substrings, so every message containing "Price" stored `is_heavy_topic=True`; `_recent_distress_from_history` then armed the distress-sticky floor for 30 min after each pasted script. Telemetry: `distress_sticky_floor` on 9 afternoon turns (16:07–16:57) and on probe turns 7–8 — LIGHT SUPPORT ("don't offer advice") on debugging questions, valence-capped retrieval, PERSPECTIVE MODE injections. Neither the Codex audit nor the Fable review caught it: the debug records carry no tone field; `logs/turn_records.jsonl` does. Substring class, 6th occurrence.
- The MEDIUM crisis keyword `need to use` matched "need to use CDF" at 16:25 → CONCERN via harm score → learned as a tone/concern exemplar; the need-detector keyword teacher learned 13 "perspective" exemplars from homework text today, one containing the injected `[ACTIVE DOCUMENT]` passage. Purge candidates: `data/exemplar_purge_candidates_20260908.txt` (owner, Daemon down).
- "base R.e": `_SINGLE_LETTER_ABBREV_RE` exempted any single capital letter, not just "i.e".
- Three identical temp-titled lecture chunks (pre-09-04 rows) admitted as uploads — content-hash dedupe at retrieval.

**Follow-ups noted, not fixed:** (a) navigation granularity — the registry chose the `Task N` family (4 items) over the twelve numbered questions; "next q" should prefer the finer family when the user says q/question and the finer family exists; (b) `[RELEVANT EMAILS]` admitted two marketing emails on a stats question (passive email leg bar 0.35); (c) F6 agentic `knowledge` on plain R questions (measurement first).

## CONTINGENCY

| Condition | Response |
|---|---|
| Dirty-tree hunks overlap a batch (`gui/handlers.py`, `core/agentic/gate.py`, `utils/query_checker.py`) | Edit around them; never reset/restore; one combined commit if they merge (workflow §3a.4). |
| Clause-level request detection starts matching quoted email/code imperatives | Add the paired quoted/unquoted counterexample to the test file and tighten `strip_quoted_correspondence`/`strip_code_shaped_lines`; do not weaken the predicate globally. |
| `test_pattern_routing_generic.py` positive stops routing after the F4 change | Restore the specific arm with a sentence-bounded span; never re-admit sentence crossing. |
| Lazy sandbox breaks the XML dispatch path | Both routers iterate `DISPATCH_TABLE`; fix the arg builder, not the handler. `test_tool_wiring_parity.py` must stay green. |
| Navigation resolves to more than one document | Ambiguity note; never pick by timestamp. |
| A test starts a real model/store/network call | Stop; fix the boundary stub. |
| Anything under `data/`, privacy, or action authority | Dry-run report, owner applies. |

## OWNER

- Commit at each batch boundary (`commit_message_10.txt` drafted per batch), then restart, then the B6.4 live probes.
- Apply `data/profile_junk_candidates_20260908.txt` (dry-run first, Daemon down) — unchanged from the 09-08 morning batch.
- Decide on Codex's optional items (active-document UI indicator, atomic debug exporter, session anomaly summary) and the deferred routing-reduction measurement.
