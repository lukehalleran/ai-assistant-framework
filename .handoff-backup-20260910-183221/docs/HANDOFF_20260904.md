# Handoff — 2026-09-04 (Fable → Codex / next agent)

Working file for continuing today's work. Delete or fold into the changelog once the batch is committed.

## State

- Branch `master`, HEAD `566cf35`. Working tree has an UNCOMMITTED batch (below). Commits are human-gated: do not commit.
- Daemon: the instance started 2026-09-04 11:25:10 loaded AFTER every working-tree edit (no changed file is newer than its start), so all five fixes are live in it. The 10:59 doc-gen turn predates them.
- CI was red on HEAD (2 parity tests); the fix is in the working tree.
- Retrospective: `docs/BUG_RETROSPECTIVE_20260715_20260904.md` (§6 open queue, §7 prioritized improvements, §9 addendum). Published copy: https://claude.ai/code/artifact/0d08928f-bd32-42df-aa6d-dbcb308a6eb7

## Changes in the working tree (all with runtime tests; targeted suites green)

| Fix | Files | Why |
|---|---|---|
| 1. Query-rewrite config honored + kwarg | `core/orchestrator.py` (`_build_context_pipeline_config`, call-time `REWRITE_TIMEOUT_S` import), `core/context_pipeline.py` (`model_name=` at the `generate_once` call, doc comment on `_rewrite_query`) | YAML `features.rewrite_timeout_s: 0` never reached the pipeline (literal 2.0 under a nonexistent root key); the call raised `TypeError` on every attempt since 2026-01-23 |
| 2. Summarizer kwarg | `core/prompt/summarizer.py` (~168): `model=` removed from the `generate_async` fallback | latent unexpected-kwarg error on the local-model branch |
| 3. Doc-gen negation guard + insight cue | `knowledge/document_generator.py` (`_NEGATION_CUE_RE`, `_trigger_match_is_negated`, `_non_negated_trigger_matches`), `core/insight/detector.py` (`_EVIDENCE_SWEEP_RE`, first-person anchor reused) | "Do not save a document for this" triggered doc generation; an explicit evidence sweep over the user's own conversations was not recognized as an insight request |
| 4. CI red | `gui/handlers.py` (~672): `[OpenAI unavailable` entry in `_API_ERROR_DISPLAY` + bracket-strip special case | prefix registered 09-03 without a display entry |
| 5. Debug records for doc-gen / self-note | `gui/handlers.py` `_run_doc_generation` (~1474-1508) and `_save_daemon_note` (~2323-2356): final yields now carry `"debug": _build_debug_record(...)` | `api/chat_service.py:104` treats a chunk as final only if it has a `debug` key; these modes never produced one, so the SPA Debug view was empty and Provenance 404'd for a doc-gen-only session (since 2026-05-23) |

Tests added/changed: `tests/unit/test_query_rewrite_wiring.py` (7, new), `tests/unit/test_docgen_negation_guard.py` (12, new), `tests/unit/test_prompt_summarizer_generation.py` (+1), `tests/unit/test_handle_submit.py` (doc-gen and self-note assertions), `tests/unit/test_api_chat.py` (+1). Runs: 314 green on the Fix 1–4 set, 99 green on the Fix 5 set. Full suite NOT run (never run it from an agent session; CI runs it on push).

## Owner steps

1. (Already running with the fixes.) 2. Open the SPA Debug view after one turn of any mode — it should now list doc-gen and self-note turns too. 3. Re-send the Daemon relay question (retrospective §8) — the first attempt was misrouted to doc-gen and produced a useless negative-result report at `documents/reports/unspecified-topic-2026-09-04.md` (safe to delete). 4. Commit the batch (the pre-commit privacy hook is case-sensitive on surnames; the diff was grepped clean for the known name list).

## Decisions left to the owner

- Codex recommends DELETING the LLM query-rewrite path in `core/context_pipeline.py` rather than keeping it disabled (redundant with the deterministic rewriter; would move the bge gate vector; can overwrite an upload's `processed_query`). Current state: disabled by config, call corrected.
- Everything in retrospective §6A (data queue) and §6B/§6C (deferred design + ops).

## Ready-to-execute next tasks (each is small and self-contained; see retrospective §5 for evidence)

- N2: lazy E2B sandbox creation — `core/agentic/controller.py` ~499 opens a persistent sandbox at every agentic run; create on first sandbox dispatch instead; silence the e2b client's 404 ERROR log on cleanup.
- N13: `DAEMON_TEST_MODE` sandbox for `core/actions/audit.py` (default log path), mirroring the pending-actions fix (F18).
- N14: apply `memory.utils.is_junk_conversation_doc` in `_get_recent_conversations` (the recent path is unfiltered; the box-test battery resurfaced 09-02).
- N16: rename `data/chroma_db` → `data/chroma_db.stale` (172 KB, stale) and make the store class default match `config.yaml` `chroma_path` (`./data/chroma_db_v4`).
- N11: demote the four leftover WARNING signatures (`NO note_images`, `No images parameter received`, `No images provided`, `AGENTIC SEARCH MODE`) to DEBUG.
- N15: convert the seven `return True/False` test functions named by `PytestReturnNotNoneWarning` to assertions; fix or delete the two files failing collection on `tests.integration.gate_system_helpers`.
- N10: meter `google_calendar` before memories in `PRIORITY_ORDER` or give dated sections a survival floor (a working calendar fetch was trimmed at 9984/10000 on 09-01).

Rules for any agent continuing: no commits, no daemon start, no writes under `data/` or `logs/`, targeted test files only, `DAEMON_TEST_MODE=1 ~/.pyenv/versions/3.11.8/bin/python -m pytest -q <file>`, no real names in code/tests/comments, validation calls the deployed function.

## Added 2026-09-04 ~11:45 from the first post-restart debug dump (enhanced turn, a one-line joke)

- **STM `reference_type: recall` promoted intent to `temporal_recall@0.60` (`intent_source: stm_refined`) on "Lmao that is a very … response hahaha".** Effect: the temporal profile (20 recent turns, 6 summaries, 10 memories, notes, uploads, passive email) + the TEMPORAL RECALL style block for a joke — 12.7K prompt tokens, 14.4 s prepare. Fix in `core/intent_classifier.py::refine_with_stm` (~835): a backward reference to the immediately preceding exchange is not a temporal-recall QUESTION; require the STM `user_question`/`intent` to carry a temporal-question shape (when/how long/what did we/last week…?) before promoting to `temporal_recall`; otherwise leave the regex result. Test: this exact message with an STM dict `{reference_type: 'recall', user_question: 'User is humorously commenting on a previous response'}` stays non-temporal; "what did we talk about last week?" still refines to temporal_recall.
- **[TEMPORAL GROUNDING] narrative asserted "he'd withdrawn from the fall semester"** while the profile (`enrolled_in`), the unresolved threads, and summary #4 ("enrolled only in MGT 6203") all say one course was dropped and one kept. Generated 11:06 today, so it is the CURRENT narrative generator. Fix: extend `daily_notes_generator.build_temporal_claim_audit` / the narrative prompt with a deterministic enrollment-status check — a "withdrew/withdrawn from the (fall|spring|summer) semester|school" claim is dropped or flagged when a current `enrolled_in`/`dropped` profile fact contradicts it; the user's own words were "dropped CSE 6040".
- **[RECENT SUMMARIES] rendered three near-duplicate summaries** (the same five bullets about the withdrawal deadline and the 5:20 appointment on 09-01 16:30, 09-01 20:49, 09-02 21:47). Fix: near-duplicate suppression at render in `get_summaries` (bullet-overlap or cosine ≥0.9 against an already-selected summary → skip, backfill the next), keeping the newest; do not delete stored summaries.
- Observed, lower priority: `temperature=mid 80s` stored as a CAREER profile fact (weather figure, 08-29) and `hallucination=…` filed under goals — `category_cache.json` poisoning class; the 10-memory [RELEVANT MEMORIES] slice on the joke included a 2025-10 8.5K-char rant with no relation to the query (middle-out snipped) — the query's only content word was "Fable"; the planner and STM both read "Fable" as the responder rather than the addressee (harmless here).

## Added 2026-09-04 ~12:00 — the re-sent evidence sweep (insight-assembly, theme_sweep) routed correctly but under-recalled

Verified in the dump + code: `core/insight/sweep.py:283/299` sort evidence newest-first and `core/insight/provenance.py:134` truncates at `max_evidence_chars=12000` — 77 items assembled, the model saw the newest 37, all dated 09-02 → 09-04, for a request spanning 2026-07-15 → today (digest-order class, 5th occurrence; the 08-31 `_interleave_phase_events` fix covered only the deliberation path — situation-coverage class). Seven of the 37 were the request itself or the reply about it (no self-exclusion in `sweep.py`). Facet keywords are ≤6 single words (`facets.py:81/141`), so the user's quoted cue phrases were never scanned. A pasted registrar email inside a user turn rendered as "you said" and was counted as the user correcting the assistant. Known corrections the sweep missed: 07-28 "No I mean of being sick", 07-25 pain-duration, 08-05 timezone, 08-23 "6 weeks off … not 1", 08-24 day-8 framing. A sonnet agent is implementing: window-fair rendering, self-exclusion, quoted-phrase corpus scans, ISO date-window enforcement, third-party-quote labeling (`tests/unit/test_insight_sweep_coverage.py`). Data note: the 10:59 request was mined into a junk fact `user | corrected_fact | date` in the `facts` collection (E2) — extractor hole on request-shaped messages; add to the junk-fact review.

**~12:20 — sweep fixes LANDED (uncommitted):** `core/insight/sweep.py` (`interleave_evidence_for_coverage`, `exclude_current_request_evidence`, `_quoted_phrase_scan`, `filter_evidence_by_date_window`), `provenance.py` (date-range line + per-week omission note, `_split_quoted_correspondence`), `facets.py` (`extract_quoted_phrases`), `detector.py` (`parse_date_window`), `types.py` (`InsightIntent.date_window`), `synthesizer.py` (attribution rule), `gui/handlers.py` insight region. Tests: `tests/unit/test_insight_sweep_coverage.py` (27) + 152 existing = 179 green. Theme-sweep only; deliberation path untouched. Daemon restart REQUIRED (running instance predates these edits).

**~12:35 — sweep round 2 (agent running):** live re-run after restart renders coverage honestly ("spans 08-24 → 09-04, 36 of 79 considered; omitted by week") but the 79 assembled items contain nothing before 08-24 although `data/corpus_v4.json` has 8 user-side "no I mean" turns in the window (07-20, 07-28, 08-02, 08-18, 09-02, 09-04) and 3 "I meant". Phrase hits are lost between `corpus_manager.search_keyword(phrases, max_results=keyword_scan_max)` and the render (suspect: newest-first cap filled by the 09-04 request turns, which contain every cue phrase; or AND semantics). Also yesterday's near-identical request turns still surface as evidence (below the 60% overlap bar). Agent is fixing at retrieval: per-week caps on the corpus phrase scan, OR semantics, request repeats excluded before capping, a content-date range arm for conversations/summaries when an explicit window exists (reuse `_window_scan_collection`), and same-day ≥30% overlap exclusion.

**~12:55 — sweep round 2 LANDED (uncommitted; 198 tests green, 38 in `test_insight_sweep_coverage.py`).** Real cause (verified by a read-only harness on a corpus copy): `core/insight/sweep.py::_finalize` applied a per-collection proportional quota, and every corpus hit — facet keyword scans AND the quoted-phrase scan — carries `collection="corpus"`, so one bucket took the whole 80-item cap and `group[:quota]` newest-first kept only the 09-0x request turns; the 07-20/07-28/08-02/08-18 hits were retrieved and then dropped. Fixes: window-fair slice per collection when a date window exists; quoted-phrase scan scans wide, bounds by window, excludes request repeats BEFORE capping, week-fair selects; new `_date_range_scan` for conversations/summaries via shared `window_scan_collection()` (moved out of `gui/handlers.py`, which delegates); same-day ≥30% overlap exclusion wired with `current_turn_date`. Restart required.

**~13:20 — sweep round 3 LANDED + class-closing batch DISPATCHED (uncommitted).** Round 3: prior insight/doc-gen/self-note outputs are never evidence (`core/insight/sweep.py` `_SWEEP_OUTPUT_MODES` at every collection site + scaffolding-regex backstop; 4 tests); SPA Debug/Provenance show all session records by default with a "since page load" toggle; `web/dist` rebuilt (new chunk hashes, no restart needed for the SPA). Live sweep after round 2 found 5 real corrections (07-22, 07-28, 08-22, 08-23, 09-03) with honest per-week omissions. Three sonnet agents now running the class-closers: (1) grounding verifier LOG-ONLY mode default + telemetry verdict, and a deterministic narrative status-claim check (`withdrew from the semester` vs current `enrolled_in`); (2) `utils/trigger_match.py` — one word-boundary + negation-window matcher adopted at gate lists, email arms, action intent patterns, visual gate, web-trigger arms; (3) single post-response hook registry for GUI and `process_user_query` paths + a mode parity test (every mode's final chunk carries `debug`), and `utils/ordered_slice.py` sort-before-slice helper + source-level guard. Owner: restart after they land; commit as one batch.

**~14:00 — class-closing batch LANDED (uncommitted).** (1) `grounding_check.mode: log_only` default (`GROUNDING_MODE`, telemetry `grounding_mode`/`grounding_verdict`), tests `test_grounding_log_only.py`; (2) `utils/status_claims.py` wired into `memory/memory_consolidator.py` narrative + `utils/daily_notes_generator.py`, tests `test_narrative_status_claims.py`; (3) `utils/trigger_match.py` adopted at gate lists/email arms/`registry.detect_action_intent`/visual gate/web explicit phrases, tests `test_trigger_match.py` (43); NOT adopted by design: `_is_request_shaped`, `_ACTION_DISAVOWAL_RE`, probabilistic recency/news keyword adders; (4) `core/orchestrator.py` `POST_RESPONSE_HOOKS` + `run_post_response_hooks()` used by both `handlers._write_turn_telemetry` and `process_user_query` (CLI + raw mode still use it — do not delete), tests `test_request_path_parity.py` (14, incl. 8-mode debug-record sweep); fixed a real crash on the pending-proposal lightweight ctx; (5) `utils/ordered_slice.py` adopted at controller digest, LLM extractor budget walk, floor top-ups, gatherer_memory splits, summarizer (2 real bugs), sweep, handlers interleave; guard `test_ordered_slice_guard.py` with a 24-entry allowlist. Combined ~2,400 targeted tests green. Owner: restart, then commit everything from today as one batch (or two: morning fixes / class closures).

## Next session starts here (close-out pass, evening)

A close-out agent did the diff review / test batch / doc sweep / privacy grep
described at the top of this file. Two small bugs found in review were fixed
in place (not new work, just cleanup of the batch above): an unreachable
`return interleaved` line left over in `core/insight/sweep.py::interleave_evidence_for_coverage`
from before it delegated to `utils.ordered_slice`, and an unused
`NEGATION_CUE_RE` import in `knowledge/document_generator.py` left over from
moving the negation regex into `utils/trigger_match.py`. `docs/generalization/12-response-integrity.md`
and `13-fable-midflight-handoff.md` got dated status notes (not rewrites) —
retrospective §6B item 18 is done. `CLAUDE.md`/`CLAUDE_CHANGELOG.md`/`docs/WEB_FRONTEND.md`/
`config/config.yaml`'s grounding_check comment were updated to match the code;
`docs/METRICS_SNAPSHOT.md` + `README.md` regenerated (805 files, 8,479 tests
across 395 files). Combined targeted test run: 863 passed, 0 failed. Full
report: the close-out agent's summary in this session's transcript.

**Data queue** — unchanged since this morning, all still owner-gated with the
daemon down; see retrospective §6A for the full table (syllabus junk facts,
the 08-28 stored narration turn, a misgendering reflection, 24 orphan graph
nodes, 176 course-transcript reference_docs chunks, the curation queue, the
adaptive-exemplar purge, an academic-advisor fact, intent labeling,
cross-dedup queue, duplicate corpus pairs, six raw-prompt debug logs, the
114GB snapshot/mirrors/old Actions runs).

**Two new observations from the last live sweep re-runs today, not yet
fixed:**
- `provenance.render_evidence_block` renders items in whatever order
  `interleave_evidence_for_coverage` / the caller hands it, and `threads`
  collection items carry no reliable per-item date in the same way
  conversations/summaries do — in a live sweep, undated `threads` rows
  clustered near the front of the rendering regardless of how old the thread
  actually was, crowding out dated evidence. Worth ordering dated items
  first (as today's window-fair reorder already does within dated items) and
  capping how many undated items can appear in one render, rather than
  appending all of them.
- The synthesizer's ATTRIBUTION rule (item 5, round 3, this file) says only
  the user's own first-person words correcting the assistant count as "you
  corrected me" — but a live run still counted a resent/duplicate message and
  a passing "on my way" schedule update as corrections. The rule as written
  guards against THIRD-PARTY and SELF-correction misattribution; it doesn't
  yet guard against a same-content resend or an unrelated status update being
  classified as a correction at all. Either tighten the synthesizer's
  ATTRIBUTION rule with an explicit "a resend or an unrelated update is not a
  correction" clause, or add a deterministic pre-filter (a "correction-shaped"
  check — contrastive markers like "no I mean"/"actually"/"that's wrong" vs.
  a bare repeat or a status update with no contrastive marker) before an item
  is offered to the synthesizer as a correction candidate.

**Retrospective §7 P1/P2 items still open** (none touched this session beyond
P2#18 above):
- P1 #7: record the Tier-4 LLM web-trigger's reason + search terms in
  telemetry, then measure precision of the 27% of agentic turns it launches
  (N6); consider a fast-model decision call (the 08-31 planner ladder
  pattern) to cut both the 75s timeouts and the 40-60s decision rounds.
- P1 #8: prepare-latency budget for the insight era (N8) — the planner runs
  serially after gather now; LLM-compress calls still pay a flat 3s timeout
  on git commits every turn.
- P1 #9: meter `google_calendar` before memories in `PRIORITY_ORDER`, or give
  dated sections a survival floor (N10) — a working calendar fetch was
  trimmed at 9984/10000 on 09-01.
- P1 #10: per-item render cap or middle-out for `[RECENT CONVERSATION]`
  history items so a giant paste stops costing 20K tokens/turn for the next
  ten turns.
- P1 #11: intent coverage (N7) — run the auto-labeler once, look at what
  `general@0.00` actually contains, widen the regex tier or lower the
  semantic tier's 0.45 floor with a probe run.
- P1 #12: graduate the deterministic curators (temporal staleness, error
  sentinels, stream artifacts) from queue to auto after one reviewed run.
- P1 #13: finish the WARNING→DEBUG demotion for the four leftover signatures
  (N11) and add a "no user text at WARNING" rule to a test.
- P1 #14: prompt cache — find the sonnet-4.5 call path with 0/24 hits and
  check its breakpoint; check whether kimi's 20% hit rate is the per-turn
  tail moving.
- P1 #15: OpenRouter-balance desktop alert (same pattern as
  `daemon-backup-failed.service`).
- P2 #16: convert the seven `return True/False` test functions to
  assertions; fix or delete the two files failing collection on
  `tests.integration.gate_system_helpers`.
- P2 #17: strip paste chrome (`▎`, "First N Lines", fenced headings) from
  topic-manager input (N12).
- P2 #19: run the owner-gated data queue in §6A in one daemon-down session.
- P2 #20: owner voice decision on the Examples-block system-prompt trim
  (`docs/SYSTEM_PROMPT_TRIM_PROPOSAL.md` lever 2).
