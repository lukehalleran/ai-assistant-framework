retrieval breakthrough: BGE embedding migration + cross-encoder reranking

Overall MRR 0.626 → 0.915 (+46%), R@1 0.43 → 0.81, all 96 benchmarks passing.

Changes:
- Migrate embedding model from all-MiniLM-L6-v2 to BAAI/bge-small-en-v1.5
- Add cross-encoder post-scoring reranker (ms-marco-MiniLM-L-6-v2, top-15)
  Blends 60% multi-factor score + 40% cross-encoder pair score
- Timeline mode weights for anchorless TEMPORAL_RECALL (relevance=0.35,
  recency=0.05, continuity=0.40) + summary bonus (+0.15)
- Temporal window reranking for retrospective queries (yesterday/last night)
- Skip meta-conversational bypass for retrospective temporal queries
- Query reformulation infrastructure (conservative, 3-word minimum)
- Fix thinking block leak: detect all-thinking responses with no answer
- Expand heuristic patterns for DeepSeek V4 thinking style
- Fix _gate_memories type forwarding (episodic bypass was broken)
- Add per-case CSV output and aggregate metrics to benchmark harness
- Daily backup script + systemd timer
- Embedding migration script (side-by-side, ID-preserving, with manifest)

Per-intent MRR gains:
  technical_help:       0.717 → 1.000
  creative_exploration: 0.362 → 0.938
  project_work:         0.692 → 0.950
  factual_recall:       0.672 → 0.932
  emotional_support:    0.634 → 0.912
  temporal_recall:      0.591 → 0.854
  meta_conversational:  0.211 → 0.400
