fix: Streaming thinking leak, agentic response pop-back, multi-term web search, broad news detection, silent retry gates

Thinking Leak Fix:
- response_parser: added likely_untagged_thinking() — fast streaming-time check
  for untagged chain-of-thought (≥2 heuristic pattern hits, no split required)
- handlers.py streaming loop: suppresses untagged thinking during streaming
  with 300-char bail-out to prevent false-positive response suppression

Agentic Response Pop-Back Fix:
- handlers.py agentic loop: skip ProgressEvents after response chunks start
  accumulating — prevents "done" event from overwriting streamed response
- Removed redundant pre-debug yield after agentic loop

Multi-Term Web Search Fix:
- web_search_manager: multi_search() accepts sub_queries param for pre-computed
  sub-queries, skips decomposition and goes straight to parallel search
- context_gatherer + agentic/controller: pass ALL LLM search terms (was first only)

Broad News Detection Improvements:
- _is_broad_news_query(): added keyword, fixed entity check to mid-sentence only
- Added _semantic_broad_news_check(): embedding fallback (5 news vs 4 non-news anchors)

Silent Retry Gates (uncertainty fallback + review gate):
- Retries run silently — no progress messages or chunk streaming to UI
- Similarity guard: retry only accepted if word overlap with original < 70%
- Review gate threshold raised 0.80 → 0.90, short responses (<120 chars) skip review
- Prevents showing same/similar response twice after unnecessary retry

Stale Test Fixes:
- test_context_pipeline: 3 tests (update_from_user_input removed in cost reduction)
- test_user_profile: 3 tests (sleep_quality → HEALTH category)
- test_citation_system: _web_source_map on bare orchestrator mocks
- test_agentic_search: compress_results mock for [WEB_N] formatting
- New: 23 tests in test_thinking_heuristic.py (likely_untagged_thinking + bail-out)

Tests: 3248 passed, 5 wizard-only failures (pre-existing)
