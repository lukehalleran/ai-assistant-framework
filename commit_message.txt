feat: Add LLM-based memory compression and rewrite README

- Two-tier compression: items ≥3x over token limit get LLM summary
  via async _llm_compress_oversized() pre-pass in builder.py;
  mildly oversized items (1x-3x) still use middle-out character slicing
- Parallel batch compression via asyncio.gather() with per-call timeout
- Graceful fallback: timeout/error/empty response leaves item unchanged
- Config: llm_compression section in config.yaml + LLM_COMPRESSION_*
  constants in app_config.py (enabled, model, timeout, ratio, max_batch)
- README.md: comprehensive rewrite with corrected stats (40K token budget,
  180+ config options, 103K lines, 2850+ tests), added LLM compression
  docs, fixed scoring weights and config key names
- Updated module contracts in builder.py, token_manager.py, app_config.py
- 16 unit tests in test_llm_compression.py, all passing
