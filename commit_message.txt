feat: 3-layer agent safety guards + daily notes auto-update + LaTeX fix

Shell command guard (utils/shell_cmd_guard.py):
- Python classifier for rm, mv, rmdir, chmod, truncate, find against
  protected paths (16 dirs, 6 files, 5 always-blocked targets)
- scripts/safe_cmd.sh bash wrapper with same unlock mechanism as safe_git.sh
- scripts/bin/ PATH wrappers (rm, mv, rmdir, chmod, truncate, find) that
  transparently intercept commands and route through safe_cmd.sh
- scripts/activate_guards.sh sources into shell to prepend PATH
- Added to ~/.bashrc for automatic activation in all sessions
- 127 tests

Python filesystem guard (utils/python_fs_guard.py):
- Monkey-patches os.remove, os.unlink, os.rmdir, os.rename, os.replace,
  shutil.rmtree, shutil.move at startup (main.py)
- ContextVar-based agent_mode() — only blocks during agentic tool dispatch;
  Daemon's own runtime (profile saves, daily notes, migrations) is unguarded
- Checks both source AND destination for rename/replace/move operations
- Corrected check ordering: always-blocked targets checked before
  outside-repo passthrough (prevents "/" from slipping through)
- Preserves *args/**kwargs for platform compatibility (dir_fd, etc.)
- Integrated into core/agentic/controller.py via _dispatch_single wrapper
- 74 tests

Daily notes auto-update (utils/daily_notes_generator.py):
- Regenerates existing daily note when conversation count grows by >=
  DAILY_NOTES_UPDATE_MIN_NEW (default 3), fixing stale notes from
  early-session shutdowns
- New methods: _find_existing_note_path, _get_existing_conversation_count,
  _should_auto_update
- New config constant: DAILY_NOTES_UPDATE_MIN_NEW
- 23 tests

Other:
- memory/memory_consolidator.py: Added temporal accuracy rules to narrative
  synthesis prompt (date attribution from frontmatter, multi-day ranges)
- gui/launch.py: Removed single-$ LaTeX delimiter from Chatbot — was
  treating currency $signs as math mode, garbling financial responses
- Updated docs: AGENT_SAFETY.md, CLAUDE.md, QUICK_REFERENCE.md,
  PROJECT_SKELETON.md, ARCHITECTURE_GUIDE.md, AGENTIC_SEARCH.md
