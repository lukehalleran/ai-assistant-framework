feat: persistent sandbox, LaTeX, graph cleanup, wiki enrichment gating, thinking leak hardening

Persistent sandbox session:
- E2B sandbox session now survives across agentic runs within a conversation
  (variables, dataframes, imports carry over between messages)
- Session held on AgenticSearchController with lazy creation + 10min idle timeout
- Shutdown cleanup wired into main.py _do_shutdown()
- System prompt tells LLM to reuse existing sandbox variables

LaTeX rendering:
- Enabled $...$ and $$...$$ plus \(...\) and \[...\] in Gradio chatbot

Knowledge graph cleanup:
- Removed 593 junk nodes (pronouns, stopwords, timestamps, measurements,
  wiki orphan clusters) and 930 dangling edges. Graph: 1729→1136 nodes.
- Cleaned 14,355 stale aliases (14772→417)
- Deleted 341 junk encyclopedia facts from ChromaDB (2849→2508 facts)
  Backup at data/facts_junk_backup_20260518_203815.json

Wiki enrichment gating (prevents future junk):
- Gate 1: Only create wiki nodes if article title matches a conversation entity
- Gate 2: Only link to entities from conversations (not other wiki nodes)
- Bridge mode: when 2+ conversation entities co-occur in wiki text, create
  direct bridge edges between them (wiki article = evidence, not entity)
- Junk entity filter applied to all extraction results

Graph entity junk prevention:
- New shared is_junk_entity() in graph_utils.py (pronouns, stopwords,
  quantifiers, adjectives, meta-words, short tokens, numbers)
- Applied at: ingestion (subjects + objects), extraction, expansion, wiki enrichment
- Subjects now filtered during graph ingestion (was objects-only)

Thinking block leak hardening:
- Added 9 new heuristic patterns (5 sentence-level + 4 line-level):
  sandbox failure reasoning, environment diagnosis, mid-sentence "I'll try/skip",
  cost-aware reasoning, previous search failure, round counting
- 19 new tests (7 real-world leak regressions, 7 false positive prevention,
  5 tag edge cases). Total thinking tests: 27→46.
- DeepSeek v4 now gets API-level reasoning separation (supports_reasoning()
  extended to match deepseek-v4, not just deepseek-r1)

Env fix: upgraded python-dateutil 2.6.1→2.9.0 (was blocking e2b SDK import)
