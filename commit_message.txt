feat: Wikidata subgraph import, graph walk generator, wiki-to-graph enrichment

Wiki Enrichment (Feature 2):
- WikiArticleTracker: session-level singleton tracking wiki titles from FAISS/ChromaDB
- WikiGraphEnricher: shutdown step 6.9 creates graph nodes (source=wiki_retrieved)
  with "mentioned_alongside" edges at weight 0.5 (distinct from fact-extracted 1.0)
- Integration: 2-3 line tracker calls in context_gatherer.py and controller.py

Wikidata Import (Feature 1):
- extract_wikidata.py: SPARQL extraction with alias expansion + descriptions
  across 10 domain categories (30K entities, 39K aliases, 7K relations)
- import_wikidata_to_graph.py: imports cache into NetworkX graph with bulk_import()
- WikidataEntityMapper: 3-strategy entity resolution — exact alias, description-
  enriched embedding (threshold 0.60 IVFPQ-corrected), category-aware filtering
- Bridge confidence scoring (1.0 exact, 0.9 high-sim, 0.7 low-sim)

Graph Walk Generator:
- GraphWalkGenerator: biased Markov walks across personal→wikidata boundary
- Node2Vec-style return bias (2.0x toward personal nodes in wikidata territory)
- Walk narration prompt gives LLM the full path to interpret, not just endpoints
- Shutdown: tries walks first (bridges >= 40), fills with SynthesisGenerator fallback

Infrastructure:
- GraphNode: source, is_wikidata, wikidata_qid properties
- GraphMemory: count_by_source(), count_bridge_edges(), bulk_import() context manager
- orjson serialization with stdlib json fallback
- WikidataEntity/Relation/ImportResult Pydantic models
- ~35 config constants across wikidata_import, graph_walk, wiki_enrichment YAML sections

Validation:
- Walk prompt scored MODERATE+ 7/8 vs random pairing 5/8 on coherence
- 30K nodes, 7K edges, 41 bridge edges in production graph
- 94 new tests (10 tracker + 18 enrichment + 38 walk generator + 28 prior), all passing
