feat: internet actions system + agentic gate extraction

Internet actions (core/actions/): propose→confirm→execute flow for
write actions on external services. The agentic loop proposes actions
via propose_action tool (19th agentic tool); GUI shows approve/reject
buttons; on approval, ActionExecutorRegistry routes to type-specific
executor. All actions logged to logs/actions_audit.jsonl.

Implemented executors: Telegram (Bot API via httpx), Discord (webhook
via httpx), Email (stdlib SMTP in thread). Stubs: github_create_issue,
github_comment_pr, calendar_create_event.

New files:
- core/actions/ (6 files): types.py (ActionType enum, ActionProposal,
  ActionResult, PendingActionsStore), executors.py, telegram.py,
  discord.py, email.py, audit.py
- core/agentic/gate.py: extracted 4-tier agentic gate from
  gui/handlers.py (AgenticDecision + evaluate_agentic_gate)
- tests/unit/test_actions.py, test_agentic_gate.py, test_handle_submit.py

Agentic changes:
- SearchDecision extended: wants_action, action_type, action_params,
  action_summary, action_reason fields
- PROPOSE_ACTION_TOOL_DEFINITION in types.py
- NativeToolsHandler + XMLMarkerHandler both parse propose_action
  (including _parse_text_tool_calls fallback for text-embedded proposals)
- controller.py: dispatches action proposals before honoring done signal;
  injects [AVAILABLE ACTIONS] instructions when internet actions enabled

GUI changes:
- gui/launch.py: action_row (approve/reject buttons), pending_action_id
  State, submit_chat now yields 12 outputs (was 10)
- gui/handlers.py: gate logic replaced by evaluate_agentic_gate() call
  (265 lines → 15-line call site); new execute_pending_action() and
  reject_pending_action() handlers; text action parsing from final
  response in both agentic and enhanced paths

Config:
- internet_actions YAML section (config.yaml)
- InternetActionsSection Pydantic model (schema.py)
- INTERNET_ACTIONS_* constants (app_config.py)
- Active model changed: deepseek-v4 → claude-opus-4.6

Other changes:
- core/prompt/formatter.py: compute relative commit age from ISO
  timestamp at render time (fixes stale age_relative snapshots)
- knowledge/obsidian_manager.py: use _get_collection() instead of
  direct .collections dict access (API consistency)
- data/benchmark_per_case.csv: minor ranking order changes
- test_corpus.json: timestamp refresh
- docs: updated CLAUDE.md, AGENTIC_SEARCH.md, ARCHITECTURE_GUIDE.md,
  PROJECT_SKELETON.md, GOALS.md, FORMAL_MODEL.md, QUICK_REFERENCE.md
