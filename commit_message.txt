refactor: Extract AgenticFormatter + ToolExecutor from controller, fix thread deadline expiry

Agentic controller decomposition (Phase 1 of god-object cleanup):
- Extract AgenticFormatter (core/agentic/formatters.py, 256 lines)
  17 pure stateless formatting methods for all result types
- Extract ToolExecutor (core/agentic/tools.py, 856 lines)
  Dispatch routing + execution for 10 tool types (web, wolfram,
  sandbox, memory, files, git stats, full document)
- Controller retains orchestration loop, prompt building, model
  interaction, quality heuristics, and delegation wrappers
- Controller: 2,189 → 1,243 lines

Thread deadline expiry fix:
- is_stale() now checks deadline_date + deadline_grace_hours (48h)
  before falling back to time-since-reference check (14 days)
- get_top_threads() passes deadline_grace_hours through to is_stale()
- Previously deadline_grace_hours was loaded from config but never used

Ephemeral appointment facts:
- Added appointment_time, appointment, appointment_date, plans_today,
  plans_tonight, schedule_today to EPHEMERAL_RELATIONS (24h TTL)
- Prevents one-off schedule mentions from persisting as permanent facts
