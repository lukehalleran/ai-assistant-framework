feat: Pydantic config validation, entity-gated visual memory, retrieval scoring fixes

Three changes:

1. Pydantic config schema validation (config/schema.py)
   - 44 section-level models with range validators, cross-field checks, type coercion
   - Fails fast at startup with actionable errors (field path + expected + actual)
   - All existing imports unchanged; validates after YAML load, before constant extraction
   - 44 unit tests

2. Entity-gated visual memory retrieval
   - Images only retrieved when query mentions an entity with stored photos
   - Prevents irrelevant images from leaking into unrelated conversations
   - Profile-aware backfill script (scripts/backfill_visual_entities.py) re-captions
     images with entity context so vision LLM outputs proper names
   - Fixed MIME type detection (detect from bytes, not metadata) fixing API 400 errors
   - Updated [VISUAL MEMORIES] instruction to tell model to ignore off-topic images

3. Memory retrieval scoring fix (CASUAL_SOCIAL)
   - Rebalanced weights: relevance 0.20->0.35, recency 0.35->0.20
   - Wired gate_threshold_override from intent classifier to actual gate system (was dead code)
   - Fixes: recent but irrelevant memories no longer outrank topically relevant ones
