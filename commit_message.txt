refactor: extract handler helpers + fix GUI streaming + optimize prompt build latency

Three areas of work in one session, all behavior-preserving:

GUI Bug Fix (gui/launch.py):
- Fix intermittent bug where response generates but GUI stays at loading spinner
- Force yield for final chunks carrying debug records (bypass throttle gate)
- Cancel pending tick tasks on loop break to prevent asyncio interference
- Catch asyncio.CancelledError (BaseException in Python 3.9+, was uncaught)
- Safety net: recover response from debug record if placeholder stays stuck

Complexity Reduction (gui/handlers.py, core/prompt/formatter.py):
- Extract 10 helpers from handle_submit (1541→1200 LOC, 22% reduction):
  _safe_count_tokens, _safe_extract_citations, _build_debug_record,
  _build_provenance, _attach_agentic_provenance, _sanitize_response_text,
  _strip_echoed_headers, _dispatch_storage, _silent_agentic_retry,
  _get_session_id — each consolidates a pattern repeated 2-4x across
  the 6 mode paths (raw/duel/agentic/enhanced/uncertainty/review)
- Extract _format_summary_section() in formatter.py for 4 identical
  prompt sections (summaries/reflections), _assemble_prompt 818→768 LOC

Latency Optimizations (~11-15s saved on first message):
- Skip LLM web search trigger when heuristic is decisive (conf=0.0 or >=0.7)
  Saves ~2-5s on queries that clearly don't need web search
- Skip LLM compression for items >6x target tokens, go straight to middle-out
  Saves ~2-3s timeout on large items (e.g. long emails)
- Set HF_HUB_OFFLINE=1 to skip HuggingFace HTTP HEAD checks on model load (~1s)
- Make proactive insights non-blocking: fire background warmup on first message,
  use session cache from message 2+ (~6s off critical path)

Benchmarks: 305/305 passed at each step, MRR=0.8865 unchanged
Tests: 2923 passed, 0 failures
