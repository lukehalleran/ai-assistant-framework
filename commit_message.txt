refactor: Decompose 3 god objects (Phases 2-3), add behavioral pattern extraction

Phase 2A — Move _assemble_prompt + helpers to formatter.py:
- _assemble_prompt (737 lines), _build_feature_inventory, _staleness_prefix,
  _is_multimodal_model, _load_upload_image moved to PromptFormatter
- Old simpler _assemble_prompt renamed to _assemble_prompt_legacy
- builder.py: 2,751 → 1,583 lines; formatter.py: 672 → 1,597 lines

Phase 2B — Extract ContentHygiene (core/prompt/hygiene.py, 345 lines):
- _hygiene_and_caps + _backfill_recent_conversations
- Dedup, caps enforcement, cross-section dedup, semantic chunk stitching

Phase 3 — Split ContextGatherer into domain mixins:
- gatherer_web.py (216 lines) — WebSearchMixin: web trigger + retrieval
- gatherer_memory.py (834 lines) — MemoryRetrievalMixin: 17 methods
- gatherer_knowledge.py (963 lines) — KnowledgeRetrievalMixin: 16 methods
- context_gatherer.py: 2,176 → 379 lines (init + properties + utilities)

Behavioral pattern extraction (shutdown step 4.5):
- New _extract_behavioral_patterns() detects cross-domain habits the user
  exhibits but never states explicitly (e.g., codes at the gym)
- Single gpt-4o-mini call per session, ~200 tokens output, max 3 patterns
- Checks existing profile to avoid duplicating known habits
- Config: behavioral_patterns.enabled in config.yaml

All delegation wrappers preserve backward compatibility. No behavior changes.
