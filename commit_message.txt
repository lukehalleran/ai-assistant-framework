feat: Phase 8 intent-conditioned section gating (eval-driven prompt optimization)

Phase 8 — Intent-Conditioned Section Gating:
- intent_classifier.py: Updated _PROFILES retrieval counts based on eval data
  (50 snapshots, 1799 pairs, two judge models). New retrieval override keys:
  max_reference_docs, max_narrative, max_user_uploads, max_proactive.
  CASUAL_SOCIAL gates 4 additional sections to 0 (~10K token savings).
  TECHNICAL_HELP drops reflections. PROJECT_WORK reduces recent_conversation.
  META_CONVERSATIONAL reduces memories 20→5, drops reflections.
  Config flag PROMPT_SECTION_GATING_ENABLED strips Phase 8 keys when disabled.
- builder.py: Added 4 new eff_max_* variables + max_narrative gate on
  narrative_state. Parallel task launches use > 0 guards so max_X=0 skips
  the async retrieval task entirely.
- app_config.py + config.yaml: PROMPT_SECTION_GATING_ENABLED flag (default True),
  env var override supported.

Docs updated: PLAN.md, PROMPT_BUILDING_PIPELINE.md, QUICK_REFERENCE.md,
ARCHITECTURE_GUIDE.md, PROJECT_SKELETON.md.

Tests: 74 intent classifier + 246 eval = 320 passing, zero regressions.
