# --- JUNK TO IGNORE ---
mutants/
/test_corpus.json
stitched.txt
stitched_project.txt
*DEBUG.txt
*.backup
trace_clean.py
debug_spacy_parse.py
demo_mutation_testing.py
demo_thinking_blocks.py
devtools/
*.kate-swp

# --- LOGS ---
*.log
conversation_logs/
logs/

# --- PYTHON ---
__pycache__/
*.py[cod]
.venv
.pytest_cache/
.coverage
htmlcov/

# --- DATA HANDLING ---
# Ignore all data contents by default
data/*
chroma/
chroma_db/

# --- SECRETS ---
.env
chroma_db_*/
embedded_parquet/
semantic_chunks/
daemon_memory/
project/
.cache/
embeddings_mmap.dat
embed_checkpoint.json

# --- EVAL RUN DATA ---
eval/runs/
eval/snapshots/
eval/phase2_output/
eval/corpus.json
# Benchmark per-case output is a test artifact (written by tests/benchmarks/conftest.py).
# Already covered by data/* above; listed explicitly to document intent.
data/benchmark_per_case.csv

# --- EXCEPTIONS (White-list) ---
!data/pipeline/
!data/scripts/
!data/corpus_v4.json
data/corpus_v4.json
dist/
build/daemon/
CLAUDE.md
build/ 
venv/ 
installer/output/ 
*.spec 
dist/

# --- GENERATED DOCUMENTS ---
documents/reports/
documents/summaries/
documents/index.json

# --- DAEMON SELF-NOTES ---
daemon_notes/*.md
daemon_notes/index.json

# --- AGENT SESSION SAFETY ---
.agent_snapshots/
.agent_allow_destructive_once
RECOVERY_*.txt
RECOVERY_*.patch
RECOVERY_*.tar.gz

# Private archives / backups
*.tar
*.tar.gz
*.tgz
*.zip
*.7z
*.db
*.sqlite
*.sqlite3
*.bak
*.backup

# Local config overrides (personal/sensitive values — never commit)
config.local.yaml
config/config.local.yaml

# Commit-message scratchpad (git log is the real archive) + gh token
commit_message.txt
.gh_token
