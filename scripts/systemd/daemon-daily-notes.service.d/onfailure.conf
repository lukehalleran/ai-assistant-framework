# Drop-in: alert on failure (scripts/daily_note_catchup.py exits 1 on a
# generation error). Install to ~/.config/systemd/user/daemon-daily-notes.service.d/
[Unit]
OnFailure=daemon-daily-notes-failed.service
